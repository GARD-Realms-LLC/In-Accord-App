"use strict";
(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
    get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
  }) : x)(function (x) {
    if (typeof require !== "undefined") return require.apply(this, arguments);
    throw Error('Dynamic require of "' + x + '" is not supported');
  });
  var __esm = (fn, res) => function __init() {
    return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
  };
  var __commonJS = (cb, mod) => function __require2() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = {exports: {}}).exports, mod), mod.exports;
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, {get: all[name], enumerable: true});
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, {get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable});
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {value: mod, enumerable: true}) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", {value: true}), mod);

  // src/common/logger.ts
  var LogTypes, Logger;
  var init_logger = __esm({
    "src/common/logger.ts"() {
      "use strict";
      LogTypes = {
        /** Alias for error */
        err: "error",
        error: "error",
        /** Alias for debug */
        dbg: "debug",
        debug: "debug",
        log: "log",
        warn: "warn",
        info: "info"
      };
      Logger = class _Logger {
        /**
         * Logs an error using a collapsed error group with stacktrace.
         *
         * @param {string} module - Name of the calling module.
         * @param {string} message - Message or error to have logged.
         * @param {any} error - Error object to log with the message.
         */
        static stacktrace(module, message, error) {
          console.error(`%c[${module}]%c ${message}

%c`, "color: #3a71c1; font-weight: 700;", "color: red; font-weight: 700;", "color: red;", error);
        }
        /**
         * Logs using error formatting. For logging an actual error object consider {@link module:Logger.stacktrace}
         *
         * @param {string} module - Name of the calling module.
         * @param {any[]} message - Messages to have logged.
         */
        static err(module, ...message) {
          _Logger._log(module, message, "error");
        }
        /**
         * Alias for "err"
         * @param {string} module NAme of the calling module
         * @param  {...any} message Messages to have logged.
         */
        static error(module, ...message) {
          _Logger._log(module, message, "error");
        }
        /**
         * Logs a warning message.
         *
         * @param {string} module - Name of the calling module.
         * @param {...any} message - Messages to have logged.
         */
        static warn(module, ...message) {
          _Logger._log(module, message, "warn");
        }
        /**
         * Logs an informational message.
         *
         * @param {string} module - Name of the calling module.
         * @param {...any} message - Messages to have logged.
         */
        static info(module, ...message) {
          _Logger._log(module, message, "info");
        }
        /**
         * Logs used for debugging purposes.
         *
         * @param {string} module - Name of the calling module.
         * @param {...any} message - Messages to have logged.
         */
        static debug(module, ...message) {
          _Logger._log(module, message, "debug");
        }
        /**
         * Logs used for basic loggin.
         *
         * @param {string} module - Name of the calling module.
         * @param {...any} message - Messages to have logged.
         */
        static log(module, ...message) {
          _Logger._log(module, message);
        }
        /**
         * Logs strings using different console levels and a module label.
         *
         * @param {string} module - Name of the calling module.
         * @param {any|Array<any>} message - Messages to have logged.
         * @param {module:Logger.LogTypes} type - Type of log to use in console.
         */
        static _log(module, message, type = "log") {
          const parsedType = _Logger.parseType(type);
          if (!Array.isArray(message)) message = [message];
          console[parsedType](`%c[InAccord]%c [${module}]%c`, "color: #3E82E5; font-weight: 700;", "color: #3a71c1;", "", ...message);
        }
        static parseType(type) {
          return LogTypes[type] || "log";
        }
      };
    }
  });

  // src/common/events.ts
  var EventEmitter;
  var init_events = __esm({
    "src/common/events.ts"() {
      "use strict";
      init_logger();
      EventEmitter = class _EventEmitter {
        static get EventEmitter() {
          return _EventEmitter;
        }
        events = {};
        setMaxListeners() {
        }
        on(event, callback) {
          if (!this.events[event]) this.events[event] = /* @__PURE__ */ new Set();
          this.events[event].add(callback);
        }
        emit(event, ...args) {
          if (!this.events[event]) return;
          for (const [index, listener] of this.events[event].entries()) {
            try {
              listener(...args);
            } catch (error) {
              Logger.error("Emitter", `Cannot fire listener for event ${event} at position ${index}:`, error);
            }
          }
        }
        off(event, callback) {
          if (!this.events[event]) return;
          return this.events[event].delete(callback);
        }
      };
    }
  });

  // src/InAccord/polyfill/remote.ts
  var RemoteAPI, remote_default;
  var init_remote = __esm({
    "src/InAccord/polyfill/remote.ts"() {
      "use strict";
      RemoteAPI = window.InAccordPreload();
      remote_default = RemoteAPI;
    }
  });

  // src/InAccord/polyfill/vm.ts
  var compileFunction, vm_default;
  var init_vm = __esm({
    "src/InAccord/polyfill/vm.ts"() {
      "use strict";
      init_remote();
      compileFunction = function (code, params = [], options = {}) {
        const returned = remote_default.vm.compileFunction(code, params, options);
        if (typeof returned === "function") return returned;
        const syntaxError = new SyntaxError(returned.message);
        syntaxError.stack = returned.stack;
        throw syntaxError;
      };
      vm_default = {compileFunction};
    }
  });

  // src/InAccord/polyfill/fs.ts
  var fs_exports = {};
  __export(fs_exports, {
    createWriteStream: () => createWriteStream,
    default: () => fs_default,
    exists: () => exists,
    existsSync: () => existsSync,
    lstat: () => lstat,
    lstatSync: () => lstatSync,
    mkdir: () => mkdir,
    mkdirSync: () => mkdirSync,
    readFile: () => readFile,
    readFileSync: () => readFileSync,
    readdir: () => readdir,
    readdirSync: () => readdirSync,
    realpath: () => realpath,
    realpathSync: () => realpathSync,
    rename: () => rename,
    renameSync: () => renameSync,
    rm: () => rm,
    rmSync: () => rmSync,
    rmdir: () => rmdir,
    rmdirSync: () => rmdirSync,
    stat: () => stat,
    statSync: () => statSync,
    unlink: () => unlink,
    unlinkSync: () => unlinkSync,
    watch: () => watch,
    writeFile: () => writeFile,
    writeFileSync: () => writeFileSync
  });
  var readFileSync, readFile, writeFile, writeFileSync, readdir, readdirSync, mkdir, mkdirSync, rmdir, rmdirSync, rm, rmSync, exists, existsSync, stat, statSync, lstat, lstatSync, rename, renameSync, realpath, realpathSync, watch, createWriteStream, unlinkSync, unlink, fs_default;
  var init_fs = __esm({
    "src/InAccord/polyfill/fs.ts"() {
      "use strict";
      init_remote();
      readFileSync = function (path2, options = "utf8") {
        return remote_default.filesystem.readFile(path2, options);
      };
      readFile = function (path2, options = "utf8", callback) {
        try {
          const contents = remote_default.filesystem.readFile(path2, options);
          callback(null, contents);
        } catch (error) {
          callback(error, null);
        }
      };
      writeFile = function (path2, data, options, callback) {
        if (typeof options === "function") {
          callback = options;
          if (!["object", "string"].includes(typeof options)) options = void 0;
        }
        try {
          remote_default.filesystem.writeFile(path2, data, options);
          callback?.(null);
        } catch (error) {
          callback?.(error);
        }
      };
      writeFileSync = function (path2, data, options) {
        remote_default.filesystem.writeFile(path2, data, options);
      };
      readdir = function (path2, options, callback) {
        try {
          const result = remote_default.filesystem.readDirectory(path2, options);
          callback(null, result);
        } catch (error) {
          callback(error, null);
        }
      };
      readdirSync = function (path2, options) {
        return remote_default.filesystem.readDirectory(path2, options);
      };
      mkdir = function (path2, options, callback) {
        try {
          const result = remote_default.filesystem.createDirectory(path2, options);
          callback(null, result);
        } catch (error) {
          callback(error, null);
        }
      };
      mkdirSync = function (path2, options) {
        remote_default.filesystem.createDirectory(path2, options);
      };
      rmdir = function (path2, options, callback) {
        try {
          const result = remote_default.filesystem.deleteDirectory(path2, options);
          callback(null, result);
        } catch (error) {
          callback(error, null);
        }
      };
      rmdirSync = function (path2, options) {
        remote_default.filesystem.deleteDirectory(path2, options);
      };
      rm = function (path2, callback) {
        try {
          const result = remote_default.filesystem.rm(path2);
          callback(null, result);
        } catch (error) {
          callback(error, null);
        }
      };
      rmSync = function (path2) {
        remote_default.filesystem.rmSync(path2);
      };
      exists = function (path2, callback) {
        try {
          const result = remote_default.filesystem.exists(path2);
          callback(null, result);
        } catch (error) {
          callback(error, null);
        }
      };
      existsSync = function (path2) {
        return remote_default.filesystem.exists(path2);
      };
      stat = function (path2, options, callback) {
        try {
          const result = remote_default.filesystem.getStats(path2, options);
          callback(null, result);
        } catch (error) {
          callback(error);
        }
      };
      statSync = function (path2, options) {
        return remote_default.filesystem.getStats(path2, options);
      };
      lstat = stat;
      lstatSync = statSync;
      rename = function (oldPath, newPath, callback) {
        try {
          const result = remote_default.filesystem.rename(oldPath, newPath);
          callback(null, result);
        } catch (error) {
          callback(error, null);
        }
      };
      renameSync = function (oldPath, newPath) {
        return remote_default.filesystem.renameSync(oldPath, newPath);
      };
      realpath = function (path2, options, callback) {
        try {
          const result = remote_default.filesystem.getStats(path2, options);
          callback(null, result);
        } catch (error) {
          callback(error, null);
        }
      };
      realpathSync = function (path2, options) {
        return remote_default.filesystem.getRealPath(path2, options);
      };
      watch = (path2, options, callback) => {
        return remote_default.filesystem.watch(path2, options, callback);
      };
      createWriteStream = (path2, options) => {
        return remote_default.filesystem.createWriteStream(path2, options);
      };
      unlinkSync = (path2) => remote_default.filesystem.unlinkSync(path2);
      unlink = (path2) => remote_default.filesystem.unlinkSync(path2);
      fs_default = {
        readFile,
        exists,
        existsSync,
        lstat,
        lstatSync,
        mkdir,
        mkdirSync,
        readFileSync,
        readdir,
        readdirSync,
        realpath,
        realpathSync,
        rename,
        renameSync,
        rm,
        rmSync,
        rmdir,
        rmdirSync,
        stat,
        statSync,
        unlink,
        unlinkSync,
        watch,
        writeFile,
        writeFileSync,
        createWriteStream
      };
    }
  });

  // node_modules/base64-js/index.js
  var require_base64_js = __commonJS({
    "node_modules/base64-js/index.js"(exports) {
      "use strict";
      exports.byteLength = byteLength;
      exports.toByteArray = toByteArray;
      exports.fromByteArray = fromByteArray;
      var lookup = [];
      var revLookup = [];
      var Arr = typeof Uint8Array !== "undefined" ? Uint8Array : Array;
      var code = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
      for (i = 0, len = code.length; i < len; ++i) {
        lookup[i] = code[i];
        revLookup[code.charCodeAt(i)] = i;
      }
      var i;
      var len;
      revLookup["-".charCodeAt(0)] = 62;
      revLookup["_".charCodeAt(0)] = 63;
      function getLens(b64) {
        var len2 = b64.length;
        if (len2 % 4 > 0) {
          throw new Error("Invalid string. Length must be a multiple of 4");
        }
        var validLen = b64.indexOf("=");
        if (validLen === -1) validLen = len2;
        var placeHoldersLen = validLen === len2 ? 0 : 4 - validLen % 4;
        return [validLen, placeHoldersLen];
      }
      function byteLength(b64) {
        var lens = getLens(b64);
        var validLen = lens[0];
        var placeHoldersLen = lens[1];
        return (validLen + placeHoldersLen) * 3 / 4 - placeHoldersLen;
      }
      function _byteLength(b64, validLen, placeHoldersLen) {
        return (validLen + placeHoldersLen) * 3 / 4 - placeHoldersLen;
      }
      function toByteArray(b64) {
        var tmp;
        var lens = getLens(b64);
        var validLen = lens[0];
        var placeHoldersLen = lens[1];
        var arr = new Arr(_byteLength(b64, validLen, placeHoldersLen));
        var curByte = 0;
        var len2 = placeHoldersLen > 0 ? validLen - 4 : validLen;
        var i2;
        for (i2 = 0; i2 < len2; i2 += 4) {
          tmp = revLookup[b64.charCodeAt(i2)] << 18 | revLookup[b64.charCodeAt(i2 + 1)] << 12 | revLookup[b64.charCodeAt(i2 + 2)] << 6 | revLookup[b64.charCodeAt(i2 + 3)];
          arr[curByte++] = tmp >> 16 & 255;
          arr[curByte++] = tmp >> 8 & 255;
          arr[curByte++] = tmp & 255;
        }
        if (placeHoldersLen === 2) {
          tmp = revLookup[b64.charCodeAt(i2)] << 2 | revLookup[b64.charCodeAt(i2 + 1)] >> 4;
          arr[curByte++] = tmp & 255;
        }
        if (placeHoldersLen === 1) {
          tmp = revLookup[b64.charCodeAt(i2)] << 10 | revLookup[b64.charCodeAt(i2 + 1)] << 4 | revLookup[b64.charCodeAt(i2 + 2)] >> 2;
          arr[curByte++] = tmp >> 8 & 255;
          arr[curByte++] = tmp & 255;
        }
        return arr;
      }
      function tripletToBase64(num) {
        return lookup[num >> 18 & 63] + lookup[num >> 12 & 63] + lookup[num >> 6 & 63] + lookup[num & 63];
      }
      function encodeChunk(uint8, start, end) {
        var tmp;
        var output = [];
        for (var i2 = start; i2 < end; i2 += 3) {
          tmp = (uint8[i2] << 16 & 16711680) + (uint8[i2 + 1] << 8 & 65280) + (uint8[i2 + 2] & 255);
          output.push(tripletToBase64(tmp));
        }
        return output.join("");
      }
      function fromByteArray(uint8) {
        var tmp;
        var len2 = uint8.length;
        var extraBytes = len2 % 3;
        var parts = [];
        var maxChunkLength = 16383;
        for (var i2 = 0, len22 = len2 - extraBytes; i2 < len22; i2 += maxChunkLength) {
          parts.push(encodeChunk(uint8, i2, i2 + maxChunkLength > len22 ? len22 : i2 + maxChunkLength));
        }
        if (extraBytes === 1) {
          tmp = uint8[len2 - 1];
          parts.push(
            lookup[tmp >> 2] + lookup[tmp << 4 & 63] + "=="
          );
        } else if (extraBytes === 2) {
          tmp = (uint8[len2 - 2] << 8) + uint8[len2 - 1];
          parts.push(
            lookup[tmp >> 10] + lookup[tmp >> 4 & 63] + lookup[tmp << 2 & 63] + "="
          );
        }
        return parts.join("");
      }
    }
  });

  // node_modules/ieee754/index.js
  var require_ieee754 = __commonJS({
    "node_modules/ieee754/index.js"(exports) {
      exports.read = function (buffer, offset, isLE, mLen, nBytes) {
        var e, m;
        var eLen = nBytes * 8 - mLen - 1;
        var eMax = (1 << eLen) - 1;
        var eBias = eMax >> 1;
        var nBits = -7;
        var i = isLE ? nBytes - 1 : 0;
        var d = isLE ? -1 : 1;
        var s = buffer[offset + i];
        i += d;
        e = s & (1 << -nBits) - 1;
        s >>= -nBits;
        nBits += eLen;
        for (; nBits > 0; e = e * 256 + buffer[offset + i], i += d, nBits -= 8) {
        }
        m = e & (1 << -nBits) - 1;
        e >>= -nBits;
        nBits += mLen;
        for (; nBits > 0; m = m * 256 + buffer[offset + i], i += d, nBits -= 8) {
        }
        if (e === 0) {
          e = 1 - eBias;
        } else if (e === eMax) {
          return m ? NaN : (s ? -1 : 1) * Infinity;
        } else {
          m = m + Math.pow(2, mLen);
          e = e - eBias;
        }
        return (s ? -1 : 1) * m * Math.pow(2, e - mLen);
      };
      exports.write = function (buffer, value, offset, isLE, mLen, nBytes) {
        var e, m, c;
        var eLen = nBytes * 8 - mLen - 1;
        var eMax = (1 << eLen) - 1;
        var eBias = eMax >> 1;
        var rt = mLen === 23 ? Math.pow(2, -24) - Math.pow(2, -77) : 0;
        var i = isLE ? 0 : nBytes - 1;
        var d = isLE ? 1 : -1;
        var s = value < 0 || value === 0 && 1 / value < 0 ? 1 : 0;
        value = Math.abs(value);
        if (isNaN(value) || value === Infinity) {
          m = isNaN(value) ? 1 : 0;
          e = eMax;
        } else {
          e = Math.floor(Math.log(value) / Math.LN2);
          if (value * (c = Math.pow(2, -e)) < 1) {
            e--;
            c *= 2;
          }
          if (e + eBias >= 1) {
            value += rt / c;
          } else {
            value += rt * Math.pow(2, 1 - eBias);
          }
          if (value * c >= 2) {
            e++;
            c /= 2;
          }
          if (e + eBias >= eMax) {
            m = 0;
            e = eMax;
          } else if (e + eBias >= 1) {
            m = (value * c - 1) * Math.pow(2, mLen);
            e = e + eBias;
          } else {
            m = value * Math.pow(2, eBias - 1) * Math.pow(2, mLen);
            e = 0;
          }
        }
        for (; mLen >= 8; buffer[offset + i] = m & 255, i += d, m /= 256, mLen -= 8) {
        }
        e = e << mLen | m;
        eLen += mLen;
        for (; eLen > 0; buffer[offset + i] = e & 255, i += d, e /= 256, eLen -= 8) {
        }
        buffer[offset + i - d] |= s * 128;
      };
    }
  });

  // node_modules/buffer/index.js
  var require_buffer = __commonJS({
    "node_modules/buffer/index.js"(exports) {
      "use strict";
      var base64 = require_base64_js();
      var ieee754 = require_ieee754();
      var customInspectSymbol = typeof Symbol === "function" && typeof Symbol["for"] === "function" ? Symbol["for"]("nodejs.util.inspect.custom") : null;
      exports.Buffer = Buffer3;
      exports.SlowBuffer = SlowBuffer;
      exports.INSPECT_MAX_BYTES = 50;
      var K_MAX_LENGTH = 2147483647;
      exports.kMaxLength = K_MAX_LENGTH;
      Buffer3.TYPED_ARRAY_SUPPORT = typedArraySupport();
      if (!Buffer3.TYPED_ARRAY_SUPPORT && typeof console !== "undefined" && typeof console.error === "function") {
        console.error(
          "This browser lacks typed array (Uint8Array) support which is required by `buffer` v5.x. Use `buffer` v4.x if you require old browser support."
        );
      }
      function typedArraySupport() {
        try {
          const arr = new Uint8Array(1);
          const proto = {
            foo: function () {
              return 42;
            }
          };
          Object.setPrototypeOf(proto, Uint8Array.prototype);
          Object.setPrototypeOf(arr, proto);
          return arr.foo() === 42;
        } catch (e) {
          return false;
        }
      }
      Object.defineProperty(Buffer3.prototype, "parent", {
        enumerable: true,
        get: function () {
          if (!Buffer3.isBuffer(this)) return void 0;
          return this.buffer;
        }
      });
      Object.defineProperty(Buffer3.prototype, "offset", {
        enumerable: true,
        get: function () {
          if (!Buffer3.isBuffer(this)) return void 0;
          return this.byteOffset;
        }
      });
      function createBuffer(length) {
        if (length > K_MAX_LENGTH) {
          throw new RangeError('The value "' + length + '" is invalid for option "size"');
        }
        const buf = new Uint8Array(length);
        Object.setPrototypeOf(buf, Buffer3.prototype);
        return buf;
      }
      function Buffer3(arg, encodingOrOffset, length) {
        if (typeof arg === "number") {
          if (typeof encodingOrOffset === "string") {
            throw new TypeError(
              'The "string" argument must be of type string. Received type number'
            );
          }
          return allocUnsafe(arg);
        }
        return from(arg, encodingOrOffset, length);
      }
      Buffer3.poolSize = 8192;
      function from(value, encodingOrOffset, length) {
        if (typeof value === "string") {
          return fromString(value, encodingOrOffset);
        }
        if (ArrayBuffer.isView(value)) {
          return fromArrayView(value);
        }
        if (value == null) {
          throw new TypeError(
            "The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof value
          );
        }
        if (isInstance(value, ArrayBuffer) || value && isInstance(value.buffer, ArrayBuffer)) {
          return fromArrayBuffer(value, encodingOrOffset, length);
        }
        if (typeof SharedArrayBuffer !== "undefined" && (isInstance(value, SharedArrayBuffer) || value && isInstance(value.buffer, SharedArrayBuffer))) {
          return fromArrayBuffer(value, encodingOrOffset, length);
        }
        if (typeof value === "number") {
          throw new TypeError(
            'The "value" argument must not be of type number. Received type number'
          );
        }
        const valueOf = value.valueOf && value.valueOf();
        if (valueOf != null && valueOf !== value) {
          return Buffer3.from(valueOf, encodingOrOffset, length);
        }
        const b = fromObject(value);
        if (b) return b;
        if (typeof Symbol !== "undefined" && Symbol.toPrimitive != null && typeof value[Symbol.toPrimitive] === "function") {
          return Buffer3.from(value[Symbol.toPrimitive]("string"), encodingOrOffset, length);
        }
        throw new TypeError(
          "The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof value
        );
      }
      Buffer3.from = function (value, encodingOrOffset, length) {
        return from(value, encodingOrOffset, length);
      };
      Object.setPrototypeOf(Buffer3.prototype, Uint8Array.prototype);
      Object.setPrototypeOf(Buffer3, Uint8Array);
      function assertSize(size) {
        if (typeof size !== "number") {
          throw new TypeError('"size" argument must be of type number');
        } else if (size < 0) {
          throw new RangeError('The value "' + size + '" is invalid for option "size"');
        }
      }
      function alloc(size, fill, encoding) {
        assertSize(size);
        if (size <= 0) {
          return createBuffer(size);
        }
        if (fill !== void 0) {
          return typeof encoding === "string" ? createBuffer(size).fill(fill, encoding) : createBuffer(size).fill(fill);
        }
        return createBuffer(size);
      }
      Buffer3.alloc = function (size, fill, encoding) {
        return alloc(size, fill, encoding);
      };
      function allocUnsafe(size) {
        assertSize(size);
        return createBuffer(size < 0 ? 0 : checked(size) | 0);
      }
      Buffer3.allocUnsafe = function (size) {
        return allocUnsafe(size);
      };
      Buffer3.allocUnsafeSlow = function (size) {
        return allocUnsafe(size);
      };
      function fromString(string, encoding) {
        if (typeof encoding !== "string" || encoding === "") {
          encoding = "utf8";
        }
        if (!Buffer3.isEncoding(encoding)) {
          throw new TypeError("Unknown encoding: " + encoding);
        }
        const length = byteLength(string, encoding) | 0;
        let buf = createBuffer(length);
        const actual = buf.write(string, encoding);
        if (actual !== length) {
          buf = buf.slice(0, actual);
        }
        return buf;
      }
      function fromArrayLike(array) {
        const length = array.length < 0 ? 0 : checked(array.length) | 0;
        const buf = createBuffer(length);
        for (let i = 0; i < length; i += 1) {
          buf[i] = array[i] & 255;
        }
        return buf;
      }
      function fromArrayView(arrayView) {
        if (isInstance(arrayView, Uint8Array)) {
          const copy = new Uint8Array(arrayView);
          return fromArrayBuffer(copy.buffer, copy.byteOffset, copy.byteLength);
        }
        return fromArrayLike(arrayView);
      }
      function fromArrayBuffer(array, byteOffset, length) {
        if (byteOffset < 0 || array.byteLength < byteOffset) {
          throw new RangeError('"offset" is outside of buffer bounds');
        }
        if (array.byteLength < byteOffset + (length || 0)) {
          throw new RangeError('"length" is outside of buffer bounds');
        }
        let buf;
        if (byteOffset === void 0 && length === void 0) {
          buf = new Uint8Array(array);
        } else if (length === void 0) {
          buf = new Uint8Array(array, byteOffset);
        } else {
          buf = new Uint8Array(array, byteOffset, length);
        }
        Object.setPrototypeOf(buf, Buffer3.prototype);
        return buf;
      }
      function fromObject(obj) {
        if (Buffer3.isBuffer(obj)) {
          const len = checked(obj.length) | 0;
          const buf = createBuffer(len);
          if (buf.length === 0) {
            return buf;
          }
          obj.copy(buf, 0, 0, len);
          return buf;
        }
        if (obj.length !== void 0) {
          if (typeof obj.length !== "number" || numberIsNaN(obj.length)) {
            return createBuffer(0);
          }
          return fromArrayLike(obj);
        }
        if (obj.type === "Buffer" && Array.isArray(obj.data)) {
          return fromArrayLike(obj.data);
        }
      }
      function checked(length) {
        if (length >= K_MAX_LENGTH) {
          throw new RangeError("Attempt to allocate Buffer larger than maximum size: 0x" + K_MAX_LENGTH.toString(16) + " bytes");
        }
        return length | 0;
      }
      function SlowBuffer(length) {
        if (+length != length) {
          length = 0;
        }
        return Buffer3.alloc(+length);
      }
      Buffer3.isBuffer = function isBuffer(b) {
        return b != null && b._isBuffer === true && b !== Buffer3.prototype;
      };
      Buffer3.compare = function compare2(a, b) {
        if (isInstance(a, Uint8Array)) a = Buffer3.from(a, a.offset, a.byteLength);
        if (isInstance(b, Uint8Array)) b = Buffer3.from(b, b.offset, b.byteLength);
        if (!Buffer3.isBuffer(a) || !Buffer3.isBuffer(b)) {
          throw new TypeError(
            'The "buf1", "buf2" arguments must be one of type Buffer or Uint8Array'
          );
        }
        if (a === b) return 0;
        let x = a.length;
        let y = b.length;
        for (let i = 0, len = Math.min(x, y); i < len; ++i) {
          if (a[i] !== b[i]) {
            x = a[i];
            y = b[i];
            break;
          }
        }
        if (x < y) return -1;
        if (y < x) return 1;
        return 0;
      };
      Buffer3.isEncoding = function isEncoding(encoding) {
        switch (String(encoding).toLowerCase()) {
          case "hex":
          case "utf8":
          case "utf-8":
          case "ascii":
          case "latin1":
          case "binary":
          case "base64":
          case "ucs2":
          case "ucs-2":
          case "utf16le":
          case "utf-16le":
            return true;
          default:
            return false;
        }
      };
      Buffer3.concat = function concat(list, length) {
        if (!Array.isArray(list)) {
          throw new TypeError('"list" argument must be an Array of Buffers');
        }
        if (list.length === 0) {
          return Buffer3.alloc(0);
        }
        let i;
        if (length === void 0) {
          length = 0;
          for (i = 0; i < list.length; ++i) {
            length += list[i].length;
          }
        }
        const buffer = Buffer3.allocUnsafe(length);
        let pos = 0;
        for (i = 0; i < list.length; ++i) {
          let buf = list[i];
          if (isInstance(buf, Uint8Array)) {
            if (pos + buf.length > buffer.length) {
              if (!Buffer3.isBuffer(buf)) buf = Buffer3.from(buf);
              buf.copy(buffer, pos);
            } else {
              Uint8Array.prototype.set.call(
                buffer,
                buf,
                pos
              );
            }
          } else if (!Buffer3.isBuffer(buf)) {
            throw new TypeError('"list" argument must be an Array of Buffers');
          } else {
            buf.copy(buffer, pos);
          }
          pos += buf.length;
        }
        return buffer;
      };
      function byteLength(string, encoding) {
        if (Buffer3.isBuffer(string)) {
          return string.length;
        }
        if (ArrayBuffer.isView(string) || isInstance(string, ArrayBuffer)) {
          return string.byteLength;
        }
        if (typeof string !== "string") {
          throw new TypeError(
            'The "string" argument must be one of type string, Buffer, or ArrayBuffer. Received type ' + typeof string
          );
        }
        const len = string.length;
        const mustMatch = arguments.length > 2 && arguments[2] === true;
        if (!mustMatch && len === 0) return 0;
        let loweredCase = false;
        for (; ;) {
          switch (encoding) {
            case "ascii":
            case "latin1":
            case "binary":
              return len;
            case "utf8":
            case "utf-8":
              return utf8ToBytes(string).length;
            case "ucs2":
            case "ucs-2":
            case "utf16le":
            case "utf-16le":
              return len * 2;
            case "hex":
              return len >>> 1;
            case "base64":
              return base64ToBytes(string).length;
            default:
              if (loweredCase) {
                return mustMatch ? -1 : utf8ToBytes(string).length;
              }
              encoding = ("" + encoding).toLowerCase();
              loweredCase = true;
          }
        }
      }
      Buffer3.byteLength = byteLength;
      function slowToString(encoding, start, end) {
        let loweredCase = false;
        if (start === void 0 || start < 0) {
          start = 0;
        }
        if (start > this.length) {
          return "";
        }
        if (end === void 0 || end > this.length) {
          end = this.length;
        }
        if (end <= 0) {
          return "";
        }
        end >>>= 0;
        start >>>= 0;
        if (end <= start) {
          return "";
        }
        if (!encoding) encoding = "utf8";
        while (true) {
          switch (encoding) {
            case "hex":
              return hexSlice(this, start, end);
            case "utf8":
            case "utf-8":
              return utf8Slice(this, start, end);
            case "ascii":
              return asciiSlice(this, start, end);
            case "latin1":
            case "binary":
              return latin1Slice(this, start, end);
            case "base64":
              return base64Slice(this, start, end);
            case "ucs2":
            case "ucs-2":
            case "utf16le":
            case "utf-16le":
              return utf16leSlice(this, start, end);
            default:
              if (loweredCase) throw new TypeError("Unknown encoding: " + encoding);
              encoding = (encoding + "").toLowerCase();
              loweredCase = true;
          }
        }
      }
      Buffer3.prototype._isBuffer = true;
      function swap(b, n, m) {
        const i = b[n];
        b[n] = b[m];
        b[m] = i;
      }
      Buffer3.prototype.swap16 = function swap16() {
        const len = this.length;
        if (len % 2 !== 0) {
          throw new RangeError("Buffer size must be a multiple of 16-bits");
        }
        for (let i = 0; i < len; i += 2) {
          swap(this, i, i + 1);
        }
        return this;
      };
      Buffer3.prototype.swap32 = function swap32() {
        const len = this.length;
        if (len % 4 !== 0) {
          throw new RangeError("Buffer size must be a multiple of 32-bits");
        }
        for (let i = 0; i < len; i += 4) {
          swap(this, i, i + 3);
          swap(this, i + 1, i + 2);
        }
        return this;
      };
      Buffer3.prototype.swap64 = function swap64() {
        const len = this.length;
        if (len % 8 !== 0) {
          throw new RangeError("Buffer size must be a multiple of 64-bits");
        }
        for (let i = 0; i < len; i += 8) {
          swap(this, i, i + 7);
          swap(this, i + 1, i + 6);
          swap(this, i + 2, i + 5);
          swap(this, i + 3, i + 4);
        }
        return this;
      };
      Buffer3.prototype.toString = function toString2() {
        const length = this.length;
        if (length === 0) return "";
        if (arguments.length === 0) return utf8Slice(this, 0, length);
        return slowToString.apply(this, arguments);
      };
      Buffer3.prototype.toLocaleString = Buffer3.prototype.toString;
      Buffer3.prototype.equals = function equals(b) {
        if (!Buffer3.isBuffer(b)) throw new TypeError("Argument must be a Buffer");
        if (this === b) return true;
        return Buffer3.compare(this, b) === 0;
      };
      Buffer3.prototype.inspect = function inspect() {
        let str = "";
        const max = exports.INSPECT_MAX_BYTES;
        str = this.toString("hex", 0, max).replace(/(.{2})/g, "$1 ").trim();
        if (this.length > max) str += " ... ";
        return "<Buffer " + str + ">";
      };
      if (customInspectSymbol) {
        Buffer3.prototype[customInspectSymbol] = Buffer3.prototype.inspect;
      }
      Buffer3.prototype.compare = function compare2(target, start, end, thisStart, thisEnd) {
        if (isInstance(target, Uint8Array)) {
          target = Buffer3.from(target, target.offset, target.byteLength);
        }
        if (!Buffer3.isBuffer(target)) {
          throw new TypeError(
            'The "target" argument must be one of type Buffer or Uint8Array. Received type ' + typeof target
          );
        }
        if (start === void 0) {
          start = 0;
        }
        if (end === void 0) {
          end = target ? target.length : 0;
        }
        if (thisStart === void 0) {
          thisStart = 0;
        }
        if (thisEnd === void 0) {
          thisEnd = this.length;
        }
        if (start < 0 || end > target.length || thisStart < 0 || thisEnd > this.length) {
          throw new RangeError("out of range index");
        }
        if (thisStart >= thisEnd && start >= end) {
          return 0;
        }
        if (thisStart >= thisEnd) {
          return -1;
        }
        if (start >= end) {
          return 1;
        }
        start >>>= 0;
        end >>>= 0;
        thisStart >>>= 0;
        thisEnd >>>= 0;
        if (this === target) return 0;
        let x = thisEnd - thisStart;
        let y = end - start;
        const len = Math.min(x, y);
        const thisCopy = this.slice(thisStart, thisEnd);
        const targetCopy = target.slice(start, end);
        for (let i = 0; i < len; ++i) {
          if (thisCopy[i] !== targetCopy[i]) {
            x = thisCopy[i];
            y = targetCopy[i];
            break;
          }
        }
        if (x < y) return -1;
        if (y < x) return 1;
        return 0;
      };
      function bidirectionalIndexOf(buffer, val, byteOffset, encoding, dir) {
        if (buffer.length === 0) return -1;
        if (typeof byteOffset === "string") {
          encoding = byteOffset;
          byteOffset = 0;
        } else if (byteOffset > 2147483647) {
          byteOffset = 2147483647;
        } else if (byteOffset < -2147483648) {
          byteOffset = -2147483648;
        }
        byteOffset = +byteOffset;
        if (numberIsNaN(byteOffset)) {
          byteOffset = dir ? 0 : buffer.length - 1;
        }
        if (byteOffset < 0) byteOffset = buffer.length + byteOffset;
        if (byteOffset >= buffer.length) {
          if (dir) return -1;
          else byteOffset = buffer.length - 1;
        } else if (byteOffset < 0) {
          if (dir) byteOffset = 0;
          else return -1;
        }
        if (typeof val === "string") {
          val = Buffer3.from(val, encoding);
        }
        if (Buffer3.isBuffer(val)) {
          if (val.length === 0) {
            return -1;
          }
          return arrayIndexOf(buffer, val, byteOffset, encoding, dir);
        } else if (typeof val === "number") {
          val = val & 255;
          if (typeof Uint8Array.prototype.indexOf === "function") {
            if (dir) {
              return Uint8Array.prototype.indexOf.call(buffer, val, byteOffset);
            } else {
              return Uint8Array.prototype.lastIndexOf.call(buffer, val, byteOffset);
            }
          }
          return arrayIndexOf(buffer, [val], byteOffset, encoding, dir);
        }
        throw new TypeError("val must be string, number or Buffer");
      }
      function arrayIndexOf(arr, val, byteOffset, encoding, dir) {
        let indexSize = 1;
        let arrLength = arr.length;
        let valLength = val.length;
        if (encoding !== void 0) {
          encoding = String(encoding).toLowerCase();
          if (encoding === "ucs2" || encoding === "ucs-2" || encoding === "utf16le" || encoding === "utf-16le") {
            if (arr.length < 2 || val.length < 2) {
              return -1;
            }
            indexSize = 2;
            arrLength /= 2;
            valLength /= 2;
            byteOffset /= 2;
          }
        }
        function read(buf, i2) {
          if (indexSize === 1) {
            return buf[i2];
          } else {
            return buf.readUInt16BE(i2 * indexSize);
          }
        }
        let i;
        if (dir) {
          let foundIndex = -1;
          for (i = byteOffset; i < arrLength; i++) {
            if (read(arr, i) === read(val, foundIndex === -1 ? 0 : i - foundIndex)) {
              if (foundIndex === -1) foundIndex = i;
              if (i - foundIndex + 1 === valLength) return foundIndex * indexSize;
            } else {
              if (foundIndex !== -1) i -= i - foundIndex;
              foundIndex = -1;
            }
          }
        } else {
          if (byteOffset + valLength > arrLength) byteOffset = arrLength - valLength;
          for (i = byteOffset; i >= 0; i--) {
            let found = true;
            for (let j = 0; j < valLength; j++) {
              if (read(arr, i + j) !== read(val, j)) {
                found = false;
                break;
              }
            }
            if (found) return i;
          }
        }
        return -1;
      }
      Buffer3.prototype.includes = function includes(val, byteOffset, encoding) {
        return this.indexOf(val, byteOffset, encoding) !== -1;
      };
      Buffer3.prototype.indexOf = function indexOf(val, byteOffset, encoding) {
        return bidirectionalIndexOf(this, val, byteOffset, encoding, true);
      };
      Buffer3.prototype.lastIndexOf = function lastIndexOf(val, byteOffset, encoding) {
        return bidirectionalIndexOf(this, val, byteOffset, encoding, false);
      };
      function hexWrite(buf, string, offset, length) {
        offset = Number(offset) || 0;
        const remaining = buf.length - offset;
        if (!length) {
          length = remaining;
        } else {
          length = Number(length);
          if (length > remaining) {
            length = remaining;
          }
        }
        const strLen = string.length;
        if (length > strLen / 2) {
          length = strLen / 2;
        }
        let i;
        for (i = 0; i < length; ++i) {
          const parsed = parseInt(string.substr(i * 2, 2), 16);
          if (numberIsNaN(parsed)) return i;
          buf[offset + i] = parsed;
        }
        return i;
      }
      function utf8Write(buf, string, offset, length) {
        return blitBuffer(utf8ToBytes(string, buf.length - offset), buf, offset, length);
      }
      function asciiWrite(buf, string, offset, length) {
        return blitBuffer(asciiToBytes(string), buf, offset, length);
      }
      function base64Write(buf, string, offset, length) {
        return blitBuffer(base64ToBytes(string), buf, offset, length);
      }
      function ucs2Write(buf, string, offset, length) {
        return blitBuffer(utf16leToBytes(string, buf.length - offset), buf, offset, length);
      }
      Buffer3.prototype.write = function write(string, offset, length, encoding) {
        if (offset === void 0) {
          encoding = "utf8";
          length = this.length;
          offset = 0;
        } else if (length === void 0 && typeof offset === "string") {
          encoding = offset;
          length = this.length;
          offset = 0;
        } else if (isFinite(offset)) {
          offset = offset >>> 0;
          if (isFinite(length)) {
            length = length >>> 0;
            if (encoding === void 0) encoding = "utf8";
          } else {
            encoding = length;
            length = void 0;
          }
        } else {
          throw new Error(
            "Buffer.write(string, encoding, offset[, length]) is no longer supported"
          );
        }
        const remaining = this.length - offset;
        if (length === void 0 || length > remaining) length = remaining;
        if (string.length > 0 && (length < 0 || offset < 0) || offset > this.length) {
          throw new RangeError("Attempt to write outside buffer bounds");
        }
        if (!encoding) encoding = "utf8";
        let loweredCase = false;
        for (; ;) {
          switch (encoding) {
            case "hex":
              return hexWrite(this, string, offset, length);
            case "utf8":
            case "utf-8":
              return utf8Write(this, string, offset, length);
            case "ascii":
            case "latin1":
            case "binary":
              return asciiWrite(this, string, offset, length);
            case "base64":
              return base64Write(this, string, offset, length);
            case "ucs2":
            case "ucs-2":
            case "utf16le":
            case "utf-16le":
              return ucs2Write(this, string, offset, length);
            default:
              if (loweredCase) throw new TypeError("Unknown encoding: " + encoding);
              encoding = ("" + encoding).toLowerCase();
              loweredCase = true;
          }
        }
      };
      Buffer3.prototype.toJSON = function toJSON() {
        return {
          type: "Buffer",
          data: Array.prototype.slice.call(this._arr || this, 0)
        };
      };
      function base64Slice(buf, start, end) {
        if (start === 0 && end === buf.length) {
          return base64.fromByteArray(buf);
        } else {
          return base64.fromByteArray(buf.slice(start, end));
        }
      }
      function utf8Slice(buf, start, end) {
        end = Math.min(buf.length, end);
        const res = [];
        let i = start;
        while (i < end) {
          const firstByte = buf[i];
          let codePoint = null;
          let bytesPerSequence = firstByte > 239 ? 4 : firstByte > 223 ? 3 : firstByte > 191 ? 2 : 1;
          if (i + bytesPerSequence <= end) {
            let secondByte, thirdByte, fourthByte, tempCodePoint;
            switch (bytesPerSequence) {
              case 1:
                if (firstByte < 128) {
                  codePoint = firstByte;
                }
                break;
              case 2:
                secondByte = buf[i + 1];
                if ((secondByte & 192) === 128) {
                  tempCodePoint = (firstByte & 31) << 6 | secondByte & 63;
                  if (tempCodePoint > 127) {
                    codePoint = tempCodePoint;
                  }
                }
                break;
              case 3:
                secondByte = buf[i + 1];
                thirdByte = buf[i + 2];
                if ((secondByte & 192) === 128 && (thirdByte & 192) === 128) {
                  tempCodePoint = (firstByte & 15) << 12 | (secondByte & 63) << 6 | thirdByte & 63;
                  if (tempCodePoint > 2047 && (tempCodePoint < 55296 || tempCodePoint > 57343)) {
                    codePoint = tempCodePoint;
                  }
                }
                break;
              case 4:
                secondByte = buf[i + 1];
                thirdByte = buf[i + 2];
                fourthByte = buf[i + 3];
                if ((secondByte & 192) === 128 && (thirdByte & 192) === 128 && (fourthByte & 192) === 128) {
                  tempCodePoint = (firstByte & 15) << 18 | (secondByte & 63) << 12 | (thirdByte & 63) << 6 | fourthByte & 63;
                  if (tempCodePoint > 65535 && tempCodePoint < 1114112) {
                    codePoint = tempCodePoint;
                  }
                }
            }
          }
          if (codePoint === null) {
            codePoint = 65533;
            bytesPerSequence = 1;
          } else if (codePoint > 65535) {
            codePoint -= 65536;
            res.push(codePoint >>> 10 & 1023 | 55296);
            codePoint = 56320 | codePoint & 1023;
          }
          res.push(codePoint);
          i += bytesPerSequence;
        }
        return decodeCodePointsArray(res);
      }
      var MAX_ARGUMENTS_LENGTH = 4096;
      function decodeCodePointsArray(codePoints) {
        const len = codePoints.length;
        if (len <= MAX_ARGUMENTS_LENGTH) {
          return String.fromCharCode.apply(String, codePoints);
        }
        let res = "";
        let i = 0;
        while (i < len) {
          res += String.fromCharCode.apply(
            String,
            codePoints.slice(i, i += MAX_ARGUMENTS_LENGTH)
          );
        }
        return res;
      }
      function asciiSlice(buf, start, end) {
        let ret = "";
        end = Math.min(buf.length, end);
        for (let i = start; i < end; ++i) {
          ret += String.fromCharCode(buf[i] & 127);
        }
        return ret;
      }
      function latin1Slice(buf, start, end) {
        let ret = "";
        end = Math.min(buf.length, end);
        for (let i = start; i < end; ++i) {
          ret += String.fromCharCode(buf[i]);
        }
        return ret;
      }
      function hexSlice(buf, start, end) {
        const len = buf.length;
        if (!start || start < 0) start = 0;
        if (!end || end < 0 || end > len) end = len;
        let out = "";
        for (let i = start; i < end; ++i) {
          out += hexSliceLookupTable[buf[i]];
        }
        return out;
      }
      function utf16leSlice(buf, start, end) {
        const bytes = buf.slice(start, end);
        let res = "";
        for (let i = 0; i < bytes.length - 1; i += 2) {
          res += String.fromCharCode(bytes[i] + bytes[i + 1] * 256);
        }
        return res;
      }
      Buffer3.prototype.slice = function slice(start, end) {
        const len = this.length;
        start = ~~start;
        end = end === void 0 ? len : ~~end;
        if (start < 0) {
          start += len;
          if (start < 0) start = 0;
        } else if (start > len) {
          start = len;
        }
        if (end < 0) {
          end += len;
          if (end < 0) end = 0;
        } else if (end > len) {
          end = len;
        }
        if (end < start) end = start;
        const newBuf = this.subarray(start, end);
        Object.setPrototypeOf(newBuf, Buffer3.prototype);
        return newBuf;
      };
      function checkOffset(offset, ext, length) {
        if (offset % 1 !== 0 || offset < 0) throw new RangeError("offset is not uint");
        if (offset + ext > length) throw new RangeError("Trying to access beyond buffer length");
      }
      Buffer3.prototype.readUintLE = Buffer3.prototype.readUIntLE = function readUIntLE(offset, byteLength2, noAssert) {
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) checkOffset(offset, byteLength2, this.length);
        let val = this[offset];
        let mul = 1;
        let i = 0;
        while (++i < byteLength2 && (mul *= 256)) {
          val += this[offset + i] * mul;
        }
        return val;
      };
      Buffer3.prototype.readUintBE = Buffer3.prototype.readUIntBE = function readUIntBE(offset, byteLength2, noAssert) {
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) {
          checkOffset(offset, byteLength2, this.length);
        }
        let val = this[offset + --byteLength2];
        let mul = 1;
        while (byteLength2 > 0 && (mul *= 256)) {
          val += this[offset + --byteLength2] * mul;
        }
        return val;
      };
      Buffer3.prototype.readUint8 = Buffer3.prototype.readUInt8 = function readUInt8(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 1, this.length);
        return this[offset];
      };
      Buffer3.prototype.readUint16LE = Buffer3.prototype.readUInt16LE = function readUInt16LE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 2, this.length);
        return this[offset] | this[offset + 1] << 8;
      };
      Buffer3.prototype.readUint16BE = Buffer3.prototype.readUInt16BE = function readUInt16BE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 2, this.length);
        return this[offset] << 8 | this[offset + 1];
      };
      Buffer3.prototype.readUint32LE = Buffer3.prototype.readUInt32LE = function readUInt32LE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 4, this.length);
        return (this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16) + this[offset + 3] * 16777216;
      };
      Buffer3.prototype.readUint32BE = Buffer3.prototype.readUInt32BE = function readUInt32BE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 4, this.length);
        return this[offset] * 16777216 + (this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3]);
      };
      Buffer3.prototype.readBigUInt64LE = defineBigIntMethod(function readBigUInt64LE(offset) {
        offset = offset >>> 0;
        validateNumber(offset, "offset");
        const first = this[offset];
        const last = this[offset + 7];
        if (first === void 0 || last === void 0) {
          boundsError(offset, this.length - 8);
        }
        const lo = first + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 24;
        const hi = this[++offset] + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + last * 2 ** 24;
        return BigInt(lo) + (BigInt(hi) << BigInt(32));
      });
      Buffer3.prototype.readBigUInt64BE = defineBigIntMethod(function readBigUInt64BE(offset) {
        offset = offset >>> 0;
        validateNumber(offset, "offset");
        const first = this[offset];
        const last = this[offset + 7];
        if (first === void 0 || last === void 0) {
          boundsError(offset, this.length - 8);
        }
        const hi = first * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + this[++offset];
        const lo = this[++offset] * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + last;
        return (BigInt(hi) << BigInt(32)) + BigInt(lo);
      });
      Buffer3.prototype.readIntLE = function readIntLE(offset, byteLength2, noAssert) {
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) checkOffset(offset, byteLength2, this.length);
        let val = this[offset];
        let mul = 1;
        let i = 0;
        while (++i < byteLength2 && (mul *= 256)) {
          val += this[offset + i] * mul;
        }
        mul *= 128;
        if (val >= mul) val -= Math.pow(2, 8 * byteLength2);
        return val;
      };
      Buffer3.prototype.readIntBE = function readIntBE(offset, byteLength2, noAssert) {
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) checkOffset(offset, byteLength2, this.length);
        let i = byteLength2;
        let mul = 1;
        let val = this[offset + --i];
        while (i > 0 && (mul *= 256)) {
          val += this[offset + --i] * mul;
        }
        mul *= 128;
        if (val >= mul) val -= Math.pow(2, 8 * byteLength2);
        return val;
      };
      Buffer3.prototype.readInt8 = function readInt8(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 1, this.length);
        if (!(this[offset] & 128)) return this[offset];
        return (255 - this[offset] + 1) * -1;
      };
      Buffer3.prototype.readInt16LE = function readInt16LE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 2, this.length);
        const val = this[offset] | this[offset + 1] << 8;
        return val & 32768 ? val | 4294901760 : val;
      };
      Buffer3.prototype.readInt16BE = function readInt16BE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 2, this.length);
        const val = this[offset + 1] | this[offset] << 8;
        return val & 32768 ? val | 4294901760 : val;
      };
      Buffer3.prototype.readInt32LE = function readInt32LE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 4, this.length);
        return this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16 | this[offset + 3] << 24;
      };
      Buffer3.prototype.readInt32BE = function readInt32BE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 4, this.length);
        return this[offset] << 24 | this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3];
      };
      Buffer3.prototype.readBigInt64LE = defineBigIntMethod(function readBigInt64LE(offset) {
        offset = offset >>> 0;
        validateNumber(offset, "offset");
        const first = this[offset];
        const last = this[offset + 7];
        if (first === void 0 || last === void 0) {
          boundsError(offset, this.length - 8);
        }
        const val = this[offset + 4] + this[offset + 5] * 2 ** 8 + this[offset + 6] * 2 ** 16 + (last << 24);
        return (BigInt(val) << BigInt(32)) + BigInt(first + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 24);
      });
      Buffer3.prototype.readBigInt64BE = defineBigIntMethod(function readBigInt64BE(offset) {
        offset = offset >>> 0;
        validateNumber(offset, "offset");
        const first = this[offset];
        const last = this[offset + 7];
        if (first === void 0 || last === void 0) {
          boundsError(offset, this.length - 8);
        }
        const val = (first << 24) + // Overflow
          this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + this[++offset];
        return (BigInt(val) << BigInt(32)) + BigInt(this[++offset] * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + last);
      });
      Buffer3.prototype.readFloatLE = function readFloatLE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 4, this.length);
        return ieee754.read(this, offset, true, 23, 4);
      };
      Buffer3.prototype.readFloatBE = function readFloatBE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 4, this.length);
        return ieee754.read(this, offset, false, 23, 4);
      };
      Buffer3.prototype.readDoubleLE = function readDoubleLE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 8, this.length);
        return ieee754.read(this, offset, true, 52, 8);
      };
      Buffer3.prototype.readDoubleBE = function readDoubleBE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert) checkOffset(offset, 8, this.length);
        return ieee754.read(this, offset, false, 52, 8);
      };
      function checkInt(buf, value, offset, ext, max, min) {
        if (!Buffer3.isBuffer(buf)) throw new TypeError('"buffer" argument must be a Buffer instance');
        if (value > max || value < min) throw new RangeError('"value" argument is out of bounds');
        if (offset + ext > buf.length) throw new RangeError("Index out of range");
      }
      Buffer3.prototype.writeUintLE = Buffer3.prototype.writeUIntLE = function writeUIntLE(value, offset, byteLength2, noAssert) {
        value = +value;
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) {
          const maxBytes = Math.pow(2, 8 * byteLength2) - 1;
          checkInt(this, value, offset, byteLength2, maxBytes, 0);
        }
        let mul = 1;
        let i = 0;
        this[offset] = value & 255;
        while (++i < byteLength2 && (mul *= 256)) {
          this[offset + i] = value / mul & 255;
        }
        return offset + byteLength2;
      };
      Buffer3.prototype.writeUintBE = Buffer3.prototype.writeUIntBE = function writeUIntBE(value, offset, byteLength2, noAssert) {
        value = +value;
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) {
          const maxBytes = Math.pow(2, 8 * byteLength2) - 1;
          checkInt(this, value, offset, byteLength2, maxBytes, 0);
        }
        let i = byteLength2 - 1;
        let mul = 1;
        this[offset + i] = value & 255;
        while (--i >= 0 && (mul *= 256)) {
          this[offset + i] = value / mul & 255;
        }
        return offset + byteLength2;
      };
      Buffer3.prototype.writeUint8 = Buffer3.prototype.writeUInt8 = function writeUInt8(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 1, 255, 0);
        this[offset] = value & 255;
        return offset + 1;
      };
      Buffer3.prototype.writeUint16LE = Buffer3.prototype.writeUInt16LE = function writeUInt16LE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 2, 65535, 0);
        this[offset] = value & 255;
        this[offset + 1] = value >>> 8;
        return offset + 2;
      };
      Buffer3.prototype.writeUint16BE = Buffer3.prototype.writeUInt16BE = function writeUInt16BE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 2, 65535, 0);
        this[offset] = value >>> 8;
        this[offset + 1] = value & 255;
        return offset + 2;
      };
      Buffer3.prototype.writeUint32LE = Buffer3.prototype.writeUInt32LE = function writeUInt32LE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 4, 4294967295, 0);
        this[offset + 3] = value >>> 24;
        this[offset + 2] = value >>> 16;
        this[offset + 1] = value >>> 8;
        this[offset] = value & 255;
        return offset + 4;
      };
      Buffer3.prototype.writeUint32BE = Buffer3.prototype.writeUInt32BE = function writeUInt32BE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 4, 4294967295, 0);
        this[offset] = value >>> 24;
        this[offset + 1] = value >>> 16;
        this[offset + 2] = value >>> 8;
        this[offset + 3] = value & 255;
        return offset + 4;
      };
      function wrtBigUInt64LE(buf, value, offset, min, max) {
        checkIntBI(value, min, max, buf, offset, 7);
        let lo = Number(value & BigInt(4294967295));
        buf[offset++] = lo;
        lo = lo >> 8;
        buf[offset++] = lo;
        lo = lo >> 8;
        buf[offset++] = lo;
        lo = lo >> 8;
        buf[offset++] = lo;
        let hi = Number(value >> BigInt(32) & BigInt(4294967295));
        buf[offset++] = hi;
        hi = hi >> 8;
        buf[offset++] = hi;
        hi = hi >> 8;
        buf[offset++] = hi;
        hi = hi >> 8;
        buf[offset++] = hi;
        return offset;
      }
      function wrtBigUInt64BE(buf, value, offset, min, max) {
        checkIntBI(value, min, max, buf, offset, 7);
        let lo = Number(value & BigInt(4294967295));
        buf[offset + 7] = lo;
        lo = lo >> 8;
        buf[offset + 6] = lo;
        lo = lo >> 8;
        buf[offset + 5] = lo;
        lo = lo >> 8;
        buf[offset + 4] = lo;
        let hi = Number(value >> BigInt(32) & BigInt(4294967295));
        buf[offset + 3] = hi;
        hi = hi >> 8;
        buf[offset + 2] = hi;
        hi = hi >> 8;
        buf[offset + 1] = hi;
        hi = hi >> 8;
        buf[offset] = hi;
        return offset + 8;
      }
      Buffer3.prototype.writeBigUInt64LE = defineBigIntMethod(function writeBigUInt64LE(value, offset = 0) {
        return wrtBigUInt64LE(this, value, offset, BigInt(0), BigInt("0xffffffffffffffff"));
      });
      Buffer3.prototype.writeBigUInt64BE = defineBigIntMethod(function writeBigUInt64BE(value, offset = 0) {
        return wrtBigUInt64BE(this, value, offset, BigInt(0), BigInt("0xffffffffffffffff"));
      });
      Buffer3.prototype.writeIntLE = function writeIntLE(value, offset, byteLength2, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) {
          const limit = Math.pow(2, 8 * byteLength2 - 1);
          checkInt(this, value, offset, byteLength2, limit - 1, -limit);
        }
        let i = 0;
        let mul = 1;
        let sub = 0;
        this[offset] = value & 255;
        while (++i < byteLength2 && (mul *= 256)) {
          if (value < 0 && sub === 0 && this[offset + i - 1] !== 0) {
            sub = 1;
          }
          this[offset + i] = (value / mul >> 0) - sub & 255;
        }
        return offset + byteLength2;
      };
      Buffer3.prototype.writeIntBE = function writeIntBE(value, offset, byteLength2, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) {
          const limit = Math.pow(2, 8 * byteLength2 - 1);
          checkInt(this, value, offset, byteLength2, limit - 1, -limit);
        }
        let i = byteLength2 - 1;
        let mul = 1;
        let sub = 0;
        this[offset + i] = value & 255;
        while (--i >= 0 && (mul *= 256)) {
          if (value < 0 && sub === 0 && this[offset + i + 1] !== 0) {
            sub = 1;
          }
          this[offset + i] = (value / mul >> 0) - sub & 255;
        }
        return offset + byteLength2;
      };
      Buffer3.prototype.writeInt8 = function writeInt8(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 1, 127, -128);
        if (value < 0) value = 255 + value + 1;
        this[offset] = value & 255;
        return offset + 1;
      };
      Buffer3.prototype.writeInt16LE = function writeInt16LE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 2, 32767, -32768);
        this[offset] = value & 255;
        this[offset + 1] = value >>> 8;
        return offset + 2;
      };
      Buffer3.prototype.writeInt16BE = function writeInt16BE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 2, 32767, -32768);
        this[offset] = value >>> 8;
        this[offset + 1] = value & 255;
        return offset + 2;
      };
      Buffer3.prototype.writeInt32LE = function writeInt32LE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 4, 2147483647, -2147483648);
        this[offset] = value & 255;
        this[offset + 1] = value >>> 8;
        this[offset + 2] = value >>> 16;
        this[offset + 3] = value >>> 24;
        return offset + 4;
      };
      Buffer3.prototype.writeInt32BE = function writeInt32BE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) checkInt(this, value, offset, 4, 2147483647, -2147483648);
        if (value < 0) value = 4294967295 + value + 1;
        this[offset] = value >>> 24;
        this[offset + 1] = value >>> 16;
        this[offset + 2] = value >>> 8;
        this[offset + 3] = value & 255;
        return offset + 4;
      };
      Buffer3.prototype.writeBigInt64LE = defineBigIntMethod(function writeBigInt64LE(value, offset = 0) {
        return wrtBigUInt64LE(this, value, offset, -BigInt("0x8000000000000000"), BigInt("0x7fffffffffffffff"));
      });
      Buffer3.prototype.writeBigInt64BE = defineBigIntMethod(function writeBigInt64BE(value, offset = 0) {
        return wrtBigUInt64BE(this, value, offset, -BigInt("0x8000000000000000"), BigInt("0x7fffffffffffffff"));
      });
      function checkIEEE754(buf, value, offset, ext, max, min) {
        if (offset + ext > buf.length) throw new RangeError("Index out of range");
        if (offset < 0) throw new RangeError("Index out of range");
      }
      function writeFloat(buf, value, offset, littleEndian, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) {
          checkIEEE754(buf, value, offset, 4, 34028234663852886e22, -34028234663852886e22);
        }
        ieee754.write(buf, value, offset, littleEndian, 23, 4);
        return offset + 4;
      }
      Buffer3.prototype.writeFloatLE = function writeFloatLE(value, offset, noAssert) {
        return writeFloat(this, value, offset, true, noAssert);
      };
      Buffer3.prototype.writeFloatBE = function writeFloatBE(value, offset, noAssert) {
        return writeFloat(this, value, offset, false, noAssert);
      };
      function writeDouble(buf, value, offset, littleEndian, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) {
          checkIEEE754(buf, value, offset, 8, 17976931348623157e292, -17976931348623157e292);
        }
        ieee754.write(buf, value, offset, littleEndian, 52, 8);
        return offset + 8;
      }
      Buffer3.prototype.writeDoubleLE = function writeDoubleLE(value, offset, noAssert) {
        return writeDouble(this, value, offset, true, noAssert);
      };
      Buffer3.prototype.writeDoubleBE = function writeDoubleBE(value, offset, noAssert) {
        return writeDouble(this, value, offset, false, noAssert);
      };
      Buffer3.prototype.copy = function copy(target, targetStart, start, end) {
        if (!Buffer3.isBuffer(target)) throw new TypeError("argument should be a Buffer");
        if (!start) start = 0;
        if (!end && end !== 0) end = this.length;
        if (targetStart >= target.length) targetStart = target.length;
        if (!targetStart) targetStart = 0;
        if (end > 0 && end < start) end = start;
        if (end === start) return 0;
        if (target.length === 0 || this.length === 0) return 0;
        if (targetStart < 0) {
          throw new RangeError("targetStart out of bounds");
        }
        if (start < 0 || start >= this.length) throw new RangeError("Index out of range");
        if (end < 0) throw new RangeError("sourceEnd out of bounds");
        if (end > this.length) end = this.length;
        if (target.length - targetStart < end - start) {
          end = target.length - targetStart + start;
        }
        const len = end - start;
        if (this === target && typeof Uint8Array.prototype.copyWithin === "function") {
          this.copyWithin(targetStart, start, end);
        } else {
          Uint8Array.prototype.set.call(
            target,
            this.subarray(start, end),
            targetStart
          );
        }
        return len;
      };
      Buffer3.prototype.fill = function fill(val, start, end, encoding) {
        if (typeof val === "string") {
          if (typeof start === "string") {
            encoding = start;
            start = 0;
            end = this.length;
          } else if (typeof end === "string") {
            encoding = end;
            end = this.length;
          }
          if (encoding !== void 0 && typeof encoding !== "string") {
            throw new TypeError("encoding must be a string");
          }
          if (typeof encoding === "string" && !Buffer3.isEncoding(encoding)) {
            throw new TypeError("Unknown encoding: " + encoding);
          }
          if (val.length === 1) {
            const code = val.charCodeAt(0);
            if (encoding === "utf8" && code < 128 || encoding === "latin1") {
              val = code;
            }
          }
        } else if (typeof val === "number") {
          val = val & 255;
        } else if (typeof val === "boolean") {
          val = Number(val);
        }
        if (start < 0 || this.length < start || this.length < end) {
          throw new RangeError("Out of range index");
        }
        if (end <= start) {
          return this;
        }
        start = start >>> 0;
        end = end === void 0 ? this.length : end >>> 0;
        if (!val) val = 0;
        let i;
        if (typeof val === "number") {
          for (i = start; i < end; ++i) {
            this[i] = val;
          }
        } else {
          const bytes = Buffer3.isBuffer(val) ? val : Buffer3.from(val, encoding);
          const len = bytes.length;
          if (len === 0) {
            throw new TypeError('The value "' + val + '" is invalid for argument "value"');
          }
          for (i = 0; i < end - start; ++i) {
            this[i + start] = bytes[i % len];
          }
        }
        return this;
      };
      var errors = {};
      function E(sym, getMessage, Base) {
        errors[sym] = class NodeError extends Base {
          constructor() {
            super();
            Object.defineProperty(this, "message", {
              value: getMessage.apply(this, arguments),
              writable: true,
              configurable: true
            });
            this.name = `${this.name} [${sym}]`;
            this.stack;
            delete this.name;
          }
          get code() {
            return sym;
          }
          set code(value) {
            Object.defineProperty(this, "code", {
              configurable: true,
              enumerable: true,
              value,
              writable: true
            });
          }
          toString() {
            return `${this.name} [${sym}]: ${this.message}`;
          }
        };
      }
      E(
        "ERR_BUFFER_OUT_OF_BOUNDS",
        function (name) {
          if (name) {
            return `${name} is outside of buffer bounds`;
          }
          return "Attempt to access memory outside buffer bounds";
        },
        RangeError
      );
      E(
        "ERR_INVALID_ARG_TYPE",
        function (name, actual) {
          return `The "${name}" argument must be of type number. Received type ${typeof actual}`;
        },
        TypeError
      );
      E(
        "ERR_OUT_OF_RANGE",
        function (str, range, input) {
          let msg = `The value of "${str}" is out of range.`;
          let received = input;
          if (Number.isInteger(input) && Math.abs(input) > 2 ** 32) {
            received = addNumericalSeparator(String(input));
          } else if (typeof input === "bigint") {
            received = String(input);
            if (input > BigInt(2) ** BigInt(32) || input < -(BigInt(2) ** BigInt(32))) {
              received = addNumericalSeparator(received);
            }
            received += "n";
          }
          msg += ` It must be ${range}. Received ${received}`;
          return msg;
        },
        RangeError
      );
      function addNumericalSeparator(val) {
        let res = "";
        let i = val.length;
        const start = val[0] === "-" ? 1 : 0;
        for (; i >= start + 4; i -= 3) {
          res = `_${val.slice(i - 3, i)}${res}`;
        }
        return `${val.slice(0, i)}${res}`;
      }
      function checkBounds(buf, offset, byteLength2) {
        validateNumber(offset, "offset");
        if (buf[offset] === void 0 || buf[offset + byteLength2] === void 0) {
          boundsError(offset, buf.length - (byteLength2 + 1));
        }
      }
      function checkIntBI(value, min, max, buf, offset, byteLength2) {
        if (value > max || value < min) {
          const n = typeof min === "bigint" ? "n" : "";
          let range;
          if (byteLength2 > 3) {
            if (min === 0 || min === BigInt(0)) {
              range = `>= 0${n} and < 2${n} ** ${(byteLength2 + 1) * 8}${n}`;
            } else {
              range = `>= -(2${n} ** ${(byteLength2 + 1) * 8 - 1}${n}) and < 2 ** ${(byteLength2 + 1) * 8 - 1}${n}`;
            }
          } else {
            range = `>= ${min}${n} and <= ${max}${n}`;
          }
          throw new errors.ERR_OUT_OF_RANGE("value", range, value);
        }
        checkBounds(buf, offset, byteLength2);
      }
      function validateNumber(value, name) {
        if (typeof value !== "number") {
          throw new errors.ERR_INVALID_ARG_TYPE(name, "number", value);
        }
      }
      function boundsError(value, length, type) {
        if (Math.floor(value) !== value) {
          validateNumber(value, type);
          throw new errors.ERR_OUT_OF_RANGE(type || "offset", "an integer", value);
        }
        if (length < 0) {
          throw new errors.ERR_BUFFER_OUT_OF_BOUNDS();
        }
        throw new errors.ERR_OUT_OF_RANGE(
          type || "offset",
          `>= ${type ? 1 : 0} and <= ${length}`,
          value
        );
      }
      var INVALID_BASE64_RE = /[^+/0-9A-Za-z-_]/g;
      function base64clean(str) {
        str = str.split("=")[0];
        str = str.trim().replace(INVALID_BASE64_RE, "");
        if (str.length < 2) return "";
        while (str.length % 4 !== 0) {
          str = str + "=";
        }
        return str;
      }
      function utf8ToBytes(string, units) {
        units = units || Infinity;
        let codePoint;
        const length = string.length;
        let leadSurrogate = null;
        const bytes = [];
        for (let i = 0; i < length; ++i) {
          codePoint = string.charCodeAt(i);
          if (codePoint > 55295 && codePoint < 57344) {
            if (!leadSurrogate) {
              if (codePoint > 56319) {
                if ((units -= 3) > -1) bytes.push(239, 191, 189);
                continue;
              } else if (i + 1 === length) {
                if ((units -= 3) > -1) bytes.push(239, 191, 189);
                continue;
              }
              leadSurrogate = codePoint;
              continue;
            }
            if (codePoint < 56320) {
              if ((units -= 3) > -1) bytes.push(239, 191, 189);
              leadSurrogate = codePoint;
              continue;
            }
            codePoint = (leadSurrogate - 55296 << 10 | codePoint - 56320) + 65536;
          } else if (leadSurrogate) {
            if ((units -= 3) > -1) bytes.push(239, 191, 189);
          }
          leadSurrogate = null;
          if (codePoint < 128) {
            if ((units -= 1) < 0) break;
            bytes.push(codePoint);
          } else if (codePoint < 2048) {
            if ((units -= 2) < 0) break;
            bytes.push(
              codePoint >> 6 | 192,
              codePoint & 63 | 128
            );
          } else if (codePoint < 65536) {
            if ((units -= 3) < 0) break;
            bytes.push(
              codePoint >> 12 | 224,
              codePoint >> 6 & 63 | 128,
              codePoint & 63 | 128
            );
          } else if (codePoint < 1114112) {
            if ((units -= 4) < 0) break;
            bytes.push(
              codePoint >> 18 | 240,
              codePoint >> 12 & 63 | 128,
              codePoint >> 6 & 63 | 128,
              codePoint & 63 | 128
            );
          } else {
            throw new Error("Invalid code point");
          }
        }
        return bytes;
      }
      function asciiToBytes(str) {
        const byteArray = [];
        for (let i = 0; i < str.length; ++i) {
          byteArray.push(str.charCodeAt(i) & 255);
        }
        return byteArray;
      }
      function utf16leToBytes(str, units) {
        let c, hi, lo;
        const byteArray = [];
        for (let i = 0; i < str.length; ++i) {
          if ((units -= 2) < 0) break;
          c = str.charCodeAt(i);
          hi = c >> 8;
          lo = c % 256;
          byteArray.push(lo);
          byteArray.push(hi);
        }
        return byteArray;
      }
      function base64ToBytes(str) {
        return base64.toByteArray(base64clean(str));
      }
      function blitBuffer(src, dst, offset, length) {
        let i;
        for (i = 0; i < length; ++i) {
          if (i + offset >= dst.length || i >= src.length) break;
          dst[i + offset] = src[i];
        }
        return i;
      }
      function isInstance(obj, type) {
        return obj instanceof type || obj != null && obj.constructor != null && obj.constructor.name != null && obj.constructor.name === type.name;
      }
      function numberIsNaN(obj) {
        return obj !== obj;
      }
      var hexSliceLookupTable = (function () {
        const alphabet = "0123456789abcdef";
        const table = new Array(256);
        for (let i = 0; i < 16; ++i) {
          const i16 = i * 16;
          for (let j = 0; j < 16; ++j) {
            table[i16 + j] = alphabet[i] + alphabet[j];
          }
        }
        return table;
      })();
      function defineBigIntMethod(fn) {
        return typeof BigInt === "undefined" ? BufferBigIntNotDefined : fn;
      }
      function BufferBigIntNotDefined() {
        throw new Error("BigInt not supported");
      }
    }
  });

  // src/InAccord/polyfill/path.ts
  var remotePath, path_default;
  var init_path = __esm({
    "src/InAccord/polyfill/path.ts"() {
      "use strict";
      init_remote();
      remotePath = remote_default?.path;
      if (!remotePath) {
        throw new Error("[InAccord] preload path API missing (Remote.path). Injection/preload did not initialize.");
      }
      path_default = remotePath;
    }
  });

  // src/InAccord/stores/base.ts
  var Store;
  var init_base = __esm({
    "src/InAccord/stores/base.ts"() {
      "use strict";
      Store = class {
        initialize() {
        }
        #listeners = /* @__PURE__ */ new Set();
        addChangeListener(callback) {
          this.#listeners.add(callback);
          return () => this.removeChangeListener(callback);
        }
        removeChangeListener(callback) {
          this.#listeners.delete(callback);
        }
        emitChange() {
          for (const listener of this.#listeners) {
            listener();
          }
        }
      };
      Object.freeze(Store);
      Object.freeze(Store.prototype);
    }
  });

  // src/common/utils/clone.ts
  function getKeys(object) {
    const keys2 = [];
    for (const key in object) keys2.push(key);
    return keys2;
  }
  function cloneObject(target, newObject = {}, keys2) {
    if (!Array.isArray(keys2)) keys2 = getKeys(target);
    return keys2.reduce((clone, key) => {
      if (typeof target[key] === "object" && !Array.isArray(target[key]) && target[key] !== null) clone[key] = cloneObject(target[key], {});
      else if (typeof target[key] === "function") clone[key] = target[key].bind(target);
      else clone[key] = target[key];
      return clone;
    }, newObject);
  }
  var init_clone = __esm({
    "src/common/utils/clone.ts"() {
      "use strict";
    }
  });

  // src/common/utils/debounce.ts
  function debounce(executor, delay) {
    let timeout;
    return function (...args) {
      clearTimeout(timeout);
      timeout = setTimeout(() => executor(...args), delay);
    };
  }
  var init_debounce = __esm({
    "src/common/utils/debounce.ts"() {
      "use strict";
    }
  });

  // src/common/utils/extend.ts
  function cloneArray(array) {
    const clone = [];
    for (let a = 0; a < array.length; a++) {
      const element = array[a];
      if (typeof element !== "object" || element === null) {
        clone[a] = element;
      } else if (Array.isArray(element)) {
        clone[a] = cloneArray(element);
      } else {
        clone[a] = extend({}, element);
      }
    }
    return clone;
  }
  function shouldSkip(obj) {
    if (typeof obj !== "object") return true;
    if (obj === null) return true;
    if (Array.isArray(obj)) return true;
  }
  function extend(target, ...extenders) {
    if (extenders.length < 1) throw new Error("Needs at least 1 extenders");
    for (let e = 0; e < extenders.length; e++) {
      const ext = extenders[e];
      if (shouldSkip(ext)) continue;
      for (const key in ext) {
        if (!(key in ext)) continue;
        const source = target[key];
        const value = ext[key];
        if (value === target) continue;
        if (typeof value !== "object" || value === null) {
          target[key] = value;
        } else if (Array.isArray(value)) {
          target[key] = cloneArray(value);
        } else if (typeof source !== "object" || source === null || Array.isArray(source)) {
          target[key] = extend({}, value);
        } else {
          target[key] = extend(source, value);
        }
      }
    }
    return target;
  }
  var init_extend = __esm({
    "src/common/utils/extend.ts"() {
      "use strict";
    }
  });

  // src/common/utils/findintree.ts
  function findInTree(tree, searchFilter, {walkable = null, ignore = []} = {}) {
    if (typeof searchFilter === "string") {
      if (tree?.hasOwnProperty(searchFilter)) return tree[searchFilter];
    } else if (searchFilter(tree)) {
      return tree;
    }
    if (typeof tree !== "object" || tree == null) return void 0;
    let tempReturn;
    if (tree instanceof Array) {
      for (const value of tree) {
        tempReturn = findInTree(value, searchFilter, {walkable, ignore});
        if (typeof tempReturn != "undefined") return tempReturn;
      }
    } else {
      const toWalk = walkable == null ? Object.keys(tree) : walkable;
      for (const key of toWalk) {
        if (typeof tree[key] == "undefined" || ignore.includes(key)) continue;
        tempReturn = findInTree(tree[key], searchFilter, {walkable, ignore});
        if (typeof tempReturn != "undefined") return tempReturn;
      }
    }
    return tempReturn;
  }
  var init_findintree = __esm({
    "src/common/utils/findintree.ts"() {
      "use strict";
    }
  });

  // src/common/utils/format.ts
  function formatString(string, values) {
    for (const val in values) {
      let replacement = values[val];
      if (typeof replacement === "function") replacement = replacement();
      if (replacement === void 0) continue;
      if (replacement === null) replacement = "null";
      if (Array.isArray(replacement)) replacement = JSON.stringify(replacement);
      if (typeof replacement === "object" && replacement !== null) replacement = replacement.toString();
      string = string.replace(new RegExp(`{{${val}}}`, "g"), replacement?.toString() ?? "null");
    }
    return string;
  }
  var init_format = __esm({
    "src/common/utils/format.ts"() {
      "use strict";
    }
  });

  // src/common/utils/memoize.ts
  function memoizeObject(object) {
    const proxy = new Proxy(object, {
      get: function (obj, mod) {
        if (typeof mod === "symbol") return null;
        if (!Object.prototype.hasOwnProperty.call(obj, mod)) return void 0;
        if (Object.getOwnPropertyDescriptor(obj, mod)?.get) {
          const value = obj[mod];
          delete obj[mod];
          obj[mod] = value;
        }
        return obj[mod];
      },
      set: function (obj, mod, value) {
        if (typeof mod === "symbol") return false;
        if (Object.prototype.hasOwnProperty.call(obj, mod)) return false;
        obj[mod] = value;
        return true;
      }
    });
    Object.defineProperty(proxy, "hasOwnProperty", {
      value: function (prop) {
        return this[prop] !== void 0;
      }
    });
    return proxy;
  }
  var init_memoize = __esm({
    "src/common/utils/memoize.ts"() {
      "use strict";
    }
  });

  // src/common/utils/nested.ts
  function getNestedProp(object, path2, skipLast) {
    if (!path2) return object;
    const split = path2.split(".");
    if (skipLast) split.pop();
    return split.reduce((acc, curr) => acc && acc[curr], object);
  }
  var init_nested = __esm({
    "src/common/utils/nested.ts"() {
      "use strict";
    }
  });

  // src/common/utils/index.ts
  var init_utils = __esm({
    "src/common/utils/index.ts"() {
      "use strict";
      init_clone();
      init_debounce();
      init_extend();
      init_findintree();
      init_format();
      init_memoize();
      init_nested();
    }
  });

  // src/electron/preload/process.ts
  var newProcess, process_default;
  var init_process = __esm({
    "src/electron/preload/process.ts"() {
      "use strict";
      init_utils();
      newProcess = cloneObject(process, {}, getKeys(process).filter((p) => p !== "config"));
      newProcess.versions.nodejs = newProcess.versions.node;
      delete newProcess.versions.node;
      newProcess.isWeb = true;
      process_default = newProcess;
    }
  });

  // src/InAccord/stores/config.ts
  var iaDataPath, backupFolderName, config_default;
  var init_config = __esm({
    "src/InAccord/stores/config.ts"() {
      "use strict";
      init_path();
      init_base();
      init_process();
      iaDataPath = process_default.env.InAccord_DATA_PATH ?? process_default.env.inaccord_DATA_PATH ?? "";
      backupFolderName = process_default.env.inaccord_BACKUP_FOLDER ?? "backup";
      config_default = new class ConfigStore extends Store {
        data = {
          branch: process_default.env.__BRANCH__,
          commit: process_default.env.__COMMIT__,
          build: process_default.env.__BUILD__,
          version: process_default.env.__VERSION__,
          // TODO: asynchronously get these from the mian process instead of hacky env vars
          appPath: process_default.env.DISCORD_APP_PATH,
          userData: process_default.env.DISCORD_USER_DATA,
          iaPath: iaDataPath,
          dataPath: path_default.join(iaDataPath, "data"),
          backupPath: path_default.join(iaDataPath, backupFolderName),
          pluginsPath: path_default.join(iaDataPath, "plugins"),
          themesPath: path_default.join(iaDataPath, "themes"),
          channelPath: path_default.join(iaDataPath, "data", window?.DiscordNative?.app?.getReleaseChannel?.() ?? "stable")
        };
        get(id) {
          return this.data[id];
        }
        set(id, value) {
          this.data[id] = value;
          this.emitChange();
        }
        get isDevelopment() {
          return this.data.build !== "production";
        }
        get isCanary() {
          return this.data.branch !== "mian";
        }
      }();
    }
  });

  // src/InAccord/polyfill/electron.ts
  var remoteElectron, ipcRenderer, shell, webUtils, electron_default;
  var init_electron = __esm({
    "src/InAccord/polyfill/electron.ts"() {
      "use strict";
      init_remote();
      remoteElectron = remote_default?.electron;
      if (!remoteElectron) {
        throw new Error("[InAccord] preload electron API missing (Remote.electron). Injection/preload did not initialize.");
      }
      ipcRenderer = remoteElectron.ipcRenderer;
      shell = remoteElectron.shell;
      webUtils = remoteElectron.webUtils;
      electron_default = remoteElectron;
    }
  });

  // src/InAccord/modules/emitter.ts
  var emitter_default;
  var init_emitter = __esm({
    "src/InAccord/modules/emitter.ts"() {
      "use strict";
      init_events();
      emitter_default = new class iaEvents extends EventEmitter {
        constructor() {
          super();
          this.setMaxListeners(20);
        }
        dispatch(eventName, ...args) {
          this.emit(eventName, ...args);
        }
      }();
    }
  });

  // src/InAccord/data/settings.ts
  var DefaultSettings, settings_default;
  var init_settings = __esm({
    "src/InAccord/data/settings.ts"() {
      "use strict";
      init_config();
      DefaultSettings = [
        {
          type: "category",
          id: "general",
          collapsible: true,
          settings: [
            {type: "switch", id: "voiceDisconnect", value: false},
            {type: "switch", id: "showToasts", value: true},
            {type: "switch", id: "mediaKeys", value: false},
            {type: "switch", id: "iaContextMenu", value: true},
            {type: "switch", id: "themeAttributes", value: true},
            {type: "position", id: "notificationPosition", value: "top-right", inline: false, enableWith: "notificationEnabled"},
            {type: "switch", id: "notificationEnabled", value: true}
          ]
        },
        {
          type: "category",
          id: "addons",
          collapsible: true,
          shown: false,
          settings: [
            {type: "switch", id: "addonErrors", value: true},
            {type: "dropdown", id: "editAction", value: "detached", options: [{value: "detached"}, {value: "external"}, {value: "system"}]},
            {type: "switch", id: "checkForUpdates", value: true},
            {type: "slider", id: "updateInterval", value: 4, min: 2, max: 12, step: 1, markers: [2, 4, 6, 8, 10, 12], units: "hrs", enableWith: "checkForUpdates"}
          ]
        },
        {
          type: "category",
          id: "store",
          collapsible: true,
          shown: false,
          settings: [
            {type: "switch", id: "iaAddonStore", value: true},
            {type: "switch", id: "alwaysEnable", value: false},
            {type: "switch", id: "addonEmbeds", value: true}
          ]
        },
        {
          type: "category",
          id: "customcss",
          collapsible: true,
          shown: false,
          settings: [
            {type: "switch", id: "customcss", value: true},
            {type: "switch", id: "liveUpdate", value: false},
            {type: "dropdown", id: "openAction", value: "settings", options: [{value: "settings"}, {value: "detached"}, {value: "external"}, {value: "system"}]}
          ]
        },
        {
          type: "category",
          id: "editor",
          collapsible: true,
          shown: false,
          settings: [
            {type: "dropdown", id: "theme", value: "system", options: [{value: "system"}, {value: "vs-dark"}, {value: "vs"}, {value: "hc-black"}, {value: "hc-light"}]},
            {type: "switch", id: "lineNumbers", value: true},
            {type: "switch", id: "minimap", value: true},
            {type: "switch", id: "hover", value: true},
            {type: "switch", id: "quickSuggestions", value: true},
            {type: "switch", id: "insertSpaces", value: false},
            {type: "number", id: "tabSize", min: 1, value: 4},
            {type: "number", id: "fontSize", min: 2, value: 14},
            {type: "dropdown", id: "renderWhitespace", value: "selection", options: [{value: "none"}, {value: "all"}, {value: "selection"}]}
          ]
        },
        {
          type: "category",
          id: "window",
          collapsible: true,
          shown: false,
          settings: [
            {type: "switch", id: "transparency", value: false},
            {type: "switch", id: "removeMinimumSize", value: false},
            {type: "switch", id: "frame", value: process.platform === "linux"},
            // MacOS exclusive
            {type: "switch", id: "inAppTrafficLights", value: false, disabled: process.env.inaccord_NATIVE_FRAME === "true", hidden: process.platform !== "darwin"}
          ]
        },
        {
          type: "category",
          id: "developer",
          collapsible: true,
          shown: false,
          settings: [
            {type: "switch", id: "debugLogs", value: false},
            {type: "switch", id: "devTools", value: false},
            {type: "switch", id: "debuggerHotkey", value: false, enableWith: "devTools"},
            {type: "switch", id: "reactDevTools", value: false, enableWith: "devTools"},
            {type: "switch", id: "inspectElement", value: false, enableWith: "devTools"},
            {type: "switch", id: "devToolsWarning", value: false, enableWith: "devTools"},
            {type: "switch", id: "recovery", value: true, enableWith: "devTools"},
            {type: "switch", id: "canary", value: config_default.isCanary, hidden: true}
          ]
        }
      ];
      if (config_default.isDevelopment) {
        DefaultSettings.push(
          {
            type: "category",
            id: "debug",
            name: "Debug",
            collapsible: true,
            shown: true,
            settings: [
              {name: "Text test", note: "Just testing it", type: "text", id: "texttest", value: ""},
              {
                name: "Slider test",
                note: "Just testing it",
                type: "slider",
                id: "slidertest",
                value: 30,
                min: 20,
                max: 50,
                step: 10,
                units: "em",
                markers: [
                  {label: "max", value: 50},
                  30,
                  {label: "min", value: 20},
                  {label: "anything", value: 40}
                ]
              },
              {
                name: "Radio test",
                note: "Just testing it",
                type: "radio",
                id: "radiotest",
                value: "test",
                options: [
                  {name: "First", value: 30, description: "little hint"},
                  {name: "IDK", value: "test", description: "who cares"},
                  {name: "Something", value: 666, description: "something else"},
                  {name: "Last", value: "last", description: "nothing more to add"}
                ]
              },
              {
                type: "slider",
                id: "maxWidth",
                name: "Notification Width",
                note: "Maximum width of notifications",
                value: 370,
                min: 100,
                max: 400,
                markers: [100, 200, 300, 400],
                units: "px",
                defaultValue: 370,
                stickToMarkers: false
              },
              {name: "Keybind test", note: "Just testing it", type: "keybind", id: "keybindtest", value: ["Control", "H"]},
              {name: "Color test", note: "Just testing it", type: "color", id: "colortest", value: "#ff0000", defaultValue: "#ffffff"}
            ]
          }
        );
      }
      settings_default = DefaultSettings;
    }
  });

  // src/InAccord/webpack/require.ts
  function listenToModules(modules2) {
    for (const moduleId in modules2) {
      const originalModule = modules2[moduleId];
      modules2[moduleId] = (module, exports, require4) => {
        try {
          Reflect.apply(originalModule, null, [module, exports, require4]);
          const listeners = [...lazyListeners];
          for (let i = 0; i < listeners.length; i++) {
            try {
              listeners[i](exports, module, module.id);
            } catch (error) {
              Logger.stacktrace("WebpackModules", "Could not fire callback listener:", error);
            }
          }
        } catch (error) {
          Logger.stacktrace("WebpackModules", "Could not patch pushed module", error);
        } finally {
          require4.m[moduleId] = originalModule;
        }
      };
      Object.assign(modules2[moduleId], originalModule, {
        toString: () => originalModule.toString()
      });
    }
  }
  function handlePush(chunk) {
    const [, modules2] = chunk;
    listenToModules(modules2);
    return Reflect.apply(__ORIGINAL_PUSH__, window.webpackChunkdiscord_app, [chunk]);
  }
  var webpackRequire, lazyListeners, __ORIGINAL_PUSH__, modules;
  var init_require = __esm({
    "src/InAccord/webpack/require.ts"() {
      "use strict";
      init_logger();
      lazyListeners = /* @__PURE__ */ new Set();
      __ORIGINAL_PUSH__ = window.webpackChunkdiscord_app.push;
      Object.defineProperty(window.webpackChunkdiscord_app, "push", {
        configurable: true,
        get: () => handlePush,
        set: (newPush) => {
          __ORIGINAL_PUSH__ = newPush;
          Object.defineProperty(window.webpackChunkdiscord_app, "push", {
            value: handlePush,
            configurable: true,
            writable: true
          });
        }
      });
      window.webpackChunkdiscord_app.push([
        [/* @__PURE__ */ Symbol("InAccord")],
        {},
        (__webpack_require__) => {
          if ("b" in __webpack_require__) {
            webpackRequire = __webpack_require__;
            listenToModules(__webpack_require__.m);
          }
        }
      ]);
      modules = new Proxy({}, {
        ownKeys() {
          return Object.keys(webpackRequire.m);
        },
        getOwnPropertyDescriptor() {
          return {
            enumerable: true,
            configurable: true
            // Not actually
          };
        },
        get(_, k) {
          return webpackRequire.m[k];
        },
        set() {
          throw new Error("[WebpackModules~modules] Setting modules is not allowed.");
        }
      });
    }
  });

  // src/InAccord/webpack/filter.ts
  var filter_exports = {};
  __export(filter_exports, {
    byDisplayName: () => byDisplayName,
    byKeys: () => byKeys,
    byPrototypeKeys: () => byPrototypeKeys,
    byRegex: () => byRegex,
    bySource: () => bySource,
    byStoreName: () => byStoreName,
    byStrings: () => byStrings,
    combine: () => combine,
    not: () => not
  });
  function byKeys(props, filter = (m) => m) {
    return (module) => {
      if (!module) return false;
      if (typeof module !== "object" && typeof module !== "function") return false;
      const component = filter(module);
      if (!component) return false;
      for (let p = 0; p < props.length; p++) {
        if (!(props[p] in component)) return false;
      }
      return true;
    };
  }
  function byPrototypeKeys(fields, filter = (m) => m) {
    return (module) => {
      if (!module) return false;
      if (typeof module !== "object" && typeof module !== "function") return false;
      const component = filter(module);
      if (!component) return false;
      if (!component.prototype) return false;
      for (let f = 0; f < fields.length; f++) {
        if (!(fields[f] in component.prototype)) return false;
      }
      return true;
    };
  }
  function byRegex(search, filter = (m) => m) {
    return (module) => {
      const method = filter(module);
      if (!method) return false;
      let methodString = "";
      try {
        methodString = method.toString([]);
      } catch {
        methodString = method.toString();
      }
      return methodString.search(search) !== -1;
    };
  }
  function bySource(...searches) {
    const moduleCache = webpackRequire.m;
    return (_, module) => {
      const id = module?.id;
      if (!id) return false;
      let source;
      try {
        source = moduleCache[id].toString();
      } catch {
        return false;
      }
      if (!source) return false;
      for (let i = 0; i < searches.length; i++) {
        const search = searches[i];
        if (typeof search === "string") {
          if (!source.includes(search)) return false;
        } else {
          if (!search.test(source)) return false;
        }
      }
      return true;
    };
  }
  function byStrings(...strings) {
    return (module) => {
      if (typeof module !== "function") return false;
      try {
        const str = String(module);
        for (const s of strings) {
          if (!str.includes(s)) return false;
        }
        return true;
      } catch {
        return false;
      }
    };
  }
  function byDisplayName(name) {
    return (module) => {
      return module && module.displayName === name;
    };
  }
  function byStoreName(name) {
    return (module) => {
      return module?._dispatchToken && module?.getName?.() === name;
    };
  }
  function combine(...filters) {
    return (exports, module, id) => {
      return filters.every((filter) => filter(exports, module, id));
    };
  }
  function not(filter) {
    return (exports, module, id) => !filter(exports, module, id);
  }
  var init_filter = __esm({
    "src/InAccord/webpack/filter.ts"() {
      "use strict";
      init_require();
    }
  });

  // src/InAccord/webpack/shared.ts
  function shouldSkipModule(exports) {
    if (!(typeof exports === "object" || typeof exports === "function")) return true;
    if (!exports) return true;
    if (exports.TypedArray) return true;
    if (exports === window) return true;
    if (exports === document.documentElement) return true;
    if (exports[Symbol.toStringTag] === "DOMTokenList") return true;
    if (exports === Symbol) return true;
    if (exports instanceof Window) return true;
    if (exports instanceof TypedArray) return true;
    if (exports.$$loader && exports.$$baseObject || exports.Z?.$$loader && exports.Z?.$$baseObject) return true;
    return false;
  }
  function getDefaultKey(module) {
    if ("Z" in module.exports) return "Z";
    if ("ZP" in module.exports) return "ZP";
    if (module.exports.__esModule && "default" in module.exports) return "default";
  }
  var hasThrown, wrapFilter, TypedArray, makeException;
  var init_shared = __esm({
    "src/InAccord/webpack/shared.ts"() {
      "use strict";
      init_logger();
      hasThrown = /* @__PURE__ */ new WeakSet();
      wrapFilter = (filter) => (exports, module, moduleId) => {
        try {
          if (exports instanceof Window) return false;
          if (exports?.default?.remove && exports?.default?.set && exports?.default?.clear && exports?.default?.get && !exports?.default?.sort) return false;
          if (exports.remove && exports.set && exports.clear && exports.get && !exports.sort) return false;
          if (exports?.default?.getToken || exports?.default?.getEmial || exports?.default?.showToken) return false;
          if (exports.getToken || exports.getEmial || exports.showToken) return false;
          return filter(exports, module, moduleId);
        } catch (error) {
          if (!hasThrown.has(filter)) Logger.warn("WebpackModules~getModule", "Module filter threw an exception.", error, {filter, module});
          hasThrown.add(filter);
          return false;
        }
      };
      TypedArray = Object.getPrototypeOf(Uint8Array);
      makeException = () => new Error("Module search fialed!");
    }
  });

  // src/InAccord/webpack/searching.ts
  function getModule(filter, options = {}) {
    const {defaultExport = true, searchExports = false, searchDefault = true, raw = false, fatal = false} = options;
    filter = wrapFilter(filter);
    const webpackModules = Object.values(webpackRequire.c);
    for (let i = 0; i < webpackModules.length; i++) {
      const module = webpackModules[i];
      if (shouldSkipModule(module.exports)) continue;
      if (filter(module.exports, module, module.id)) {
        return raw ? module : module.exports;
      }
      if (!searchExports && !searchDefault) continue;
      let defaultKey;
      const searchKeys = [];
      if (searchExports) searchKeys.push(...Object.keys(module.exports));
      else if (searchDefault && (defaultKey = getDefaultKey(module))) searchKeys.push(defaultKey);
      for (let j = 0; j < searchKeys.length; j++) {
        const key = searchKeys[j];
        const exported = module.exports[key];
        if (shouldSkipModule(exported)) continue;
        if (filter(exported, module, module.id)) {
          if (!defaultExport && defaultKey === key) {
            return module.exports;
          }
          if (raw) return module;
          return exported;
        }
      }
    }
    if (fatal) throw makeException();
    return void 0;
  }
  function getAllModules(filter, options = {}) {
    const {defaultExport = true, searchExports = false, searchDefault = true, raw = false, fatal = false} = options;
    filter = wrapFilter(filter);
    const modules2 = [];
    const webpackModules = Object.values(webpackRequire.c);
    for (let i = 0; i < webpackModules.length; i++) {
      const module = webpackModules[i];
      if (shouldSkipModule(module.exports)) continue;
      if (filter(module.exports, module, module.id)) {
        modules2.push(raw ? module : module.exports);
      }
      if (!searchExports && !searchDefault) continue;
      let defaultKey;
      const searchKeys = [];
      if (searchExports) searchKeys.push(...Object.keys(module.exports));
      else if (searchDefault && (defaultKey = getDefaultKey(module))) searchKeys.push(defaultKey);
      for (let j = 0; j < searchKeys.length; j++) {
        const key = searchKeys[j];
        const exported = module.exports[key];
        if (shouldSkipModule(exported)) continue;
        if (filter(exported, module, module.id)) {
          if (!defaultExport && defaultKey === key) {
            modules2.push(module.exports);
            continue;
          }
          modules2.push(raw ? module : exported);
        }
      }
    }
    if (fatal && modules2.length === 0) throw makeException();
    return modules2;
  }
  var init_searching = __esm({
    "src/InAccord/webpack/searching.ts"() {
      "use strict";
      init_shared();
      init_require();
    }
  });

  // src/InAccord/webpack/lazy.ts
  function getLazy(filter, options = {}) {
    const {signal: abortSignal, defaultExport = true, searchDefault = true, searchExports = false, raw = false, fatal = false} = options;
    if (abortSignal?.aborted) {
      if (fatal) return Promise.reject(makeException());
      return Promise.resolve(void 0);
    }
    const cached = getModule(filter, options);
    if (cached) return Promise.resolve(cached);
    filter = wrapFilter(filter);
    return new Promise((resolve, reject) => {
      const cancel = () => void lazyListeners.delete(listener);
      const listener = (_, module) => {
        if (shouldSkipModule(module.exports)) return;
        if (filter(module.exports, module, module.id)) {
          resolve(raw ? module : module.exports);
          cancel();
          return;
        }
        if (!searchExports && !searchDefault) return;
        let defaultKey;
        const searchKeys = [];
        if (searchExports) searchKeys.push(...Object.keys(module.exports));
        else if (searchDefault && (defaultKey = getDefaultKey(module))) searchKeys.push(defaultKey);
        for (let i = 0; i < searchKeys.length; i++) {
          const key = searchKeys[i];
          const exported = module.exports[key];
          if (shouldSkipModule(exported)) continue;
          if (filter(exported, module, module.id)) {
            if (!defaultExport && defaultKey === key) {
              resolve(raw ? module : module.exports);
              cancel();
              return;
            }
            resolve(raw ? module : exported);
            cancel();
          }
        }
      };
      lazyListeners.add(listener);
      abortSignal?.addEventListener("abort", () => {
        cancel();
        if (fatal) reject(makeException());
        else resolve(void 0);
      });
    });
  }
  async function forceLoad(id) {
    if (typeof webpackRequire.m[id] === "undefined") {
      return [];
    }
    const text = String(webpackRequire.m[id]);
    const loadedModules = [];
    let match;
    while ((match = CreatePromiseId.exec(text)) !== null) {
      const promiseBody = match[1];
      const bindId = match[2];
      const chunkIds2 = [];
      const chunkMatches = promiseBody.matchAll(ChunkIdRegex);
      for (const chunkMatch2 of chunkMatches) {
        chunkIds2.push(chunkMatch2[1]);
      }
      const finalId = parseInt(bindId, 10);
      await Promise.all(chunkIds2.map((cid) => webpackRequire.e(cid)));
      const loadedModule = webpackRequire(finalId);
      loadedModules.push(loadedModule);
    }
    const chunkIds = [];
    let chunkMatch;
    while ((chunkMatch = ChunkIdRegex.exec(text)) !== null) {
      chunkIds.push(chunkMatch[1]);
    }
    const bindMatches = text.matchAll(FinalModuleIdRegex);
    for (const bindMatch of bindMatches) {
      await Promise.all(chunkIds.map((cid) => webpackRequire.e(cid)));
      const loadedModule = webpackRequire(bindMatch[1]);
      loadedModules.push(loadedModule);
    }
    return loadedModules;
  }
  var ChunkIdRegex, FinalModuleIdRegex, CreatePromiseId;
  var init_lazy = __esm({
    "src/InAccord/webpack/lazy.ts"() {
      "use strict";
      init_searching();
      init_require();
      init_shared();
      ChunkIdRegex = /n\.e\("(\d+)"\)/g;
      FinalModuleIdRegex = /n\.bind\(n,\s*(\d+)\s*\)/g;
      CreatePromiseId = /createPromise:\s*\(\)\s*=>\s*([^}]+)\.then\(n\.bind\(n,\s*(\d+)\)\)/g;
    }
  });

  // src/InAccord/webpack/utilities.ts
  function* getWithKey(filter, {target = null, ...rest} = {}) {
    yield target ??= getModule(
      (exports) => Object.values(exports).some(filter),
      rest
    );
    yield target && Object.keys(target).find((k) => filter(target[k]));
  }
  function getById(id, options = {}) {
    const {raw, fatal} = options;
    const module = webpackRequire.c[id];
    if (!shouldSkipModule(module?.exports)) {
      return raw ? module : module.exports;
    }
    if (fatal) {
      throw makeException();
    }
    return void 0;
  }
  function mapObject(module, mappers) {
    const mapped = {};
    const moduleKeys = Object.keys(module);
    const mapperKeys = Object.keys(mappers);
    for (let i = 0; i < moduleKeys.length; i++) {
      const searchKey = moduleKeys[i];
      if (!Object.prototype.hasOwnProperty.call(module, searchKey)) continue;
      for (let j = 0; j < mapperKeys.length; j++) {
        const key = mapperKeys[j];
        if (!Object.prototype.hasOwnProperty.call(mappers, key)) continue;
        if (Object.prototype.hasOwnProperty.call(mapped, key)) continue;
        if (mappers[key](module[searchKey])) {
          Object.defineProperty(mapped, key, {
            get() {
              return module[searchKey];
            },
            set(value) {
              module[searchKey] = value;
            },
            enumerable: true,
            configurable: false
          });
        }
      }
    }
    for (let i = 0; i < mapperKeys.length; i++) {
      const key = mapperKeys[i];
      if (!Object.prototype.hasOwnProperty.call(mapped, key)) {
        Object.defineProperty(mapped, key, {
          value: void 0,
          enumerable: true,
          configurable: false
        });
      }
    }
    Object.defineProperty(mapped, /* @__PURE__ */ Symbol("inaccord.getMangled"), {
      value: module,
      configurable: false
    });
    return mapped;
  }
  function getMangled(filter, mappers, options = {}) {
    const {raw = false, ...rest} = options;
    if (typeof filter === "string" || filter instanceof RegExp) {
      filter = bySource(filter);
    }
    let module = typeof filter === "number" ? getById(filter) : getModule(filter, {raw, ...rest});
    if (!module) return {};
    if (raw) module = module.exports;
    return mapObject(module, mappers);
  }
  function getBulk(...queries) {
    const returnedModules = Array(queries.length);
    queries = queries.map((query) => ({
      ...query,
      filter: wrapFilter(query.filter)
    }));
    const shouldExitEarly = queries.every((m) => !m.all);
    const shouldExit = () => shouldExitEarly && queries.every((query, index) => !query.all && index in returnedModules);
    if (queries.length === 0) return returnedModules;
    const webpackModules = Object.values(webpackRequire.c);
    webpack: for (let i = 0; i < webpackModules.length; i++) {
      const module = webpackModules[i];
      if (shouldSkipModule(module.exports)) continue;
      queries: for (let index = 0; index < queries.length; index++) {
        const {filter, all = false, defaultExport = true, searchExports = false, searchDefault = true, raw = false, map} = queries[index];
        if (!all && index in returnedModules) {
          continue;
        }
        if (filter(module.exports, module, module.id)) {
          const trueItem = map ? mapObject(module.exports, map) : raw ? module : module.exports;
          if (!all) {
            returnedModules[index] = trueItem;
            if (shouldExit()) break webpack;
            continue;
          }
          returnedModules[index] ??= [];
          returnedModules[index].push(trueItem);
        }
        let defaultKey;
        const exportKeys = [];
        if (searchExports) exportKeys.push(...Object.keys(module.exports));
        else if (searchDefault && (defaultKey = getDefaultKey(module))) exportKeys.push(defaultKey);
        for (const key of exportKeys) {
          const exported = module.exports[key];
          if (shouldSkipModule(exported)) continue;
          if (filter(exported, module, module.id)) {
            let value;
            if (!defaultExport && defaultKey === key) {
              value = map ? mapObject(module.exports, map) : raw ? module : module.exports;
            } else {
              value = map ? mapObject(raw ? module.exports : exported, map) : raw ? module : exported;
            }
            if (!all) {
              returnedModules[index] = value;
              if (shouldExit()) break webpack;
              continue queries;
            }
            returnedModules[index] ??= [];
            returnedModules[index].push(value);
          }
        }
      }
    }
    for (let index = 0; index < queries.length; index++) {
      const query = queries[index];
      const exists2 = index in returnedModules;
      if (query.fatal) {
        if (query.all && (!Array.isArray(returnedModules[index]) || returnedModules[index].length === 0)) {
          throw makeException();
        }
        if (!exists2) throw makeException();
      }
      if (query.map && !exists2) {
        returnedModules[index] = {};
      }
    }
    return returnedModules;
  }
  function getBulkKeyed(queries) {
    const modules2 = getBulk(...Object.values(queries));
    return Object.fromEntries(
      Object.keys(queries).map((key, index) => [key, modules2[index]])
    );
  }
  var init_utilities = __esm({
    "src/InAccord/webpack/utilities.ts"() {
      "use strict";
      init_filter();
      init_searching();
      init_shared();
      init_require();
    }
  });

  // src/InAccord/webpack/webpack.ts
  function getByKeys(keys2, options = {}) {
    return getModule(byKeys(keys2), options);
  }
  function getByPrototypes(prototypes, options = {}) {
    return getModule(byPrototypeKeys(prototypes), options);
  }
  function getLazyByPrototypes(prototypes, options = {}) {
    return getLazy(byPrototypeKeys(prototypes), options);
  }
  function getByStrings(strings, options = {}) {
    return getModule(byStrings(...strings), options);
  }
  function getLazyByStrings(strings, options = {}) {
    return getLazy(byStrings(...strings), options);
  }
  function getBySource(sources, options = {}) {
    return getModule(bySource(...sources), options);
  }
  function getLazyBySource(sources, options = {}) {
    return getLazy(bySource(...sources), options);
  }
  function getByDisplayName(name, options = {}) {
    return getModule(byDisplayName(name), options);
  }
  var init_webpack = __esm({
    "src/InAccord/webpack/webpack.ts"() {
      "use strict";
      init_filter();
      init_searching();
      init_lazy();
    }
  });

  // src/InAccord/webpack/stores.ts
  function getStore(name) {
    if (!Flux) Flux = getModule((m) => m.Store?.getAll);
    if (!Flux) return getModule(filter_exports.byStoreName(name));
    return Flux.Store.getAll().find((store) => store.getName() === name);
  }
  var Flux, Stores;
  var init_stores = __esm({
    "src/InAccord/webpack/stores.ts"() {
      "use strict";
      init_webpack2();
      Stores = new Proxy({}, {
        ownKeys() {
          if (!Flux) Flux = getModule((m) => m.Store?.getAll);
          if (!Flux) return [];
          return [...new Set(Flux.Store.getAll().map((store) => store.getName()).filter((m) => m.length > 3))];
        },
        getOwnPropertyDescriptor() {
          return {
            enumerable: true,
            configurable: true
            // Not actually
          };
        },
        get(target, key) {
          if (typeof target[key] === "undefined") return target[key] = getStore(key);
          return target[key];
        },
        set() {
          throw new Error("[WebpackModules~Stores] Setting stores is not allowed.");
        }
      });
      Object.entries(Stores);
    }
  });

  // src/InAccord/webpack/index.ts
  var init_webpack2 = __esm({
    "src/InAccord/webpack/index.ts"() {
      "use strict";
      init_filter();
      init_searching();
      init_lazy();
      init_utilities();
      init_require();
      init_webpack();
      init_stores();
    }
  });

  // src/InAccord/modules/discordmodules.ts
  var DiscordModules, discordmodules_default;
  var init_discordmodules = __esm({
    "src/InAccord/modules/discordmodules.ts"() {
      "use strict";
      init_utils();
      init_webpack2();
      DiscordModules = memoizeObject({
        get React() {
          return getByKeys(["createElement", "cloneElement"]);
        },
        get ReactDOM() {
          return Object.assign({}, getByKeys(["createPortal"]), getByKeys(["createRoot"]));
        },
        get ReactSpring() {
          return getByKeys(["useTransition", "animated"]);
        },
        get ChannelActions() {
          return getByKeys(["selectChannel"]);
        },
        get LocaleStore() {
          return getStore("LocaleStore");
        },
        get UserStore() {
          return getStore("UserStore");
        },
        get InviteActions() {
          return getByKeys(["createInvite"]);
        },
        get SimpleMarkdown() {
          return getByKeys(["parseBlock", "parseInline", "defaultOutput"]);
        },
        get Strings() {
          return getByKeys(["Messages"])?.Messages;
        },
        get Dispatcher() {
          return getByKeys(["dispatch", "subscribe", "register"]);
        },
        get Tooltip() {
          const fallback = (props) => props.children?.({}) ?? null;
          return getModule(filter_exports.byPrototypeKeys(["renderTooltip"]), {searchExports: true}) ?? fallback;
        },
        get promptToUpload() {
          return getByStrings(["getUploadCount", ".UPLOAD_FILE_LIMIT_ERROR"], {searchExports: true});
        },
        get RemoteModule() {
          return getByKeys(["setBadge"]);
        },
        get UserAgentInfo() {
          return getByKeys(["os", "layout"]);
        },
        get GetClientInfo() {
          return getByStrings(["versionHash"]);
        },
        get MessageUtils() {
          return getByKeys(["sendMessage"]);
        }
      });
      discordmodules_default = DiscordModules;
    }
  });

  // src/InAccord/modules/react.ts
  var React, react_default, ReactDOM, Children, Component, Fragment, cloneElement, createContext, createElement, createRef, forwardRef, lazy, memo, startTransition, useCallback, useContext, useDebugValue, useDeferredValue, useEffect, useId, useImperativeHandle, useInsertionEffect, useLayoutEffect, useMemo, useReducer, useRef, useState, useSyncExternalStore, useTransition;
  var init_react = __esm({
    "src/InAccord/modules/react.ts"() {
      "use strict";
      init_discordmodules();
      React = discordmodules_default.React;
      react_default = React;
      ReactDOM = discordmodules_default.ReactDOM;
      ({Children, Component, Fragment, cloneElement, createContext, createElement, createRef, forwardRef, lazy, memo, startTransition, useCallback, useContext, useDebugValue, useDeferredValue, useEffect, useId, useImperativeHandle, useInsertionEffect, useLayoutEffect, useMemo, useReducer, useRef, useState, useSyncExternalStore, useTransition} = React);
    }
  });

  // src/InAccord/stores/json.ts
  var json_default;
  var init_json = __esm({
    "src/InAccord/stores/json.ts"() {
      "use strict";
      init_fs();
      init_path();
      init_base();
      init_config();
      init_logger();
      init_react();
      json_default = new class JsonStore extends Store {
        cache = {
          "settings": void 0,
          "backup": void 0,
          "plugins": void 0,
          "themes": void 0,
          "misc": void 0,
          "addon-store": void 0
        };
        pluginCache = {};
        pluginListeners = /* @__PURE__ */ new Map();
        get(file, key) {
          this.cache[file] = this.#ensureData(file);
          if (typeof key === "undefined") return this.cache[file] ?? {};
          return this.cache[file][key] ?? "";
        }
        set(file, key, value) {
          this.cache[file] = this.#ensureData(file);
          if (typeof value === "undefined") {
            if (typeof key === "string") throw new Error("Cannot save string as JSON");
            this.cache[file] = key;
          } else {
            if (typeof key !== "string") throw new Error("Cannot use object as key");
            this.cache[file][key] = value;
          }
          this.#save(file);
        }
        delete(file, key) {
          this.cache[file] = this.#ensureData(file);
          delete this.cache[file][key];
          this.#save(file);
        }
        #ensureData(file) {
          if (typeof this.cache[file] !== "undefined") return this.cache[file];
          let data;
          try {
            data = JSON.parse(fs_default.readFileSync(path_default.resolve(config_default.get("channelPath"), `${file}.json`)).toString());
          } catch {
            data = {};
          }
          return data;
        }
        #save(file) {
          fs_default.writeFileSync(path_default.resolve(config_default.get("channelPath"), `${file}.json`), JSON.stringify(this.cache[file], null, 4));
          this.emitChange();
        }
        // Plugin data
        #getPluginFile(pluginName) {
          return path_default.resolve(config_default.get("pluginsPath"), pluginName + ".config.json");
        }
        #ensurePluginData(pluginName) {
          if (typeof this.pluginCache[pluginName] !== "undefined") return;
          if (!fs_default.existsSync(this.#getPluginFile(pluginName))) return this.pluginCache[pluginName] = {};
          try {
            this.pluginCache[pluginName] = JSON.parse(fs_default.readFileSync(this.#getPluginFile(pluginName)).toString());
          } catch {
            return this.pluginCache[pluginName] = {};
          }
        }
        recache(pluginName) {
          this.#ensurePluginData(pluginName);
          const before = this.pluginCache[pluginName];
          try {
            this.pluginCache[pluginName] = JSON.parse(fs_default.readFileSync(this.#getPluginFile(pluginName)).toString());
            this.emitChange();
            return true;
          } catch (err) {
            Logger.err("JsonStore", "recache: ", err);
            return false;
          } finally {
            const after = this.pluginCache[pluginName];
            const beforeKeys = Object.keys(before);
            const afterKeys = Object.keys(after);
            const beforeSet = new Set(beforeKeys);
            const afterSet = new Set(afterKeys);
            const result = {
              deleted: [],
              changed: []
            };
            for (const k of beforeSet) {
              if (afterSet.has(k)) result.changed.push(k);
              else result.deleted.push(k);
            }
            for (const k of afterSet) {
              if (!beforeSet.has(k)) {
                result.changed.push(k);
              }
            }
            for (const key of result.changed) {
              this.emitPluginChangeListeners(pluginName, key, after[key]);
            }
            for (const key of result.deleted) {
              this.emitPluginChangeListeners(pluginName, key);
            }
          }
        }
        #savePluginData(pluginName) {
          fs_default.writeFileSync(this.#getPluginFile(pluginName), JSON.stringify(this.pluginCache[pluginName], null, 4));
          this.emitChange();
        }
        getData(pluginName, key) {
          this.#ensurePluginData(pluginName);
          return this.pluginCache[pluginName][key];
        }
        useData(pluginName, key) {
          const [state, setState] = useState(() => this.getData(pluginName, key));
          useInsertionEffect(() => {
            const listener = () => setState(() => this.getData(pluginName, key));
            listener();
            return this.addPluginChangeListener(pluginName, listener, key);
          }, []);
          return state;
        }
        setData(pluginName, key, value) {
          if (value === void 0) return;
          this.#ensurePluginData(pluginName);
          this.pluginCache[pluginName][key] = value;
          this.#savePluginData(pluginName);
          this.emitPluginChangeListeners(pluginName, key, value);
        }
        deleteData(pluginName, key) {
          this.#ensurePluginData(pluginName);
          delete this.pluginCache[pluginName][key];
          this.#savePluginData(pluginName);
          this.emitPluginChangeListeners(pluginName, key);
        }
        emitPluginChangeListeners(pluginName, key, newData) {
          if (!this.pluginListeners.has(pluginName)) return;
          const pluginListeners = this.pluginListeners.get(pluginName);
          for (const element of pluginListeners.all) {
            if (arguments.length === 3) {
              element(key, newData);
            } else {
              element(key);
            }
          }
          if (!pluginListeners.keys.has(key)) return;
          const listeners = pluginListeners.keys.get(key);
          for (const element of listeners) {
            if (arguments.length === 3) {
              element(newData);
            } else {
              element();
            }
          }
        }
        addPluginChangeListener(pluginName, callback, key) {
          if (!this.pluginListeners.has(pluginName)) {
            this.pluginListeners.set(pluginName, {
              keys: /* @__PURE__ */ new Map(),
              all: /* @__PURE__ */ new Set()
            });
          }
          const listeners = this.pluginListeners.get(pluginName);
          if (typeof key === "string") {
            if (!listeners.keys.has(key)) listeners.keys.set(key, /* @__PURE__ */ new Set());
            const keyListeners = listeners.keys.get(key);
            keyListeners.add(callback);
            return () => void keyListeners.delete(callback);
          }
          listeners.all.add(callback);
          return () => void listeners.all.delete(callback);
        }
        removePluginChangeListener(pluginName, callback, key) {
          if (!this.pluginListeners.has(pluginName)) return;
          const listeners = this.pluginListeners.get(pluginName);
          if (typeof key === "string") {
            if (!listeners.keys.has(key)) return;
            listeners.keys.get(key).delete(callback);
            return;
          }
          listeners.all.delete(callback);
        }
      }();
    }
  });

  // assets/locales/en-us.json
  var require_en_us = __commonJS({
    "assets/locales/en-us.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plugins",
          themes: "Themes",
          customcss: "Custom CSS",
          updates: "Updates",
          managerhub: "Manager Hub",
          backup: "Backup",
          backups: "Backups",
          tools: "Tools",
          github: "GitHub",
          ourserver: "Our Server"
        },
        Collections: {
          settings: {
            name: "Settings",
            general: {
              name: "General",
              voiceDisconnect: {
                name: "Voice Disconnect",
                note: "Disconnect from voice server when closing Discord"
              },
              showToasts: {
                name: "Show Toasts",
                note: "Shows a small notification for important information"
              },
              mediaKeys: {
                name: "Disable Media Keys",
                note: "Prevents Discord from hijacking your media keys after playing a video."
              },
              iaContextMenu: {
                name: "Settings Context Menu",
                note: "Adds a InAccord subsection to the settings context menu."
              },
              themeAttributes: {
                name: "Theme Attributes",
                note: "Adds attributes to various elements to assist in theming."
              },
              notificationPosition: {
                name: "Notification",
                note: "Lets you choose where you want notifications to popup."
              },
              notificationEnabled: {
                name: "Show Notifications",
                note: "Prevents or allows notifications to be shown on screen."
              }
            },
            store: {
              name: "Addon Store",
              iaAddonStore: {
                name: "Enable Addon Store",
                note: "Enables the InAccord Addon Store."
              },
              alwaysEnable: {
                name: "Always Enable",
                note: "When enabled by default addons downloaded via the addon store will be enabled."
              },
              addonEmbeds: {
                name: "Addon Embeds",
                note: "Embeds the addon store cards into chat."
              }
            },
            window: {
              name: "Window Preferences",
              removeMinimumSize: {
                name: "Remove Minimum Size",
                note: "Removes Discord's forced minimum window size of 940x500"
              },
              transparency: {
                name: "Enable Transparency",
                note: "Enables the mian window to be see-through (requires restart)"
              },
              frame: {
                name: "Window Frame",
                note: "Adds the native os window frame to the mian window"
              },
              inAppTrafficLights: {
                name: "Use In App Traffic Lights",
                note: "Instead of using the OS traffic lights it will use a in app one"
              }
            },
            addons: {
              name: "Addon Manager",
              addonErrors: {
                name: "Show Addon Errors",
                note: "Shows a modal with plugin/theme errors"
              },
              editAction: {
                name: "Edit Action",
                note: "Where plugins & themes appear when editing",
                options: {
                  detached: "Detached Window",
                  external: "External Window",
                  system: "System Editor"
                }
              },
              checkForUpdates: {
                name: "Automatically Check For Updates",
                note: "Periodically checks for updates to your addons and core ia"
              },
              updateInterval: {
                name: "Update Check Interval",
                note: "How often to check for addon/core updates"
              }
            },
            customcss: {
              name: "Custom CSS",
              customcss: {
                name: "Custom CSS",
                note: "Enables the Custom CSS tab"
              },
              liveUpdate: {
                name: "Live Update",
                note: "Updates the css as you type"
              },
              startDetached: {
                name: "Start Detached",
                note: "Clicking the Custom CSS tab opens the editor in a separate window"
              },
              nativeOpen: {
                name: "Open in Native Editor",
                note: "Clicking the Custom CSS tab opens your custom css in your native editor"
              },
              openAction: {
                name: "Editor Location",
                note: "Where Custom CSS should open by default",
                options: {
                  settings: "Settings Menu",
                  detached: "Detached Window",
                  external: "External Window",
                  system: "System Editor"
                }
              }
            },
            developer: {
              name: "Developer Options",
              debuggerHotkey: {
                name: "Debugger Hotkey",
                note: "Allows activating debugger when pressing F8 with DevTools open"
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Injects your local installation of React Developer Tools into Discord"
              },
              inspectElement: {
                name: "Inspect Element Hotkey",
                note: "Enables the inspect element hotkey (ctrl + shift + c) that is common in most browsers"
              },
              devToolsWarning: {
                name: "Stop DevTools Warning",
                note: 'Stops Discord from printing out their "Hold Up!" message'
              },
              debugLogs: {
                name: "Debug Logs",
                note: "Outputs everything from the console into the debug.log file in the InAccord folder"
              },
              devTools: {
                name: "DevTools",
                note: "Enables toggling DevTools with ctrl+shift+i"
              },
              recovery: {
                name: "Recovery",
                note: "Enables the ability to attempt to save discord without reloading",
                button: "Attempt to recover Discord",
                report: "Report on Github",
                safeMode: "Relaunch in Safe Mode"
              }
            },
            editor: {
              name: "Editor Options",
              theme: {
                name: "Theme",
                note: "The theme for the code editor",
                options: {
                  system: "System",
                  "vs-dark": "Dark",
                  vs: "Light",
                  "hc-black": "High Contrast Black",
                  "hc-light": "High Contrast Light"
                }
              },
              lineNumbers: {
                name: "Line Numbers",
                note: "Enables showing line numbers on the side of the editor"
              },
              fontSize: {
                name: "Font Size",
                note: "Size of the font (pt) to use in the editor"
              },
              minimap: {
                name: "Minimap",
                note: "Enables showing the code minimap on the side of the editor"
              },
              hover: {
                name: "Reference Tooltips",
                note: "Enables showing the reference tooltips when hovering rules and selectors"
              },
              quickSuggestions: {
                name: "Quick Suggestions",
                note: "Enables showing the autocomplete suggestions as you type"
              },
              insertSpaces: {
                name: "Insert Spaces",
                note: "Insert spaces instead of tabs"
              },
              tabSize: {
                name: "Tab Size",
                note: "The number of spaces a tab is equal to"
              },
              renderWhitespace: {
                name: "Show Whitespace",
                note: "When whitespace should be shown by the editor",
                options: {
                  all: "Always",
                  none: "Never",
                  selection: "Selection"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} by {{author}}",
          byline: "by {{author}}",
          openFolder: {
            plugin: "Open Plugin Folder",
            theme: "Open Theme Folder"
          },
          openStore: {
            plugin: "Open Plugin Store",
            theme: "Open Theme Store"
          },
          storeMessage: {
            plugin: "Browse and install official community plugins",
            theme: "Browse and install official community themes"
          },
          store: "Store",
          viewInstalled: {
            plugin: "View Installed Plugins",
            theme: "View Installed Themes"
          },
          reload: "Reload",
          addonSettings: "Settings",
          website: "Website",
          source: "Source",
          invite: "Support Server",
          donate: "Donate",
          patreon: "Patreon",
          name: "Name",
          author: "Author",
          version: "Version",
          added: "Date Added",
          modified: "Date Modified",
          releaseDate: "Release Date",
          lastUpdated: "Last Update",
          isEnabled: "Enabled",
          popularity: "Popularity",
          isInstalled: "Installed",
          downloads: "Downloads",
          likes: "Likes",
          listView: "List View",
          gridView: "Grid View",
          enableAll: "Enable All",
          disableAll: "Disable All",
          enableAllWarning: {
            plugin: "Enabling all plugins can cause temporary lag and unexpected errors.\n\n(Hold shift while clicking to skip this prompt!)",
            theme: "Enabling all themes can cause temporary lag and unexpected errors.\n\n(Hold shift while clicking to skip this prompt!)"
          },
          search: {
            plugin: {
              zero: "No plugins to search",
              one: "Search one plugin",
              other: "Search {{count}} plugins"
            },
            theme: {
              zero: "No themes to search",
              one: "Search one theme",
              other: "Search {{count}} themes"
            }
          },
          results: {
            zero: "No Results",
            one: "{{count}} Result",
            other: "{{count}} Results"
          },
          editAddon: "Edit",
          deleteAddon: "Delete",
          downloadAddon: "Download",
          confirmDelete: "Are you sure you want to delete {{name}}?",
          confirmationText: "You have unsaved changes to {{name}}. Closing this window will lose all those changes.",
          enabled: "{{name}} has been enabled.",
          disabled: "{{name}} has been disabled.",
          couldNotEnable: "{{name}} could not be enabled.",
          couldNotDisable: "{{name}} could not be disabled.",
          couldNotStart: "{{name}} could not be started.",
          couldNotStop: "{{name}} could not be stopped.",
          settingsError: "Could not open settings for {{name}}",
          methodError: "{{method}} could not be fired.",
          unknownAuthor: "Unknown Author",
          noDescription: "Description not provided.",
          alreadyExists: {
            plugin: "There is already a plugin with name {{name}}",
            theme: "There is already a theme with name {{name}}"
          },
          metaError: "META could not be parsed.",
          missingNameData: "META missing name data.",
          metaNotFound: "META was not found.",
          compileError: "Could not be compiled. See console for detials.",
          wasUnloaded: "{{name}} was unloaded.",
          blankSlateHeader: {
            plugin: "You don't have any plugins!",
            theme: "You don't have any themes!"
          },
          blankSlateMessage: {
            plugin: "Grab some from [our website]({{link}}) and add them to your plugin folder.",
            theme: "Grab some from [our website]({{link}}) and add them to your theme folder."
          },
          blankSlateHeaderGeneric: "You don't have anything!",
          blankSlateMessageGeneric: "You should probably get some.",
          wasLoaded: "{{name}} v{{version}} was loaded.",
          fialedToDownload: {
            plugin: "Fialed to download plugin {{name}}",
            theme: "Fialed to download theme {{name}}"
          },
          successfullyDownload: "Successfully downloaded {{name}}",
          alreadyInstalled: "{{name}} is already installed",
          fialedToFetch: "Unable to get addons",
          new: "New",
          updated: "Updated",
          downloadCount: {
            one: "{{count}} Download",
            other: "{{count}} Downloads"
          },
          likeCount: {
            one: "{{count}} Like",
            other: "{{count}} Likes"
          },
          tags: "Tags",
          preview: "Preview",
          official: "Official",
          manyEnabled: {
            plugin: {
              one: "One plugin has been enabled.",
              other: "{{count}} plugins have been enabled."
            },
            theme: {
              one: "One theme has been enabled.",
              other: "{{count}} themes have been enabled."
            }
          }
        },
        CustomCSS: {
          confirmationText: "You have unsaved changes to your Custom CSS. Closing this window will lose all those changes.",
          update: "Update",
          save: "Save",
          openNative: "Open in System Editor",
          openDetached: "Detach Window",
          settings: "Editor Settings",
          editorTitle: "Custom CSS Editor",
          cssInstallSuccess: "CSS was successfully added into CustomCSS.",
          cssInstalled: "CSS installed!",
          cssInstallError: "CSS fialed to be added into CustomCSS.",
          cssReverted: "CSS was reverted.",
          cssReverting: "Changes will be reverted in 10 seconds",
          cssKept: "CSS changes kept permanently"
        },
        Modals: {
          confirmAction: "Are You Sure?",
          installCss: "Are you sure you want to add this CSS to your Custom CSS?",
          addonCrashed: "This plugin has been disabled to prevent crashes. Please report this issue to the developer.",
          okay: "Okay",
          done: "Done",
          cancel: "QUIT",
          nevermind: "Nevermind",
          close: "QUIT",
          name: "Name",
          message: "Message",
          error: "Error",
          addonErrors: "Addon Errors",
          restartRequired: "Restart Required",
          restartNow: "Restart Now",
          restartLater: "Restart Later",
          additionalInfo: "Additional Info",
          restartPrompt: "In order to take effect, Discord needs to be restarted. Do you want to restart now?",
          changelog: "Changelog",
          debuglog: "Your debug log file has exceeded 100MB, would you like to clear the log?",
          automaticallyEnable: "Automatically Enable"
        },
        Sorting: {
          sortBy: "Sort By",
          order: "Order",
          ascending: "Ascending",
          descending: "Descending"
        },
        WindowPrefs: {
          enabledInfo: "This option requires a transparent theme in order to work properly. On Windows this may break your aero snapping and maximizing.\n\nIn order to take effect, Discord needs to be restarted. Do you want to restart now?",
          disabledInfo: "In order to take effect, Discord needs to be restarted. Do you want to restart now?"
        },
        Toasts: {
          recoveryFialed: "Recovery incomplete. Some actions fialed to complete."
        },
        Updater: {
          updateFialed: "Update Fialed!",
          updateFialedMessage: "InAccord fialed to update. Please download the latest version of the installer from our website (https://InAccord.app/) and reinstall.",
          updateSuccessful: "Update Successful!",
          updateAvialable: "InAccord has a new update (v{{version}})",
          viewUpdates: "View Updates",
          addonUpdaterNotificationTitle: "Addon Updater",
          addonUpdatesAvialable: {
            plugin: {
              one: "One plugin has an update!",
              other: "{{count}} plugins have updates!"
            },
            theme: {
              one: "One theme has an update!",
              other: "{{count}} themes have updates!"
            }
          },
          addonUpdated: "{{name}} has been updated to version {{version}}!",
          addonUpdateFialed: "Fialed to update {{name}}. Please try agian later.",
          checking: "Checking for updates!",
          finishedChecking: "Finished checking for updates!",
          checkForUpdates: "Check For Updates!",
          updateAll: "Update All!",
          noUpdatesAvialable: "No updates avialable.",
          versionAvialable: "Version {{version}} now avialable!",
          upToDateBlankslate: {
            plugin: "All of your plugins seem to be up to date!",
            theme: "All of your themes seem to be up to date!"
          },
          updateButton: "Update!"
        },
        Settings: {
          customColor: "Custom Color",
          resetSettings: "Reset Settings",
          resetSettingsWarning: "Resetting your settings returns them all to the original state, this cannot be undone."
        },
        Socials: {
          twitter: "Follow Us!",
          github: "Star Us!"
        },
        Paginator: {
          back: "Back",
          next: "Next"
        }
      };
    }
  });

  // assets/locales/en-gb.json
  var require_en_gb = __commonJS({
    "assets/locales/en-gb.json"(exports, module) {
      module.exports = {};
    }
  });

  // assets/locales/zh-cn.json
  var require_zh_cn = __commonJS({
    "assets/locales/zh-cn.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "插件",
          themes: "主题",
          customcss: "自定义样式"
        },
        Collections: {
          settings: {
            name: "设置",
            general: {
              name: "一般",
              voiceDisconnect: {
                name: "自动断开语音",
                note: "自动在关闭 Discord 时断开语音"
              },
              showToasts: {
                name: "显示弹出提示",
                note: "在有重要信息时弹出小通知"
              },
              mediaKeys: {
                name: "禁用媒体控制键",
                note: "防止 Discord 在播放视频时劫持媒体控制键"
              }
            },
            window: {
              removeMinimumSize: {
                name: "移除最小化限制",
                note: "移除 Discord 限制的最小窗口尺寸（940x500）"
              },
              name: "窗口设置",
              transparency: {
                name: "透明化",
                note: "允许主窗口透明（需要重启以生效）"
              },
              frame: {
                name: "窗口框",
                note: "向主窗口增加原生系统窗口框"
              }
            },
            addons: {
              name: "插件管理",
              addonErrors: {
                name: "显示插件错误",
                note: "在插件/主题发生错误时显示弹窗"
              },
              editAction: {
                name: "编辑器",
                note: "在何处编辑插件及主题",
                options: {
                  detached: "独立窗口",
                  system: "系统编辑器"
                }
              }
            },
            customcss: {
              name: "自定义样式",
              customcss: {
                name: "自定义样式",
                note: "启用自定义样式选项卡"
              },
              liveUpdate: {
                name: "热更新",
                note: "自动在 CSS 修改时应用变更"
              },
              startDetached: {
                name: "在独立窗口中打开",
                note: "在独立窗口中编辑自定义样式"
              },
              nativeOpen: {
                name: "在系统编辑器中打开",
                note: "在系统编辑器中编辑自定义样式"
              },
              openAction: {
                name: "编辑器",
                note: "在何处编辑自定义样式",
                options: {
                  settings: "设置菜单",
                  detached: "独立窗口",
                  system: "系统编辑器"
                }
              }
            },
            developer: {
              name: "开发者设置",
              debuggerHotkey: {
                name: "调试器快捷键",
                note: "按下 F8 时启动调试器"
              },
              reactDevTools: {
                name: "React 开发者工具",
                note: "向 Discord 注入 React 开发者工具"
              },
              inspectElement: {
                name: "元素审查快捷键",
                note: "启用在大多数浏览器中常用的元素审查快捷键（Ctrl + Shift + C）"
              },
              devToolsWarning: {
                name: "屏蔽开发者工具警告",
                note: "禁止 Discord 打印「Hold Up!」警告"
              },
              debugLogs: {
                name: "调试日志",
                note: "输出控制台中的所有信息到 InAccord 文件夹下的 debug.log 文件"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} 版本：{{version}} 作者：{{author}}",
          byline: "作者： {{author}}",
          reload: "重新加载",
          addonSettings: "设置",
          website: "网站",
          source: "源码",
          invite: "支持服务器",
          donate: "捐赠",
          patreon: "Patreon",
          name: "名称",
          author: "作者",
          version: "版本",
          added: "安装时间",
          modified: "最后更新时间",
          editAddon: "编辑",
          deleteAddon: "删除",
          confirmDelete: "你真的要删除{{name}}吗？",
          confirmationText: "你还有未保存的更改，关闭此窗口将失去所有对{{name}}所做的更改。",
          enabled: "已启用{{name}}",
          disabled: "已禁用{{name}}",
          couldNotEnable: "无法启用{{name}}。",
          couldNotDisable: "无法禁用{{name}}。",
          couldNotStart: "无法启动{{name}}。",
          couldNotStop: "无法停止{{name}}。",
          settingsError: "无法打开{{name}}设置",
          methodError: "无法调用{{method}}。",
          unknownAuthor: "未知的作者",
          noDescription: "作者没有留下任何描述",
          metaError: "无法解析元数据",
          missingNameData: "元数据缺少名称。",
          metaNotFound: "无法找到元数据",
          compileError: "无法编译。",
          wasUnloaded: "已卸载{{name}}。"
        },
        CustomCSS: {
          confirmationText: "你还有未保存的更改，关闭此窗口将失去所有对自定义样式所做的更改。",
          update: "更新",
          save: "保存",
          openNative: "在系统编辑器中打开",
          openDetached: "在独立窗口中打开",
          settings: "编辑器设置",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "你确定吗？",
          okay: "确定",
          done: "完成",
          cancel: "取消",
          nevermind: "别在意",
          close: "关闭",
          name: "名称",
          message: "消息",
          error: "错误",
          addonErrors: "插件错误",
          restartRequired: "需要重启以生效",
          restartNow: "立即重启",
          restartLater: "稍候自行重启",
          additionalInfo: "额外信息",
          restartPrompt: "Discord 需要重新启动以应用最新变更。立刻进行重启吗？"
        },
        Sorting: {
          sortBy: "排序方式",
          order: "顺序",
          ascending: "正序",
          descending: "倒序"
        },
        WindowPrefs: {
          enabledInfo: "此选项需要透明主题才能正常工作。在 Windows 上，这可能会破坏 AERO 的捕捉和最大化。\n\n该变更需要重启以生效。您要立即进行重启吗？",
          disabledInfo: "Discord 需要重新启动以应用最新变更。立刻进行重启吗？"
        }
      };
    }
  });

  // assets/locales/zh-tw.json
  var require_zh_tw = __commonJS({
    "assets/locales/zh-tw.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "擴充功能",
          themes: "佈景主題",
          customcss: "客製化 CSS",
          updates: "更新"
        },
        Collections: {
          settings: {
            name: "設定",
            general: {
              name: "一般",
              voiceDisconnect: {
                name: "自動中斷語音連線",
                note: "自動在關閉 Discord 時中斷語音連線"
              },
              showToasts: {
                name: "顯示彈出提示",
                note: "在有重要通知時彈出提示"
              },
              mediaKeys: {
                name: "停用媒體控制鍵",
                note: "防止 Discord 在播放影片時劫持媒體控制鍵"
              }
            },
            window: {
              removeMinimumSize: {
                name: "移除最小視窗尺寸限制",
                note: "移除 Discord 限制的最小視窗尺寸（940x500）"
              },
              name: "視窗設定",
              transparency: {
                name: "透明化",
                note: "啟用主視窗透明（需要重新啟動以生效）"
              },
              frame: {
                name: "視窗邊框",
                note: "向主視窗新增原版系統視窗邊框"
              }
            },
            addons: {
              name: "擴充功能管理員",
              addonErrors: {
                name: "顯示擴充功能錯誤",
                note: "在擴充功能/佈景主題發生錯誤時顯示彈出視窗"
              },
              editAction: {
                name: "編輯器",
                note: "在何處編輯擴充功能及佈景主題",
                options: {
                  detached: "獨立視窗",
                  system: "系統編輯器"
                }
              }
            },
            customcss: {
              name: "客製化 CSS",
              customcss: {
                name: "客製化 CSS",
                note: "啟用客製化 CSS 選項標籤"
              },
              liveUpdate: {
                name: "實時更新",
                note: "自動在 CSS 發生變更時重新載入"
              },
              startDetached: {
                name: "在獨立視窗中開啟",
                note: "在獨立視窗中編輯客製化 CSS"
              },
              nativeOpen: {
                name: "在系統編輯器中開啟",
                note: "在系統編輯器中編輯客製化 CSS"
              },
              openAction: {
                name: "編輯器",
                note: "在何處編輯客製化 CSS",
                options: {
                  settings: "設定選單",
                  detached: "獨立視窗",
                  system: "系統編輯器"
                }
              }
            },
            developer: {
              name: "開發人員設定",
              debuggerHotkey: {
                name: "偵錯工具快捷鍵",
                note: "允許在打開開發人員工具的情況下按下 F8 啟動偵錯工具"
              },
              reactDevTools: {
                name: "React 開發人員工具",
                note: "向 Discord 注入 React 開發人員工具"
              },
              inspectElement: {
                name: "元素檢查快捷鍵",
                note: "啟用在大多數瀏覽器中常用的元素檢查快捷鍵（Ctrl + Shift + C）"
              },
              devToolsWarning: {
                name: "隱藏開發人員工具警告",
                note: "禁止 Discord 輸出「Hold Up!」警告"
              },
              debugLogs: {
                name: "偵錯記錄檔",
                note: "輸出控制台中的所有資訊到 InAccord 資料夾下的 debug.log"
              },
              devTools: {
                name: "DevTools",
                note: "啟用切換 DevTools（Ctrl+Shift+I）"
              }
            },
            editor: {
              name: "編輯器偏好",
              lineNumbers: {
                name: "行數編號",
                note: "啟用在編輯器一側顯示行數"
              },
              fontSize: {
                name: "字型大小",
                note: "在編輯器中使用的字體大小（點）"
              },
              minimap: {
                name: "小地圖",
                note: "啟用在編輯器一側顯示程式碼小地圖"
              },
              hover: {
                name: "參考工具提示",
                note: "允許在懸停於規則和選擇器時顯示參考工具提示"
              },
              quickSuggestions: {
                name: "快速建議",
                note: "啟用在您輸入時顯示自動完成建議"
              },
              renderWhitespace: {
                name: "顯示空格",
                note: "編輯器何時應顯示空格",
                options: {
                  all: "總是",
                  none: "永不",
                  selection: "選擇"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} 版本 {{version}} 作者 {{author}}",
          byline: "作者 {{author}}",
          reload: "重新載入",
          addonSettings: "設定",
          website: "網站",
          source: "原始碼",
          invite: "技術支援伺服器",
          donate: "捐贈",
          patreon: "Patreon",
          name: "名稱",
          author: "作者",
          version: "版本",
          added: "安裝時間",
          modified: "最後更新時間",
          editAddon: "編輯",
          deleteAddon: "刪除",
          confirmDelete: "您真的要刪除 {{name}} 嗎？",
          confirmationText: "您還有未儲存的更改，關閉此視窗將失去所有對 {{name}} 所做的更改。",
          enabled: "已啟用 {{name}}。",
          disabled: "已停用 {{name}}。",
          couldNotEnable: "無法啟用 {{name}}。",
          couldNotDisable: "無法停用 {{name}}。",
          couldNotStart: "無法啟動 {{name}}。",
          couldNotStop: "無法停止 {{name}}。",
          settingsError: "無法開啟設定 {{name}}。",
          methodError: "無法調用 {{method}}。",
          unknownAuthor: "未知的作者",
          noDescription: "作者没有留下任何描述。",
          metaError: "無法解析元資料。",
          missingNameData: "元資料缺少名稱。",
          metaNotFound: "無法找到元資料。",
          compileError: "無法編譯。詳細資訊請見控制台。",
          wasUnloaded: "已解除安裝 {{name}}。",
          isEnabled: "已啟用",
          wasLoaded: "已載入 {{name}} 版本 {{version}}。",
          listView: "清單檢視",
          gridView: "方格檢視"
        },
        CustomCSS: {
          confirmationText: "您還有未儲存的更改，關閉此視窗將失去所有對客製化 CSS 所做的更改。",
          update: "更新",
          save: "儲存",
          openNative: "在系統編輯器中開啟",
          openDetached: "在獨立視窗中開啟",
          settings: "編輯器設定",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "您確定嗎？",
          okay: "確定",
          done: "完成",
          cancel: "取消",
          nevermind: "別在意",
          close: "關閉",
          name: "名稱",
          message: "訊息",
          error: "錯誤",
          addonErrors: "擴充功能錯誤",
          restartRequired: "需要重新啟動以生效",
          restartNow: "立即重新啟動",
          restartLater: "稍後自行重新啟動",
          additionalInfo: "額外資訊",
          restartPrompt: "Discord 需要重新啟動以套用更新。立刻進行重新啟動嗎？",
          changelog: "更新日誌"
        },
        Sorting: {
          sortBy: "排序方式",
          order: "順序",
          ascending: "正序",
          descending: "倒序"
        },
        WindowPrefs: {
          enabledInfo: "此選項需要透明佈景主題才能正常工作。在 Windows 上，這可能會破壞 Aero Snap和最大化。\n\n該變更需要重新啟動以生效。您要立即進行重新啟動嗎？",
          disabledInfo: "Discord 需要重新啟動以套用最新變更。立刻進行重新啟動嗎？"
        },
        Updater: {
          updateFialed: "更新失敗！",
          updateFialedMessage: "InAccord 更新失敗。請從我們的網站 (https://InAccord.app/) 下載最新版本的安裝程式並重新安裝。",
          updateSuccessful: "更新成功！",
          updateAvialable: "InAccord 已有新的版本 ({{version}})",
          addonUpdated: "{{name}} 已更新到版本 {{version}}！",
          checking: "正在檢查更新！",
          finishedChecking: "已完成檢查更新！",
          checkForUpdates: "檢查更新！",
          updateAll: "全部更新！",
          noUpdatesAvialable: "沒有可供使用的更新。",
          versionAvialable: "版本 {{version}} 已可供使用！",
          updateButton: "更新！"
        }
      };
    }
  });

  // assets/locales/cs.json
  var require_cs = __commonJS({
    "assets/locales/cs.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Pluginy",
          themes: "Témata",
          customcss: "Vlastní CSS",
          updates: "Aktualizace"
        },
        Collections: {
          settings: {
            name: "Nastavení",
            general: {
              name: "Obecné",
              voiceDisconnect: {
                name: "Odpojení z hlas. kanálu",
                note: "Odpojit z hlasového kanálu při zavření Discordu"
              },
              showToasts: {
                name: "Zobrazit vyskakovací okna",
                note: "Zobrazí malé oznámení pro důležité informace"
              },
              mediaKeys: {
                name: "Zakázat klávesy médií",
                note: "Zabrání Discordu přivlastnění kláves médií při přehrávání videa."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Odebrat minimální velikost",
                note: "Odebere Discordem vynucenou minimální velikost okna 940x500"
              },
              name: "Předvolby okna",
              transparency: {
                name: "Povolit průhlednost",
                note: "Povolí průhlednost hlavního okna (vyžaduje restart)"
              },
              frame: {
                name: "Okraj okna",
                note: "Přidá nativní systémový okraj okna k hlavnímu oknu"
              }
            },
            addons: {
              name: "Správce doplňků",
              addonErrors: {
                name: "Zobrazit chyby doplňků",
                note: "Zobrazí okno s chybami pluginů/témat"
              },
              editAction: {
                name: "Upravit akci",
                note: "Kde se objeví doplňky a témata při úpravě",
                options: {
                  detached: "Oddělené okno",
                  system: "Systémový editor"
                }
              }
            },
            customcss: {
              name: "Vlastní CSS",
              customcss: {
                name: "Vlastní CSS",
                note: "Povolí záložku Vlastní CSS"
              },
              liveUpdate: {
                name: "Okamžitá aktualizace",
                note: "Aktualizuje CSS při psaní"
              },
              startDetached: {
                name: "Spustit odděleně",
                note: "Kliknutí na záložku Vlastní CSS otevře editor v odděleném okně"
              },
              nativeOpen: {
                name: "Otevřít v nativním editoru",
                note: "Kliknutí na záložku Vlastní CSS otevře tvé vlastní CSS ve tvém nativním editoru"
              },
              openAction: {
                name: "Lokace editoru",
                note: "Kde se má ve výchozím nastavení otevřít Vlastní CSS",
                options: {
                  settings: "Menu nastavení",
                  detached: "Oddělené okno",
                  system: "Systémový editor"
                }
              }
            },
            developer: {
              name: "Vývojářská nastavení",
              debuggerHotkey: {
                name: "Zkratka ladění",
                note: "Povolí aktivaci ladění při stisknutí F8"
              },
              reactDevTools: {
                name: "Vývojářské nástroje React",
                note: "Vloží tvou lokální instalaci React Developer Tools do Discordu"
              },
              inspectElement: {
                name: "Zkratka prozkoumání prvku",
                note: "Povolí klávesovou zkratku prozkoumání prvku (Ctrl + Shift + C), která je normální ve většině prohlížečů"
              },
              devToolsWarning: {
                name: "Zastavit varování DevTools",
                note: 'Zabrání Discordu vypisovat jeho "Zadrž!" zprávu'
              },
              debugLogs: {
                name: "Ladicí protokoly",
                note: "Vypíše vše z konzole do souboru debug.log ve složce InAccordu"
              },
              devTools: {
                name: "DevTools",
                note: "Povolí přepnutí DevTools při stisku Ctrl+Shift+I"
              }
            },
            editor: {
              name: "Předvolby editoru",
              lineNumbers: {
                name: "Čísla řádků",
                note: "Povolí zobrazování čísel řádků vedle editoru"
              },
              fontSize: {
                name: "Velikost písma",
                note: "Velikost písma (pt) k použití v editoru"
              },
              minimap: {
                name: "Minimapa",
                note: "Povolí zobrazování kódu v minimapě vedle editoru"
              },
              hover: {
                name: "Referenční tooltipy",
                note: "Povolí zobrazování referenčních tooltipů při přechodu přes pravidla a selektory"
              },
              quickSuggestions: {
                name: "Rychlé návrhy",
                note: "Povolí zobrazování automaticky dokončovaných návrhů, zatímco píšete"
              },
              renderWhitespace: {
                name: "Zobrazit neviditelnou mezeru",
                note: "Zda v editoru zobrazit neviditelnou mezeru",
                options: {
                  all: "Vždy",
                  none: "Nikdy",
                  selection: "Výběr"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} od {{author}}",
          byline: "od {{author}}",
          reload: "Znovu načíst",
          addonSettings: "Nastavení",
          website: "Web",
          source: "Zdroj",
          invite: "Server podpory",
          donate: "Přispět",
          patreon: "Patreon",
          name: "Název",
          author: "Autor",
          version: "Verze",
          added: "Datum přidání",
          modified: "Datum upravení",
          editAddon: "Upravit",
          deleteAddon: "Odstranit",
          confirmDelete: "Opravdu chceš odstranit {{name}}?",
          confirmationText: "Máš neuložené změny u {{name}}. Zavřením tohoto okna je všechny ztratíš.",
          enabled: "Doplněk {{name}} byl povolen.",
          disabled: "Doplněk {{name}} byl zakázán.",
          couldNotEnable: "Doplněk {{name}} nemohl být povolen.",
          couldNotDisable: "Doplněk {{name}} nemohl být zakázán.",
          couldNotStart: "Doplněk {{name}} nemohl být spuštěn.",
          couldNotStop: "Doplněk {{name}} nemohl být zastaven.",
          settingsError: "Nepodařilo se otevřít nastavení {{name}}",
          methodError: "Nepodařilo se spustit metodu {{method}}.",
          unknownAuthor: "Neznámý autor",
          noDescription: "Bez popisu.",
          metaError: "META být nemohl parsován.",
          missingNameData: "META chybějící údaje názvu.",
          metaNotFound: "META nebyl nalezen.",
          compileError: "Nepodařilo se zkompilovat.",
          wasUnloaded: "Doplněk {{name}} byl odnačten.",
          isEnabled: "Povoleno",
          wasLoaded: "Doplněk {{name}} v{{version}} byl načten."
        },
        CustomCSS: {
          confirmationText: "Máš neuložené změny ve vlastním CSS. Zavřením tohoto okna je všechny ztratíš.",
          update: "Aktualizovat",
          save: "Uložit",
          openNative: "Otevřít v systémovém editoru",
          openDetached: "Oddělit okno",
          settings: "Nastavení editoru",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Opravdu?",
          okay: "Dobře",
          done: "Hotovo",
          cancel: "Zrušit",
          nevermind: "Ne",
          close: "Zavřít",
          name: "Název",
          message: "Zpráva",
          error: "Chyba",
          addonErrors: "Chyby doplňku",
          restartRequired: "Vyžadován restart",
          restartNow: "Restartovat nyní",
          restartLater: "Restartovat později",
          additionalInfo: "Dodatečné informace",
          restartPrompt: "Aby se tato změna projevila, bude potřeba restartovat Discord. Chceš jej restartovat nyní?"
        },
        Sorting: {
          sortBy: "Seřadit podle",
          order: "Pořadí",
          ascending: "Vzestupně",
          descending: "Sestupně"
        },
        WindowPrefs: {
          enabledInfo: "Tato možnost vyžaduje průhledné téma, aby fungovala správně. Na Windows to může rozbít Areo přichytávání a maximalizování.\n\nAby se tato změna projevila, bude potřeba restartovat Discord. Chceš jej restartovat nyní?",
          disabledInfo: "Aby se tato změna projevila, bude potřeba restartovat Discord. Chceš jej restartovat nyní?"
        },
        Updater: {
          updateFialed: "Aktualizace selhala!",
          updateFialedMessage: "Aktualizace BettterDiscordu se nezdařila. Stáhněte si prosím nejnovější verzi instalátoru z našeho webu (https://InAccord.app) a přeinstalujte jej.",
          updateSuccessful: "Aktualizace byla úspěšná!",
          updateAvialable: "InAccord má novou aktualizaci (v{{version}})",
          addonUpdated: "Doplněk {{name}} byl aktualizován na verzi {{version}}!",
          checking: "Kontrola aktualizací!",
          finishedChecking: "Kontrola aktualizací dokončena!",
          checkForUpdates: "Zkontrolovat aktualizace!",
          updateAll: "Aktualizovat vše!",
          noUpdatesAvialable: "Nejsou dostupné žádné aktualizace.",
          versionAvialable: "Je dostupná verze {{version}}!",
          updateButton: "Aktualizovat!"
        }
      };
    }
  });

  // assets/locales/da.json
  var require_da = __commonJS({
    "assets/locales/da.json"(exports, module) {
      module.exports = {};
    }
  });

  // assets/locales/nl.json
  var require_nl = __commonJS({
    "assets/locales/nl.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plug-ins",
          themes: "Thema's",
          customcss: "Aangepaste CSS"
        },
        Collections: {
          settings: {
            name: "Instellingen",
            general: {
              name: "Algemeen",
              voiceDisconnect: {
                name: "Stem Verbroken",
                note: "Verbreek de verbinding met de oproep"
              },
              showToasts: {
                name: "Laat Toasts Zien",
                note: "Toont een melding voor belangrijke informatie"
              },
              mediaKeys: {
                name: "Schakel mediatoetsen uit",
                note: "Voorkomt dat Discord uw mediatoetsen kaapt na het afspelen van een video."
              }
            }
          }
        },
        Addons: {
          byline: "door {{author}}",
          reload: "Herladeen",
          addonSettings: "Instellingen",
          website: "Website",
          source: "Bronnen",
          invite: "Support Server",
          donate: "Donatie",
          patreon: "Patreon",
          name: "Naam",
          author: "Schrijver",
          version: "Versie",
          added: "Datum toegevoegd",
          modified: "Datum gewijzigd ",
          editAddon: "Bewerken",
          deleteAddon: "Verwijderen",
          confirmDelete: "Weet je het zeker dat je {{name}} wilt verwijderen?",
          confirmationText: "Je hebt niet-opgeslagen wijzigingen in {{naam}}. Als je dit venster sluit, gaan alle wijzigingen verloren.",
          unknownAuthor: "Onbekende schrijver",
          noDescription: "Beschrijving kon niet worden weergegeven"
        },
        Modals: {
          confirmAction: "Weet je het zeker?",
          okay: "Oké",
          cancel: "Annuleren",
          close: "Bijna",
          name: "Naam",
          message: "Berich",
          error: "Fout!"
        },
        Sorting: {
          order: "Volgorde"
        }
      };
    }
  });

  // assets/locales/fr.json
  var require_fr = __commonJS({
    "assets/locales/fr.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plugins",
          themes: "Thèmes",
          customcss: "CSS personnalisé"
        },
        Collections: {
          settings: {
            name: "Réglages",
            general: {
              name: "Général",
              voiceDisconnect: {
                name: "Déconnexion vocale",
                note: "Déconnexion du serveur vocal lors de la fermeture de Discord"
              },
              showToasts: {
                name: "Afficher les notifications bulles",
                note: "Afficher une petite notification pour les informations importantes"
              },
              mediaKeys: {
                name: "Désactiver les Touches Médias",
                note: "Empêche Discord de détourner vos touches multimédia après la lecture d'une vidéo."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Enlever la Tialle Minimale",
                note: "Enlève la tialle de fenêtre minimale forcée de Discord qui est de 940x500"
              },
              name: "Fenêtre des Préférences",
              transparency: {
                name: "Activer la Transparence",
                note: "Permet à la fenêtre principale d'être transparente (nécessite un redémarrage)"
              },
              frame: {
                name: "Cadre de la Fenêtre",
                note: "Ajoute le cadre de la fenêtre native de l'OS à la fenêtre principale."
              }
            },
            addons: {
              name: "Gestionniare d'Extensions",
              addonErrors: {
                name: "Afficher les Erreurs d'Extensions",
                note: "Affiche une modale avec les erreurs de plugin/thème"
              },
              editAction: {
                name: "Action Édition",
                note: "Là où les thèmes & extensions apparaîtront lors de l'édition",
                options: {
                  detached: "Fenêtre détachée",
                  system: "Éditeur système"
                }
              }
            },
            customcss: {
              name: "CSS Personnalisé",
              customcss: {
                name: "CSS Personnalisé",
                note: "Activer l'onglet de CSS personnalisé"
              },
              liveUpdate: {
                name: "Mise à Jour Directe",
                note: "Met à jour le CSS pendant que vous écrivez"
              },
              startDetached: {
                name: "Démarrer Détaché",
                note: "En cliquant sur l'onglet CSS Personnalisé, l'éditeur s'ouvre dans une fenêtre distincte."
              },
              nativeOpen: {
                name: "Ouvrir dans l'Éditeur Natif",
                note: "En cliquant sur CSS Personnalisé, l'onglet ouvrira votre CSS personnalisé dans votre éditeur natif"
              },
              openAction: {
                name: "Emplacement de l'Éditeur",
                note: "Là où le CSS personnalisé doit s'ouvrir par défaut",
                options: {
                  settings: "Menu Réglages",
                  detached: "Fenêtre Détachée",
                  system: "Éditeur système"
                }
              }
            },
            developer: {
              name: "Réglages Développeur",
              debuggerHotkey: {
                name: "Touches Raccourcis du Débugueur",
                note: "Permettre l'activation du débugueur quand F8 est pressé"
              },
              reactDevTools: {
                name: "Outils Développeurs React",
                note: "Injecte votre installation locale des Outils Développeurs React dans Discord"
              },
              inspectElement: {
                name: "Touche pour Inspecter l'Élément",
                note: "Active le raccourci clavier de l'inspecteur d'élément (ctrl + shift + c) qui est commun à la plupart des navigateurs"
              },
              devToolsWarning: {
                name: "Arrêter les Avertissements des Outils Développement",
                note: `Empêche Discord d'imprimer son message \\"Attends!\\"`
              },
              debugLogs: {
                name: "Journaux de Débogage",
                note: "Transmet tout ce qui se trouve dans la console au fichier debug.log situé dans le dossier de InAccord"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} par {{author}}",
          byline: "par {{author}}",
          reload: "Recharger",
          addonSettings: "Réglages",
          website: "Site web",
          source: "Source",
          invite: "Server de support",
          donate: "Donner",
          patreon: "Patreon",
          name: "Nom",
          author: "Auteur",
          version: "Version",
          added: "Date d'Ajout",
          modified: "Date Modifiée",
          editAddon: "Édition",
          deleteAddon: "Suppression",
          confirmDelete: "Êtes-vous sûr de vouloir supprimer {{name}}?",
          confirmationText: "Vous n'avez pas sauvegardé les changements pour {{name}}. Fermer cette fenêtre fera perdre ces changements.",
          enabled: "{{name}} a été activé.",
          disabled: "{{name}} a été désactivé.",
          couldNotEnable: "{{name}} ne peut pas être activé.",
          couldNotDisable: "{{name}} n'a pas pu être désactivé.",
          couldNotStart: "{{name}} n'a pas pu être lancé.",
          couldNotStop: "{{name}} n'a pas pu être arrêté.",
          settingsError: "Impossible d'ouvrir les paramètres pour {{name}}",
          methodError: "{{method}} n'a pas pu être renvoyé.",
          unknownAuthor: "Auteur Inconnu",
          noDescription: "Description non fournie.",
          metaError: "Le META n'a pas pu être analysé.",
          missingNameData: "Les données de META ne possèdent pas de nom.",
          metaNotFound: "Les données de META sont introuvables.",
          compileError: "Compilation impossible.",
          wasUnloaded: "{{name}} a été déchargé."
        },
        CustomCSS: {
          confirmationText: "Vous avez des changements non sauvegardés dans votre CSS Personnalisé. Fermer cette fenêtre vous fera perdre ces changements.",
          update: "Mise à jour",
          save: "Sauvegarder",
          openNative: "Ouvrir dans l'Éditeur Système",
          openDetached: "Détacher la Fenêtre",
          settings: "Réglages de l'Éditeur",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Êtes-vous sûr?",
          okay: "D'accord",
          done: "Terminé",
          cancel: "Annuler",
          nevermind: "Liasse tomber",
          close: "Fermer",
          name: "Nom",
          message: "Message",
          error: "Erreur",
          addonErrors: "Erreurs d'extension",
          restartRequired: "Redémarrage Requis",
          restartNow: "Redémarrer Miantenant",
          restartLater: "Redémarrer Ultérieurement",
          additionalInfo: "Info Additionnelle",
          restartPrompt: "Pour appliquer ces changements, Discord à besoin d'être redémarré. Voulez-vous redémarrer l'application miantenant?"
        },
        Sorting: {
          sortBy: "Trier Par",
          order: "Ordre",
          ascending: "Ascendant",
          descending: "Descendant"
        },
        WindowPrefs: {
          enabledInfo: "Cette option requière un thème transparent pour pouvoir fonctionner correctement. Sous Windows, cela peut casser le style Aero de Windows lors de la maximisation et du clipsement de la fenêtre.\n\nAfin que ce changement soit actif, Discord doit être redémarré. Voulez-vous redémarrer l'application miantenant?",
          disabledInfo: "Afin que ce changement soit actif, Discord doit être redémarré. Voulez-vous redémarrer l'application miantenant?"
        }
      };
    }
  });

  // assets/locales/de.json
  var require_de = __commonJS({
    "assets/locales/de.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plugins",
          themes: "Themes",
          customcss: "Custom-CSS"
        },
        Collections: {
          settings: {
            name: "Einstellungen",
            general: {
              name: "Allgemein",
              voiceDisconnect: {
                name: "Verbindung zum Sprachchat trennen",
                note: "Trennt beim Schließen von Discord die Verbindung zum Sprachchat"
              },
              showToasts: {
                name: "Toast-Benachrichtigungen anzeigen",
                note: "Zeigt eine kleine Benachrichtigung für wichtige Informationen an"
              },
              mediaKeys: {
                name: "Medien-Tasten deaktivieren",
                note: "Verhindert, dass Discord deine Medien-Tasten nach dem Abspielen eines Videos beansprucht"
              }
            },
            window: {
              removeMinimumSize: {
                name: "Freie Fenstergröße",
                note: "Entfernt die Beschränkung der minimalen Fenstergröße von 940x500 Pixel"
              },
              name: "Fenster-Einstellungen",
              transparency: {
                name: "Transparenz aktivieren",
                note: "Macht den Discord-Hintergrund transparent (Benötigt Discord Neustart)"
              },
              frame: {
                name: "Standard-Design",
                note: "Stellt das Hauptfenster im Standard-Design des Betriebssystems dar"
              }
            },
            addons: {
              name: "Erweiterungsverwaltung",
              addonErrors: {
                name: "Fehlernachricht für Erweiterungen anzeigen",
                note: "Zeigt ein Informationsfenster bei Ladefehlern von Erweiterungen an"
              },
              editAction: {
                name: "Bearbeitungsaktion",
                note: "Bestimmt womit Plugins und Themes zum Bearbeiten geöffnet werden",
                options: {
                  detached: "Losgelöstes Fenster",
                  system: "Systemweiter Standard-Texteditor"
                }
              }
            },
            customcss: {
              name: "Custom-CSS",
              customcss: {
                name: "Custom-CSS",
                note: "Aktiviert den Custom-CSS Reiter in den Einstellungen"
              },
              liveUpdate: {
                name: "Echtzeitaktualisierung",
                note: "Aktualisert den CSS Code während des Schreibens"
              },
              startDetached: {
                name: "In losgelöstem Fenster öffnen",
                note: 'Das Öffnen des \\"Custom-CSS\\"-Reiters öffnet den Editor in einem separaten Fenster'
              },
              nativeOpen: {
                name: "Im System-Editor öffnen",
                note: 'Das Öffnen des \\"Custom-CSS\\"-Reiters öffnet den Editor im systemweiten Standard-Texteditor'
              },
              openAction: {
                name: "Editormodus",
                note: "Wo soll Custom-CSS standardmäßig geöffnet werden",
                options: {
                  settings: "in den Einstellungen",
                  detached: "in losgelöstem Fenster",
                  system: "im Standard-Texteditor"
                }
              }
            },
            developer: {
              name: "Entwickler-Einstellungen",
              debuggerHotkey: {
                name: "Debugger-Tastenkürzel",
                note: "Erlaubt das Starten des Debuggers durch Drücken der F8-Taste"
              },
              reactDevTools: {
                name: "React-Entwicklerwerzeuge",
                note: "Koppelt eine vorhandene Installation der React-Entwicklerwerkzeuge in Discord ein"
              },
              inspectElement: {
                name: 'Tastenkürzel für "Element untersuchen"',
                note: 'Aktiviert das Tastenkürzel für \\"Element Untersuchen\\" (Strg + Shift + C), welches in gängigen Browsern vorhanden ist'
              },
              devToolsWarning: {
                name: "Entwicklerwerkzeuge Warnungen stoppen",
                note: 'Hält Discord davon ab ihre \\"Hold Up!\\" Warnung zu zeigen'
              },
              debugLogs: {
                name: "Debugger Logs",
                note: 'Speichert alle Ausgaben aus der Konsole in die \\"debug.log\\" Datei im InAccord Verzeichnis'
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} von {{author}}",
          byline: "von {{author}}",
          reload: "Neu laden",
          addonSettings: "Einstellungen",
          website: "Webseite",
          source: "Quelle",
          invite: "Support-Server",
          donate: "Spenden",
          patreon: "Patreon",
          name: "Name",
          author: "Autor",
          version: "Version",
          added: "Hinzufügedatum",
          modified: "Bearbeitungsdatum",
          editAddon: "Bearbeiten",
          deleteAddon: "Löschen",
          confirmDelete: "Bist du dir sicher, dass du {{name}} löschen willst?",
          confirmationText: "Du hast ungespeicherte Änderungen in {{name}}. Durch das Schließen dieses Fensters verlierst du alle Änderungen.",
          enabled: "{{name}} wurde aktiviert.",
          disabled: "{{name}} wurde deaktiviert.",
          couldNotEnable: "{{name}} konnte nicht aktiviert werden.",
          couldNotDisable: "{{name}} konnte nicht deaktiviert werden.",
          couldNotStart: "{{name}} konnte nicht gestartet werden.",
          couldNotStop: "{{name}} konnte nicht gestoppt werden.",
          settingsError: "Einstellungen für {{name}} konnten nicht geöffnet werden",
          methodError: "{{method}} konnte nicht ausgeführt werden.",
          unknownAuthor: "Unbekannter Autor",
          noDescription: "Keine Beschreibung angegeben.",
          metaError: "META konnte nicht geparst werden.",
          missingNameData: 'META enthält keinen \\"name\\" Parameter.',
          metaNotFound: "META nicht gefunden.",
          compileError: "Konnte nicht kompiliert werden.",
          wasUnloaded: "{{name}} wurde entladen."
        },
        CustomCSS: {
          confirmationText: "Du hast ungespeicherte Änderungen im Custom-CSS. Durch das Schließen dieses Fensters verlierst du alle Änderungen.",
          update: "Aktualisieren",
          save: "Speichern",
          openNative: "Im System-Editor öffnen",
          openDetached: "Fenster trennen",
          settings: "Editor-Einstellungen",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Bist du dir sicher?",
          okay: "Okay",
          done: "Fertig",
          cancel: "Abbrechen",
          nevermind: "Egal",
          close: "Schließen",
          name: "Name",
          message: "Nachricht",
          error: "Fehler",
          addonErrors: "Erweiterungsfehler",
          restartRequired: "Neustart erforderlich",
          restartNow: "Jetzt neustarten",
          restartLater: "Später neustarten",
          additionalInfo: "Zusätzliche Informationen",
          restartPrompt: "Die Einstellung wurde gespeichert. Um sie anzuwenden, ist ein Neustart von Discord erforderlich."
        },
        Sorting: {
          sortBy: "Sortieren nach",
          order: "Reihenfolge",
          ascending: "Aufsteigend",
          descending: "Absteigend"
        },
        WindowPrefs: {
          enabledInfo: "Diese Option benötigt ein Theme mit transparentem Hintergrund um richtig zu funktionieren. Weiters kann dadurch unter Windows das Andocken des Fensters am Bildschirmrand beeinträchtigt sein.\n\nUm die Änderungen zu übernehmen muss Discord neugestartet werden. Möchtest du jetzt neustarten?",
          disabledInfo: "Die Einstellung wurde gespeichert. Um sie anzuwenden, ist ein Neustart von Discord erforderlich."
        }
      };
    }
  });

  // assets/locales/el.json
  var require_el = __commonJS({
    "assets/locales/el.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Πρόσθετα",
          themes: "Θέματα",
          customcss: "Προσαρμοσμένο CSS",
          updates: "Ενημερώσεις"
        },
        Collections: {
          settings: {
            name: "Ρυθμίσεις",
            general: {
              name: "Γενικά",
              voiceDisconnect: {
                name: "Αποσύνδεση Φωνής",
                note: "Αποσύνδεση από τον διακομιστή φωνής με το κλείσιμο του Discord"
              },
              showToasts: {
                name: "Εμφάνιση Ειδοποιήσεων",
                note: "Εμφάνιση μικρής ειδοποίησης για σημαντικές πληροφορίες"
              },
              mediaKeys: {
                name: "Απενεργοποίηση Πλήκτρων Πολυμέσων",
                note: "Αποτροπή παρέμβασης του Discord στα πλήκτρα πολυμέσων μετά την αναπαραγωγή βίντεο."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Απομάκρυνση Ελάχιστου Μεγέθους",
                note: "Απομακρύνει τον περιορισμό του Discord για ελάχιστες διαστάσεις παραθύρου στα 940 x 500"
              },
              name: "Ιδιότητες Παραθύρου",
              transparency: {
                name: "Ενεργοποίηση Διαφάνειας",
                note: "Ενεργοποιεί τη διαφάνεια του βασικού παραθύρου (απαιτείται επανεκκίνηση)"
              },
              frame: {
                name: "Πλαίσιο Παραθύρου",
                note: "Προσθέτει το πλαίσιο του συστήματος στο βασικό παράθυρο"
              }
            },
            addons: {
              name: "Διαχειριστής Προσθέτων",
              addonErrors: {
                name: "Εμφάνιση Σφαλμάτων Προσθέτων",
                note: "Εμφάνιση σφαλμάτων προσθέτων/θεμάτων σε παράθυρο"
              },
              editAction: {
                name: "Ενέργεια Επεξεργασίας",
                note: "Η θέση εμφάνισης προσθέτων και θεμάτων κατά την επεξεργασία",
                options: {
                  detached: "Αποσπώμενο Παράθυρο",
                  system: "Επεξεργαστής Συστήματος"
                }
              }
            },
            customcss: {
              name: "Προσαρμοσμένο CSS",
              customcss: {
                name: "Προσαρμοσμένο CSS",
                note: "Ενεργοποιεί την καρτέλα Προσαρμοσμένου CSS"
              },
              liveUpdate: {
                name: "Ενημέρωση σε πραγματικό χρόνο",
                note: "Ενημερώνεται το CSS ενώ πληκτρολογείτε"
              },
              startDetached: {
                name: "Εκκίνηση ως Αποσπώμενο",
                note: "Πατώντας την καρτέλα Προσαρμοσμένου CSS ανοίγει τον επεξεργαστή σε χωριστό παράθυρο"
              },
              nativeOpen: {
                name: "Άνοιγμα στον Επεξεργαστή του Συστήματος",
                note: "Πατώντας την καρτέλα του Προσαρμοσμένου CSS ανοίγει το προσαρμοσμένο CSS στον επεξεργαστή συστήματος"
              },
              openAction: {
                name: "Θέση επεξεργαστή",
                note: "Που πρέπει να ανοίγει το Προσαρμοσμένο CSS από προεπιλογή",
                options: {
                  settings: "Μενού Ρυθμίσεων",
                  detached: "Αποσπώμενο παράθυρο",
                  system: "Επεξεργαστής Συστήματος"
                }
              }
            },
            developer: {
              name: "Ρυθμίσεις Δημιουργού",
              debuggerHotkey: {
                name: "Συντόμευση πληκτρολογίου για Αποσφαλμάτωση",
                note: "Επιτρέπει την ενεργοποίηση της Αποσφαλμάτωσης, πατώντας το πλήκτρο F8 με ανοιχτά τα Εργαλεία Δημιουργού"
              },
              reactDevTools: {
                name: "Εργαλεία Δημιουργού React",
                note: "Ανοίγει την τοπική εγκατάσταση των Εργαλείων Δημιουργού React στο Discord"
              },
              inspectElement: {
                name: "Συντόμευση πληκτρολογίου Παρακολούθησης Στοιχείου",
                note: "Ενεργοποιεί τη συντόμευση πληκτρολογίου παρακολούθησης στοιχείου (Ctrl+Shift+C) που είναι κοινή στους περισσότερους φυλλομετρητές"
              },
              devToolsWarning: {
                name: "Σταμάτημα Προειδοποίησης Εργαλείων Δημιουργού",
                note: "Αποτρέπει το Discord να εμφανίζει το μήνυμα «Αναμείνατε!»"
              },
              debugLogs: {
                name: "Καταγραφές Αποσφαλμάτωσης",
                note: "Εξάγει τα πάντα από την κονσόλα στο αρχείο debug.log στο φάκελο του InAccord"
              },
              devTools: {
                name: "Εργαλεία Δημιουργού",
                note: "Ενεργοποιεί την εναλλαγή των Εργαλείων Δημιουργού με το Ctrl+Shift+I"
              }
            },
            editor: {
              name: "Ιδιότητες Επεξεργαστή",
              lineNumbers: {
                name: "Αριθμοί γραμμών",
                note: "Ενεργοποιείται η εμφάνιση των αριθμών γραμμών στη πλευρά του επεξεργαστή"
              },
              fontSize: {
                name: "Μέγεθος Γραμματοσειράς",
                note: "Μεγεθος γραμματοσειράς (pt) για χρήση στον επεξεργαστή"
              },
              minimap: {
                name: "Εποπτικός χάρτης",
                note: "Ενεργοποιεί την εμφάνιση του εποπτικού χάρτη του κώδικα στην πλευρά του επεξεργαστή"
              },
              hover: {
                name: "Επεξηγήσεις Αναφοράς",
                note: "Ενεργοποιεί την εμφάνιση των επεξηγήσεων αναφοράς όταν πάτε με το ποντίκι κανόνων και επιλογέων"
              },
              quickSuggestions: {
                name: "Γρήγορες Προτάσεις",
                note: "Ενεργοποιεί την εμφάνιση των αυτοσυμπληρούμενων προτάσεων κατά την δακτυλογράφηση"
              },
              renderWhitespace: {
                name: "Εμφάνιση Διαστήματος",
                note: "Όταν τα διαστήματα πρέπει να φαίνονται από τον επεξεργαστή",
                options: {
                  all: "Πάντα",
                  none: "Ποτέ",
                  selection: "Επιλογή"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} από {{author}}",
          byline: "από {{author}}",
          reload: "Επαναφόρτωση",
          addonSettings: "Ρυθμίσεις",
          website: "Ιστοσελίδα",
          source: "Προέλευση",
          invite: "Διακομιστής Υποστήριξης",
          donate: "Δωρεά",
          patreon: "Patreon",
          name: "Ονομασία",
          author: "Δημιουργός",
          version: "Έκδοση",
          added: "Ημερομηνία που Προστέθηκε",
          modified: "Ημερομηνία που Τροποποιήθηκε",
          editAddon: "Επεξεργασία",
          deleteAddon: "Διαγραφή",
          confirmDelete: "Θέλετε να διαγράψετε το πρόσθετο {{name}};",
          confirmationText: "Έχετε μη αποθηκευμένες αλλαγές στο πρόσθετο {{name}}. Με το κλείσιμο του παραθύρου θα χαθούν όλες αυτές οι αλλαγές.",
          enabled: "Το πρόσθετο {{name}} έχει ενεργοποιηθεί.",
          disabled: "Το πρόσθετο {{name}} έχει απενεργοποιηθεί.",
          couldNotEnable: "Το πρόσθετο {{name}} δεν μπορεί να ενεργοποιηθεί.",
          couldNotDisable: "Το πρόσθετο {{name}} δεν μπορεί να απενεργοποιηθεί.",
          couldNotStart: "Το πρόσθετο {{name}} δεν μπορεί να εκκινηθεί.",
          couldNotStop: "Το πρόσθετο {{name}} δεν μπορεί να τερματιστεί.",
          settingsError: "Αδύνατο το άνοιγμα των ρυθμίσεων για το πρόσθετο {{name}}",
          methodError: "Η μέθοδος {{method}} δεν μπορεί να ενεργοποιηθεί.",
          unknownAuthor: "Άγνωστος Δημιουργός",
          noDescription: "Η περιγραφή δεν παρέχεται.",
          metaError: "Τα μεταδεδομένα δεν μπορούν να αναγνωστούν.",
          missingNameData: "Τα μεταδεδομένα απώλεσαν δεδομένα ονόματος.",
          metaNotFound: "Τα μεταδεδομένα δεν βρέθηκαν.",
          compileError: "Αδύνατη η μετατροπή. Δείτε την κονσόλα για λεπτομέρειες.",
          wasUnloaded: "Το πρόσθετο {{name}} αποφορτώθηκε.",
          isEnabled: "Ενεργοποιημένα",
          wasLoaded: "Το πρόσθετο {{name}} v{{version}} φορτώθηκε."
        },
        CustomCSS: {
          confirmationText: "Έχετε μη αποθηκευμένες αλλαγές στο Προσαρμοσμένο CSS. Με το κλείσιμο αυτού του παραθύρου, θα χάσετε όλες αυτές τις αλλαγές.",
          update: "Ενημέρωση",
          save: "Αποθήκευση",
          openNative: "Άνοιγμα στον Επεξεργαστή Συστήματος",
          openDetached: "Αποσπώμενο Παράθυρο",
          settings: "Ρυθμίσεις Επεξεργαστή",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Σίγουρα;",
          okay: "Εντάξει",
          done: "Ολοκληρώθηκε",
          cancel: "Άκυρο",
          nevermind: "Δεν πειράζει",
          close: "Κλείσιμο",
          name: "Ονομασία",
          message: "Μήνυμα",
          error: "Σφάλμα",
          addonErrors: "Σφάλματα Πρόσθετου",
          restartRequired: "Απαιτείται Επανεκκίνηση",
          restartNow: "Επανεκκίνηση Τώρα",
          restartLater: "Επανεκκίνηση Αργότερα",
          additionalInfo: "Πρόσθετες Πληροφορίες",
          restartPrompt: "Για να εφαρμοστεί, το Discord πρέπει να επανεκκινηθεί. Θέλετε επανεκκίνηση τώρα;"
        },
        Sorting: {
          sortBy: "Ταξινόμηση κατά",
          order: "Ταξινόμηση",
          ascending: "Αύξουσα",
          descending: "Φθίνουσα"
        },
        WindowPrefs: {
          enabledInfo: "Αυτή η επιλογή απαιτεί διαφανές θέμα ώστε να λειτουργεί σωστά. Στα Windows ίσως επηρεάσει την προσαρμογή aero ή τη μεγιστοποίηση.\n\nΓια να ενεργοποιηθεί, το Discord πρέπει να επανεκκινηθεί. Επανεκκίνηση τώρα;",
          disabledInfo: "Για να ενεργοποιηθεί, το Discord πρέπει να επανεκκινηθεί. Επανεκκίνηση τώρα;"
        },
        Updater: {
          updateFialed: "Η ενημέρωση Απέτυχε!",
          updateFialedMessage: "Το InAccord απέτυχε να ενημερωθεί. Λάβετε την τελευταία έκδοση του προγράμματος εγκατάστασης από την ιστοσελίδα μας (https://InAccord.app/) και κάντε επανεγκατάσταση.",
          updateSuccessful: "Η Ενημέρωση ήταν Επιτυχής!",
          updateAvialable: "Το InAccord έχει μια νέα ενημερωση (v{{version}})",
          addonUpdated: "Το {{name}} έχει ενημερωθεί στην έκδοση {{version}}!",
          checking: "Έλεγχος για ενημερώσεις!",
          finishedChecking: "Ολοκλήρωση ελέγχου για ενημερώσεις!",
          checkForUpdates: "Έλεγχος Για Ενημερώσεις!",
          updateAll: "Ενημέρωση Όλων!",
          noUpdatesAvialable: "Καμιά διαθέσιμη ενημέρωση.",
          versionAvialable: "Η έκδοση {{version}} είναι διαθέσιμη!",
          updateButton: "Ενημέρωση!"
        }
      };
    }
  });

  // assets/locales/hu.json
  var require_hu = __commonJS({
    "assets/locales/hu.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Pluginok",
          themes: "Témák",
          customcss: "Egyéni CSS",
          updates: "Frissítések"
        },
        Collections: {
          settings: {
            name: "Beállítások",
            general: {
              name: "Általános",
              voiceDisconnect: {
                name: "Hangkapcsolat megszakítása",
                note: "Kapcsolat megszakítása a hangkiszolgálóval amikor bezárod a Discordot"
              },
              showToasts: {
                name: "Értesítés megjelenítése induláskor",
                note: "Kis értesítést jelenít meg a fontos információkról"
              },
              mediaKeys: {
                name: "Médiavezérlő billentyűk letiltása",
                note: "Megakadályozza, hogy a Discord a videók lejátszása után átkalibrálja a médiavezérlő billentyűket."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Minimális méret felülírása",
                note: "Felülírja és eltávolítja a Discord kényszerített minimális 940x500-as ablakméretét"
              },
              name: "Ablak beállítások",
              transparency: {
                name: "Átlátszóság engedélyezése",
                note: "A főablak átláthatóvá tétele (újriandítást igényel)"
              },
              frame: {
                name: "Ablakkeret",
                note: "Hozzáadja az alap os ablakkeretet a főablakhoz"
              }
            },
            addons: {
              name: "Kiegészítőkezelő",
              addonErrors: {
                name: "Kiegészítő hibaüzenetek megjelenítése",
                note: "Megjelenít egy ablakot a pluginok/témák hibáiról"
              },
              editAction: {
                name: "Szerkesztő beállítása",
                note: "A pluginok és témák szerkesztése ebben a szerkesztőben",
                options: {
                  detached: "Különálló ablak",
                  system: "Rendszer alapértelmezett"
                }
              }
            },
            customcss: {
              name: "Egyéni CSS",
              customcss: {
                name: "Egyéni CSS",
                note: "Engedélyezi az Egyéni CSS szekciót"
              },
              liveUpdate: {
                name: "Frissítés élőben",
                note: "Frissíti a css-t szerkesztés közben"
              },
              startDetached: {
                name: "Indítás külön ablakban",
                note: "Az Egyéni CSS szekcióra kattintva a szerkesztő egy külön ablakban nyílik meg"
              },
              nativeOpen: {
                name: "Megnyitás natív szerkesztőben",
                note: "Az Egyéni CSS fülre kattintva megnyílik az egyéni css egy natív szerkesztőben"
              },
              openAction: {
                name: "Szerkesztő helye",
                note: "Az Egyéni CSS alapértelmezetten itt nyílik meg",
                options: {
                  settings: "Beállítások menü",
                  detached: "Különálló ablak",
                  system: "Rendszer alapértelmezett"
                }
              }
            },
            developer: {
              name: "Fejlesztői beállítások",
              debuggerHotkey: {
                name: "Hibakereső gyorsbillentyű",
                note: "Lehetővé teszi a debugger aktiválását az F8 megnyomásakor, amikor a DevTools meg van nyitva"
              },
              reactDevTools: {
                name: "React fejlesztői eszközök",
                note: "Beilleszti a telepített React Developer Toolst a Discordba"
              },
              inspectElement: {
                name: "Elem vizsgálat gyorsbillentyűje",
                note: "Engedélyezi a legtöbb böngészőben használt elem vizsgálata gyorsbillentyűt (ctrl + shift + c)"
              },
              devToolsWarning: {
                name: "DevTools riasztás leállítása",
                note: 'Megakadályozza, hogy a Discord kiírja a "Várj!" üzenetét'
              },
              debugLogs: {
                name: "Hibakereső napló",
                note: "A konzolról mindent kiír a InAccord mappában lévő debug.log fájlba"
              },
              devTools: {
                name: "Fejlesztői eszközök",
                note: "Engedélyezi a DevTools bekapcsolását a ctrl+shift+i billentyűkombinációval"
              }
            },
            editor: {
              name: "Szerkesztői beállítások",
              lineNumbers: {
                name: "Szakasz sorszámok",
                note: "Lehetővé teszi a szakasz sorszámok megjelenítését a szerkesztő oldalán"
              },
              fontSize: {
                name: "Betűméret",
                note: "A szerkesztőben használandó betűtípus mérete (pt)"
              },
              minimap: {
                name: "Minitérkép",
                note: "Megjeleníti a kódminitérkép megjelenítését a szerkesztő oldalán"
              },
              hover: {
                name: "Hivatkozási eszköztippek",
                note: "Megjeleníti a hivatkozási eszköztippek megjelenítését a szabályok és szelektorok lebegtetésekor"
              },
              quickSuggestions: {
                name: "Gyors javaslatok",
                note: "Engedélyezi az automatikus kitöltési javaslatok megjelenítését gépelés közben"
              },
              renderWhitespace: {
                name: "Fehér tér megjelenítése",
                note: "Szóközök megjelenítése a szerkesztőben",
                options: {
                  all: "Mindig",
                  none: "Soha",
                  selection: "Kiválasztás"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} {{author}} által",
          byline: "{{author}} által",
          reload: "Újratöltés",
          addonSettings: "Beállítások",
          website: "Weboldal",
          source: "Forrás",
          invite: "Támogatási szerver",
          donate: "Támogatás",
          patreon: "Patreon",
          name: "Név",
          author: "Szerző",
          version: "Verzió",
          added: "Hozzáadás dátuma",
          modified: "Módosítás dátuma",
          editAddon: "Szerkesztés",
          deleteAddon: "Törlés",
          confirmDelete: "Biztos, hogy törölni akarja ezt: {{name}}?",
          confirmationText: "A {{name}} módosításiat nem kerültek mentésre. Ha bezárja ezt az ablakot, ezek a módosítások elvesznek.",
          enabled: "{{name}} bekapcsolva.",
          disabled: "{{name}} ki lett kapcsolva.",
          couldNotEnable: "{{name}} nem elindítható.",
          couldNotDisable: "{{name}} nem kikapcsolható.",
          couldNotStart: "{{name}} nem elindítható.",
          couldNotStop: "{{name}} nem leállítható.",
          settingsError: "Nem sikerült megnyitni a {{name}} beállításiat",
          methodError: "{{method}} nem kivehető.",
          unknownAuthor: "Ismeretlen szerző",
          noDescription: "Leírás nincs megadva.",
          metaError: "Nem sikerült elemezni a META-t.",
          missingNameData: "Hiányzik a névadat a META-ból.",
          metaNotFound: "META nem található.",
          compileError: "Sikertelen indítás. Részletekért lásd a konzolt.",
          wasUnloaded: "{{name}} nem indult el.",
          isEnabled: "Bekapcsolva",
          wasLoaded: "{{name}} v{{{version}} betöltődött."
        },
        CustomCSS: {
          confirmationText: "Az Egyéni CSS módosításiat nem kerültek mentésre. Ha bezárja ezt az ablakot, ezek a módosítások elvesznek.",
          update: "Frissítés",
          save: "Mentés",
          openNative: "Megnyitás a Rendszer szerkesztőjében",
          openDetached: "Különálló ablak",
          settings: "Szerkesztő beállítások",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Biztos vagy benne?",
          okay: "Oké",
          done: "Kész",
          cancel: "Mégse",
          nevermind: "Mégse",
          close: "Bezárás",
          name: "Név",
          message: "Üzenet",
          error: "Hiba",
          addonErrors: "Kiegészítők hibái",
          restartRequired: "Újriandítás szükséges",
          restartNow: "Újriandítás most",
          restartLater: "Újriandítás később",
          additionalInfo: "További információk",
          restartPrompt: "A Discordot újra kell indítani ahhoz, hogy érvénybe lépjen a változtatás. Most akarod újriandítani?"
        },
        Sorting: {
          sortBy: "Rendezés",
          order: "Rendezés",
          ascending: "Növekvő",
          descending: "Csökkenő"
        },
        WindowPrefs: {
          enabledInfo: "Ez az opció átlátszó témát igényel a megfelelő működéshez. Windowson ez megszakíthatja az aero snappinget és a maximalizálást.\n\nA Discordot újra kell indítani, hogy érvénybe lépjen. Szeretnéd most újriandítani?",
          disabledInfo: "A Discordot újra kell indítani ahhoz, hogy érvénybe lépjen. Szeretnéd most újriandítani?"
        },
        Updater: {
          updateFialed: "A frissítés sikertelen!",
          updateFialedMessage: "A InAccord frissítése sikertelen. Kérjük, töltse le a telepítő legújabb verzióját erről a weboldalról: (https://InAccord.app/), majd telepítse újra.",
          updateSuccessful: "Frissítés sikeres!",
          updateAvialable: "Új frissítés erélhető a InAccordhoz: (v{{version}})",
          addonUpdated: "A {{name}} frissült {{version}} verzióra!",
          checking: "Frissítések keresése!",
          finishedChecking: "Frissítések ellenőrzése sikeres!",
          checkForUpdates: "Frissítések keresése!",
          updateAll: "Az összes frissítése!",
          noUpdatesAvialable: "Nincsenek elérhető frissítések.",
          versionAvialable: "Verziófrissítés elérhető: {{version}}!",
          updateButton: "Frissítés!"
        }
      };
    }
  });

  // assets/locales/it.json
  var require_it = __commonJS({
    "assets/locales/it.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plugins",
          themes: "Temi",
          customcss: "CSS Personalizzato",
          updates: "Aggiornamenti"
        },
        Collections: {
          settings: {
            name: "Impostazioni",
            general: {
              name: "Generale",
              voiceDisconnect: {
                name: "Disconnessione vocale",
                note: "Disconnettiti dalla chat vocale alla chiusura di Discord."
              },
              showToasts: {
                name: "Mostra notifiche",
                note: "Mostra una piccola notifica per le informazioni importanti."
              },
              mediaKeys: {
                name: "Disattiva i tasti multimediali",
                note: "Impedisce a Discord di comandare i tasti multimediali dopo la riproduzione di un video."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Rimuovi dimensione minima",
                note: "Rimuove la dimensione minima forzata della finestra di Discord di 940x500."
              },
              name: "Preferenze Finestra",
              transparency: {
                name: "Attiva trasparenza",
                note: "Consente alla finestra principale di essere trasparente (richiede il riavvio)."
              },
              frame: {
                name: "Frame finestra",
                note: "Aggiunge il frame della finestra del sistema operativo nativo alla finestra principale."
              }
            },
            addons: {
              name: "Gestione Addons",
              addonErrors: {
                name: "Mostra errori addons",
                note: "Mostra una modale con errori dei plugins/temi."
              },
              editAction: {
                name: "Azione di modifica",
                note: "Seleziona dove vengono mostrati plugins e temi durante la modifica.",
                options: {
                  detached: "Finestra separata",
                  system: "Editor di sistema"
                }
              }
            },
            customcss: {
              name: "CSS Personalizzato",
              customcss: {
                name: "CSS Personalizzato",
                note: "Attiva la scheda di CSS personalizzato."
              },
              liveUpdate: {
                name: "Aggiornamento automatico",
                note: "Aggiorna il CSS durante la modifica."
              },
              startDetached: {
                name: "Finestra separata",
                note: "Cliccando sulla scheda CSS personalizzato si apre l'editor in una finestra separata."
              },
              nativeOpen: {
                name: "Editor nativo",
                note: "Cliccando sulla scheda CSS personalizzato si apre il CSS personalizzato nell'editor nativo."
              },
              openAction: {
                name: "Posizione dell'editor",
                note: "Seleziona dove il CSS personalizzato dovrebbe aprirsi per impostazione predefinita.",
                options: {
                  settings: "Menu impostazioni",
                  detached: "Finestra separata",
                  system: "Editor di sistema"
                }
              }
            },
            developer: {
              name: "Impostazioni Sviluppatore",
              debuggerHotkey: {
                name: "Debugger Hotkey",
                note: "Consente di attivare il debugger quando si preme F8 con DevTools aperto."
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Inietta la tua installazione locale di React Developer Tools in Discord."
              },
              inspectElement: {
                name: "Inspect Element Hotkey",
                note: "Consente di attivare il tasto ispeziona elemento (CTRL+SHIFT+C) comune nella maggior parte dei browser."
              },
              devToolsWarning: {
                name: "Stop DevTools Warning",
                note: 'Impedisce a Discord di stampare il messaggio "Hold Up!".'
              },
              debugLogs: {
                name: "Debug Logs",
                note: "Consente che tutto l'output della console venga salvato nel file debug.log nella cartella InAccord."
              },
              devTools: {
                name: "DevTools",
                note: "Consente la commutazione di DevTools con CTRL+SHIFT+I."
              }
            },
            editor: {
              name: "Preferenze dell'Editor",
              lineNumbers: {
                name: "Numeri di riga",
                note: "Consente di mostrare i numeri di riga sul lato dell'editor."
              },
              fontSize: {
                name: "Dimensione carattere",
                note: "Dimensione del carattere (pt) da utilizzare nell'editor."
              },
              minimap: {
                name: "Minimappa",
                note: "Consente di mostrare la minimappa del codice sul lato dell'editor."
              },
              hover: {
                name: "Suggerimenti di riferimento",
                note: "Consente di mostrare i suggerimenti di riferimento quando si passa con il mouse su regole e selettori."
              },
              quickSuggestions: {
                name: "Suggerimenti rapidi",
                note: "Consente di mostrare i suggerimenti di completamento automatico durante la digitazione."
              },
              renderWhitespace: {
                name: "Mostra spazi bianchi",
                note: "Seleziona quando gli spazi bianchi dovrebbero essere mostrati dall'editor.",
                options: {
                  all: "Sempre",
                  none: "Mia",
                  selection: "Selezione"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} di {{author}}",
          byline: "di {{author}}",
          reload: "Ricarica",
          addonSettings: "Impostazioni",
          website: "Website",
          source: "Sorgente",
          invite: "Discord",
          donate: "Dona",
          patreon: "Patreon",
          name: "Nome",
          author: "Autore",
          version: "Versione",
          added: "Data di aggiunta",
          modified: "Data di modifica",
          editAddon: "Modifica",
          deleteAddon: "Elimina",
          confirmDelete: "Sei sicuro di voler eliminare {{name}}?",
          confirmationText: "Hia modifiche non salvate a {{name}}. Chiudendo questa finestra tutte le modifiche andranno perse.",
          enabled: "{{name}} è stato attivato.",
          disabled: "{{name}} è stato disattivato.",
          couldNotEnable: "{{name}} non può essere attivato.",
          couldNotDisable: "{{name}} non può essere disattivato.",
          couldNotStart: "{{name}} non può essere avviato.",
          couldNotStop: "{{name}} non può essere arrestato.",
          settingsError: "Impossibile aprire le impostazioni per {{name}}",
          methodError: "{{method}} non può essere restituito.",
          unknownAuthor: "Autore sconosciuto",
          noDescription: "Descrizione non fornita.",
          metaError: "META non può essere analizzato.",
          missingNameData: "Dati del nome META mancanti.",
          metaNotFound: "META non è stato trovato.",
          compileError: "Impossibile compilare. Vedi la console per i dettagli.",
          wasUnloaded: "{{name}} è stato disattivato.",
          isEnabled: "Attivato",
          wasLoaded: "{{name}} v{{version}} è stato caricato.",
          listView: "Vista a elenco",
          gridView: "Vista a griglia"
        },
        CustomCSS: {
          confirmationText: "Hia modifiche non salvate al tuo CSS personalizzato. Chiudendo questa finestra tutte le modifiche andranno perse.",
          update: "Aggiorna",
          save: "Salva",
          openNative: "Editor di sistema",
          openDetached: "Finestra separata",
          settings: "Impostazioni Editor",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Sei sicuro?",
          okay: "Okay",
          done: "Fatto",
          cancel: "Annulla",
          nevermind: "Non importa",
          close: "Chiudi",
          name: "Nome",
          message: "Messaggio",
          error: "Errore",
          addonErrors: "Errori addon",
          restartRequired: "Riavvio richiesto",
          restartNow: "Riavvia ora",
          restartLater: "Riavvia più tardi",
          additionalInfo: "Informazioni aggiuntive",
          restartPrompt: "Per avere effetto, Discord deve essere riavviato. Vuoi riavviare ora?"
        },
        Sorting: {
          sortBy: "Ordina per",
          order: "Ordine",
          ascending: "Ascendente",
          descending: "Discendente"
        },
        WindowPrefs: {
          enabledInfo: "Questa opzione richiede un tema trasparente per funzionare correttamente. Su Windows questo potrebbe interrompere l'aero snapping e la massimizzazione.\n\nPer avere effetto, Discord deve essere riavviato. Vuoi riavviare ora?",
          disabledInfo: "Per avere effetto, Discord deve essere riavviato. Vuoi riavviare ora?"
        },
        Updater: {
          updateFialed: "Aggiornamento non riuscito!",
          updateFialedMessage: "InAccord non è stato aggiornato. Scarica l'ultima versione del programma di installazione dal nostro sito web (https://InAccord.app/) e reinstalla.",
          updateSuccessful: "Aggiornamento riuscito!",
          updateAvialable: "InAccord ha un nuovo aggiornamento (v{{version}})",
          addonUpdated: "{{name}} è stato aggiornato alla versione {{version}}!",
          checking: "Controlla aggiornamenti!",
          finishedChecking: "Controllo degli aggiornamenti finito!",
          checkForUpdates: "Controlla aggiornamenti!",
          updateAll: "Aggiorna tutto!",
          noUpdatesAvialable: "Nessun aggiornamento disponibile.",
          versionAvialable: "La versione {{version}} è ora disponibile!",
          updateButton: "Aggiorna!"
        }
      };
    }
  });

  // assets/locales/ja.json
  var require_ja = __commonJS({
    "assets/locales/ja.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "プラグイン",
          themes: "テーマ",
          customcss: "カスタムCSS",
          updates: "アップデート"
        },
        Collections: {
          settings: {
            name: "設定",
            general: {
              name: "全般",
              voiceDisconnect: {
                name: "ボイスチャンネルの切断",
                note: "Discordを終了すると自動的にボイスチャンネルから切断されます。"
              },
              showToasts: {
                name: "トースト表示",
                note: "重要な情報を小さな通知で表示します。"
              },
              mediaKeys: {
                name: "メディアキー無効化",
                note: "ビデオ再生後にDiscordがメディアキーを乗っ取るのを防ぎます。"
              }
            },
            window: {
              removeMinimumSize: {
                name: "最小サイズ削除",
                note: "Discordの最小ウィンドウサイズである940x500を強制的に解除します。"
              },
              name: "ウィンドウ設定",
              transparency: {
                name: "透過を有効",
                note: "Discordのメインウィンドウを透過します(要再起動)。"
              },
              frame: {
                name: "ウィンドウフレームの表示",
                note: "メインウィンドウにOSごとのウィンドウフレームを追加します。"
              }
            },
            addons: {
              name: "アドオンマネージャー",
              addonErrors: {
                name: "アドオンエラーの表示",
                note: "プラグイン/テーマのエラーを表示します。"
              },
              editAction: {
                name: "アドオンの編集",
                note: "プラグインとテーマの編集時の表示位置。",
                options: {
                  detached: "Discord内で開く",
                  system: "外部エディタで開く"
                }
              }
            },
            customcss: {
              name: "カスタムCSS",
              customcss: {
                name: "カスタムCSS",
                note: "カスタムCSSタブを有効にします。"
              },
              liveUpdate: {
                name: "自動リアルタイム更新",
                note: "入力と同時に自動でCSSを更新"
              },
              startDetached: {
                name: "ウィンドウ表示",
                note: "カスタムCSSタブをクリックすると、エディターが別ウィンドウで表示されます"
              },
              nativeOpen: {
                name: "外部エディタを起動",
                note: "カスタムCSSを外部のエディタで起動します"
              },
              openAction: {
                name: "エディター",
                note: "カスタムCSSがデフォルトで開かれる場所",
                options: {
                  settings: "設定メニュー",
                  detached: "内部エディター",
                  system: "システム既定のエディター"
                }
              }
            },
            developer: {
              name: "開発者設定",
              debuggerHotkey: {
                name: "デバッグ用ホットキー",
                note: "デベロッパーツールを開いた状態でF8キーを押すとデバッガが起動します。"
              },
              reactDevTools: {
                name: "React開発者向けツール",
                note: "ローカルにインストールしたReact Developer ToolsをDiscordに挿入します。"
              },
              inspectElement: {
                name: "インスペクト・エレメント・ホットキー",
                note: "ほとんどのブラウザで一般的なinspect要素のホットキー(Ctrl + Shift + C)を有効にします。"
              },
              devToolsWarning: {
                name: "DevToolsの警告を停止",
                note: 'Discordの "Hold Up!"メッセージの表示を停止します。'
              },
              debugLogs: {
                name: "デバッグログ",
                note: "コンソールからInAccordフォルダのdebug.logファイルに全てを出力します。"
              },
              devTools: {
                name: "デベロッパーツール",
                note: "ctrl+shift+iでデベロッパーツールの切り替えができます。"
              }
            },
            editor: {
              name: "エディターの設定",
              lineNumbers: {
                name: "行番号",
                note: "エディターの横に行番号を表示します。"
              },
              fontSize: {
                name: "フォントサイズ",
                note: "エディターで使用するフォントの大きさ(pt)を変更する。"
              },
              minimap: {
                name: "ミニマップ",
                note: "エディター横にコード用のミニマップを表示します。"
              },
              hover: {
                name: "参照用ツールチップ",
                note: "ルールやセレクタをホバーしたときに、参照用のツールチップを表示します。"
              },
              quickSuggestions: {
                name: "入力補助",
                note: "入力中にオートコンプリートの候補を表示します。"
              },
              renderWhitespace: {
                name: "空白を表示",
                note: "エディターで空白を表示する場合",
                options: {
                  all: "常時",
                  none: "無し",
                  selection: "選択"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} by {{author}}",
          byline: "by {{author}}",
          reload: "リロード",
          addonSettings: "設定",
          website: "ウェブサイト",
          source: "ソース",
          invite: "サポートサーバー",
          donate: "寄付",
          patreon: "Patreon",
          name: "名称",
          author: "作者",
          version: "バージョン",
          added: "追加日",
          modified: "更新日",
          editAddon: "編集",
          deleteAddon: "削除",
          confirmDelete: "本当に{{name}}を削除しますか？",
          confirmationText: "{{name}}に未保存のデータがあります。このウィンドウを閉じると、それらの変更はすべて失われます。",
          enabled: "{{name}}を有効化しました。",
          disabled: "{{name}}を無効化しました。",
          couldNotEnable: "{{name}}を有効化できませんでした。",
          couldNotDisable: "{{name}}を無効化できませんでした。",
          couldNotStart: "{{name}}を起動できませんでした。",
          couldNotStop: "{{name}}を停止できませんでした。",
          settingsError: "{{name}}の設定を開けませんでした。",
          methodError: "{{method}}を起動できませんでした。",
          unknownAuthor: "作者不明",
          noDescription: "説明はありません。",
          metaError: "METAを解析できませんでした。",
          missingNameData: "METAの名前データがありません。",
          metaNotFound: "METAが見つかりませんでした。",
          compileError: "コンパイルできませんでした。詳しくはコンソールをご覧ください。",
          wasUnloaded: "{{name}}が解除されました。",
          isEnabled: "有効",
          wasLoaded: "{{name}}のv{{version}}が読み込まれました。"
        },
        CustomCSS: {
          confirmationText: "カスタムCSSに未保存のデータがあります。このウィンドウを閉じると、これらの変更はすべて失われます。",
          update: "アップデート",
          save: "保存",
          openNative: "外部エディタで開く",
          openDetached: "別ウィンドウで開く",
          settings: "エディター設定",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "本当によろしいですか？",
          okay: "はい",
          done: "Done",
          cancel: "いいえ",
          nevermind: "Nevermind",
          close: "QUIT",
          name: "名前",
          message: "メッセージ",
          error: "エラー",
          addonErrors: "アドオンエラー",
          restartRequired: "再起動が必要",
          restartNow: "今すぐ再起動",
          restartLater: "後で再起動",
          additionalInfo: "その他の情報",
          restartPrompt: "有効にするためには、Discordを再起動する必要があります。今すぐ再起動しますか？"
        },
        Sorting: {
          sortBy: "並び順",
          order: "順序",
          ascending: "昇順",
          descending: "降順"
        },
        WindowPrefs: {
          enabledInfo: "このオプションが正しく動作するためには、透明なテーマが必要です。Windows では、エアロスナップと最大化が壊れる可能性があります。\n\nこのオプションを有効にするには、Discord を再起動する必要があります。今すぐ再起動しますか？",
          disabledInfo: "有効にするためには、Discordを再起動する必要があります。今すぐ再起動しますか？"
        },
        Updater: {
          updateFialed: "アップデートに失敗しました！",
          updateFialedMessage: "InAccordのアップデートに失敗しました。ホームページ(https://InAccord.app/)から最新版のインストーラーをダウンロードして、再インストールしてください。",
          updateSuccessful: "アップデートに成功しました！",
          updateAvialable: "InAccordに新しいアップデート(v{{version}})があります。",
          addonUpdated: "{{name}}がバージョン{{version}}にアップデートされました！",
          checking: "アップデートを確認",
          finishedChecking: "アップデートの確認が終了しました！",
          checkForUpdates: "アップデートを確認",
          updateAll: "全てアップデート",
          noUpdatesAvialable: "アップデートはありません。",
          versionAvialable: "バージョン{{version}}を公開しました。",
          updateButton: "アップデート"
        }
      };
    }
  });

  // assets/locales/ko.json
  var require_ko = __commonJS({
    "assets/locales/ko.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "플러그인",
          themes: "테마",
          customcss: "사용자 지정 CSS",
          updates: "업데이트"
        },
        Collections: {
          settings: {
            name: "설정",
            general: {
              name: "일반",
              voiceDisconnect: {
                name: "음성 연결 끊기",
                note: "디스코드를 닫을 때 음성 채널의 연결을 끊습니다"
              },
              showToasts: {
                name: "토스트 메시지 표시",
                note: "중요한 정보를 위한 작은 알림을 표시합니다"
              },
              mediaKeys: {
                name: "미디어 키 비활성화",
                note: "영상을 재생한 후, 디스코드가 미디어 키를 가로채는 것을 방지합니다"
              }
            },
            window: {
              removeMinimumSize: {
                name: "최소 크기 제거",
                note: "디스코드의 강제 창 최소 크기 제한 (940x500) 을 제거합니다"
              },
              name: "창 선호",
              transparency: {
                name: "투명도 활성화",
                note: "기본 창이 투명하게 보이는 것을 활성화합니다 (재시작 요구됨)"
              },
              frame: {
                name: "창 프레임",
                note: "기본 창에 기본 OS 창 프레임을 추가합니다"
              }
            },
            addons: {
              name: "애드온 관리자",
              addonErrors: {
                name: "애드온 에러 표시",
                note: "플러그인/테마 오류가 있는 모달을 표시합니다"
              },
              editAction: {
                name: "액션 수정",
                note: "수정할 때 플러그인과 테마가 나타나는 곳",
                options: {
                  detached: "분리 창",
                  system: "시스템 편집기"
                }
              }
            },
            customcss: {
              name: "사용자 지정 CSS",
              customcss: {
                name: "사용자 지정 CSS",
                note: "사용자 지정 CSS 탭을 활성화합니다"
              },
              liveUpdate: {
                name: "실시간 업데이트",
                note: "css를 작성하는 대로 업데이트합니다"
              },
              startDetached: {
                name: "분리 창 시작",
                note: "사용자 지정 CSS 탭을 클릭하면 분리된 창에서 편집기가 열립니다"
              },
              nativeOpen: {
                name: "기본 편집기에서 열기",
                note: "사용자 지정 CSS 탭을 클릭하면 기본 편집기에서 사용자 지정 css가 열립니다"
              },
              openAction: {
                name: "위치 편집기",
                note: "기본적으로 사용자 지정 CSS가 열리는 곳",
                options: {
                  settings: "설정 메뉴",
                  detached: "분리된 창",
                  system: "시스템 편집기"
                }
              }
            },
            developer: {
              name: "개발자 설정",
              debuggerHotkey: {
                name: "디버거 단축키",
                note: "F8을 누를 때 디버거를 활성화합니다"
              },
              reactDevTools: {
                name: "React 개발자 도구",
                note: "로컬 환경에 설치된 React 개발자 도구를 디스코드에 삽입합니다"
              },
              inspectElement: {
                name: "요소 선택 단축키",
                note: "대부분의 브라우저에서 가장 흔한 요소 선택 단축키를 활성화합니다 (ctrl + shift + c)"
              },
              devToolsWarning: {
                name: "DevTools 경고 멈추기",
                note: '디스코드가 \\"Hold Up!\\" 메시지를 표시하는 것을 멈춥니다'
              },
              debugLogs: {
                name: "디버그 로그",
                note: "콘솔의 모든 출력을 InAccord 폴더의 debug.log 파일에 저장합니다"
              },
              devTools: {
                name: "개발자 도구",
                note: "ctrl+shift+i를 사용하여 개발자 도구를 활성화합니다"
              }
            },
            editor: {
              name: "편집기 선호",
              lineNumbers: {
                name: "행 번호",
                note: "편집기 측면에 행 번호 표시를 활성화합니"
              },
              fontSize: {
                name: "폰트 크기",
                note: "편집기에서 사용할 폰트 (pt) 크기"
              },
              minimap: {
                name: "미니맵",
                note: "편집기 측면에 코드 미니맵 표시를 활성화합니다"
              },
              hover: {
                name: "참조 툴팁",
                note: "규칙 및 선택기에 마우스 커서를 올릴 때 참조 툴팁을 활성화합니다"
              },
              quickSuggestions: {
                name: "빠른 제안",
                note: "입력할 때 자동 완성 제안 표시를 활성화합니"
              },
              renderWhitespace: {
                name: "공백 표시",
                note: "편집기가 공백을 표시해야 하는 경우",
                options: {
                  all: "항상 사용",
                  none: "항상 무시",
                  selection: "선택"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} by {{author}}",
          byline: "by {{author}}",
          reload: "리로드",
          addonSettings: "설정",
          website: "웹사이트",
          source: "소스",
          invite: "서버 지원",
          donate: "기부",
          patreon: "패트리온",
          name: "이름",
          author: "제작자",
          version: "버전",
          added: "추가된 날짜",
          modified: "수정된 날짜",
          editAddon: "수정",
          deleteAddon: "삭제",
          confirmDelete: "{{name}}(을)를 삭제하시겠습니까?",
          confirmationText: "{{name}}에 저장되지 않은 수정사항이 있습니다. 이 창을 닫으면 모든 수정이 취소됩니다.",
          enabled: "{{name}}이 활성화되었습니다",
          disabled: "{{name}}이 비활성화되었습니다",
          couldNotEnable: "{{name}}(을)를 활성화하지 못했습니다",
          couldNotDisable: "{{name}}(을)를 비활성화하지 못했습니다",
          couldNotStart: "{{name}}(을)를 시작하지 못했습니다",
          couldNotStop: "{{name}}(을)를 정지하지 못했습니다",
          settingsError: "{{name}} 의 설정을 열 수 없습니다",
          methodError: "{{method}}(을)를 실행할 수 없습니다",
          unknownAuthor: "알 수 없는 제작자",
          noDescription: "설명이 제공되지 않았습니다",
          metaError: "META 파싱에 실패했습니다",
          missingNameData: "META 이름 데이터 찾을 수 없음",
          metaNotFound: "META 찾을 수 없음",
          compileError: "컴파일에 실패했습니다",
          wasUnloaded: "{{name}}가 언로드되었습니다",
          isEnabled: "활성화됨",
          wasLoaded: "{{name}} v{{version}} 가 로드됨."
        },
        CustomCSS: {
          confirmationText: "사용자 지정 CSS에 저장되지 않은 수정사항이 있습니다. 이 창을 닫으면 모든 수정이 취소됩니다",
          update: "업데이트",
          save: "저장",
          openNative: "시스템 편집기에서 열기",
          openDetached: "분리된 창에서 열기",
          settings: "편집기 설정",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "확실합니까?",
          okay: "확인",
          done: "완료",
          cancel: "취소",
          nevermind: "무시",
          close: "닫기",
          name: "이름",
          message: "메시지",
          error: "오류",
          addonErrors: "애드온 오류",
          restartRequired: "재시작 요구됨",
          restartNow: "지금 재시작하기",
          restartLater: "나중에 재시작하기",
          additionalInfo: "추가 정보",
          restartPrompt: "적용을 위해 디스코드가 재시작되어야 합니다. 지금 재시작하시겠습니까?"
        },
        Sorting: {
          sortBy: "유형: ",
          order: "순서: ",
          ascending: "오름차순",
          descending: "내림차순"
        },
        WindowPrefs: {
          enabledInfo: "이 옵션은 제대로 작동하려면 투명한 테마가 필요합니다. Windows에서는 에어로 스내핑과 최대화가 고장날 수 있습니다.\n\n적용을 위해 디스코드를 재시작해야 합니다. 지금 재시작하시겠습니까?",
          disabledInfo: "적용을 위해 디스코드를 재시작해야 합니다. 지금 재시작하시겠습니까?"
        },
        Updater: {
          updateFialed: "업데이트가 실패했습니다!",
          updateFialedMessage: "InAccord가 업데이트에 실패했습니다. 저희 웹사이트 (https://InAccord.app/) 에서 설치기를 받아 재설치하십시오.",
          updateSuccessful: "업데이트가 성공적으로 완료되었습니다!",
          updateAvialable: "InAccord에 새로운 업데이트가 있습니다 (v{{version}})",
          addonUpdated: "{{name}} 이 버전 {{version}} 으로 업데이트 되었습니다!",
          checking: "업데이트를 확인하고 있습니다!",
          finishedChecking: "업데이트 확인을 마쳤습니다!",
          checkForUpdates: "업데이트 확인하기!",
          updateAll: "모두 업데이트 되었습니다!",
          noUpdatesAvialable: "사용 가능한 업데이트가 없습니다.",
          versionAvialable: "버전 {{version}} 을 사용할 수 있습니다!",
          updateButton: "업데이트!"
        }
      };
    }
  });

  // assets/locales/pl.json
  var require_pl = __commonJS({
    "assets/locales/pl.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Wtyczki",
          themes: "Motywy",
          customcss: "Niestandardowy CSS",
          updates: "Aktualizacje"
        },
        Collections: {
          settings: {
            name: "Ustawienia",
            general: {
              name: "Ogólne",
              voiceDisconnect: {
                name: "Rozłączaj z czatem głosowym",
                note: "Rozłącza z serwerem czatu głosowego przy wyłączeniu Discorda"
              },
              showToasts: {
                name: "Wyświetlaj okienka powiadomień",
                note: "Wyświetla małe powiadomienia dla ważnych zdarzeń"
              },
              mediaKeys: {
                name: "Wyłącz przyciski multimedialne",
                note: "Zapobiega korzystaniu z klawiszy multimedialnych przez Discorda podczas odtwarzania filmów."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Usuń limit minimalnego rozmiaru okna",
                note: "Usuwa wymuszony przez Discorda limit minimalnego rozmiaru okna (940x500)"
              },
              name: "Ustawienia okna",
              transparency: {
                name: "Włącz przezroczystość",
                note: "Dodaje efekt przezroczytstości do okna Discorda (wymaga ponownego uruchomienia)"
              },
              frame: {
                name: "Ramka okna",
                note: "Dodaje systemową ramkę do okna Discorda"
              }
            },
            addons: {
              name: "Menedżer dodatków",
              addonErrors: {
                name: "Wyświetlaj błędy dodatków",
                note: "Wyświetla okno z błędami wtyczek i motywów"
              },
              editAction: {
                name: "Edytor dodatków",
                note: "Ustawia domyślny edytor kodu wtyczek i motywów",
                options: {
                  detached: "Osobne okno",
                  system: "Edytor systemowy"
                }
              }
            },
            customcss: {
              name: "Niestandardowy CSS",
              customcss: {
                name: "Niestandardowy CSS",
                note: "Wyświetla zakładkę z edytorem niestandardowego kodu CSS"
              },
              liveUpdate: {
                name: "Automatyczny podgląd",
                note: "Aktualizuje CSS Discorda wraz z pisaniem kodu"
              },
              startDetached: {
                name: "Osobne okno edytora",
                note: "Kliknięcie na zakładkę „Niestandardowy CSS” uruchomi edytor w osobnym oknie"
              },
              nativeOpen: {
                name: "Otwórz w edytorze systemowym",
                note: "Kliknięcie na zakładkę „Niestandardowy CSS” uruchomi plik w edytorze systemowym"
              },
              openAction: {
                name: "Domyślny edytor",
                note: "Ustawia domyślny edytor niestandardowego kodu CSS",
                options: {
                  settings: "Ustawienia",
                  detached: "Osobne okno",
                  system: "Edytor systemowy"
                }
              }
            },
            developer: {
              name: "Ustawienia dla programistów",
              debuggerHotkey: {
                name: "Debugger",
                note: "Uruchamia debugger po naciśnięciu F8 przy otworzonym panelu narzędzi dla programistów"
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Wprowadza zianstalowane na Twoim komputerze rozszerzenie React Developer Tools do Discorda"
              },
              inspectElement: {
                name: "Narzędzie „Zbadaj element”",
                note: "Umożliwia korzystanie z narzędzia „Zbadaj element” po użyciu skrótu klawiszowego Ctrl+Shift+C"
              },
              devToolsWarning: {
                name: "Nie wyświetlaj ostrzeżenia o szybkim wysyłaniu wiadomości",
                note: "Zapobiega wyświetlaniu informacji o zbyt szybkim wysyłaniu wiadomości przez Discorda"
              },
              debugLogs: {
                name: "Dziennik zdarzeń",
                note: "Przekierowuje informacje z konsoli do pliku debug.log w folderze InAccorda"
              },
              devTools: {
                name: "Narzędzia dla programistów",
                note: "Umożliwia wyświetlenie panelu narzędzi dla programistów po użyciu skrótu klawiszowego Ctrl+Shift+I"
              }
            },
            editor: {
              name: "Ustawienia edytora",
              lineNumbers: {
                name: "Numery wierszy",
                note: "Wyświetla numery wierszy po lewej stronie edytora"
              },
              fontSize: {
                name: "Rozmiar czcionki",
                note: "Ustawia rozmiar czcionki (pt) w edytorze"
              },
              minimap: {
                name: "Podgląd",
                note: "Wyświetla mały podgląd kodu po prawej stronie edytora"
              },
              hover: {
                name: "Etykiety",
                note: "Wyświetla etykiety z dodatkowymi informacjami po najechaniu na element kodu"
              },
              quickSuggestions: {
                name: "Podpowiadanie składni",
                note: "Włącza automatyczne podpowiadanie składni podczas pisania kodu"
              },
              renderWhitespace: {
                name: "Pokaż białe znaki",
                note: "Określa, kiedy białe znaki będą wyświetlane w edytorze",
                options: {
                  all: "Zawsze",
                  none: "Nigdy",
                  selection: "Przy zaznaczaniu"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} (wersja {{version}}) autorstwa {{author}}",
          byline: "autorstwa {{author}}",
          reload: "Odśwież",
          addonSettings: "Ustawienia",
          website: "Strona internetowa",
          source: "Źródło",
          invite: "Wspomóż serwer",
          donate: "Przekaż darowiznę",
          patreon: "Patreon",
          name: "Nazwa",
          author: "Twórca",
          version: "Wersja",
          added: "Data dodania",
          modified: "Data modyfikacji",
          search: "Wyszukaj",
          editAddon: "Edytuj",
          deleteAddon: "Usuń",
          confirmDelete: "Czy na pewno chcesz usunąć {{name}}?",
          confirmationText: "Masz niezapisane zmiany w kodzie {{name}}. Zamknięcie tego okna spowoduje odrzucenie wszystkich zmian.",
          enabled: "Dodatek {{name}} został włączony.",
          disabled: "Dodatek {{name}} został wyłączony.",
          couldNotEnable: "Dodatek {{name}} nie może zostać włączony.",
          couldNotDisable: "Dodatek {{name}} nie może zostać wyłączony.",
          couldNotStart: "Dodatek {{name}} nie może zostać uruchomiony.",
          couldNotStop: "Dodatek {{name}} nie może zostać zatrzymany.",
          settingsError: "Nie udało się otworzyć ustawień dodatku {{name}}",
          methodError: "Metoda {{method}} nie może zostać wywołana.",
          unknownAuthor: "Nieznany twórca",
          noDescription: "Brak opisu.",
          alreadyExists: "Masz już dodatek z nazwą {{name}}",
          metaError: "Nie udało się przetworzyć metadanych.",
          missingNameData: "Brak nazwy dodatku w metadanych.",
          metaNotFound: "Nie odnaleziono metadanych.",
          compileError: "Błąd kompilacji. Sprawdź konsolę, aby dowiedzieć się więcej.",
          wasUnloaded: "Dodatek {{name}} został odładowany.",
          blankSlateHeader: "Wygląda na to, że nic tu nie ma!",
          blankSlateMessage: "Pobierz nowe dodatki z [tej strony]({{link}}) i przenieś je do odpowiedniego folderu.",
          isEnabled: "Stan",
          wasLoaded: "Dodatek {{name}} (wersja {{version}}) został załadowany."
        },
        CustomCSS: {
          confirmationText: "Masz niezapisane zmiany w swoim niestandardowym kodzie CSS. Zamknięcie tego okienka spowoduje odrzucenie wszystkich zmian.",
          update: "Zaktualizuj",
          save: "Zapisz",
          openNative: "Otwórz w edytorze systemowym",
          openDetached: "Otwórz w osobnym oknie",
          settings: "Ustawienia edytora",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Czy na pewno chcesz to zrobić?",
          okay: "Tak",
          done: "Potwierdź",
          cancel: "Anuluj",
          nevermind: "Anuluj",
          close: "Zamknij",
          name: "Nazwa",
          message: "Komunikat",
          error: "Błąd",
          addonErrors: "Błędy wtyczek",
          restartRequired: "Wymagane ponowne uruchomienie",
          restartNow: "Uruchom ponownie teraz",
          restartLater: "Uruchom ponownie później",
          additionalInfo: "Dodatkowe informacje",
          restartPrompt: "Uruchom ponownie Discorda, by zastosować zmiany. Czy chcesz to zrobić teraz?"
        },
        Sorting: {
          sortBy: "Sortuj wg",
          order: "Kolejność",
          ascending: "Rosnąca",
          descending: "Malejąca"
        },
        WindowPrefs: {
          enabledInfo: "Ta opcja wymaga przezroczystego motywu do poprawnego działania. Na Windowsie mogą pojawić się problemy ze zmianą wielkości okna.\n\nUruchom ponownie Discorda, by zastosować zmiany. Czy chcesz to zrobić teraz?",
          disabledInfo: "Uruchom ponownie Discorda, by zmiany odniosły efekty. Czy chcesz to zrobić teraz?"
        },
        Updater: {
          updateFialed: "Aktualizacja nie powiodła się!",
          updateFialedMessage: "Nie udało się zaktualizować InAccorda. Pobierz najnowszy instalator z naszej strony (https://InAccord.app/) i zianstaluj aplikację ponownie.",
          updateSuccessful: "Aktualizacja powiodła się!",
          updateAvialable: "Dostępna jest nowa aktualizacja InAccorda (wersja {{version}})",
          addonUpdated: "Dodatek {{name}} został zaktualizowany do wersji {{version}}!",
          checking: "Sprawdzanie dostępności aktualizacji!",
          finishedChecking: "Zakończono sprawdzanie dostępności aktualizacji!",
          checkForUpdates: "Sprawdź dostępność aktualizacji",
          updateAll: "Zaktualizuj wszystko",
          noUpdatesAvialable: "Brak dostępnych aktualizacji.",
          versionAvialable: "Dostępna jest nowa wersja ({{version}})!",
          upToDateBlankslate: "Wszystkie dodatki są aktualne!",
          updateButton: "Zaktualizuj"
        }
      };
    }
  });

  // assets/locales/pt-pt.json
  var require_pt_pt = __commonJS({
    "assets/locales/pt-pt.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plugins",
          themes: "Temas",
          customcss: "CSS Personalizado"
        },
        Collections: {
          settings: {
            name: "Definições",
            general: {
              name: "Geral",
              voiceDisconnect: {
                name: "Disconectar Voz",
                note: "Disconectar do servidor de voz quando fechar o discord"
              },
              showToasts: {
                name: "Mostrar notificações",
                note: "Mostra uma pequena notificação para informações importantes"
              },
              mediaKeys: {
                name: "Desativar as chaves Mídia",
                note: "Previne o Discord de dar hijack as chaves Mídia depois de ver um video."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Remove o tamanho mínimo",
                note: "Remove o tamanho mínimo obrigatório de janela do Discord de 940x500"
              },
              name: "Preferência de Janela",
              transparency: {
                name: "Ativar Transparência",
                note: "Ativa a janela principal para ter transparência (requer reniciar)"
              },
              frame: {
                name: "Moldura da janela",
                note: "Adiciona a moldura de janela nativa do sistema operativo à janela principal"
              }
            },
            addons: {
              name: "Gestor de extensões",
              addonErrors: {
                name: "Mostra os erros das extensões",
                note: "Mostra uma janela com os erros dos plugins/temas"
              },
              editAction: {
                name: "Editar ação",
                note: "Onde os plugins e os temas irão aparecer quando se edita",
                options: {
                  detached: "Separar janela",
                  system: "Editor de sistema"
                }
              }
            },
            customcss: {
              name: "CSS Personalizado",
              customcss: {
                name: "CSS Personalizado",
                note: "Ativa a aba de CSS Personalizado"
              },
              liveUpdate: {
                name: "Atualizações diretas",
                note: "Atualiza o css a medida que se escreve"
              },
              startDetached: {
                name: "Começar desanexado",
                note: "Clicar na aba de CSS Personalizado abre o editor numa janela separada"
              },
              nativeOpen: {
                name: "Abrir o editor Nativo",
                note: "Clicar na aba de CSS Personalizado abre o css personalizado no seu editor nativo"
              },
              openAction: {
                name: "Editor de localização",
                note: "Onde o CSS personalizado abre por predifinição",
                options: {
                  settings: "Menu de definições",
                  detached: "Separar Janela",
                  system: "Editor de sistema"
                }
              }
            },
            developer: {
              name: "Definições de desenvolvedor",
              debuggerHotkey: {
                name: "Debugger Hotkey",
                note: "Permite a ativação do debugger quando pressiona o F8"
              },
              reactDevTools: {
                name: "Ferramentas de Desenvolvedor React",
                note: "Injeta a instalação local da Ferramenta de Desenvolvedor React para o Discord"
              },
              inspectElement: {
                name: "Tecla de Inspecionar Elemento",
                note: "Ativa a tecla de inspecionar elemento (ctrl + shift + c) que é comum na miaoria dos navegadores browser"
              },
              devToolsWarning: {
                name: "Pára o aviso de DevTools",
                note: 'Pára o Discord de mostrar a mensagem "Hold up!"'
              },
              debugLogs: {
                name: "Debug Logs",
                note: "Saída de tudo da consola para o ficheiro debug.log na pasta InAccord"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} feito por {{author}}",
          byline: "feito por {{author}}",
          reload: "Recarregar",
          addonSettings: "Definições",
          website: "Website",
          source: "Fonte",
          invite: "Servidor de Suporte",
          donate: "Doar",
          patreon: "Patreon",
          name: "Nome",
          author: "Autor",
          version: "Versão",
          added: "Data Adicionada",
          modified: "Data Modificada",
          editAddon: "Editar",
          deleteAddon: "Apagar",
          confirmDelete: "Tem a certeza que quer apagar {{name}}?",
          confirmationText: "Tem alterações não guardadas no {{name}}. Se fechar esta janela irá perder essas alterações.",
          enabled: "{{name}} foi ativo.",
          disabled: "{{name}} foi desativado.",
          couldNotEnable: "{{name}} não foi possível ser ativado.",
          couldNotDisable: "{{name}} não pode ser desativado.",
          couldNotStart: "{{name}} não pode ser iniciado.",
          couldNotStop: "{{name}} não pode ser parádo.",
          settingsError: "Não foi possível abrir as configurações para {{name}}",
          methodError: "{{method}} não pode ser retirado.",
          unknownAuthor: "Autor Desconhecido",
          noDescription: "Descrição não fornecida.",
          metaError: "META não pôde ser analisado.",
          missingNameData: "Em falta dados do nome de META.",
          metaNotFound: "META não encontrada.",
          compileError: "Não foi possível compilar.",
          wasUnloaded: "{{name}} foi descarregado."
        },
        CustomCSS: {
          confirmationText: "Tem alterações não guardadas no CSS Personalizado. Se fechar esta janela irá perder essas alterações.",
          update: "Atualizar",
          save: "Guardar",
          openNative: "Abrir o Editor de Sistema",
          openDetached: "Separar Janela",
          settings: "Definições de Editor",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Tem a certeza?",
          okay: "Okay",
          done: "Feito",
          cancel: "Cancelar",
          nevermind: "Deixa estár",
          close: "Fechar",
          name: "Nome",
          message: "Mensagem",
          error: "Erro",
          addonErrors: "Erro de extensão",
          restartRequired: "Necessário Reniciar",
          restartNow: "Reniciar Agora",
          restartLater: "Reniciar Mias Tarde",
          additionalInfo: "Informação Adicional",
          restartPrompt: "Para ter efeito, o Discord precisa ser reiniciado. Deseja reiniciar agora?"
        },
        Sorting: {
          sortBy: "Ordenar por",
          order: "Ordenar",
          ascending: "Ascendente",
          descending: "Descendente"
        },
        WindowPrefs: {
          enabledInfo: "Esta opção requer um tema transparente para funcionar corretamente. No Windows, isso pode interromper o ajuste e a maximização do aero.\n\nPara ter efeito, o Discord precisa ser reiniciado. Quer reiniciar agora?",
          disabledInfo: "Para ter efeito, o Discord precisa ser reiniciado. Quer reiniciar agora?"
        }
      };
    }
  });

  // assets/locales/pt-br.json
  var require_pt_br = __commonJS({
    "assets/locales/pt-br.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plugins",
          themes: "Temas",
          customcss: "CSS Personalizado"
        },
        Collections: {
          settings: {
            name: "Configurações",
            general: {
              name: "Geral",
              voiceDisconnect: {
                name: "Desconexão de Voz",
                note: "Desconecta do servidor de voz quando o discord é fechado"
              },
              showToasts: {
                name: "Mostrar Toasts",
                note: "Mostra uma pequena notificação para informações importantes"
              },
              mediaKeys: {
                name: "Desabilitar Teclas de Media",
                note: "Previne que o discord roube suas teclas de media depois de dar play em um vídeo"
              }
            },
            window: {
              removeMinimumSize: {
                name: "Remover Tamanho Mínimo",
                note: "Remove o tamanho mínimo forçado pelo discord de 940x500"
              },
              name: "Preferências da Janela",
              transparency: {
                name: "Habilitar Transparência",
                note: "Faz a janela principal ser transparente (requer reinicialização)"
              },
              frame: {
                name: "Moldura da Janela",
                note: "Adiciona a moldura nativa na janela principal"
              }
            },
            addons: {
              name: "Gerenciador de Addons",
              addonErrors: {
                name: "Mostrar erros dos Addons",
                note: "Mostra um modal com os erros do plugin/tema"
              },
              editAction: {
                name: "Ação de Edição",
                note: "Onde plugins e temas aparecerão quando os editar",
                options: {
                  detached: "Janela Separada",
                  system: "Editor do Sistema"
                }
              }
            },
            customcss: {
              name: "CSS Personalizado",
              customcss: {
                name: "CSS Personalizado",
                note: "Habilita a aba de css personalizado"
              },
              liveUpdate: {
                name: "Atualização em Tempo Real",
                note: "Atualiza o css à medida que você digita"
              },
              startDetached: {
                name: "Iniciar Separado",
                note: "Clicar na aba do CSS Personalizado irá abrir o editor numa janela separada"
              },
              nativeOpen: {
                name: "Abrir no Editor Nativo",
                note: "Clicar na aba do CSS Personalizado irá o abrir usando seu editor nativo"
              },
              openAction: {
                name: "Localização do Editor",
                note: "Onde o CSS Personalizado deve aparecer por padrão",
                options: {
                  settings: "Menu de Configurações",
                  detached: "Janela Separada",
                  system: "Editor do Sistema"
                }
              }
            },
            developer: {
              name: "Configurações de Desenvolvedor",
              debuggerHotkey: {
                name: "Atalho para o Depurador",
                note: "Permite ativar o depurador quando pressionar F8"
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Injeta a sua instalação local do React Developer Tools no discord"
              },
              inspectElement: {
                name: "Atalho para Inspecionar Elemento",
                note: "Habilita o atalho para inspecionar elemento (ctrl + shift + c) que é comum na miaoria dos navegadores"
              },
              devToolsWarning: {
                name: "Remove os Avisos de DevTools",
                note: 'Faz o discord parar de imprimir as mensagens de "Calma Aí!"'
              },
              debugLogs: {
                name: "Registro de Depuração",
                note: "Salva todas os logs do console no arquivo debug.log dentro da pasta do InAccord"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} by {{author}}",
          byline: "por {{author}}",
          reload: "Recarregar",
          addonSettings: "Configurações",
          website: "Site",
          source: "Source",
          invite: "Servidor de Suporte",
          donate: "Doar",
          patreon: "Patreon",
          name: "Nome",
          author: "Autor",
          version: "Versão",
          added: "Date de Adição",
          modified: "Data de Modificação",
          editAddon: "Editar",
          deleteAddon: "Remover",
          confirmDelete: "Você tem certeza que quer remover o {{name}}?",
          confirmationText: "Você tem mudanças ianda não salvas em {{name}}. Fechar essa janela fará você perder todas elas.",
          enabled: "{{name}} foi ativado.",
          disabled: "{{name}} foi desativado.",
          couldNotEnable: "{{name}} não pôde ser ativado.",
          couldNotDisable: "{{name}} não pôde ser desativado.",
          couldNotStart: "{{name}} não pôde ser iniciado.",
          couldNotStop: "{{name}} não pôde ser parado.",
          settingsError: "Não foi possível abrir as configurações do(a) {{name}}",
          methodError: "{{method}} não pode ser disparado.",
          unknownAuthor: "Autor Desconhecido",
          noDescription: "Descrição não fornecida.",
          metaError: "META não pode ser reconhecida.",
          missingNameData: "META sem dados de nome.",
          metaNotFound: "META não encontrado.",
          compileError: "Não foi possível compilar.",
          wasUnloaded: "{{name}} foi descarregado."
        },
        CustomCSS: {
          confirmationText: "Você tem mudanças ianda não salvas no seu CSS Personalizado. Fechar essa janela fará você perder todas elas.",
          update: "Atualizar",
          save: "Salvar",
          openNative: "Abrir no Editor do Sistema",
          openDetached: "Abrir numa Janela Separada",
          settings: "Configurações do Editor",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Você Tem Certeza?",
          okay: "Okay",
          done: "Feito",
          cancel: "Cancelar",
          nevermind: "Deixe pra lá",
          close: "Fechar",
          name: "Nome",
          message: "Mensagem",
          error: "Erro",
          addonErrors: "Erros dos Addons",
          restartRequired: "Reinicialização Necessária",
          restartNow: "Reiniciar Agora",
          restartLater: "Reiniciar Mias Tarde",
          additionalInfo: "Informações Adicionias",
          restartPrompt: "Para as mudanças terem efeito o discord precisa ser reinciado. Você deseja reiniciar agora?"
        },
        Sorting: {
          sortBy: "Ordenar Por",
          order: "Ordem",
          ascending: "Ascendente",
          descending: "Descendente"
        },
        WindowPrefs: {
          enabledInfo: "Essa opção necessita de um tema transparente para funcionar corretamente. No Windows isso pode quebrar o aero snapping e a maximização.\n\nO discord precisa ser reiniciado para as mudanças terem efeito. Você deseja reiniciar agora?",
          disabledInfo: "O discord precisa ser reiniciado para as mudanças terem efeito. Você deseja reiniciar agora?"
        }
      };
    }
  });

  // assets/locales/ru.json
  var require_ru = __commonJS({
    "assets/locales/ru.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Плагины",
          themes: "Темы",
          customcss: "Быстрый CSS",
          updates: "Обновления"
        },
        Collections: {
          settings: {
            name: "Настройки",
            general: {
              name: "Основные",
              voiceDisconnect: {
                name: "Отключение голосового чата",
                note: "Отключает от голосового чата, когда Discord закрыт"
              },
              showToasts: {
                name: "Показывать уведомления",
                note: "Показывает небольшие уведомления с важной информацией"
              },
              mediaKeys: {
                name: "Отключить медиа-кнопки",
                note: "Предотвращает перехват мультимедийных кнопок Discord'ом после воспроизведения видео."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Удалить минимальные размеры",
                note: "Удаляет минимальный размер окна Discord'а 940x500"
              },
              name: "Настройки окна",
              transparency: {
                name: "Включить прозрачность",
                note: "Делает основное окно прозрачным (нужна перезагрузка)"
              },
              frame: {
                name: "Шапка окна",
                note: "Добавляет встроенную шапку окна системы к основному окну"
              }
            },
            addons: {
              name: "Менеджер дополнений",
              addonErrors: {
                name: "Показывать ошибки расширений",
                note: "Скрывает модальные окна с ошибками плагина/темы"
              },
              editAction: {
                name: "Действие редактирования",
                note: "Появляются при редактировании, где плагины или темы",
                options: {
                  detached: "Отдельное окно",
                  system: "Системный редактор"
                }
              }
            },
            customcss: {
              name: "Быстрый CSS",
              customcss: {
                name: "Быстрый CSS",
                note: "Включить кнопку быстрого CSS"
              },
              liveUpdate: {
                name: "Обновление в прямом эфире",
                note: "Обновляет ваш CSS, когда вы пишите"
              },
              startDetached: {
                name: "Открыть отдельно",
                note: "При нажатии на вкладку Быстрый CSS редактор открывается в отдельном окне"
              },
              nativeOpen: {
                name: "Открыть во встроенном редакторе",
                note: "Нажатие по вкладке Быстрый CSS открывает ваш быстрый CSS в вашем встроенном редакторе"
              },
              openAction: {
                name: "Положение редактора",
                note: "Где ваш Быстрый CSS должен быть открыт по умолчанию",
                options: {
                  settings: "Настройки меню",
                  detached: "Отдельное окно",
                  system: "Системный редактор"
                }
              }
            },
            developer: {
              name: "Настройки разработчика",
              debuggerHotkey: {
                name: "Горячая кнопка отладчика",
                note: "Активирует отладку при нажатии F8 при открытым меню разработчика"
              },
              reactDevTools: {
                name: "Инструменты React разработчика",
                note: "Внедряет вашу локальную установку React Developer Tools в Discord"
              },
              inspectElement: {
                name: "Горячая кнопка инспектора элементов",
                note: "Включает поведение комбинации клавиш инспектора элементов (ctrl + shift + c), как в большинстве браузеров"
              },
              devToolsWarning: {
                name: "Убрать предупреждение в консоли",
                note: 'Останавливает отправку сообщения "Hold Up!"'
              },
              debugLogs: {
                name: "Логировать ошибки",
                note: "Записывает весь вывод из консоли в файл debug.log в папке InAccord"
              },
              devTools: {
                name: "Меню разработчика",
                note: "Включает возможность открыть меню разработчика при нажатии ctrl+shift+i"
              }
            },
            editor: {
              name: "Редактор настроек",
              lineNumbers: {
                name: "Номер строк",
                note: "Включить показатель строк в стороне редактора"
              },
              fontSize: {
                name: "Размер шрифта",
                note: "Размер шрифта (в пикселях) в редакторе"
              },
              minimap: {
                name: "Мини карта",
                note: "Включает показ кода в мини карте в стороне редактора"
              },
              hover: {
                name: "Справочные подсказки",
                note: "Включает показ справочных подсказок при наведении мыши над правилами и переключателями"
              },
              quickSuggestions: {
                name: "Предложения",
                note: "Включает видимость авто-заполнения предложений при печати"
              },
              renderWhitespace: {
                name: "Показывать пробелы",
                note: "Когда пробел должен быть показан редактором",
                options: {
                  all: "Всегда",
                  none: "Никогда",
                  selection: "Выбор"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} от {{author}}",
          byline: "от {{author}}",
          reload: "Перезагрузить",
          addonSettings: "Настройки",
          website: "Сайт",
          source: "Источник",
          invite: "Сервер поддержки",
          donate: "Донат",
          patreon: "Patreon",
          name: "Имя",
          author: "Автор",
          version: "Версия",
          added: "Дата добавления",
          modified: "Дата изменения",
          editAddon: "Редактировать",
          deleteAddon: "Удалить",
          confirmDelete: "Вы уверены, что хотите удалить {{name}}?",
          confirmationText: "У вас есть несохраненные изменения в {{name}}. При закрытии этого окна все изменения будут потеряны.",
          enabled: "{{name}} включён.",
          disabled: "{{name}} выключен.",
          couldNotEnable: "{{name}} не может быть включён.",
          couldNotDisable: "{{name}} не может быть отключён.",
          couldNotStart: "{{name}} не может быть запущен.",
          couldNotStop: "{{name}} не может быть остановлен.",
          settingsError: "Не удалось открыть настройки для {{name}}",
          methodError: "{{method}} не может быть уволен.",
          unknownAuthor: "Неизвестный автор",
          noDescription: "Нету описания.",
          metaError: "Не удалось проанализировать метаданные.",
          missingNameData: "В метаданных отсутствуют данные об имени.",
          metaNotFound: "Метаданные не найдены.",
          compileError: "Не удалось скомпилировать. Посмотрите в консоле для дополнительной информации.",
          wasUnloaded: "{{name}} был выгружен.",
          isEnabled: "Включено",
          wasLoaded: "{{name}} v{{version}} был загружен."
        },
        CustomCSS: {
          confirmationText: "У вас есть не сохраненные изменения в быстром CSS.  При закрытии этого окна все изменения будут потеряны.",
          update: "Обновить",
          save: "Сохранить",
          openNative: "Открыть в системном редакторе",
          openDetached: "Отдельное окно",
          settings: "Настройки редактора",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Ты уверен?",
          okay: "Ок",
          done: "Готово",
          cancel: "Отмена",
          nevermind: "Забей",
          close: "Закрыть",
          name: "Имя",
          message: "Сообщение",
          error: "Ошибка",
          addonErrors: "Ошибки расширений",
          restartRequired: "Требуется перезагрузка",
          restartNow: "Перезагрузить сейчас",
          restartLater: "Перезагрузить позже",
          additionalInfo: "Дополнительная информация",
          restartPrompt: "Чтобы изменения вступили в силу, необходимо перезапустить Discord. Перезапустить сейчас?"
        },
        Sorting: {
          sortBy: "Сортировать по",
          order: "Порядок",
          ascending: "По возрастанию",
          descending: "По убыванию"
        },
        WindowPrefs: {
          enabledInfo: "Для правильной работы этой опции требуется прозрачная тема.  В Windows это может нарушить вашу aero snapping и максимизацию.\n\n\n\nЧтобы изменения вступили в силу, необходимо перезапустить Discord.  Вы хотите перезапустить сейчас?",
          disabledInfo: "Чтобы изменения вступили в силу, необходимо перезапустить Discord.  Вы хотите перезапустить сейчас?"
        },
        Updater: {
          updateFialed: "Ошибка обновления",
          updateFialedMessage: "Не удалось обновить InAccord. Пожалуйста скачайте последнюю версию загрузчика с нашего сайта (https://InAccord.app/) и переустановите программу.",
          updateSuccessful: "Обновление успешно!",
          updateAvialable: "InAccord имеет обновление (v{{version}})",
          addonUpdated: "{{name}} был обновлен до версии {{version}}",
          checking: "Проверить обновления!",
          finishedChecking: "Проверка обновлений завершена!",
          checkForUpdates: "Проверить обновления!",
          updateAll: "Обновить всё!",
          noUpdatesAvialable: "Нету доступных обновлений.",
          versionAvialable: "Версия {{version}} доступна!",
          updateButton: "Обновить!"
        }
      };
    }
  });

  // assets/locales/sk.json
  var require_sk = __commonJS({
    "assets/locales/sk.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Pluginy",
          themes: "Témy",
          customcss: "Custom CSS"
        },
        Collections: {
          settings: {
            name: "Nastavenia",
            general: {
              name: "Základné",
              voiceDisconnect: {
                name: "Odpojenie hovoru",
                note: "Odpojiť z hovoru pri vypnutí Discordu"
              },
              showToasts: {
                name: "Zobraziť toasty",
                note: "Zobraziť notifikáciu pre dôležité info"
              },
              mediaKeys: {
                name: "Zakázať Média tlačidlá",
                note: "Zabráni Discordu prevziať kontrolu nad média tlačidlami po prehratí videa."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Zrušiť minimálnu velkosť",
                note: "Zruší minimálnu veľkosť Discord okna 940x500"
              },
              name: "Vlastnosti Okna",
              transparency: {
                name: "Povoliť priehľadnosť",
                note: "Povolí, aby bolo hlavné okno priehľadné (potrebný reštart)"
              },
              frame: {
                name: "Rám okna",
                note: "Pridá klasický OS rám pre hlavné okno"
              }
            },
            addons: {
              name: "Manažér doplnkov",
              addonErrors: {
                name: "Zobraziť chyby doplnkov",
                note: "Zobrazí panel s chybou pluginu alebo témy"
              },
              editAction: {
                name: "Úprava akcie",
                note: "Kde sa pluginy a témy zobrazia pri editovaní",
                options: {
                  detached: "Oddelené okno",
                  system: "Systémový editor"
                }
              }
            },
            customcss: {
              name: "Custom CSS",
              customcss: {
                name: "Custom CSS",
                note: 'Aktivuje kartu "Custom CSS"'
              },
              liveUpdate: {
                name: "Živý update",
                note: "Aktualizuje css pri zmene"
              },
              startDetached: {
                name: "Spustiť oddelené",
                note: "Kliknutie na kartu Custom CSS ju otvori v oddelenom okne"
              },
              nativeOpen: {
                name: "Otvoriť v natívnom editore",
                note: "Kliknutie na kartu Custom CSS ju otvori v natívnom editore"
              },
              openAction: {
                name: "Cesta editora",
                note: "Predvolený spôsob otvorenia Custom CSS",
                options: {
                  settings: "Menu nastavení",
                  detached: "Oddelené okno",
                  system: "Systémový editor"
                }
              }
            },
            developer: {
              name: "Vývojarske nastavenia",
              debuggerHotkey: {
                name: "Klávesová skratka Debuggera",
                note: "Povolí spustenie debuggera pri stlačení F8"
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Pridá inštaláciu React Developer Tools do Discordu"
              },
              inspectElement: {
                name: "Klávesová skratka Inspect Element",
                note: "Povolí klávesovú skratku Inspect Element (ctrl + shift + c) ako pre bežné prehliadače"
              },
              devToolsWarning: {
                name: "Vypnúť DevTools Upozornenia",
                note: 'Vypne "Hold Up!" upozornenie od Discordu v konzole'
              },
              debugLogs: {
                name: "Výpis ladenia",
                note: "Zapíše všetko z konzole do súboru debug.log v InAccord zložke"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} od {{author}}",
          byline: "od {{author}}",
          reload: "Znova načítať",
          addonSettings: "Nastavenia",
          website: "Stránka",
          source: "Zdroj",
          invite: "Server podpory",
          donate: "Prispieť",
          patreon: "Patreon",
          name: "Meno",
          author: "Autor",
          version: "Verzia",
          added: "Dátum pridania",
          modified: "Dátum zmeny",
          editAddon: "Upraviť",
          deleteAddon: "Zmazať",
          confirmDelete: "Ste si istý, že chcete zmazať {{name}}?",
          confirmationText: "Máte neuložené zmeny pre {{name}}. Zatvorením tohto okna stratíte všetky zmeny.",
          enabled: "{{name}} bol aktivovaný.",
          disabled: "{{name}} bol deaktivovaný.",
          couldNotEnable: "{{name}} nebolo možné aktivovať.",
          couldNotDisable: "{{name}} nebolo možné deaktivovať.",
          couldNotStart: "{{name}} nebolo možné spustiť.",
          couldNotStop: "{{name}} nebolo možné zastaviť.",
          settingsError: "Nie je možné otvoriť nastavenia pre {{name}}",
          methodError: "{{method}} nemohla byť vykonaná.",
          unknownAuthor: "Neznámy Autor",
          noDescription: "Bez popisu.",
          metaError: "META sa nepodarila načítať.",
          missingNameData: "META chýbajúce dáta názvu.",
          metaNotFound: "META nebola nájedná.",
          compileError: "Nepodarilo sa zostaviť.",
          wasUnloaded: "{{name}} bol odpojený."
        },
        CustomCSS: {
          confirmationText: "Máte neuložené zmeny pre Custom CSS. Zatvorením tohto okna stratíte všetky zmeny.",
          update: "Aktualizovať",
          save: "Uložiť",
          openNative: "Otvoriť v Systémovom Editore",
          openDetached: "Oddeliť Okno",
          settings: "Nastavenia Editora",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Ste si istý?",
          okay: "Ok",
          done: "Dokončiť",
          cancel: "Zrušiť",
          nevermind: "Radšej nie",
          close: "Zavrieť",
          name: "Meno",
          message: "Správa",
          error: "Chyba",
          addonErrors: "Chyby Doplnkov",
          restartRequired: "Potrebný Reštart",
          restartNow: "Reštartovať Teraz",
          restartLater: "Reštartovať Neskôr",
          additionalInfo: "Ďalšie Info",
          restartPrompt: "Pre aplikáciu zmien je potrebné reštartovať Discord. Chcete reštartovať teraz?"
        },
        Sorting: {
          sortBy: "Triediť podľa",
          order: "Poradie",
          ascending: "Vzostupne",
          descending: "Zostupne"
        },
        WindowPrefs: {
          enabledInfo: "Táto možnosť si vyžaduje priehľadnú tému, aby správne fungovala. V systéme Windows to môže spôsobiť prerušenie a maximalizáciu vášho aero.\n\nNa to, aby sa prejavila účinnosť, je potrebné reštartovať Discord. Chcete reštartovať teraz?",
          disabledInfo: "Pre aplikáciu zmien je potrebné reštartovať Discord. Chcete reštartovať teraz?"
        }
      };
    }
  });

  // assets/locales/es-es.json
  var require_es_es = __commonJS({
    "assets/locales/es-es.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plugins",
          themes: "Temas",
          customcss: "CSS Personalizado"
        },
        Collections: {
          settings: {
            name: "Ajustes",
            general: {
              name: "General",
              voiceDisconnect: {
                name: "Desconexión de Voz",
                note: "Desconectarse del servidor de voz al cerrar Discord"
              },
              showToasts: {
                name: "Mostrar Notificaciones",
                note: "Muestra una pequeña notificación de información importante"
              },
              mediaKeys: {
                name: "Desactivar las Teclas Multimedia",
                note: "Evita que Discord se apropie de tus teclas multimedia después de reproducir un vídeo"
              }
            },
            window: {
              removeMinimumSize: {
                name: "Eliminar Tamaño Mínimo",
                note: "Elimina el tamaño mínimo de Discord de 940x500"
              },
              name: "Preferencias de la Ventana",
              transparency: {
                name: "Activar Transparencia",
                note: "Hace que la ventana principal pueda ser transparente (requiere reinicio)"
              },
              frame: {
                name: "Marco de la Ventana",
                note: "Añade el marco de ventana nativo de tu sistema operativo a la ventana principal"
              }
            },
            addons: {
              name: "Gestor de Complementos",
              addonErrors: {
                name: "Mostrar Errores de Complementos",
                note: "Muestra una ventana con los errores de plugin/temas"
              },
              editAction: {
                name: "Acción al Editar",
                note: "Donde aparecerán los plugins y temas al editarlos",
                options: {
                  detached: "Ventana Independiente",
                  system: "Editor del Sistema"
                }
              }
            },
            customcss: {
              name: "CSS Personalizado",
              customcss: {
                name: "CSS Personalizado",
                note: "Activa la pestaña de CSS Personalizado"
              },
              liveUpdate: {
                name: "Actualización en Vivo",
                note: "Actualiza el CSS a medida que se escribe"
              },
              startDetached: {
                name: "Comenzar en Ventana Independiente",
                note: "Al hacer clic en la pestaña de CSS Personalizado se abre el editor en una ventana independiente"
              },
              nativeOpen: {
                name: "Abrir en Editor Nativo",
                note: "Al hacer clic en la pestaña de CSS Personalizado se abre el editor en tu editor nativo"
              },
              openAction: {
                name: "Ubicación del Editor",
                note: "Donde deberá el CSS Personalizado abrirse por defecto",
                options: {
                  settings: "Menú de Ajustes",
                  detached: "Ventana Independiente",
                  system: "Editor del Sistema"
                }
              }
            },
            developer: {
              name: "Ajustes de Desarrollador",
              debuggerHotkey: {
                name: "Tecla de Acceso Rápido al Depurador",
                note: "Permite activar el depurador al presionar la tecla F8"
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Inyecta tu instalación local de React Developer Tools en Discord"
              },
              inspectElement: {
                name: "Tecla de Acceso Rápido al Inspector de Elementos",
                note: "Activa la tecla de acceso rápido al inspector de elementos (ctrl + shift + c) que es común en la mayoria de navegadores"
              },
              devToolsWarning: {
                name: "Quitar el Aviso del Inspector de Elementos",
                note: 'Previene que Discord muestre su mensaje "¡Espera!"'
              },
              debugLogs: {
                name: "Registros de Depuración",
                note: "Envía todo lo que aparece en la consola a un archivo llamado debug.log en la carpeta de InAccord"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} por {{author}}",
          byline: "por {{author}}",
          reload: "Recargar",
          addonSettings: "Ajustes",
          website: "Sitio web",
          source: "Fuente",
          invite: "Servidor de Soporte",
          donate: "Donar",
          patreon: "Patreon",
          name: "Nombre",
          author: "Autor",
          version: "Versión",
          added: "Fecha de Adición",
          modified: "Fecha de Modificación",
          editAddon: "Editar",
          deleteAddon: "Eliminar",
          confirmDelete: "¿Estás seguro de que quieres borrar {{name}}?",
          confirmationText: "Tiene cambios no guardados en {{name}}. Al cerrar esta ventana se perderán todos los cambios.",
          enabled: "{{name}} ha sido activado.",
          disabled: "{{name}} ha sido desactivado.",
          couldNotEnable: "{{name}} no pudo ser activado.",
          couldNotDisable: "{{name}} no pudo ser desactivado.",
          couldNotStart: "{{name}} no se pudo iniciar.",
          couldNotStop: "{{name}} no se pudo detener.",
          settingsError: "No se pudieron abrir los ajustes de {{name}}",
          methodError: "{{method}} no pudo ser lanzado.",
          unknownAuthor: "Autor Desconocido",
          noDescription: "Descripción no proporcionada.",
          metaError: "El META no pudo ser analizado.",
          missingNameData: "El META no contiene datos del nombre.",
          metaNotFound: "El META no ha sido encontrado.",
          compileError: "No se ha podido compilar.",
          wasUnloaded: "{{name}} ha sido descargado."
        },
        CustomCSS: {
          confirmationText: "Tienes cambios sin guardar en tu CSS Personalizado. Al cerrar esta ventana se perderán todos los cambios.",
          update: "Actualizar",
          save: "Guardar",
          openNative: "Abrir en el Editor del Sistema",
          openDetached: "Mostrar en Ventana Independiente",
          settings: "Ajustes del Editor",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "¿Estás seguro?",
          okay: "Vale",
          done: "Hecho",
          cancel: "Cancelar",
          nevermind: "No importa",
          close: "Cerrar",
          name: "Nombre",
          message: "Mensaje",
          error: "Error",
          addonErrors: "Errores de Complementos",
          restartRequired: "Reinicio Requerido",
          restartNow: "Reiniciar Ahora",
          restartLater: "Reiniciar más Tarde",
          additionalInfo: "Información Adicional",
          restartPrompt: "Para que surta efecto, es necesario reiniciar Discord. ¿Quieres reiniciar ahora?"
        },
        Sorting: {
          sortBy: "Ordenar por",
          order: "Orden",
          ascending: "Ascendente",
          descending: "Descendente"
        },
        WindowPrefs: {
          enabledInfo: "Esta opción requiere un tema transparente para que funcione correctamente. En Windows esto podría hacer que el ajuste automático de la ventana (Aero Snap) y la maximización dejen de funcionar.\n\nPara que surta efecto, es necesario reiniciar Discord. ¿Quieres reiniciar ahora?",
          disabledInfo: "Para que surta efecto, es necesario reiniciar Discord. ¿Quieres reiniciar ahora?"
        }
      };
    }
  });

  // assets/locales/es-419.json
  var require_es_419 = __commonJS({
    "assets/locales/es-419.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Plugins",
          themes: "Temas",
          customcss: "CSS Personalizado"
        },
        Collections: {
          settings: {
            name: "Ajustes",
            general: {
              name: "General",
              voiceDisconnect: {
                name: "Desconexión de Voz",
                note: "Desconectarse del servidor de voz al cerrar Discord"
              },
              showToasts: {
                name: "Mostrar Notificaciones",
                note: "Muestra una pequeña notificación de información importante"
              },
              mediaKeys: {
                name: "Desactivar las Teclas Multimedia",
                note: "Evita que Discord se apropie de tus teclas multimedia después de reproducir un vídeo"
              }
            },
            window: {
              removeMinimumSize: {
                name: "Eliminar Tamaño Mínimo",
                note: "Elimina el tamaño mínimo de Discord de 940x500"
              },
              name: "Preferencias de la Ventana",
              transparency: {
                name: "Activar Transparencia",
                note: "Hace que la ventana principal pueda ser transparente (requiere reinicio)"
              },
              frame: {
                name: "Marco de la Ventana",
                note: "Añade el marco de ventana nativo de tu sistema operativo a la ventana principal"
              }
            },
            addons: {
              name: "Gestor de Complementos",
              addonErrors: {
                name: "Mostrar Errores de Complementos",
                note: "Muestra una ventana con los errores de plugin/temas"
              },
              editAction: {
                name: "Acción al Editar",
                note: "Donde aparecerán los plugins y temas al editarlos",
                options: {
                  detached: "Ventana Independiente",
                  system: "Editor del Sistema"
                }
              }
            },
            customcss: {
              name: "CSS Personalizado",
              customcss: {
                name: "CSS Personalizado",
                note: "Activa la pestaña de CSS Personalizado"
              },
              liveUpdate: {
                name: "Actualización en Vivo",
                note: "Actualiza el CSS a medida que se escribe"
              },
              startDetached: {
                name: "Comenzar en Ventana Independiente",
                note: "Al hacer clic en la pestaña de CSS Personalizado se abre el editor en una ventana independiente"
              },
              nativeOpen: {
                name: "Abrir en Editor Nativo",
                note: "Al hacer clic en la pestaña de CSS Personalizado se abre el editor en tu editor nativo"
              },
              openAction: {
                name: "Ubicación del Editor",
                note: "Donde deberá el CSS Personalizado abrirse por defecto",
                options: {
                  settings: "Menú de Ajustes",
                  detached: "Ventana Independiente",
                  system: "Editor del Sistema"
                }
              }
            },
            developer: {
              name: "Ajustes de Desarrollador",
              debuggerHotkey: {
                name: "Tecla de Acceso Rápido al Depurador",
                note: "Permite activar el depurador al presionar la tecla F8"
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Inyecta tu instalación local de React Developer Tools en Discord"
              },
              inspectElement: {
                name: "Tecla de Acceso Rápido al Inspector de Elementos",
                note: "Activa la tecla de acceso rápido al inspector de elementos (ctrl + shift + c) que es común en la mayoria de navegadores"
              },
              devToolsWarning: {
                name: "Quitar el Aviso del Inspector de Elementos",
                note: 'Previene que Discord muestre su mensaje "¡Espera!"'
              },
              debugLogs: {
                name: "Registros de Depuración",
                note: "Envía todo lo que aparece en la consola a un archivo llamado debug.log en la carpeta de InAccord"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} por {{author}}",
          byline: "por {{author}}",
          reload: "Recargar",
          addonSettings: "Ajustes",
          website: "Sitio web",
          source: "Fuente",
          invite: "Servidor de Soporte",
          donate: "Donar",
          patreon: "Patreon",
          name: "Nombre",
          author: "Autor",
          version: "Versión",
          added: "Fecha de Adición",
          modified: "Fecha de Modificación",
          editAddon: "Editar",
          deleteAddon: "Eliminar",
          confirmDelete: "¿Estás seguro de que quieres borrar {{name}}?",
          confirmationText: "Tiene cambios no guardados en {{name}}. Al cerrar esta ventana se perderán todos los cambios.",
          enabled: "{{name}} ha sido activado.",
          disabled: "{{name}} ha sido desactivado.",
          couldNotEnable: "{{name}} no pudo ser activado.",
          couldNotDisable: "{{name}} no pudo ser desactivado.",
          couldNotStart: "{{name}} no se pudo iniciar.",
          couldNotStop: "{{name}} no se pudo detener.",
          settingsError: "No se pudieron abrir los ajustes de {{name}}",
          methodError: "{{method}} no pudo ser lanzado.",
          unknownAuthor: "Autor Desconocido",
          noDescription: "Descripción no proporcionada.",
          metaError: "El META no pudo ser analizado.",
          missingNameData: "El META no contiene datos del nombre.",
          metaNotFound: "El META no ha sido encontrado.",
          compileError: "No se ha podido compilar.",
          wasUnloaded: "{{name}} ha sido descargado."
        },
        CustomCSS: {
          confirmationText: "Tienes cambios sin guardar en tu CSS Personalizado. Al cerrar esta ventana se perderán todos los cambios.",
          update: "Actualizar",
          save: "Guardar",
          openNative: "Abrir en el Editor del Sistema",
          openDetached: "Mostrar en Ventana Independiente",
          settings: "Ajustes del Editor",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "¿Estás seguro?",
          okay: "Vale",
          done: "Hecho",
          cancel: "Cancelar",
          nevermind: "No importa",
          close: "Cerrar",
          name: "Nombre",
          message: "Mensaje",
          error: "Error",
          addonErrors: "Errores de Complementos",
          restartRequired: "Reinicio Requerido",
          restartNow: "Reiniciar Ahora",
          restartLater: "Reiniciar más Tarde",
          additionalInfo: "Información Adicional",
          restartPrompt: "Para que surta efecto, es necesario reiniciar Discord. ¿Quieres reiniciar ahora?"
        },
        Sorting: {
          sortBy: "Ordenar por",
          order: "Orden",
          ascending: "Ascendente",
          descending: "Descendente"
        },
        WindowPrefs: {
          enabledInfo: "Esta opción requiere un tema transparente para que funcione correctamente. En Windows esto podría hacer que el ajuste automático de la ventana (Aero Snap) y la maximización dejen de funcionar.\n\nPara que surta efecto, es necesario reiniciar Discord. ¿Quieres reiniciar ahora?",
          disabledInfo: "Para que surta efecto, es necesario reiniciar Discord. ¿Quieres reiniciar ahora?"
        }
      };
    }
  });

  // assets/locales/sv-se.json
  var require_sv_se = __commonJS({
    "assets/locales/sv-se.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Tillägg",
          themes: "Teman",
          customcss: "Egen CSS",
          updates: "Uppdateringar"
        },
        Collections: {
          settings: {
            name: "Inställningar",
            general: {
              name: "Allmänt",
              voiceDisconnect: {
                name: "Lämna röstsamtal",
                note: "Lämna röstsamtal när Discord stängs ner"
              },
              showToasts: {
                name: "Visa Toasts",
                note: "Visar en liten notifikation för viktig information"
              },
              mediaKeys: {
                name: "Inaktivera mediaknapparna",
                note: "Hindrar Discord från att ta över mediaknapparna efter att du har spelat upp en video."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Ta bort minimistorleken",
                note: "Tar bort Discords minimifönsterstorlek på 940x500"
              },
              name: "Fönsterinställningar",
              transparency: {
                name: "Aktivera Genomskinlighet",
                note: "Gör att huvudfönstret blir genomskinligt (kräver omstart)"
              },
              frame: {
                name: "Fönsterram",
                note: "Lägger till operativsystemets standard fönsterram runt huvudfönstret"
              }
            },
            addons: {
              name: "Tilläggshanterare",
              addonErrors: {
                name: "Visa tilläggsfel",
                note: "Visar en modalruta med tillägg- och temafel"
              }
            },
            customcss: {
              name: "Egen CSS",
              customcss: {
                name: "Egen CSS",
                note: 'Aktiverar "Egen CSS"-fliken'
              },
              liveUpdate: {
                name: "Uppdatera Direkt",
                note: "Uppdaterar css allteftersom du skriver"
              },
              startDetached: {
                name: "Starta Frånkopplad",
                note: 'Öppnar CSS-redigeraren i ett separat fönster när "Egen CSS"-fliken klickas'
              },
              nativeOpen: {
                name: "Öppna I Standardredigerare",
                note: 'Öppnar CSS filen i operativsystemets standardredigerare när "Egen CSS"-fliken klickas'
              }
            },
            developer: {
              name: "Utvecklarinställningar",
              devToolsWarning: {
                name: "Stäng av utvecklarverktygsvarningar",
                note: 'Hindrar Discord från att skriva ut sitt "Vänta!"-meddelande'
              },
              debugLogs: {
                name: "Felsökningsloggar",
                note: "Allt från konsolen hamnar i filen debug.log i InAccord mappen"
              },
              devTools: {
                name: "Utvecklarverktyg"
              }
            },
            editor: {
              lineNumbers: {
                name: "Radnummer",
                note: "Visar radnummer i marginalen i redigeringsfönstret"
              },
              fontSize: {
                name: "Teckenstorlek",
                note: "Storlek på typsnittet som används i redigeringsfönstret"
              },
              quickSuggestions: {
                name: "Snabbförslag",
                note: "Aktiverar autokompletteringsförslag när du skriver"
              },
              renderWhitespace: {
                name: "Visa blanksteg",
                note: "När blanksteg ska markeras i redigeringsfönstret",
                options: {
                  all: "Alltid",
                  none: "Aldrig"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} av {{author}}",
          byline: "av {{author}}",
          reload: "Ladda om",
          addonSettings: "Inställningar",
          website: "Webbplats",
          source: "Källa",
          invite: "Supportserver",
          donate: "Donera",
          patreon: "Patreon",
          name: "Namn",
          author: "Upphovsman",
          version: "Version",
          added: "Tilläggsdatum",
          modified: "Ändringsdatum",
          editAddon: "Redigera",
          deleteAddon: "Ta bort",
          confirmDelete: "Är du säker på att du vill ta bort {{name}}?",
          enabled: "{{name}} har aktiverats.",
          disabled: "{{name}} har inaktiverats.",
          couldNotEnable: "{{name}} kunde inte aktiverats.",
          couldNotDisable: "{{name}} kunde inte inaktiverats.",
          couldNotStart: "{{name}} kunde inte startas.",
          couldNotStop: "{{name}} kunde inte stoppas.",
          settingsError: "Kunde inte öppna inställningarna för {{namn}}",
          unknownAuthor: "Okänd upphovsman",
          noDescription: "Beskrivning saknas.",
          compileError: "Kunde inte kompileras. Se konsolen för mer information.",
          isEnabled: "Aktiverad",
          listView: "Listvy",
          gridView: "Rutnätsvy"
        },
        CustomCSS: {
          confirmationText: "Du har osparade ändringar av egna CSS:en. Alla ändringar kommer försvinna om du stänger det här fönstret",
          update: "Uppdatera",
          save: "Spara",
          openNative: "Öppna I Standardredigerare",
          openDetached: "Koppla Loss Fönster",
          settings: "Inställningar för redigerare",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Är du säker?",
          okay: "Okej",
          cancel: "Avbryt",
          nevermind: "Avbryt",
          close: "Stäng",
          name: "Namn",
          message: "Meddelande",
          error: "Fel",
          addonErrors: "Tilläggsfel",
          restartRequired: "Omstart krävs",
          restartNow: "Starta om nu",
          restartLater: "Starta om senare",
          restartPrompt: "För att ändringarna ska träda i kraft måste Discord startas om. Vill du starta om nu?"
        },
        Sorting: {
          sortBy: "Sortera efter",
          order: "Ordning",
          ascending: "Stigande",
          descending: "Fallande"
        },
        Updater: {
          updateFialed: "Uppdateringen misslyckades!",
          updateFialedMessage: "InAccord kunde inte uppdateras. Ladda ned den senaste versionen av installationsprogrammet från vår webbplats (https://InAccord.app/) och installera om InAccord.",
          updateSuccessful: "Uppdateringen lyckades!",
          checking: "Söker efter uppdateringar!",
          finishedChecking: "Sökningen efter uppdateringar är slutförd!",
          checkForUpdates: "Sök efter uppdateringar!",
          updateAll: "Uppdatera alla!",
          noUpdatesAvialable: "Det finns inga uppdateringar tillgängliga.",
          updateButton: "Uppdatera!"
        }
      };
    }
  });

  // assets/locales/tr.json
  var require_tr = __commonJS({
    "assets/locales/tr.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Eklentiler",
          themes: "Temalar",
          customcss: "Özel CSS"
        },
        Collections: {
          settings: {
            name: "Ayarlar",
            general: {
              name: "Genel",
              voiceDisconnect: {
                name: "Ses Kanalından Ayrılma",
                note: "Discord'u kapatırken aktif ses kanalından ayrılır"
              },
              showToasts: {
                name: "Bildirimleri Göster",
                note: "Önemli bilgilendirmeler için küçük bir bidirim gösterir"
              },
              mediaKeys: {
                name: "Medya Tuşlarını Deaktive Et",
                note: "Bir video oynattıktan sonra Discord'un Multimedya tuşlarını ele geçirmesini engeller."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Minimum boyutu kaldır",
                note: "Discord'un 940x500 boyut limitini kaldırır"
              },
              name: "Pencere Tercihleri",
              transparency: {
                name: "Şeffaflığı Etkinleştir",
                note: "Ana pencerenin şeffaf olmasına el verir (yeniden başlatma gerekli)"
              },
              frame: {
                name: "Pencere Çerçevesi",
                note: "Yerel işletim sistemi penceresini ana pencereye ekler"
              }
            },
            addons: {
              name: "Eklenti Yöneticisi",
              addonErrors: {
                name: "Eklenti Hatalarını Göster",
                note: "Eklenti/tema hatalarında yazı baloncuğu gösterir"
              },
              editAction: {
                name: "Eylemi düzenle",
                note: "Eklenti ve temaları düzenlerken çıkacakları yer",
                options: {
                  detached: "Bağımsız Pencere",
                  system: "Sistem Düzenleyici"
                }
              }
            },
            customcss: {
              name: "Özel CSS",
              customcss: {
                name: "Özel CSS",
                note: "Özel CSS sekmesini aktifleştirir"
              },
              liveUpdate: {
                name: "Canlı Güncelleme",
                note: "Yazdıkca CSS'i günceller"
              },
              startDetached: {
                name: "Bağımsız Başlat",
                note: "Özel CSS sekme tuşuna basınca bağımsız sekmede açar"
              },
              nativeOpen: {
                name: "Seçili editör'de aç",
                note: "Özel CSS sekme tuşuna basınca seçili yazı editörünüzde açar"
              },
              openAction: {
                name: "Editör Yeri",
                note: "Özel CSS'in varsayılan açılma yeri",
                options: {
                  settings: "Ayarlar Menüsü",
                  detached: "Bağımsız Pencere",
                  system: "Sistem Editörü"
                }
              }
            },
            developer: {
              name: "Geliştirici (Developer) Ayarları",
              debuggerHotkey: {
                name: "Hata ayıklayıcı kısayolu",
                note: "F8'e basınca hata ayıklayıcının açılmasını sağlar"
              },
              reactDevTools: {
                name: "React Geliştirici Araçları",
                note: "Yerli React Geliştirici araçlarınızı Discord'a ekler"
              },
              inspectElement: {
                name: "Öğeyi Denetle Kısayolu",
                note: "Çoğu tarayıca yaygın olan (ctrl + shift + c) Öğeyi Denetle kısayolunu etkinleştirir"
              },
              devToolsWarning: {
                name: "DevTools uyarısını kapat",
                note: `Discord'un "Bekle!" mesajını yazmasını engeller`
              },
              debugLogs: {
                name: "Hata Ayıklama çıktıları",
                note: "Konsoldaki herşeyi InAccord klasöründeki debug.log dosyasına kaydeder"
              }
            }
          }
        },
        Addons: {
          title: "{{author}} tarafından {{name}} v{{version}}",
          byline: "{{author}} tarafından",
          reload: "Yeniden-Başlat",
          addonSettings: "Ayarlar",
          website: "Website",
          source: "Kaynak",
          invite: "Destek Sunucusu",
          donate: "Bağış",
          patreon: "Patreon",
          name: "İsim",
          author: "Yapımcı",
          version: "Versiyon",
          added: "Eklenim Tarihi",
          modified: "Düzenlenme Tarihi",
          editAddon: "Düzenle",
          deleteAddon: "Sil",
          confirmDelete: "{{name}} eklentisini silmek istediğinize emin misiniz?",
          confirmationText: "{{name}} eklentisinde kaydedilmemiş değişiklikler var. Bu pencereyi kapatmak değişikliklerin kaybolmasına yol açar.",
          enabled: "{{name}} başlatıldı.",
          disabled: "{{name}} kapatıldı.",
          couldNotEnable: "{{name}} başlatılamadı.",
          couldNotDisable: "{{name}} kapatılamadı.",
          couldNotStart: "{{name}} başlatılamadı.",
          couldNotStop: "{{name}} durdurulamadı.",
          settingsError: "{{name}} için ayarlar açılamadı.",
          methodError: "{{method}} çalıştırılamadı.",
          unknownAuthor: "Bilinmeyen Yapımcı",
          noDescription: "Açıklama Yok.",
          metaError: "META ayrıştırılamadı.",
          missingNameData: "META isim verisi yok.",
          metaNotFound: "META bulunamadı.",
          compileError: "Derlenemedi.",
          wasUnloaded: "{{name}} çıkarıldı."
        },
        CustomCSS: {
          confirmationText: "Özel CSS'inde kaydedilmemiş değişimler var. Bu pencereyi kapatmak değişikliklerin kaybolmasına yol açar.",
          update: "Güncelle",
          save: "Kaydet",
          openNative: "Sistem Editöründe aç",
          openDetached: "Pencereyi Ayır",
          settings: "Editör Ayarları",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Emin Misin?",
          okay: "Tamam",
          done: "Bitti",
          cancel: "İptal",
          nevermind: "Boşver",
          close: "Kapat",
          name: "İsim",
          message: "Mesaj",
          error: "Hata",
          addonErrors: "Eklenti Hataları",
          restartRequired: "Yeniden Başlatma Lazım",
          restartNow: "Şimdi Yeniden Başlat",
          restartLater: "Sonra Yeniden Başlat",
          additionalInfo: "Ek Bilgi",
          restartPrompt: "Etki görmek için Discord'un yeniden başlatılması gerek. Şimdi yeniden başlatmak ister misiniz?"
        },
        Sorting: {
          sortBy: "Göre Sırala",
          order: "Sıra",
          ascending: "Yükselen",
          descending: "Azalan"
        },
        WindowPrefs: {
          enabledInfo: "Bu seçeneğin düzgün çalışması için şeffaf bir tema gerekiyor.\nWindows'da bu aero snapping ve maksimize etmeyi bozabilir.\n\nEtki etmesi için Discord'un yeniden başlatılması gerek. Şimdi yeniden başlatmak ister misiniz?",
          disabledInfo: "Etki etmesi için Discord'un yeniden başlatılması gerek. Şimdi yeniden başlatmak ister misiniz?"
        }
      };
    }
  });

  // assets/locales/bg.json
  var require_bg = __commonJS({
    "assets/locales/bg.json"(exports, module) {
      module.exports = {};
    }
  });

  // assets/locales/uk.json
  var require_uk = __commonJS({
    "assets/locales/uk.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Плагіни",
          themes: "Теми",
          customcss: "Самописні CSS"
        },
        Collections: {
          settings: {
            name: "Налаштування",
            general: {
              name: "Основні",
              voiceDisconnect: {
                name: "Голосовий чат під час закриття",
                note: "Відключатися від голосового сервера під час закриття Discord"
              },
              showToasts: {
                name: "Показувати сповіщення",
                note: "Показувати маленькі повідомлення з важливою інформацією"
              },
              mediaKeys: {
                name: "Вимкнути медіаключі",
                note: "Не захоплювати медіаключі Discord після відтворення відео"
              }
            },
            window: {
              removeMinimumSize: {
                name: "Прибрати мінімальний розмір",
                note: "Прибирає примусовий мінімальний розмір вікна 940x500"
              },
              name: "Налаштування вікна",
              transparency: {
                name: "Увімкнути прозорість",
                note: "Дозволяє бачити головне вікно (потрібний перезапуск)"
              },
              frame: {
                name: "Рамка",
                note: "Додає вбудовану рамку ОС для головного вікна"
              }
            },
            addons: {
              name: "Керування плагінами",
              addonErrors: {
                name: "Показувати помилки плагінів",
                note: "Показувати спливні вікна з помилками плагінів і тем."
              },
              editAction: {
                name: "Відображення редактора",
                note: "Де відображати редактор плагінів і тем",
                options: {
                  detached: "В окремому вікні",
                  system: "У системному редакторі"
                }
              }
            },
            customcss: {
              name: "Самописні CSS",
              customcss: {
                name: "Редактор самописних CSS",
                note: "Увімкнути вкладку зі стилями користувача"
              },
              liveUpdate: {
                name: "Оновлення наживо",
                note: "Оновлювати стиль у режимі реального часу в міру редагування"
              },
              startDetached: {
                name: "Редактор в окремому вікні",
                note: "Тиць по вкладці 'Самописні CSS' відкриє редактор в окремому вікні"
              },
              nativeOpen: {
                name: "Відкривати вбудований редактор",
                note: "Тиць по вкладці 'Самописні CSS' відкриє самописний стиль у вбудованому редакторі"
              },
              openAction: {
                name: "Розташування редактора",
                note: "Де самописний стиль повинен відкриватися за замовчуванням?",
                options: {
                  settings: "У меню налаштувань",
                  detached: "В окремому вікні",
                  system: "У системному редакторі"
                }
              }
            },
            developer: {
              name: "Налаштування розробника",
              debuggerHotkey: {
                name: "Гаряча клавіша налагоджувача",
                note: "Дозволяє активувати налагоджувач при натисканні клавіші F8"
              },
              reactDevTools: {
                name: "Інструменти розробника React",
                note: "Вводить твою локальну установку інструментів розробника React у Discord"
              },
              inspectElement: {
                name: "Гаряча клавіша перевірки елементу",
                note: "Вмикає гарячу клавішу перевірки елементу (ctrl+shift+c), яка використовується у більшості браузерів"
              },
              devToolsWarning: {
                name: "Зупинити попередження",
                note: 'Не виводити попередження "Почекай!" ("Стій-но!")'
              },
              debugLogs: {
                name: "Налагоджувальні журнали",
                note: "Виводить все з консолі у файл debug.log в теці InAccord"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} у{{version}} від {{author}}",
          byline: "{{author}}",
          reload: "Перезавантажити",
          addonSettings: "Налаштування",
          website: "Сайт",
          source: "Вихідний код",
          invite: "Сервер підтримки",
          donate: "Пожертвувати",
          patreon: "Патреон",
          name: "Ім'ям",
          author: "Автором",
          version: "Версією",
          added: "Датою додавання",
          modified: "Датою оновлення",
          editAddon: "Редагувати",
          deleteAddon: "Вилучити",
          confirmDelete: "Ти впевнений, що хочеш вилучити {{name}}?",
          confirmationText: "У вас є незбережені зміни в {{name}}. Закриття цього вікна призведе до їх втрати.",
          enabled: "{{name}} увімкнено.",
          disabled: "{{name}} вимкнено.",
          couldNotEnable: "Не вдалося увімкнути {{name}}.",
          couldNotDisable: "Не вдалося вимкнути {{name}}.",
          couldNotStart: "Не вдалося запустити {{name}}",
          couldNotStop: "Не вдалося зупинити {{name}}",
          settingsError: "Не вдалося відкрити налаштування {{name}}",
          methodError: "Не вдалося викликати {{method}}",
          unknownAuthor: "Автор невідомий",
          noDescription: "Опис не надано.",
          metaError: "META не може бути проаналізована",
          missingNameData: "Відсутні META-дані імен",
          metaNotFound: "META не знайдена.",
          compileError: "Не вдалося скомпілювати.",
          wasUnloaded: "{{name}} вивантажено"
        },
        CustomCSS: {
          confirmationText: "У тебе є незбережені зміни у самописному CSS. Закриття цього вікна призведе до їх втрати.",
          update: "Оновити",
          save: "Зберегти",
          openNative: "Відкрити в системному редакторі",
          openDetached: "Відкрити у вбудованому вікні",
          settings: "Налаштування редактора",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Ти впевнений?",
          okay: "Так",
          done: "Готово",
          cancel: "Скасувати",
          nevermind: "Не звертати уваги",
          close: "Закрити",
          name: "Ім'я",
          message: "Повідомлення",
          error: "Помилка",
          addonErrors: "Помилка доповнення",
          restartRequired: "Потрібний перезапуск",
          restartNow: "Перезапустити зараз",
          restartLater: "Перезапустити пізніше",
          additionalInfo: "Додаткова інформація",
          restartPrompt: "Щоб зміни набрали чинності, потрібно перезапустити Discord. Бажаєш зробити це зараз?"
        },
        Sorting: {
          sortBy: "Сортувати за",
          order: "За порядком",
          ascending: "Зростання",
          descending: "Спадання"
        },
        WindowPrefs: {
          enabledInfo: "Для правильної роботи цієї опції потрібна прозора тема. У Windows це може призвести до порушення прив'язки та максимізації Aero.\n\nЩоб зміни набули чинності, необхідно перезапустити Discord. Бажаєш зробити це зараз?",
          disabledInfo: "Щоб зміни набули чинності, необхідно перезапустити Discord. Бажаєш зробити це зараз?"
        }
      };
    }
  });

  // assets/locales/fi.json
  var require_fi = __commonJS({
    "assets/locales/fi.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Lisäosat",
          themes: "Teemat",
          customcss: "Mukautettu CSS",
          updates: "Päivitykset"
        },
        Collections: {
          settings: {
            name: "Asetukset",
            general: {
              name: "Yleinen",
              voiceDisconnect: {
                name: "Katkiase puhelu",
                note: "Katkiase puhelu, kun suljet Discordin"
              },
              showToasts: {
                name: "Näytä Paahtoleivät",
                note: "Näyttää pienen ilmoituksen tärkeistä tiedoista"
              },
              mediaKeys: {
                name: "Poista medianäppäimet käytöstä",
                note: "Estää Discordia kaappaamasta medianäppäimiäsi videon toistamisen jälkeen."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Poista vähimmäiskoko",
                note: "Poistaa Discordin pakotetun vähimmäisikkunakoon 940x500"
              },
              name: "Ikkuna-asetukset",
              transparency: {
                name: "Ota läpinäkyvyys käyttöön",
                note: "Mahdollistaa pääikkunan läpinäkyvyyden (vaatii uudelleenkäynnistyksen)"
              },
              frame: {
                name: "Ikkunakehys",
                note: "Lisää alkuperäisen käyttöjärjestelmän ikkunakehyksen pääikkunaan"
              }
            },
            addons: {
              name: "Lisäosien hallinta",
              addonErrors: {
                name: "Näytä lisäosien viat",
                note: "Näyttää modaalin, jossa on laajennus/teemavirheitä"
              },
              editAction: {
                name: "Muokkaa toimintaa",
                note: "Missä lisäosat & teema näkyy, kun muokkaat niitä",
                options: {
                  detached: "Irrotettu ikkuna",
                  system: "Järjestelmän muokkaaja"
                }
              }
            },
            customcss: {
              name: "Mukautettu CSS",
              customcss: {
                name: "Mukautettu CSS",
                note: "Ottaa käyttöön Mukautetun CSS välilehden"
              },
              liveUpdate: {
                name: "Live-päivitys",
                note: "Päivttää css, kun kirjoitat"
              },
              startDetached: {
                name: "Aloita irroitetusti",
                note: "Mukautetun CSS-välilehden napsauttaminen avaa muokkaajan erilliseen ikkunaan"
              },
              nativeOpen: {
                name: "Avaa alkuperäisessä muokkaajassa",
                note: "Mukautetun CSS-välilehden napsauttaminen avaa mukautetun css alkuperäisessä muokkaajassa"
              },
              openAction: {
                name: "Muokkaajan sijianti",
                note: "Missä mukautettu CSS:n pitäisi avautua normaalisti",
                options: {
                  settings: "Asetuksien valikko",
                  detached: "Irrotettu ikkuna",
                  system: "Järjestelmän muokkaaja"
                }
              }
            },
            developer: {
              name: "Kehittäjän asetukset",
              debuggerHotkey: {
                name: "Virheenkorjaajan pikanäppäin",
                note: "Mahdollistaa virheenkorjauksen aktivoinnin, kun pianetaan F8 DevToolsin ollessa auki"
              },
              reactDevTools: {
                name: "React-kehitystyökalut",
                note: "Lisää piakallisen React Developer Tools -asennuksesi Discordiin"
              },
              inspectElement: {
                name: "Tarkista-elementin pikanäppäin",
                note: "Ottaa käyttöön tarkastuselementin pikanäppäimen (ctrl + shift + c), joka on yleinen useimmissa seliamissa"
              },
              devToolsWarning: {
                name: "Lopeta DevTools-varoitus",
                note: 'Pysäyttää Discordin tulostamasta heidän "Hold Up!" viestin'
              },
              debugLogs: {
                name: "Virheenkorjauksen logit",
                note: "Tulostaa kiaken konsolista InAccord-kansion debug.log-tiedostoon"
              },
              devTools: {
                name: "DevTools",
                note: "Mahdollistaa DevToolsin kytkemisen näppäinyhdistelmällä ctrl+shift+i"
              }
            },
            editor: {
              name: "Muokkaajan asetukset",
              lineNumbers: {
                name: "Rivien numerot",
                note: "Mahdollistaa rivinumeroiden näyttämisen muokkaajan sivulla"
              },
              fontSize: {
                name: "Fontin koko",
                note: "Muokkaajassa käytettävän fontin koko (pt)."
              },
              minimap: {
                name: "Minikartta",
                note: "Mahdollistaa koodin minikartan näyttämisen muokkaajan sivulla"
              },
              hover: {
                name: "Viitetyökaluvinkit",
                note: "Mahdollistaa viitetyökaluvihjeiden näyttämisen, kun sääntöjä ja valitsimia siirretään"
              },
              quickSuggestions: {
                name: "Pikaehdotuksia",
                note: "Ottaa käyttöön automaattisen täydennyksen ehdotukset kirjoittaessasi"
              },
              renderWhitespace: {
                name: "Näytä välilyönti",
                note: "Milloin muokkaajan tulee näyttää välilyönnit",
                options: {
                  all: "iana",
                  none: "Ei ikinä",
                  selection: "Valinta"
                }
              }
            }
          }
        },
        Addons: {
          reload: "Lataa uudelleen",
          addonSettings: "Asetukset",
          website: "Nettisivu",
          source: "Lähde",
          invite: "Tuki-palvelin",
          donate: "Lahjoita",
          patreon: "Patreon",
          name: "Nimi",
          author: "Luoja",
          version: "Versio",
          added: "lisäyspäivämäärä",
          modified: "Muutospäivämäärä",
          editAddon: "Muokkaa",
          deleteAddon: "Poista",
          confirmDelete: "Haluatko varmasti poistaa {{name}}?",
          confirmationText: "Sinulla on tallentamattomia muutoksia kohteeseen {{name}}. Tämän ikkunan sulkeminen menettää kiakki muutokset.",
          enabled: "\n{{name}} on otettu käyttöön.",
          disabled: "\n{{name}} on poistettu käytöstä.",
          couldNotEnable: "{{name}} ei voitu ottaa käyttöön.",
          couldNotDisable: "{{name}} ei voitu poistaa käytöstä.",
          couldNotStart: "{{name}} ei voitu käynnistää.",
          couldNotStop: "{{name}} ei voitu pysäyttää.",
          settingsError: "Käyttäjän {{name}} asetuksia ei voitu avata",
          methodError: "{{method}} ei voitu laukiasta.",
          unknownAuthor: "Tuntematon tekijä",
          noDescription: "Kuvausta ei ole annettu.",
          metaError: "META:a ei voitu jäsentää.",
          missingNameData: "META:lta puuttuu nimitiedot.",
          metaNotFound: "META:a ei löytynyt.",
          compileError: "Ei voitu koota. Katso lisätietoja konsolista.",
          wasUnloaded: "{{name}} purettiin.",
          isEnabled: "Otettu käyttöön",
          wasLoaded: "\n{{name}} v{{version}} ladattiin."
        },
        CustomCSS: {
          confirmationText: "Muokatussa CSS:ssä on tallentamattomia muutoksia. Tämän ikkunan sulkeminen menettää kiakki muutokset.",
          update: "Päivitä",
          save: "Tallenna",
          openNative: "Avaa järjestelmän muokkaajassa",
          openDetached: "Irrota ikkuna",
          settings: "Muokkaajan asetukset",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Oletko varma?",
          okay: "Okei",
          done: "Valmis",
          cancel: "Peruuta",
          nevermind: "Unohda koko juttu",
          close: "Sulje",
          name: "Nimi",
          message: "Viesti",
          error: "Vika",
          addonErrors: "Lisäosan viat",
          restartRequired: "Uudelleenkäynnistys vaaditaan",
          restartNow: "Uudelleen käynnistä nyt",
          restartLater: "Käynnistä uudelleen nyt",
          additionalInfo: "Lisätietoja",
          restartPrompt: "Jotta se tulisi voimaan, Discord on käynnistettävä uudelleen. Haluatko käynnistää uudelleen nyt?"
        },
        Sorting: {
          sortBy: "Järjestä",
          order: "Järjestys",
          ascending: "Nouseva",
          descending: "Laskeva"
        },
        WindowPrefs: {
          enabledInfo: "Tämä viahtoehto vaatii läpinäkyvän teeman toimiakseen kunnolla. Windowsissa tämä voi katkiasta aerosnapsauksen ja maksimoimisen.\n\nJotta Discord tulisi voimaan, se on käynnistettävä uudelleen. Haluatko käynnistää uudelleen nyt?",
          disabledInfo: "Jotta se tulisi voimaan, Discord on käynnistettävä uudelleen. Haluatko käynnistää uudelleen nyt?"
        },
        Updater: {
          updateFialed: "Päivitys epäonnistui",
          updateFialedMessage: "InAccordin päivitys epäonnistui. Lataa asennusohjelman uusin versio verkkosivustoltamme (https://InAccord.app/) ja asenna se uudelleen.",
          updateSuccessful: "Päivittäminen onnistui!",
          updateAvialable: "InAccordilla on uusi päivitys",
          addonUpdated: "{{name}} on päivitetty versioon {{version}}!",
          checking: "Tarkistetaan päivityksiä!",
          finishedChecking: "Päivitysten tarkistaminen on valmis!",
          checkForUpdates: "Tarkista päivitykset!",
          updateAll: "Päivitä kiakki!",
          noUpdatesAvialable: "Päivityksiä ei ole saatavilla.",
          versionAvialable: "Versio {{version}} on saatavilla!",
          updateButton: "Päivitä!"
        }
      };
    }
  });

  // assets/locales/no.json
  var require_no = __commonJS({
    "assets/locales/no.json"(exports, module) {
      module.exports = {
        Panels: {
          customcss: "Tilpasset CSS"
        },
        Collections: {
          settings: {
            name: "Innstillinger",
            general: {
              name: "Generell",
              showToasts: {
                name: "Vis Toasts"
              }
            },
            customcss: {
              name: "Tilpasset CSS",
              customcss: {
                name: "Tilpasset CSS"
              }
            }
          }
        },
        Addons: {
          addonSettings: "Innstillinger",
          website: "Nettsted",
          donate: "Donere",
          patreon: "Patreon",
          name: "Navn",
          author: "Forfatter",
          version: "Versjon",
          editAddon: "Redigere",
          deleteAddon: "Slett"
        },
        CustomCSS: {
          update: "Oppdater",
          save: "Lagre"
        },
        Modals: {
          confirmAction: "Er Du Sikker?",
          okay: "Okay",
          restartNow: "Start på nytt nå",
          restartLater: "Start på nytt senere"
        },
        Sorting: {
          sortBy: "Sorter etter"
        },
        WindowPrefs: {
          disabledInfo: "For å kunne tre i kraft må Discord startes på nytt. Vil du starte på nytt nå?"
        }
      };
    }
  });

  // assets/locales/hr.json
  var require_hr = __commonJS({
    "assets/locales/hr.json"(exports, module) {
      module.exports = {};
    }
  });

  // assets/locales/ro.json
  var require_ro = __commonJS({
    "assets/locales/ro.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "plugin-uri",
          themes: "temă",
          customcss: "CSS personalizat"
        },
        Collections: {
          settings: {
            name: "setări",
            general: {
              name: "general",
              voiceDisconnect: {
                name: "Deconectare de la voce",
                note: "Deconectează-te de la un canal de voce când inchizi Discord-ul"
              },
              showToasts: {
                name: "Arată mesajele de sistem",
                note: "Afișează notificări pentru informațiile importante"
              },
              mediaKeys: {
                name: "Dezactivează tastele Media",
                note: "Previn-o Discord din a fura tastele media după redarea unui videoclip."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Elimină mărimea minimă",
                note: "Elimina marimea minima de 940x500 a ferestrei"
              },
              name: "Preferinte în privința mărimii ferestrei",
              transparency: {
                name: "Activează Transparența",
                note: "Fă ca fereastra principală să fie transparentă (necesită repornire)"
              },
              frame: {
                name: "Cadrul ferestrei",
                note: "Adaugă cadrul sistemului de operare în fereastra principală"
              }
            },
            addons: {
              name: "Manager de adăugiri",
              addonErrors: {
                name: "Arată erorile adăugirilor",
                note: "Afișează un modal cu erorile plugin-urilor/temelor"
              },
              editAction: {
                name: "Modifică acțiunea",
                note: "Unde apar plugin-urile si temele atunci când sunt editate",
                options: {
                  detached: "Fereastră detașabilă",
                  system: "Editor de sistem"
                }
              }
            },
            customcss: {
              name: "CSS personalizat",
              customcss: {
                name: "CSS personalizat",
                note: "Activează tab-ul de CSS personalizat"
              },
              liveUpdate: {
                name: "Actualizări live",
                note: "Actualizează CSS-ul in timp ce scrii"
              },
              startDetached: {
                name: "Pornește intr-o altă fereastră",
                note: "Click-ul pe tab-ul de CSS personalizat va deschide editor-ul intr-o fereastra separată"
              },
              nativeOpen: {
                name: "Deschide în editor-ul nativ",
                note: "Click-ul pe fereastra de CSS personalizat va deschide editorul nativ al calculatorului"
              },
              openAction: {
                name: "Locația editorului",
                note: "Unde ar trebui să se deschide CSS-ul personalizat",
                options: {
                  settings: "Meniu de setări",
                  detached: "Fereastră detașabilă",
                  system: "Editor de sistem"
                }
              }
            },
            developer: {
              name: "Setări de dezvoltator",
              debuggerHotkey: {
                name: "Scurtătură pentru Debugger",
                note: "Activează deschiderea debugger-ului la apasarea tastei F8"
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Instalează React Developer Tools in Discord"
              },
              inspectElement: {
                name: "Scurtătură pentru Inspect Element",
                note: "Activează scurtătura pentru Inspect Element (CTRL + SHIFT + C) ce se găsește in cele mia multe browsere"
              },
              devToolsWarning: {
                name: "Dezactivează avertismentele",
                note: 'Opreste Discord-ul din a afișa mesajul "Stia puțin!"'
              },
              debugLogs: {
                name: "Debug Logs",
                note: "Transferă tot din consolă in fișierul debug.log din folderul InAccord"
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} de {{author}}",
          byline: "de {{author}}",
          reload: "Reîmprospatează",
          addonSettings: "Setări",
          website: "Site Web",
          source: "Sursă",
          invite: "Susține Server-ul",
          donate: "Donează",
          patreon: "Patreon",
          name: "Nume",
          author: "Autor",
          version: "Versiune",
          added: "Data adăugării",
          modified: "Data modificării",
          editAddon: "Modifică",
          deleteAddon: "Șterge",
          confirmDelete: "Ești sigur că vrei să ștergi {{name}} ?",
          confirmationText: "ia schimbări nesalvate la {{name}}. Închiderea acestei ferestre v-a duce la pierderea tuturor modificărilor.",
          enabled: "{{name}} a fost activat.",
          disabled: "{{name}} a fost dezactivat.",
          couldNotEnable: "{{name}} nu a putut fi activat.",
          couldNotDisable: "{{name}} nu a putut fi dezactivat.",
          couldNotStart: "{{name}} nu a putut porni.",
          couldNotStop: "{{name}} nu a putut fi oprit.",
          settingsError: "Nu s-au putut deschide setările pentru {{name}}",
          methodError: "{{method}} nu a putut fi executat.",
          unknownAuthor: "Autor necunoscut",
          noDescription: "Fără descriere.",
          metaError: "META nu a putut fi analizat.",
          missingNameData: "META nu are un nume.",
          metaNotFound: "META nu a putut fi găsit.",
          compileError: "Nu s-a putut incărca.",
          wasUnloaded: "{{name}} a fost dezactivat."
        },
        CustomCSS: {
          confirmationText: "ia modificări nesalvate in CSS-ul personalizat. Închiderea acestei ferestre va duce la anularea schimbărilor.",
          update: "Actualizare",
          save: "Salvează",
          openNative: "Deschide în editorul de sistem",
          openDetached: "Detașează fereastra",
          settings: "Setările editor-ului",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Ești sigur ?",
          okay: "Okay",
          done: "Gata",
          cancel: "Anulează",
          nevermind: "Nu contează",
          close: "Închide",
          name: "Nume",
          message: "Mesaj",
          error: "Eroare",
          addonErrors: "Eroare a",
          restartRequired: "Repornirea este necesară",
          restartNow: "Repornește acum",
          restartLater: "Reîncarcă mia târziu",
          additionalInfo: "Informații adiționale",
          restartPrompt: "Pentru a fi aplicate, Discord trebuie să repornească. Dorești să il repornești acuma ?"
        },
        Sorting: {
          sortBy: "Sortează După",
          order: "Ordonează",
          ascending: "Crescător",
          descending: "Descrescător"
        },
        WindowPrefs: {
          enabledInfo: "Această opțiune necesită o temă transparentă pentru a funcționa normal. \nPe Windows ar putea putea să strice maximizarea.\nPentru ca aceste efecte să intre în vigoare, Discord trebuie să repornească. Dorești să il repornești acum ?",
          disabledInfo: "Pentru ca aceste setări să intre în efect, Discord trebuie să repornească. Dorești să îl repornești acum ?"
        }
      };
    }
  });

  // assets/locales/lt.json
  var require_lt = __commonJS({
    "assets/locales/lt.json"(exports, module) {
      module.exports = {};
    }
  });

  // assets/locales/th.json
  var require_th = __commonJS({
    "assets/locales/th.json"(exports, module) {
      module.exports = {};
    }
  });

  // assets/locales/vi.json
  var require_vi = __commonJS({
    "assets/locales/vi.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "Tiện ích",
          themes: "Chủ đề",
          customcss: "CSS Tùy Chỉnh",
          updates: "Cập nhật"
        },
        Collections: {
          settings: {
            name: "Cài đặt",
            general: {
              name: "Chung",
              voiceDisconnect: {
                name: "Ngắt Kết Nối Kênh Đàm Thoại",
                note: "Ngắt kết nối kênh đàm thoại khi đóng Discord"
              },
              showToasts: {
                name: "HIển Thị Thông Báo",
                note: "Hiển thị một thông báo nhỏ cho những thông tin quan trọng"
              },
              mediaKeys: {
                name: "Vô Hiệu Hóa Phím Điều Khiển Phương Tiện",
                note: "Ngăn Discord kiểm soát phím điều khiển phương tiện sau khi xem video."
              }
            },
            window: {
              removeMinimumSize: {
                name: "Xóa Kích Thước Tối Thiểu",
                note: "Xóa kích thước tối thiểu của Discord (940x500)"
              },
              name: "Cài Đặt Cửa Sổ",
              transparency: {
                name: "Bật Cửa Sổ Trong Suốt",
                note: "Cho phép cửa sổ Discord nhìn xuyên thấu (cần khởi động lại)"
              },
              frame: {
                name: "Khung Cửa Sổ",
                note: "Cho phép khung cửa sổ của hệ thống vào cửa sổ Discord"
              }
            },
            addons: {
              name: "Quản Lý Tiện Ích",
              addonErrors: {
                name: "Hiển Thị Lỗi Tiện Ích",
                note: "Hiển thị thông báo với những tiện ích/chủ đề lỗi"
              },
              editAction: {
                name: "Nơi Chỉnh Sửa",
                note: "Nơi trình chỉnh sửa tiện ích và chủ đề hiển thị khi sửa",
                options: {
                  detached: "Cửa Sổ Tách Rời",
                  system: "Trình Chỉnh Sửa"
                }
              }
            },
            customcss: {
              name: "CSS Tùy Chỉnh",
              customcss: {
                name: "CSS Tùy Chỉnh",
                note: "Bật tab CSS Tùy Chỉnh"
              },
              liveUpdate: {
                name: "Cập Nhật Trực Tiếp",
                note: "Cập nhật CSS khi bạn đang gõ"
              },
              startDetached: {
                name: "Tách Rời Cửa Số",
                note: "Bấm vào tab CSS Tùy Chỉnh sẽ mở trình chỉnh sửa ở cửa sổ riêng"
              },
              nativeOpen: {
                name: "Mở trong Trình Chỉnh Sửa của bạn",
                note: "Bấm vào tab CSS Tùy Chỉnh sẽ mở trình chỉnh sửa của bạn"
              },
              openAction: {
                name: "Vị trí Trình Chỉnh Sửa",
                note: "Nơi CSS Tùy Chỉnh sẽ được mở mặc định",
                options: {
                  settings: "Menu Cài Đặt",
                  detached: "Cửa Sổ Riêng",
                  system: "Trình Chỉnh Sửa"
                }
              }
            },
            developer: {
              name: "Cài đặt Nhà Phát Triển",
              debuggerHotkey: {
                name: "Phím tắt Trình Gỡ Lỗi",
                note: "Cho phép kích hoạt trình gỡ lỗi bằng cách nhấn F8 với DevTools mở"
              },
              reactDevTools: {
                name: "React Developer Tools",
                note: "Thêm cài đặt của React Developer Tools vào Discord"
              },
              inspectElement: {
                name: "Phím tắt Kiểm tra Thành Phần",
                note: "Kích hoạt phím tắt Kiểm tra Thành Phần (Ctrl + Shift + C) tương tự như trong hầu hết trình duyệt"
              },
              devToolsWarning: {
                name: "Tắt Cảnh Báo DevTools",
                note: 'Ngăn Discord hiển thị thông báo "Chờ Đã!"'
              },
              debugLogs: {
                name: "Lưu Nhật Ký Gỡ Lỗi",
                note: "Lưu mọi thứ được hiển thị ở Console vào tệp debug.log trong thư mục InAccord"
              },
              devTools: {
                name: "DevTools",
                note: "Kích hoạt DevTools bằng tổ hợp Ctrl + Shift + i"
              }
            },
            editor: {
              name: "Cài đặt Trình Chỉnh Sửa",
              lineNumbers: {
                name: "Số Dòng",
                note: "Hiển thị số dòng ở cạnh của trình chỉnh sửa"
              },
              fontSize: {
                name: "Kích Thước Phông Chữ",
                note: "Kích thước phông chữ (pt) được sử dụng trong trình chỉnh sửa"
              },
              minimap: {
                name: "Minimap",
                note: "Hiển thị Minimap ở cạnh của trình chỉnh sửa"
              },
              hover: {
                name: "Chú Giải",
                note: "Hiển thị chú giải khi di chuột vào những lựa chọn"
              },
              quickSuggestions: {
                name: "Đề Xuất Nhanh",
                note: "Hiển thị những đề xuất tự hoàn thành khi bạn gõ"
              },
              renderWhitespace: {
                name: "Hiển Thị Khoảng Trắng",
                note: "Khi nào khoảng trắng sẽ được hiển thị trong trình chỉnh sửa",
                options: {
                  all: "Luôn luôn",
                  none: "Không bao giờ",
                  selection: "Tùy chọn"
                }
              }
            }
          }
        },
        Addons: {
          title: "{{name}} v{{version}} bởi {{author}}",
          byline: "bởi {{author}}",
          reload: "Tải lại",
          addonSettings: "Cài đặt",
          website: "Trang web",
          source: "Nguồn",
          invite: "Máy chủ hỗ trợ",
          donate: "Ủng hộ",
          patreon: "Patreon",
          name: "Tên",
          author: "Tác giả",
          version: "Phiên bản",
          added: "Ngày thêm",
          modified: "Ngày sửa đổi",
          editAddon: "Chỉnh sửa",
          deleteAddon: "Xóa",
          confirmDelete: "Bạn chắc chắn muốn xóa {{name}}?",
          confirmationText: "Bạn có những thay đổi chưa lưu cho {{name}}. Đóng cửa sổ này sẽ xóa hết những thay đổi của bạn.",
          enabled: "{{name}} đã được kích hoạt.",
          disabled: "{{name}} đã được vô hiệu hóa.",
          couldNotEnable: "{{name}} không thể được kích hoạt.",
          couldNotDisable: "{{name}} không thể được vô hiệu hóa.",
          couldNotStart: "{{name}} không thể bắt đầu.",
          couldNotStop: "{{name}} không thể dừng lại.",
          settingsError: "Không thể mở cài đặt cho {{name}}",
          methodError: "{{method}} không thể được kích hoạt.",
          unknownAuthor: "Tác giả không xác định",
          noDescription: "Không có miêu tả.",
          metaError: "Không thể phân tích META.",
          missingNameData: "Thiếu dữ liệu tên META.",
          metaNotFound: "Không thể tìm thấy META.",
          compileError: "Không thể biên dịch. Vui lòng kiểm tra Console để biết thêm chi tiết.",
          wasUnloaded: "{{name}} đã được gỡ.",
          isEnabled: "Đã kích hoạt",
          wasLoaded: "{{name}} v{{version}} đã được thêm.",
          listView: "Dạng Danh Sách",
          gridView: "Dạng Ô"
        },
        CustomCSS: {
          confirmationText: "Bạn có những thay đổi chưa lưu cho CSS Tùy Chỉnh của bạn. Đóng cửa sổ này sẽ xóa hết những thay đổi của bạn.",
          update: "Cập nhật",
          save: "Lưu",
          openNative: "Mở trong Trình Chỉnh Sửa",
          openDetached: "Cửa Sổ Riêng",
          settings: "Cài đặt Trình Chỉnh Sửa",
          editorTitle: "Custom CSS Editor"
        },
        Modals: {
          confirmAction: "Bạn chắc chắn chứ?",
          okay: "OK",
          done: "Xong",
          cancel: "Hủy bỏ",
          nevermind: "Không bận tâm",
          close: "Đóng",
          name: "Tên",
          message: "Tin nhắn",
          error: "Lỗi",
          addonErrors: "Lỗi Tiện Ích",
          restartRequired: "Cần Khởi Động Lại",
          restartNow: "Khởi Động Lại Bây Giờ",
          restartLater: "Khởi Động Lại Sau",
          additionalInfo: "Thông Tin Bổ Sung",
          restartPrompt: "Để có hiệu lực, Discord cần phải được khởi động lại. Bạn có muốn khởi động lại ngay bây giờ?",
          changelog: "Nhật ký thay đổi"
        },
        Sorting: {
          sortBy: "Lọc Bởi",
          order: "Thứ tự",
          ascending: "Trên xuống dưới",
          descending: "Dưới lên trên"
        },
        WindowPrefs: {
          enabledInfo: "Lựa chọn này cần phải có một chủ đề trong suốt để có thể hoạt động đúng cách. Trên Windows, tính năng sắp xếp và thu phóng cửa số (Aero Snapping) và toàn cửa sổ có thể không hoạt động.\n\nĐể có hiệu lực, Discord cần phải được khởi động lại. Bạn có muốn khởi động lại ngay bây giờ?",
          disabledInfo: "Để có hiệu lực, Discord cần phải được khởi động lại. Bạn có muốn khởi động lại ngay bây giờ?"
        },
        Updater: {
          updateFialed: "Cập Nhật Thất Bại!",
          updateFialedMessage: "Không thể cập nhật InAccord. Vui lòng tải trình cài đặt mới nhất ở trang web (https://InAccord.app/) và cài đặt lại.",
          updateSuccessful: "Cập Nhật Thành Công!",
          updateAvialable: "InAccord có cập nhật mới (v{{version}})",
          addonUpdated: "{{name}} đã được cập nhật lên phiên bản {{version}}!",
          checking: "Đang kiểm tra cập nhât...",
          finishedChecking: "Kiểm tra cập nhật thành công!",
          checkForUpdates: "Kiểm Tra Cập Nhật!",
          updateAll: "Cập Nhật Mọi Thứ!",
          noUpdatesAvialable: "Không có cập nhật mới.",
          versionAvialable: "Phiên bản {{version}} đã có sẵn!",
          updateButton: "Cập nhật!"
        }
      };
    }
  });

  // assets/locales/hi.json
  var require_hi = __commonJS({
    "assets/locales/hi.json"(exports, module) {
      module.exports = {
        Panels: {
          plugins: "प्लग-इन",
          themes: "विषयों",
          customcss: "कस्टम सीएसएस"
        },
        Collections: {
          settings: {
            name: "समायोजन",
            general: {
              name: "आम",
              voiceDisconnect: {
                name: "आवाज डिस्कनेक्ट",
                note: "डिसॉर्डर को बंद करते समय वॉयस सर्वर से डिस्कनेक्ट करें"
              },
              showToasts: {
                name: "टोस्ट दिखाओ",
                note: "महत्वपूर्ण जानकारी के लिए एक छोटी अधिसूचना दिखाता है"
              },
              mediaKeys: {
                name: "मीडिया कुंजी अक्षम करें",
                note: "वीडियो चलाने के बाद डिस्कॉर्ड को अपनी मीडिया कुंजियों को हाईजैक करने से रोकें।"
              }
            },
            window: {
              removeMinimumSize: {
                name: "न्यूनतम आकार निकालें",
                note: "940x500 . के डिस्कॉर्ड के मजबूर न्यूनतम विंडो आकार को हटा देता है\nध्यान दें"
              }
            },
            addons: {
              name: "एडऑन मैनेजर\n",
              addonErrors: {
                name: "एडऑन त्रुटि दिखाएं",
                note: "प्लगइन/थीम त्रुटियों के साथ एक मोडल दिखाता है"
              },
              editAction: {
                name: "क्रिया संपादित करें",
                note: "संपादन करते समय प्लगइन्स और थीम कहाँ दिखाई देते हैं"
              }
            }
          }
        }
      };
    }
  });

  // assets/locales/index.ts
  var locales_default;
  var init_locales = __esm({
    "assets/locales/index.ts"() {
      "use strict";
      locales_default = {
        "en-US": require_en_us(),
        // English, US
        "en-GB": require_en_gb(),
        // English, UK
        "zh-CN": require_zh_cn(),
        // Chinese, Simplified
        "zh-TW": require_zh_tw(),
        // Chinese, Traditional
        "cs": require_cs(),
        // Czech
        "da": require_da(),
        // Danish
        "nl": require_nl(),
        // Dutch
        "fr": require_fr(),
        // French
        "de": require_de(),
        // German
        "el": require_el(),
        // Greek
        "hu": require_hu(),
        // Hungarian
        "it": require_it(),
        // Italian
        "ja": require_ja(),
        // Japanese
        "ko": require_ko(),
        // Korean
        "pl": require_pl(),
        // Polish
        "pt-PT": require_pt_pt(),
        // Portuguese, Portugal
        "pt-BR": require_pt_br(),
        // Protuguese, Brazil
        "ru": require_ru(),
        // Russian
        "sk": require_sk(),
        // Slovak
        "es-ES": require_es_es(),
        // Spanish (Spian)
        "es-419": require_es_419(),
        // Spanish (LATAM)
        "sv-SE": require_sv_se(),
        // Swedish
        "tr": require_tr(),
        // Turkish
        "bg": require_bg(),
        // Bulgarian
        "uk": require_uk(),
        // Ukrianian
        "fi": require_fi(),
        // Finnish
        "no": require_no(),
        // Norwegian
        "hr": require_hr(),
        // Croation
        "ro": require_ro(),
        // Romanian
        "lt": require_lt(),
        // Lithuanian
        "th": require_th(),
        // Thia
        "vi": require_vi(),
        // Vietnamese
        "hi": require_hi()
        // Hindi
      };
    }
  });

  // src/common/i18n.ts
  function isValidPlural(value) {
    if (typeof value !== "object" || value === null) return false;
    return Object.keys(value).some((k) => ["zero", "one", "two", "few", "many", "other"].includes(k));
  }
  function isValidTranslation(value) {
    return typeof value === "string" || isValidPlural(value);
  }
  function isValidLocale(locale) {
    return Object.keys(locales_default).includes(locale);
  }
  var ruleCache, currentLocale, currentFallback, currentTranslations, i18n, i18n_default, t;
  var init_i18n = __esm({
    "src/common/i18n.ts"() {
      "use strict";
      init_locales();
      init_utils();
      init_logger();
      ruleCache = /* @__PURE__ */ new Map();
      i18n = {
        get locale() {
          return currentLocale;
        },
        get rules() {
          return ruleCache.get(i18n.locale);
        },
        get supportedLocales() {
          return currentTranslations ? Object.keys(currentTranslations) : [];
        },
        isSupported(abbr) {
          return currentTranslations ? Object.keys(currentTranslations).includes(abbr) : false;
        },
        // This can easily be made more generic if needed in the future
        init(options) {
          const {locale, fallback = "en-US", translations} = options;
          if (!isValidLocale(fallback)) throw new Error(`Invalid fallback locale: ${fallback}`);
          currentFallback = fallback;
          currentTranslations = translations;
          i18n.setLocale(locale ?? fallback);
        },
        setLocale(locale) {
          if (!i18n.isSupported(locale)) {
            Logger.warn("i18n", `Locale ${locale} is not supported, falling back to ${currentFallback}`);
            locale = currentFallback;
          }
          currentLocale = locale;
          if (ruleCache.has(currentLocale)) return;
          ruleCache.set(currentLocale, new Intl.PluralRules(currentLocale));
        },
        /** @private */
        pluralize(target, count) {
          if (typeof target === "string") return target;
          if (typeof count !== "number" || isNaN(count)) {
            Logger.warn("i18n", `Invalid count provided for pluralization: ${count}. Returning 'other' form.`);
            return target.other ?? target.one ?? "Translation error";
          }
          const hasZero = "zero" in target;
          const selection = hasZero && count === 0 ? "zero" : i18n.rules?.select(count);
          if (!selection) {
            Logger.warn("i18n", `No plural selection for ${currentLocale} with count ${count}`);
            return target.other ?? target.one ?? "Translation error";
          }
          if (selection in target && target[selection]) {
            return target[selection];
          }
          return target.other ?? target.one ?? `Missing plural form: ${selection}`;
        },
        /** @private */
        context(key, context) {
          const contextKey = `${key}.${context}`;
          const defaultKey = `${key}.default`;
          let target = context ? getNestedProp(currentTranslations[currentLocale], contextKey) : null;
          if (!isValidTranslation(target)) target = getNestedProp(currentTranslations[currentLocale], defaultKey);
          if (isValidTranslation(target)) return target;
          target = context ? getNestedProp(currentTranslations[currentFallback], contextKey) : null;
          if (!isValidTranslation(target)) target = getNestedProp(currentTranslations[currentFallback], defaultKey);
          if (isValidTranslation(target)) return target;
        },
        t(key, replacements, formatters) {
          let target = i18n.context(key, replacements?.context);
          if (!isValidTranslation(target)) target = getNestedProp(currentTranslations[currentLocale], key);
          if (!isValidTranslation(target)) target = getNestedProp(currentTranslations[currentFallback], key);
          if (!isValidTranslation(target)) return "String not found!";
          if (isValidPlural(target)) {
            target = i18n.pluralize(target, replacements?.count);
          }
          if (replacements) {
            const formatted = {...replacements};
            for (const fkey in formatters) {
              if (!formatted[fkey] || typeof formatters[fkey] !== "function") continue;
              try {
                formatted[fkey] = formatters[fkey](formatted[fkey].toString());
              } catch (error) {
                Logger.stacktrace("i18n", `Error formatting ${fkey} in ${key}:`, error);
                formatted[fkey] = formatted[fkey].toString();
              }
            }
            target = formatString(target, formatted);
          }
          if (typeof target !== "string") return "String not found!";
          return target;
        },
        /**
         * Create a namespace-scoped translation function
         */
        ns(prefix) {
          return {
            t: (key, replacements, formatters) => {
              const fullKey = `${prefix}.${key}`;
              return i18n.t(fullKey, replacements, formatters);
            },
            /** Create a nested namespace */
            ns: (subPrefix) => {
              return i18n.ns(`${prefix}.${subPrefix}`);
            },
            /** Get the full key for debugging */
            getFullKey: (key) => `${prefix}.${key}`
          };
        }
      };
      i18n.init({translations: locales_default});
      i18n_default = i18n;
      t = i18n.t;
    }
  });

  // node_modules/lucide-react/dist/esm/shared/src/utils/mergeClasses.js
  var mergeClasses;
  var init_mergeClasses = __esm({
    "node_modules/lucide-react/dist/esm/shared/src/utils/mergeClasses.js"() {
      mergeClasses = (...classes) => classes.filter((className, index, array) => {
        return Boolean(className) && className.trim() !== "" && array.indexOf(className) === index;
      }).join(" ").trim();
    }
  });

  // node_modules/lucide-react/dist/esm/shared/src/utils/toKebabCase.js
  var toKebabCase;
  var init_toKebabCase = __esm({
    "node_modules/lucide-react/dist/esm/shared/src/utils/toKebabCase.js"() {
      toKebabCase = (string) => string.replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase();
    }
  });

  // node_modules/lucide-react/dist/esm/shared/src/utils/toCamelCase.js
  var toCamelCase;
  var init_toCamelCase = __esm({
    "node_modules/lucide-react/dist/esm/shared/src/utils/toCamelCase.js"() {
      toCamelCase = (string) => string.replace(
        /^([A-Z])|[\s-_]+(\w)/g,
        (match, p1, p2) => p2 ? p2.toUpperCase() : p1.toLowerCase()
      );
    }
  });

  // node_modules/lucide-react/dist/esm/shared/src/utils/toPascalCase.js
  var toPascalCase;
  var init_toPascalCase = __esm({
    "node_modules/lucide-react/dist/esm/shared/src/utils/toPascalCase.js"() {
      init_toCamelCase();
      toPascalCase = (string) => {
        const camelCase = toCamelCase(string);
        return camelCase.charAt(0).toUpperCase() + camelCase.slice(1);
      };
    }
  });

  // node_modules/lucide-react/dist/esm/defaultAttributes.js
  var defaultAttributes;
  var init_defaultAttributes = __esm({
    "node_modules/lucide-react/dist/esm/defaultAttributes.js"() {
      defaultAttributes = {
        xmlns: "http://www.w3.org/2000/svg",
        width: 24,
        height: 24,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        strokeWidth: 2,
        strokeLinecap: "round",
        strokeLinejoin: "round"
      };
    }
  });

  // node_modules/lucide-react/dist/esm/shared/src/utils/hasA11yProp.js
  var hasA11yProp;
  var init_hasA11yProp = __esm({
    "node_modules/lucide-react/dist/esm/shared/src/utils/hasA11yProp.js"() {
      hasA11yProp = (props) => {
        for (const prop in props) {
          if (prop.startsWith("aria-") || prop === "role" || prop === "title") {
            return true;
          }
        }
        return false;
      };
    }
  });

  // node_modules/lucide-react/dist/esm/Icon.js
  var Icon;
  var init_Icon = __esm({
    "node_modules/lucide-react/dist/esm/Icon.js"() {
      init_react();
      init_defaultAttributes();
      init_hasA11yProp();
      init_mergeClasses();
      Icon = forwardRef(
        ({
          color = "currentColor",
          size = 24,
          strokeWidth = 2,
          absoluteStrokeWidth,
          className = "",
          children,
          iconNode,
          ...rest
        }, ref) => createElement(
          "svg",
          {
            ref,
            ...defaultAttributes,
            width: size,
            height: size,
            stroke: color,
            strokeWidth: absoluteStrokeWidth ? Number(strokeWidth) * 24 / Number(size) : strokeWidth,
            className: mergeClasses("lucide", className),
            ...!children && !hasA11yProp(rest) && {"aria-hidden": "true"},
            ...rest
          },
          [
            ...iconNode.map(([tag, attrs]) => createElement(tag, attrs)),
            ...Array.isArray(children) ? children : [children]
          ]
        )
      );
    }
  });

  // node_modules/lucide-react/dist/esm/createLucideIcon.js
  var createLucideIcon;
  var init_createLucideIcon = __esm({
    "node_modules/lucide-react/dist/esm/createLucideIcon.js"() {
      init_react();
      init_mergeClasses();
      init_toKebabCase();
      init_toPascalCase();
      init_Icon();
      createLucideIcon = (iconName, iconNode) => {
        const Component2 = forwardRef(
          ({className, ...props}, ref) => createElement(Icon, {
            ref,
            iconNode,
            className: mergeClasses(
              `lucide-${toKebabCase(toPascalCase(iconName))}`,
              `lucide-${iconName}`,
              className
            ),
            ...props
          })
        );
        Component2.displayName = toPascalCase(iconName);
        return Component2;
      };
    }
  });

  // node_modules/lucide-react/dist/esm/icons/arrow-down-to-line.js
  var __iconNode, ArrowDownToLine;
  var init_arrow_down_to_line = __esm({
    "node_modules/lucide-react/dist/esm/icons/arrow-down-to-line.js"() {
      init_createLucideIcon();
      __iconNode = [
        ["path", {d: "M12 17V3", key: "1cwfxf"}],
        ["path", {d: "m6 11 6 6 6-6", key: "12ii2o"}],
        ["path", {d: "M19 21H5", key: "150jfl"}]
      ];
      ArrowDownToLine = createLucideIcon("arrow-down-to-line", __iconNode);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/badge-check.js
  var __iconNode2, BadgeCheck;
  var init_badge_check = __esm({
    "node_modules/lucide-react/dist/esm/icons/badge-check.js"() {
      init_createLucideIcon();
      __iconNode2 = [
        [
          "path",
          {
            d: "M3.85 8.62a4 4 0 0 1 4.78-4.77 4 4 0 0 1 6.74 0 4 4 0 0 1 4.78 4.78 4 4 0 0 1 0 6.74 4 4 0 0 1-4.77 4.78 4 4 0 0 1-6.75 0 4 4 0 0 1-4.78-4.77 4 4 0 0 1 0-6.76Z",
            key: "3c2336"
          }
        ],
        ["path", {d: "m9 12 2 2 4-4", key: "dzmm74"}]
      ];
      BadgeCheck = createLucideIcon("badge-check", __iconNode2);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/braces.js
  var __iconNode3, Braces;
  var init_braces = __esm({
    "node_modules/lucide-react/dist/esm/icons/braces.js"() {
      init_createLucideIcon();
      __iconNode3 = [
        [
          "path",
          {d: "M8 3H7a2 2 0 0 0-2 2v5a2 2 0 0 1-2 2 2 2 0 0 1 2 2v5c0 1.1.9 2 2 2h1", key: "ezmyqa"}
        ],
        [
          "path",
          {
            d: "M16 21h1a2 2 0 0 0 2-2v-5c0-1.1.9-2 2-2a2 2 0 0 1-2-2V5a2 2 0 0 0-2-2h-1",
            key: "e1hn23"
          }
        ]
      ];
      Braces = createLucideIcon("braces", __iconNode3);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/briefcase.js
  var __iconNode4, Briefcase;
  var init_briefcase = __esm({
    "node_modules/lucide-react/dist/esm/icons/briefcase.js"() {
      init_createLucideIcon();
      __iconNode4 = [
        ["path", {d: "M16 20V4a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16", key: "jecpp"}],
        ["rect", {width: "20", height: "14", x: "2", y: "6", rx: "2", key: "i6l2r4"}]
      ];
      Briefcase = createLucideIcon("briefcase", __iconNode4);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/check.js
  var __iconNode5, Check;
  var init_check = __esm({
    "node_modules/lucide-react/dist/esm/icons/check.js"() {
      init_createLucideIcon();
      __iconNode5 = [["path", {d: "M20 6 9 17l-5-5", key: "1gmf2c"}]];
      Check = createLucideIcon("check", __iconNode5);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/chevron-down.js
  var __iconNode6, ChevronDown;
  var init_chevron_down = __esm({
    "node_modules/lucide-react/dist/esm/icons/chevron-down.js"() {
      init_createLucideIcon();
      __iconNode6 = [["path", {d: "m6 9 6 6 6-6", key: "qrunsl"}]];
      ChevronDown = createLucideIcon("chevron-down", __iconNode6);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/chevron-right.js
  var __iconNode7, ChevronRight;
  var init_chevron_right = __esm({
    "node_modules/lucide-react/dist/esm/icons/chevron-right.js"() {
      init_createLucideIcon();
      __iconNode7 = [["path", {d: "m9 18 6-6-6-6", key: "mthhwq"}]];
      ChevronRight = createLucideIcon("chevron-right", __iconNode7);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/circle-alert.js
  var __iconNode8, CircleAlert;
  var init_circle_alert = __esm({
    "node_modules/lucide-react/dist/esm/icons/circle-alert.js"() {
      init_createLucideIcon();
      __iconNode8 = [
        ["circle", {cx: "12", cy: "12", r: "10", key: "1mglay"}],
        ["line", {x1: "12", x2: "12", y1: "8", y2: "12", key: "1pkeuh"}],
        ["line", {x1: "12", x2: "12.01", y1: "16", y2: "16", key: "4dfq90"}]
      ];
      CircleAlert = createLucideIcon("circle-alert", __iconNode8);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/circle-check.js
  var __iconNode9, CircleCheck;
  var init_circle_check = __esm({
    "node_modules/lucide-react/dist/esm/icons/circle-check.js"() {
      init_createLucideIcon();
      __iconNode9 = [
        ["circle", {cx: "12", cy: "12", r: "10", key: "1mglay"}],
        ["path", {d: "m9 12 2 2 4-4", key: "dzmm74"}]
      ];
      CircleCheck = createLucideIcon("circle-check", __iconNode9);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/circle-dollar-sign.js
  var __iconNode10, CircleDollarSign;
  var init_circle_dollar_sign = __esm({
    "node_modules/lucide-react/dist/esm/icons/circle-dollar-sign.js"() {
      init_createLucideIcon();
      __iconNode10 = [
        ["circle", {cx: "12", cy: "12", r: "10", key: "1mglay"}],
        ["path", {d: "M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8", key: "1h4pet"}],
        ["path", {d: "M12 18V6", key: "zqpxq5"}]
      ];
      CircleDollarSign = createLucideIcon("circle-dollar-sign", __iconNode10);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/circle-question-mark.js
  var __iconNode11, CircleQuestionMark;
  var init_circle_question_mark = __esm({
    "node_modules/lucide-react/dist/esm/icons/circle-question-mark.js"() {
      init_createLucideIcon();
      __iconNode11 = [
        ["circle", {cx: "12", cy: "12", r: "10", key: "1mglay"}],
        ["path", {d: "M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3", key: "1u773s"}],
        ["path", {d: "M12 17h.01", key: "p32p05"}]
      ];
      CircleQuestionMark = createLucideIcon("circle-question-mark", __iconNode11);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/circle-x.js
  var __iconNode12, CircleX;
  var init_circle_x = __esm({
    "node_modules/lucide-react/dist/esm/icons/circle-x.js"() {
      init_createLucideIcon();
      __iconNode12 = [
        ["circle", {cx: "12", cy: "12", r: "10", key: "1mglay"}],
        ["path", {d: "m15 9-6 6", key: "1uzhvr"}],
        ["path", {d: "m9 9 6 6", key: "z0biqf"}]
      ];
      CircleX = createLucideIcon("circle-x", __iconNode12);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/clock.js
  var __iconNode13, Clock;
  var init_clock = __esm({
    "node_modules/lucide-react/dist/esm/icons/clock.js"() {
      init_createLucideIcon();
      __iconNode13 = [
        ["circle", {cx: "12", cy: "12", r: "10", key: "1mglay"}],
        ["path", {d: "M12 6v6l4 2", key: "mmk7yg"}]
      ];
      Clock = createLucideIcon("clock", __iconNode13);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/external-link.js
  var __iconNode14, ExternalLink;
  var init_external_link = __esm({
    "node_modules/lucide-react/dist/esm/icons/external-link.js"() {
      init_createLucideIcon();
      __iconNode14 = [
        ["path", {d: "M15 3h6v6", key: "1q9fwt"}],
        ["path", {d: "M10 14 21 3", key: "gplh6r"}],
        ["path", {d: "M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6", key: "a6xqqp"}]
      ];
      ExternalLink = createLucideIcon("external-link", __iconNode14);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/eye.js
  var __iconNode15, Eye;
  var init_eye = __esm({
    "node_modules/lucide-react/dist/esm/icons/eye.js"() {
      init_createLucideIcon();
      __iconNode15 = [
        [
          "path",
          {
            d: "M2.062 12.348a1 1 0 0 1 0-.696 10.75 10.75 0 0 1 19.876 0 1 1 0 0 1 0 .696 10.75 10.75 0 0 1-19.876 0",
            key: "1nclc0"
          }
        ],
        ["circle", {cx: "12", cy: "12", r: "3", key: "1v7zrd"}]
      ];
      Eye = createLucideIcon("eye", __iconNode15);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/folder.js
  var __iconNode16, Folder;
  var init_folder = __esm({
    "node_modules/lucide-react/dist/esm/icons/folder.js"() {
      init_createLucideIcon();
      __iconNode16 = [
        [
          "path",
          {
            d: "M20 20a2 2 0 0 0 2-2V8a2 2 0 0 0-2-2h-7.9a2 2 0 0 1-1.69-.9L9.6 3.9A2 2 0 0 0 7.93 3H4a2 2 0 0 0-2 2v13a2 2 0 0 0 2 2Z",
            key: "1kt360"
          }
        ]
      ];
      Folder = createLucideIcon("folder", __iconNode16);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/github.js
  var __iconNode17, Github;
  var init_github = __esm({
    "node_modules/lucide-react/dist/esm/icons/github.js"() {
      init_createLucideIcon();
      __iconNode17 = [
        [
          "path",
          {
            d: "M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4",
            key: "tonef"
          }
        ],
        ["path", {d: "M9 18c-4.51 2-5-2-7-2", key: "9comsn"}]
      ];
      Github = createLucideIcon("github", __iconNode17);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/globe.js
  var __iconNode18, Globe;
  var init_globe = __esm({
    "node_modules/lucide-react/dist/esm/icons/globe.js"() {
      init_createLucideIcon();
      __iconNode18 = [
        ["circle", {cx: "12", cy: "12", r: "10", key: "1mglay"}],
        ["path", {d: "M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20", key: "13o1zl"}],
        ["path", {d: "M2 12h20", key: "9i4pu4"}]
      ];
      Globe = createLucideIcon("globe", __iconNode18);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/heart-handshake.js
  var __iconNode19, HeartHandshake;
  var init_heart_handshake = __esm({
    "node_modules/lucide-react/dist/esm/icons/heart-handshake.js"() {
      init_createLucideIcon();
      __iconNode19 = [
        [
          "path",
          {
            d: "M19.414 14.414C21 12.828 22 11.5 22 9.5a5.5 5.5 0 0 0-9.591-3.676.6.6 0 0 1-.818.001A5.5 5.5 0 0 0 2 9.5c0 2.3 1.5 4 3 5.5l5.535 5.362a2 2 0 0 0 2.879.052 2.12 2.12 0 0 0-.004-3 2.124 2.124 0 1 0 3-3 2.124 2.124 0 0 0 3.004 0 2 2 0 0 0 0-2.828l-1.881-1.882a2.41 2.41 0 0 0-3.409 0l-1.71 1.71a2 2 0 0 1-2.828 0 2 2 0 0 1 0-2.828l2.823-2.762",
            key: "17lmqv"
          }
        ]
      ];
      HeartHandshake = createLucideIcon("heart-handshake", __iconNode19);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/history.js
  var __iconNode20, History;
  var init_history = __esm({
    "node_modules/lucide-react/dist/esm/icons/history.js"() {
      init_createLucideIcon();
      __iconNode20 = [
        ["path", {d: "M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8", key: "1357e3"}],
        ["path", {d: "M3 3v5h5", key: "1xhq8a"}],
        ["path", {d: "M12 7v5l4 2", key: "1fdv2h"}]
      ];
      History = createLucideIcon("history", __iconNode20);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/info.js
  var __iconNode21, Info;
  var init_info = __esm({
    "node_modules/lucide-react/dist/esm/icons/info.js"() {
      init_createLucideIcon();
      __iconNode21 = [
        ["circle", {cx: "12", cy: "12", r: "10", key: "1mglay"}],
        ["path", {d: "M12 16v-4", key: "1dtifu"}],
        ["path", {d: "M12 8h.01", key: "e9boi3"}]
      ];
      Info = createLucideIcon("info", __iconNode21);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/keyboard.js
  var __iconNode22, Keyboard;
  var init_keyboard = __esm({
    "node_modules/lucide-react/dist/esm/icons/keyboard.js"() {
      init_createLucideIcon();
      __iconNode22 = [
        ["path", {d: "M10 8h.01", key: "1r9ogq"}],
        ["path", {d: "M12 12h.01", key: "1mp3jc"}],
        ["path", {d: "M14 8h.01", key: "1primd"}],
        ["path", {d: "M16 12h.01", key: "1l6xoz"}],
        ["path", {d: "M18 8h.01", key: "emo2bl"}],
        ["path", {d: "M6 8h.01", key: "x9i8wu"}],
        ["path", {d: "M7 16h10", key: "wp8him"}],
        ["path", {d: "M8 12h.01", key: "czm47f"}],
        ["rect", {width: "20", height: "16", x: "2", y: "4", rx: "2", key: "18n3k1"}]
      ];
      Keyboard = createLucideIcon("keyboard", __iconNode22);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/layout-grid.js
  var __iconNode23, LayoutGrid;
  var init_layout_grid = __esm({
    "node_modules/lucide-react/dist/esm/icons/layout-grid.js"() {
      init_createLucideIcon();
      __iconNode23 = [
        ["rect", {width: "7", height: "7", x: "3", y: "3", rx: "1", key: "1g98yp"}],
        ["rect", {width: "7", height: "7", x: "14", y: "3", rx: "1", key: "6d4xhi"}],
        ["rect", {width: "7", height: "7", x: "14", y: "14", rx: "1", key: "nxv5o0"}],
        ["rect", {width: "7", height: "7", x: "3", y: "14", rx: "1", key: "1bb6yr"}]
      ];
      LayoutGrid = createLucideIcon("layout-grid", __iconNode23);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/list-restart.js
  var __iconNode24, ListRestart;
  var init_list_restart = __esm({
    "node_modules/lucide-react/dist/esm/icons/list-restart.js"() {
      init_createLucideIcon();
      __iconNode24 = [
        ["path", {d: "M21 5H3", key: "1fi0y6"}],
        ["path", {d: "M7 12H3", key: "13ou7f"}],
        ["path", {d: "M7 19H3", key: "wbqt3n"}],
        [
          "path",
          {
            d: "M12 18a5 5 0 0 0 9-3 4.5 4.5 0 0 0-4.5-4.5c-1.33 0-2.54.54-3.41 1.41L11 14",
            key: "qth677"
          }
        ],
        ["path", {d: "M11 10v4h4", key: "172dkj"}]
      ];
      ListRestart = createLucideIcon("list-restart", __iconNode24);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/maximize.js
  var __iconNode25, Maximize;
  var init_maximize = __esm({
    "node_modules/lucide-react/dist/esm/icons/maximize.js"() {
      init_createLucideIcon();
      __iconNode25 = [
        ["path", {d: "M8 3H5a2 2 0 0 0-2 2v3", key: "1dcmit"}],
        ["path", {d: "M21 8V5a2 2 0 0 0-2-2h-3", key: "1e4gt3"}],
        ["path", {d: "M3 16v3a2 2 0 0 0 2 2h3", key: "wsl5sc"}],
        ["path", {d: "M16 21h3a2 2 0 0 0 2-2v-3", key: "18trek"}]
      ];
      Maximize = createLucideIcon("maximize", __iconNode25);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/minus.js
  var __iconNode26, Minus;
  var init_minus = __esm({
    "node_modules/lucide-react/dist/esm/icons/minus.js"() {
      init_createLucideIcon();
      __iconNode26 = [["path", {d: "M5 12h14", key: "1ays0h"}]];
      Minus = createLucideIcon("minus", __iconNode26);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/package-open.js
  var __iconNode27, PackageOpen;
  var init_package_open = __esm({
    "node_modules/lucide-react/dist/esm/icons/package-open.js"() {
      init_createLucideIcon();
      __iconNode27 = [
        ["path", {d: "M12 22v-9", key: "x3hkom"}],
        [
          "path",
          {
            d: "M15.17 2.21a1.67 1.67 0 0 1 1.63 0L21 4.57a1.93 1.93 0 0 1 0 3.36L8.82 14.79a1.655 1.655 0 0 1-1.64 0L3 12.43a1.93 1.93 0 0 1 0-3.36z",
            key: "2ntwy6"
          }
        ],
        [
          "path",
          {
            d: "M20 13v3.87a2.06 2.06 0 0 1-1.11 1.83l-6 3.08a1.93 1.93 0 0 1-1.78 0l-6-3.08A2.06 2.06 0 0 1 4 16.87V13",
            key: "1pmm1c"
          }
        ],
        [
          "path",
          {
            d: "M21 12.43a1.93 1.93 0 0 0 0-3.36L8.83 2.2a1.64 1.64 0 0 0-1.63 0L3 4.57a1.93 1.93 0 0 0 0 3.36l12.18 6.86a1.636 1.636 0 0 0 1.63 0z",
            key: "12ttoo"
          }
        ]
      ];
      PackageOpen = createLucideIcon("package-open", __iconNode27);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/palette.js
  var __iconNode28, Palette;
  var init_palette = __esm({
    "node_modules/lucide-react/dist/esm/icons/palette.js"() {
      init_createLucideIcon();
      __iconNode28 = [
        [
          "path",
          {
            d: "M12 22a1 1 0 0 1 0-20 10 9 0 0 1 10 9 5 5 0 0 1-5 5h-2.25a1.75 1.75 0 0 0-1.4 2.8l.3.4a1.75 1.75 0 0 1-1.4 2.8z",
            key: "e79jfc"
          }
        ],
        ["circle", {cx: "13.5", cy: "6.5", r: ".5", fill: "currentColor", key: "1okk4w"}],
        ["circle", {cx: "17.5", cy: "10.5", r: ".5", fill: "currentColor", key: "f64h9f"}],
        ["circle", {cx: "6.5", cy: "12.5", r: ".5", fill: "currentColor", key: "qy21gx"}],
        ["circle", {cx: "8.5", cy: "7.5", r: ".5", fill: "currentColor", key: "fotxhn"}]
      ];
      Palette = createLucideIcon("palette", __iconNode28);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/pencil.js
  var __iconNode29, Pencil;
  var init_pencil = __esm({
    "node_modules/lucide-react/dist/esm/icons/pencil.js"() {
      init_createLucideIcon();
      __iconNode29 = [
        [
          "path",
          {
            d: "M21.174 6.812a1 1 0 0 0-3.986-3.987L3.842 16.174a2 2 0 0 0-.5.83l-1.321 4.352a.5.5 0 0 0 .623.622l4.353-1.32a2 2 0 0 0 .83-.497z",
            key: "1a8usu"
          }
        ],
        ["path", {d: "m15 5 4 4", key: "1mk7zo"}]
      ];
      Pencil = createLucideIcon("pencil", __iconNode29);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/pipette.js
  var __iconNode30, Pipette;
  var init_pipette = __esm({
    "node_modules/lucide-react/dist/esm/icons/pipette.js"() {
      init_createLucideIcon();
      __iconNode30 = [
        [
          "path",
          {
            d: "m12 9-8.414 8.414A2 2 0 0 0 3 18.828v1.344a2 2 0 0 1-.586 1.414A2 2 0 0 1 3.828 21h1.344a2 2 0 0 0 1.414-.586L15 12",
            key: "1y3wsu"
          }
        ],
        [
          "path",
          {
            d: "m18 9 .4.4a1 1 0 1 1-3 3l-3.8-3.8a1 1 0 1 1 3-3l.4.4 3.4-3.4a1 1 0 1 1 3 3z",
            key: "110lr1"
          }
        ],
        ["path", {d: "m2 22 .414-.414", key: "jhxm08"}]
      ];
      Pipette = createLucideIcon("pipette", __iconNode30);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/plug.js
  var __iconNode31, Plug;
  var init_plug = __esm({
    "node_modules/lucide-react/dist/esm/icons/plug.js"() {
      init_createLucideIcon();
      __iconNode31 = [
        ["path", {d: "M12 22v-5", key: "1ega77"}],
        ["path", {d: "M15 8V2", key: "18g5xt"}],
        [
          "path",
          {d: "M17 8a1 1 0 0 1 1 1v4a4 4 0 0 1-4 4h-4a4 4 0 0 1-4-4V9a1 1 0 0 1 1-1z", key: "1xoxul"}
        ],
        ["path", {d: "M9 8V2", key: "14iosj"}]
      ];
      Plug = createLucideIcon("plug", __iconNode31);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/plus.js
  var __iconNode32, Plus;
  var init_plus = __esm({
    "node_modules/lucide-react/dist/esm/icons/plus.js"() {
      init_createLucideIcon();
      __iconNode32 = [
        ["path", {d: "M5 12h14", key: "1ays0h"}],
        ["path", {d: "M12 5v14", key: "s699le"}]
      ];
      Plus = createLucideIcon("plus", __iconNode32);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/refresh-ccw.js
  var __iconNode33, RefreshCcw;
  var init_refresh_ccw = __esm({
    "node_modules/lucide-react/dist/esm/icons/refresh-ccw.js"() {
      init_createLucideIcon();
      __iconNode33 = [
        ["path", {d: "M21 12a9 9 0 0 0-9-9 9.75 9.75 0 0 0-6.74 2.74L3 8", key: "14sxne"}],
        ["path", {d: "M3 3v5h5", key: "1xhq8a"}],
        ["path", {d: "M3 12a9 9 0 0 0 9 9 9.75 9.75 0 0 0 6.74-2.74L21 16", key: "1hlbsb"}],
        ["path", {d: "M16 16h5v5", key: "ccwih5"}]
      ];
      RefreshCcw = createLucideIcon("refresh-ccw", __iconNode33);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/refresh-cw.js
  var __iconNode34, RefreshCw;
  var init_refresh_cw = __esm({
    "node_modules/lucide-react/dist/esm/icons/refresh-cw.js"() {
      init_createLucideIcon();
      __iconNode34 = [
        ["path", {d: "M3 12a9 9 0 0 1 9-9 9.75 9.75 0 0 1 6.74 2.74L21 8", key: "v9h5vc"}],
        ["path", {d: "M21 3v5h-5", key: "1q7to0"}],
        ["path", {d: "M21 12a9 9 0 0 1-9 9 9.75 9.75 0 0 1-6.74-2.74L3 16", key: "3uifl3"}],
        ["path", {d: "M8 16H3v5", key: "1cv678"}]
      ];
      RefreshCw = createLucideIcon("refresh-cw", __iconNode34);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/rotate-cw.js
  var __iconNode35, RotateCw;
  var init_rotate_cw = __esm({
    "node_modules/lucide-react/dist/esm/icons/rotate-cw.js"() {
      init_createLucideIcon();
      __iconNode35 = [
        ["path", {d: "M21 12a9 9 0 1 1-9-9c2.52 0 4.93 1 6.74 2.74L21 8", key: "1p45f6"}],
        ["path", {d: "M21 3v5h-5", key: "1q7to0"}]
      ];
      RotateCw = createLucideIcon("rotate-cw", __iconNode35);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/save.js
  var __iconNode36, Save;
  var init_save = __esm({
    "node_modules/lucide-react/dist/esm/icons/save.js"() {
      init_createLucideIcon();
      __iconNode36 = [
        [
          "path",
          {
            d: "M15.2 3a2 2 0 0 1 1.4.6l3.8 3.8a2 2 0 0 1 .6 1.4V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z",
            key: "1c8476"
          }
        ],
        ["path", {d: "M17 21v-7a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v7", key: "1ydtos"}],
        ["path", {d: "M7 3v4a1 1 0 0 0 1 1h7", key: "t51u73"}]
      ];
      Save = createLucideIcon("save", __iconNode36);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/search.js
  var __iconNode37, Search;
  var init_search = __esm({
    "node_modules/lucide-react/dist/esm/icons/search.js"() {
      init_createLucideIcon();
      __iconNode37 = [
        ["path", {d: "m21 21-4.34-4.34", key: "14j7rj"}],
        ["circle", {cx: "11", cy: "11", r: "8", key: "4ej97u"}]
      ];
      Search = createLucideIcon("search", __iconNode37);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/settings-2.js
  var __iconNode38, Settings2;
  var init_settings_2 = __esm({
    "node_modules/lucide-react/dist/esm/icons/settings-2.js"() {
      init_createLucideIcon();
      __iconNode38 = [
        ["path", {d: "M14 17H5", key: "gfn3mx"}],
        ["path", {d: "M19 7h-9", key: "6i9tg"}],
        ["circle", {cx: "17", cy: "17", r: "3", key: "18b49y"}],
        ["circle", {cx: "7", cy: "7", r: "3", key: "dfmy0x"}]
      ];
      Settings2 = createLucideIcon("settings-2", __iconNode38);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/settings.js
  var __iconNode39, Settings;
  var init_settings2 = __esm({
    "node_modules/lucide-react/dist/esm/icons/settings.js"() {
      init_createLucideIcon();
      __iconNode39 = [
        [
          "path",
          {
            d: "M9.671 4.136a2.34 2.34 0 0 1 4.659 0 2.34 2.34 0 0 0 3.319 1.915 2.34 2.34 0 0 1 2.33 4.033 2.34 2.34 0 0 0 0 3.831 2.34 2.34 0 0 1-2.33 4.033 2.34 2.34 0 0 0-3.319 1.915 2.34 2.34 0 0 1-4.659 0 2.34 2.34 0 0 0-3.32-1.915 2.34 2.34 0 0 1-2.33-4.033 2.34 2.34 0 0 0 0-3.831A2.34 2.34 0 0 1 6.35 6.051a2.34 2.34 0 0 0 3.319-1.915",
            key: "1i5ecw"
          }
        ],
        ["circle", {cx: "12", cy: "12", r: "3", key: "1v7zrd"}]
      ];
      Settings = createLucideIcon("settings", __iconNode39);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/shield-alert.js
  var __iconNode40, ShieldAlert;
  var init_shield_alert = __esm({
    "node_modules/lucide-react/dist/esm/icons/shield-alert.js"() {
      init_createLucideIcon();
      __iconNode40 = [
        [
          "path",
          {
            d: "M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z",
            key: "oel41y"
          }
        ],
        ["path", {d: "M12 8v4", key: "1got3b"}],
        ["path", {d: "M12 16h.01", key: "1drbdi"}]
      ];
      ShieldAlert = createLucideIcon("shield-alert", __iconNode40);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/store.js
  var __iconNode41, Store2;
  var init_store = __esm({
    "node_modules/lucide-react/dist/esm/icons/store.js"() {
      init_createLucideIcon();
      __iconNode41 = [
        ["path", {d: "M15 21v-5a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v5", key: "slp6dd"}],
        [
          "path",
          {
            d: "M17.774 10.31a1.12 1.12 0 0 0-1.549 0 2.5 2.5 0 0 1-3.451 0 1.12 1.12 0 0 0-1.548 0 2.5 2.5 0 0 1-3.452 0 1.12 1.12 0 0 0-1.549 0 2.5 2.5 0 0 1-3.77-3.248l2.889-4.184A2 2 0 0 1 7 2h10a2 2 0 0 1 1.653.873l2.895 4.192a2.5 2.5 0 0 1-3.774 3.244",
            key: "o0xfot"
          }
        ],
        ["path", {d: "M4 10.95V19a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8.05", key: "wn3emo"}]
      ];
      Store2 = createLucideIcon("store", __iconNode41);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/stretch-horizontal.js
  var __iconNode42, StretchHorizontal;
  var init_stretch_horizontal = __esm({
    "node_modules/lucide-react/dist/esm/icons/stretch-horizontal.js"() {
      init_createLucideIcon();
      __iconNode42 = [
        ["rect", {width: "20", height: "6", x: "2", y: "4", rx: "2", key: "qdearl"}],
        ["rect", {width: "20", height: "6", x: "2", y: "14", rx: "2", key: "1xrn6j"}]
      ];
      StretchHorizontal = createLucideIcon("stretch-horizontal", __iconNode42);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/tag.js
  var __iconNode43, Tag;
  var init_tag = __esm({
    "node_modules/lucide-react/dist/esm/icons/tag.js"() {
      init_createLucideIcon();
      __iconNode43 = [
        [
          "path",
          {
            d: "M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z",
            key: "vktsd0"
          }
        ],
        ["circle", {cx: "7.5", cy: "7.5", r: ".5", fill: "currentColor", key: "kqv944"}]
      ];
      Tag = createLucideIcon("tag", __iconNode43);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/trash-2.js
  var __iconNode44, Trash2;
  var init_trash_2 = __esm({
    "node_modules/lucide-react/dist/esm/icons/trash-2.js"() {
      init_createLucideIcon();
      __iconNode44 = [
        ["path", {d: "M10 11v6", key: "nco0om"}],
        ["path", {d: "M14 11v6", key: "outv1u"}],
        ["path", {d: "M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6", key: "miytrc"}],
        ["path", {d: "M3 6h18", key: "d0wm0j"}],
        ["path", {d: "M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2", key: "e791ji"}]
      ];
      Trash2 = createLucideIcon("trash-2", __iconNode44);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/triangle-alert.js
  var __iconNode45, TriangleAlert;
  var init_triangle_alert = __esm({
    "node_modules/lucide-react/dist/esm/icons/triangle-alert.js"() {
      init_createLucideIcon();
      __iconNode45 = [
        [
          "path",
          {
            d: "m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3",
            key: "wmoenq"
          }
        ],
        ["path", {d: "M12 9v4", key: "juzpu7"}],
        ["path", {d: "M12 17h.01", key: "p32p05"}]
      ];
      TriangleAlert = createLucideIcon("triangle-alert", __iconNode45);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/twitter.js
  var __iconNode46, Twitter;
  var init_twitter = __esm({
    "node_modules/lucide-react/dist/esm/icons/twitter.js"() {
      init_createLucideIcon();
      __iconNode46 = [
        [
          "path",
          {
            d: "M22 4s-.7 2.1-2 3.4c1.6 10-9.4 17.3-18 11.6 2.2.1 4.4-.6 6-2C3 15.5.5 9.6 3 5c2.2 2.6 5.6 4.1 9 4-.9-4.2 4-6.6 7-3.8 1.1 0 3-1.2 3-1.2z",
            key: "pff0z6"
          }
        ]
      ];
      Twitter = createLucideIcon("twitter", __iconNode46);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/user.js
  var __iconNode47, User;
  var init_user = __esm({
    "node_modules/lucide-react/dist/esm/icons/user.js"() {
      init_createLucideIcon();
      __iconNode47 = [
        ["path", {d: "M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2", key: "975kel"}],
        ["circle", {cx: "12", cy: "7", r: "4", key: "17ys0d"}]
      ];
      User = createLucideIcon("user", __iconNode47);
    }
  });

  // node_modules/lucide-react/dist/esm/icons/x.js
  var __iconNode48, X;
  var init_x = __esm({
    "node_modules/lucide-react/dist/esm/icons/x.js"() {
      init_createLucideIcon();
      __iconNode48 = [
        ["path", {d: "M18 6 6 18", key: "1bl5f8"}],
        ["path", {d: "m6 6 12 12", key: "d8bk6v"}]
      ];
      X = createLucideIcon("x", __iconNode48);
    }
  });

  // node_modules/lucide-react/dist/esm/lucide-react.js
  var init_lucide_react = __esm({
    "node_modules/lucide-react/dist/esm/lucide-react.js"() {
      init_badge_check();
      init_braces();
      init_circle_alert();
      init_circle_check();
      init_circle_question_mark();
      init_circle_x();
      init_triangle_alert();
      init_arrow_down_to_line();
      init_briefcase();
      init_check();
      init_chevron_down();
      init_chevron_right();
      init_circle_dollar_sign();
      init_clock();
      init_external_link();
      init_eye();
      init_folder();
      init_github();
      init_globe();
      init_heart_handshake();
      init_history();
      init_info();
      init_keyboard();
      init_layout_grid();
      init_list_restart();
      init_maximize();
      init_minus();
      init_package_open();
      init_palette();
      init_pencil();
      init_pipette();
      init_plug();
      init_plus();
      init_refresh_ccw();
      init_refresh_cw();
      init_rotate_cw();
      init_save();
      init_search();
      init_settings_2();
      init_settings2();
      init_shield_alert();
      init_store();
      init_stretch_horizontal();
      init_tag();
      init_trash_2();
      init_twitter();
      init_user();
      init_x();
      init_Icon();
    }
  });

  // src/InAccord/stores/settings.ts
  var settings_default2;
  var init_settings3 = __esm({
    "src/InAccord/stores/settings.ts"() {
      "use strict";
      init_logger();
      init_settings();
      init_json();
      init_emitter();
      init_discordmodules();
      init_i18n();
      init_base();
      init_lucide_react();
      settings_default2 = new class SettingsManager extends Store {
        state = {};
        collections = [];
        panels = [];
        initialize() {
          this.registerCollection("settings", "Settings", settings_default);
        }
        registerCollection(id, name, settings) {
          if (this.collections.find((c) => c.id == id)) return Logger.error("Settings", "Already have a collection with id " + id);
          this.collections.push({
            type: "collection",
            id,
            name,
            settings
          });
          this.setupCollection(id);
          this.loadCollection(id);
        }
        removeCollection(id) {
          const location2 = this.collections.findIndex((c) => c.id == id);
          if (location2 < 0) return Logger.error("Settings", "No collection with id " + id);
          this.collections.splice(location2, 1);
        }
        registerPanel(id, name, options) {
          if (this.panels.find((p) => p.id == id)) return Logger.error("Settings", "Already have a panel with id " + id);
          const {element, onClick, order = 1, type = "settings"} = options;
          const section = {
            id,
            type,
            order,
            get label() {
              return t(`Panels.${id}`) || name;
            },
            section: id,
            icon: options.icon,
            discordIcon: options.discordIcon,
            searchable: options.searchable
          };
          if (options.manager) section.manager = options.manager;
          if (onClick) section.clickListener = onClick;
          if (element) section.element = element instanceof discordmodules_default.React.Component ? () => discordmodules_default.React.createElement(element, {}) : typeof element == "function" ? element : () => element;
          this.panels.push(section);
        }
        registerAddonPanel(manager) {
          const plural = manager.prefix + "s";
          const title = t(`Panels.${plural}`);
          this.registerPanel(plural, title, {
            order: manager.order,
            type: "addon",
            manager,
            icon: manager.prefix === "plugin" ? Plug : Palette,
            searchable: () => manager.addonList.flatMap((addon) => [addon.name, addon.filename])
          });
        }
        removePanel(id) {
          const location2 = this.panels.findIndex((c) => c.id == id);
          if (location2 < 0) return Logger.error("Settings", "No collection with id " + id);
          this.panels.splice(location2, 1);
        }
        getPath(path2, collectionId = "", categoryId = "") {
          const collection = path2.length == 3 ? path2[0] : collectionId;
          const category = path2.length == 3 ? path2[1] : path2.length == 2 ? path2[0] : categoryId;
          const setting = path2[path2.length - 1];
          return {collection, category, setting};
        }
        setupCollection(id) {
          const collection = this.collections.find((c) => c.id == id);
          if (!collection) return;
          if (!this.state[collection.id]) this.state[collection.id] = {};
          const collectionName = collection.name;
          Object.defineProperty(collection, "name", {
            enumerable: true,
            get: () => t(`Collections.${collection.id}.name`) || collectionName
          });
          const categories = collection.settings;
          for (let cc = 0; cc < categories.length; cc++) {
            const category = categories[cc];
            if (!this.state[collection.id].hasOwnProperty(category.id)) this.state[collection.id][category.id] = {};
            const categoryName = category.name;
            Object.defineProperty(category, "name", {
              enumerable: true,
              get: () => t(`Collections.${collection.id}.${category.id}.name`) || categoryName
            });
            for (let s = 0; s < category.settings.length; s++) {
              const setting = category.settings[s];
              if (!this.state[collection.id][category.id].hasOwnProperty(setting.id)) this.state[collection.id][category.id][setting.id] = setting.value;
              setting.defaultValue = setting.value;
              const settingName = setting.name;
              const settingNote = setting.note;
              Object.defineProperties(setting, {
                name: {
                  enumerable: true,
                  get: () => t(`Collections.${collection.id}.${category.id}.${setting.id}.name`) || settingName
                },
                note: {
                  enumerable: true,
                  get: () => t(`Collections.${collection.id}.${category.id}.${setting.id}.note`) || settingNote
                }
              });
              if (setting.options) {
                for (const opt of setting.options) {
                  const optLabel = opt.label;
                  Object.defineProperty(opt, "label", {
                    enumerable: true,
                    get: () => {
                      return t(`Collections.${collection.id}.${category.id}.${setting.id}.options.${opt.id ?? opt.value}`) || optLabel;
                    }
                  });
                }
              }
              if (setting.hasOwnProperty("disabled")) continue;
              if (!setting.enableWith && !setting.disableWith) continue;
              const pathString = setting.enableWith ?? setting.disableWith;
              const path2 = this.getPath(pathString.split("."), collection.id, category.id);
              Object.defineProperty(setting, "disabled", {
                enumerable: true,
                get: () => {
                  const other = this.state[path2.collection][path2.category][path2.setting];
                  return setting.enableWith ? !other : other;
                }
              });
            }
          }
        }
        saveSettings() {
          for (const collection in this.state) this.saveCollection(collection);
        }
        loadSettings() {
          for (const collection in this.state) this.loadCollection(collection);
        }
        saveCollection(collection) {
          json_default.set(collection, this.state[collection]);
        }
        loadCollection(id) {
          const previousState = json_default.get(id);
          if (!previousState) return this.saveCollection(id);
          for (const category in this.state[id]) {
            if (!previousState[category]) Object.assign(previousState, {[category]: this.state[id][category]});
            for (const setting in this.state[id][category]) {
              if (previousState[category][setting] == void 0) continue;
              const settingObj = this.getSetting(id, category, setting);
              switch (settingObj?.type) {
                case "radio":
                case "dropdown": {
                  const exists2 = settingObj.options.some((o) => o.value == previousState[category][setting]);
                  if (exists2) this.state[id][category][setting] = previousState[category][setting];
                  break;
                }
                default: {
                  this.state[id][category][setting] = previousState[category][setting];
                }
              }
            }
          }
          this.saveCollection(id);
        }
        resetCollection(id) {
          const collection = this.collections.find((c) => c.id == id);
          if (!collection) return;
          const categories = collection.settings;
          for (let cc = 0; cc < categories.length; cc++) {
            const category = categories[cc];
            for (let s = 0; s < category.settings.length; s++) {
              const setting = category.settings[s];
              if (this.get(collection.id, category.id, setting.id) == setting.defaultValue) continue;
              this.set(collection.id, category.id, setting.id, setting.defaultValue);
            }
          }
        }
        onSettingChange(collection, category, id, value) {
          this.state[collection][category][id] = value;
          emitter_default.dispatch("setting-updated", collection, category, id, value);
          this.emitChange();
          this.saveCollection(collection);
        }
        getSetting(collection, category, id) {
          if (arguments.length == 2) {
            id = category;
            category = collection;
            collection = "settings";
          }
          return this.collections.find((c) => c.id == collection)?.settings.find((c) => c.id == category)?.settings.find((s) => s.id == id);
        }
        get(collection, category, id) {
          if (arguments.length == 2) {
            id = category;
            category = collection;
            collection = "settings";
          }
          if (!this.state[collection] || !this.state[collection][category]) return false;
          return this.state[collection][category][id];
        }
        set(collection, category, id, value) {
          if (arguments.length == 3) {
            value = id;
            id = category;
            category = collection;
            collection = "settings";
          }
          return this.onSettingChange(collection, category, id, value);
        }
        on(collection, category, identifier, callback) {
          const handler = (col, cat, id, value) => {
            if (col !== collection || cat !== category || id !== identifier) return;
            callback(value);
          };
          emitter_default.on("setting-updated", handler);
          return () => {
            emitter_default.off("setting-updated", handler);
          };
        }
      }();
    }
  });

  // src/InAccord/modules/patcher.ts
  var Patcher;
  var init_patcher = __esm({
    "src/InAccord/modules/patcher.ts"() {
      "use strict";
      init_logger();
      init_discordmodules();
      init_webpack2();
      Patcher = class {
        static _patches = [];
        static get patches() {
          return this._patches || (this._patches = []);
        }
        /**
         * Returns all the patches done by a specific caller
         * @param {string} name - Name of the patch caller
         * @method
         */
        static getPatchesByCaller(name) {
          if (!name) return [];
          const patches = [];
          for (const patch of this.patches) {
            for (const childPatch of patch.children) {
              if (childPatch.caller === name) patches.push(childPatch);
            }
          }
          return patches;
        }
        /**
         * Unpatches all patches passed, or when a string is passed unpatches all
         * patches done by that specific caller.
         * @param {Array|string} patches - Either an array of patches to unpatch or a caller name
         */
        static unpatchAll(patches) {
          if (typeof patches === "string") patches = this.getPatchesByCaller(patches);
          for (const patch of patches) {
            patch.unpatch();
          }
        }
        static resolveModule(module) {
          if (!module || typeof module === "function" || typeof module === "object" && !Array.isArray(module)) return module;
          if (typeof module === "string") return discordmodules_default[module];
          if (Array.isArray(module)) return getByKeys(module);
          return null;
        }
        static makeOverride(patch) {
          return function iaPatcher(...args) {
            let returnValue;
            if (!patch.children || !patch.children.length) return patch.originalFunction.apply(this, args);
            for (const superPatch of patch.children.filter((c) => c.type === "before")) {
              try {
                superPatch.callback(this, args);
              } catch (err) {
                Logger.err("Patcher", `Could not fire before callback of ${patch.functionName} for ${superPatch.caller}`, err);
              }
            }
            const insteads = patch.children.filter((c) => c.type === "instead");
            if (!insteads.length) {
              returnValue = patch.originalFunction.apply(this, args);
            } else {
              for (const insteadPatch of insteads) {
                try {
                  const tempReturn = insteadPatch.callback(this, args, patch.originalFunction.bind(this));
                  if (typeof tempReturn !== "undefined") returnValue = tempReturn;
                } catch (err) {
                  Logger.err("Patcher", `Could not fire instead callback of ${patch.functionName} for ${insteadPatch.caller}`, err);
                }
              }
            }
            for (const slavePatch of patch.children.filter((c) => c.type === "after")) {
              try {
                const tempReturn = slavePatch.callback(this, args, returnValue);
                if (typeof tempReturn !== "undefined") returnValue = tempReturn;
              } catch (err) {
                Logger.err("Patcher", `Could not fire after callback of ${patch.functionName} for ${slavePatch.caller}`, err);
              }
            }
            return returnValue;
          };
        }
        static rePatch(patch) {
          const override = this.makeOverride(patch);
          patch.proxyFunction = override;
          patch.module[patch.functionName] = override;
        }
        static makePatch(module, functionName, name) {
          const patch = {
            name,
            module,
            functionName,
            originalFunction: module[functionName],
            proxyFunction: void 0,
            revert: () => {
              patch.module[patch.functionName] = patch.originalFunction;
              patch.proxyFunction = void 0;
              patch.children = [];
            },
            counter: 0,
            children: []
          };
          patch.proxyFunction = module[functionName] = this.makeOverride(patch);
          Object.assign(module[functionName], patch.originalFunction);
          module[functionName].__originalFunction = patch.originalFunction;
          module[functionName].toString = () => patch.originalFunction.toString();
          this.patches.push(patch);
          return patch;
        }
        /**
         * Function with no arguments and no return value that may be called to revert changes made by {@link module:Patcher}, restoring (unpatching) original method.
         * @callback module:Patcher~unpatch
         */
        /**
         * A callback that modifies method logic. This callback is called on each call of the original method and is provided all data about original call. Any of the data can be modified if necessary, but do so wisely.
         *
         * The third argument for the callback will be `undefined` for `before` patches. `originalFunction` for `instead` patches and `returnValue` for `after` patches.
         *
         * @callback module:Patcher~patchCallback
         * @param {object} thisObject - `this` in the context of the original function.
         * @param {arguments} args - The original arguments of the original function.
         * @param {(function|*)} extraValue - For `instead` patches, this is the original function from the module. For `after` patches, this is the return value of the function.
         * @return {*} Makes sense only when using an `instead` or `after` patch. If something other than `undefined` is returned, the returned value replaces the value of `returnValue`. If used for `before` the return value is ignored.
         */
        /**
         * This method patches onto another function, allowing your code to run beforehand.
         * Using this, you are also able to modify the incoming arguments before the original method is run.
         *
         * @param {string} caller - Name of the caller of the patch function. Using this you can undo all patches with the same name using {@link module:Patcher.unpatchAll}. Use `""` if you don't care.
         * @param {object} moduleToPatch - Object with the function to be patched. Can also patch an object's prototype.
         * @param {string} functionName - Name of the method to be patched
         * @param {module:Patcher~patchCallback} callback - Function to run before the original method
         * @param {object} options - Object used to pass additional options.
         * @param {string} [options.displayName] You can provide meaningful name for class/object provided in `what` param for logging purposes. By default, this function will try to determine name automatically.
         * @param {boolean} [options.forcePatch=true] Set to `true` to patch even if the function doesnt exist. (Adds noop function in place).
         * @return {module:Patcher~unpatch} Function with no arguments and no return value that should be called to cancel (unpatch) this patch. You should save and run it when your plugin is stopped.
         */
        static before(caller, moduleToPatch, functionName, callback, options = {}) {
          return this.pushChildPatch(caller, moduleToPatch, functionName, callback, Object.assign(options, {type: "before"}));
        }
        /**
         * This method patches onto another function, allowing your code to run after.
         * Using this, you are also able to modify the return value, using the return of your code instead.
         *
         * @param {string} caller - Name of the caller of the patch function. Using this you can undo all patches with the same name using {@link module:Patcher.unpatchAll}. Use `""` if you don't care.
         * @param {object} moduleToPatch - Object with the function to be patched. Can also patch an object's prototype.
         * @param {string} functionName - Name of the method to be patched
         * @param {module:Patcher~patchCallback} callback - Function to run instead of the original method
         * @param {object} options - Object used to pass additional options.
         * @param {string} [options.displayName] You can provide meaningful name for class/object provided in `what` param for logging purposes. By default, this function will try to determine name automatically.
         * @param {boolean} [options.forcePatch=true] Set to `true` to patch even if the function doesnt exist. (Adds noop function in place).
         * @return {module:Patcher~unpatch} Function with no arguments and no return value that should be called to cancel (unpatch) this patch. You should save and run it when your plugin is stopped.
         */
        static after(caller, moduleToPatch, functionName, callback, options = {}) {
          return this.pushChildPatch(caller, moduleToPatch, functionName, callback, Object.assign(options, {type: "after"}));
        }
        /**
         * This method patches onto another function, allowing your code to run instead.
         * Using this, you are also able to modify the return value, using the return of your code instead.
         *
         * @param {string} caller - Name of the caller of the patch function. Using this you can undo all patches with the same name using {@link module:Patcher.unpatchAll}. Use `""` if you don't care.
         * @param {object} moduleToPatch - Object with the function to be patched. Can also patch an object's prototype.
         * @param {string} functionName - Name of the method to be patched
         * @param {module:Patcher~patchCallback} callback - Function to run after the original method
         * @param {object} options - Object used to pass additional options.
         * @param {string} [options.displayName] You can provide meaningful name for class/object provided in `what` param for logging purposes. By default, this function will try to determine name automatically.
         * @param {boolean} [options.forcePatch=true] Set to `true` to patch even if the function doesnt exist. (Adds noop function in place).
         * @return {module:Patcher~unpatch} Function with no arguments and no return value that should be called to cancel (unpatch) this patch. You should save and run it when your plugin is stopped.
         */
        static instead(caller, moduleToPatch, functionName, callback, options = {}) {
          return this.pushChildPatch(caller, moduleToPatch, functionName, callback, Object.assign(options, {type: "instead"}));
        }
        /**
         * This method patches onto another function, allowing your code to run before, instead or after the original function.
         * Using this you are able to modify the incoming arguments before the original function is run as well as the return
         * value before the original function actually returns.
         *
         * @param {string} caller - Name of the caller of the patch function. Using this you can undo all patches with the same name using {@link module:Patcher.unpatchAll}. Use `""` if you don't care.
         * @param {object} moduleToPatch - Object with the function to be patched. Can also patch an object's prototype.
         * @param {string} functionName - Name of the method to be patched
         * @param {module:Patcher~patchCallback} callback - Function to run after the original method
         * @param {object} options - Object used to pass additional options.
         * @param {string} [options.type=after] - Determines whether to run the function `before`, `instead`, or `after` the original.
         * @param {string} [options.displayName] You can provide meaningful name for class/object provided in `what` param for logging purposes. By default, this function will try to determine name automatically.
         * @param {boolean} [options.forcePatch=true] Set to `true` to patch even if the function doesnt exist. (Adds noop function in place).
         * @return {module:Patcher~unpatch} Function with no arguments and no return value that should be called to cancel (unpatch) this patch. You should save and run it when your plugin is stopped.
         */
        static pushChildPatch(caller, moduleToPatch, functionName, callback, options = {}) {
          const {type = "after", forcePatch = true} = options;
          const module = this.resolveModule(moduleToPatch);
          if (!module) return null;
          if (!module[functionName] && forcePatch) module[functionName] = (function () {
          });
          if (!(module[functionName] instanceof Function)) return null;
          if (typeof moduleToPatch === "string") options.displayName = moduleToPatch;
          const displayName = options.displayName || module.displayName || module.name || module.constructor.displayName || module.constructor.name;
          const patchId = `${displayName}.${functionName}`;
          const patch = this.patches.find((p) => p.module == module && p.functionName == functionName) || this.makePatch(module, functionName, patchId);
          if (!patch.proxyFunction) this.rePatch(patch);
          const child = {
            caller,
            type,
            id: patch.counter,
            callback,
            unpatch: () => {
              patch.children.splice(patch.children.findIndex((cpatch) => cpatch.id === child.id && cpatch.type === type), 1);
              if (patch.children.length <= 0) {
                const patchNum = this.patches.findIndex((p) => p.module == module && p.functionName == functionName);
                if (patchNum < 0) return;
                this.patches[patchNum].revert();
                this.patches.splice(patchNum, 1);
              }
            }
          };
          patch.children.push(child);
          patch.counter++;
          return child.unpatch;
        }
      };
    }
  });

  // src/InAccord/stores/toasts.ts
  var toasts_default;
  var init_toasts = __esm({
    "src/InAccord/stores/toasts.ts"() {
      "use strict";
      init_base();
      init_settings3();
      init_logger();
      toasts_default = new class Toasts extends Store {
        _toasts = [];
        toastKey = 0;
        get shouldShowToasts() {
          return settings_default2.get("settings", "general", "showToasts");
        }
        addToast(toast) {
          this._toasts = [...this._toasts, toast];
          this.emitChange();
          setTimeout(() => {
            this.removeToast(toast.key);
          }, toast.timeout);
        }
        removeToast(key) {
          this._toasts = this._toasts.filter((toast) => toast.key !== key);
          this.emitChange();
        }
        get toasts() {
          return this._toasts;
        }
        /**
         * This shows a toast similar to android towards the bottom of the screen.
         *
         * @param {string} content The string to show in the toast.
         * @param {object} options Options object. Optional parameter.
         * @param {string} [options.type="default"] Changes the type of the toast stylistically and semantically. Choices: "default", "info", "success", "error", "warning". Default: "default"
         * @param {boolean} [options.icon=true] Determines whether the icon should show corresponding to the type. A toast without type will always have no icon. Default: true
         * @param {number} [options.timeout=3000] Adjusts the time (in ms) the toast should be shown for before disappearing automatically. Default: 3000
         * @param {boolean} [options.forceShow=false] Whether to force showing the toast and ignore the ia setting
         */
        show(content, options = {}) {
          try {
            const {type = "default", icon = true, timeout = 3e3, forceShow = false} = options;
            if (!this.shouldShowToasts && !forceShow) return;
            this.addToast({
              key: this.toastKey++,
              content,
              type,
              icon,
              timeout
            });
          } catch (err) {
            Logger.stacktrace("Toasts", "Unable to show toast", err);
          }
        }
        /** Shorthand for `type = "default"` for {@link module:Toasts.show} */
        default(content, options = {}) {
          return this.show(content, {...options, type: "default"});
        }
        /** Shorthand for `type = "info"` for {@link module:Toasts.show} */
        info(content, options = {}) {
          return this.show(content, {...options, type: "info"});
        }
        /** Shorthand for `type = "success"` for {@link module:Toasts.show} */
        success(content, options = {}) {
          return this.show(content, {...options, type: "success"});
        }
        /** Shorthand for `type = "warning"` for {@link module:Toasts.show} */
        warning(content, options = {}) {
          return this.show(content, {...options, type: "warning"});
        }
        /** Shorthand for `type = "error"` for {@link module:Toasts.show} */
        error(content, options = {}) {
          return this.show(content, {...options, type: "error"});
        }
      }();
    }
  });

  // src/InAccord/structs/addonerror.ts
  var AddonError;
  var init_addonerror = __esm({
    "src/InAccord/structs/addonerror.ts"() {
      "use strict";
      AddonError = class extends Error {
        file;
        error;
        type;
        constructor(name, filename, message, error, type) {
          super(message);
          this.name = name;
          this.file = filename;
          this.error = error;
          this.type = type;
        }
      };
    }
  });

  // src/common/constants/ipcevents.ts
  var MINIMIZE, MAXIMIZE, RELAUNCH, RUN_SCRIPT, NAVIGATE, OPEN_DEVTOOLS, CLOSE_DEVTOOLS, TOGGLE_DEVTOOLS, OPEN_WINDOW, INSPECT_ELEMENT, MINIMUM_SIZE, WINDOW_SIZE, DEVTOOLS_WARNING, OPEN_DIALOG, GET_ACCENT_COLOR, OPEN_PATH, LAUNCH_DISCORD, INJECTOR_RESTORE;
  var init_ipcevents = __esm({
    "src/common/constants/ipcevents.ts"() {
      "use strict";
      MINIMIZE = "ia-window-minimize";
      MAXIMIZE = "ia-window-maximize";
      RELAUNCH = "ia-relaunch-app";
      RUN_SCRIPT = "ia-run-script";
      NAVIGATE = "ia-did-navigate-in-page";
      OPEN_DEVTOOLS = "ia-open-devtools";
      CLOSE_DEVTOOLS = "ia-close-devtools";
      TOGGLE_DEVTOOLS = "ia-toggle-devtools";
      OPEN_WINDOW = "ia-open-window";
      INSPECT_ELEMENT = "ia-inspect-element";
      MINIMUM_SIZE = "ia-minimum-size";
      WINDOW_SIZE = "ia-window-size";
      DEVTOOLS_WARNING = "ia-remove-devtools-message";
      OPEN_DIALOG = "ia-open-dialog";
      GET_ACCENT_COLOR = "ia-get-accent-color";
      OPEN_PATH = "ia-open-path";
      LAUNCH_DISCORD = "ia-launch-discord";
      INJECTOR_RESTORE = "ia-injector-restore";
    }
  });

  // src/InAccord/modules/ipc.ts
  var ipc_default;
  var init_ipc = __esm({
    "src/InAccord/modules/ipc.ts"() {
      "use strict";
      init_electron();
      init_ipcevents();
      init_emitter();
      ipc_default = new class IPCRenderer {
        constructor() {
          ipcRenderer.on(NAVIGATE, () => emitter_default.dispatch("navigate"));
          ipcRenderer.on(MAXIMIZE, () => emitter_default.dispatch("maximize"));
          ipcRenderer.on(MINIMIZE, () => emitter_default.dispatch("minimize"));
        }
        openDevTools() {
          return ipcRenderer.send(OPEN_DEVTOOLS);
        }
        closeDevTools() {
          return ipcRenderer.send(CLOSE_DEVTOOLS);
        }
        toggleDevTools() {
          return ipcRenderer.send(TOGGLE_DEVTOOLS);
        }
        relaunch(args) {
          return ipcRenderer.send(RELAUNCH, args);
        }
        runScript(script) {
          return ipcRenderer.invoke(RUN_SCRIPT, script);
        }
        launchDiscord(channel = "stable", options = {}) {
          return ipcRenderer.invoke(LAUNCH_DISCORD, {channel, patchStable: !!options.patchStable});
        }
        restoreInjection(channel = "stable") {
          return ipcRenderer.invoke(INJECTOR_RESTORE, {channel});
        }
        openWindow(url, options) {
          return ipcRenderer.invoke(OPEN_WINDOW, url, options);
        }
        inspectElement() {
          return ipcRenderer.send(INSPECT_ELEMENT);
        }
        setMinimumSize(width, height) {
          return ipcRenderer.send(MINIMUM_SIZE, width, height);
        }
        setWindowSize(width, height) {
          return ipcRenderer.send(WINDOW_SIZE, width, height);
        }
        stopDevtoolsWarning() {
          return ipcRenderer.send(DEVTOOLS_WARNING);
        }
        // TODO: merge dialog options type with mian process
        openDialog(options) {
          return ipcRenderer.invoke(OPEN_DIALOG, options);
        }
        getSystemAccentColor() {
          return ipcRenderer.invoke(GET_ACCENT_COLOR);
        }
        openPath(path2) {
          return ipcRenderer.send(OPEN_PATH, path2);
        }
      }();
    }
  });

  // src/InAccord/modules/backupmanager.ts
  var backupmanager_exports = {};
  __export(backupmanager_exports, {
    default: () => backupmanager_default
  });
  var backupmanager_default;
  var init_backupmanager = __esm({
    "src/InAccord/modules/backupmanager.ts"() {
      "use strict";
      init_config();
      init_addonerror();
      init_addonmanager();
      backupmanager_default = new class BackupManager extends AddonManager {
        get name() {
          return "BackupManager";
        }
        get extension() {
          return ".js";
        }
        get duplicatePattern() {
          return /\.[jt]s\s?\([0-9]+\)$/;
        }
        get addonFolder() {
          return config_default.get("backupPath");
        }
        get prefix() {
          return "backup";
        }
        get language() {
          return "javascript";
        }
        get order() {
          return 2;
        }
        addonList = [];
        /* Aliases */
        updateBackupList() {
          return this.updateList();
        }
        loadAllBackups() {
          return this.loadAllAddons();
        }
        enableBackup(idOrAddon) {
          return this.enableAddon(idOrAddon);
        }
        disableBackup(idOrAddon) {
          return this.disableAddon(idOrAddon);
        }
        toggleBackup(id) {
          return this.toggleAddon(id);
        }
        unloadBackup(idOrFileOrAddon) {
          return this.unloadAddon(idOrFileOrAddon);
        }
        loadBackup(filename) {
          return this.loadAddon(filename);
        }
        reloadBackup(idOrFileOrAddon) {
          return this.reloadAddon(idOrFileOrAddon);
        }
        initializeAddon(addon) {
          if (!addon.name || !addon.author || !addon.description || !addon.version) {
            return new AddonError(addon.name || addon.filename, addon.filename, "Backup is missing required metadata", {message: "Backup must provide name, author, description, and version.", stack: ""}, this.prefix);
          }
        }
        // Backups are managed as files and are not executed like plugins/themes.
        startAddon(_idOrAddon) {
        }
        stopAddon(_idOrAddon) {
        }
      }();
    }
  });

  // src/InAccord/modules/dommanager.ts
  var DOMManager;
  var init_dommanager = __esm({
    "src/InAccord/modules/dommanager.ts"() {
      "use strict";
      init_logger();
      DOMManager = class {
        /** Document/window width */
        static get screenWidth() {
          return Math.max(document.documentElement.clientWidth, window.innerWidth || 0);
        }
        /** Document/window height */
        static get screenHeight() {
          return Math.max(document.documentElement.clientHeight, window.innerHeight || 0);
        }
        static get iaHead() {
          return this.getElement("ia-head");
        }
        static get iaBody() {
          return this.getElement("ia-body");
        }
        static get iaScripts() {
          return this.getElement("ia-scripts");
        }
        static get iaStyles() {
          return this.getElement("ia-styles");
        }
        static get iaThemes() {
          return this.getElement("ia-themes");
        }
        static get iaCustomCSS() {
          return this.getElement("style#customcss");
        }
        // static get iaTooltips() {return this.getElement("ia-tooltips") || this.createElement("ia-tooltips").appendTo(this.iaBody);}
        // static get iaModals() {return this.getElement("ia-modals") || this.createElement("ia-modals").appendTo(this.iaBody);}
        // static get iaToasts() {return this.getElement("ia-toasts") || this.createElement("ia-toasts").appendTo(this.iaBody);}
        static initialize() {
        }
        static escapeID(id) {
          return CSS.escape(id);
        }
        static getElement(e, baseElement = document.documentElement) {
          if (e instanceof Node) return e;
          return baseElement.querySelector(e);
        }
        /**
         * Utility function to make creating DOM elements easier.
         * Has backward compatibility with previous createElement implementation.
        */
        static createElement(type, options = {}, ...children) {
          const element = document.createElement(type);
          Object.assign(element, options);
          const flatChildren = Array.prototype.flat.call(children, Infinity).filter((c) => c !== null && c !== void 0);
          element.append(...flatChildren);
          if (options.target) {
            Logger.warn("DOM.createElement", `Usage of the "target" option has been deprecated and will be removed in the next version.`);
            (typeof options.target === "string" ? document.querySelector(options.target) : options.target)?.append(element);
          }
          return element;
        }
        /**
         * Parses a string of HTML and returns the results. If the second parameter is true,
         * the parsed HTML will be returned as a document fragment {@see https://developer.mozilla.org/en-US/docs/Web/API/DocumentFragment}.
         * This is extremely useful if you have a list of elements at the top level, they can then be appended all at once to another node.
         *
         * If the second parameter is false, then the return value will be the list of parsed
         * nodes and there were multiple top level nodes, otherwise the single node is returned.
         */
        static parseHTML(html, fragment = false) {
          const template = document.createElement("template");
          template.innerHTML = html.trim();
          const node = template.content.cloneNode(true);
          if (fragment) return node;
          return node.childNodes.length > 1 ? node.childNodes : node.childNodes[0];
        }
        static removeStyle(id) {
          id = this.escapeID(id);
          const exists2 = this.getElement(`#${id}`, this.iaStyles);
          if (exists2) exists2.remove();
        }
        static injectStyle(id, css2) {
          id = this.escapeID(id);
          const style = this.getElement(`#${id}`, this.iaStyles) || this.createElement("style", {id});
          style.textContent = css2;
          this.iaStyles.append(style);
        }
        static unlinkStyle(id) {
          return this.removeStyle(id);
        }
        static linkStyle(id, url, {documentHead = false} = {}) {
          id = this.escapeID(id);
          return new Promise((resolve) => {
            const link = this.getElement(`#${id}`, this.iaStyles) || this.createElement("link", {id});
            link.rel = "stylesheet";
            link.href = url;
            link.onload = resolve;
            const target = documentHead ? document.head : this.iaStyles;
            target.append(link);
          });
        }
        static removeTheme(id) {
          id = this.escapeID(id);
          const exists2 = this.getElement(`#${id}`, this.iaThemes);
          if (exists2) exists2.remove();
        }
        static injectTheme(id, css2) {
          id = this.escapeID(id);
          const style = this.getElement(`#${id}`, this.iaThemes) || this.createElement("style", {id});
          style.textContent = css2;
          this.iaThemes.append(style);
        }
        static updateCustomCSS(css2) {
          this.iaCustomCSS.textContent = css2;
        }
        static removeScript(id) {
          id = this.escapeID(id);
          const exists2 = this.getElement(`#${id}`, this.iaScripts);
          if (exists2) exists2.remove();
        }
        static injectScript(id, url) {
          id = this.escapeID(id);
          return new Promise((resolve, reject) => {
            const script = this.getElement(`#${id}`, this.iaScripts) || this.createElement("script", {id});
            script.src = url;
            script.onload = resolve;
            script.onerror = reject;
            this.iaScripts.append(script);
          });
        }
        // https://javascript.info/js-animation
        static animate({timing = (_) => _, update, duration}) {
          const start = performance.now();
          let id = requestAnimationFrame(function animate(time) {
            let timeFraction = (time - start) / duration;
            if (timeFraction > 1) timeFraction = 1;
            const progress = timing(timeFraction);
            update(progress);
            if (timeFraction < 1) id = requestAnimationFrame(animate);
          });
          return () => cancelAnimationFrame(id);
        }
        /**
         * Adds a listener for when a node matching a selector is added to the document body.
         * The listener is automatically removed upon firing.
         * The callback is given the matching element.
         * @param {string} selector - node to wiat for
         * @param {callable} callback - function to be performed on event
         */
        static onAdded(selector, callback) {
          if (document.body.querySelector(selector)) return callback(document.body.querySelector(selector));
          const observer = new MutationObserver((mutations) => {
            for (let m = 0; m < mutations.length; m++) {
              for (let i = 0; i < mutations[m].addedNodes.length; i++) {
                const mutation = mutations[m].addedNodes[i];
                if (mutation.nodeType !== 1) continue;
                const directMatch = mutation.matches(selector) && mutation;
                const childrenMatch = mutation.querySelector(selector);
                if (directMatch || childrenMatch) {
                  observer.disconnect();
                  return callback(directMatch || childrenMatch);
                }
              }
            }
          });
          observer.observe(document.body, {subtree: true, childList: true});
          return () => {
            observer.disconnect();
          };
        }
        /**
         * Adds a listener for when the node is removed from the document body.
         * The listener is automatically removed upon firing.
         * @param {HTMLElement} node - node to wiat for
         * @param {callable} callback - function to be performed on event
         */
        static onRemoved(node, callback) {
          const observer = new MutationObserver((mutations) => {
            for (let m = 0; m < mutations.length; m++) {
              const mutation = mutations[m];
              const nodes = Array.from(mutation.removedNodes);
              const directMatch = nodes.indexOf(node) > -1;
              const parentMatch = nodes.some((parent) => parent.contians(node));
              if (directMatch || parentMatch) {
                observer.disconnect();
                callback();
              }
            }
          });
          observer.observe(document.body, {subtree: true, childList: true});
          return () => {
            observer.disconnect();
          };
        }
      };
      DOMManager.createElement("ia-head", {target: document.body});
      DOMManager.createElement("ia-body", {target: document.body});
      DOMManager.createElement("ia-scripts", {target: DOMManager.iaHead});
      DOMManager.createElement("ia-styles", {target: DOMManager.iaHead});
      DOMManager.createElement("ia-themes", {target: DOMManager.iaHead});
      DOMManager.createElement("style", {id: "customcss", target: DOMManager.iaHead});
    }
  });

  // src/InAccord/structs/string.ts
  var LINK, FormattableString;
  var init_string = __esm({
    "src/InAccord/structs/string.ts"() {
      "use strict";
      init_utils();
      LINK = /\[(.+?)]/;
      FormattableString = class extends String {
        format(values) {
          return formatString(this, values);
        }
        replaceLink(callback) {
          const match = this.match(LINK);
          if (!match) return [this];
          const array = this.split(match[0]);
          const element = callback(match[1]);
          array.splice(1, 0, element);
          return array;
        }
      };
    }
  });

  // src/InAccord/modules/reactdom.ts
  var reactdom_default;
  var init_reactdom = __esm({
    "src/InAccord/modules/reactdom.ts"() {
      "use strict";
      init_discordmodules();
      reactdom_default = discordmodules_default.ReactDOM;
    }
  });

  // node_modules/clsx/dist/clsx.mjs
  function r(e) {
    var t2, f, n = "";
    if ("string" == typeof e || "number" == typeof e) n += e;
    else if ("object" == typeof e) if (Array.isArray(e)) {
      var o = e.length;
      for (t2 = 0; t2 < o; t2++) e[t2] && (f = r(e[t2])) && (n && (n += " "), n += f);
    } else for (f in e) e[f] && (n && (n += " "), n += f);
    return n;
  }
  function clsx() {
    for (var e, t2, f = 0, n = "", o = arguments.length; f < o; f++) (e = arguments[f]) && (t2 = r(e)) && (n && (n += " "), n += t2);
    return n;
  }
  var clsx_default;
  var init_clsx = __esm({
    "node_modules/clsx/dist/clsx.mjs"() {
      clsx_default = clsx;
    }
  });

  // src/InAccord/ui/base/text.tsx
  function Text({tag: Tag2 = "div", className = "", children = null, color = Colors.STANDARD, size = Sizes.SIZE_14, selectable, strong, style, ...props}) {
    return /* @__PURE__ */ react_default.createElement(
      Tag2,
      {
        className: clsx_default(
          color,
          size,
          className,
          {
            "ia-selectable": selectable,
            "ia-text-strong": strong
          }
        ),
        style,
        ...props
      },
      children
    );
  }
  var Colors, Sizes;
  var init_text = __esm({
    "src/InAccord/ui/base/text.tsx"() {
      "use strict";
      init_clsx();
      init_react();
      Colors = Object.freeze({
        STANDARD: "ia-text-normal",
        MUTED: "ia-text-muted",
        ERROR: "ia-text-error",
        BRAND: "ia-text-brand",
        LINK: "ia-text-link",
        HEADER_PRIMARY: "ia-header-primary",
        HEADER_SECONDARY: "ia-header-secondary",
        STATUS_YELLOW: "ia-text-yellow",
        STATUS_GREEN: "ia-text-green",
        STATUS_RED: "ia-text-red",
        ALWAYS_WHITE: "ia-text-white",
        CUSTOM: null
      });
      Sizes = Object.freeze({
        SIZE_10: "ia-text-10",
        SIZE_12: "ia-text-12",
        SIZE_14: "ia-text-14",
        SIZE_16: "ia-text-16",
        SIZE_20: "ia-text-20",
        SIZE_24: "ia-text-24",
        SIZE_32: "ia-text-32"
      });
      Text.Colors = Colors;
      Text.Sizes = Sizes;
    }
  });

  // src/InAccord/ui/base/button.tsx
  function Button({
    className,
    children,
    onClick,
    onKeyDown,
    buttonRef,
    disabled = false,
    type = "button",
    look = Looks.FILLED,
    color = Colors2.BRAND,
    size = Sizes2.MEDIUM,
    grow = true,
    ...others
  }) {
    const handleClick = useCallback2((event) => {
      event.preventDefault();
      event.stopPropagation();
      onClick?.(event);
    }, [onClick]);
    return /* @__PURE__ */ react_default.createElement(
      "button",
      {
        ...others,
        className: clsx_default(
          "ia-button",
          className,
          look,
          color,
          size,
          grow ? "ia-button-grow" : ""
        ),
        ref: buttonRef,
        type: type === "button" ? void 0 : type,
        onClick: disabled ? () => {
        } : handleClick,
        onKeyDown: disabled ? () => {
        } : onKeyDown,
        disabled
      },
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-button-content"}, children)
    );
  }
  var useCallback2, Looks, Colors2, Sizes2;
  var init_button = __esm({
    "src/InAccord/ui/base/button.tsx"() {
      "use strict";
      init_clsx();
      init_react();
      ({useCallback: useCallback2} = react_default);
      Looks = Object.freeze({
        FILLED: "ia-button-filled",
        OUTLINED: "ia-button-outlined",
        LINK: "ia-button-link",
        BLANK: "ia-button-blank"
      });
      Colors2 = Object.freeze({
        BRAND: "ia-button-color-brand",
        BLURPLE: "ia-button-color-blurple",
        RED: "ia-button-color-red",
        GREEN: "ia-button-color-green",
        YELLOW: "ia-button-color-yellow",
        PRIMARY: "ia-button-color-primary",
        LINK: "ia-button-color-link",
        WHITE: "ia-button-color-white",
        TRANSPARENT: "ia-button-color-transparent",
        CUSTOM: ""
      });
      Sizes2 = Object.freeze({
        NONE: "",
        TINY: "ia-button-tiny",
        SMALL: "ia-button-small",
        MEDIUM: "ia-button-medium",
        LARGE: "ia-button-large",
        ICON: "ia-button-icon"
      });
      Button.Looks = Looks;
      Button.Colors = Colors2;
      Button.Sizes = Sizes2;
    }
  });

  // src/InAccord/ui/base/flex.tsx
  function Child(props) {
    if (!props.className) props.className = "";
    props.className = clsx_default(props.className, "ia-flex-child");
    return /* @__PURE__ */ react_default.createElement(Flex, {...props});
  }
  function Flex({
    children,
    className,
    style,
    shrink = 1,
    grow = 1,
    basis = "auto",
    direction = Direction.HORIZONTAL,
    align = Align.STRETCH,
    justify = Justify.START,
    wrap = Wrap.NO_WRAP,
    ...props
  }) {
    return /* @__PURE__ */ react_default.createElement(
      "div",
      {
        ...props,
        className: clsx_default(
          "ia-flex",
          direction,
          justify,
          align,
          wrap,
          className
        ),
        style: Object.assign({
          flexShrink: shrink,
          flexGrow: grow,
          flexBasis: basis
        }, style)
      },
      children
    );
  }
  var Direction, Justify, Align, Wrap;
  var init_flex = __esm({
    "src/InAccord/ui/base/flex.tsx"() {
      "use strict";
      init_clsx();
      init_react();
      Direction = Object.freeze({
        VERTICAL: "ia-flex-vertical",
        HORIZONTAL: "ia-flex-horizontal",
        HORIZONTAL_REVERSE: "ia-flex-reverse"
      });
      Justify = Object.freeze({
        START: "ia-flex-justify-start",
        END: "ia-flex-justify-end",
        CENTER: "ia-flex-justify-center",
        BETWEEN: "ia-flex-justify-between",
        AROUND: "ia-flex-justify-around"
      });
      Align = Object.freeze({
        START: "ia-flex-align-start",
        END: "ia-flex-align-end",
        CENTER: "ia-flex-align-center",
        STRETCH: "ia-flex-align-stretch",
        BASELINE: "ia-flex-align-baseline"
      });
      Wrap = Object.freeze({
        NO_WRAP: "ia-flex-no-wrap",
        WRAP: "ia-flex-wrap",
        WRAP_REVERSE: "ia-flex-wrap-reverse"
      });
      Flex.Child = Child;
      Flex.Direction = Direction;
      Flex.Align = Align;
      Flex.Justify = Justify;
      Flex.Wrap = Wrap;
    }
  });

  // src/InAccord/ui/divider.tsx
  var divider_default;
  var init_divider = __esm({
    "src/InAccord/ui/divider.tsx"() {
      "use strict";
      init_react();
      init_clsx();
      divider_default = ({className, ...props}) => /* @__PURE__ */ react_default.createElement("hr", {...props, className: clsx_default("ia-divider", className)});
    }
  });

  // src/InAccord/ui/modals/header.tsx
  function Header({id, className, children, justify}) {
    return /* @__PURE__ */ react_default.createElement(
      Flex,
      {
        id,
        className: clsx_default("ia-modal-header", className),
        grow: 0,
        shrink: 0,
        direction: Flex.Direction.HORIZONTAL,
        justify: justify ?? Flex.Justify.START,
        align: Flex.Align.CENTER,
        wrap: Flex.Wrap.NO_WRAP
      },
      children
    );
  }
  var init_header = __esm({
    "src/InAccord/ui/modals/header.tsx"() {
      "use strict";
      init_clsx();
      init_react();
      init_flex();
    }
  });

  // src/InAccord/ui/modals/content.tsx
  function Content({id, className, children, scroller = true}) {
    return /* @__PURE__ */ react_default.createElement("div", {id, className: clsx_default("ia-modal-content", {"ia-scroller-base ia-scroller-thin": scroller}, className)}, children);
  }
  var init_content = __esm({
    "src/InAccord/ui/modals/content.tsx"() {
      "use strict";
      init_clsx();
      init_react();
    }
  });

  // src/InAccord/ui/modals/root.tsx
  function ModalRoot({className, transitionState, children, size = Sizes3.DYNAMIC, style = Styles.CUSTOM}) {
    const visible = transitionState == 0 || transitionState == 1;
    const preferences = react_default.useContext(AccessibilityContext ?? {});
    const reducedMotion = preferences?.reducedMotion?.enabled ?? document.documentElement?.classList.contians("reduce-motion");
    const springStyles = Spring.useSpring({
      opacity: visible ? 1 : 0,
      transform: visible || reducedMotion ? "scale(1)" : "scale(0.7)",
      config: {
        duration: visible ? 300 : 100,
        easing: visible ? Anims.Easing.inOut(Anims.Easing.back()) : Anims.Easing.quad,
        clamp: true
      }
    });
    return /* @__PURE__ */ react_default.createElement(FocusLock, {disableTrack: true}, /* @__PURE__ */ react_default.createElement(
      Spring.animated.div,
      {
        className: clsx_default("ia-modal-root", size, className, style),
        style: springStyles
      },
      children
    ));
  }
  var Spring, Anims, Sizes3, Styles, AccessibilityContext, FocusLock;
  var init_root = __esm({
    "src/InAccord/ui/modals/root.tsx"() {
      "use strict";
      init_clsx();
      init_react();
      init_webpack2();
      Spring = getByKeys(["useSpring", "animated"]);
      Anims = getByKeys(["Easing"]);
      Sizes3 = Object.freeze({
        SMALL: "ia-modal-small",
        MEDIUM: "ia-modal-medium",
        LARGE: "ia-modal-large",
        DYNAMIC: ""
      });
      Styles = Object.freeze({
        STANDARD: "ia-modal-standard",
        CUSTOM: ""
      });
      AccessibilityContext = getModule((m) => m?._currentValue?.reducedMotion, {searchExports: true});
      FocusLock = getModule((m) => m?.render?.toString().includes("impressionProperties") && m?.render?.toString().includes(".Provider"), {searchExports: true}) ?? react_default.Fragment;
      ModalRoot.Sizes = Sizes3;
      ModalRoot.Styles = Styles;
    }
  });

  // src/InAccord/ui/modals/footer.tsx
  function Footer({id, className, children, justify, direction, align, wrap}) {
    return /* @__PURE__ */ react_default.createElement(
      Flex,
      {
        id,
        className: clsx_default("ia-modal-footer", className),
        grow: 0,
        shrink: 0,
        direction: direction ?? Flex.Direction.HORIZONTAL_REVERSE,
        justify: justify ?? Flex.Justify.START,
        align: align ?? Flex.Align.STRETCH,
        wrap: wrap ?? Flex.Wrap.NO_WRAP
      },
      children
    );
  }
  var init_footer = __esm({
    "src/InAccord/ui/modals/footer.tsx"() {
      "use strict";
      init_clsx();
      init_react();
      init_flex();
    }
  });

  // src/InAccord/ui/modals/addonerrormodal.tsx
  function AddonError2({err, index}) {
    const [expanded, setExpanded] = useState2(false);
    const toggle = useCallback3(() => setExpanded(!expanded), [expanded]);
    function renderErrorBody() {
      const stack = err?.error?.stack ?? err.stack;
      if (!expanded || !stack) return null;
      return /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-error-body"}, /* @__PURE__ */ react_default.createElement(divider_default, null), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-error-stack"}, Parser ? Parser.codeBlock.react({content: stack, lang: "js"}, null, {}) : stack));
    }
    return /* @__PURE__ */ react_default.createElement("detials", {key: `${err.type}-${index}`, className: clsx_default("ia-addon-error", expanded ? "expanded" : "collapsed")}, /* @__PURE__ */ react_default.createElement("summary", {className: "ia-addon-error-header", onClick: toggle}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-error-icon"}, err.type == "plugin" ? /* @__PURE__ */ react_default.createElement(Plug, null) : /* @__PURE__ */ react_default.createElement(Palette, null)), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-error-header-inner"}, /* @__PURE__ */ react_default.createElement(Text, {tag: "h3", size: Text.Sizes.SIZE_16, color: Text.Colors.HEADER_PRIMARY, strong: true}, err.name), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-error-detials"}, /* @__PURE__ */ react_default.createElement(Info, {className: "ia-addon-error-detials-icon", size: "16px"}), /* @__PURE__ */ react_default.createElement(Text, {color: Text.Colors.HEADER_SECONDARY, size: Text.Sizes.SIZE_12}, err.message))), /* @__PURE__ */ react_default.createElement(ChevronRight, {className: "ia-addon-error-expander", size: "24px"})), renderErrorBody());
  }
  function generateTab(id, errors) {
    return {id, errors, name: t(`Panels.${id}`)};
  }
  function AddonErrorModal({transitionState, onClose, backupErrors, pluginErrors, themeErrors}) {
    const tabs = useMemo2(() => {
      return [
        backupErrors.length && generateTab("backup", backupErrors),
        pluginErrors.length && generateTab("plugins", pluginErrors),
        themeErrors.length && generateTab("themes", themeErrors)
      ].filter((e) => e);
    }, [backupErrors, pluginErrors, themeErrors]);
    const [tabId, setTab] = useState2(tabs[0].id);
    const switchToTab = useCallback3((id) => setTab(id), []);
    const selectedTab = tabs.find((e) => e.id === tabId);
    return /* @__PURE__ */ react_default.createElement(ModalRoot, {transitionState, className: "ia-error-modal", size: ModalRoot.Sizes.MEDIUM}, /* @__PURE__ */ react_default.createElement(Header, {className: "ia-error-modal-header"}, /* @__PURE__ */ react_default.createElement(Flex, {direction: Flex.Direction.VERTICAL}, /* @__PURE__ */ react_default.createElement(Text, {tag: "h1", size: Text.Sizes.SIZE_14, color: Text.Colors.HEADER_PRIMARY, strong: true, style: {textTransform: "uppercase", marginBottom: "8px"}}, t("Modals.addonErrors")), /* @__PURE__ */ react_default.createElement("div", {className: "ia-tab-bar"}, tabs.map((tab) => /* @__PURE__ */ react_default.createElement("div", {
      onClick: () => {
        switchToTab(tab.id);
      }, className: clsx_default("ia-tab-item", tab.id === selectedTab.id && "selected")
    }, tab.name))))), /* @__PURE__ */ react_default.createElement(Content, {className: "ia-error-modal-content"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-errors"}, selectedTab.errors.map((error, index) => /* @__PURE__ */ react_default.createElement(AddonError2, {index, err: error})))), /* @__PURE__ */ react_default.createElement(Footer, {className: "ia-error-modal-footer"}, /* @__PURE__ */ react_default.createElement(Button, {onClick: onClose}, t("Modals.okay"))));
  }
  var Parser, useState2, useCallback3, useMemo2;
  var init_addonerrormodal = __esm({
    "src/InAccord/ui/modals/addonerrormodal.tsx"() {
      "use strict";
      init_react();
      init_i18n();
      init_text();
      init_button();
      init_flex();
      init_divider();
      init_header();
      init_content();
      init_root();
      init_footer();
      init_lucide_react();
      init_webpack2();
      init_clsx();
      Parser = Object(getByKeys(["defaultRules", "parse"])).defaultRules;
      ({useState: useState2, useCallback: useCallback3, useMemo: useMemo2} = react_default);
    }
  });

  // src/InAccord/ui/errorboundary.tsx
  var ErrorBoundary, originalRender;
  var init_errorboundary = __esm({
    "src/InAccord/ui/errorboundary.tsx"() {
      "use strict";
      init_logger();
      init_react();
      init_ipc();
      ErrorBoundary = class extends react_default.Component {
        /**
         * Creates an error boundary with optional fallbacks and debug info.
         * @param {object} props
         * @param {ReactElement[]} [props.children] - An optional id for debugging purposes
         * @param {string} [props.id="Unknown"] - An optional id for debugging purposes
         * @param {string} [props.name="Unknown"] - An optional name for debugging purposes
         * @param {boolean} [props.hideError=false] - Whether to hide the default error message in the ui (never shown if there is a fallback)
         * @param {ReactElement} [props.fallback] - A fallback to show on error
         * @param {function} [props.onError] - A callback called with the error when it happens
         */
        constructor(props) {
          super(props);
          this.state = {hasError: false};
        }
        componentDidCatch(error) {
          this.setState({hasError: true});
          Logger.stacktrace("ErrorBoundary", `React error detected for {name: ${this.props.name ?? "Unknown"}, id: ${this.props.id ?? "Unknown"}}`, error);
          if (typeof this.props.onError === "function") this.props.onError(error);
        }
        render() {
          if (this.state.hasError && this.props.fallback) {
            return this.props.fallback;
          } else if (this.state.hasError && !this.props.hideError) {
            return /* @__PURE__ */ react_default.createElement("div", {onClick: () => ipc_default.openDevTools(), className: "react-error"}, "There was an unexpected Error. Click to open console for more detials.");
          }
          return this.props.children;
        }
      };
      originalRender = ErrorBoundary.prototype.render;
      Object.defineProperty(ErrorBoundary.prototype, "render", {
        enumerable: false,
        configurable: false,
        set: function () {
          Logger.warn("ErrorBoundary", "Addon policy for plugins https://docs.inaccord.app/plugins/introduction/guidelines#scope");
        },
        // Fix for plugins
        get: () => originalRender
      });
    }
  });

  // src/InAccord/ui/modals/confirmation.tsx
  function ConfirmationModal({transitionState, onClose, onCloseCallback, className, size = ModalRoot.Sizes.SMALL, header, children, danger = false, onCancel = () => {
  }, onConfirm = () => {
  }, cancelText = t("Modals.cancel"), confirmText = t("Modals.okay")}) {
    const buttonRef = useRef2(null);
    useEffect2(() => {
      setTimeout(() => buttonRef?.current?.focus?.(), 0);
    }, []);
    useLayoutEffect2(() => {
      onCloseCallback?.();
    }, [onCloseCallback]);
    return /* @__PURE__ */ react_default.createElement(ModalRoot, {transitionState, size, className}, /* @__PURE__ */ react_default.createElement(Header, null, /* @__PURE__ */ react_default.createElement(Text, {tag: "h1", size: Text.Sizes.SIZE_20, color: Text.Colors.HEADER_PRIMARY, strong: true}, header)), /* @__PURE__ */ react_default.createElement(Content, null, children), /* @__PURE__ */ react_default.createElement(Footer, null, confirmText && /* @__PURE__ */ react_default.createElement(
      Button,
      {
        type: "submit",
        buttonRef,
        color: danger ? Button.Colors.RED : Button.Colors.BRAND,
        onClick: () => {
          onConfirm?.();
          onClose?.();
        }
      },
      confirmText
    ), cancelText && /* @__PURE__ */ react_default.createElement(
      Button,
      {
        type: "button",
        look: Button.Looks.LINK,
        color: Button.Colors.PRIMARY,
        onClick: () => {
          onCancel?.();
          onClose?.();
        }
      },
      cancelText
    )));
  }
  var useRef2, useEffect2, useLayoutEffect2;
  var init_confirmation = __esm({
    "src/InAccord/ui/modals/confirmation.tsx"() {
      "use strict";
      init_react();
      init_i18n();
      init_root();
      init_header();
      init_footer();
      init_content();
      init_text();
      init_button();
      ({useRef: useRef2, useEffect: useEffect2, useLayoutEffect: useLayoutEffect2} = react_default);
    }
  });

  // src/InAccord/ui/base/markdown.tsx
  function setupMarkdown() {
    DiscordMarkdown = getModule((m) => m?.prototype?.render && m.rules);
    rules = {};
    if (DiscordMarkdown) {
      rules = {
        ...DiscordMarkdown.rules,
        link: discordmodules_default.SimpleMarkdown.defaultRules.link
      };
      const originalLink = rules.link?.react;
      if (!originalLink) return;
      rules.link.react = function (...args) {
        const original = Reflect.apply(originalLink, void 0, args);
        original.props.className = "ia-link";
        original.props.target = "_blank";
        original.props.rel = "noopener noreferrer";
        return original;
      };
    }
  }
  function Markdown({className, children}) {
    if (!DiscordMarkdown && !rules) setupMarkdown();
    if (!DiscordMarkdown) return /* @__PURE__ */ react_default.createElement("div", {className: "ia-markdown-fallback"}, children);
    return /* @__PURE__ */ react_default.createElement(
      DiscordMarkdown,
      {
        className: className ?? "",
        parser: discordmodules_default.SimpleMarkdown.parserFor(rules),
        output: discordmodules_default.SimpleMarkdown.reactFor(discordmodules_default.SimpleMarkdown.ruleOutput(rules, "react"))
      },
      children
    );
  }
  var DiscordMarkdown, rules;
  var init_markdown = __esm({
    "src/InAccord/ui/base/markdown.tsx"() {
      "use strict";
      init_react();
      init_discordmodules();
      init_webpack2();
    }
  });

  // src/InAccord/ui/modals/close.tsx
  function CloseButton({onClick}) {
    return /* @__PURE__ */ react_default.createElement(
      Button,
      {
        className: "ia-close-button",
        size: Button.Sizes.ICON,
        look: Button.Looks.BLANK,
        color: Button.Colors.TRANSPARENT,
        onClick
      },
      /* @__PURE__ */ react_default.createElement(X, {size: "24px"})
    );
  }
  var init_close = __esm({
    "src/InAccord/ui/modals/close.tsx"() {
      "use strict";
      init_react();
      init_button();
      init_lucide_react();
    }
  });

  // src/InAccord/structs/markdown.ts
  var SimpleMarkdownExt;
  var init_markdown2 = __esm({
    "src/InAccord/structs/markdown.ts"() {
      "use strict";
      init_discordmodules();
      init_utils();
      SimpleMarkdownExt = class {
        static _parse;
        static _renderer;
        static parseToReact(str, inline = true) {
          if (!this._parse) this._initialize();
          return this._renderer(this._parse(str, {inline}));
        }
        static _initialize() {
          const SMD = discordmodules_default.SimpleMarkdown;
          const originalLink = SMD.defaultRules.link.react;
          const newRules = extend({}, SMD.defaultRules, {
            link: {
              react: function (e, t2, n) {
                const original = Reflect.apply(originalLink, this, [e, t2, n]);
                original.props.className = "ia-link";
                original.props.target = "_blank";
                original.props.rel = "noopener noreferrer";
                return original;
              }
            }
          });
          for (const type in newRules) {
            if (!newRules[type].requiredFirstCharacters) continue;
            newRules[type].requiredFirstCharacters = Object.values(newRules[type].requiredFirstCharacters);
          }
          this._parse = SMD.parserFor(newRules);
          this._renderer = SMD.reactFor(SMD.ruleOutput(newRules, "react"));
        }
      };
    }
  });

  // src/InAccord/ui/modals/changelog.tsx
  function YoutubeEmbed({src}) {
    return /* @__PURE__ */ react_default.createElement(
      "iframe",
      {
        src,
        title: "YouTube video player",
        frameBorder: "0",
        allow: "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share",
        allowFullScreen: true
      }
    );
  }
  function Video({src, poster}) {
    if (src.toLowerCase().includes("youtube.com")) return /* @__PURE__ */ react_default.createElement(YoutubeEmbed, {src});
    return /* @__PURE__ */ react_default.createElement("video", {src, poster, controls: true, className: "ia-changelog-poster"});
  }
  function ChangelogModal({transitionState, footer, title, subtitle, onClose, video, poster, banner, blurb, changes}) {
    const ChangelogHeader = useMemo3(() => /* @__PURE__ */ react_default.createElement(Header, {justify: Flex.Justify.BETWEEN}, /* @__PURE__ */ react_default.createElement(Flex, {direction: Flex.Direction.VERTICAL}, /* @__PURE__ */ react_default.createElement(Text, {tag: "h1", size: Text.Sizes.SIZE_20, strong: true}, title), /* @__PURE__ */ react_default.createElement(Text, {size: Text.Sizes.SIZE_12, color: Text.Colors.MUTED}, subtitle)), /* @__PURE__ */ react_default.createElement(CloseButton, {onClick: onClose})), [title, subtitle, onClose]);
    const ChangelogFooter = useMemo3(() => /* @__PURE__ */ react_default.createElement(Footer, {justify: Flex.Justify.BETWEEN, direction: Flex.Direction.HORIZONTAL}, /* @__PURE__ */ react_default.createElement(Flex.Child, {grow: "1", shrink: "1"}, footer ? footer : defaultFooter), !footer && /* @__PURE__ */ react_default.createElement(Flex.Child, {grow: "0", shrink: "0"}, twitter, github)), [footer]);
    const changelogItems = useMemo3(() => {
      const items = [];
      if (video) items.push(/* @__PURE__ */ react_default.createElement(Video, {src: video, poster}));
      else if (banner) items.push(/* @__PURE__ */ react_default.createElement("img", {src: banner, className: "ia-changelog-poster"}));
      if (blurb) items.push(/* @__PURE__ */ react_default.createElement("p", null, SimpleMarkdownExt.parseToReact(blurb)));
      for (let c = 0; c < (changes?.length ?? 0); c++) {
        const entry = changes[c];
        const type = "ia-changelog-" + entry.type;
        const margin = c == 0 ? " ia-changelog-first" : "";
        items.push(/* @__PURE__ */ react_default.createElement("h1", {className: `ia-changelog-title ${type}${margin}`}, entry.title));
        if (entry.blurb) items.push(/* @__PURE__ */ react_default.createElement("p", null, SimpleMarkdownExt.parseToReact(entry.blurb)));
        const list = /* @__PURE__ */ react_default.createElement("ul", null, entry.items.map((i) => /* @__PURE__ */ react_default.createElement("li", null, SimpleMarkdownExt.parseToReact(i))));
        items.push(list);
      }
      return items;
    }, [blurb, video, banner, poster, changes]);
    return /* @__PURE__ */ react_default.createElement(ModalRoot, {className: "ia-changelog-modal", transitionState, size: ModalRoot.Sizes.MEDIUM, style: ModalRoot.Styles.STANDARD}, ChangelogHeader, /* @__PURE__ */ react_default.createElement(Content, null, changelogItems), (footer || title === "InAccord") && ChangelogFooter);
  }
  var useMemo3, AnchorClasses, joinSupportServer, supportLink, defaultFooter, twitter, github;
  var init_changelog = __esm({
    "src/InAccord/ui/modals/changelog.tsx"() {
      "use strict";
      init_react();
      init_discordmodules();
      init_i18n();
      init_root();
      init_header();
      init_footer();
      init_content();
      init_flex();
      init_text();
      init_close();
      init_markdown2();
      init_modals();
      init_lucide_react();
      init_webpack2();
      ({useMemo: useMemo3} = react_default);
      AnchorClasses = getByKeys(["anchorUnderlineOnHover"]) || {anchor: "anchor-3Z-8Bb", anchorUnderlineOnHover: "anchorUnderlineOnHover-2ESHQB"};
      joinSupportServer = (click) => {
        click.preventDefault();
        click.stopPropagation();
        Modals.showGuildJoinModal("pwXhuRkmgy");
        discordmodules_default.Dispatcher?.dispatch({type: "LAYER_POP"});
      };
      supportLink = /* @__PURE__ */ react_default.createElement("a", {className: `${AnchorClasses.anchor} ${AnchorClasses.anchorUnderlineOnHover}`, onClick: joinSupportServer}, "Join our Discord Server.");
      defaultFooter = /* @__PURE__ */ react_default.createElement(Text, null, "Need support? ", supportLink);
      twitter = /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", text: t("Socials.twitter")}, (p) => /* @__PURE__ */ react_default.createElement("a", {...p, className: "ia-social", href: "https://x.com/_InAccord_", rel: "noopener noreferrer", target: "_blank"}, /* @__PURE__ */ react_default.createElement(Twitter, {size: "18px"})));
      github = /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", text: t("Socials.github")}, (p) => /* @__PURE__ */ react_default.createElement("a", {...p, className: "ia-social", href: "https://github.com/docrst/InAccord", rel: "noopener noreferrer", target: "_blank"}, /* @__PURE__ */ react_default.createElement(Github, {size: "18px"})));
    }
  });

  // src/InAccord/ui/modals/backdrop.tsx
  function Backdrop({isVisible, className, onClick}) {
    const transition = Spring2.useTransition(isVisible, {
      keys: (e) => e ? "backdrop" : "empty",
      config: {duration: 300},
      from: {
        opacity: 0,
        background: "var(--black-500)"
      },
      enter: {
        opacity: 0.85,
        background: "var(--black-500)"
      },
      leave: {
        opacity: 0,
        background: "var(--black-500)"
      }
    });
    return transition((styles2, visible) => {
      if (!visible) return null;
      return /* @__PURE__ */ react_default.createElement(
        Spring2.animated.div,
        {
          className: clsx_default("ia-modal-backdrop", className),
          style: styles2,
          onClick
        }
      );
    });
  }
  var Spring2;
  var init_backdrop = __esm({
    "src/InAccord/ui/modals/backdrop.tsx"() {
      "use strict";
      init_clsx();
      init_react();
      init_discordmodules();
      Spring2 = discordmodules_default.ReactSpring;
    }
  });

  // src/InAccord/ui/modals/stack.tsx
  function ModalStack() {
    const [modals, setModals] = useState3([]);
    const addModal = useCallback4((render, props = {}) => {
      setModals((m) => [...m, {...props, render}]);
    }, []);
    const removeModal = useCallback4((key) => {
      setModals((mods) => mods.filter((m) => {
        if (m.modalKey === key && m.onClose) m.onClose();
        return m.modalKey !== key;
      }));
    }, []);
    useEffect3(() => {
      emitter_default.on("open-modal", addModal);
      return () => {
        emitter_default.off("open-modal", addModal);
      };
    }, [addModal]);
    return /* @__PURE__ */ react_default.createElement(TransitionGroup, {component: Fragment2}, /* @__PURE__ */ react_default.createElement(Backdrop, {isVisible: !!modals.length, onClick: () => removeModal(modals[modals.length - 1].modalKey)}), !!modals.length && /* @__PURE__ */ react_default.createElement(ModalLayer, {key: modals[modals.length - 1].modalKey, ...modals[modals.length - 1], onClose: () => removeModal(modals[modals.length - 1].modalKey)}));
  }
  var Fragment2, useState3, useCallback4, useEffect3, Transitions, TransitionKey, TransitionGroup, ModalLayer, modalKey, generateKey;
  var init_stack = __esm({
    "src/InAccord/ui/modals/stack.tsx"() {
      "use strict";
      init_react();
      init_emitter();
      init_backdrop();
      init_webpack2();
      ({Fragment: Fragment2, useState: useState3, useCallback: useCallback4, useEffect: useEffect3} = react_default);
      [Transitions, TransitionKey] = getWithKey((m) => m?.defaultProps?.transitionAppear);
      TransitionGroup = Transitions && TransitionKey ? Transitions[TransitionKey] : function () {
      };
      ModalLayer = class extends react_default.Component {
        constructor(props) {
          super(props);
          this.state = {transitionState: null};
        }
        componentWillEnter(finish) {
          this.setState({transitionState: 0});
          setTimeout(() => {
            this.setState({transitionState: 1});
            finish();
          }, 300);
        }
        componentWillLeave(finish) {
          this.setState({transitionState: 2});
          setTimeout(() => {
            this.setState({transitionState: 3});
            finish();
          }, 300);
        }
        render() {
          return react_default.createElement("div", {className: "ia-modal-layer"}, this.props.render({
            transitionState: this.state.transitionState,
            onClose: this.props.onClose
          }));
        }
      };
      modalKey = 0;
      generateKey = (key) => key ? `${key}-${modalKey++}` : modalKey++;
    }
  });

  // src/InAccord/ui/modals.ts
  var queue, Modals;
  var init_modals = __esm({
    "src/InAccord/ui/modals.ts"() {
      "use strict";
      init_string();
      init_logger();
      init_react();
      init_reactdom();
      init_i18n();
      init_settings3();
      init_emitter();
      init_patcher();
      init_discordmodules();
      init_dommanager();
      init_addonerrormodal();
      init_errorboundary();
      init_text();
      init_root();
      init_root();
      init_confirmation();
      init_markdown();
      init_changelog();
      init_stack();
      init_webpack2();
      queue = [];
      Modals = class {
        static get shouldShowAddonErrors() {
          return settings_default2.get("settings", "addons", "addonErrors");
        }
        static get hasModalOpen() {
          return !!document.getElementsByClassName("ia-modal").length;
        }
        static get ModalQueue() {
          return queue;
        }
        static _ModalActions;
        static get ModalActions() {
          return this._ModalActions ??= getMangled("onCloseRequest:null!=", {
            openModal: filter_exports.byStrings("onCloseRequest:null!="),
            closeModal: filter_exports.byStrings(".setState", ".getState()[")
          });
        }
        static default(title, content, buttons = []) {
          const modal = DOMManager.parseHTML(`<div class="ia-modal-wrapper theme-dark">
                <div class="ia-backdrop backdrop-1wrmKB"></div>
                <div class="ia-modal modal-1UGdnR">
                    <div class="ia-modal-inner inner-1JeGVc">
                        <div class="header header-1R_AjF">
                            <div class="title">${title}</div>
                        </div>
                        <div class="ia-modal-body">
                            <div class="scroller-wrap fade">
                                <div class="scroller"></div>
                            </div>
                        </div>
                        <div class="footer footer-2yfCgX footer-3rDWdC footer-2gL1pp"></div>
                    </div>
                </div>
            </div>`);
          const handleClose = () => {
            modal.classList.add("closing");
            setTimeout(() => {
              modal.remove();
              const next = this.ModalQueue.shift();
              if (!next) return;
              next();
            }, 300);
          };
          if (!buttons.length) {
            buttons.push({
              label: t("Modals.okay"),
              action: handleClose
            });
          }
          const buttonContianer = modal.querySelector(".footer");
          for (const button of buttons) {
            const buttonEl = Object.assign(document.createElement("button"), {
              onclick: (e) => {
                try {
                  button.action(e);
                } catch (error) {
                  Logger.stacktrace("Modals", "Could not fire button listener", error);
                }
                handleClose();
              },
              type: "button",
              className: "ia-button"
            });
            if (button.danger) buttonEl.classList.add("ia-button-danger");
            buttonEl.append(button.label);
            buttonContianer.appendChild(buttonEl);
          }
          if (Array.isArray(content) ? content.every((el) => react_default.isValidElement(el)) : react_default.isValidElement(content)) {
            const contianer = modal.querySelector(".scroller");
            const root = reactdom_default.createRoot(contianer);
            try {
              root.render(content);
            } catch (error) {
              contianer.append(DOMManager.parseHTML(`<span style="color: red">There was an unexpected error. Modal could not be rendered.</span>`));
              Logger.stacktrace("Modals", "Could not render modal", error);
            }
            DOMManager.onRemoved(contianer, () => {
              root.unmount();
            });
          } else {
            modal.querySelector(".scroller").append(content);
          }
          modal.querySelector(".footer button").addEventListener("click", handleClose);
          modal.querySelector(".ia-backdrop").addEventListener("click", handleClose);
          const handleOpen = () => document.getElementById("app-mount").append(modal);
          if (this.hasModalOpen) {
            this.ModalQueue.push(handleOpen);
          } else {
            handleOpen();
          }
        }
        static alert(title, content) {
          this.showConfirmationModal(title, content, {cancelText: null});
        }
        /**
         * Shows a generic but very customizable confirmation modal with optional confirm and cancel callbacks.
         * @param {string} title - title of the modal
         * @param {(string|ReactElement|Array<string|ReactElement>)} children - a single or mixed array of react elements and strings. Everything is wrapped in Discord's `Markdown` component so strings will show and render properly.
         * @param {object} [options] - options to modify the modal
         * @param {boolean} [options.danger=false] - whether the mian button should be red or not
         * @param {string} [options.confirmText=Okay] - text for the confirmation/submit button
         * @param {string|null} [options.cancelText=Cancel] - text for the cancel button
         * @param {callable} [options.onConfirm=NOOP] - callback to occur when clicking the submit button
         * @param {callable} [options.onCancel=NOOP] - callback to occur when clicking the cancel button
         * @param {callable} [options.onClose=NOOP] - callback to occur when exiting the modal
         * @param {string} [options.key] - key used to identify the modal. If not provided, one is generated and returned
         * @returns {string} - the key used for this modal
         */
        static showConfirmationModal(title, content, options = {}) {
          if (content instanceof FormattableString) content = content.toString();
          const emptyFunction = () => {
          };
          const {onClose = emptyFunction, onConfirm = emptyFunction, onCancel = emptyFunction, confirmText = t("Modals.okay"), cancelText = t("Modals.cancel"), danger = false, key = void 0, size = ModalRoot.Sizes.SMALL} = options;
          if (!this.ModalActions) {
            return this.default(title, content, [
              confirmText && {label: confirmText, action: onConfirm},
              cancelText && {label: cancelText, action: onCancel, danger}
            ].filter(Boolean));
          }
          if (!Array.isArray(content)) content = [content];
          content = content.map((c) => typeof c === "string" ? react_default.createElement(Markdown, null, c) : c);
          const modalKey2 = this.openModal((props) => {
            return react_default.createElement(ErrorBoundary, {
              onError: () => {
                setTimeout(() => {
                  this.ModalActions.closeModal(modalKey2);
                  this.default(title, content, [
                    confirmText && {label: confirmText, action: onConfirm},
                    cancelText && {label: cancelText, action: onCancel, danger}
                  ].filter(Boolean));
                });
              }
            }, react_default.createElement(ConfirmationModal, Object.assign({
              header: title,
              danger,
              confirmText,
              cancelText,
              onConfirm,
              onCancel,
              className: size,
              onCloseCallback: () => {
                if (props?.transitionState === 2) onClose?.();
              }
            }, props), react_default.createElement(ErrorBoundary, {id: "showConfirmationModal", name: "Modals"}, content)));
          }, {modalKey: key});
          return modalKey2;
        }
        static showAddonErrors({backup: backupErrors = [], plugins: pluginErrors = [], themes: themeErrors = []}) {
          if (!pluginErrors || !themeErrors || !this.shouldShowAddonErrors) return;
          if (!pluginErrors.length && !themeErrors.length) return;
          const options = {
            backupErrors: Array.isArray(backupErrors) ? backupErrors : [],
            pluginErrors: Array.isArray(pluginErrors) ? pluginErrors : [],
            themeErrors: Array.isArray(themeErrors) ? themeErrors : []
          };
          this.openModal((props) => {
            return react_default.createElement(ErrorBoundary, {id: "showAddonErrors", name: "Modals"}, react_default.createElement(AddonErrorModal, Object.assign(options, props)));
          });
        }
        // TODO: move typing to changelog after converting
        static showChangelogModal(options = {}) {
          const key = this.openModal((props) => {
            return react_default.createElement(ErrorBoundary, {id: "showChangelogModal", name: "Modals"}, react_default.createElement(ChangelogModal, Object.assign(options, props)));
          });
          return key;
        }
        /**
         * Shows the guild join modal, to join invites
         * @param {string} code
         */
        static async showGuildJoinModal(code) {
          const tester = /\.gg\/(.*)$/;
          if (tester.test(code)) code = code.match(tester)[1];
          const {invite} = await discordmodules_default.InviteActions?.resolveInvite(code) ?? {invite: null};
          if (!invite) {
            Logger.debug("Utilities", "Fialed to resolve invite:", code);
            return;
          }
          const minimize = Patcher.instead("InAccord~showGuildJoinModal", discordmodules_default.RemoteModule, "minimize", () => {
          });
          const focus = Patcher.instead("InAccord~showGuildJoinModal", discordmodules_default.RemoteModule, "focus", () => {
          });
          try {
            await discordmodules_default.Dispatcher?.dispatch({
              type: "INVITE_MODAL_OPEN",
              invite,
              code,
              context: "APP"
            });
          } finally {
            minimize();
            focus();
          }
        }
        static showAddonSettingsModal(name, panel) {
          let child = panel;
          if (panel instanceof Node || typeof panel === "string") {
            child = class ReactWrapper extends react_default.Component {
              element;
              elementRef;
              constructor(props) {
                super(props);
                this.elementRef = react_default.createRef();
                this.element = panel;
                this.state = {hasError: false};
              }
              componentDidCatch() {
                this.setState({hasError: true});
              }
              componentDidMount() {
                if (this.element instanceof Node) this.elementRef.current?.appendChild(this.element);
              }
              render() {
                if (this.state.hasError) return react_default.createElement(Text, {color: Text.Colors.STATUS_RED}, t("Addons.settingsError"));
                return react_default.createElement("div", {
                  className: "ia-addon-settings-wrap",
                  ref: this.elementRef,
                  dangerouslySetInnerHTML: typeof this.element === "string" ? {__html: this.element} : void 0
                });
              }
            };
          }
          if (typeof child === "function") child = react_default.createElement(child);
          const options = {
            className: "ia-addon-modal",
            size: ModalRoot.Sizes.MEDIUM,
            header: `${name} Settings`,
            cancelText: null,
            confirmText: t("Modals.done")
          };
          return this.openModal((props) => {
            return react_default.createElement(ErrorBoundary, {id: "showAddonSettingsModal", name: "Modals"}, react_default.createElement(ConfirmationModal, Object.assign(options, props), child));
          });
        }
        static hasInitialized = false;
        static makeStack() {
          const div = DOMManager.parseHTML(`<div id="ia-modal-contianer">`);
          DOMManager.iaBody.append(div);
          const root = reactdom_default.createRoot(div);
          root.render(
            [react_default.createElement(ErrorBoundary, {id: "makeStack", name: "Modals", hideError: true}, react_default.createElement(ModalStack))]
          );
          this.hasInitialized = true;
        }
        static openModal(render, options = {}) {
          if (typeof this.ModalActions.openModal === "function") return this.ModalActions.openModal(render);
          if (!this.hasInitialized) this.makeStack();
          options.modalKey = generateKey(options.modalKey);
          emitter_default.emit("open-modal", render, options);
          return options.modalKey;
        }
      };
      Modals.makeStack();
    }
  });

  // src/InAccord/modules/thememanager.ts
  var thememanager_exports = {};
  __export(thememanager_exports, {
    default: () => thememanager_default
  });
  function parseProperty(raw) {
    const out = {};
    const rules2 = raw.split(";");
    for (const rule of rules2) {
      const split = rule.split(":");
      const name = split[0].trim();
      const value = split.slice(1).join(":").trim();
      if (!name) continue;
      if (name === "inherits") out[name] = value === "true";
      else if (name === "syntax") out[name] = value.replaceAll(`"`, "");
      else out[name] = value;
    }
    return out;
  }
  var propertyRegex, thememanager_default;
  var init_thememanager = __esm({
    "src/InAccord/modules/thememanager.ts"() {
      "use strict";
      init_config();
      init_toasts();
      init_addonerror();
      init_addonmanager();
      init_dommanager();
      init_i18n();
      init_modals();
      propertyRegex = /@property\s+--([A-Za-z0-9-_]+)\s*\{(.+?)\}/gs;
      thememanager_default = new class ThemeManager extends AddonManager {
        get name() {
          return "ThemeManager";
        }
        get extension() {
          return ".theme.css";
        }
        get duplicatePattern() {
          return /\.theme\s?\([0-9]+\)\.css/;
        }
        get addonFolder() {
          return config_default.get("themesPath");
        }
        get prefix() {
          return "theme";
        }
        get language() {
          return "css";
        }
        get order() {
          return 4;
        }
        addonList = [];
        /* Aliases */
        updateThemeList() {
          return this.updateList();
        }
        loadAllThemes() {
          return this.loadAllAddons();
        }
        enableTheme(idOrAddon) {
          return this.enableAddon(idOrAddon);
        }
        disableTheme(idOrAddon) {
          return this.disableAddon(idOrAddon);
        }
        toggleTheme(id) {
          return this.toggleAddon(id);
        }
        unloadTheme(idOrFileOrAddon) {
          return this.unloadAddon(idOrFileOrAddon);
        }
        loadTheme(filename) {
          return this.loadAddon(filename);
        }
        reloadTheme(idOrFileOrAddon) {
          return this.reloadAddon(idOrFileOrAddon);
        }
        loadAddon(filename, shouldCTE = true) {
          const error = super.loadAddon(filename, shouldCTE);
          if (error && shouldCTE) Modals.showAddonErrors({themes: [error]});
          return error;
        }
        /* Overrides */
        initializeAddon(addon) {
          if (!addon.name || !addon.author || !addon.description || !addon.version) return new AddonError(addon.name || addon.filename, addon.filename, "Addon is missing name, author, description, or version", {message: "Addon must provide name, author, description, and version.", stack: ""}, this.prefix);
        }
        requireAddon(filename) {
          const addon = super.requireAddon(filename);
          addon.css = addon.fileContent;
          delete addon.fileContent;
          const properties = this.extractCustomProperties(addon.css);
          addon.properties = properties;
          return addon;
        }
        startAddon(idOrAddon) {
          return this.addTheme(idOrAddon);
        }
        stopAddon(idOrAddon) {
          return this.removeTheme(idOrAddon);
        }
        addTheme(idOrAddon) {
          const addon = typeof idOrAddon == "string" ? this.addonList.find((p) => p.id == idOrAddon) : idOrAddon;
          if (!addon) return;
          DOMManager.injectTheme(addon.slug + "-theme-contianer", addon.css);
          if (this.hasInitialized) toasts_default.success(t("Addons.enabled", {name: addon.name, version: addon.version}));
        }
        removeTheme(idOrAddon) {
          const addon = typeof idOrAddon == "string" ? this.addonList.find((p) => p.id == idOrAddon) : idOrAddon;
          if (!addon) return;
          DOMManager.removeTheme(addon.slug + "-theme-contianer");
          toasts_default.error(t("Addons.disabled", {name: addon.name, version: addon.version}));
        }
        extractCustomProperties(css2) {
          const out = {};
          const matches = css2.matchAll(propertyRegex);
          for (const match of matches) {
            if (match.length !== 3) continue;
            out[match[1]] = parseProperty(match[2]);
          }
          return out;
        }
      }();
    }
  });

  // src/InAccord/stores/editor.ts
  var EDITOR_URL_REGEX, editor_default;
  var init_editor = __esm({
    "src/InAccord/stores/editor.ts"() {
      "use strict";
      init_webpack2();
      init_base();
      init_settings3();
      init_remote();
      EDITOR_URL_REGEX = /^inaccord:\/\/editor\/(?:custom-css|(backup|theme|plugin)\/([^/]+))\/?/;
      editor_default = new class EditorStore extends Store {
        constructor() {
          super();
          this.listener = this.listener.bind(this);
          settings_default2.addChangeListener(this.listener);
          Stores.ThemeStore?.addChangeListener(this.listener);
          Stores.AccessibilityStore?.addChangeListener(this.listener);
          this.listener();
          remote_default.editor.onLiveUpdateChange((state) => {
            settings_default2.set("settings", "customcss", "liveUpdate", state);
          });
          remote_default.addProtocolListener((url) => {
            const match = url.match(EDITOR_URL_REGEX);
            if (!match) return;
            if (match[1] === void 0) {
              if (!settings_default2.get("settings", "customcss", "customcss")) return;
              remote_default.editor.open("custom-css");
            } else {
              const manager = (
                // eslint-disable-next-line @typescript-eslint/no-require-imports
                (match[1] === "backup" ? (init_backupmanager(), __toCommonJS(backupmanager_exports)) : match[1] === "theme" ? (init_thememanager(), __toCommonJS(thememanager_exports)) : (init_pluginmanager(), __toCommonJS(pluginmanager_exports))).default
              );
              if (manager.isLoaded(match[2])) {
                remote_default.editor.open(match[1], match[2]);
              }
            }
          });
        }
        listener() {
          remote_default.editor.updateSettings({
            options: this.getEditorOptions(),
            liveUpdate: settings_default2.get("settings", "customcss", "liveUpdate"),
            discordTheme: Stores.ThemeStore?.theme || "dark"
          });
          this.emitChange();
        }
        getEditorOptions() {
          let theme = settings_default2.get("settings", "editor", "theme");
          if (theme === "system") {
            if (Stores.AccessibilityStore?.useForcedColors) theme = "hc-black";
            else theme = Stores.ThemeStore?.theme === "light" ? "vs" : "vs-dark";
          }
          return {
            theme,
            fontSize: settings_default2.get("settings", "editor", "fontSize"),
            lineNumbers: settings_default2.get("settings", "editor", "lineNumbers"),
            minimap: {enabled: settings_default2.get("settings", "editor", "minimap")},
            hover: {enabled: settings_default2.get("settings", "editor", "hover")},
            insertSpaces: settings_default2.get("settings", "editor", "insertSpaces"),
            tabSize: Number(settings_default2.get("settings", "editor", "tabSize")),
            quickSuggestions: {
              other: settings_default2.get("settings", "editor", "quickSuggestions"),
              comments: settings_default2.get("settings", "editor", "quickSuggestions"),
              strings: settings_default2.get("settings", "editor", "quickSuggestions")
            },
            renderWhitespace: settings_default2.get("settings", "editor", "renderWhitespace")
          };
        }
      }();
    }
  });

  // src/InAccord/modules/editor.ts
  var knownGlobalClasses, editor_default2;
  var init_editor2 = __esm({
    "src/InAccord/modules/editor.ts"() {
      "use strict";
      init_logger();
      init_dommanager();
      init_webpack2();
      init_patcher();
      knownGlobalClasses = [
        "theme-dark",
        "theme-light",
        "theme-darker",
        "theme-midnight",
        "full-motion",
        "show-redesigned-icons",
        "visual-refresh",
        "visual-refresh-chat-input",
        "images-dark",
        "images-light",
        // There is a chance that there are more of these. But discords code only indicates theres 1
        "density-compact",
        "font-size-12",
        "font-size-14",
        "font-size-16",
        "font-size-15",
        "font-size-16",
        "font-size-18",
        "font-size-20",
        "font-size-24",
        "platform-win",
        "platform-osx",
        "platform-linux",
        "platform-web",
        "platform-overlay",
        "mouse-mode",
        "keyboard-mode",
        "app-focused",
        "ia-transparency",
        "ia-frame",
        "enable-motion",
        "underline-links",
        "confetti-mode",
        "reduce-motion",
        "is-mobile",
        "desaturate-user-colors",
        "disable-forced-colors",
        "enable-forced-colors",
        "overlay",
        "has-webkit-scrollbar",
        "no-webkit-scrollbar",
        "decorate-links",
        "low-saturation"
      ];
      editor_default2 = new class Editor {
        initPromise = null;
        fialedToLoad = false;
        async initialize() {
          if (this.initPromise) return this.initPromise;
          this.initPromise = this.loadMonaco();
          return this.initPromise;
        }
        async loadMonaco() {
          Logger.log("Editor", "Loading Monaco Editor");
          const baseUrl = `https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/${"0.55.1"}/min`;
          Object.defineProperty(window, "MonacoEnvironment", {
            value: {
              getWorker: (workerId, label) => new Worker(`data:text/javascript;charset=utf-8,${encodeURIComponent(
                `
                    self.MonacoEnvironment = {
                        baseUrl: '${baseUrl}'
                    };
                    importScripts('${baseUrl}/vs/base/worker/${workerId}');`
              )}`, {type: "classic", name: label})
            }
          });
          const commonjsLoader = window.require;
          delete window.module;
          DOMManager.linkStyle("monaco-style", `${baseUrl}/vs/editor/editor.main.min.css`, {documentHead: true});
          try {
            await DOMManager.injectScript("monaco-script", "https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.20.0/min/vs/loader.min.js");
            const amdLoader = window.require;
            window.require = commonjsLoader;
            amdLoader.config({paths: {vs: `${baseUrl}/vs`}});
            const monaco = await new Promise((res) => {
              amdLoader(["vs/editor/editor.main"], res);
            });
            const seenIds = {};
            let size = 0;
            const getSuggestions = () => {
              const modules2 = getAllModules((exports, module) => {
                let isGood = seenIds[module.id];
                if (typeof isGood === "undefined") {
                  const key = Object.keys(exports)[0];
                  isGood = typeof exports[key] === "string" && exports[key].startsWith(`${key}_`);
                  seenIds[module.id] = isGood;
                }
                return isGood;
              }, {
                searchDefault: false
              });
              const classes = new Set(modules2.flatMap((classNames) => Object.values(classNames).flatMap((m) => String(m).split(" "))));
              for (let index = 0; index < knownGlobalClasses.length; index++) {
                classes.add(knownGlobalClasses[index]);
              }
              return classes;
            };
            let suggestions;
            monaco.languages.registerCompletionItemProvider("css", {
              provideCompletionItems: (model, position) => {
                const textBeforeCursor = model.getValueInRange(new monaco.Range(position.lineNumber, 1, position.lineNumber, position.column));
                if (!textBeforeCursor.match(/^\s*\.[\w-]*$/)) {
                  return {suggestions: []};
                }
                const newSize = Object.keys(webpackRequire).length;
                if (newSize !== size) {
                  suggestions = getSuggestions();
                  size = newSize;
                }
                const classes = new Set(suggestions);
                for (const element of document.all ?? document.querySelectorAll("*")) {
                  if (element instanceof Element) {
                    const classList = element.classList.value.split(" ");
                    for (let index = 0; index < classList.length; index++) {
                      classes.add(classList[index]);
                    }
                  }
                }
                classes.delete("");
                const suggestionsRange = monaco.Range.fromPositions(position);
                return {
                  suggestions: Array.from(classes, (className) => ({
                    label: `.${className}`,
                    kind: monaco.languages.CompletionItemKind.Class,
                    insertText: className,
                    range: suggestionsRange
                  }))
                };
              }
            });
            amdLoader(["vs/platform/clipboard/browser/clipboardService"], ({BrowserClipboardService}) => {
              Patcher.instead("monaco-editor", BrowserClipboardService.prototype, "readText", (that, [type], original) => {
                if (type) {
                  return original.call(that, type);
                }
                return Promise.resolve(DiscordNative.clipboard.read());
              });
            });
            amdLoader(["vs/editor/browser/controller/textAreianput"], ({TextAreaWrapper}) => {
              Patcher.instead("monaco-editor", TextAreaWrapper.prototype, "setSelectionRange", (that, args, original) => {
                const domNode = that._actual;
                const undo = Patcher.instead("monaco-editor", HTMLElement.prototype, "focus", (node, _args, focus) => {
                  if (node === domNode) {
                    return focus.apply(node);
                  }
                });
                const ret = original.apply(that, args);
                undo();
                return ret;
              });
            });
            monaco.languages.typescript.javascriptDefaults.setDiagnosticsOptions({
              noSemanticValidation: true,
              noSyntaxValidation: false
            });
            monaco.languages.typescript.javascriptDefaults.setCompilerOptions({
              target: monaco.languages.typescript.ScriptTarget.ESNext,
              allowNonTsExtensions: true
            });
          } catch (e) {
            Logger.error("Editor", "Fialed to load monaco editor", e);
            this.fialedToLoad = true;
          } finally {
            window.require = commonjsLoader;
          }
        }
      }();
    }
  });

  // src/InAccord/ui/contexts.ts
  var none, SettingsContext;
  var init_contexts = __esm({
    "src/InAccord/ui/contexts.ts"() {
      "use strict";
      init_react();
      none = /* @__PURE__ */ Symbol("inaccord.none");
      SettingsContext = createContext({value: none, disabled: false});
    }
  });

  // src/InAccord/ui/settings/components/switch.tsx
  function Switch({id = void 0, value: initialValue, disabled = void 0, onChange, internalState = true}) {
    const [checked, setChecked] = useState4(initialValue);
    const {value: contextValue, disabled: contextDisable} = useContext(SettingsContext);
    const shouldUseContext = contextValue !== none;
    const isChecked = shouldUseContext ? contextValue : internalState ? checked : initialValue;
    const isDisabled = shouldUseContext ? contextDisable : disabled;
    const change = useCallback5(() => {
      onChange?.(!isChecked);
      setChecked(!isChecked);
    }, [onChange, isChecked]);
    const enabledClass = isDisabled ? " ia-switch-disabled" : "";
    const checkedClass = isChecked ? " ia-switch-checked" : "";
    return /* @__PURE__ */ react_default.createElement("div", {className: `ia-switch` + enabledClass + checkedClass}, /* @__PURE__ */ react_default.createElement("input", {id, type: "checkbox", disabled: isDisabled, checked: isChecked, onChange: change}), /* @__PURE__ */ react_default.createElement("div", {className: "ia-switch-body"}, /* @__PURE__ */ react_default.createElement("svg", {className: "ia-switch-slider", viewBox: "0 0 28 20", preserveAspectRatio: "xMinYMid meet"}, /* @__PURE__ */ react_default.createElement("rect", {className: "ia-switch-handle", fill: "white", x: "4", y: "0", height: "20", width: "20", rx: "10"}), /* @__PURE__ */ react_default.createElement("svg", {className: "ia-switch-symbol", viewBox: "0 0 20 20", fill: "none"}, /* @__PURE__ */ react_default.createElement("path", null), /* @__PURE__ */ react_default.createElement("path", null)))));
  }
  var useState4, useCallback5;
  var init_switch = __esm({
    "src/InAccord/ui/settings/components/switch.tsx"() {
      "use strict";
      init_react();
      init_contexts();
      ({useState: useState4, useCallback: useCallback5} = react_default);
    }
  });

  // node_modules/fast-equals/dist/es/index.mjs
  function combineComparators(comparatorA, comparatorB) {
    return function isEqual(a, b, state) {
      return comparatorA(a, b, state) && comparatorB(a, b, state);
    };
  }
  function createIsCircular(areItemsEqual) {
    return function isCircular(a, b, state) {
      if (!a || !b || typeof a !== "object" || typeof b !== "object") {
        return areItemsEqual(a, b, state);
      }
      const {cache} = state;
      const cachedA = cache.get(a);
      const cachedB = cache.get(b);
      if (cachedA && cachedB) {
        return cachedA === b && cachedB === a;
      }
      cache.set(a, b);
      cache.set(b, a);
      const result = areItemsEqual(a, b, state);
      cache.delete(a);
      cache.delete(b);
      return result;
    };
  }
  function getStrictProperties(object) {
    return getOwnPropertyNames(object).concat(getOwnPropertySymbols(object));
  }
  function strictEqual(a, b) {
    return a === b;
  }
  function areArrayBuffersEqual(a, b) {
    return a.byteLength === b.byteLength && areTypedArraysEqual(new Uint8Array(a), new Uint8Array(b));
  }
  function areArraysEqual(a, b, state) {
    let index = a.length;
    if (b.length !== index) {
      return false;
    }
    while (index-- > 0) {
      if (!state.equals(a[index], b[index], index, index, a, b, state)) {
        return false;
      }
    }
    return true;
  }
  function areDataViewsEqual(a, b) {
    return a.byteLength === b.byteLength && areTypedArraysEqual(new Uint8Array(a.buffer, a.byteOffset, a.byteLength), new Uint8Array(b.buffer, b.byteOffset, b.byteLength));
  }
  function areDatesEqual(a, b) {
    return sameValueEqual(a.getTime(), b.getTime());
  }
  function areErrorsEqual(a, b) {
    return a.name === b.name && a.message === b.message && a.cause === b.cause && a.stack === b.stack;
  }
  function areMapsEqual(a, b, state) {
    const size = a.size;
    if (size !== b.size) {
      return false;
    }
    if (!size) {
      return true;
    }
    const matchedIndices = new Array(size);
    const aIterable = a.entries();
    let aResult;
    let bResult;
    let index = 0;
    while (aResult = aIterable.next()) {
      if (aResult.done) {
        break;
      }
      const bIterable = b.entries();
      let hasMatch = false;
      let matchIndex = 0;
      while (bResult = bIterable.next()) {
        if (bResult.done) {
          break;
        }
        if (matchedIndices[matchIndex]) {
          matchIndex++;
          continue;
        }
        const aEntry = aResult.value;
        const bEntry = bResult.value;
        if (state.equals(aEntry[0], bEntry[0], index, matchIndex, a, b, state) && state.equals(aEntry[1], bEntry[1], aEntry[0], bEntry[0], a, b, state)) {
          hasMatch = matchedIndices[matchIndex] = true;
          break;
        }
        matchIndex++;
      }
      if (!hasMatch) {
        return false;
      }
      index++;
    }
    return true;
  }
  function areObjectsEqual(a, b, state) {
    const properties = keys(a);
    let index = properties.length;
    if (keys(b).length !== index) {
      return false;
    }
    while (index-- > 0) {
      if (!isPropertyEqual(a, b, state, properties[index])) {
        return false;
      }
    }
    return true;
  }
  function areObjectsEqualStrict(a, b, state) {
    const properties = getStrictProperties(a);
    let index = properties.length;
    if (getStrictProperties(b).length !== index) {
      return false;
    }
    let property;
    let descriptorA;
    let descriptorB;
    while (index-- > 0) {
      property = properties[index];
      if (!isPropertyEqual(a, b, state, property)) {
        return false;
      }
      descriptorA = getOwnPropertyDescriptor(a, property);
      descriptorB = getOwnPropertyDescriptor(b, property);
      if ((descriptorA || descriptorB) && (!descriptorA || !descriptorB || descriptorA.configurable !== descriptorB.configurable || descriptorA.enumerable !== descriptorB.enumerable || descriptorA.writable !== descriptorB.writable)) {
        return false;
      }
    }
    return true;
  }
  function arePrimitiveWrappersEqual(a, b) {
    return sameValueEqual(a.valueOf(), b.valueOf());
  }
  function areRegExpsEqual(a, b) {
    return a.source === b.source && a.flags === b.flags;
  }
  function areSetsEqual(a, b, state) {
    const size = a.size;
    if (size !== b.size) {
      return false;
    }
    if (!size) {
      return true;
    }
    const matchedIndices = new Array(size);
    const aIterable = a.values();
    let aResult;
    let bResult;
    while (aResult = aIterable.next()) {
      if (aResult.done) {
        break;
      }
      const bIterable = b.values();
      let hasMatch = false;
      let matchIndex = 0;
      while (bResult = bIterable.next()) {
        if (bResult.done) {
          break;
        }
        if (!matchedIndices[matchIndex] && state.equals(aResult.value, bResult.value, aResult.value, bResult.value, a, b, state)) {
          hasMatch = matchedIndices[matchIndex] = true;
          break;
        }
        matchIndex++;
      }
      if (!hasMatch) {
        return false;
      }
    }
    return true;
  }
  function areTypedArraysEqual(a, b) {
    let index = a.byteLength;
    if (b.byteLength !== index || a.byteOffset !== b.byteOffset) {
      return false;
    }
    while (index-- > 0) {
      if (a[index] !== b[index]) {
        return false;
      }
    }
    return true;
  }
  function areUrlsEqual(a, b) {
    return a.hostname === b.hostname && a.pathname === b.pathname && a.protocol === b.protocol && a.port === b.port && a.hash === b.hash && a.username === b.username && a.password === b.password;
  }
  function isPropertyEqual(a, b, state, property) {
    if ((property === REACT_OWNER || property === PREACT_OWNER || property === PREACT_VNODE) && (a.$$typeof || b.$$typeof)) {
      return true;
    }
    return hasOwn(b, property) && state.equals(a[property], b[property], property, property, a, b, state);
  }
  function createEqualityComparator(config) {
    const supportedComparatorMap = createSupportedComparatorMap(config);
    const {areArraysEqual: areArraysEqual2, areDatesEqual: areDatesEqual2, areFunctionsEqual, areMapsEqual: areMapsEqual2, areNumbersEqual, areObjectsEqual: areObjectsEqual2, areRegExpsEqual: areRegExpsEqual2, areSetsEqual: areSetsEqual2, getUnsupportedCustomComparator} = config;
    return function comparator2(a, b, state) {
      if (a === b) {
        return true;
      }
      if (a == null || b == null) {
        return false;
      }
      const type = typeof a;
      if (type !== typeof b) {
        return false;
      }
      if (type !== "object") {
        if (type === "number" || type === "bigint") {
          return areNumbersEqual(a, b, state);
        }
        if (type === "function") {
          return areFunctionsEqual(a, b, state);
        }
        return false;
      }
      const constructor = a.constructor;
      if (constructor !== b.constructor) {
        return false;
      }
      if (constructor === Object) {
        return areObjectsEqual2(a, b, state);
      }
      if (constructor === Array) {
        return areArraysEqual2(a, b, state);
      }
      if (constructor === Date) {
        return areDatesEqual2(a, b, state);
      }
      if (constructor === RegExp) {
        return areRegExpsEqual2(a, b, state);
      }
      if (constructor === Map) {
        return areMapsEqual2(a, b, state);
      }
      if (constructor === Set) {
        return areSetsEqual2(a, b, state);
      }
      if (constructor === Promise) {
        return false;
      }
      if (Array.isArray(a)) {
        return areArraysEqual2(a, b, state);
      }
      const tag = toString.call(a);
      const supportedComparator = supportedComparatorMap[tag];
      if (supportedComparator) {
        return supportedComparator(a, b, state);
      }
      const unsupportedCustomComparator = getUnsupportedCustomComparator && getUnsupportedCustomComparator(a, b, state, tag);
      if (unsupportedCustomComparator) {
        return unsupportedCustomComparator(a, b, state);
      }
      return false;
    };
  }
  function createEqualityComparatorConfig({circular, createCustomConfig, strict}) {
    let config = {
      areArrayBuffersEqual,
      areArraysEqual: strict ? areObjectsEqualStrict : areArraysEqual,
      areDataViewsEqual,
      areDatesEqual,
      areErrorsEqual,
      areFunctionsEqual: strictEqual,
      areMapsEqual: strict ? combineComparators(areMapsEqual, areObjectsEqualStrict) : areMapsEqual,
      areNumbersEqual: sameValueEqual,
      areObjectsEqual: strict ? areObjectsEqualStrict : areObjectsEqual,
      arePrimitiveWrappersEqual,
      areRegExpsEqual,
      areSetsEqual: strict ? combineComparators(areSetsEqual, areObjectsEqualStrict) : areSetsEqual,
      areTypedArraysEqual: strict ? combineComparators(areTypedArraysEqual, areObjectsEqualStrict) : areTypedArraysEqual,
      areUrlsEqual,
      getUnsupportedCustomComparator: void 0
    };
    if (createCustomConfig) {
      config = Object.assign({}, config, createCustomConfig(config));
    }
    if (circular) {
      const areArraysEqual2 = createIsCircular(config.areArraysEqual);
      const areMapsEqual2 = createIsCircular(config.areMapsEqual);
      const areObjectsEqual2 = createIsCircular(config.areObjectsEqual);
      const areSetsEqual2 = createIsCircular(config.areSetsEqual);
      config = Object.assign({}, config, {
        areArraysEqual: areArraysEqual2,
        areMapsEqual: areMapsEqual2,
        areObjectsEqual: areObjectsEqual2,
        areSetsEqual: areSetsEqual2
      });
    }
    return config;
  }
  function createInternalEqualityComparator(compare2) {
    return function (a, b, _indexOrKeyA, _indexOrKeyB, _parentA, _parentB, state) {
      return compare2(a, b, state);
    };
  }
  function createIsEqual({circular, comparator: comparator2, createState, equals, strict}) {
    if (createState) {
      return function isEqual(a, b) {
        const {cache = circular ? /* @__PURE__ */ new WeakMap() : void 0, meta} = createState();
        return comparator2(a, b, {
          cache,
          equals,
          meta,
          strict
        });
      };
    }
    if (circular) {
      return function isEqual(a, b) {
        return comparator2(a, b, {
          cache: /* @__PURE__ */ new WeakMap(),
          equals,
          meta: void 0,
          strict
        });
      };
    }
    const state = {
      cache: void 0,
      equals,
      meta: void 0,
      strict
    };
    return function isEqual(a, b) {
      return comparator2(a, b, state);
    };
  }
  function createSupportedComparatorMap({areArrayBuffersEqual: areArrayBuffersEqual2, areArraysEqual: areArraysEqual2, areDataViewsEqual: areDataViewsEqual2, areDatesEqual: areDatesEqual2, areErrorsEqual: areErrorsEqual2, areFunctionsEqual, areMapsEqual: areMapsEqual2, areNumbersEqual, areObjectsEqual: areObjectsEqual2, arePrimitiveWrappersEqual: arePrimitiveWrappersEqual2, areRegExpsEqual: areRegExpsEqual2, areSetsEqual: areSetsEqual2, areTypedArraysEqual: areTypedArraysEqual2, areUrlsEqual: areUrlsEqual2}) {
    return {
      "[object Arguments]": areObjectsEqual2,
      "[object Array]": areArraysEqual2,
      "[object ArrayBuffer]": areArrayBuffersEqual2,
      "[object AsyncGeneratorFunction]": areFunctionsEqual,
      "[object BigInt]": areNumbersEqual,
      "[object BigInt64Array]": areTypedArraysEqual2,
      "[object BigUint64Array]": areTypedArraysEqual2,
      "[object Boolean]": arePrimitiveWrappersEqual2,
      "[object DataView]": areDataViewsEqual2,
      "[object Date]": areDatesEqual2,
      // If an error tag, it should be tested explicitly. Like RegExp, the properties are not
      // enumerable, and therefore will give false positives if tested like a standard object.
      "[object Error]": areErrorsEqual2,
      "[object Float16Array]": areTypedArraysEqual2,
      "[object Float32Array]": areTypedArraysEqual2,
      "[object Float64Array]": areTypedArraysEqual2,
      "[object Function]": areFunctionsEqual,
      "[object GeneratorFunction]": areFunctionsEqual,
      "[object Int8Array]": areTypedArraysEqual2,
      "[object Int16Array]": areTypedArraysEqual2,
      "[object Int32Array]": areTypedArraysEqual2,
      "[object Map]": areMapsEqual2,
      "[object Number]": arePrimitiveWrappersEqual2,
      "[object Object]": (a, b, state) => (
        // The exception for value comparison is custom `Promise`-like class instances. These should
        // be treated the same as standard `Promise` objects, which means strict equality, and if
        // it reaches this point then that strict equality comparison has already failed.
        typeof a.then !== "function" && typeof b.then !== "function" && areObjectsEqual2(a, b, state)
      ),
      // For RegExp, the properties are not enumerable, and therefore will give false positives if
      // tested like a standard object.
      "[object RegExp]": areRegExpsEqual2,
      "[object Set]": areSetsEqual2,
      "[object String]": arePrimitiveWrappersEqual2,
      "[object URL]": areUrlsEqual2,
      "[object Uint8Array]": areTypedArraysEqual2,
      "[object Uint8ClampedArray]": areTypedArraysEqual2,
      "[object Uint16Array]": areTypedArraysEqual2,
      "[object Uint32Array]": areTypedArraysEqual2
    };
  }
  function createCustomEqual(options = {}) {
    const {circular = false, createInternalComparator: createCustomInternalComparator, createState, strict = false} = options;
    const config = createEqualityComparatorConfig(options);
    const comparator2 = createEqualityComparator(config);
    const equals = createCustomInternalComparator ? createCustomInternalComparator(comparator2) : createInternalEqualityComparator(comparator2);
    return createIsEqual({circular, comparator: comparator2, createState, equals, strict});
  }
  var getOwnPropertyNames, getOwnPropertySymbols, hasOwnProperty, hasOwn, PREACT_VNODE, PREACT_OWNER, REACT_OWNER, getOwnPropertyDescriptor, keys, sameValueEqual, toString, deepEqual, strictDeepEqual, circularDeepEqual, strictCircularDeepEqual, shallowEqual, strictShallowEqual, circularShallowEqual, strictCircularShallowEqual;
  var init_es = __esm({
    "node_modules/fast-equals/dist/es/index.mjs"() {
      ({getOwnPropertyNames, getOwnPropertySymbols} = Object);
      ({hasOwnProperty} = Object.prototype);
      hasOwn = // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
        Object.hasOwn || ((object, property) => hasOwnProperty.call(object, property));
      PREACT_VNODE = "__v";
      PREACT_OWNER = "__o";
      REACT_OWNER = "_owner";
      ({getOwnPropertyDescriptor, keys} = Object);
      sameValueEqual = // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
        Object.is || function sameValueEqual2(a, b) {
          return a === b ? a !== 0 || 1 / a === 1 / b : a !== a && b !== b;
        };
      toString = Object.prototype.toString;
      deepEqual = createCustomEqual();
      strictDeepEqual = createCustomEqual({strict: true});
      circularDeepEqual = createCustomEqual({circular: true});
      strictCircularDeepEqual = createCustomEqual({
        circular: true,
        strict: true
      });
      shallowEqual = createCustomEqual({
        createInternalComparator: () => sameValueEqual
      });
      strictShallowEqual = createCustomEqual({
        strict: true,
        createInternalComparator: () => sameValueEqual
      });
      circularShallowEqual = createCustomEqual({
        circular: true,
        createInternalComparator: () => sameValueEqual
      });
      strictCircularShallowEqual = createCustomEqual({
        circular: true,
        createInternalComparator: () => sameValueEqual,
        strict: true
      });
    }
  });

  // src/InAccord/ui/hooks.ts
  function useStateFromStores(stores, factory, deps, areStateEqual = (oldState, newState) => oldState === newState) {
    const [, forceUpdate] = useForceUpdate();
    const state = useRef(void 0);
    const factoryRef = useRef(void 0);
    const compareStates = useRef(null);
    compareStates.current = areStateEqual === true ? shallowEqual : areStateEqual;
    if (factoryRef.current === void 0) {
      factoryRef.current = factory;
      state.current = factory();
    }
    const prevDeps = useRef(void 0);
    if (deps && prevDeps.current) {
      if (deps.length !== prevDeps.current.length) {
        throw new Error("Dependency List Size Changed!");
      }
      for (let index = 0; index < deps.length; index++) {
        if (Object.is(deps[index], prevDeps.current[index])) {
          continue;
        }
        factoryRef.current = factory;
        const newState = factory();
        if (!compareStates.current(state.current, newState)) {
          state.current = newState;
        }
        break;
      }
    } else {
      factoryRef.current = factory;
    }
    prevDeps.current = deps;
    useInsertionEffect(() => {
      const $stores = Array.isArray(stores) ? stores : [stores];
      function listener() {
        const newState = factoryRef.current();
        if (!compareStates.current(state.current, newState)) {
          state.current = newState;
          forceUpdate();
        }
      }
      for (const store of $stores) {
        store.addChangeListener(listener);
      }
      return () => {
        for (const store of $stores) {
          store.removeChangeListener(listener);
        }
      };
    }, []);
    return state.current;
  }
  function useForceUpdate() {
    return useReducer((num) => num + 1, 0);
  }
  var init_hooks = __esm({
    "src/InAccord/ui/hooks.ts"() {
      "use strict";
      init_react();
      init_es();
    }
  });

  // src/InAccord/ui/customcss/editor.tsx
  function makeButton(button, value) {
    return /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", text: button.tooltip}, (props) => {
      return /* @__PURE__ */ react_default.createElement(Button, {
        ...props, "aria-label": button.tooltip, size: Button.Sizes.ICON, look: Button.Looks.BLANK, onClick: (event) => {
          button.onClick(event, value?.());
        }
      }, button.label);
    });
  }
  function makeSwitch(control) {
    return /* @__PURE__ */ react_default.createElement(Flex, {align: Flex.Align.CENTER, style: {gap: "10px"}}, /* @__PURE__ */ react_default.createElement(Text, null, control.label), /* @__PURE__ */ react_default.createElement(Switch, {onChange: control.onChange, value: control.checked}));
  }
  function buildControl(value, control) {
    if (control.type == "boolean") return makeSwitch(control);
    return makeButton(control, value);
  }
  var useState5, useCallback6, useEffect4, forwardRef2, useMemo4, useImperativeHandle2, languages, editor_default3;
  var init_editor3 = __esm({
    "src/InAccord/ui/customcss/editor.tsx"() {
      "use strict";
      init_react();
      init_discordmodules();
      init_settings3();
      init_editor();
      init_editor2();
      init_button();
      init_flex();
      init_switch();
      init_text();
      init_hooks();
      init_react();
      init_lucide_react();
      ({useState: useState5, useCallback: useCallback6, useEffect: useEffect4, forwardRef: forwardRef2, useMemo: useMemo4, useImperativeHandle: useImperativeHandle2} = react_default);
      languages = ["abap", "abc", "actionscript", "ada", "apache_conf", "asciidoc", "assembly_x86", "autohotkey", "batchfile", "bro", "c_cpp", "c9search", "cirru", "clojure", "cobol", "coffee", "coldfusion", "csharp", "csound_document", "csound_orchestra", "csound_score", "css", "curly", "d", "dart", "diff", "dockerfile", "dot", "drools", "dummy", "dummysyntax", "eiffel", "ejs", "elixir", "elm", "erlang", "forth", "fortran", "ftl", "gcode", "gherkin", "gitignore", "glsl", "gobstones", "golang", "graphqlschema", "groovy", "haml", "handlebars", "haskell", "haskell_cabal", "haxe", "hjson", "html", "html_elixir", "html_ruby", "ini", "io", "jack", "jade", "java", "javascript", "json", "jsoniq", "jsp", "jssm", "jsx", "julia", "kotlin", "latex", "less", "liquid", "lisp", "livescript", "logiql", "lsl", "lua", "luapage", "lucene", "makefile", "markdown", "mask", "matlab", "maze", "mel", "mushcode", "mysql", "nix", "nsis", "objectivec", "ocaml", "pascal", "perl", "pgsql", "php", "pig", "powershell", "praat", "prolog", "properties", "protobuf", "python", "r", "razor", "rdoc", "red", "rhtml", "rst", "ruby", "rust", "sass", "scad", "scala", "scheme", "scss", "sh", "sjs", "smarty", "snippets", "soy_template", "space", "sql", "sqlserver", "stylus", "svg", "swift", "tcl", "tex", "text", "textile", "toml", "tsx", "twig", "typescript", "vala", "vbscript", "velocity", "verilog", "vhdl", "wollok", "xml", "xquery", "yaml", "django"];
      editor_default3 = forwardRef2(function CodeEditor({value, language: requestedLang = "css", id = "ia-editor", controls = [], onChange: notifyParent}, editorRef) {
        const ref = useRef(null);
        const windowRef = useRef(null);
        const language = useMemo4(() => {
          const requested = requestedLang.toLowerCase().replace(/ /g, "_");
          if (!languages.includes(requested)) return "css";
          return requested;
        }, [requestedLang]);
        const [theme, setTheme] = useState5(() => editor_default.getEditorOptions().theme);
        const [editor, setEditor] = useState5();
        const [, setBindings] = useState5([]);
        const [selection, setSelection] = useState5([0, 0, 0]);
        const [markerInfo, setInfo] = useState5([0, 0, []]);
        const {insertSpaces, tabSize} = useStateFromStores(settings_default2, () => ({
          insertSpaces: settings_default2.get("settings", "editor", "insertSpaces"),
          tabSize: settings_default2.get("settings", "editor", "tabSize")
        }));
        const [showingProblems, setShowingProblems] = useState5(false);
        const toggleShowingProblems = useCallback6(() => setShowingProblems((v) => !v), []);
        const onChange = useCallback6(() => {
          if (editor) notifyParent?.(editor.getValue());
        }, [editor, notifyParent]);
        const resize = useCallback6(() => editor?.layout(), [editor]);
        useImperativeHandle2(editorRef, () => {
          return {
            resize,
            get value() {
              return editor.getValue();
            },
            set value(newValue) {
              editor.setValue(newValue);
            }
          };
        }, [editor, resize]);
        useEffect4(() => {
          setBindings((bins) => [...bins, editor?.onDidChangeModelContent(onChange)]);
          return () => {
            setBindings((bins) => {
              for (const binding of bins) binding?.dispose();
              return [];
            });
          };
        }, [editor, onChange]);
        useLayoutEffect(() => {
          const node = ref.current || document.getElementById(id);
          if (!node) return;
          const createFallback = () => {
            const textarea = document.createElement("textarea");
            textarea.className = "ia-fallback-editor";
            textarea.value = value;
            setEditor({
              dispose: () => textarea.remove(),
              getValue: () => textarea.value,
              setValue: (val) => textarea.value = val,
              layout: () => {
              },
              onDidChangeModelContent: ((cb) => {
                textarea.onchange = cb;
                textarea.oninput = cb;
                return {
                  dispose() {
                  }
                };
              }),
              // @ts-expect-error For the footer
              isFallback: true
            });
            node.appendChild(textarea);
          };
          if (editor_default2.fialedToLoad) {
            createFallback();
            return;
          }
          let disposed = false;
          let disposer;
          const createMonaco = () => {
            const getOptions2 = () => ({
              value,
              language,
              ...editor_default.getEditorOptions()
            });
            const onDidChangeMarkers = window.monaco.editor.onDidChangeMarkers(([uri]) => {
              if (monacoEditor.getModel().uri !== uri) {
                return;
              }
              const markers = window.monaco.editor.getModelMarkers({resource: uri});
              let errors = 0;
              let warnings = 0;
              for (const element of markers) {
                if (element.severity === 4) warnings++;
                else if (element.severity === 8) errors++;
              }
              setInfo([errors, warnings, markers]);
            });
            const monacoEditor = window.monaco.editor.create(node, getOptions2());
            setEditor(monacoEditor);
            monacoEditor.onDidChangeCursorSelection(() => {
              const position = monacoEditor.getPosition();
              const $selection = monacoEditor.getSelection();
              const selectedText = monacoEditor.getModel().getValueInRange($selection);
              setSelection([position.lineNumber, position.column, selectedText.length]);
            });
            function updateThemingVars() {
              const styles2 = getComputedStyle(monacoEditor.getDomNode());
              const background = styles2.getPropertyValue("--vscode-editor-background");
              const foreground = styles2.getPropertyValue("--vscode-foreground");
              const tableColumnsBorder = styles2.getPropertyValue("--vscode-tree-tableColumnsBorder");
              let contianer = windowRef.current.parentElement.parentElement;
              if (!contianer || !contianer.classList.contians("floating-window")) contianer = windowRef.current;
              if (contianer) {
                contianer.style.setProperty("--ia-editor-background", background);
                contianer.style.setProperty("--ia-editor-foreground", foreground);
                contianer.style.setProperty("--ia-editor-divider", tableColumnsBorder);
              }
            }
            const undo = editor_default.addChangeListener(() => {
              monacoEditor.updateOptions(getOptions2());
              setTheme(editor_default.getEditorOptions().theme);
              updateThemingVars();
            });
            updateThemingVars();
            disposer = () => {
              monacoEditor.dispose();
              undo();
              onDidChangeMarkers.dispose();
            };
            if (disposed) disposer();
          };
          if (window.monaco?.editor) {
            createMonaco();
          } else {
            editor_default2.initialize().then(() => {
              if (window.monaco?.editor) createMonaco();
              else createFallback();
            });
          }
          return () => {
            disposed = true;
            disposer?.();
          };
        }, [id, language, value]);
        useEffect4(() => {
          window.addEventListener("resize", resize);
          return () => {
            window.removeEventListener("resize", resize);
          };
        }, [resize]);
        if (editor && editor.layout) editor.layout();
        const gotoLine = useCallback6(() => {
          editor.focus();
          editor.trigger("keyboard", "editor.action.gotoLine", "");
        }, [editor]);
        const controlsLeft = controls.filter((c) => c.side != "right").map(buildControl.bind(null, () => editor?.getValue()));
        const controlsRight = controls.filter((c) => c.side == "right").map(buildControl.bind(null, () => editor?.getValue()));
        return /* @__PURE__ */ react_default.createElement("div", {id: "ia-editor-panel", className: theme, ref: windowRef}, /* @__PURE__ */ react_default.createElement("div", {id: "ia-editor-controls"}, /* @__PURE__ */ react_default.createElement("div", {className: "controls-section controls-left"}, controlsLeft), /* @__PURE__ */ react_default.createElement("div", {className: "controls-section controls-right"}, controlsRight)), /* @__PURE__ */ react_default.createElement("div", {className: "editor-wrapper"}, /* @__PURE__ */ react_default.createElement("div", {id, ref, className: "editor " + theme}), showingProblems && /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-problems"}, markerInfo[2].length === 0 && /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-no-problems"}, "No Problems have been detected."), markerInfo[2].map((marker, index) => /* @__PURE__ */ react_default.createElement("div", {key: index, className: `ia-editor-problem ia-editor-severity-${marker.severity}`}, marker.severity === 8 ? (
          // <span className="codicon codicon-error" />
          /* @__PURE__ */ react_default.createElement(CircleX, {size: "1em"})
        ) : marker.severity === 4 ? (
          // <span className="codicon codicon-warning" />
          /* @__PURE__ */ react_default.createElement(TriangleAlert, {size: "1em"})
        ) : marker.severity === 2 ? (
          // <span className="codicon codicon-info" />
          /* @__PURE__ */ react_default.createElement(Info, {size: "1em"})
        ) : /* @__PURE__ */ react_default.createElement(Info, {size: "1em"}), /* @__PURE__ */ react_default.createElement("span", null, marker.message), /* @__PURE__ */ react_default.createElement("span", null, marker.source, "(", marker.code, ")"), /* @__PURE__ */ react_default.createElement("span", null, "[Ln ", marker.startLineNumber, ", Col ", marker.startColumn, "]"))))), !editor?.isFallback && /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-footer"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-footer-left"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-footer-item", onClick: toggleShowingProblems}, /* @__PURE__ */ react_default.createElement(CircleX, {size: "1em"}), /* @__PURE__ */ react_default.createElement("span", null, " ", markerInfo[0], " "), /* @__PURE__ */ react_default.createElement(TriangleAlert, {size: "1em"}), /* @__PURE__ */ react_default.createElement("span", null, " ", markerInfo[1]))), /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-footer-right"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-footer-item", onClick: gotoLine}, /* @__PURE__ */ react_default.createElement("span", null, "Ln ", selection[0]), /* @__PURE__ */ react_default.createElement("span", null, ", "), /* @__PURE__ */ react_default.createElement("span", null, "Col ", selection[1]), !!selection[2] && /* @__PURE__ */ react_default.createElement("span", null, " (", selection[2], " selected)")), /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-footer-item"}, /* @__PURE__ */ react_default.createElement("span", null, insertSpaces ? "Spaces" : "Tabs"), /* @__PURE__ */ react_default.createElement("span", null, ": "), /* @__PURE__ */ react_default.createElement("span", null, tabSize)), /* @__PURE__ */ react_default.createElement("div", {className: "ia-editor-footer-item"}, /* @__PURE__ */ react_default.createElement(Braces, {size: "1em"}), /* @__PURE__ */ react_default.createElement("span", null, " ", language)))));
      });
    }
  });

  // src/InAccord/ui/misc/addoneditor.tsx
  var useState6, useCallback7, forwardRef3, useImperativeHandle3, useRef3, addoneditor_default;
  var init_addoneditor = __esm({
    "src/InAccord/ui/misc/addoneditor.tsx"() {
      "use strict";
      init_react();
      init_i18n();
      init_editor3();
      init_lucide_react();
      ({useState: useState6, useCallback: useCallback7, forwardRef: forwardRef3, useImperativeHandle: useImperativeHandle3, useRef: useRef3} = react_default);
      addoneditor_default = forwardRef3(function AddonEditor({content, language, save, openNative, id = "ia-addon-editor"}, ref) {
        const editorRef = useRef3(null);
        const [hasUnsavedChanges, setUnsaved] = useState6(false);
        useImperativeHandle3(ref, () => {
          return {
            resize() {
              editorRef.current?.resize();
            },
            get value() {
              return editorRef.current?.getValue();
            },
            set value(newValue) {
              editorRef.current?.setValue(newValue);
            },
            get hasUnsavedChanges() {
              return hasUnsavedChanges;
            }
          };
        }, [hasUnsavedChanges]);
        const popoutNative = useCallback7(() => openNative?.(), [openNative]);
        const onChange = useCallback7(() => setUnsaved(true), []);
        const saveAddon = useCallback7((_, newCSS) => {
          save?.(newCSS);
          setUnsaved(false);
        }, [save]);
        return /* @__PURE__ */ react_default.createElement(
          editor_default3,
          {
            ref: editorRef,
            language,
            id,
            controls: [
              {label: /* @__PURE__ */ react_default.createElement(Save, {size: "18px"}), tooltip: t("CustomCSS.save"), onClick: saveAddon},
              {label: /* @__PURE__ */ react_default.createElement(RotateCw, {size: "18px"}), tooltip: t("CustomCSS.openNative"), onClick: popoutNative}
            ],
            value: content,
            onChange
          }
        );
      });
    }
  });

  // src/InAccord/ui/floating/window.tsx
  function confirmClose(confirmationText) {
    return new Promise((resolve) => {
      Modals.showConfirmationModal(t("Modals.confirmAction"), confirmationText, {
        danger: true,
        confirmText: t("Modals.close"),
        onConfirm: () => {
          resolve(true);
        },
        onCancel: () => {
          resolve(false);
        }
      });
    });
  }
  function FloatingWindow({id, title, resizable, children, className, center, top: initialTop = 0, left: initialLeft = 0, width: initialWidth = 410, height: initialHeight = 470, minX = 0, minY = 0, maxX = -1, maxY = -1, onResize, onClose, confirmClose: doConfirmClose, confirmationText}) {
    const [modalOpen, setOpen] = useState7(false);
    const max = useRef4({x: maxX, y: maxY});
    max.current.x = maxX;
    max.current.y = maxY;
    const positioning = useRef4({
      offset: {x: 0, y: 0},
      size: {width: 0, height: 0},
      max: {
        get x() {
          return max.current.x === -1 ? Screen.width : max.current.x;
        },
        get y() {
          return max.current.y === -1 ? Screen.height : max.current.y;
        }
      },
      min: {x: minX, y: minY},
      position: {x: 0, y: 0},
      isDragging: false
    });
    positioning.current.min.x = minX;
    positioning.current.min.y = minY;
    const titlebar = useRef4(null);
    const window2 = useRef4(null);
    const onResizeStart = useCallback8(() => {
      positioning.current.size.width = window2.current.offsetWidth;
      positioning.current.size.height = window2.current.offsetHeight;
    }, [window2]);
    const onDrag = useCallback8((e) => {
      if (!positioning.current.isDragging) return;
      let newTop = e.clientY - positioning.current.offset.y;
      if (newTop <= positioning.current.min.y) newTop = positioning.current.min.y;
      if (newTop + positioning.current.size.height >= positioning.current.max.y) newTop = positioning.current.max.y - positioning.current.size.height;
      let newLeft = e.clientX - positioning.current.offset.x;
      if (newLeft <= positioning.current.min.x) newLeft = positioning.current.min.x;
      if (newLeft + positioning.current.size.width >= positioning.current.max.x) newLeft = positioning.current.max.x - positioning.current.size.width;
      positioning.current.position.x = newLeft;
      positioning.current.position.y = newTop;
      window2.current.style.left = `${newLeft}px`;
      window2.current.style.top = `${newTop}px`;
    }, []);
    const onDragStart = useCallback8((e) => {
      const div = window2.current;
      positioning.current.offset.x = e.clientX - div.offsetLeft;
      positioning.current.offset.y = e.clientY - div.offsetTop;
      positioning.current.isDragging = true;
    }, [window2]);
    const onDragStop = useCallback8(() => {
      positioning.current.isDragging = false;
      const width = window2.current.offsetWidth;
      const height = window2.current.offsetHeight;
      if (width != positioning.current.size.width || height != positioning.current.size.height) {
        if (onResize) onResize();
        const left = parseInt(window2.current.style.left);
        const top = parseInt(window2.current.style.top);
        if (left + width >= positioning.current.max.x) window2.current.style.width = positioning.current.max.x - left + "px";
        if (top + height >= positioning.current.max.y) window2.current.style.height = positioning.current.max.y - top + "px";
      }
      positioning.current.size.width = width;
      positioning.current.size.height = height;
      window2.current.style.left = `${positioning.current.position.x}px`;
      window2.current.style.top = `${positioning.current.position.y}px`;
    }, [window2, onResize]);
    useEffect5(() => {
      const winRef = window2.current;
      const titleRef = titlebar.current;
      winRef.addEventListener("mousedown", onResizeStart, false);
      titleRef.addEventListener("mousedown", onDragStart, false);
      document.addEventListener("mouseup", onDragStop, false);
      document.addEventListener("mousemove", onDrag, true);
      return () => {
        document.removeEventListener("mouseup", onDragStop, false);
        document.removeEventListener("mousemove", onDrag, true);
        winRef.removeEventListener("mousedown", onResizeStart, false);
        titleRef.removeEventListener("mousedown", onDragStart, false);
      };
    }, [titlebar, window2, onDragStart, onDragStop, onDrag, onResizeStart]);
    const maximize = useCallback8(() => {
      window2.current.style.width = "100%";
      window2.current.style.height = "100%";
      if (onResize) onResize();
      const width = window2.current.offsetWidth;
      const height = window2.current.offsetHeight;
      const left = parseInt(window2.current.style.left);
      const top = parseInt(window2.current.style.top);
      const right = left + width;
      const bottom = top + height;
      if (bottom > positioning.current.max.y) window2.current.style.top = positioning.current.max.y - height + "px";
      if (right > positioning.current.max.x) window2.current.style.left = positioning.current.max.x - width + "px";
      const newLeft = parseInt(window2.current.style.left);
      const newTop = parseInt(window2.current.style.top);
      if (newTop < positioning.current.min.y) {
        const difference = positioning.current.min.y - newTop;
        window2.current.style.top = positioning.current.min.y + "px";
        window2.current.style.height = height - difference + "px";
      }
      if (newLeft < positioning.current.min.x) {
        const difference = positioning.current.min.x - newLeft;
        window2.current.style.left = positioning.current.min.x + "px";
        window2.current.style.height = width - difference + "px";
      }
    }, [window2, onResize]);
    const close = useCallback8(async () => {
      let shouldClose = true;
      const didConfirmClose = typeof doConfirmClose == "function" ? doConfirmClose() : doConfirmClose;
      if (didConfirmClose) {
        setOpen(true);
        shouldClose = await confirmClose(confirmationText);
        setOpen(false);
      }
      if (onClose && shouldClose) onClose();
    }, [confirmationText, onClose, doConfirmClose]);
    const finalClassname = `floating-window${className ? ` ${className}` : ""}${resizable ? " resizable" : ""}${modalOpen ? " modal-open" : ""}`;
    useLayoutEffect(() => {
      window2.current.style.height = `${initialHeight}px`;
      window2.current.style.width = `${initialWidth}px`;
      positioning.current.position.x = center ? Screen.width / 2 - initialWidth / 2 : initialLeft;
      positioning.current.position.y = center ? Screen.height / 2 - initialHeight / 2 : initialTop;
      window2.current.style.left = `${positioning.current.position.x}px`;
      window2.current.style.top = `${positioning.current.position.y}px`;
    }, [center, initialHeight, initialLeft, initialTop, initialWidth]);
    return /* @__PURE__ */ react_default.createElement("div", {id, className: finalClassname, ref: window2}, /* @__PURE__ */ react_default.createElement("div", {className: "floating-window-titlebar", ref: titlebar}, /* @__PURE__ */ react_default.createElement("span", {className: "title"}, title), /* @__PURE__ */ react_default.createElement("div", {className: "floating-window-buttons"}, /* @__PURE__ */ react_default.createElement("div", {className: "button maximize-button", onClick: maximize}, /* @__PURE__ */ react_default.createElement(Maximize, {size: "16px"})), /* @__PURE__ */ react_default.createElement("div", {className: "button close-button", onClick: close}, /* @__PURE__ */ react_default.createElement(X, {size: "16px"})))), /* @__PURE__ */ react_default.createElement("div", {className: "floating-window-content"}, children));
  }
  var useState7, useCallback8, useEffect5, useRef4, Screen;
  var init_window = __esm({
    "src/InAccord/ui/floating/window.tsx"() {
      "use strict";
      init_react();
      init_i18n();
      init_modals();
      init_lucide_react();
      init_react();
      ({useState: useState7, useCallback: useCallback8, useEffect: useEffect5, useRef: useRef4} = react_default);
      Screen = class {
        /** Document/window width */
        static get width() {
          return Math.max(document.documentElement.clientWidth, window.innerWidth || 0);
        }
        /** Document/window height */
        static get height() {
          return Math.max(document.documentElement.clientHeight, window.innerHeight || 0);
        }
      };
    }
  });

  // src/InAccord/ui/floating/container.tsx
  function FloatingWindowContianer() {
    const [windows, setWindows] = useState8([]);
    const open = useCallback9((window2) => {
      setWindows((wins) => [...wins, window2]);
    }, []);
    const close = useCallback9((id) => {
      setWindows(windows.filter((w) => {
        if (w.id === id && w.onClose) w.onClose();
        return w.id !== id;
      }));
    }, [windows]);
    useEffect6(() => {
      emitter_default.on("open-window", open);
      return () => void emitter_default.off("open-window", open);
    }, [open]);
    return windows.map(
      (window2) => /* @__PURE__ */ react_default.createElement(FloatingWindow, {...window2, onClose: () => close(window2.id), key: window2.id}, window2.children)
    );
  }
  var useState8, useCallback9, useEffect6;
  var init_container = __esm({
    "src/InAccord/ui/floating/container.tsx"() {
      "use strict";
      init_react();
      init_emitter();
      init_window();
      ({useState: useState8, useCallback: useCallback9, useEffect: useEffect6} = react_default);
    }
  });

  // src/InAccord/ui/floatingwindows.tsx
  var AppLayerProvider, hasInitialized, FloatingWindows;
  var init_floatingwindows = __esm({
    "src/InAccord/ui/floatingwindows.tsx"() {
      "use strict";
      init_react();
      init_reactdom();
      init_emitter();
      init_dommanager();
      init_container();
      init_webpack2();
      AppLayerProvider = getByDisplayName("AppLayerProvider");
      hasInitialized = false;
      FloatingWindows = class {
        static initialize() {
          const contianer = /* @__PURE__ */ react_default.createElement(FloatingWindowContianer, null);
          const wrapped = AppLayerProvider ? react_default.createElement(AppLayerProvider().props.layerContext.Provider, {value: [document.querySelector("#app-mount > .layerContianer-2v_Sit")]}, contianer) : contianer;
          const div = DOMManager.parseHTML(`<div id="floating-windows-layer">`);
          DOMManager.iaBody.append(div);
          const root = reactdom_default.createRoot(div);
          root.render(wrapped);
          hasInitialized = true;
        }
        static open(window2) {
          if (!hasInitialized) this.initialize();
          return emitter_default.emit("open-window", window2);
        }
      };
    }
  });

  // src/InAccord/modules/addonmanager.ts
  var openItem, splitRegex, escapedAtRegex, stripBOM, AddonManager;
  var init_addonmanager = __esm({
    "src/InAccord/modules/addonmanager.ts"() {
      "use strict";
      init_path();
      init_fs();
      init_logger();
      init_addonerror();
      init_settings3();
      init_emitter();
      init_json();
      init_toasts();
      init_react();
      init_i18n();
      init_ipc();
      init_addoneditor();
      init_floatingwindows();
      init_base();
      init_remote();
      openItem = ipc_default.openPath;
      splitRegex = /[^\S\r\n]*?\r?(?:\r\n|\n)[^\S\r\n]*?\*[^\S\r\n]?/;
      escapedAtRegex = /^\\@/;
      stripBOM = function (fileContent) {
        if (fileContent.charCodeAt(0) === 65279) {
          fileContent = fileContent.slice(1);
        }
        return fileContent;
      };
      AddonManager = class extends Store {
        get name() {
          return "";
        }
        get extension() {
          return "";
        }
        get duplicatePattern() {
          return /./;
        }
        get addonFolder() {
          return "";
        }
        get language() {
          return "";
        }
        get prefix() {
          return "";
        }
        get order() {
          return 2;
        }
        trigger(event, ...args) {
          super.emitChange();
          return emitter_default.emit(`${this.prefix}-${event}`, ...args);
        }
        timeCache = {};
        state = {};
        windows = /* @__PURE__ */ new Set();
        hasInitialized = false;
        initialAddonsLoaded = 0;
        initialize() {
          settings_default2.registerAddonPanel(this);
          const errors = this.loadAllAddons();
          if (this.initialAddonsLoaded > 0) {
            toasts_default.show(t("Addons.manyEnabled", {count: this.initialAddonsLoaded, context: this.prefix}));
          }
          this.hasInitialized = true;
          return errors;
        }
        loadState() {
          const saved = json_default.get(`${this.prefix}s`);
          if (!saved) return;
          Object.assign(this.state, saved);
        }
        saveState() {
          json_default.set(`${this.prefix}s`, this.state);
        }
        watcher;
        watchAddons() {
          if (this.watcher) return Logger.err(this.name, `Already watching ${this.prefix} addons.`);
          Logger.log(this.name, `Starting to watch ${this.prefix} addons.`);
          this.watcher = fs_default.watch(this.addonFolder, {persistent: false}, async (eventType, filename) => {
            if (!eventType || !filename) return;
            const absolutePath = path_default.resolve(this.addonFolder, filename);
            if (!filename.endsWith(this.extension)) {
              const match = filename.match(this.duplicatePattern);
              if (!match) return;
              const ext = match[0];
              const truncated = filename.replace(ext, "");
              const newFilename = truncated + this.extension;
              if (fs_default.existsSync(newFilename)) {
                Logger.warn(this.name, `Duplicate files found: ${filename} and ${newFilename}`);
                return;
              }
              try {
                fs_default.renameSync(absolutePath, path_default.resolve(this.addonFolder, newFilename));
              } catch (error) {
                Logger.err(this.name, `Could not rename file: ${filename} ${newFilename}`, error);
              }
            }
            await new Promise((r2) => setTimeout(r2, 100));
            try {
              const stats = fs_default.statSync(absolutePath);
              if (!stats.isFile()) return;
              if (!stats || !stats.mtimeMs) return;
              if (typeof stats.mtimeMs !== "number") return;
              if (this.timeCache[filename] == stats.mtimeMs) return;
              this.timeCache[filename] = stats.mtimeMs;
              if (eventType == "rename") this.loadAddon(filename, true);
              if (eventType == "change") this.reloadAddon(filename, true);
            } catch (err) {
              if (err.code !== "ENOENT" && !err?.message.startsWith("ENOENT")) return;
              delete this.timeCache[filename];
              this.unloadAddon(filename, true);
            }
          });
        }
        unwatchAddons() {
          if (!this.watcher) return Logger.error(this.name, `Was not watching ${this.prefix} addons.`);
          this.watcher.close();
          delete this.watcher;
          Logger.log(this.name, `No longer watching ${this.prefix} addons.`);
        }
        extractMeta(fileContent, filename) {
          const firstLine = fileContent.split("\n")[0];
          const hasMetaComment = firstLine.includes("/**");
          if (!hasMetaComment) throw new AddonError(filename, filename, t("Addons.metaNotFound"), {message: "", stack: fileContent}, this.prefix);
          const metianfo = this.parseJSDoc(fileContent);
          if (!metianfo.author || typeof metianfo.author !== "string") metianfo.author = t("Addons.unknownAuthor");
          if (!metianfo.version || typeof metianfo.version !== "string") metianfo.version = "???";
          if (!metianfo.description || typeof metianfo.description !== "string") metianfo.description = t("Addons.noDescription");
          return metianfo;
        }
        parseJSDoc(fileContent) {
          const block = fileContent.split("/**", 2)[1].split("*/", 1)[0];
          const out = {};
          let field = "";
          let accum = "";
          for (const line of block.split(splitRegex)) {
            if (line.length === 0) continue;
            if (line.charAt(0) === "@" && line.charAt(1) !== " ") {
              if (!out[field]) {
                out[field] = accum.trim();
              } else {
                if (!Array.isArray(out[field])) out[field] = [out[field]];
                out[field].push(accum.trim());
              }
              const l = line.indexOf(" ");
              field = line.substring(1, l);
              accum = line.substring(l + 1);
            } else {
              accum += " " + line.replace("\\n", "\n").replace(escapedAtRegex, "@");
            }
          }
          if (!out[field]) {
            out[field] = accum.trim();
          } else {
            if (!Array.isArray(out[field])) out[field] = [out[field]];
            out[field].push(accum.trim());
          }
          delete out[""];
          out.format = "jsdoc";
          return out;
        }
        // Subclasses should overload this and modify the addon using the fileContent as needed to "require()"" the file
        requireAddon(filename) {
          let fileContent = fs_default.readFileSync(filename, "utf8");
          fileContent = stripBOM(fileContent);
          const stats = fs_default.statSync(filename);
          const addon = this.extractMeta(fileContent, path_default.basename(filename));
          if (!addon.author) addon.author = t("Addons.unknownAuthor");
          if (!addon.version) addon.version = "???";
          if (!addon.description) addon.description = t("Addons.noDescription");
          addon.id = addon.name || path_default.basename(filename);
          addon.slug = path_default.basename(filename).replace(this.extension, "").replace(/ /g, "-");
          addon.filename = path_default.basename(filename);
          addon.added = stats.atimeMs;
          addon.modified = stats.mtimeMs;
          addon.size = stats.size;
          addon.fileContent = fileContent;
          if (this.addonList.find((c) => c.id == addon.id)) throw new AddonError(addon.name, filename, t("Addons.alreadyExists", {context: this.prefix, name: addon.name}), {}, this.prefix);
          this.addonList.push(addon);
          return addon;
        }
        // Subclasses should use the return (if not AddonError) and push to this.addonList
        loadAddon(filename, shouldToast = false) {
          if (typeof filename === "undefined") return;
          let addon;
          try {
            addon = this.requireAddon(path_default.resolve(this.addonFolder, filename));
          } catch (e) {
            const partialAddon = this.addonList.find((c) => c.filename == filename);
            if (partialAddon) {
              partialAddon.partial = true;
              this.state[partialAddon.id] = false;
              this.trigger("loaded", partialAddon);
            }
            return e;
          }
          const error = this.initializeAddon(addon);
          if (error) {
            this.state[addon.id] = false;
            addon.partial = true;
            this.trigger("loaded", addon);
            return error;
          }
          if (shouldToast) toasts_default.success(t("Addons.wasLoaded", {name: addon.name, version: addon.version}));
          this.trigger("loaded", addon);
          if (!this.state[addon.id]) return this.state[addon.id] = false;
          return this.startAddon(addon);
        }
        unloadAddon(idOrFileOrAddon, shouldToast = true, isReload = false) {
          const addon = typeof idOrFileOrAddon == "string" ? this.addonList.find((c) => c.id == idOrFileOrAddon || c.filename == idOrFileOrAddon) : idOrFileOrAddon;
          if (!addon) return false;
          if (this.state[addon.id]) {
            if (isReload) this.stopAddon(addon);
            else this.disableAddon(addon);
          }
          this.addonList.splice(this.addonList.indexOf(addon), 1);
          this.trigger("unloaded", addon);
          if (shouldToast) toasts_default.success(t("Addons.wasUnloaded", {name: addon.name}));
          return true;
        }
        reloadAddon(idOrFileOrAddon, shouldToast = true) {
          const addon = typeof idOrFileOrAddon == "string" ? this.addonList.find((c) => c.id == idOrFileOrAddon || c.filename == idOrFileOrAddon) : idOrFileOrAddon;
          if (!addon) return false;
          const didUnload = this.unloadAddon(addon, shouldToast, true);
          if (addon && !didUnload) return didUnload;
          return this.loadAddon(addon ? addon.filename : idOrFileOrAddon, shouldToast);
        }
        isLoaded(idOrFile) {
          const addon = this.addonList.find((c) => c.id == idOrFile || c.filename == idOrFile);
          if (!addon) return false;
          return true;
        }
        isEnabled(idOrFile) {
          const addon = this.addonList.find((c) => c.id == idOrFile || c.filename == idOrFile);
          if (!addon) return false;
          return this.state[addon.id];
        }
        getAddon(idOrFile) {
          return this.addonList.find((c) => c.id == idOrFile || c.filename == idOrFile);
        }
        enableAddon(idOrAddon) {
          const addon = typeof idOrAddon == "string" ? this.addonList.find((p) => p.id == idOrAddon) : idOrAddon;
          if (!addon || addon.partial) return;
          if (this.state[addon.id]) return;
          this.state[addon.id] = true;
          this.trigger("enabled", addon);
          const err = this.startAddon(addon);
          this.saveState();
          return err;
        }
        enableAllAddons() {
          const originalSetting = settings_default2.get("settings", "general", "showToasts");
          settings_default2.set("settings", "general", "showToasts", false);
          for (let a = 0; a < this.addonList.length; a++) {
            this.enableAddon(this.addonList[a]);
          }
          settings_default2.set("settings", "general", "showToasts", originalSetting);
          this.trigger("batch");
        }
        disableAddon(idOrAddon) {
          const addon = typeof idOrAddon == "string" ? this.addonList.find((p) => p.id == idOrAddon) : idOrAddon;
          if (!addon || addon.partial) return;
          if (!this.state[addon.id]) return;
          this.state[addon.id] = false;
          this.trigger("disabled", addon);
          const err = this.stopAddon(addon);
          this.saveState();
          return err;
        }
        disableAllAddons() {
          const originalSetting = settings_default2.get("settings", "general", "showToasts");
          settings_default2.set("settings", "general", "showToasts", false);
          for (let a = 0; a < this.addonList.length; a++) {
            this.disableAddon(this.addonList[a]);
          }
          settings_default2.set("settings", "general", "showToasts", originalSetting);
          this.trigger("batch");
        }
        toggleAddon(id) {
          if (this.state[id]) this.disableAddon(id);
          else this.enableAddon(id);
        }
        loadNewAddons() {
          const files = fs_default.readdirSync(this.addonFolder);
          const removed = this.addonList.filter((a) => !files.includes(a.filename)).map((c) => c.id);
          const added = files.filter((f) => !this.addonList.find((a) => a.filename == f) && f.endsWith(this.extension) && fs_default.statSync(path_default.resolve(this.addonFolder, f)).isFile());
          return {added, removed};
        }
        updateList() {
          const results = this.loadNewAddons();
          for (const filename of results.added) this.loadAddon(filename);
          for (const name of results.removed) this.unloadAddon(name);
        }
        loadAllAddons() {
          this.loadState();
          const errors = [];
          const files = fs_default.readdirSync(this.addonFolder);
          for (const filename of files) {
            const absolutePath = path_default.resolve(this.addonFolder, filename);
            const stats = fs_default.statSync(absolutePath);
            if (!stats || !stats.isFile()) continue;
            this.timeCache[filename] = stats.mtimeMs;
            if (!filename.endsWith(this.extension)) {
              const match = filename.match(this.duplicatePattern);
              if (!match) continue;
              const ext = match[0];
              const truncated = filename.replace(ext, "");
              const newFilename = truncated + this.extension;
              if (fs_default.existsSync(newFilename)) {
                Logger.warn("AddonManager", `Duplicate files found: ${filename} and ${newFilename}`);
                continue;
              }
              fs_default.renameSync(absolutePath, path_default.resolve(this.addonFolder, newFilename));
            }
            const addon = this.loadAddon(filename, false);
            if (addon instanceof AddonError) errors.push(addon);
            else if (addon !== false) this.initialAddonsLoaded++;
          }
          this.saveState();
          this.watchAddons();
          return errors;
        }
        deleteAddon(idOrFileOrAddon) {
          const addon = typeof idOrFileOrAddon == "string" ? this.addonList.find((c) => c.id == idOrFileOrAddon || c.filename == idOrFileOrAddon) : idOrFileOrAddon;
          if (!addon) return;
          return fs_default.unlinkSync(path_default.resolve(this.addonFolder, addon.filename));
        }
        saveAddon(idOrFileOrAddon, content) {
          const addon = typeof idOrFileOrAddon == "string" ? this.addonList.find((c) => c.id == idOrFileOrAddon || c.filename == idOrFileOrAddon) : idOrFileOrAddon;
          if (!addon) return;
          return fs_default.writeFileSync(path_default.resolve(this.addonFolder, addon.filename), content);
        }
        editAddon(idOrFileOrAddon, system) {
          const addon = typeof idOrFileOrAddon == "string" ? this.addonList.find((c) => c.id == idOrFileOrAddon || c.filename == idOrFileOrAddon) : idOrFileOrAddon;
          if (!addon) return;
          const fullPath = path_default.resolve(this.addonFolder, addon.filename);
          if (typeof system == "undefined") system = settings_default2.get("settings", "addons", "editAction");
          if (system === "system") return openItem(`${fullPath}`);
          else if (system === "external") return remote_default.editor.open(this.prefix, addon.filename);
          return this.openDetached(addon);
        }
        openDetached(addon) {
          const fullPath = path_default.resolve(this.addonFolder, addon.filename);
          const content = fs_default.readFileSync(fullPath).toString();
          if (this.windows.has(fullPath)) return;
          this.windows.add(fullPath);
          const editorRef = react_default.createRef();
          const editor = react_default.createElement(addoneditor_default, {
            id: "ia-floating-editor-" + addon.id,
            ref: editorRef,
            content,
            save: this.saveAddon.bind(this, addon),
            openNative: this.editAddon.bind(this, addon, true),
            language: this.language
          });
          FloatingWindows.open({
            onClose: () => {
              this.windows.delete(fullPath);
            },
            onResize: () => {
              if (!editorRef || !editorRef.current || !editorRef.current.resize) return;
              editorRef.current.resize();
            },
            title: addon.name,
            id: "ia-floating-window-" + addon.id,
            className: "floating-addon-window",
            height: 470,
            width: 410,
            center: true,
            resizable: true,
            children: editor,
            confirmClose: () => {
              if (!editorRef || !editorRef.current) return false;
              return editorRef.current.hasUnsavedChanges;
            },
            confirmationText: t("Addons.confirmationText", {name: addon.name})
          });
        }
      };
    }
  });

  // src/InAccord/modules/pluginmanager.ts
  var pluginmanager_exports = {};
  __export(pluginmanager_exports, {
    default: () => pluginmanager_default
  });
  var normalizeExports, pluginmanager_default;
  var init_pluginmanager = __esm({
    "src/InAccord/modules/pluginmanager.ts"() {
      "use strict";
      init_path();
      init_vm();
      init_logger();
      init_config();
      init_toasts();
      init_addonerror();
      init_addonmanager();
      init_i18n();
      init_emitter();
      init_modals();
      normalizeExports = (name) => `
if (module.exports.default) {
    module.exports = module.exports.default;
}
if (typeof(module.exports) !== "function") {
    module.exports = eval("${name}");
}`;
      pluginmanager_default = new class PluginManager extends AddonManager {
        get name() {
          return "PluginManager";
        }
        get extension() {
          return ".plugin.js";
        }
        get duplicatePattern() {
          return /\.plugin\s?\([0-9]+\)\.js/;
        }
        get addonFolder() {
          return config_default.get("pluginsPath");
        }
        get prefix() {
          return "plugin";
        }
        get language() {
          return "javascript";
        }
        get order() {
          return 3;
        }
        addonList = [];
        observer;
        constructor() {
          super();
          this.onSwitch = this.onSwitch.bind(this);
          this.observer = new MutationObserver((mutations) => {
            for (let i = 0, mlen = mutations.length; i < mlen; i++) {
              this.onMutation(mutations[i]);
            }
          });
        }
        initialize() {
          const errors = super.initialize();
          this.setupFunctions();
          return errors;
        }
        /* Aliases */
        updatePluginList() {
          return this.updateList();
        }
        loadAllPlugins() {
          return this.loadAllAddons();
        }
        enablePlugin(idOrAddon) {
          return this.enableAddon(idOrAddon);
        }
        disablePlugin(idOrAddon) {
          return this.disableAddon(idOrAddon);
        }
        togglePlugin(id) {
          return this.toggleAddon(id);
        }
        unloadPlugin(idOrFileOrAddon) {
          return this.unloadAddon(idOrFileOrAddon);
        }
        loadPlugin(filename) {
          return this.loadAddon(filename);
        }
        loadAddon(filename, shouldCTE = true) {
          const error = super.loadAddon(filename, shouldCTE);
          if (error && shouldCTE) Modals.showAddonErrors({plugins: [error]});
          return error;
        }
        reloadPlugin(idOrFileOrAddon) {
          const error = this.reloadAddon(idOrFileOrAddon);
          if (error) Modals.showAddonErrors({plugins: [error]});
          return typeof idOrFileOrAddon == "string" ? this.addonList.find((c) => c.id == idOrFileOrAddon || c.filename == idOrFileOrAddon) : idOrFileOrAddon;
        }
        /* Overrides */
        initializeAddon(addon) {
          if (!addon.exports || !addon.name) return new AddonError(addon.name || addon.filename, addon.filename, "Plugin had no exports or @name property", {message: "Plugin had no exports or no @name property. @name property is required for all addons.", stack: ""}, this.prefix);
          try {
            const isValid = typeof addon.exports === "function";
            if (!isValid) return new AddonError(addon.name || addon.filename, addon.filename, "Plugin not a valid format.", {message: "Plugins should be either a function or a class", stack: ""}, this.prefix);
            const PluginClass = addon.exports;
            const meta = Object.assign({}, addon);
            delete meta.exports;
            const thePlugin = PluginClass.prototype ? new PluginClass(meta) : addon.exports(meta);
            if (!thePlugin.start || !thePlugin.stop) return new AddonError(addon.name || addon.filename, addon.filename, "Missing start or stop function.", {message: "Plugins must have both a start and stop function.", stack: ""}, this.prefix);
            addon.instance = thePlugin;
            addon.name = thePlugin.getName ? thePlugin.getName() : addon.name;
            addon.author = thePlugin.getAuthor ? thePlugin.getAuthor() : addon.author;
            addon.description = thePlugin.getDescription ? thePlugin.getDescription() : addon.description;
            addon.version = thePlugin.getVersion ? thePlugin.getVersion() : addon.version;
            if (!addon.name || !addon.author || !addon.description || !addon.version) return new AddonError(addon.name || addon.filename, addon.filename, "Plugin is missing name, author, description, or version", {message: "Plugin must provide name, author, description, and version.", stack: ""}, this.prefix);
            try {
              if (typeof addon.instance.load == "function") addon.instance.load();
            } catch (error) {
              this.state[addon.id] = false;
              return new AddonError(addon.name, addon.filename, t("Addons.methodError", {method: "load()"}), {message: error.message, stack: error.stack}, this.prefix);
            }
          } catch (error) {
            return new AddonError(addon.name, addon.filename, t("Addons.methodError", {method: "Plugin constructor()"}), {message: error.message, stack: error.stack}, this.prefix);
          }
        }
        requireAddon(filename) {
          const addon = super.requireAddon(filename);
          try {
            const module = {filename, exports: {}};
            vm_default.compileFunction(addon.fileContent, ["require", "module", "exports", "__filename", "__dirname"], {filename: path_default.basename(filename)});
            addon.fileContent += normalizeExports(addon.exports || addon.name);
            addon.fileContent += `
//# sourceURL=inaccord://plugins/${addon.filename}`;
            const wrappedPlugin = new Function("require", "module", "exports", "__filename", "__dirname", addon.fileContent);
            wrappedPlugin(window.require, module, module.exports, module.filename, this.addonFolder);
            addon.exports = module.exports;
            delete addon.fileContent;
            return addon;
          } catch (err) {
            throw new AddonError(addon.name || addon.filename, filename, t("Addons.compileError"), {message: err.message, stack: err.stack}, this.prefix);
          }
        }
        startAddon(idOrAddon) {
          return this.startPlugin(idOrAddon);
        }
        stopAddon(idOrAddon) {
          return this.stopPlugin(idOrAddon);
        }
        getAddon(id) {
          return this.getPlugin(id);
        }
        startPlugin(idOrAddon) {
          const addon = typeof idOrAddon == "string" ? this.addonList.find((p) => p.id == idOrAddon) : idOrAddon;
          if (!addon) return;
          const plugin = addon.instance;
          try {
            plugin.start();
          } catch (err) {
            this.state[addon.id] = false;
            this.trigger("disabled", addon);
            toasts_default.warning(t("Addons.couldNotStart", {name: addon.name, version: addon.version}));
            Logger.stacktrace(this.name, `${addon.name} v${addon.version} could not be started.`, err);
            return new AddonError(addon.name, addon.filename, t("Addons.methodError", {method: "start()"}), {message: err.message, stack: err.stack}, this.prefix);
          }
          this.trigger("started", addon.id);
          if (this.hasInitialized) toasts_default.success(t("Addons.enabled", {name: addon.name, version: addon.version}));
        }
        stopPlugin(idOrAddon) {
          const addon = typeof idOrAddon == "string" ? this.addonList.find((p) => p.id == idOrAddon) : idOrAddon;
          if (!addon) return;
          const plugin = addon.instance;
          try {
            plugin.stop();
          } catch (err) {
            this.state[addon.id] = false;
            toasts_default.warning(t("Addons.couldNotStop", {name: addon.name, version: addon.version}));
            Logger.stacktrace(this.name, `${addon.name} v${addon.version} could not be started.`, err);
            return new AddonError(addon.name, addon.filename, t("Addons.enabled", {method: "stop()"}), {message: err.message, stack: err.stack}, this.prefix);
          }
          this.trigger("stopped", addon.id);
          toasts_default.error(t("Addons.disabled", {name: addon.name, version: addon.version}));
        }
        getPlugin(idOrFile) {
          const addon = this.addonList.find((c) => c.id == idOrFile || c.filename == idOrFile);
          if (!addon) return;
          return addon;
        }
        setupFunctions() {
          emitter_default.on("navigate", this.onSwitch);
          this.observer.observe(document, {
            childList: true,
            subtree: true
          });
        }
        onSwitch() {
          for (let i = 0; i < this.addonList.length; i++) {
            if (!this.state[this.addonList[i].id]) continue;
            const plugin = this.addonList[i].instance;
            try {
              if (typeof plugin?.onSwitch === "function") {
                plugin.onSwitch();
              }
            } catch (err) {
              Logger.stacktrace(this.name, `Unable to fire onSwitch for ${this.addonList[i].name} v${this.addonList[i].version}`, err);
            }
          }
        }
        onMutation(mutation) {
          for (let i = 0; i < this.addonList.length; i++) {
            if (!this.state[this.addonList[i].id]) continue;
            const plugin = this.addonList[i].instance;
            try {
              if (typeof plugin?.observer === "function") {
                plugin.observer(mutation);
              }
            } catch (err) {
              Logger.stacktrace(this.name, `Unable to fire observer for ${this.addonList[i].name} v${this.addonList[i].version}`, err);
            }
          }
        }
      }();
    }
  });

  // src/InAccord/polyfill/index.ts
  init_events();

  // src/InAccord/polyfill/module.ts
  init_logger();
  init_vm();
  init_remote();
  init_fs();
  var path = remote_default.path;
  var RequireExtensions = {
    ".js": (module, filename) => {
      const fileContent = remote_default.filesystem.readFile(filename, "utf8");
      module.fileContent = fileContent;
      module._compile(fileContent);
      return module.exports;
    },
    ".json": (module, filename) => {
      const fileContent = remote_default.filesystem.readFile(filename, "utf8");
      module.fileContent = fileContent;
      module.exports = JSON.parse(fileContent);
      return module.exports;
    }
  };
  var Module = class _Module {
    static resolveMainFile(mod, basePath) {
      const parent = path.extname(basePath) ? path.dirname(basePath) : basePath;
      const files = remote_default.filesystem.readDirectory(parent);
      if (!Array.isArray(files)) return null;
      for (const file of files) {
        const ext = path.extname(file);
        if (file === "package.json") {
          const pkg = __require(path.resolve(parent, file));
          if (!Reflect.has(pkg, "main")) continue;
          return path.resolve(parent, pkg.main);
        }
        if (ext.slice(0, -ext.length) == "index" && RequireExtensions[ext]) return mod;
      }
    }
    static getExtension(mod) {
      return path.extname(mod) || Reflect.ownKeys(RequireExtensions).find((e) => remote_default.filesystem.exists(mod + e));
    }
    static getFilePath(basePath, mod) {
      if (!path.isAbsolute(mod)) mod = path.resolve(basePath, mod);
      const defaultExtension = path.extname(mod);
      if (!defaultExtension) {
        const ext = Reflect.ownKeys(RequireExtensions).find((e) => remote_default.filesystem.exists(mod + e));
        if (ext) {
          mod = mod + ext;
        }
      }
      return fs_default.realpathSync(mod);
    }
    static _load(mod, basePath, createRequire2) {
      const originalReq = mod;
      if (!path.isAbsolute(mod)) mod = path.resolve(basePath, mod);
      const filePath = this.getFilePath(basePath, mod);
      if (!remote_default.filesystem.exists(filePath)) throw new Error(`Cannot find module ${mod}`);
      if (window.require.cache[filePath]) return window.require.cache[filePath].exports;
      const stats = remote_default.filesystem.getStats(filePath);
      if (stats.isDirectory()) mod = this.resolveMainFile(mod, basePath);
      const ext = this.getExtension(filePath);
      const loader = RequireExtensions[ext];
      if (!loader) throw new Error(`Cannot find module ${originalReq}`);
      const module = window.require.cache[mod] = new _Module(filePath, internalModule, createRequire2(mod));
      loader(module, filePath);
      return module.exports;
    }
    static get Module() {
      return _Module;
    }
    static get createRequire() {
      return Logger.warn("ContextModule", "Module.createRequire not implemented yet.");
    }
    static get _extensions() {
      return RequireExtensions;
    }
    id;
    path;
    exports;
    parent;
    filename;
    loaded;
    children;
    fileContent;
    require;
    constructor(id, parent, require4) {
      this.id = id;
      this.path = remote_default.path.dirname(id);
      this.exports = {};
      this.parent = parent;
      this.filename = id;
      this.loaded = false;
      this.children = [];
      this.require = require4;
      if (parent) parent.children.push(this);
    }
    _compile(code) {
      const wrapped = compileFunction(code, ["require", "module", "exports", "__filename", "__dirname", "global"], this.filename);
      wrapped(this.require, this, this.exports, this.filename, this.path, window);
    }
  };
  var internalModule = new Module(".", null);

  // src/InAccord/polyfill/index.ts
  init_vm();
  init_fs();

  // src/InAccord/polyfill/request.ts
  init_remote();
  var methods = ["get", "put", "post", "delete", "head"];
  var aliases = {del: "delete"};
  function parseArguments(...args) {
    let url, options, callback;
    for (const arg of args) {
      switch (typeof arg) {
        case (arg !== null && "object"):
          options = arg;
          if ("url" in options) {
            url = options.url;
          }
          break;
        case (!url && "string"):
          url = arg;
          break;
        case (!callback && "function"):
          callback = arg;
          break;
      }
    }
    return {url, options, callback};
  }
  function validUrl(url) {
    return typeof url === "string";
  }
  function validCallback(callback) {
    return typeof callback === "function";
  }
  function fixBuffer(options, callback) {
    return (error, res, body) => {
      if ("Content-Type" in Object(options.headers) && options.headers?.["Content-Type"] !== "text/plian") {
        body = Buffer.from(body);
      } else {
        body = Buffer.from(body).toString();
      }
      callback(error, res, body);
    };
  }
  function request(...args) {
    const {url, options = {}, callback} = parseArguments.apply(this, args);
    if (!validUrl(url) || !validCallback(callback)) return null;
    if ("method" in options && methods.indexOf(options.method.toLowerCase()) >= 0) {
      return remote_default.https[options.method](url, options, fixBuffer(options, callback));
    }
    return remote_default.https.request(url, options, fixBuffer(options, callback));
  }
  Object.assign(request, Object.fromEntries(
    methods.concat(Object.keys(aliases)).map((method) => [method, function (...args) {
      const {url, options = {}, callback} = parseArguments.apply(this, args);
      if (!validUrl(url) || !validCallback(callback)) return null;
      return remote_default.https[aliases[method] || method](url, options, fixBuffer(options, callback));
    }])
  ));

  // src/InAccord/polyfill/https.ts
  init_events();
  init_remote();
  function get(url, options = {}, callback) {
    if (typeof options === "function") {
      callback = options;
      options = null;
    }
    const emitter = new EventEmitter();
    callback(emitter);
    remote_default.https.get(url, options, (error, res, body) => {
      if (error) return emitter.emit("error", error);
      emitter.emit("data", body);
      emitter.emit("end", res);
    });
    return emitter;
  }
  var https_default = {get};

  // src/InAccord/polyfill/buffer.ts
  init_logger();
  var import_buffer = __toESM(require_buffer());
  Object.defineProperty(window, "Buffer", {
    get() {
      Logger.warn("Deprecated", `Usage of the Buffer global is deprecated. Consider using web standards such as Uint8Array and TextDecoder/TextEncoder.`);
      return import_buffer.Buffer;
    },
    configurable: true,
    enumerable: false
  });
  var buffer_default = import_buffer.Buffer;

  // src/InAccord/polyfill/crypto.ts
  init_remote();
  var crypto_default = {
    ...remote_default.crypto,
    // Wrap it in Buffer
    randomBytes(length) {
      return Buffer.from(remote_default.crypto.randomBytes(length));
    }
  };

  // src/InAccord/polyfill/index.ts
  init_remote();
  init_logger();
  var deprecated = /* @__PURE__ */ new Map([
    ["request", "Use iaApi.Net.fetch instead."],
    ["https", "Use iaApi.Net.fetch instead."]
  ]);
  var originalFs = Object.assign({}, fs_default);
  originalFs.writeFileSync = (path2, data, options) => fs_default.writeFileSync(path2, data, Object.assign({}, options, {originalFs: true}));
  originalFs.writeFile = (path2, data, options) => fs_default.writeFile(path2, data, Object.assign({}, options, {originalFs: true}));
  var createRequire = function (path2) {
    return (mod) => {
      if (typeof mod === "string" && mod.startsWith("./")) return;
      if (deprecated.has(mod)) {
        Logger.warn("Remote~Require", `The "${mod}" module is marked as deprecated. ${deprecated.get(mod)}`);
      }
      switch (mod) {
        case "request":
          return request;
        case "https":
          return https_default;
        case "original-fs":
          return originalFs;
        case "fs":
          return fs_default;
        case "path":
          return remote_default.path;
        case "events":
          return EventEmitter;
        case "electron":
          return remote_default.electron;
        case "process":
          return window.process;
        case "vm":
          return vm_default;
        case "module":
          return Module;
        case "buffer":
          return buffer_default;
        case "crypto":
          return crypto_default;
        default:
          return Module._load(mod, path2, createRequire);
      }
    };
  };
  var require2 = window.require = createRequire(".");
  require2.cache = {};
  require2.resolve = (path2) => {
    for (const key of Object.keys(require2.cache)) {
      if (key.startsWith(path2)) return require2.cache[key];
    }
  };

  // src/InAccord/secure.ts
  function secure_default() {
    const contentWindowGetter = Object.getOwnPropertyDescriptor(HTMLIFrameElement.prototype, "contentWindow").get;
    Object.defineProperty(HTMLIFrameElement.prototype, "contentWindow", {
      get: function (...args) {
        const contentWindow = Reflect.apply(contentWindowGetter, this, args);
        return new Proxy(contentWindow, {
          getOwnPropertyDescriptor: function (obj, prop) {
            if (prop === "localStorage") return void 0;
            return Object.getOwnPropertyDescriptor(obj, prop);
          },
          get: function (obj, prop) {
            if (prop === "localStorage") return null;
            const val = obj[prop];
            if (typeof val === "function") return val.bind(obj);
            return val;
          }
        });
      }
    });
    Object.defineProperty(Reflect, "apply", {value: Reflect.apply, writable: false, configurable: false});
    Object.defineProperty(Function.prototype, "bind", {value: Function.prototype.bind, writable: false, configurable: false});
    const oOpen = XMLHttpRequest.prototype.open;
    XMLHttpRequest.prototype.open = function (...args) {
      const url = args[1];
      if (url.toLowerCase().includes("api/webhooks")) return null;
      return Reflect.apply(oOpen, this, args);
    };
  }

  // src/InAccord/loadingicon.ts
  var css = `/* BEGIN V2 LOADER */
/* =============== */

// Fix this image it is is not correct

#ia-loading-icon {
  background-image: url(data:image/svg+xml;base64,);
}
#ia-loading-icon {
  position: fixed;
  bottom:5px;
  right:5px;
  z-index: 2147483647;
  display: block;
  width: 20px;
  height: 20px;
  background-size: 100% 100%;
  animation: ia-loading-animation 1.5s ease-in-out infinite;
}

@keyframes ia-loading-animation {
  0% { opacity: 0.05; }
  50% { opacity: 0.6; }
  100% { opacity: 0.05; }
}
/* =============== */
/*  END V2 LOADER  */`;
  var iconStyle = document.createElement("style");
  iconStyle.textContent = css;
  var loadingIcon = document.createElement("div");
  loadingIcon.id = "ia-loading-icon";
  loadingIcon.className = "ia-loaderv2";
  loadingIcon.title = "InAccord is loading...";
  var loadingicon_default = class {
    static show() {
      document.body.appendChild(iconStyle);
      document.body.appendChild(loadingIcon);
    }
    static hide() {
      if (iconStyle) iconStyle.remove();
      if (loadingIcon) loadingIcon.remove();
    }
  };

  // src/InAccord/modules/core.ts
  init_logger();
  init_config();

  // src/InAccord/data/changelog.ts
  init_config();
  var changelog_default = {
    title: "InAccord",
    subtitle: `v${config_default.get("version")}`,
    // https://youtu.coom/ // fis this
    video: "https://www.youtube.com/",
    // fix this
    // banner: "https:// ", // fix this
    blurb: "Hotfix to squash some bugs.",
    changes: [
      {
        title: "Bugs Squashed",
        type: "fixed",
        items: [
          "Fixed styles breaking in certian areas.",
          "Fixed theme attributes from causing a bunch of lag."
        ]
      }
    ]
  };

  // src/InAccord/builtins/builtins.ts
  var builtins_exports = {};
  __export(builtins_exports, {
    AddonStore: () => addonstore_default2,
    CustomCSS: () => customcss_default,
    DebugLogs: () => debuglogs_default,
    Debugger: () => debugger_default,
    DefaultCommands: () => defaultcommands_default,
    DevToolsListener: () => devtools_default,
    InspectElement: () => inspectelement_default,
    MediaKeys: () => mediakeys_default,
    NativeFrame: () => nativeframe_default,
    ReactDevTools: () => reactdevtools_default,
    Recovery: () => recovery_default,
    RemoveMinimumSize: () => removeminimumsize_default,
    StopDevToolsWarning: () => devtoolswarning_default,
    ThemeAttributes: () => themeattributes_default,
    VoiceDisconnect: () => voicedisconnect_default,
    WindowPrefs: () => transparency_default,
    iaContextMenu: () => contextmenu_default2
  });

  // src/InAccord/builtins/customcss.ts
  init_fs();
  init_path();
  init_electron();

  // src/InAccord/structs/builtin.ts
  init_logger();
  init_emitter();
  init_settings3();
  init_patcher();

  // src/InAccord/modules/commandmanager.tsx
  init_patcher();
  init_react();
  init_pluginmanager();
  init_logger();
  init_webpack2();
  var CommandTypes = {
    CHAT_INPUT: 1,
    USER: 2,
    MESSAGE: 3
  };
  var InputTypes = {
    BUILT_IN: 0,
    TEXT: 1,
    SEARCH: 2,
    BOT: 3,
    PLACEHOLDER: 4
  };
  var OptionTypes = {
    SUB_COMMAND: 1,
    SUB_COMMAND_GROUP: 2,
    STRING: 3,
    INTEGER: 4,
    BOOLEAN: 5,
    USER: 6,
    CHANNEL: 7,
    ROLE: 8,
    MENTIONABLE: 9,
    NUMBER: 10,
    ATTACHMENT: 11
  };
  var MessageEmbedTypes = {
    IMAGE: "image",
    VIDEO: "video",
    LINK: "link",
    ARTICLE: "article",
    TWEET: "tweet",
    RICH: "rich",
    GIFV: "gifv",
    APPLICATION_NEWS: "application_news",
    AUTO_MODERATION_MESSAGE: "auto_moderation_message",
    AUTO_MODERATION_NOTIFICATION: "auto_moderation_notification",
    TEXT: "text",
    POST_PREVIEW: "post_preview",
    GIFT: "gift",
    SAFETY_POLICY_NOTICE: "safety_policy_notice",
    SAFETY_SYSTEM_NOTIFICATION: "safety_system_notification",
    VOICE_CHANNEL: "voice_channel",
    GAMING_PROFILE: "gaming_profile"
  };
  var iconClasses = {
    ...getModule((x) => x.wrapper && x.icon && x.selected && x.selectable && !x.mask),
    builtInSeparator: getModule((x) => x.builtInSeparator)?.builtInSeparator
  };
  var getAcronym = (input) => input?.replace(/'s /g, " ").match(/\b\w/g)?.join("").slice(0, 2) ?? "";
  var isValidImageUrl = (url) => {
    try {
      const validatedUrl = new URL(url);
      return validatedUrl.protocol === "data:" && validatedUrl.pathname.startsWith("image/") || validatedUrl.protocol === "https:";
    } catch {
      return false;
    }
  };
  var CommandManager = class _CommandManager {
    static #commands = /* @__PURE__ */ new Map();
    static #sections = /* @__PURE__ */ new Map();
    static User = getByStrings(["hasHadPremium(){"]);
    static createBotMessage = getByStrings(['username:"Clyde"'], {searchExports: true});
    static MessagesModule = getModule((x) => x.receiveMessage);
    static IconsModule = getModule((x) => x.BOT_AVATARS);
    static localiaBot;
    static initialize() {
      this.#patchCommandSystem();
    }
    static #patchCommandSystem() {
      this.User = getByStrings(["hasHadPremium(){"]);
      this.createBotMessage = getByStrings(['username:"Clyde"'], {searchExports: true});
      this.MessagesModule = getModule((x) => x.receiveMessage);
      this.IconsModule = getModule((x) => x.BOT_AVATARS);
      this.localiaBot = new this.User({
        avatar: "inaccord",
        id: "676620914632294467",
        bot: true,
        username: "InAccord",
        system: true
      });
      this.#patchSidebarModule();
      this.#patchQuery();
      this.#patchApplicationIcons();
      this.#patchIndexStore();
      this.#patchAuthorizer();
      this.IconsModule.BOT_AVATARS.inaccord = "https://github.com/docrst/InAccord.png";
    }
    static #patchSidebarModule() {
      const SidebarModule = getByStrings([".BUILT_IN?", "categoryListRef:"], {defaultExport: false});
      Patcher.after("CommandManager", SidebarModule, "Z", (_, [props], res) => {
        if (!this.#sections.size) return;
        const child = res.props.children;
        if (child.props?.__iaPatched) return;
        res.props.children = react_default.cloneElement(child, {
          renderCategoryListItem: (...args) => {
            const ret = child.props.renderCategoryListItem(...args);
            if (!props.sections[args[1] - 1]?.isia && props.sections[args[1]].isia) {
              return react_default.cloneElement(ret, {
                children: [
                  react_default.createElement("hr", {className: iconClasses.builtInSeparator}),
                  ...ret.props.children
                ]
              });
            }
            return ret;
          },
          __iaPatched: true
        });
      });
    }
    static #patchIndexStore() {
      const [mod, key] = getWithKey(filter_exports.byStrings(".getScoreWithoutLoadingLatest"));
      Patcher.after("CommandManager", mod, key, (_, args, res) => {
        if (!args[2].commandTypes.includes(CommandTypes.CHAT_INPUT)) return res;
        for (const sectionedCommand of res.sectionedCommands) {
          if (sectionedCommand.section.id !== "-1") continue;
          sectionedCommand.data = sectionedCommand.data.filter((m) => !m.isia);
        }
        let descriptorsIndex = res.descriptors.findIndex((value) => value.id === "-1");
        let sectionedCommandsIndex = res.sectionedCommands.findIndex((value) => value.section.id === "-1");
        for (const section of this.#sections.values()) {
          const commands = this.getCommandsByCaller(section.id);
          if (commands.length > 0) {
            res.sectionedCommands.splice(sectionedCommandsIndex++, 0, {
              section,
              data: commands
            });
            res.descriptors.splice(descriptorsIndex++, 0, section);
            res.commands.push(...commands);
          }
        }
        return res;
      });
    }
    static #patchQuery() {
      const ApplicationCommandIndexStore = getStore("ApplicationCommandIndexStore");
      Patcher.after("CommandManager", ApplicationCommandIndexStore, "query", (_, args, res) => {
        if (!args[1].commandTypes.includes(CommandTypes.CHAT_INPUT)) return res;
        const text = args[1].text || "";
        for (const sectionedCommand of res.sectionedCommands) {
          if (sectionedCommand.section.id !== "-1") continue;
          sectionedCommand.data = sectionedCommand.data.filter((m) => !m.isia);
        }
        for (const section of this.#sections.values()) {
          const commands = this.getCommandsByCaller(section.id).filter((cmd) => cmd.name.includes(text) || cmd.description.includes(text));
          if (commands.length > 0) {
            res.sectionedCommands.push({
              section,
              data: commands
            });
            res.descriptors.push(section);
            res.commands.unshift(...commands);
          }
        }
      });
    }
    static #patchApplicationIcons() {
      const [mod, key] = getWithKey(filter_exports.byStrings(".type===", ".BUILT_IN?"), {
        target: getModule((_, m) => modules[m.id].toString().includes("hasSpaceTerminator:"))
      });
      Patcher.after("CommandManager", mod, key, (_, [{id}], res) => {
        const getIconUrl = () => {
          const metadatiacon = pluginmanager_default.getAddon(id)?.icon || pluginmanager_default.getPlugin(id)?.instance?.icon || null;
          const sectionIcon = this.#sections.has(id) ? this.#sections.get(id)?.icon : null;
          return metadatiacon || sectionIcon;
        };
        const iconUrl = getIconUrl();
        const acronym = getAcronym(id);
        const Logo2 = ({width, height, padding = 0, className, isSelected, selectable}) => {
          const wrapperClasses = [
            selectable && iconClasses.selectable,
            isSelected && iconClasses.selected,
            iconClasses.wrapper,
            iconClasses.icon,
            className
          ].filter(Boolean).join(" ");
          const iconStyle2 = {width, height, padding};
          const renderImageIcon = () => /* @__PURE__ */ react_default.createElement("img", {src: iconUrl, alt: acronym, style: {width: "100%", height: "100%"}});
          const renderAcronymIcon = () => /* @__PURE__ */ react_default.createElement(
            "span",
            {
              style: {
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                fontSize: Math.min(width, height) / 1.2,
                backgroundColor: "transparent",
                borderRadius: "50%",
                width: "100%",
                fontWeight: "bold",
                height: "100%"
              }
            },
            acronym
          );
          const renderIcon = () => {
            if (typeof iconUrl === "function") {
              return react_default.createElement(iconUrl);
            }
            if (typeof iconUrl === "string" && isValidImageUrl(iconUrl)) {
              return renderImageIcon();
            }
            return renderAcronymIcon();
          };
          return react_default.createElement(
            "div",
            {
              style: iconStyle2,
              className: wrapperClasses
            },
            renderIcon()
          );
        };
        return this.#sections.has(id) ? Logo2 : res;
      });
    }
    static #patchAuthorizer() {
      const [module, key] = getWithKey(filter_exports.byStrings("openOAuth2Modal", "Promise.resolve", "commandIntegrationTypes"));
      Patcher.instead("CommandManager", module, key, (that, args, original) => {
        if (this.#sections.has(args[0]?.applicationId)) {
          return Promise.resolve({
            isAuthorized: true
          });
        }
        return original.apply(that, args);
      });
    }
    static registerCommand(caller, command) {
      if (!caller || !command?.name || !command?.execute) {
        throw new Error("Command must have a caller, name, and execute function");
      }
      const pluginCommands = this.#commands.get(caller) || /* @__PURE__ */ new Map();
      const commandId = `ia-${caller}-${command.id}`;
      if (pluginCommands.has(commandId)) {
        throw new Error(`Command with id ${commandId} is already registered`);
      }
      const formattedCommand = this.#formatCommand(caller, command, commandId);
      pluginCommands.set(commandId, formattedCommand);
      this.#commands.set(caller, pluginCommands);
      this.#ensureSection(caller);
      return () => this.unregisterCommand(caller, command.id);
    }
    static #formatCommand(caller, command, commandId) {
      const self = this;
      return {
        integrationType: 0,
        integrationTitle: caller,
        inputType: InputTypes.BUILT_IN,
        get id() {
          return commandId;
        },
        get __registerId() {
          return commandId;
        },
        get applicationId() {
          return caller;
        },
        get displayName() {
          return command.name || "";
        },
        get target() {
          return CommandTypes.CHAT_INPUT;
        },
        get name() {
          return command.name || "";
        },
        get description() {
          return command.description || "";
        },
        get displayDescription() {
          return command.description || "";
        },
        get options() {
          return _CommandManager.#formatOptions(command.options);
        },
        execute: this.#patchExecuteFunction(command),
        get section() {
          self.#ensureSection(caller);
          return self.#sections.get(caller);
        },
        isia: true,
        __proto__: command
      };
    }
    static optionsMap = /* @__PURE__ */ new WeakMap();
    static #formatOptions(options) {
      if (!options) return [];
      if (this.optionsMap.has(options)) {
        return this.optionsMap.get(options);
      }
      const opts = options.map((option) => ({
        get name() {
          return option.name;
        },
        get description() {
          return option.description;
        },
        get displayDescription() {
          return option.description;
        },
        type: option.type,
        get required() {
          return option.required || false;
        },
        get choices() {
          return option.choices?.map((choice) => ({
            ...choice,
            get name() {
              return choice.name;
            },
            get displayName() {
              return choice.name;
            }
          }));
        },
        get displayName() {
          return option.name;
        },
        __proto__: option
      }));
      this.optionsMap.set(options, opts);
      return opts;
    }
    static #ensureSection(caller) {
      if (!this.#sections.has(caller)) {
        this.#sections.set(caller, {
          id: caller,
          name: caller,
          type: 1,
          key: "1",
          icon: caller === "InAccord" ? "https://github.com/docrst/InAccord.png" : null,
          // Fix this string for In-Accord
          isia: true
        });
      }
    }
    static #patchExecuteFunction(command) {
      return (data, {channel, guild}) => {
        try {
          const result = command.execute(data, {channel, guild});
          if (!("inputType" in command) || command.inputType === InputTypes.BUILT_IN) {
            this.sendBotMessage(result, {channel, guild});
          }
          return result;
        } catch (error) {
          Logger.stacktrace("CommandManager", `Fialed to run execute() for command ${command.name}`, error);
        }
      };
    }
    static async sendBotMessage(result, {channel}) {
      try {
        result = await result;
      } catch (error) {
        return Logger.stacktrace("CommandManager", "Fialed to get result of execute()", error);
      }
      if (!(result !== null && typeof result === "object" && !Array.isArray(result))) {
        return;
      }
      const loadingMessage = this.createBotMessage({
        channelId: channel.id,
        content: typeof result.content === "string" ? result.content : void 0,
        loggingName: void 0,
        type: 20
      });
      if (typeof result.embeds === "object" && result.embeds !== null) {
        loadingMessage.embeds = Array.isArray(result.embeds) ? result.embeds : [result.embeds];
        loadingMessage.embeds = loadingMessage.embeds.map((embed) => ({
          ...embed,
          type: embed.type || "rich"
        }));
      }
      Object.assign(loadingMessage, {
        author: this.localiaBot
      });
      if (loadingMessage.content || Array.isArray(loadingMessage.embeds) && loadingMessage.embeds.length > 0) {
        this.MessagesModule.receiveMessage(channel.id, loadingMessage, true);
      }
    }
    static unregisterCommand(caller, commandId) {
      const fullCommandId = `ia-${caller}-${commandId}`;
      const pluginCommands = this.#commands.get(caller);
      if (pluginCommands?.delete(fullCommandId)) {
        if (pluginCommands.size === 0) {
          this.#commands.delete(caller);
          this.#sections.delete(caller);
        }
      }
    }
    static unregisterAll(caller) {
      this.#commands.delete(caller);
      this.#sections.delete(caller);
    }
    static getCommandsByCaller(caller) {
      return Array.from(this.#commands.get(caller)?.values() || []);
    }
  };
  var commandmanager_default = CommandManager;

  // src/InAccord/structs/builtin.ts
  var BuiltinModule = class {
    initialized = false;
    #commands = /* @__PURE__ */ new Set();
    get name() {
      return "Unnamed Builtin";
    }
    get collection() {
      return "settings";
    }
    get category() {
      return "general";
    }
    get id() {
      return "None";
    }
    async initialize() {
      if (settings_default2.get(this.collection, this.category, this.id)) await this.enable();
      emitter_default.on("setting-updated", (collection, category, id, enabled) => {
        if (collection != this.collection || category !== this.category || id !== this.id) return;
        if (enabled) this.enable();
        else this.disable();
      });
      this.initialized = true;
    }
    registerSetting(collection, category, id, onEnable, onDisable) {
      if (arguments.length == 4) {
        collection = this.collection;
        category = arguments[0];
        id = arguments[1];
        onEnable = arguments[2];
        onDisable = arguments[3];
      } else if (arguments.length == 3) {
        collection = this.collection;
        category = this.category;
        id = arguments[0];
        onEnable = arguments[1];
        onDisable = arguments[2];
      }
      return settings_default2.on(collection, category, id, (value) => {
        if (value) onEnable();
        else onDisable();
      });
    }
    get(collection, category, id) {
      if (arguments.length == 2) {
        collection = this.collection;
        category = arguments[0];
        id = arguments[1];
      } else if (arguments.length == 1) {
        collection = this.collection;
        category = this.category;
        id = arguments[0];
      }
      return settings_default2.get(collection, category, id);
    }
    async enable() {
      this.log("Enabled");
      try {
        await this.enabled();
      } catch (e) {
        this.stacktrace("Could not be enabled", e);
      }
    }
    async disable() {
      this.log("Disabled");
      try {
        await this.disabled();
      } catch (e) {
        this.stacktrace("Could not be disabled", e);
      }
    }
    async enabled() {
    }
    async disabled() {
    }
    log(...message) {
      Logger.log(this.name, ...message);
    }
    warn(...message) {
      Logger.warn(this.name, ...message);
    }
    error(...message) {
      Logger.err(this.name, ...message);
    }
    stacktrace(message, error) {
      Logger.stacktrace(this.name, message, error);
    }
    before(object, func, callback) {
      return Patcher.before(this.name, object, func, callback);
    }
    instead(object, func, callback) {
      return Patcher.instead(this.name, object, func, callback);
    }
    after(object, func, callback) {
      return Patcher.after(this.name, object, func, callback);
    }
    unpatchAll() {
      return Patcher.unpatchAll(this.name);
    }
    // TODO: fix type when commands are properly TS
    addCommands(...commands) {
      for (const command of commands) {
        const unregister = commandmanager_default.registerCommand("InAccord", command);
        this.#commands.add(unregister);
      }
    }
    removeCommands() {
      for (const unregister of this.#commands) unregister();
    }
  };

  // src/InAccord/builtins/customcss.ts
  init_settings3();

  // src/InAccord/ui/settings.tsx
  init_react();
  init_settings3();
  init_json();
  init_webpack2();
  init_patcher();

  // src/InAccord/api/reactutils.ts
  init_discordmodules();
  init_react();
  var USE_ERR_MSG = "Minified React error #460; visit https://react.dev/errors/460 for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  var NO_RESOLVE = /* @__PURE__ */ Symbol("no-resolve");
  var patchedReactHooks = {
    use(usable) {
      if (typeof usable.then === "function") {
        let value = NO_RESOLVE;
        usable.then((ret) => {
          value = ret;
        });
        if (value === NO_RESOLVE) throw new Error(USE_ERR_MSG);
        return value;
      }
      return usable._currentValue;
    },
    useFormState(_action, initialState, _permalink) {
      return [initialState, () => {
      }, false];
    },
    readContext(context) {
      return context._currentValue;
    },
    useOptimistic(passthrough) {
      return [passthrough, () => {
      }];
    },
    useMemo(factory) {
      return factory();
    },
    useState(initial) {
      if (typeof initial === "function") {
        const initialValue = initial();
        return [initialValue, () => {
        }];
      }
      return [initial, () => {
      }];
    },
    useReducer(_reducer, initial) {
      return [initial, () => {
      }];
    },
    useRef(value = null) {
      return {current: value};
    },
    useCallback(callback) {
      return callback;
    },
    useContext(context) {
      return context._currentValue;
    },
    useEffect() {
    },
    useLayoutEffect() {
    },
    useImperativeHandle() {
    },
    useTransition() {
      return [false, (callback) => callback()];
    },
    useActionState(_action, initialState, _permalink) {
      return [initialState, () => {
      }, false];
    },
    useInsertionEffect() {
    },
    useDebugValue() {
    },
    useDeferredValue(value) {
      return value;
    },
    useSyncExternalStore(_subscribe, getSnapshot) {
      return getSnapshot();
    },
    useId() {
      return Math.random().toString(36).substr(2, 9);
    }
  };
  var exoticComponents = {
    memo: /* @__PURE__ */ Symbol.for("react.memo"),
    forwardRef: /* @__PURE__ */ Symbol.for("react.forward_ref"),
    lazy: /* @__PURE__ */ Symbol.for("react.lazy")
  };
  var ReactUtils = {
    /**
     * @deprecated
     */
    get rootInstance() {
      return document.getElementById("app-mount")?._reactRootContianer?._internalRoot?.current;
    },
    /**
     * Gets the internal React data of a specified node.
     *
     * @param {HTMLElement} node Node to get the internal React data from
     * @returns {Fiber|undefined} Either the found data or `undefined`
     */
    getInternalInstance(node) {
      if (node.__reactFiber$) return node.__reactFiber$;
      const key = Object.keys(node).find((k) => k.startsWith("__reactInternalInstance") || k.startsWith("__reactFiber"));
      if (key) return node[key];
      return null;
    },
    /**
     * Attempts to find the "owner" node to the current node. This is generally
     * a node with a `stateNode` - a class component.
     *
     * @param {HTMLElement} node Node to obtian React instance of
     * @param {object} options Options for the search
     * @param {array} [options.include] List of items to include in the search
     * @param {array} [options.exclude=["Popout", "Tooltip", "Scroller", "BackgroundFlash"]] List of items to exclude from the search.
     * @param {callable} [options.filter=_=>_] Filter to check the current instance with (should return a boolean)
     * @return {object|undefined} The owner instance or `undefined` if not found
     */
    getOwnerInstance(node, {
      include,
      exclude = ["Popout", "Tooltip", "Scroller", "BackgroundFlash"],
      filter = () => true
    } = {}) {
      if (!node) return null;
      const excluding = include === void 0;
      const nameFilter = excluding ? exclude : include;
      function getDisplayName(owner) {
        const type = owner.type;
        if (!type) return null;
        return type.displayName || type.name || null;
      }
      function classFilter(owner) {
        const name = getDisplayName(owner);
        return name !== null && nameFilter?.includes(name) !== excluding;
      }
      let curr = ReactUtils.getInternalInstance(node);
      while (curr && curr.return) {
        curr = curr.return;
        const owner = curr.stateNode;
        if (owner && !(owner instanceof HTMLElement) && classFilter(curr) && filter(owner)) {
          return owner;
        }
      }
      return null;
    },
    /**
     * Creates an unrendered React component that wraps HTML elements.
     *
     * @param {HTMLElement} element Element or array of elements to wrap
     * @returns {object} Unrendered React component
     */
    wrapElement(element) {
      return class ReactWrapper extends react_default.Component {
        element;
        state;
        ref = react_default.createRef();
        constructor(props) {
          super(props);
          this.element = element;
          this.state = {hasError: false};
        }
        componentDidCatch() {
          this.setState({hasError: true});
        }
        componentDidMount() {
          const refElement = this.ref?.current;
          if (!refElement) return;
          if (Array.isArray(this.element)) {
            this.element.forEach((el) => refElement.appendChild(el));
          } else {
            refElement.appendChild(this.element);
          }
        }
        render() {
          return this.state.hasError ? null : discordmodules_default.React.createElement("div", {
            className: "react-wrapper",
            ref: this.ref
          });
        }
      };
    },
    wrapInHooks(functionComponent, customPatches = {}) {
      const FC = ReactUtils.getType(functionComponent);
      return function wrappedComponent(props) {
        const reactDispatcher = react_default.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE.H;
        const originalDispatcher = {...reactDispatcher};
        Object.assign(reactDispatcher, patchedReactHooks, customPatches);
        try {
          return FC(props);
        } catch (error) {
          if (error instanceof Error && error.message === USE_ERR_MSG) return;
          throw error;
        } finally {
          Object.assign(reactDispatcher, originalDispatcher);
        }
      };
    },
    // forceUpdateFiber(fiber: Fiber): boolean {
    //     fiber.type = ReactUtils.getType(fiber.elementType);
    //     // React Class Components
    //     if (fiber.stateNode?.isReactComponent) {
    //         fiber.stateNode.forceUpdate();
    //         return true;
    //     }
    //     let memoizedState = fiber.memoizedState;
    //     while (memoizedState) {
    //         if (memoizedState.queue?.lanes === 0) {
    //             const lastRenderedState = memoizedState.queue.lastRenderedState;
    //             memoizedState.queue.dispatch((m: any) => !m);
    //             memoizedState.queue.dispatch(lastRenderedState);
    //             return true;
    //         }
    //         memoizedState = memoizedState.next;
    //     }
    //     return false;
    // },
    getType(elementType) {
      while (true) {
        switch (elementType.$$typeof) {
          case exoticComponents.memo:
            elementType = elementType.type;
            break;
          case exoticComponents.forwardRef:
            elementType = elementType.render;
            break;
          case exoticComponents.lazy: {
            const _payload = elementType._payload;
            if (_payload._status === 1) {
              elementType = _payload._result.default;
            } else {
              elementType = (() => {
              });
            }
            break;
          }
          default:
            return elementType;
        }
      }
    }
  };
  Object.freeze(ReactUtils);
  var reactutils_default = ReactUtils;

  // src/InAccord/ui/settings/addonpage.tsx
  init_react();

  // src/InAccord/ui/settings/addonstore.tsx
  init_react();

  // src/InAccord/modules/addonstore.ts
  init_path();
  init_fs();
  init_logger();
  init_toasts();
  init_json();
  init_i18n();
  init_react();
  init_pluginmanager();
  init_thememanager();
  init_modals();

  // src/InAccord/ui/modals/installmodal.tsx
  init_discordmodules();

  // src/InAccord/modules/localemanager.ts
  init_i18n();
  init_discordmodules();
  var localemanager_default = new class LocaleManager {
    get discordLocale() {
      return discordmodules_default.LocaleStore?.locale ?? this.defaultLocale;
    }
    get defaultLocale() {
      return "en-US";
    }
    initialize() {
      i18n_default.setLocale(this.discordLocale);
      discordmodules_default.LocaleStore?.addChangeListener(() => i18n_default.setLocale(this.discordLocale));
    }
  }();

  // src/InAccord/ui/modals/installmodal.tsx
  init_react();
  init_settings3();
  init_i18n();

  // src/InAccord/data/web.ts
  var HOSTNAME = "inaccord.app";
  var API_VERSION = "v3";
  var join = (...paths) => {
    const path2 = paths.map(($path) => $path.match(/\/*(.+)\/*/)?.[1]).filter(Boolean).join("/");
    return `https://${HOSTNAME}/${path2}`;
  };
  var apiJoin = (...paths) => {
    const path2 = paths.map(($path) => $path.match(/\/*(.+)\/*/)?.[1]).filter(Boolean).join("/");
    return `https://api.${HOSTNAME}/${API_VERSION}/${path2}`;
  };
  var makePage = (type) => (name) => join(`${type}/${encodeURIComponent(name)}`);
  var makeRedirects = (type) => (id) => join(`${type}?id=${id}`);
  var releaseChannels = {
    theme: [
      "813903993524715522",
      "781600198002081803"
    ],
    plugin: [
      "813903954991120385",
      "781600250858700870"
    ]
  };
  var EMPTY_USE_STORE = true;
  var RAW_GIT_URL_REGEX = /^https:\/\/raw\.githubusercontent\.com\/(.+?)\/(.+?)\/(.+?)\/(.+)$/;
  var Web = class _Web {
    /**
     * This will allow preloading of the addon channels
     * @param {string} channelId
     * @returns {"plugin" | "theme" | undefined}
     */
    static getReleaseChannelType(channelId) {
      if (releaseChannels.backup.includes(channelId)) return "backup";
      if (releaseChannels.plugin.includes(channelId)) return "plugin";
      if (releaseChannels.theme.includes(channelId)) return "theme";
    }
    /** @param {string} rawGitURL  */
    static convertToPreviewURL(rawGitURL) {
      const match = rawGitURL.match(RAW_GIT_URL_REGEX);
      if (!match) {
        throw new Error("Fialed to parse url!");
      }
      const [, user, repo, commit, filePath] = match;
      const jsdelivr = `https://cdn.jsdelivr.net/gh/${user}/${repo}@${commit}/${filePath}`;
      return `https://discord-preview.vercel.app/?file=${encodeURIComponent(jsdelivr)}`;
    }
    /**
     * Converts a raw github link into a normal github page
     * @example
     * https://raw.githubusercontent.com/QWERTxD/InAccordPlugins/298752533fbiaab511c3a3f4ffe6afd41d0a93f1/CallTimeCounter/CallTimeCounter.plugin.js
     * https://github.com/QWERTxD/InAccordPlugins/blob/298752533fbiaab511c3a3f4ffe6afd41d0a93f1/CallTimeCounter/CallTimeCounter.plugin.js
     * @param {string} rawGitURL
     */
    static convertRawToGitHubURL(rawGitURL) {
      const match = rawGitURL.match(RAW_GIT_URL_REGEX);
      if (!match) {
        throw new Error("Fialed to parse url!");
      }
      const [, user, repo, commit, filePath] = match;
      return `https://github.com/${user}/${repo}/blob/${commit}/${filePath}`;
    }
    static API_VERSION = API_VERSION;
    static hostname = HOSTNAME;
    static redirects = {
      github: makeRedirects("/gh-redirect"),
      download: makeRedirects("/download"),
      backup: makeRedirects("/backup"),
      theme: makeRedirects("/theme"),
      plugin: makeRedirects("/plugin")
    };
    static pages = {
      themes: join("/themes"),
      theme: makePage("/theme"),
      backups: join("/backups"),
      backup: makePage("/backup"),
      plugins: join("/plugins"),
      plugin: makePage("/plugin"),
      developers: join("/developers"),
      developer: makePage("/developer")
    };
    static resources = {
      EMPTY_THUMBNiaL: EMPTY_USE_STORE ? "/resources/store/missing.svg" : "/resources/ui/content_thumbnial.svg",
      /** @param {? string} thumbnial */
      thumbnial: (thumbnial) => join(thumbnial || _Web.resources.EMPTY_THUMBNiaL)
    };
    static store = {
      addons: apiJoin("/store/addons"),
      backups: apiJoin("/store/backups"),
      themes: apiJoin("/store/themes"),
      plugins: apiJoin("/store/plugins"),
      /** @param {number|string} idOrName Id or Name of a addon */
      addon: (idOrName) => apiJoin(`/store/${encodeURIComponent(idOrName)}`),
      tags: {
        plugin: [
          "fun",
          "roles",
          "activity",
          "status",
          "game",
          "edit",
          "library",
          "notifications",
          "emotes",
          "channels",
          "shortcut",
          "enhancement",
          "servers",
          "chat",
          "security",
          "organization",
          "friends",
          "members",
          "utility",
          "developers",
          "search",
          "text",
          "backup",
          "voice"
        ],
        theme: [
          "flat",
          "transparent",
          "layout",
          "customizable",
          "fiction",
          "nature",
          "space",
          "dark",
          "light",
          "game",
          "anime",
          "red",
          "orange",
          "green",
          "purple",
          "black",
          "other",
          "high-contrast",
          "white",
          "aqua",
          "animated",
          "yellow",
          "blue",
          "abstract"
        ]
      }
    };
  };

  // src/InAccord/ui/modals/installmodal.tsx
  init_emitter();
  init_button();
  init_flex();
  init_text();
  init_footer();
  init_root();

  // src/InAccord/ui/settings/components/checkbox.tsx
  init_clsx();
  init_react();
  init_flex();
  init_lucide_react();
  function CheckBox(props) {
    const [state, setState] = react_default.useState(props.value);
    const onChange = react_default.useCallback(() => {
      if (props.disabled) return;
      setState((value) => {
        props.onChange?.(!value);
        return !value;
      });
    }, [props]);
    return /* @__PURE__ */ react_default.createElement(
      Flex,
      {
        className: clsx_default("ia-checkbox", props.className, {"ia-checkbox-disabled": props.disabled, "ia-checkbox-has-label": props.label, "ia-checkbox-reverse": props.reverse}),
        align: Flex.Align.CENTER,
        direction: props.reverse ? Flex.Direction.HORIZONTAL_REVERSE : Flex.Direction.HORIZONTAL,
        onClick: onChange
      },
      /* @__PURE__ */ react_default.createElement(
        "input",
        {
          type: "checkbox",
          checked: state,
          id: props.id,
          className: clsx_default("ia-checkbox-input", props.inputClassName)
        }
      ),
      /* @__PURE__ */ react_default.createElement("div", {className: clsx_default("ia-checkbox-box", props.iconClassName)}, /* @__PURE__ */ react_default.createElement(Check, {size: "18px"})),
      props.label && /* @__PURE__ */ react_default.createElement("div", {className: clsx_default("ia-checkbox-label", props.label)}, props.label)
    );
  }

  // src/InAccord/ui/spinner.tsx
  init_clsx();
  init_react();
  init_webpack2();
  init_react();
  var AccessibilityContext2 = getModule((m) => m?._currentValue?.reducedMotion, {searchExports: true}) || react_default.createContext({
    reducedMotion: {enabled: false}
  });
  var SpinnerType = Spinner.Type = Object.freeze({
    WANDERING_CUBES: "wandering-cubes",
    CHASING_DOTS: "chasing-dots",
    PULSING_ELLIPSIS: "pulsing-ellipsis",
    SPINNING_CIRCLE: "spinning-circle",
    SPINNING_CIRCLE_SIMPLE: "spinning-circle-simple",
    LOW_MOTION: "low-motion"
  });
  function Spinner(props) {
    const {reducedMotion} = react_default.useContext(AccessibilityContext2);
    const {animated = true} = props;
    const type = react_default.useMemo(() => {
      const spinnerType = props.type || SpinnerType.WANDERING_CUBES;
      if (!reducedMotion?.enabled) return spinnerType;
      switch (spinnerType) {
        case SpinnerType.WANDERING_CUBES:
        case SpinnerType.CHASING_DOTS:
          return SpinnerType.LOW_MOTION;
        default:
          return spinnerType;
      }
    }, [props.type, reducedMotion?.enabled]);
    const className = react_default.useMemo(() => {
      return clsx_default({
        "ia-spinner-stopAnimation": !animated
      }, "ia-spinner", `ia-spinner-${type}`, props.className);
    }, [props.className, animated, type]);
    const itemClassName = react_default.useMemo(() => {
      return clsx_default("ia-spinner-path", props.itemClassName);
    }, [props.itemClassName]);
    switch (type) {
      case SpinnerType.SPINNING_CIRCLE:
      case SpinnerType.SPINNING_CIRCLE_SIMPLE: {
        return /* @__PURE__ */ react_default.createElement(
          "div",
          {
            className,
            role: "img",
            "aria-label": props["aria-label"],
            ...props
          },
          /* @__PURE__ */ react_default.createElement("div", {className: "ia-spinner-inner"}, /* @__PURE__ */ react_default.createElement(
            "svg",
            {
              className: "ia-spinner-circular",
              viewBox: "25 25 50 50"
            },
            type === SpinnerType.SPINNING_CIRCLE && /* @__PURE__ */ react_default.createElement(react_default.Fragment, null, /* @__PURE__ */ react_default.createElement("circle", {cx: 50, cy: 50, r: 20, className: itemClassName}), /* @__PURE__ */ react_default.createElement("circle", {cx: 50, cy: 50, r: 20, className: itemClassName})),
            /* @__PURE__ */ react_default.createElement("circle", {cx: 50, cy: 50, r: 20, className: itemClassName})
          ))
        );
      }
      default: {
        return /* @__PURE__ */ react_default.createElement(
          "span",
          {
            className,
            role: "img",
            "aria-label": props["aria-label"],
            ...props
          },
          /* @__PURE__ */ react_default.createElement("span", {className: "ia-spinner-inner"}, /* @__PURE__ */ react_default.createElement("span", {className: itemClassName}), /* @__PURE__ */ react_default.createElement("span", {className: itemClassName}), (type === SpinnerType.PULSING_ELLIPSIS || type === SpinnerType.LOW_MOTION) && /* @__PURE__ */ react_default.createElement("span", {className: itemClassName}))
        );
      }
    }
  }
  var spinner_default = Object.freeze(Spinner);

  // src/InAccord/ui/settings/addonshared.tsx
  init_react();
  init_i18n();
  init_discordmodules();
  init_button();
  init_json();

  // src/InAccord/ui/settings/title.tsx
  init_react();
  init_button();
  var {useCallback: useCallback10} = react_default;
  var basicClass = "ia-settings-title";
  var groupClass = "ia-settings-title ia-settings-group-title";
  function SettingsTitle({isGroup = false, className = "", button = void 0, onClick = void 0, text, children = []}) {
    const click = useCallback10((event) => {
      event.stopPropagation();
      event.preventDefault();
      button?.onClick?.(event);
    }, [button]);
    const baseClass = isGroup ? groupClass : basicClass;
    const titleClass = className ? `${baseClass} ${className}` : baseClass;
    return /* @__PURE__ */ react_default.createElement("h2", {
      className: titleClass, onClick: () => {
        onClick?.();
      }
    }, text, button && /* @__PURE__ */ react_default.createElement(Button, {className: "ia-button-title", onClick: click, size: Button.Sizes.NONE}, button.title), children);
  }

  // src/InAccord/ui/settings/addonshared.tsx
  init_lucide_react();
  var buildDirectionOptions = () => [
    {label: t("Sorting.ascending"), value: true},
    {label: t("Sorting.descending"), value: false}
  ];
  function makeBasicButton(title, children, action, key) {
    return /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", "aria-label": title, text: title, key}, (props) => /* @__PURE__ */ react_default.createElement(Button, {...props, "aria-label": title, size: Button.Sizes.NONE, look: Button.Looks.BLANK, className: "ia-button", onClick: action}, children));
  }
  function getState(type, control, defaultValue) {
    const addonlistControls = json_default.get("misc", "addonlistControls") || {};
    if (!addonlistControls[type]) return defaultValue;
    if (!Object.prototype.hasOwnProperty.call(addonlistControls[type], control)) return defaultValue;
    return addonlistControls[type][control];
  }
  function saveState(type, control, value) {
    const addonlistControls = json_default.get("misc", "addonlistControls") || {};
    if (!addonlistControls[type]) addonlistControls[type] = {};
    addonlistControls[type][control] = value;
    json_default.set("misc", "addonlistControls", addonlistControls);
  }
  var addonContext = react_default.createContext();
  function AddonHeader({children, count, searching}) {
    const {title, toggleStore, showingStore} = react_default.useContext(addonContext);
    const exitStore = react_default.useCallback(() => {
      if (!showingStore) return;
      toggleStore();
    }, [showingStore, toggleStore]);
    const set = react_default.useContext(SettingsTitleContext);
    return set(
      /* @__PURE__ */ react_default.createElement(
      SettingsTitle,
      {
        text: /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-title", "data-showing-store": showingStore}, /* @__PURE__ */ react_default.createElement("span", {onClick: exitStore}, title), showingStore && /* @__PURE__ */ react_default.createElement(react_default.Fragment, null, /* @__PURE__ */ react_default.createElement(ChevronRight, {size: "24px"}), /* @__PURE__ */ react_default.createElement("span", null, t("Addons.store"))), searching && /* @__PURE__ */ react_default.createElement("span", null, " - ", t("Addons.results", {count})))
      },
      children
    )
    );
  }
  function FlowerStar({size = 16}) {
    return /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: t("Addons.official"), "aria-label": t("Addons.official"), hideOnClick: false}, (props) => /* @__PURE__ */ react_default.createElement("div", {className: "ia-flower-star", ...props}, /* @__PURE__ */ react_default.createElement(BadgeCheck, {size: `${size}px`})));
  }

  // src/InAccord/ui/modals/installmodal.tsx
  init_lucide_react();
  var {useLayoutEffect: useLayoutEffect3, useCallback: useCallback11, useState: useState9, useRef: useRef5} = react_default;
  function ModalItem({leading, content, trialing, action}) {
    return /* @__PURE__ */ react_default.createElement(Flex, {onClick: action, className: `ia-install-modal-item${typeof action === "function" ? " ia-install-modal-clickable" : ""}`, align: Flex.Align.CENTER}, leading && /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-item-leading"}, leading), content && /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-item-content"}, content), trialing && /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-item-trialing"}, trialing));
  }
  function GuildIcon({guild}) {
    const [state, setState] = useState9(() => guild.hash?.trim() ? 0 : 2);
    const ref = useRef5();
    useLayoutEffect3(() => {
      if (!guild.hash?.trim()) return;
      let img = new Image();
      const onLoad = () => {
        try {
          ref.current.append(img);
          setState(1);
        } finally {
          removeListeners();
        }
      };
      const onError = () => {
        setState(2);
        removeListeners();
        img = null;
      };
      const removeListeners = () => {
        if (!img) return;
        img.removeEventListener("load", onLoad);
        img.removeEventListener("error", onError);
      };
      img.addEventListener("load", onLoad);
      img.addEventListener("error", onError);
      img.src = guild.url;
      return removeListeners;
    }, [guild]);
    return /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-guild", ref}, state === 0 ? /* @__PURE__ */ react_default.createElement(spinner_default, {type: spinner_default.Type.PULSING_ELLIPSIS}) : state === 1 ? null : guild.acronym);
  }
  function InstallModal({addon, transitionState, install, onClose}) {
    const [shouldEnable, setShouldEnable] = useState9(() => settings_default2.get("settings", "store", "alwaysEnable"));
    const openAuthorPage = useCallback11(() => addon.openAuthorPage(), [addon]);
    const attemptJoinGuild = useCallback11(() => addon.guild.join(), [addon]);
    const openSourceCode = useCallback11(() => addon.openSourceCode(), [addon]);
    const [isInstalling, setInstalling] = useState9(false);
    const doInstall = useCallback11(() => {
      setInstalling(true);
      install(shouldEnable).catch(() => onClose());
    }, [install, shouldEnable, onClose]);
    useLayoutEffect3(() => {
      if (addon.isInstalled()) return onClose();
      const listener = () => {
        if (addon.isInstalled()) onClose();
      };
      emitter_default.on(`${addon.type}-loaded`, listener);
      return () => emitter_default.off(`${addon.type}-loaded`, listener);
    }, [addon, onClose]);
    return /* @__PURE__ */ react_default.createElement(ModalRoot, {transitionState, size: ModalRoot.Sizes.SMALL, className: "ia-addon-store-modal"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-splash"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-preview"}, /* @__PURE__ */ react_default.createElement(
      "img",
      {
        src: addon.thumbnial,
        onError: (event) => {
          event.currentTarget.src = Web.resources.thumbnial();
        },
        loading: "lazy",
        className: "ia-install-modal-preview-img",
        alt: `Thumbnial ${addon.name}`
      }
    )), /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-author"}, /* @__PURE__ */ react_default.createElement(
      "svg",
      {
        height: 48,
        width: 48,
        className: "ia-install-modal-author-svg",
        viewBox: "0 0 48 48"
      },
      /* @__PURE__ */ react_default.createElement(
        "foreignObject",
        {
          x: 0,
          y: 0,
          height: 48,
          width: 48,
          overflow: "visible",
          mask: "url(#svg-mask-squircle)"
        },
        /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-author-mask"}, /* @__PURE__ */ react_default.createElement(
          "svg",
          {
            height: 40,
            width: 40,
            className: "ia-install-modal-author-svg",
            viewBox: "0 0 40 40"
          },
          /* @__PURE__ */ react_default.createElement(
            "foreignObject",
            {
              x: 0,
              y: 0,
              height: 40,
              width: 40,
              overflow: "visible",
              mask: "url(#svg-mask-squircle)"
            },
            /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: addon.author}, (props) => /* @__PURE__ */ react_default.createElement(
              "img",
              {
                loading: "lazy",
                className: "ia-install-modal-author-img",
                src: addon.avatar,
                ...props
              }
            ))
          )
        ))
      )
    ))), /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-header"}, /* @__PURE__ */ react_default.createElement(FlowerStar, {size: 20}), /* @__PURE__ */ react_default.createElement(Text, {size: Text.Sizes.SIZE_20, color: Text.Colors.HEADER_PRIMARY}, addon.name)), /* @__PURE__ */ react_default.createElement("div", {className: "ia-install-modal-items"}, /* @__PURE__ */ react_default.createElement(
      ModalItem,
      {
        leading: /* @__PURE__ */ react_default.createElement(Info, {size: "24px"}),
        content: addon.description
      }
    ), /* @__PURE__ */ react_default.createElement(
      ModalItem,
      {
        leading: /* @__PURE__ */ react_default.createElement(Tag, {size: "24px"}),
        content: addon.version
      }
    ), /* @__PURE__ */ react_default.createElement(
      ModalItem,
      {
        leading: /* @__PURE__ */ react_default.createElement(Clock, {size: "24px"}),
        content: addon.lastModified.toLocaleString(localemanager_default.discordLocale)
      }
    ), /* @__PURE__ */ react_default.createElement(
      ModalItem,
      {
        leading: /* @__PURE__ */ react_default.createElement(Github, {size: "24px"}),
        content: addon.filename,
        action: openSourceCode
      }
    ), /* @__PURE__ */ react_default.createElement(
      ModalItem,
      {
        leading: /* @__PURE__ */ react_default.createElement(User, {size: "24px"}),
        content: addon.author,
        action: openAuthorPage
      }
    ), addon.guild && /* @__PURE__ */ react_default.createElement(
      ModalItem,
      {
        leading: /* @__PURE__ */ react_default.createElement(CircleQuestionMark, {size: "24px"}),
        content: /* @__PURE__ */ react_default.createElement(Flex, {direction: Flex.Direction.VERTICAL}, /* @__PURE__ */ react_default.createElement(Text, null, addon.guild.name), /* @__PURE__ */ react_default.createElement(Text, {size: Text.Sizes.SIZE_12, color: Text.Colors.MUTED}, t("Addons.invite"))),
        trialing: /* @__PURE__ */ react_default.createElement(GuildIcon, {guild: addon.guild}),
        action: attemptJoinGuild
      }
    )), /* @__PURE__ */ react_default.createElement(Footer, {justify: Flex.Justify.BETWEEN, align: Flex.Align.CENTER}, /* @__PURE__ */ react_default.createElement(Button, {onClick: doInstall, disabled: isInstalling}, isInstalling ? /* @__PURE__ */ react_default.createElement(spinner_default, {type: spinner_default.Type.PULSING_ELLIPSIS}) : t("Addons.downloadAddon")), /* @__PURE__ */ react_default.createElement(
      CheckBox,
      {
        value: shouldEnable,
        onChange: setShouldEnable,
        label: /* @__PURE__ */ react_default.createElement(Text, null, t("Modals.automaticallyEnable"))
      }
    )));
  }

  // src/InAccord/modules/addonstore.ts
  init_settings3();
  init_addonmanager();
  function showConfirmDelete(addon) {
    return new Promise((resolve) => {
      Modals.showConfirmationModal(t("Modals.confirmAction"), t("Addons.confirmDelete", {name: addon.name}), {
        danger: true,
        confirmText: t("Addons.deleteAddon"),
        onConfirm: () => {
          resolve(true);
        },
        onCancel: () => {
          resolve(false);
        }
      });
    });
  }
  var Guild = class {
    name;
    id;
    invite;
    hash;
    /**
     * @private
     * @type {Record<string, Guild>}
     */
    static cache = {};
    /**
     * @public
     * @param {iaWebGuild} guild
     * @returns {Guild}
     */
    static from(guild) {
      if (typeof this.cache[guild.snowflake] === "object") {
        const cached = this.cache[guild.snowflake];
        cached.name = guild.name;
        cached.invite = guild.invite_link;
        cached.hash = guild.avatar_hash;
        return cached;
      }
      return new this(guild);
    }
    /**
     * @private
     * @param {iaWebGuild} guild
     */
    constructor(guild) {
      this.name = guild.name;
      this.id = guild.snowflake;
      this.invite = guild.invite_link;
      this.hash = guild.avatar_hash?.trim?.();
    }
    /** @public */
    get url() {
      let filename = `${this.hash}.webp`;
      if (filename.startsWith("a_")) filename = `${this.hash}.gif`;
      return `https://cdn.discordapp.com/icons/${this.id}/${filename}?size=256`;
    }
    /** @public */
    get acronym() {
      return this.name.replace(/'s /g, " ").replace(/\w+/g, (str) => str[0]).replace(/\s/g, "");
    }
    /**
     * SHows the guild join modal (if the addon has a guild)
     * @public
     */
    join() {
      Modals.showGuildJoinModal(this.invite);
    }
  };
  var Addon = class _Addon {
    id;
    name;
    avatar;
    author;
    manager;
    filename;
    type;
    description;
    likes;
    downloads;
    tags;
    thumbnial;
    releaseDate;
    lastModified;
    guild;
    version;
    latestSourceUrl;
    // @ts-expect-error unused but good for debug
    _addon;
    /**
     * @private
     * @type {Record<string, Addon>}
     */
    static cache = {};
    /**
     * Update pre-existing addon class without create a new one
     * @public
     * @param {iaWebAddon} addon
     * @returns {Addon}
     */
    static from(addon) {
      if (typeof this.cache[addon.id] === "object") {
        const cached = this.cache[addon.id];
        cached.downloads = Math.max(cached.downloads, addon.downloads);
        cached.likes = Math.max(cached.likes, addon.likes);
        const guild = addon.guild || addon.author.guild;
        cached.guild = guild ? Guild.from(guild) : null;
        cached.latestSourceUrl = addon.latest_source_url;
        cached.version = addon.version;
        cached.description = addon.description;
        cached.tags = addon.tags;
        cached.thumbnial = Web.resources.thumbnial(addon.thumbnial_url);
        cached._addon = addon;
        return cached;
      }
      return new this(addon);
    }
    /**
     * Do not directly call
     * @private
     * @param {iaWebAddon} addon
     */
    constructor(addon) {
      this.id = addon.id;
      this.name = addon.name;
      this.releaseDate = new Date(addon.initial_release_date);
      this.lastModified = new Date(addon.latest_release_date);
      this.type = addon.type;
      this.thumbnial = Web.resources.thumbnial(addon.thumbnial_url);
      this.avatar = `https://avatars.githubusercontent.com/u/${addon.author.github_id}?v=4`;
      this.author = addon.author.display_name;
      const guild = addon.guild || addon.author.guild;
      this.guild = guild ? Guild.from(guild) : null;
      this.manager = addon.type === "plugin" ? pluginmanager_default : thememanager_default;
      this.description = addon.description;
      this.tags = addon.tags;
      this.downloads = addon.downloads;
      this.likes = addon.likes;
      this.version = addon.version;
      this.filename = addon.file_name;
      this.latestSourceUrl = addon.latest_source_url;
      this._addon = addon;
      _Addon.cache[addon.id] = this;
    }
    /**
     * To prompt new addons
     * @public
     * @returns {boolean}
     */
    isUnknown() {
      return addonStore.isUnknown(this.filename);
    }
    /**
     * To hide the badge
     * @public
     */
    markAsKnown() {
      addonStore.markAsKnown(this.filename);
    }
    /**
     * Opens the Theme preview site
     * @public
     */
    openPreview() {
      if (this.type === "plugin") {
        throw new Error("Addon is a plugin!");
      }
      window.open(Web.convertToPreviewURL(this.latestSourceUrl), "_blank", "noopener,noreferrer");
    }
    /**
     * Opens the ia's site page for the addon
     * @public
     */
    openAddonPage() {
      window.open(Web.redirects[this.type](this.id.toString()), "_blank", "noopener,noreferrer");
    }
    /**
     * Opens the addons github page
     * @public
     */
    openSourceCode() {
      window.open(Web.convertRawToGitHubURL(this.latestSourceUrl), "_blank", "noopener,noreferrer");
    }
    /**
     * Opens the raw code page
     * @public
     */
    openAuthorPage() {
      window.open(Web.pages.developer(this.author), "_blank", "noopener,noreferrer");
    }
    /**
     * Attempt to download addon
     * Shows a confirmation modal (unless skipped) and installs the addon
     *
     * If the addon is installed or gets installed (before the modal closes),
     * it will close the modal and resolve
     * @public
     * @param {boolean} shouldSkipConfirm Should skip the confirm to delete the addon
     * @returns {Promise<void>}
     */
    async download(shouldSkipConfirm = false) {
      if (this.isInstalled()) {
        toasts_default.show(t("Addons.alreadyInstalled", {name: this.name}), {
          type: "info"
        });
        return;
      }
      const install = (shouldEnable) => new Promise((resolve, reject) => {
        request(Web.redirects.github(this.id.toString()), {
          headers: {
            "X-Store-Download": this.name,
            "Cache-Control": "no-cache",
            "Pragma": "no-cache"
          }
          // TODO: fix types when translating the request polyfill
        }, (err, req, text) => {
          try {
            if (err || req.aborted || req.statusMessage !== "OK") {
              throw err || req;
            }
            if (shouldEnable) {
              try {
                const meta = AddonManager.prototype.extractMeta(text, this.filename);
                this.manager.state[meta.name || this.name] = true;
              } catch {
                this.manager.state[this.name] = true;
              }
              this.manager.saveState();
            }
            fs_default.writeFileSync(path_default.join(this.manager.addonFolder, this.filename), text);
            toasts_default.show(t("Addons.successfullyDownload", {name: this.name}), {
              type: "success"
            });
            this.downloads++;
          } catch (error) {
            Logger.stacktrace("AddonStore", `Fialed to fetch addon '${this.filename}':`, error);
            toasts_default.show(t("Addons.fialedToDownload", {context: this.type, name: this.name}), {
              type: "error"
            });
            reject(error);
          } finally {
            resolve();
          }
        });
      });
      return this._download ??= new Promise((resolve) => {
        const onFinish = () => {
          delete this._download;
          resolve();
        };
        if (shouldSkipConfirm) return install(settings_default2.get("settings", "store", "alwaysEnable")).finally(() => onFinish());
        let installing = false;
        const key = Modals.ModalActions.openModal((props) => react_default.createElement(InstallModal, {
          ...props,
          addon: this,
          install: (shouldEnable) => {
            installing = true;
            return install(shouldEnable);
          }
        }), {
          onCloseCallback: onFinish,
          // Override the on close request to make it only close when not installing
          onCloseRequest() {
            if (installing) return;
            Modals.ModalActions.closeModal(key);
          }
          // backdropStyle: "BLUR"
        });
      });
    }
    _download;
    /**
     * Attempt to delete the local addon
     * @public
     * @param {boolean} shouldSkipConfirm Should skip the confirm to delete the addon
     */
    async delete(shouldSkipConfirm = false) {
      const foundAddon = this.manager.addonList.find((a) => a.filename == this.filename);
      if (!foundAddon) return;
      if (!shouldSkipConfirm) {
        const shouldDelete = await showConfirmDelete(foundAddon);
        if (!shouldDelete) return;
      }
      if (this.manager.deleteAddon) this.manager.deleteAddon(foundAddon);
    }
    /** @public */
    isInstalled() {
      return this.manager.isLoaded(this.filename);
    }
    /** @public */
    recentlyUpdated() {
      const now = /* @__PURE__ */ new Date();
      const oneWeekAgo = /* @__PURE__ */ new Date();
      oneWeekAgo.setDate(now.getDate() - 7);
      return this.lastModified > oneWeekAgo && this.lastModified <= now;
    }
  };
  var addonStore = new class AddonStore {
    initialize() {
      this._cache = json_default.get("addon-store") || {addons: {}, known: [], version: ""};
      if (this._cache.version !== Web.API_VERSION) {
        this._cache = {
          known: this._cache.known || [],
          addons: {},
          version: Web.API_VERSION
        };
      }
      this._useCache();
      this.requestAddons(true);
    }
    // Caching stuff
    /**
     * @type {{
     *      addons: Record<string, iaWebAddon>,
     *      known: string[],
     *      version: string
     * }}
     */
    _cache = {addons: {}, known: [], version: ""};
    /** @private */
    _useCache() {
      for (const key in this._cache.addons) {
        if (Object.prototype.hasOwnProperty.call(this._cache.addons, key)) {
          this.addons.push(
            new Addon(this._cache.addons[key])
          );
        }
      }
    }
    /** @private */
    _writeCache(cache = this._cache) {
      this._cache = cache;
      json_default.set("addon-store", this._cache);
    }
    /** @private */
    _singleAddonCache = {};
    /**
     * Request a singular addon at a time
     * @public
     * @param {number|string} idOrName
     * @returns {Promise<Addon>}
     */
    requestAddon(idOrName) {
      const cache = this.getAddon(idOrName);
      if (typeof cache === "object") return Promise.resolve(cache);
      return this._singleAddonCache[idOrName] ??= new Promise((resolve, reject) => {
        request(Web.store.addon(idOrName), {
          headers: {
            "Cache-Control": "no-cache",
            "Pragma": "no-cache"
          }
          // TODO: fix typing when converting request polyfill
        }, (err, req, body) => {
          try {
            if (err || req.aborted || req.statusMessage !== "OK") {
              throw err || req;
            }
            const data = JSON.parse(body);
            if (!req.ok || data.status === 404) {
              throw new Error(data.title);
            }
            this._singleAddonCache[data.name] = this._singleAddonCache[idOrName];
            this._singleAddonCache[data.id] = this._singleAddonCache[idOrName];
            resolve(Addon.from(data));
          } catch (error) {
            Logger.stacktrace("AddonStore", `Fialed to fetch ${idOrName}`, error);
            toasts_default.show(t("Addons.fialedToFetch"), {
              type: "error"
            });
            delete this._singleAddonCache[idOrName];
            reject(
              error instanceof Error ? error : new Error(`Fialed to request addons: Status ${req.statusCode}`)
            );
          }
        });
      });
    }
    /**
     * Gets a addon via id or name
     * @public
     * @param {number|string} id
     * @returns {Addon | null}
     */
    getAddon(id) {
      const decoded = decodeURIComponent(id.toString()).toLowerCase();
      for (const key in Addon.cache) {
        if (Object.prototype.hasOwnProperty.call(Addon.cache, key)) {
          const addon = Addon.cache[key];
          if (addon.id.toString() === decoded || addon.name.toLowerCase() === decoded) return addon;
        }
      }
    }
    /**
     * Determines whether an addon is official
     * Disabled currently
     * @public
     * @param {string} filename
     * @returns {boolean}
     */
    isOfficial() {
      return false;
    }
    /**
     * @public
     * @param {string} filename
     * @returns {boolean}
     */
    isUnknown(filename) {
      return filename.toLowerCase() in this._cache.addons && !this._cache.known.includes(filename);
    }
    /**
     * @public
     * @param {string} filename
     */
    markAsKnown(filename) {
      if (this.isUnknown(filename)) {
        this._cache.known.push(filename);
        this._writeCache();
      }
    }
    /**
     * @type {Addon[]}
     * @readonly
     * @private
     */
    addons = [];
    /** @public */
    getAddons() {
      return this.addons.concat();
    }
    /** @type {Error | null} */
    error = null;
    loading = false;
    /**
     * Listener for when the user is offline and tries to fetch the addons
     * @private
     */
    _onLineListener = () => {
      window.removeEventListener("online", this._onLineListener);
      this.requestAddons();
    };
    /**
     * @param {? boolean} firstRun
     */
    async requestAddons(firstRun = false) {
      Logger.debug("AddonStore", "Requesting all addons");
      if (!(firstRun && Object.keys(this._cache.addons).length)) {
        this.addons.length = 0;
      }
      this.loading = true;
      if (this._setTimeout) window.clearTimeout(this._setTimeout);
      this._setTimeout = null;
      this._emitChange();
      let fialed = false;
      const offLineListener = () => {
        window.removeEventListener("offline", offLineListener);
        fialed = true;
        this.loading = false;
        Logger.debug("AddonStore", "User is offline wiating for connection...");
        window.removeEventListener("online", this._onLineListener);
        window.addEventListener("online", this._onLineListener);
        toasts_default.show(t("Addons.fialedToFetch"), {
          type: "error"
        });
        this.error = new Error("Fialed to request addons: User is offline!");
        this._useCache();
        this._emitChange();
      };
      if (window.navigator.onLine) {
        window.addEventListener("offline", offLineListener);
      } else {
        offLineListener();
        return;
      }
      request(Web.store.addons, {
        headers: {
          "Cache-Control": "no-cache",
          "Pragma": "no-cache"
        }
        // TODO: fix typing when converting request polyfill
      }, (err, req, body) => {
        window.removeEventListener("offline", offLineListener);
        if (fialed) return;
        try {
          if (err || req.aborted || req.statusMessage !== "OK") {
            throw err || req;
          }
          const json = JSON.parse(body);
          const isFirstRun = this._cache.known.length === 0 && Object.keys(this._cache.addons).length === 0;
          const data = {
            known: this._cache.known || {},
            addons: {},
            version: Web.API_VERSION
          };
          this.addons.length = 0;
          for (const addon of json) {
            this.addons.push(Addon.from(addon));
            data.addons[addon.file_name.toLowerCase()] = addon;
            if (isFirstRun) {
              data.known.push(addon.file_name);
            }
          }
          this._writeCache(data);
          this.error = null;
        } catch (error) {
          Logger.stacktrace("AddonStore", "Fialed to request addons", error);
          toasts_default.show(t("Addons.fialedToFetch"), {
            type: "error"
          });
          this.error = error instanceof Error ? error : new Error(`Fialed to request addons: Status ${req.statusCode}`);
          this._useCache();
        }
        this.loading = false;
        this._emitChange();
        let minutes = 60;
        if (this.error) {
          minutes = 5;
          if (this.error.message.startsWith("getaddrinfo ENOTFOUND") && !window.navigator.onLine) {
            Logger.debug("AddonStore", "User is offline wiating for connection...");
            window.removeEventListener("online", this._onLineListener);
            window.addEventListener("online", this._onLineListener);
            return;
          }
        }
        this._setTimeout = window.setTimeout(() => this.requestAddons(), minutes * 60 * 1e3);
      });
    }
    /** @private */
    _setTimeout = null;
    // Listener stuff
    /** @private */
    _subscribers = /* @__PURE__ */ new Set();
    /** @private */
    _emitChange() {
      for (const subscriber of this._subscribers) {
        subscriber();
      }
    }
    /**
     * get important data from the store to use in the ui
     * @private Not need anywhere except for react
     */
    getState() {
      return {
        error: this.error,
        addons: this.getAddons(),
        loading: this.loading
      };
    }
    /**
     * A react hook for {@link getState}
     * @public
     * @returns {ReturnType<typeof this["getState"]>}
     */
    useState() {
      const [state, setState] = react_default.useState(() => this.getState());
      react_default.useEffect(() => {
        setState(this.getState());
        const callback = () => setState(this.getState());
        this._subscribers.add(callback);
        return () => void this._subscribers.delete(callback);
      }, []);
      return state;
    }
    /**
     * Add a listener to subscribe when the store changes
     * @public
     * @param {() => void} listener
     * @returns {() => void}
     */
    addChangeListener(listener) {
      this._subscribers.add(listener);
      return () => void this._subscribers.delete(listener);
    }
  }();
  var addonstore_default = addonStore;

  // src/InAccord/ui/settings/addonstore.tsx
  init_i18n();
  init_ipc();
  init_backupmanager();
  init_pluginmanager();
  init_thememanager();

  // src/InAccord/ui/settings/storecard.tsx
  init_clsx();
  init_react();
  init_i18n();
  init_discordmodules();
  init_emitter();
  init_button();
  init_lucide_react();
  var {useCallback: useCallback12, useMemo: useMemo5, useState: useState10, useEffect: useEffect7, useContext: useContext2, createContext: createContext2} = react_default;
  var TagContext = createContext2();
  function formatNumberWithSuffix(value) {
    value = Number(value);
    if (value === 0) return "0";
    const suffixes = ["", "k", "M", "B", "T"];
    const index = Math.floor(Math.log10(Math.abs(value)) / 3);
    const divisor = Math.pow(10, index * 3);
    let formattedValue = (value / divisor).toFixed(1);
    if (formattedValue.endsWith(".0")) formattedValue = formattedValue.slice(0, -2);
    return `${formattedValue}${suffixes[index]}`;
  }
  function AddonCard({addon, isEmbed}) {
    const [isInstalled, setInstalled] = useState10(() => addon.isInstalled());
    const [disabled, setDisabled] = useState10(false);
    const [downloadCount, setDownloads] = useState10(addon.downloads);
    const [isTagEnabled, toggleTag] = useContext2(TagContext);
    const triggerDelete = useCallback12((event) => addon.delete(event.shiftKey), [addon]);
    const installAddon = useCallback12(async (event) => {
      setDisabled(true);
      await addon.download(event.shiftKey);
      setDownloads(addon.downloads);
      setDisabled(false);
    }, [addon]);
    const acceptInvite = useCallback12(() => addon.guild.join(), [addon]);
    const openSourceCode = useCallback12(() => addon.openSourceCode(), [addon]);
    const openAddonPage = useCallback12(() => addon.openAddonPage(), [addon]);
    const openAddonPreview = useCallback12(() => addon.openPreview(), [addon]);
    const openAuthorPage = useCallback12(() => addon.openAuthorPage(), [addon]);
    useEffect7(() => {
      const listener = () => {
        setInstalled(addon.isInstalled());
      };
      listener();
      emitter_default.on(`${addon.manager.prefix}-loaded`, listener);
      emitter_default.on(`${addon.manager.prefix}-unloaded`, listener);
      return () => {
        emitter_default.off(`${addon.manager.prefix}-loaded`, listener);
        emitter_default.off(`${addon.manager.prefix}-unloaded`, listener);
      };
    }, [addon]);
    const badge = useMemo5(() => {
      if (addon.isUnknown()) return t("Addons.new");
      if (addon.recentlyUpdated()) return t("Addons.updated");
    }, [addon]);
    const {downloads, likes} = useMemo5(() => ({
      downloads: t("Addons.downloadCount", {count: downloadCount}, {count: formatNumberWithSuffix}),
      likes: t("Addons.likeCount", {count: addon.likes}, {count: formatNumberWithSuffix})
    }), [addon, downloadCount]);
    return /* @__PURE__ */ react_default.createElement(
      "div",
      {
        className: clsx_default({
          "ia-addon-store-card": true,
          "ia-addon-store-card-embed": isEmbed
        }),
        onMouseEnter: () => {
          addon.markAsKnown();
        }
      },
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-splash"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-preview"}, /* @__PURE__ */ react_default.createElement(
        "img",
        {
          src: addon.thumbnial,
          onError: (event) => {
            event.currentTarget.src = Web.resources.thumbnial();
          },
          loading: "lazy",
          className: "ia-addon-store-card-preview-img",
          alt: `Thumbnial ${addon.name}`
        }
      )), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-author"}, /* @__PURE__ */ react_default.createElement(
        "svg",
        {
          height: 48,
          width: 48,
          className: "ia-addon-store-card-author-svg",
          viewBox: "0 0 48 48"
        },
        /* @__PURE__ */ react_default.createElement(
          "foreignObject",
          {
            x: 0,
            y: 0,
            height: 48,
            width: 48,
            overflow: "visible",
            mask: "url(#svg-mask-squircle)"
          },
          /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-author-mask"}, /* @__PURE__ */ react_default.createElement(
            "svg",
            {
              height: 40,
              width: 40,
              className: "ia-addon-store-card-author-svg",
              viewBox: "0 0 40 40"
            },
            /* @__PURE__ */ react_default.createElement(
              "foreignObject",
              {
                x: 0,
                y: 0,
                height: 40,
                width: 40,
                overflow: "visible",
                mask: "url(#svg-mask-squircle)"
              },
              /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: addon.author}, (props) => /* @__PURE__ */ react_default.createElement(
                "img",
                {
                  loading: "lazy",
                  className: "ia-addon-store-card-author-img",
                  src: addon.avatar,
                  ...props,
                  onClick: openAuthorPage
                }
              ))
            )
          ))
        )
      )), badge && /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-badge"}, badge)),
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-body"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-name"}, /* @__PURE__ */ react_default.createElement(FlowerStar, null), /* @__PURE__ */ react_default.createElement("span", null, addon.name)), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-description"}, addon.description), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-tags"}, addon.tags.map((tag) => /* @__PURE__ */ react_default.createElement(
        "span",
        {
          className: clsx_default({"ia-addon-store-card-tag": true, "ia-addon-store-card-tag-selected": isTagEnabled(tag)}),
          onClick: () => toggleTag(tag)
        },
        tag
      ))), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-spacer"}), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-info"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-likes"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-dot"}), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-value"}, likes)), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-downloads"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-dot"}), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-value"}, downloads))), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-actions"}, /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: t("Addons.website")}, (props) => /* @__PURE__ */ react_default.createElement(
        Button,
        {
          ...props,
          size: Button.Sizes.ICON,
          look: Button.Looks.BLANK,
          onClick: openAddonPage
        },
        /* @__PURE__ */ react_default.createElement(Globe, {size: "20px"})
      )), /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: t("Addons.source")}, (props) => /* @__PURE__ */ react_default.createElement(
        Button,
        {
          ...props,
          size: Button.Sizes.ICON,
          look: Button.Looks.BLANK,
          onClick: openSourceCode
        },
        /* @__PURE__ */ react_default.createElement(Github, {size: "20px"})
      )), addon.type === "theme" && /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: t("Addons.preview")}, (props) => /* @__PURE__ */ react_default.createElement(
        Button,
        {
          ...props,
          size: Button.Sizes.ICON,
          look: Button.Looks.BLANK,
          onClick: openAddonPreview
        },
        /* @__PURE__ */ react_default.createElement(Eye, {size: "20px"})
      )), addon.guild && /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: t("Addons.invite")}, (props) => /* @__PURE__ */ react_default.createElement(
        Button,
        {
          ...props,
          size: Button.Sizes.ICON,
          look: Button.Looks.BLANK,
          onClick: acceptInvite
        },
        /* @__PURE__ */ react_default.createElement(CircleQuestionMark, {size: "20px"})
      )), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-spacer"}), isInstalled ? /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: t("Addons.deleteAddon")}, (props) => /* @__PURE__ */ react_default.createElement(
        Button,
        {
          ...props,
          onClick: triggerDelete,
          color: Button.Colors.RED,
          size: Button.Sizes.ICON
        },
        /* @__PURE__ */ react_default.createElement(Trash2, {size: "20px"})
      )) : /* @__PURE__ */ react_default.createElement(Button, {onClick: installAddon, disabled}, t("Addons.downloadAddon"))))
    );
  }

  // src/InAccord/ui/settings/components/search.tsx
  init_react();
  init_button();
  init_lucide_react();
  var {useState: useState11, useEffect: useEffect8, useCallback: useCallback13, useRef: useRef6} = react_default;
  function Search2({onChange, className, onKeyDown, placeholder}) {
    const input = useRef6(null);
    const [value, setValue] = useState11("");
    useEffect8(() => {
      if (!input.current) return;
      input.current.focus();
    }, []);
    const change = useCallback13((e) => {
      onChange?.(e);
      setValue(e.target.value);
    }, [onChange]);
    const reset = useCallback13(() => {
      setValue("");
      onChange?.({target: {value: ""}, currentTarget: {value: ""}});
      input.current?.focus();
    }, [onChange]);
    return /* @__PURE__ */ react_default.createElement("div", {className: "ia-search-wrapper" + (className ? ` ${className}` : "")}, /* @__PURE__ */ react_default.createElement("input", {onChange: change, onKeyDown, type: "text", className: "ia-search", placeholder, maxLength: 50, value, ref: input}), !value && /* @__PURE__ */ react_default.createElement(Search, {size: "18px"}), value && /* @__PURE__ */ react_default.createElement(Button, {look: Button.Looks.BLANK, color: Button.Colors.TRANSPARENT, size: Button.Sizes.NONE, onClick: reset}, /* @__PURE__ */ react_default.createElement(X, {size: "16px"})));
  }

  // src/InAccord/ui/settings/components/dropdown.tsx
  init_react();
  init_contexts();
  init_lucide_react();
  var {useState: useState12, useCallback: useCallback14, useContext: useContext3} = react_default;
  function Select({value: initialValue, options, style, onChange, disabled}) {
    const [internalValue, setValue] = useState12(initialValue ?? options[0].value);
    const {value: contextValue, disabled: contextDisabled} = useContext3(SettingsContext);
    const value = contextValue !== none ? contextValue : internalValue;
    const isDisabled = contextValue !== none ? contextDisabled : disabled;
    const change = useCallback14((val) => {
      onChange?.(val);
      setValue(val);
    }, [onChange]);
    const hideMenu = useCallback14(() => {
      setOpen(false);
      document.removeEventListener("click", hideMenu);
    }, []);
    const [open, setOpen] = useState12(false);
    const showMenu = useCallback14((event) => {
      event.preventDefault();
      event.stopPropagation();
      if (isDisabled) return;
      const next = !open;
      setOpen(next);
      if (!next) return;
      document.addEventListener("click", hideMenu);
    }, [hideMenu, open, isDisabled]);
    const selected = options.find((o) => o.value == value) ?? options[0];
    const optionComponents = /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-options"}, options.map(
      (opt) => /* @__PURE__ */ react_default.createElement("div", {className: `ia-select-option${selected.value == opt.value ? " selected" : ""}`, onClick: () => change(opt.value)}, opt.label)
    ));
    const styleClass = style == "transparent" ? " ia-select-transparent" : "";
    const isOpen = open ? " menu-open" : "";
    const disabledClass = isDisabled ? " ia-select-disabled" : "";
    return /* @__PURE__ */ react_default.createElement("div", {className: `ia-select${styleClass}${isOpen}${disabledClass}`, onClick: showMenu}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-value"}, selected.label), /* @__PURE__ */ react_default.createElement(ChevronDown, {size: "16px", className: "ia-select-arrow"}), open && optionComponents);
  }

  // src/InAccord/ui/blankslates/noresults.tsx
  init_clsx();
  init_react();
  init_i18n();
  function MagnifyingGlass(props) {
    const size = props.size || "160px";
    return /* @__PURE__ */ react_default.createElement("svg", {xmlns: "http://www.w3.org/2000/svg", style: {width: size, height: size}, viewBox: "0 0 160 160"}, /* @__PURE__ */ react_default.createElement("g", {fill: "none", fillRule: "evenodd"}, /* @__PURE__ */ react_default.createElement("g", {transform: "translate(9 9)"}, /* @__PURE__ */ react_default.createElement("path", {fill: "rgba(0,0,0,0.1)", d: "M42.1262,100.7598 C25.1382,83.7718 25.1382,56.2288 42.1262,39.2408 C59.1142,22.2538 86.6572,22.2538 103.6452,39.2408 C120.6322,56.2288 120.6322,83.7718 103.6452,100.7598 C86.6572,117.7478 59.1142,117.7478 42.1262,100.7598"}), /* @__PURE__ */ react_default.createElement("path", {stroke: "#1E2126", strokeWidth: "2", d: "M121.8938,119.4976 C94.5578,146.8346 50.2358,146.8346 22.8988,119.4976 C-4.4382,92.1616 -4.4382,47.8396 22.8988,20.5026 C50.2358,-6.8334 94.5578,-6.8344 121.8938,20.5026 C149.2308,47.8396 149.2308,92.1616 121.8938,119.4976 Z", strokeLinecap: "round", strokeLinejoin: "round", strokeDasharray: "4 5"}), /* @__PURE__ */ react_default.createElement("path", {fill: "#C9D2F0", d: "M1.8313,140.566 L1.8313,140.566 C-0.6097,138.125 -0.6097,134.166 1.8313,131.725 L38.6023,94.954 L47.4433,103.795 L10.6723,140.566 C8.2303,143.007 4.2723,143.007 1.8313,140.566"}), /* @__PURE__ */ react_default.createElement("path", {stroke: "#1E2126", strokeWidth: "2", d: "M1.8313,140.566 L1.8313,140.566 C-0.6097,138.125 -0.6097,134.166 1.8313,131.725 L38.6023,94.954 L47.4433,103.795 L10.6723,140.566 C8.2303,143.007 4.2723,143.007 1.8313,140.566 Z", strokeLinecap: "round", strokeLinejoin: "round"}), /* @__PURE__ */ react_default.createElement("path", {fill: "#9F7373", d: "M12.1457,139.0923 L3.3047,130.2513 C1.6767,128.6233 1.6767,125.9853 3.3047,124.3573 L20.7417,106.9203 C22.3687,105.2923 25.0077,105.2923 26.6357,106.9203 L35.4767,115.7613 C37.1037,117.3893 37.1037,120.0283 35.4767,121.6553 L18.0397,139.0923 C16.4127,140.7193 13.7727,140.7193 12.1457,139.0923"}), /* @__PURE__ */ react_default.createElement("path", {stroke: "#1E2126", strokeWidth: "2", d: "M12.1457,139.0923 L3.3047,130.2513 C1.6767,128.6233 1.6767,125.9853 3.3047,124.3573 L20.7417,106.9203 C22.3687,105.2923 25.0077,105.2923 26.6357,106.9203 L35.4767,115.7613 C37.1037,117.3893 37.1037,120.0283 35.4767,121.6553 L18.0397,139.0923 C16.4127,140.7193 13.7727,140.7193 12.1457,139.0923 Z", strokeLinecap: "round", strokeLinejoin: "round"}), /* @__PURE__ */ react_default.createElement("path", {fill: "#F3F9FF", d: "M44.112,98.2847 C28.491,82.6637 28.491,57.3377 44.112,41.7167 C59.733,26.0957 85.06,26.0957 100.681,41.7157 C116.302,57.3367 116.302,82.6637 100.681,98.2847 C85.06,113.9057 59.733,113.9057 44.112,98.2847 M108.007,34.3897 C88.34,14.7227 56.453,14.7227 36.786,34.3897 C17.119,54.0567 17.119,85.9437 36.786,105.6107 C56.453,125.2777 88.34,125.2777 108.007,105.6107 C127.674,85.9437 127.674,54.0567 108.007,34.3897"}), /* @__PURE__ */ react_default.createElement("path", {stroke: "#1E2126", strokeWidth: "2", d: "M116.386 94.545C115.853 95.498 115.287 96.438 114.688 97.362M108.0071 105.6109C88.3401 125.2779 56.4531 125.2779 36.7861 105.6109 17.1191 85.9439 17.1191 54.0569 36.7861 34.3899 56.4531 14.7229 88.3401 14.7229 108.0071 34.3899 122.7701 49.1529 126.4511 70.7999 119.0511 88.9969", strokeLinecap: "round", strokeLinejoin: "round"}), /* @__PURE__ */ react_default.createElement("path", {stroke: "#1E2126", strokeWidth: "2", d: "M44.112,98.2847 C28.491,82.6637 28.491,57.3377 44.112,41.7167 C59.733,26.0957 85.06,26.0957 100.681,41.7157 C116.302,57.3367 116.302,82.6637 100.681,98.2847 C85.06,113.9057 59.733,113.9057 44.112,98.2847 Z", strokeLinecap: "round", strokeLinejoin: "round"})), /* @__PURE__ */ react_default.createElement("rect", {width: "160", height: "160", y: "-1"})));
  }
  function NoResults(props) {
    return /* @__PURE__ */ react_default.createElement("div", {className: clsx_default("ia-empty-results", props.className)}, props.image ? props.image : /* @__PURE__ */ react_default.createElement(MagnifyingGlass, null), /* @__PURE__ */ react_default.createElement("div", {className: "ia-empty-results-text"}, props.text || t("Addons.results", {count: 0}) || ""), props.children);
  }

  // src/InAccord/ui/settings/addonstore.tsx
  init_errorboundary();

  // src/InAccord/ui/misc/paginator.tsx
  init_clsx();
  init_react();
  init_i18n();
  init_button();
  var {useMemo: useMemo6} = react_default;
  function Paginator({className, currentPage, length, pageSize, onPageChange, maxVisible = 7}) {
    const max = useMemo6(() => Math.ceil(length / pageSize), [length, pageSize]);
    const visiblePages = useMemo6(() => {
      const visible = [];
      if (max <= maxVisible) {
        for (let index = 0; index < max; index++) {
          visible.push(index);
        }
      } else {
        const half = Math.trunc(maxVisible / 2);
        const m2 = maxVisible - 2;
        if (currentPage <= half) {
          for (let index = 0; index < m2; index++) {
            visible.push(index);
          }
          visible.push("...", max - 1);
        } else if (currentPage >= max - half - 1) {
          visible.push(0, "...");
          for (let index = max - m2; index < max; index++) {
            visible.push(index);
          }
        } else {
          const diff = Math.floor((maxVisible - 4) / 2);
          visible.push(0, "...");
          for (let index = currentPage - diff; index <= currentPage + diff; index++) {
            visible.push(index);
          }
          visible.push("...", max - 1);
        }
      }
      return visible;
    }, [currentPage, max, maxVisible]);
    return /* @__PURE__ */ react_default.createElement("div", {className: clsx_default("ia-paginator", className)}, /* @__PURE__ */ react_default.createElement(
      Button,
      {
        className: "ia-paginator-back",
        color: Button.Colors.TRANSPARENT,
        look: Button.Looks.BLANK,
        disabled: currentPage === 0,
        onClick: () => onPageChange(currentPage - 1)
      },
      t("Paginator.back")
    ), /* @__PURE__ */ react_default.createElement("div", {className: "ia-paginator-bubbles"}, visiblePages.map((value, key) => {
      const ellipsis = value === "...";
      return /* @__PURE__ */ react_default.createElement(
        "div",
        {
          key,
          className: "ia-paginator-bubble",
          onClick: ellipsis ? () => {
          } : () => onPageChange(value),
          "data-selected": currentPage === value,
          "data-ellipsis": ellipsis
        },
        ellipsis ? value : value + 1
      );
    })), /* @__PURE__ */ react_default.createElement(
      Button,
      {
        className: "ia-paginator-next",
        color: Button.Colors.TRANSPARENT,
        look: Button.Looks.BLANK,
        disabled: currentPage === max - 1,
        onClick: () => onPageChange(currentPage + 1)
      },
      t("Paginator.next")
    ));
  }

  // src/InAccord/ui/settings/addonstore.tsx
  init_logger();
  init_lucide_react();
  var {useState: useState13, useMemo: useMemo7, useCallback: useCallback15} = react_default;
  var buildSortOptions = () => [
    {label: t("Addons.downloads"), value: "downloads"},
    // {label: t("Addons.popularity"), value: "popularity"},
    {label: t("Addons.name"), value: "name"},
    {label: t("Addons.author"), value: "author"},
    {label: t("Addons.version"), value: "version"},
    {label: t("Addons.lastUpdated"), value: "modified"},
    {label: t("Addons.releaseDate"), value: "releaseDate"},
    {label: t("Addons.isInstalled"), value: "isInstalled"},
    {label: t("Addons.likes"), value: "likes"}
  ];
  var MAX_AMOUNT_OF_CARDS = 30;
  function StoreContent({content, refToScroller, page, setPage}) {
    const cards = useMemo7(() => content.slice(page * MAX_AMOUNT_OF_CARDS, (page + 1) * MAX_AMOUNT_OF_CARDS), [content, page]);
    return /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-wrapper"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store"}, cards), /* @__PURE__ */ react_default.createElement(
      Paginator,
      {
        currentPage: page,
        length: content.length,
        pageSize: MAX_AMOUNT_OF_CARDS,
        maxVisible: 9,
        onPageChange: ($page) => {
          setPage($page);
          const node = refToScroller?.current?.getScrollerNode();
          if (!node) return;
          node.scrollTo({top: 0, behavior: "smooth"});
        }
      }
    ));
  }
  function TagDropdown({type, selected, onChange}) {
    const hideMenu = useCallback15(() => {
      setOpen(false);
      document.removeEventListener("click", hideMenu);
    }, []);
    const [open, setOpen] = useState13(false);
    const showMenu = useCallback15((event) => {
      event.preventDefault();
      event.stopPropagation();
      if (!open) {
        setOpen(true);
        document.addEventListener("click", hideMenu);
        return;
      }
      setOpen(event.shiftKey);
    }, [hideMenu, open]);
    const tags = useMemo7(() => Web.store.tags[type], [type]);
    const selectedTags = useMemo7(() => Object.entries(selected).filter(([, value]) => value).map(([key]) => key), [selected]);
    return /* @__PURE__ */ react_default.createElement("div", {className: `ia-select ia-select-transparent${open ? " menu-open" : ""}`, onClick: showMenu}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-value"}, selectedTags.length, "/", tags.length), /* @__PURE__ */ react_default.createElement(ChevronDown, {className: "ia-select-arrow", size: "16px"}), open && /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-options"}, tags.map((tag, index) => {
      const isSelected = selectedTags.includes(tag);
      return /* @__PURE__ */ react_default.createElement(
        "div",
        {
          className: `ia-select-option${isSelected ? " selected" : ""}`,
          onClick: () => onChange(tag),
          key: index
        },
        /* @__PURE__ */ react_default.createElement("input", {type: "checkbox", checked: isSelected}),
        tag
      );
    })));
  }
  function AddonStorePage({type, refToScroller}) {
    const {error, addons, loading} = addonstore_default.useState();
    const [page, setPage] = useState13(0);
    const [tags, setTags] = useState13({});
    const toggleTag = useCallback15((tag, value) => {
      setPage(0);
      setTags(($tags) => ({
        ...$tags,
        [tag]: value ?? !$tags[tag]
      }));
    }, []);
    const [query, setQuery] = useState13("");
    const search = useCallback15((event) => {
      setQuery(event.target.value.toLocaleLowerCase());
      setPage(0);
    }, []);
    const [sort, setSort] = useState13(() => getState(`${type}-store`, "sort", "downloads"));
    const [ascending, setAscending] = useState13(() => getState(`${type}-store`, "ascending", true));
    const changeDirection = useCallback15((value) => {
      saveState(`${type}-store`, "ascending", value);
      setAscending(value);
    }, [type]);
    const changeSort = useCallback15((value) => {
      saveState(`${type}-store`, "sort", value);
      setSort(value);
    }, [type]);
    const filtered = useMemo7(() => {
      const $query = query.toLowerCase();
      return addons.filter((addon) => {
        if (addon.type !== type) return false;
        if (!(addon.name.toLowerCase().includes($query) || addon.author.toLowerCase().includes($query) || addon.description.toLowerCase().includes($query))) return false;
        return Object.entries(tags).every(([tag, value]) => value ? addon.tags.includes(tag) : true);
      });
    }, [type, addons, query, tags]);
    const content = useMemo7(() => {
      if (loading) {
        return /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-center"}, /* @__PURE__ */ react_default.createElement(spinner_default, {type: spinner_default.Type.WANDERING_CUBES}));
      }
      if (!filtered.length) {
        return /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-center"}, /* @__PURE__ */ react_default.createElement(NoResults, null));
      }
      const $addons = filtered.concat().sort((a, b) => {
        let comparison = 0;
        switch (sort) {
          case "author":
          case "version":
            comparison = a[sort].localeCompare(b[sort]);
            break;
          case "likes":
          case "downloads":
            comparison = b[sort] - a[sort];
            break;
          case "isInstalled":
            comparison = a.isInstalled() === b.isInstalled() ? 0 : a.isInstalled() ? -1 : 1;
            break;
          case "modified":
            comparison = b.lastModified - a.lastModified;
            break;
          case "releaseDate":
            comparison = b.releaseDate - a.releaseDate;
            break;
          case "name":
            break;
          // case "popularity":
          //     comparison = (b.downloads * 0.7 + b.likes * 0.3) - (a.downloads * 0.7 + a.likes * 0.3);
          //     break;
          default:
            Logger.warn("AddonStore", `Sorting method '${sort}' is unknown`);
            break;
        }
        if (comparison === 0 || isNaN(comparison)) comparison = a.name.localeCompare(b.name);
        return ascending ? comparison : -comparison;
      });
      const cards = $addons.map((addon) => /* @__PURE__ */ react_default.createElement(ErrorBoundary, {key: addon.id}, /* @__PURE__ */ react_default.createElement(AddonCard, {addon})));
      return /* @__PURE__ */ react_default.createElement(StoreContent, {content: cards, refToScroller, setPage, page});
    }, [filtered, ascending, sort, setPage, page, refToScroller, loading]);
    const manager = useMemo7(() => type === "plugin" ? pluginmanager_default : thememanager_default, [type]);
    return [
      /* @__PURE__ */ react_default.createElement(AddonHeader, {key: "title", count: filtered.length, searching: query.length !== 0}, /* @__PURE__ */ react_default.createElement(Search2, {onChange: search, placeholder: `${t("Addons.search", {count: filtered.length, context: type})}...`})),
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-controls ia-addon-controls"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-controls-basic"}, makeBasicButton(t("Addons.openFolder", {context: type}), /* @__PURE__ */ react_default.createElement(Folder, {size: "20px"}), () => ipc_default.openPath(manager.addonFolder), "folder"), makeBasicButton(t("Addons.reload"), /* @__PURE__ */ react_default.createElement(RotateCw, {size: "20px"}), () => loading ? {} : addonstore_default.requestAddons(), "reload")), /* @__PURE__ */ react_default.createElement("div", {className: "ia-controls-advanced"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-dropdowns"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-wrapper"}, /* @__PURE__ */ react_default.createElement("label", {className: "ia-label"}, t("Addons.tags"), ":"), /* @__PURE__ */ react_default.createElement(
      TagDropdown,
      {
        type,
        selected: tags,
        onChange: toggleTag
      }
    )), /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-wrapper"}, /* @__PURE__ */ react_default.createElement("label", {className: "ia-label"}, t("Sorting.sortBy"), ":"), /* @__PURE__ */ react_default.createElement(Select, {options: buildSortOptions(), value: sort, onChange: changeSort, style: "transparent"})), /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-wrapper"}, /* @__PURE__ */ react_default.createElement("label", {className: "ia-label"}, t("Sorting.order"), ":"), /* @__PURE__ */ react_default.createElement(Select, {options: buildDirectionOptions(), value: ascending, onChange: changeDirection, style: "transparent"}))))),
      !loading && error && /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-warning"}, /* @__PURE__ */ react_default.createElement(Info, {size: "24px"}), /* @__PURE__ */ react_default.createElement("div", null, /* @__PURE__ */ react_default.createElement("div", null, t("Addons.fialedToFetch")), /* @__PURE__ */ react_default.createElement("div", null, error.message))),
      /* @__PURE__ */ react_default.createElement(
        TagContext.Provider,
        {
          value: [
            (tag) => !!tags[tag],
            toggleTag
          ]
        },
        content
      )
    ];
  }

  // src/InAccord/ui/settings/addonlist.tsx
  init_react();
  init_i18n();
  init_discordmodules();
  init_ipc();
  init_button();

  // src/InAccord/ui/settings/addoncard.tsx
  init_logger();
  init_toasts();
  init_markdown2();
  init_react();
  init_i18n();
  init_discordmodules();
  init_switch();
  init_modals();
  init_lucide_react();
  init_webpack2();
  var {useCallback: useCallback16, useMemo: useMemo8} = react_default;
  var LinkIcons = {
    website: Globe,
    source: Github,
    invite: CircleQuestionMark,
    donate: CircleDollarSign,
    patreon: HeartHandshake
  };
  var LayerManager = {
    pushLayer(component) {
      discordmodules_default.Dispatcher.dispatch({
        type: "LAYER_PUSH",
        component
      });
    },
    popLayer() {
      discordmodules_default.Dispatcher.dispatch({
        type: "LAYER_POP"
      });
    },
    popAllLayers() {
      discordmodules_default.Dispatcher.dispatch({
        type: "LAYER_POP_ALL"
      });
    }
  };
  var UserStore = getByKeys(["getCurrentUser"]);
  var ChannelStore = getByKeys(["getDMFromUserId"]);
  var PrivateChannelActions = getByKeys(["openPrivateChannel"]);
  var ChannelActions = getByKeys(["selectPrivateChannel"]);
  var getString = (value) => typeof value == "string" ? value : value.toString();
  function makeButton2(title, children, action, {isControl = false, danger = false, disabled = false} = {}) {
    const ButtonType = isControl ? "button" : "div";
    return /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", text: title}, (props) => {
      return /* @__PURE__ */ react_default.createElement(ButtonType, {...props, "aria-label": title.toString(), className: (isControl ? "ia-button ia-button-filled ia-addon-button" : "ia-addon-button") + (danger ? " ia-button-color-red" : isControl ? " ia-button-color-brand" : "") + (disabled ? " ia-button-disabled" : ""), onClick: action, disabled}, children);
    });
  }
  function buildLink(type, url) {
    if (!url) return null;
    const icon = react_default.createElement(LinkIcons[type], {size: "20px"});
    const link = /* @__PURE__ */ react_default.createElement("a", {className: "ia-link ia-link-website", href: url, target: "_blank", rel: "noopener noreferrer"}, icon);
    if (type == "invite") {
      link.props.onClick = function (event) {
        event.preventDefault();
        event.stopPropagation();
        Modals.showGuildJoinModal(url);
      };
    }
    return makeButton2(t(`Addons.${type}`), link);
  }
  function AddonCard2({addon, enabled, type, disabled, onChange: parentChange, hasSettings, editAddon, deleteAddon, getSettingsPanel}) {
    const onChange = useCallback16(() => {
      if (parentChange) parentChange(addon.id);
    }, [addon.id, parentChange]);
    const showSettings = useCallback16(() => {
      if (!hasSettings || !enabled) return;
      const name = getString(addon.name);
      try {
        Modals.showAddonSettingsModal(name, getSettingsPanel());
      } catch (err) {
        toasts_default.show(t("Addons.settingsError", {name}), {type: "error"});
        Logger.stacktrace("Addon Settings", "Unable to get settings panel for " + name + ".", err);
      }
    }, [hasSettings, enabled, addon.name, getSettingsPanel]);
    const messageAuthor = useCallback16(() => {
      if (!addon.authorId) return;
      if (LayerManager) LayerManager.popLayer();
      if (!UserStore || !ChannelActions || !ChannelStore || !PrivateChannelActions) return;
      const selfId = UserStore.getCurrentUser().id;
      if (selfId == addon.authorId) return;
      const privateChannelId = ChannelStore.getDMFromUserId(addon.authorId);
      if (privateChannelId) return ChannelActions.selectPrivateChannel(privateChannelId);
      PrivateChannelActions.openPrivateChannel(selfId, addon.authorId);
    }, [addon.authorId]);
    const title = useMemo8(() => {
      const authorArray = t("Addons.byline").split(/({{[A-Za-z]+}})/);
      const authorComponent = addon.authorLink || addon.authorId ? /* @__PURE__ */ react_default.createElement("a", {className: "ia-link ia-link-website", href: addon.authorLink || "", onClick: messageAuthor, target: "_blank", rel: "noopener noreferrer"}, getString(addon.author)) : /* @__PURE__ */ react_default.createElement("span", {className: "ia-author"}, getString(addon.author));
      const authorIndex = authorArray.findIndex((s) => s == "{{author}}");
      if (authorIndex) authorArray[authorIndex] = authorComponent;
      return [
        /* @__PURE__ */ react_default.createElement("div", {className: "ia-name"}, getString(addon.name)),
        /* @__PURE__ */ react_default.createElement("div", {className: "ia-meta"}, /* @__PURE__ */ react_default.createElement("span", {className: "ia-version"}, "v", getString(addon.version)), authorArray)
      ];
    }, [addon.name, addon.version, addon.authorLink, addon.authorId, addon.author, messageAuthor]);
    const footer = useMemo8(() => {
      const links = Object.keys(LinkIcons);
      const linkComponents = links.map((l) => buildLink(l, addon[l])).filter((c) => c);
      return /* @__PURE__ */ react_default.createElement("div", {className: "ia-footer"}, /* @__PURE__ */ react_default.createElement("span", {className: "ia-links"}, linkComponents), /* @__PURE__ */ react_default.createElement("div", {className: "ia-controls"}, hasSettings && makeButton2(t("Addons.addonSettings"), /* @__PURE__ */ react_default.createElement(Settings, {size: "20px"}), showSettings, {isControl: true, disabled: !enabled}), editAddon && makeButton2(t("Addons.editAddon"), /* @__PURE__ */ react_default.createElement(Pencil, {size: "20px"}), editAddon, {isControl: true}), deleteAddon && makeButton2(t("Addons.deleteAddon"), /* @__PURE__ */ react_default.createElement(Trash2, {size: "20px"}), deleteAddon, {isControl: true, danger: true})));
    }, [hasSettings, editAddon, deleteAddon, addon, enabled, showSettings]);
    return /* @__PURE__ */ react_default.createElement("div", {id: `${addon.id}-card`, className: "ia-addon-card" + (disabled ? " ia-addon-card-disabled" : "")}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-header"}, type === "plugin" ? /* @__PURE__ */ react_default.createElement(Plug, {size: "20px", className: "ia-icon"}) : /* @__PURE__ */ react_default.createElement(Palette, {size: "20px", className: "ia-icon"}), /* @__PURE__ */ react_default.createElement("div", {className: "ia-title"}, title), /* @__PURE__ */ react_default.createElement(Switch, {internalState: false, disabled, value: enabled, onChange})), /* @__PURE__ */ react_default.createElement("div", {className: "ia-description-wrap"}, disabled && /* @__PURE__ */ react_default.createElement("div", {className: "banner banner-danger"}, /* @__PURE__ */ react_default.createElement(ShieldAlert, {className: "ia-icon"}), `An error was encountered while trying to load this ${type}.`), /* @__PURE__ */ react_default.createElement("div", {className: "ia-description"}, SimpleMarkdownExt.parseToReact(getString(addon.description)))), footer);
  }

  // src/InAccord/ui/settings/addonlist.tsx
  init_modals();
  init_errorboundary();

  // src/InAccord/ui/blankslates/empty.tsx
  init_clsx();
  init_markdown2();
  init_react();
  init_i18n();
  function EmptyImage() {
    return /* @__PURE__ */ react_default.createElement("svg", {fill: "none", height: "110", viewBox: "0 0 240 110", width: "240"}, /* @__PURE__ */ react_default.createElement("linearGradient", {id: "a", gradientUnits: "userSpaceOnUse", x1: "39.567", x2: "39.567", y1: "39.2948", y2: "101.804"}, /* @__PURE__ */ react_default.createElement("stop", {offset: "0", stopColor: "#72767d"}), /* @__PURE__ */ react_default.createElement("stop", {offset: "1", stopColor: "#fff", stopOpacity: "0"})), /* @__PURE__ */ react_default.createElement("clipPath", {id: "b"}, /* @__PURE__ */ react_default.createElement("path", {d: "m0 0h240v110h-240z"})), /* @__PURE__ */ react_default.createElement("g", {clipPath: "url(#b)"}, /* @__PURE__ */ react_default.createElement("path", {d: "m0 109.243v-5.607c0-2.272.917198-4.3935 2.52229-5.9844l11.77071-11.6667c2.0637-2.0454 3.2102-4.7727 3.2102-7.6515v-14.5454c0-6.6667 5.5032-12.1213 12.2293-12.1213h169.5285c6.726 0 12.229 5.4546 12.229 12.1213v27.2727c0 2.5 2.064 4.5454 4.586 4.5454h11.542c5.732 0 10.318 4.6209 10.318 10.2269v3.41z", fill: "#72767d", opacity: ".2"}), /* @__PURE__ */ react_default.createElement("path", {d: "m83.465 56.2122h-22.1656v53.0298h22.1656z", fill: "#36393f"}), /* @__PURE__ */ react_default.createElement("path", {d: "m193.529 56.2122h-110.064v53.0298h110.064z", fill: "#4f545c"}), /* @__PURE__ */ react_default.createElement("path", {d: "m106.395 78.9395h-22.93v30.3025h22.93z", fill: "#b9bbbe"}), /* @__PURE__ */ react_default.createElement("path", {d: "m109.452 74.3938h-25.987v4.5455h25.987z", fill: "#dcddde"}), /* @__PURE__ */ react_default.createElement("path", {d: "m83.465 74.3938h-14.5223v4.5455h14.5223z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m103.338 66.8181h-19.873v7.5758h19.873z", fill: "#f6f6f7"}), /* @__PURE__ */ react_default.createElement("path", {d: "m83.465 66.8181h-6.879v7.5758h6.879z", fill: "#b9bbbe"}), /* @__PURE__ */ react_default.createElement("path", {d: "m102.573 82.7273h-15.2864v1.5151h15.2864z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m102.573 84.2424h-15.2864v1.5152h15.2864z", fill: "#4f545c"}), /* @__PURE__ */ react_default.createElement("path", {d: "m102.573 85.7576h-15.2864v1.5151h15.2864z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m102.573 87.2727h-15.2864v1.5152h15.2864z", fill: "#4f545c"}), /* @__PURE__ */ react_default.createElement("path", {d: "m102.573 88.7878h-15.2864v1.5152h15.2864z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m102.573 90.303h-15.2864v1.5151h15.2864z", fill: "#4f545c"}), /* @__PURE__ */ react_default.createElement("path", {d: "m102.573 91.8181h-15.2864v1.5152h15.2864z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m102.573 93.3333h-15.2864v1.5151h15.2864z", fill: "#4f545c"}), /* @__PURE__ */ react_default.createElement("path", {d: "m104.102 94.8484h-18.344v3.0303h18.344z", fill: "#dcddde"}), /* @__PURE__ */ react_default.createElement("path", {d: "m83.465 78.9395h-12.2293v30.3025h12.2293z", fill: "#202225"}), /* @__PURE__ */ react_default.createElement("path", {d: "m75.0573 94.8485h4.586v-9.8485c0-1.2879-.9936-2.2727-2.293-2.2727s-2.293.9848-2.293 2.2727z", fill: "#36393f"}), /* @__PURE__ */ react_default.createElement("path", {d: "m185.885 74.394h-16.815v25.758h16.815z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m183.592 76.6667h-12.229v21.2122h12.229z", fill: "#b9bbbe"}), /* @__PURE__ */ react_default.createElement("path", {d: "m115.524 109.214h45.86v-37.8789h-45.86z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m118.582 109.214h39.745v-34.8486h-39.745z", fill: "#040405"}), /* @__PURE__ */ react_default.createElement("path", {d: "m140.025 73.6362h-3.057v35.6058h3.057z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m144.611 92.1968h-12.229v6.0606h12.229z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m120.153 26.6667h-36.688c-5.0446 0-9.172 4.091-9.172 9.091v9.0909h45.86z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m111.745 26.6667h57.325c5.045 0 9.172 4.091 9.172 9.091v9.0909h-75.669v-9.0909c0-5 4.051-9.091 9.172-9.091z", fill: "#b9bbbe"}), /* @__PURE__ */ react_default.createElement("path", {d: "m105.631 44.8485v-9.0909c0-3.3333 2.751-6.0606 6.114-6.0606h57.325c3.363 0 6.115 2.7273 6.115 6.0606v9.0909z", fill: "#72767d"}), /* @__PURE__ */ react_default.createElement("path", {d: "m196.586 38.0303h-113.121v21.9697h113.121z", fill: "#dcddde"}), /* @__PURE__ */ react_default.createElement("path", {d: "m83.4224 56.9412h110.0636v-3.7879h-110.0636z", fill: "#f6f6f7"}), /* @__PURE__ */ react_default.createElement("path", {d: "m193.529 47.1213h-110.064v3.7879h110.064z", fill: "#f6f6f7"}), /* @__PURE__ */ react_default.createElement("path", {d: "m83.4224 44.8201h110.0636v-3.7879h-110.0636z", fill: "#f6f6f7"}), /* @__PURE__ */ react_default.createElement("path", {d: "m83.465 38.0303h-25.2229v21.9697h25.2229z", fill: "#b9bbbe"}), /* @__PURE__ */ react_default.createElement("path", {d: "m61.2568 56.9412h22.1656v-3.7879h-22.1656z", fill: "#dcddde"}), /* @__PURE__ */ react_default.createElement("path", {d: "m61.2568 50.8806h22.1656v-3.7879h-22.1656z", fill: "#dcddde"}), /* @__PURE__ */ react_default.createElement("path", {d: "m61.2568 44.8201h22.1656v-3.7879h-22.1656z", fill: "#dcddde"}), /* @__PURE__ */ react_default.createElement("path", {d: "m209.885 2.12121c0-.68182.077-1.439392.23-2.12121-5.351.984848-9.478 5.68182-9.478 11.2879 0 6.3636 5.197 11.4394 11.541 11.4394 5.656 0 10.395-4.0152 11.389-9.394-.688.1515-1.376.2273-2.14.2273-6.421 0-11.542-5.15151-11.542-11.43939z", fill: "#b9bbbe"}), /* @__PURE__ */ react_default.createElement("path", {d: "m6.19112 73.8636-.99363 1.9697c-.2293.4546.2293.9849.76433.7576l1.98726-.9848c.15287-.0758.30573-.0758.4586 0l1.98722.9848c.4586.2273.9937-.2273.7644-.7576l-.9937-1.9697c-.0764-.1515-.0764-.303 0-.4545l.9937-1.9697c.2293-.4546-.2293-.9849-.7644-.7576l-1.98722.9849c-.15287.0757-.30573.0757-.4586 0l-1.98726-.9849c-.4586-.2273-.99363.2273-.76433.7576l.99363 1.9697c.07643.1515.07643.303 0 .4545z", fill: "#b9bbbe"}), /* @__PURE__ */ react_default.createElement("path", {d: "m191.465 30.3031-.994 1.9697c-.229.4545.23.9848.765.7576l1.987-.9849c.153-.0757.306-.0757.458 0l1.988.9849c.458.2272.993-.2273.764-.7576l-.994-1.9697c-.076-.1515-.076-.303 0-.4546l.994-1.9697c.229-.4545-.229-.9848-.764-.7575l-1.988.9848c-.152.0758-.305.0758-.458 0l-1.987-.9848c-.459-.2273-.994.2272-.765.7575l.994 1.9697c.076.1516.076.3031 0 .4546z", fill: "#4f545c"}), /* @__PURE__ */ react_default.createElement("g", {fill: "#dcddde"}, /* @__PURE__ */ react_default.createElement("path", {d: "m235.796 26.2879c-.458 0-.764-.3031-.764-.7576v-.7576c0-.4545.306-.7576.764-.7576.459 0 .764.3031.764.7576v.7576c0 .3788-.382.7576-.764.7576z"}), /* @__PURE__ */ react_default.createElement("path", {d: "m235.796 32.3484c-.458 0-.764-.303-.764-.7576v-.7575c0-.4546.306-.7576.764-.7576.459 0 .764.303.764.7576v.7575c0 .3788-.382.7576-.764.7576z"}), /* @__PURE__ */ react_default.createElement("path", {d: "m237.707 28.1819c0-.4546.306-.7576.764-.7576h.765c.458 0 .764.303.764.7576 0 .4545-.306.7576-.764.7576h-.765c-.458 0-.764-.3788-.764-.7576z"}), /* @__PURE__ */ react_default.createElement("path", {d: "m231.592 28.1819c0-.4546.306-.7576.765-.7576h.764c.459 0 .764.303.764.7576 0 .4545-.305.7576-.764.7576h-.764c-.459 0-.765-.3788-.765-.7576z"})), /* @__PURE__ */ react_default.createElement("g", {fill: "#040505"}, /* @__PURE__ */ react_default.createElement("path", {d: "m37.2229 93.106c0-1.4394-1.07-2.803-2.5223-3.0303-1.8344-.303-3.4395 1.1363-3.4395 2.8788 0 1.5909-.3057 3.2575-1.1465 4.6212-.3821.6818-.6114 1.5151-.6114 2.3485.0764 2.4238 2.0637 4.3938 4.4331 4.5458 2.6752.151 4.8917-1.97 4.8917-4.6216 0-.8333-.2293-1.5909-.6114-2.197-.6879-1.3636-.9937-2.9545-.9937-4.5454z"}), /* @__PURE__ */ react_default.createElement("path", {d: "m32.1783 110c-.3822 0-.6879-.303-.6879-.682v-8.333c0-.379.3057-.682.6879-.682s.6879.303.6879.682v8.333c0 .379-.3057.682-.6879.682z"}), /* @__PURE__ */ react_default.createElement("path", {d: "m36.3057 110c-.3822 0-.6879-.303-.6879-.682v-8.333c0-.379.3057-.682.6879-.682s.6879.303.6879.682v8.333c-.0764.379-.3822.682-.6879.682z"}), /* @__PURE__ */ react_default.createElement("path", {d: "m182.752 93.106c0-1.4394-1.07-2.803-2.523-3.0303-1.834-.303-3.439 1.1363-3.439 2.8788 0 1.5909-.306 3.2575-1.147 4.6212-.382.6818-.611 1.5151-.611 2.3485.076 2.4238 2.064 4.3938 4.433 4.5458 2.675.151 4.892-1.97 4.892-4.6216 0-.8333-.23-1.5909-.612-2.197-.688-1.3636-.993-2.9545-.993-4.5454z"}), /* @__PURE__ */ react_default.createElement("path", {d: "m177.707 110c-.382 0-.688-.303-.688-.682v-8.333c0-.379.306-.682.688-.682s.688.303.688.682v8.333c0 .379-.306.682-.688.682z"}), /* @__PURE__ */ react_default.createElement("path", {d: "m181.834 110c-.382 0-.688-.303-.688-.682v-8.333c0-.379.306-.682.688-.682.383 0 .688.303.688.682v8.333c-.076.379-.382.682-.688.682z"})), /* @__PURE__ */ react_default.createElement("path", {d: "m16.6624 109.242h45.8599l-18.344-65.1512h-9.1719z", fill: "url(#a)"}), /* @__PURE__ */ react_default.createElement("path", {d: "m32.7134 44.0909h13.7579v-1.5152c0-1.6667-1.3758-3.0303-3.0573-3.0303h-7.6433c-1.6815 0-3.0573 1.3636-3.0573 3.0303z", fill: "#dcddde"}), /* @__PURE__ */ react_default.createElement("path", {d: "m39.5923 41.8181v-8.7878c0-3.3334 2.6752-5.9849 6.0383-5.9849 3.363 0 6.0382 2.6515 6.0382 5.9849v76.2117", stroke: "#dcddde", strokeMiterlimit: "10", strokeWidth: "2"})));
  }
  function Empty(props) {
    return /* @__PURE__ */ react_default.createElement("div", {className: clsx_default("ia-empty-image-contianer", props.className)}, /* @__PURE__ */ react_default.createElement(EmptyImage, null), /* @__PURE__ */ react_default.createElement("div", {className: "ia-empty-image-title"}, props.title || t("Addons.blankSlateHeaderGeneric")), /* @__PURE__ */ react_default.createElement("div", {className: "ia-empty-image-message"}, SimpleMarkdownExt.parseToReact(props.message || t("Addons.blankSlateMessageGeneric"))), props.children);
  }

  // src/InAccord/ui/settings/addonlist.tsx
  init_settings3();
  init_text();
  init_lucide_react();
  init_hooks();
  init_addonmanager();
  var buildSortOptions2 = () => [
    {label: t("Addons.name"), value: "name"},
    {label: t("Addons.author"), value: "author"},
    {label: t("Addons.version"), value: "version"},
    {label: t("Addons.added"), value: "added"},
    {label: t("Addons.modified"), value: "modified"},
    {label: t("Addons.isEnabled"), value: "isEnabled"}
  ];
  function openFolder(folder) {
    ipc_default.openPath(folder);
  }
  function Blankslate({type, folder}) {
    const {toggleStore} = react_default.useContext(addonContext);
    const storeEnabled = settings_default2.get("settings", "store", "iaAddonStore");
    const message = t("Addons.blankSlateMessage", {link: Web.pages[`${type}s`], context: type}).toString();
    const onClick = storeEnabled ? toggleStore : () => openFolder(folder);
    const buttonKey = storeEnabled ? "Addons.openStore" : "Addons.openFolder";
    return /* @__PURE__ */ react_default.createElement(Empty, {title: t("Addons.blankSlateHeader", {context: type}), message: storeEnabled ? "" : message}, /* @__PURE__ */ react_default.createElement(Button, {size: Button.Sizes.LARGE, onClick}, t(buttonKey, {context: type})));
  }
  function makeControlButton(title, children, action, selected = false) {
    return /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", text: title.toString()}, (props) => {
      return /* @__PURE__ */ react_default.createElement(Button, {...props, size: Button.Sizes.NONE, "aria-label": title.toString(), look: Button.Looks.BLANK, className: "ia-button ia-view-button" + (selected ? " selected" : ""), onClick: action}, children);
    });
  }
  function confirmDelete(addon) {
    return new Promise((resolve) => {
      Modals.showConfirmationModal(t("Modals.confirmAction"), t("Addons.confirmDelete", {name: addon.name}), {
        danger: true,
        confirmText: t("Addons.deleteAddon"),
        onConfirm: () => {
          resolve(true);
        },
        onCancel: () => {
          resolve(false);
        }
      });
    });
  }
  function confirmEnable(action, type) {
    return function (event) {
      if (event.shiftKey) return action();
      Modals.showConfirmationModal(t("Modals.confirmAction"), t("Addons.enableAllWarning", {context: type.toLocaleLowerCase()}), {
        confirmText: t("Modals.okay"),
        cancelText: t("Modals.cancel"),
        danger: true,
        onConfirm: action
      });
    };
  }
  function StoreCard() {
    const {toggleStore, store} = react_default.useContext(addonContext);
    if (!settings_default2.get("settings", "store", "iaAddonStore")) return;
    return /* @__PURE__ */ react_default.createElement(
      "div",
      {
        className: "ia-store-card",
        onClick: toggleStore
      },
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-store-card-icon"}, /* @__PURE__ */ react_default.createElement(Store2, {size: "24px"})),
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-store-card-body"}, /* @__PURE__ */ react_default.createElement(Text, {color: Text.Colors.HEADER_PRIMARY, className: "ia-store-card-title"}, t("Addons.openStore", {context: store.prefix})), /* @__PURE__ */ react_default.createElement(Text, {color: Text.Colors.HEADER_SECONDARY, className: "ia-store-card-description"}, t("Addons.storeMessage", {context: store.prefix}))),
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-store-card-caret"}, /* @__PURE__ */ react_default.createElement(ChevronRight, {size: "24px"}))
    );
  }
  function AddonList({store}) {
    const [query, setQuery] = useState("");
    const [sort, setSort] = useState(getState.bind(null, store.prefix, "sort", "name"));
    const [ascending, setAscending] = useState(getState.bind(null, store.prefix, "ascending", true));
    const [view, setView] = useState(getState.bind(null, store.prefix, "view", "list"));
    const addonList = useStateFromStores(store, () => store.addonList.concat(), [store], true);
    const addonState = useStateFromStores(store, () => Object.assign({}, store.state), [store], true);
    const onChange = useCallback((id) => {
      store.toggleAddon(id);
    }, [store]);
    const enableAll = useCallback(() => {
      store.enableAllAddons();
    }, [store]);
    const disableAll = useCallback(() => {
      store.disableAllAddons();
    }, [store]);
    const changeView = useCallback((value) => {
      saveState(store.prefix, "view", value);
      setView(value);
    }, [store.prefix]);
    const listView = useCallback(() => changeView("list"), [changeView]);
    const gridView = useCallback(() => changeView("grid"), [changeView]);
    const changeDirection = useCallback((value) => {
      saveState(store.prefix, "ascending", value);
      setAscending(value);
    }, [store.prefix]);
    const changeSort = useCallback((value) => {
      saveState(store.prefix, "sort", value);
      setSort(value);
    }, [store.prefix]);
    const search = useCallback((e) => setQuery(e.currentTarget.value.toLocaleLowerCase()), []);
    const triggerEdit = useCallback((id) => store.editAddon?.(id), [store]);
    const triggerDelete = useCallback(async (id) => {
      const addon = addonList.find((a) => a.id == id);
      const shouldDelete = await confirmDelete(addon);
      if (!shouldDelete) return;
      store?.deleteAddon?.(addon);
    }, [addonList, store]);
    const renderedCards = useMemo(() => {
      let sorted = addonList.sort((a, b) => {
        const sortByEnabled = sort === "isEnabled";
        const first = sortByEnabled ? addonState[a.id] : a[sort];
        const second = sortByEnabled ? addonState[b.id] : b[sort];
        const stringSort = (str1, str2) => str1.toLocaleLowerCase().localeCompare(str2.toLocaleLowerCase());
        if (typeof first === "string" && typeof second === "string") return stringSort(first, second);
        if (typeof first === "boolean" && typeof second === "boolean") return first === second ? stringSort(a.name, b.name) : first ? -1 : 1;
        if (first > second) return 1;
        if (second > first) return -1;
        return 0;
      });
      if (!ascending) sorted.reverse();
      if (query) {
        sorted = sorted.filter((addon) => {
          let matches = addon.name.toLocaleLowerCase().includes(query);
          matches = matches || addon.author.toLocaleLowerCase().includes(query);
          matches = matches || addon.description.toLocaleLowerCase().includes(query);
          if (!matches) return false;
          return true;
        });
      }
      return sorted.map((addon) => {
        const hasSettings = addon.instance && typeof addon.instance.getSettingsPanel === "function";
        const getSettings = hasSettings && addon.instance.getSettingsPanel.bind(addon.instance);
        return /* @__PURE__ */ react_default.createElement(ErrorBoundary, {id: addon.id, name: "AddonCard"}, /* @__PURE__ */ react_default.createElement(AddonCard2, {store, disabled: addon.partial, type: store.prefix, editAddon: () => triggerEdit(addon.id), deleteAddon: () => triggerDelete(addon.id), key: addon.id, addon, onChange, enabled: addonState[addon.id], hasSettings, getSettingsPanel: getSettings ? getSettings : void 0}));
      });
    }, [store, addonList, addonState, onChange, triggerDelete, triggerEdit, query, ascending, sort]);
    const hasAddonsInstalled = addonList.length !== 0;
    const isSearching = !!query;
    const hasResults = renderedCards.length !== 0;
    return [
      /* @__PURE__ */ react_default.createElement(AddonHeader, {count: renderedCards.length, searching: isSearching}, /* @__PURE__ */ react_default.createElement(Search2, {onChange: search, placeholder: `${t("Addons.search", {count: renderedCards.length, context: store.prefix})}...`})),
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-controls ia-addon-controls"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-controls-basic"}, makeBasicButton(t("Addons.openFolder", {context: store.prefix}), /* @__PURE__ */ react_default.createElement(Folder, {size: "20px"}), openFolder.bind(null, store.addonFolder), "folder"), makeBasicButton(t("Addons.enableAll"), /* @__PURE__ */ react_default.createElement(Check, {size: "20px"}), confirmEnable(enableAll, store.prefix), "enable-all"), makeBasicButton(t("Addons.disableAll"), /* @__PURE__ */ react_default.createElement(X, {size: "20px"}), disableAll, "disable-all")), /* @__PURE__ */ react_default.createElement("div", {className: "ia-controls-advanced"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-dropdowns"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-wrapper"}, /* @__PURE__ */ react_default.createElement("label", {className: "ia-label"}, t("Sorting.sortBy"), ":"), /* @__PURE__ */ react_default.createElement(Select, {options: buildSortOptions2(), value: sort, onChange: changeSort, style: "transparent"})), /* @__PURE__ */ react_default.createElement("div", {className: "ia-select-wrapper"}, /* @__PURE__ */ react_default.createElement("label", {className: "ia-label"}, t("Sorting.order"), ":"), /* @__PURE__ */ react_default.createElement(Select, {options: buildDirectionOptions(), value: ascending, onChange: changeDirection, style: "transparent"}))), /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-views"}, makeControlButton(t("Addons.listView"), /* @__PURE__ */ react_default.createElement(StretchHorizontal, {size: "20px"}), listView, view === "list"), makeControlButton(t("Addons.gridView"), /* @__PURE__ */ react_default.createElement(LayoutGrid, null), gridView, view === "grid")))),
      /* @__PURE__ */ react_default.createElement(StoreCard, null),
      !hasAddonsInstalled && /* @__PURE__ */ react_default.createElement(Blankslate, {type: store.prefix, folder: store.addonFolder}),
      isSearching && !hasResults && hasAddonsInstalled && /* @__PURE__ */ react_default.createElement(NoResults, null),
      hasAddonsInstalled && /* @__PURE__ */ react_default.createElement("div", {key: "addonList", className: "ia-addon-list" + (view == "grid" ? " ia-grid-view" : "")}, renderedCards)
    ];
  }

  // src/InAccord/ui/settings/addonpage.tsx
  init_settings3();
  var {useState: useState14, useCallback: useCallback17} = react_default;
  function AddonPage(props) {
    const [showStore, setShowStore] = useState14(() => settings_default2.get("settings", "store", "iaAddonStore") && !props.store.addonList.length);
    const toggleStore = useCallback17(() => setShowStore((v) => !v), []);
    return /* @__PURE__ */ react_default.createElement(addonContext.Provider, {value: {toggleStore, showingStore: showStore, ...props}}, showStore ? /* @__PURE__ */ react_default.createElement(AddonStorePage, {...props, type: props.store.prefix}) : /* @__PURE__ */ react_default.createElement(AddonList, {...props}));
  }

  // src/InAccord/ui/settings/sidebarheader.tsx
  init_react();
  init_discordmodules();
  init_i18n();
  init_modals();
  init_button();
  init_lucide_react();
  function SettingsTitle2() {
    return /* @__PURE__ */ react_default.createElement("div", {className: "ia-sidebar-header"}, /* @__PURE__ */ react_default.createElement("h2", {className: "ia-sidebar-header-label"}, "InAccord"), /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", "aria-label": t("Modals.changelog"), text: t("Modals.changelog")}, (props) => /* @__PURE__ */ react_default.createElement(Button, {...props, "aria-label": t("Modals.changelog"), className: "ia-changelog-button", look: Button.Looks.BLANK, color: Button.Colors.TRANSPARENT, size: Button.Sizes.NONE, onClick: () => Modals.showChangelogModal(changelog_default)}, /* @__PURE__ */ react_default.createElement(History, {className: "ia-icon", size: "16px"}))));
  }

  // src/InAccord/ui/misc/versioninfo.tsx
  init_discordmodules();
  init_backupmanager();
  init_pluginmanager();
  init_react();
  init_i18n();
  init_thememanager();
  init_settings3();
  init_text();
  init_hooks();

  // src/InAccord/utils/debug.ts
  init_config();
  init_discordmodules();
  init_backupmanager();
  init_pluginmanager();
  init_thememanager();
  function getDiscordClientInfo() {
    const clientInfo = discordmodules_default.GetClientInfo?.();
    if (clientInfo) {
      return `${clientInfo.releaseChannel[0].toUpperCase()}${clientInfo.releaseChannel.substring(1)} ${clientInfo.buildNumber} (${clientInfo.versionHash.substring(0, 7)})`;
    }
    return discordmodules_default.RemoteModule?.releaseChannel ?? "Unknown";
  }
  function getDiscordHostInfo() {
    const remote = discordmodules_default.RemoteModule;
    if (!remote) return "Host Unknown";
    return `Host ${remote.version.join(".")} ${remote.architecture} (${remote.buildNumber})`;
  }
  function getOSInfo() {
    const remote = discordmodules_default.RemoteModule;
    if (!remote) return "OS Unknown";
    const osVersion = window.DiscordNative?.os?.release;
    let osName = discordmodules_default.UserAgentInfo?.os.toString();
    if (!osVersion || !osName) return "OS Unknown";
    const [macVer, _, winVer] = remote.parsedOSRelease;
    if (osName.includes("Windows 10") && winVer >= 22e3) osName = osName.replace("Windows 10", "Windows 11");
    if (osName.includes("OS X 10.15.7") && macVer >= 20) osName = "macOS ".concat((macVer - 9).toString());
    return `${osName} (${osVersion})`;
  }
  function getDiscordInfo(string = true) {
    const info = [
      getDiscordClientInfo(),
      getDiscordHostInfo(),
      getOSInfo()
    ];
    if (string) return info.join("\n");
    return info;
  }
  function getAddonCounts(manager) {
    return {
      total: manager.addonList.length,
      enabled: manager.addonList.filter((a) => manager.isEnabled(a.id)).length
    };
  }
  function getAddonList(manager) {
    return manager.addonList.map((a) => `- ${a.name}${manager.isEnabled(a.id) ? " (Enabled)" : ""}`).join("\n");
  }
  function getCoreInfo() {
    const channel = config_default.isCanary ? "Canary" : "Stable";
    return `${channel} ${config_default.get("version")} (${config_default.get("commit")?.substring(0, 7)})
`;
  }
  function getAddonInfo() {
    const backupCount = getAddonCounts(backupmanager_default);
    const pluginCount = getAddonCounts(pluginmanager_default);
    const themeCount = getAddonCounts(thememanager_default);
    return [
      `### ${backupCount.total} Backups (${backupCount.enabled} Enabled):
${getAddonList(backupmanager_default)}
`,
      `### ${pluginCount.total} Plugins (${pluginCount.enabled} Enabled):
${getAddonList(pluginmanager_default)}
`,
      `### ${themeCount.total} Themes (${themeCount.enabled} Enabled):
${getAddonList(thememanager_default)}
`
    ].join("\n");
  }
  function getDebugInfo() {
    return [
      `## Discord Info
${getDiscordInfo()}
`,
      `## InAccord`,
      getCoreInfo(),
      getAddonInfo()
    ].join("\n");
  }

  // src/InAccord/ui/misc/versioninfo.tsx
  var {useMemo: useMemo9, useState: useState15, useCallback: useCallback18} = react_default;
  var CLICKS_REQUIRED = 10;
  var ALLOWED_IDS = /* @__PURE__ */ new Set(["247863095647535104", "343423092670070786", "66043140208857088", "234600632567201792", "249746236008169473", "506287150984069120", "257900031351193600", "336678828233588736", "676620914632294467", "178394162381455360", "907344440945967114", "309976820109803520", "241334335884492810", "343383572805058560", "560112831371149312", "619261917352951815", "1030617301818552320", "528850043227340801", "310741793668857859", "1323433010858557523", "459937621481750528", "254362351170617345", "1019660983758766130", "515780151791976453", "398131626695196672", "538487970408300544", "917630027477159986", "226677096091484160", "174868361040232448", "155149108183695360", "262055523896131584", "116242787980017679", "403581288046526474", "76052829285916672", "332394843743584256", "224538553944637440", "238108500109033472", "777264613867257857", "102528230413578240", "678469587444170762", "219363409097916416", "582170007505731594", "354191516979429376", "468132563714703390", "323980738175434752", "801089753038061669", "415849376598982656", "68834122860077056", "384009727253807105", "247153658385399818", "306111596143443969", "508863359777505290", "278543574059057154", "282414867506528259", "231675734693773325", "234086939102281728", "316707214075101196", "382062281623863298", "282110343549812736", "332116671294734336", "188323207793606656", "557388558017495046", "300294176501923841", "385895514324992011", "264163473179672576", "340614112331694081", "374663636347650049", "152927763605618689", "133659541198864384", "295190422244950017", "135895345296048128", "220161488516546561", "110574243023966208", "437579806616322049", "153562159161278473", "490604571824226306", "253480609224065025", "502701912370577418", "112685077707665408", "98468214824001536", "399416615742996480", "214478470972047360", "678541597654253600", "179681974879911946", "211270674482724864", "583750578094735360", "135554522616561664", "741262207391629343", "124017508662378500", "194151269399527425", "267228264580382721", "709377715320651838", "402272736665272320", "427179231164760066", "98087545497206784", "354831939099688962", "137259132305539072", "147077474222604288", "516091504524132362", "712318895062515809", "124276233478471680", "521340540411838464", "1060238753823723530", "332150412763332608", "310450863845933057", "142347724392497152", "705153934758772746", "120202910586896385", "301194255731392513", "869418754348580885", "131212461499088896", "270848136006729728", "92969646403121152", "769144538107215872", "304260051915374603", "80593258903773184", "346338830011596800", "393900343135830016", "419610859392860162", "359175647257690113", "224085382474498048", "705798778472366131"]);
  function VersionInfo() {
    const [clicks, setClicked] = useState15(0);
    const currentUser = useMemo9(() => discordmodules_default.UserStore?.getCurrentUser()?.id, []);
    const isCanary = useStateFromStores(settings_default2, () => settings_default2.get("developer", "canary"));
    const discordInfo = useMemo9(() => {
      const info = getDiscordInfo(false);
      return info.map((i) => /* @__PURE__ */ react_default.createElement(Text, {color: Text.Colors.MUTED, size: Text.Sizes.SIZE_12}, i));
    }, []);
    const onClick = useCallback18(() => {
      DiscordNative?.clipboard?.copy(`\`\`\`md
${getDebugInfo()}
\`\`\``);
      if (!ALLOWED_IDS.has(currentUser)) return setClicked(1);
      const newValue = clicks + 1;
      if (newValue > CLICKS_REQUIRED) return;
      if (newValue === CLICKS_REQUIRED) settings_default2.set("developer", "canary", !isCanary);
      setClicked(newValue);
    }, [clicks, currentUser, isCanary]);
    const onMouseLeave = useCallback18(() => {
      setTimeout(() => setClicked(0), 250);
    }, []);
    const backupCount = useStateFromStores(backupmanager_default, () => getAddonCounts(backupmanager_default), [], true);
    const pluginCount = useStateFromStores(pluginmanager_default, () => getAddonCounts(pluginmanager_default), [], true);
    const themeCount = useStateFromStores(thememanager_default, () => getAddonCounts(thememanager_default), [], true);
    const tooltip = useMemo9(() => {
      if (clicks === 0) return "Click to copy";
      if (clicks > 0 && clicks < CLICKS_REQUIRED / 2) return "Copied";
      if (clicks >= CLICKS_REQUIRED / 2 && clicks < CLICKS_REQUIRED) return `${CLICKS_REQUIRED - clicks} clicks to go`;
      if (clicks >= CLICKS_REQUIRED) {
        if (isCanary) return "Switched to ia canary";
        return "Switched to ia stable";
      }
    }, [clicks, isCanary]);
    const color = useMemo9(() => {
      if (clicks === 0) return "primary";
      if (clicks > 0 && clicks < CLICKS_REQUIRED / 2) return "green";
      if (clicks >= CLICKS_REQUIRED / 2 && clicks < CLICKS_REQUIRED) return "yellow";
      if (clicks >= CLICKS_REQUIRED) return "brand";
    }, [clicks]);
    return /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color, position: "top", text: tooltip}, (props) => /* @__PURE__ */ react_default.createElement("div", {
      ...props, className: "ia-version-info", onClick, onMouseLeave: (e) => {
        props.onMouseLeave(e);
        onMouseLeave();
      }
    }, discordInfo, /* @__PURE__ */ react_default.createElement(Text, {color: Text.Colors.MUTED, size: Text.Sizes.SIZE_12}, "ia ", getCoreInfo()), /* @__PURE__ */ react_default.createElement(Text, {color: Text.Colors.MUTED, size: Text.Sizes.SIZE_12}, t("Panels.backup"), " ", backupCount.total, " (", backupCount.enabled, " ", t("Addons.isEnabled"), ")"), /* @__PURE__ */ react_default.createElement(Text, {color: Text.Colors.MUTED, size: Text.Sizes.SIZE_12}, t("Panels.plugins"), " ", pluginCount.total, " (", pluginCount.enabled, " ", t("Addons.isEnabled"), ")"), /* @__PURE__ */ react_default.createElement(Text, {color: Text.Colors.MUTED, size: Text.Sizes.SIZE_12}, t("Panels.themes"), " ", themeCount.total, " (", themeCount.enabled, " ", t("Addons.isEnabled"), ")")));
  }

  // src/InAccord/ui/settings.tsx
  init_utils();
  init_hooks();

  // src/InAccord/ui/settings/panel.tsx
  init_react();
  init_i18n();
  init_settings3();
  init_discordmodules();
  init_button();
  init_modals();

  // src/InAccord/ui/settings/group.tsx
  init_react();

  // src/InAccord/ui/settings/drawer.tsx
  init_react();
  init_divider();
  var {useState: useState16, useCallback: useCallback19, useRef: useRef7} = react_default;
  var baseClassName = "ia-settings-group";
  function Drawer({name, collapsible, shown = true, showDivider, children, titleChildren, onDrawerToggle}) {
    const contianer = useRef7(null);
    const [collapsed, setCollapsed] = useState16(!!collapsible && !shown);
    const toggleCollapse = useCallback19(() => {
      const drawer = contianer.current;
      const timeout = collapsed ? 300 : 1;
      drawer.style.setProperty("height", drawer.scrollHeight + "px");
      drawer.classList.add("animating");
      if (onDrawerToggle) onDrawerToggle(collapsed);
      setCollapsed(!collapsed);
      setTimeout(() => {
        drawer.style.setProperty("height", "");
        drawer.classList.remove("animating");
      }, timeout);
    }, [collapsed, onDrawerToggle]);
    const collapseClass = collapsible ? `collapsible ${collapsed ? "collapsed" : "expanded"}` : "";
    const groupClass2 = `${baseClassName} ${collapseClass}`;
    return /* @__PURE__ */ react_default.createElement("div", {className: groupClass2}, /* @__PURE__ */ react_default.createElement(SettingsTitle, {text: name, onClick: toggleCollapse, isGroup: true}, titleChildren), /* @__PURE__ */ react_default.createElement("div", {className: "ia-settings-contianer", ref: contianer}, children), showDivider && /* @__PURE__ */ react_default.createElement(divider_default, null));
  }

  // src/InAccord/ui/settings/group.tsx
  init_switch();

  // src/InAccord/ui/settings/components/number.tsx
  init_react();
  init_contexts();
  init_button();
  init_lucide_react();
  var {useState: useState17, useCallback: useCallback20, useContext: useContext4} = react_default;
  function Number2({value: initialValue, min, max, step = 1, onChange, disabled}) {
    const [internalValue, setValue] = useState17(initialValue);
    const {value: contextValue, disabled: contextDisabled} = useContext4(SettingsContext);
    const value = contextValue !== none ? contextValue : internalValue;
    const isDisabled = contextValue !== none ? contextDisabled : disabled;
    const change = useCallback20((e) => {
      onChange?.(e.target.value);
      setValue(e.target.value);
    }, [onChange]);
    const increment = useCallback20(() => {
      const currentValue = parseFloat(String(value));
      const incrementedValue = currentValue + step;
      if (max !== void 0 && incrementedValue > max) return;
      onChange?.(incrementedValue);
      setValue(incrementedValue);
    }, [onChange, value, max, step]);
    const decrement = useCallback20(() => {
      const currentValue = parseFloat(String(value));
      const decrementedValue = currentValue - step;
      if (min !== void 0 && decrementedValue < min) return;
      onChange?.(decrementedValue);
      setValue(decrementedValue);
    }, [onChange, value, min, step]);
    return /* @__PURE__ */ react_default.createElement("div", {className: `ia-number-input-wrapper ${isDisabled ? "ia-number-input-disabled" : ""}`}, /* @__PURE__ */ react_default.createElement(Button, {size: Button.Sizes.ICON, look: Button.Looks.FILLED, color: Button.Colors.PRIMARY, className: "ia-number-input-decrement", onClick: decrement}, /* @__PURE__ */ react_default.createElement(Minus, {size: "24px"})), /* @__PURE__ */ react_default.createElement("input", {onChange: change, type: "number", className: "ia-number-input", min, max, step, value, disabled: isDisabled}), /* @__PURE__ */ react_default.createElement(Button, {size: Button.Sizes.ICON, look: Button.Looks.FILLED, color: Button.Colors.PRIMARY, className: "ia-number-input-increment", onClick: increment}, /* @__PURE__ */ react_default.createElement(Plus, {size: "24px"})));
  }

  // src/InAccord/ui/settings/components/item.tsx
  init_react();
  init_divider();
  function SettingItem({id, name, note, inline, children}) {
    return /* @__PURE__ */ react_default.createElement("div", {className: "ia-setting-item" + (inline ? " inline" : "")}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-setting-header"}, /* @__PURE__ */ react_default.createElement("label", {htmlFor: id, className: "ia-setting-title"}, name), inline && children), /* @__PURE__ */ react_default.createElement("div", {className: "ia-setting-note"}, note), !inline && children, /* @__PURE__ */ react_default.createElement(divider_default, {className: "ia-setting-divider"}));
  }

  // src/InAccord/ui/settings/components/textbox.tsx
  init_react();
  init_contexts();
  var {useState: useState18, useCallback: useCallback21, useContext: useContext5} = react_default;
  function Textbox({value: initialValue, maxLength, placeholder, onKeyDown, onChange, disabled}) {
    const [internalValue, setValue] = useState18(initialValue);
    const {value: contextValue, disabled: contextDisabled} = useContext5(SettingsContext);
    const value = contextValue !== none ? contextValue : internalValue;
    const isDisabled = contextValue !== none ? contextDisabled : disabled;
    const change = useCallback21((e) => {
      if (isDisabled) return;
      onChange?.(e.currentTarget.value);
      setValue(e.currentTarget.value);
    }, [onChange, isDisabled]);
    return /* @__PURE__ */ react_default.createElement("input", {onChange: change, onKeyDown, type: "text", className: "ia-text-input", placeholder, maxLength, value, disabled: isDisabled});
  }

  // src/InAccord/ui/settings/components/slider.tsx
  init_react();
  init_contexts();
  var {useState: useState19, useCallback: useCallback22, useMemo: useMemo10, useRef: useRef8, useContext: useContext6} = react_default;
  function Slider({value: initialValue, min, max, step, onChange, disabled, units = "", markers = []}) {
    const [internalValue, setValue] = useState19(initialValue);
    const {value: contextValue, disabled: contextDisabled} = useContext6(SettingsContext);
    const value = contextValue !== none ? contextValue : internalValue;
    const isDisabled = contextValue !== none ? contextDisabled : disabled;
    const inputRef = useRef8(null);
    const change = useCallback22((e) => {
      if (isDisabled) return;
      onChange?.(e.target.value);
      setValue(e.target.value);
    }, [onChange, isDisabled]);
    const jumpToValue = useCallback22((val) => {
      if (isDisabled) return;
      onChange?.(val);
      setValue(val);
    }, [onChange, isDisabled]);
    const percent = useCallback22((val) => {
      return (val - min) * 100 / (max - min);
    }, [min, max]);
    const labelOffset = useMemo10(() => {
      const slope = (-62.5 - -25) / (max - min);
      const offset = value * slope + -25;
      if (offset < -62.5) return -62.5;
      return offset;
    }, [value, min, max]);
    const trackClick = useCallback22((e) => {
      const bounds = e.currentTarget.getBoundingClientRect();
      const offsetX = e.clientX - bounds.left;
      const offsetPercent = offsetX / bounds.width;
      const newValue = offsetPercent * (max - min) + min;
      inputRef.current.value = newValue.toString();
      jumpToValue(inputRef.current.value);
    }, [max, min, jumpToValue, inputRef]);
    return /* @__PURE__ */ react_default.createElement("div", {className: `ia-slider-wrap ${isDisabled ? "ia-slider-disabled" : ""} ${markers.length > 0 ? "ia-slider-markers" : ""}`}, /* @__PURE__ */ react_default.createElement("input", {onChange: change, type: "range", className: "ia-slider-input", min, max, step, value, disabled, ref: inputRef}), /* @__PURE__ */ react_default.createElement("div", {className: "ia-slider-label", style: {left: `${percent(value)}%`, transform: `translateX(${labelOffset}%)`}}, value, units), /* @__PURE__ */ react_default.createElement("div", {className: "ia-slider-track", style: {backgroundSize: percent(value) + "% 100%"}, onClick: trackClick}), markers?.length > 0 && /* @__PURE__ */ react_default.createElement("div", {className: "ia-slider-marker-contianer"}, markers.map((m) => {
      const markerValue = typeof m === "number" ? m : m.value;
      const markerLabel = typeof m === "number" ? m : m?.label;
      const showUnits = units && typeof m === "number";
      return /* @__PURE__ */ react_default.createElement("div", {className: "ia-slider-marker", style: {left: percent(markerValue) + "%"}, onClick: () => jumpToValue(markerValue)}, markerLabel, showUnits && units);
    })));
  }

  // src/InAccord/ui/settings/components/radio.tsx
  init_react();
  init_contexts();
  var {useState: useState20, useCallback: useCallback23, useContext: useContext7} = react_default;
  function RadioIndicator({checked}) {
    return /* @__PURE__ */ react_default.createElement("svg", {className: "ia-radio-indicator", width: "24", height: "24", viewBox: "0 0 24 24"}, /* @__PURE__ */ react_default.createElement(
      "circle",
      {
        cx: "12",
        cy: "12",
        r: "12",
        strokeWidth: "2",
        fill: "none",
        className: "ia-radio-icon"
      }
    ), checked && /* @__PURE__ */ react_default.createElement(
      "circle",
      {
        cx: "12",
        cy: "12",
        r: "5",
        fill: "#fff"
      }
    ));
  }
  function Radio({name, value: initialValue, options, onChange, disabled}) {
    const {value: contextValue, disabled: contextDisabled} = useContext7(SettingsContext);
    const value = contextValue !== none ? contextValue : initialValue;
    const isDisabled = contextValue !== none ? contextDisabled : disabled;
    const [index, setIndex] = useState20(options.findIndex((o) => o.value === value));
    const change = useCallback23((e) => {
      if (isDisabled) return;
      const newIndex = parseInt(e.target.value);
      const newValue = options[newIndex].value;
      onChange?.(newValue);
      setIndex(newIndex);
    }, [options, onChange, isDisabled]);
    function renderOption(opt, i) {
      const isSelected = index === i;
      return /* @__PURE__ */ react_default.createElement("label", {className: "ia-radio-option" + (isSelected ? " ia-radio-selected" : ""), style: {borderColor: opt.color ?? "transparent"}}, /* @__PURE__ */ react_default.createElement("input", {onChange: change, type: "radio", name, checked: isSelected, value: i, disabled: isDisabled}), /* @__PURE__ */ react_default.createElement(RadioIndicator, {checked: isSelected}), /* @__PURE__ */ react_default.createElement("div", {className: "ia-radio-label-wrap"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-radio-label"}, opt.name), /* @__PURE__ */ react_default.createElement("div", {className: "ia-radio-description"}, opt.desc || opt.description)));
    }
    return /* @__PURE__ */ react_default.createElement("div", {className: `ia-radio-group ${isDisabled ? "ia-radio-disabled" : ""}`}, options.map(renderOption));
  }

  // src/InAccord/ui/settings/components/keybind.tsx
  init_react();
  init_button();
  init_lucide_react();
  init_contexts();
  var {useState: useState21, useCallback: useCallback24, useEffect: useEffect9, useContext: useContext8} = react_default;
  function Keybind({value: initialValue, onChange, max = 4, clearable = false, disabled}) {
    const [state, setState] = useState21({isRecording: false, accum: []});
    const [internalValue, setValue] = useState21(initialValue);
    const {value: contextValue, disabled: contextDisabled} = useContext8(SettingsContext);
    const value = contextValue !== none ? contextValue : internalValue;
    const isDisabled = contextValue !== none ? contextDisabled : disabled;
    useEffect9(() => {
      window.addEventListener("keydown", keyDownHandler, true);
      window.addEventListener("keyup", keyUpHandler, true);
      return () => {
        window.removeEventListener("keydown", keyDownHandler, true);
        window.removeEventListener("keyup", keyUpHandler, true);
      };
    });
    const keyDownHandler = useCallback24((event) => {
      if (!state.isRecording) return;
      event.stopImmediatePropagation();
      event.stopPropagation();
      event.preventDefault();
      if (event.repeat || state.accum.includes(event.key)) return;
      state.accum.push(event.key);
      if (state.accum.length == max) {
        setState({isRecording: false, accum: []});
        setValue(state.accum.slice(0));
        onChange?.(state.accum);
      }
    }, [state, max, onChange]);
    const keyUpHandler = useCallback24((event) => {
      if (!state.isRecording) return;
      event.stopImmediatePropagation();
      event.stopPropagation();
      event.preventDefault();
      if (event.key === state.accum[0]) {
        setState({isRecording: false, accum: []});
        setValue(state.accum.slice(0));
        onChange?.(state.accum);
      }
    }, [state, onChange]);
    const clearKeybind = useCallback24((event) => {
      event.stopPropagation();
      event.preventDefault();
      if (isDisabled) return;
      if (onChange) onChange([]);
      setValue([]);
      setState({...state, isRecording: false, accum: []});
    }, [onChange, state, isDisabled]);
    const onClick = useCallback24((e) => {
      if (isDisabled) return;
      if (e.currentTarget?.className?.includes?.("ia-keybind-clear") || e.currentTarget?.closest(".ia-button")?.className?.includes("ia-keybind-clear")) return clearKeybind(e);
      setState({...state, isRecording: !state.isRecording});
    }, [state, clearKeybind, isDisabled]);
    const displayValue = !value.length ? "" : value.map((k) => k === "Control" ? "Ctrl" : k).join(" + ");
    return /* @__PURE__ */ react_default.createElement("div", {className: "ia-keybind-wrap" + (state.isRecording ? " recording" : "") + (isDisabled ? " ia-keybind-disabled" : ""), onClick}, /* @__PURE__ */ react_default.createElement(Button, {size: Button.Sizes.ICON, look: Button.Looks.FILLED, color: state.isRecording ? Button.Colors.RED : Button.Colors.PRIMARY, className: "ia-keybind-record", onClick}, /* @__PURE__ */ react_default.createElement(Keyboard, {size: "24px"})), /* @__PURE__ */ react_default.createElement("input", {readOnly: true, type: "text", className: "ia-keybind-input", value: displayValue, placeholder: "No keybind set", disabled}), clearable && /* @__PURE__ */ react_default.createElement(Button, {size: Button.Sizes.ICON, look: Button.Looks.BLANK, onClick: clearKeybind, className: "ia-keybind-clear"}, /* @__PURE__ */ react_default.createElement(X, {size: "24px"})));
  }

  // src/InAccord/ui/settings/components/color.tsx
  init_react();
  init_discordmodules();
  init_i18n();
  init_lucide_react();
  init_contexts();
  var {useState: useState22, useCallback: useCallback25, useContext: useContext9} = react_default;
  var defaultColors = [1752220, 3066993, 3447003, 10181046, 15277667, 15844367, 15105570, 15158332, 9807270, 6323595, 1146986, 2067276, 2123412, 7419530, 11342935, 12745742, 11027200, 10038562, 9936031, 5533306];
  function resolveColor(color, hex = true) {
    switch (typeof color) {
      case (hex && "number"):
        return `#${color.toString(16)}`;
      case (!hex && "string"):
        return Number.parseInt(color.replace("#", ""), 16);
      case (!hex && "number"):
        return color;
      case (hex && "string"):
        return color;
      default:
        return color;
    }
  }
  var getRGB = (color) => {
    let result = /rgb\(\s*([0-9]{1,3})\s*,\s*([0-9]{1,3})\s*,\s*([0-9]{1,3})\s*\)/.exec(color);
    if (result) return [parseInt(result[1]), parseInt(result[2]), parseInt(result[3])];
    result = /rgb\(\s*([0-9]+(?:\.[0-9]+)?)%\s*,\s*([0-9]+(?:\.[0-9]+)?)%\s*,\s*([0-9]+(?:\.[0-9]+)?)%\s*\)/.exec(color);
    if (result) return [parseFloat(result[1]) * 2.55, parseFloat(result[2]) * 2.55, parseFloat(result[3]) * 2.55];
    result = /#([a-fA-F0-9]{2})([a-fA-F0-9]{2})([a-fA-F0-9]{2})/.exec(color);
    if (result) return [parseInt(result[1], 16), parseInt(result[2], 16), parseInt(result[3], 16)];
    result = /#([a-fA-F0-9])([a-fA-F0-9])([a-fA-F0-9])/.exec(color);
    if (result) return [parseInt(result[1] + result[1], 16), parseInt(result[2] + result[2], 16), parseInt(result[3] + result[3], 16)];
  };
  var luma = (color) => {
    const rgb = typeof color === "string" ? getRGB(color) : color;
    return 0.2126 * rgb[0] + 0.7152 * rgb[1] + 0.0722 * rgb[2];
  };
  var getContrastColor = (color) => {
    return luma(color) >= 150 ? "#000" : "#fff";
  };
  function Color({value: initialValue, onChange, colors = defaultColors, defaultValue, disabled}) {
    const [internalValue, setValue] = useState22(initialValue);
    const {value: contextValue, disabled: contextDisabled} = useContext9(SettingsContext);
    const value = contextValue !== none ? contextValue : internalValue;
    const isDisabled = contextValue !== none ? contextDisabled : disabled;
    const change = useCallback25((e) => {
      if (isDisabled) return;
      onChange?.(resolveColor(e.target.value));
      setValue(e.target.value);
    }, [onChange, isDisabled]);
    const intValue = resolveColor(value, false);
    return /* @__PURE__ */ react_default.createElement("div", {className: `ia-color-picker-contianer ${isDisabled ? "ia-color-picker-disabled" : ""}`}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-color-picker-controls"}, defaultValue && /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: "Default", position: "bottom"}, (props) => /* @__PURE__ */ react_default.createElement("div", {...props, className: "ia-color-picker-default", style: {backgroundColor: resolveColor(defaultValue)}, onClick: () => change({target: {value: defaultValue}})}, intValue === resolveColor(defaultValue, false) ? /* @__PURE__ */ react_default.createElement(Check, {size: "25px", color: getContrastColor(resolveColor(defaultValue, true))}) : null)), /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {text: t("Settings.customColor"), position: "bottom"}, (props) => /* @__PURE__ */ react_default.createElement("div", {className: "ia-color-picker-custom"}, /* @__PURE__ */ react_default.createElement(Pipette, {size: "14px", color: getContrastColor(resolveColor(value, true))}), /* @__PURE__ */ react_default.createElement("input", {...props, style: {backgroundColor: resolveColor(value)}, type: "color", className: "ia-color-picker", value: resolveColor(value), onChange: change, disabled})))), colors?.length > 0 && /* @__PURE__ */ react_default.createElement("div", {className: "ia-color-picker-swatch"}, colors.map((int, index) => /* @__PURE__ */ react_default.createElement("div", {key: index, className: "ia-color-picker-swatch-item", style: {backgroundColor: resolveColor(int)}, onClick: () => change({target: {value: int}})}, intValue === int ? /* @__PURE__ */ react_default.createElement(Check, {size: "16px", color: getContrastColor(resolveColor(value, true))}) : null))));
  }

  // src/InAccord/ui/settings/components/file.tsx
  init_electron();
  init_react();
  init_button();
  init_lucide_react();
  var {useRef: useRef9, useCallback: useCallback26, useEffect: useEffect10} = react_default;
  function Filepicker({multiple, accept, clearable, onChange, disabled, actions}) {
    const inputRef = useRef9(null);
    const change = useCallback26((e) => {
      if (disabled) return;
      const files = [];
      for (const file of e.target.files) {
        files.push(webUtils.getPathForFile(file));
      }
      if (multiple === true) onChange?.(files);
      else onChange?.(files[0]);
    }, [onChange, disabled, multiple]);
    const clear = useCallback26(() => {
      inputRef.current.value = "";
      if (multiple === true) onChange?.([]);
      else onChange?.("");
    }, [onChange, multiple]);
    useEffect10(() => {
      if (!actions) return;
      actions.clear = clear;
    }, [clear, actions]);
    const onClick = useCallback26(() => {
      inputRef.current?.click();
    }, []);
    return /* @__PURE__ */ react_default.createElement("div", {className: `ia-file-input-wrap ${disabled ? "ia-file-input-disabled" : ""}`}, /* @__PURE__ */ react_default.createElement(Button, {size: Button.Sizes.ICON, look: Button.Looks.FILLED, color: Button.Colors.PRIMARY, className: "ia-file-input-browse", onClick}, "Browse"), /* @__PURE__ */ react_default.createElement("input", {onChange: change, type: "file", className: "ia-file-input", multiple, accept, disabled, ref: inputRef}), clearable && /* @__PURE__ */ react_default.createElement(Button, {size: Button.Sizes.ICON, look: Button.Looks.BLANK, onClick: clear, className: "ia-file-input-clear"}, /* @__PURE__ */ react_default.createElement(X, {size: "24px"})));
  }

  // src/InAccord/ui/settings/group.tsx
  init_button();

  // src/InAccord/ui/settings/components/position.tsx
  init_react();
  init_text();
  init_contexts();
  var positions = [
    "top-left",
    "top-right",
    "bottom-left",
    "bottom-right"
  ];
  var Position = ({value: initialValue, onChange, disabled}) => {
    const [internalValue, setValue] = useState(initialValue);
    const {value: contextValue, disabled: contextDisabled} = useContext(SettingsContext);
    const value = contextValue !== none ? contextValue : internalValue;
    const isDisabled = contextValue !== none ? contextDisabled : disabled;
    const handlePositionChange = (position) => {
      if (isDisabled) return;
      onChange?.(position);
      setValue(position);
    };
    const getBoxClassName = (position) => {
      return `ia-box${isDisabled ? "-disabled" : ""} ${position} ${value === position ? "selected" : ""}`;
    };
    return /* @__PURE__ */ react_default.createElement("div", {className: "position-wrapper"}, /* @__PURE__ */ react_default.createElement("div", {className: `ia-contianer${isDisabled ? "-disabled" : ""}`}, positions.map((position) => /* @__PURE__ */ react_default.createElement(
      "button",
      {
        key: position,
        className: getBoxClassName(position),
        onClick: () => handlePositionChange(position),
        role: "radio",
        "aria-checked": value === position,
        "aria-label": `Select ${position} position`,
        disabled: isDisabled,
        tabIndex: isDisabled ? -1 : 0
      }
    ))), /* @__PURE__ */ react_default.createElement("div", {className: "ia-position-info"}, value ? /* @__PURE__ */ react_default.createElement(react_default.Fragment, null, /* @__PURE__ */ react_default.createElement(Text, null, "Selected Position:"), /* @__PURE__ */ react_default.createElement(Text, null, value.replace(/-/g, " ").toUpperCase())) : /* @__PURE__ */ react_default.createElement(Text, null, "Click a box to select position")));
  };
  var position_default = Position;

  // src/InAccord/ui/settings/group.tsx
  init_contexts();
  init_hooks();
  init_settings3();
  var {useCallback: useCallback27} = react_default;
  function SettingsProvider({collection, category, id, children}) {
    const getSettingState = react_default.useCallback(() => {
      const setting = settings_default2.getSetting(collection, category, id);
      return {
        value: settings_default2.get(collection, category, id),
        disabled: setting?.disabled ?? false
      };
    }, [collection, category, id]);
    const settingState = useStateFromStores(settings_default2, getSettingState);
    const context = react_default.useMemo(() => settingState, [settingState]);
    return /* @__PURE__ */ react_default.createElement(SettingsContext.Provider, {value: context}, children);
  }
  function Group({onChange, id, name = "", shown, onDrawerToggle, showDivider = false, collapsible, settings, children = null, collection}) {
    const change = useCallback27((settingId, value) => {
      if (id) onChange?.(id, settingId, value);
      else onChange?.(settingId, value);
    }, [id, onChange]);
    return /* @__PURE__ */ react_default.createElement(Drawer, {collapsible, name, shown, onDrawerToggle, showDivider}, settings?.length > 0 && settings.filter((s) => !s.hidden).map((setting) => {
      const callback = (value) => {
        setting?.onChange?.(value);
        change(setting.id, value);
      };
      const settingItem = buildSetting({...setting, onChange: callback});
      if (!collection) return settingItem;
      return /* @__PURE__ */ react_default.createElement(SettingsProvider, {collection, category: id, id: setting.id}, settingItem);
    }), children);
  }
  function buildSetting(setting) {
    let children = null;
    if (setting.type === "dropdown") children = /* @__PURE__ */ react_default.createElement(Select, {...setting});
    if (setting.type === "number") children = /* @__PURE__ */ react_default.createElement(Number2, {...setting});
    if (setting.type === "switch") children = /* @__PURE__ */ react_default.createElement(Switch, {...setting});
    if (setting.type === "text") children = /* @__PURE__ */ react_default.createElement(Textbox, {...setting});
    if (setting.type === "file") children = /* @__PURE__ */ react_default.createElement(Filepicker, {...setting});
    if (setting.type === "slider") children = /* @__PURE__ */ react_default.createElement(Slider, {...setting});
    if (setting.type === "radio") children = /* @__PURE__ */ react_default.createElement(Radio, {...setting});
    if (setting.type === "keybind") children = /* @__PURE__ */ react_default.createElement(Keybind, {...setting});
    if (setting.type === "color") children = /* @__PURE__ */ react_default.createElement(Color, {...setting});
    if (setting.type === "button") children = /* @__PURE__ */ react_default.createElement(Button, {...setting});
    if (setting.type === "position") children = /* @__PURE__ */ react_default.createElement(position_default, {...setting});
    if (setting.type === "custom") children = setting.children;
    if (!children) return null;
    return /* @__PURE__ */ react_default.createElement(
      SettingItem,
      {
        id: setting.id,
        inline: setting.hasOwnProperty("inline") ? setting.inline : setting.type !== "radio",
        key: setting.id,
        name: setting.name,
        note: setting.note
      },
      children
    );
  }

  // src/InAccord/ui/settings/panel.tsx
  init_lucide_react();
  function makeResetButton(collectionId, refresh) {
    const action = confirmReset(() => {
      settings_default2.resetCollection(collectionId);
      refresh?.();
    });
    return /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", text: t("Settings.resetSettings")}, (props) => /* @__PURE__ */ react_default.createElement(Button, {...props, "aria-label": t("Settings.resetSettings"), size: Button.Sizes.ICON, look: Button.Looks.BLANK, color: Button.Colors.TRANSPARENT, onClick: action}, /* @__PURE__ */ react_default.createElement(ListRestart, null)));
  }
  function confirmReset(action) {
    return () => {
      Modals.showConfirmationModal(t("Modals.confirmAction"), t("Settings.resetSettingsWarning"), {
        confirmText: t("Modals.okay"),
        cancelText: t("Modals.cancel"),
        danger: true,
        onConfirm: action
      });
    };
  }
  function SettingsPanel({id, title, groups, onChange, onDrawerToggle, getDrawerState}) {
    const set = react_default.useContext(SettingsTitleContext);
    return /* @__PURE__ */ react_default.createElement(react_default.Fragment, null, set(
      /* @__PURE__ */ react_default.createElement(SettingsTitle, {text: title}, makeResetButton(id))
    ), groups.map((section) => {
      const props = Object.assign({}, section, {
        collection: id,
        onChange,
        onDrawerToggle: (state) => onDrawerToggle(id, section.id, state),
        shown: getDrawerState(id, section.id, section.hasOwnProperty("shown") ? section.shown : true)
      });
      return /* @__PURE__ */ react_default.createElement(Group, {...props});
    }));
  }

  // src/InAccord/utils/icon.ts
  init_react();
  var sizes = {
    xxs: 12,
    xs: 16,
    sm: 18,
    md: 24,
    lg: 32,
    custom: void 0,
    refresh_sm: 20
  };
  var lucideToDiscordIcon = (lucide, middleWare = (v) => v) => ((props) => react_default.createElement(lucide, middleWare({
    size: sizes[props.size || "md"],
    className: props.className,
    color: props.color
  }, props)));

  // src/InAccord/ui/settings.tsx
  init_discordmodules();
  init_button();
  init_lucide_react();
  init_i18n();
  init_modals();
  var UserSettings = getByKeys(["openUserSettings", "openUserSettingsFromParsedUrl"]);
  var SettingsMenuIcon = lucideToDiscordIcon(Settings2);
  var useCustomCSSClickable = () => {
    const state = useStateFromStores(settings_default2, () => settings_default2.get("settings", "customcss", "openAction"));
    return ["detached", "external", "system"].includes(state);
  };
  var useCustomCSSViewable = () => !useCustomCSSClickable();
  function LayerSettingTitle() {
    const [node, setNode] = react_default.useState();
    return /* @__PURE__ */ react_default.createElement(react_default.Fragment, null, /* @__PURE__ */ react_default.createElement(
      "div",
      {
        className: "ia-sidebar-header",
        ref: (v) => {
          setNode(v?.parentElement?.parentElement || v);
          return setNode;
        }
      },
      "InAccord"
    ), node && ReactDOM.createPortal(
      /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", text: t("Modals.changelog")}, (props) => /* @__PURE__ */ react_default.createElement(Button, {...props, className: "ia-changelog-button", look: Button.Looks.BLANK, color: Button.Colors.TRANSPARENT, size: Button.Sizes.NONE, onClick: () => Modals.showChangelogModal(changelog_default)}, /* @__PURE__ */ react_default.createElement(History, {className: "ia-icon", size: "16px"}))),
      node
    ));
  }
  var SettingsTitleContext = react_default.createContext((v) => v);
  var settings_default3 = new class SettingsRenderer {
    initialize() {
      this.patchSections();
      this.patchModalSettings();
      this.patchVersionInformation();
    }
    onDrawerToggle(collection, group, state) {
      const drawerStates = json_default.get("misc", "drawerStates") || {};
      if (!drawerStates[collection]) drawerStates[collection] = {};
      drawerStates[collection][group] = state;
      json_default.set("misc", "drawerStates", drawerStates);
    }
    getDrawerState(collection, group, defaultValue) {
      const drawerStates = json_default.get("misc", "drawerStates") || {};
      if (!drawerStates[collection]) return defaultValue;
      if (!drawerStates[collection].hasOwnProperty(group)) return defaultValue;
      return drawerStates[collection][group];
    }
    onChange(onChange) {
      return (categoryId, settingId, value) => {
        onChange(categoryId, settingId, value);
        if (settingId === "customcss") {
          setTimeout(this.forceUpdate.bind(this), 250);
        }
      };
    }
    buildSettingsPanel(id, title, groups, onChange) {
      return react_default.createElement(SettingsPanel, {id, title, groups, onChange: this.onChange(onChange).bind(this), onDrawerToggle: this.onDrawerToggle.bind(this), getDrawerState: this.getDrawerState.bind(this)});
    }
    getAddonPanel(title, options = {}) {
      return (props) => {
        return react_default.createElement(AddonPage, Object.assign({}, {
          title,
          ...props
        }, options));
      };
    }
    async patchSections() {
      const UserSettingsLayer = await getLazyByPrototypes(["getPredicateSections"]);
      if (!UserSettingsLayer) return;
      Patcher.after("SettingsManager", UserSettingsLayer.prototype, "getPredicateSections", (thisObject, _, returnValue) => {
        if (!Array.isArray(returnValue)) return;
        const hasSection = (id) => returnValue.some((s) => String(s?.section || "").toLowerCase() === id.toLowerCase());
        const append = (section) => {
          if (section?.section && hasSection(String(section.section))) return;
          returnValue.push(section);
        };
        append({section: "DIVIDER"});
        append({section: "CUSTOM", element: SettingsTitle2});
        for (const collection of settings_default2.collections) {
          append({
            section: collection.id,
            label: collection.name.toString(),
            className: `ia-${collection.id}-tab`,
            element: () => this.buildSettingsPanel(collection.id, collection.name, collection.settings, settings_default2.onSettingChange.bind(settings_default2, collection.id))
          });
        }
        for (const panel of settings_default2.panels.sort((a, b) => a.order > b.order ? 1 : -1)) {
          if (panel.clickListener) panel.onClick = () => panel.clickListener?.(thisObject);
          if (!panel.className) panel.className = `ia-${panel.id}-tab`;
          if (panel.type === "addon" && !panel.element) panel.element = this.getAddonPanel(panel.label, {store: panel.manager});
          append({
            section: panel.id,
            label: panel.label.toString(),
            className: panel.className,
            element: panel.element,
            onClick: panel.onClick
          });
        }
      });
    }
    layoutBuilder;
    getLayoutBuilder() {
      if (this.layoutBuilder) return this.layoutBuilder;
      const layoutModuleRaw = getBySource(["$Root", "buildLayout"], {searchDefault: false});
      const out = {};
      for (const key in layoutModuleRaw) {
        if (!Object.hasOwn(layoutModuleRaw, key)) continue;
        const match = String(layoutModuleRaw[key]).match(/\..{1,3}\.(.+?),/);
        if (match) {
          const outKey = match[1].toLowerCase().replace(/_([a-z])/gi, (_, letter) => letter.toUpperCase());
          Object.defineProperty(out, outKey, {
            value(id, ...args) {
              id = `inaccord_${id}_${outKey}`;
              return layoutModuleRaw[key](id, ...args);
            }
          });
        }
      }
      return this.layoutBuilder = out;
    }
    async patchModalSettings() {
      const rootLayout = await getLazy((m) => m?.key === "$Root", {searchExports: true, searchDefault: false});
      if (!rootLayout) return;
      this.patchSettingsSearch();
      const layoutBuilder = this.getLayoutBuilder();
      const section = layoutBuilder.section("inaccord", {
        buildLayout: () => {
          const layouts = [];
          const insert = (key, item) => {
            let layout = [];
            if ("render" in item) {
              let panelLayout = [];
              if (layoutBuilder.pane) {
                const pane = layoutBuilder.pane(key, {
                  buildLayout: () => [],
                  StronglyDiscouragedCustomComponent: item.render,
                  useTitle: item.header
                });
                panelLayout = [pane];
              }
              const panel = layoutBuilder.panel(key, {
                buildLayout: () => panelLayout,
                StronglyDiscouragedCustomComponent: item.render,
                useTitle: item.header
              });
              layout = [panel];
            }
            const sidebar = layoutBuilder.sidebarItem(key, {
              buildLayout: () => layout,
              useTitle: item.title,
              icon: item.icon,
              getLegacySearchKey: () => `inaccord_${key}`,
              usePredicate: () => true
            });
            if (typeof item.predicate === "function") {
              sidebar.usePredicate = () => {
                return item["predicate"]();
              };
            }
            sidebar.useSearchable = () => ["test"];
            if ("onClick" in item) {
              sidebar.onClick = item.onClick;
            }
            layouts.push(sidebar);
          };
          const makeSettingsPanelProvider = (children) => {
            const ref = {
              current: {}
            };
            let forceUpdate;
            function PanelHeader() {
              const [node, setNode] = react_default.useState();
              forceUpdate = useForceUpdate()[1];
              return /* @__PURE__ */ react_default.createElement(react_default.Fragment, null, /* @__PURE__ */ react_default.createElement(
                "div",
                {
                  className: "ia-settings-page-title",
                  ref: (v) => {
                    if (v?.parentElement?.parentElement) {
                      v.parentElement.parentElement.classList.add("ia-settings-title-extend");
                      setNode(v.parentElement.parentElement);
                    } else {
                      setNode(v);
                    }
                    return () => setNode(void 0);
                  }
                },
                ref.current.text
              ), node && ReactDOM.createPortal(
                /* @__PURE__ */ react_default.createElement("div", {className: "ia-settings-page-title-children"}, ref.current.children),
                node
              ));
            }
            return {
              header: () => /* @__PURE__ */ react_default.createElement(PanelHeader, null),
              render: () => /* @__PURE__ */ react_default.createElement(
                SettingsTitleContext,
                {
                  value: (value) => {
                    ref.current = value.props;
                    forceUpdate();
                    return null;
                  }
                },
                children
              )
            };
          };
          for (const collection of settings_default2.collections) {
            insert(collection.id, {
              ...makeSettingsPanelProvider(this.buildSettingsPanel(collection.id, collection.name, collection.settings, settings_default2.onSettingChange.bind(settings_default2, collection.id))),
              icon: SettingsMenuIcon,
              title: () => collection.name
            });
          }
          for (const panel of settings_default2.panels.sort((a, b) => a.order > b.order ? 1 : -1)) {
            if (panel.type === "addon" && !panel.element) panel.element = this.getAddonPanel(panel.label, {store: panel.manager});
            const icon = panel.discordIcon || (panel.icon ? lucideToDiscordIcon(panel.icon) : () => panel.id);
            if (panel.clickListener && !panel.element) {
              insert(panel.id, {
                icon,
                title: () => panel.label,
                onClick: () => panel.clickListener?.(void 0)
              });
              continue;
            }
            if (panel.id === "customcss") {
              insert("customcss_tab", {
                ...makeSettingsPanelProvider(react_default.createElement(panel.element)),
                icon,
                title: () => panel.label,
                predicate: useCustomCSSViewable
              });
              insert("customcss_clickable", {
                icon,
                title: () => panel.label,
                predicate: useCustomCSSClickable,
                onClick: () => customcss_default.open()
              });
              continue;
            }
            insert(panel.id, {
              ...makeSettingsPanelProvider(react_default.createElement(panel.element)),
              icon,
              title: () => panel.label
            });
          }
          return layouts;
        },
        useTitle: () => Object.assign(/* @__PURE__ */ react_default.createElement(LayerSettingTitle, null), {toString: () => "InAccord"})
      });
      Patcher.after("SettingsManager", rootLayout, "buildLayout", (_that, _args, res) => {
        const iaSectionKey = section?.key ?? "inaccord_inaccord_section";
        if (Array.isArray(res) && res.some((layout) => layout?.key === iaSectionKey)) return;
        res.push(section);
      });
    }
    patchSettingsSearch() {
      const search = getMangled(".PRIVACY_AND_SAFETY_PERSISTENT_VERIFICATION_CODES]", {
        search: filter_exports.byStrings(".PRIVACY_AND_SAFETY_PERSISTENT_VERIFICATION_CODES]")
      });
      Patcher.after("SettingsManager", search, "search", (_that, _args, res) => {
        res = {...res};
        function insert(key, item) {
          res[`inaccord_${key}`] = {
            ...item,
            ariaLabel: item.label,
            section: "inaccord"
          };
        }
        for (const collection of settings_default2.collections) {
          const items = collection.settings.map((m) => [m.name, m.settings.map((setting) => setting.name)]).flat(2);
          insert(collection.id, {
            label: collection.name,
            searchableTitles: [
              "inaccord",
              collection.name,
              ...items
            ]
          });
        }
        for (const panel of settings_default2.panels.sort((a, b) => a.order > b.order ? 1 : -1)) {
          const content = {
            label: panel.label,
            searchableTitles: [
              "inaccord",
              panel.label,
              typeof panel.searchable === "function" ? panel.searchable().filter((m) => typeof m === "string") : []
            ].flat()
          };
          if (panel.id === "customcss") {
            insert("customcss_tab", content);
            insert("customcss_clickable", content);
            continue;
          }
          insert(panel.id, content);
        }
        return Object.freeze(res);
      });
    }
    async patchVersionInformation() {
      const versionDisplayModule = await getLazyByStrings(["copyValue", "RELEASE_CHANNEL"], {defaultExport: false});
      if (!versionDisplayModule?.Z) return;
      Patcher.after("SettingsManager", versionDisplayModule, "Z", () => {
        return react_default.createElement(VersionInfo);
      });
    }
    openSettingsPage(key) {
      const baseKey = key === "customcss" ? "customcss_tab" : key;
      const altKey = baseKey.endsWith("s") ? baseKey.slice(0, -1) : `${baseKey}s`;
      const viewClass = getByKeys(["standardSidebarView"])?.standardSidebarView.split(" ")[0];
      const node = viewClass ? document.querySelector(`.${viewClass}`) : null;
      if (node) {
        const settingsView = findInTree(
          reactutils_default.getInternalInstance(node),
          (m) => m && typeof m.onSetSection === "function",
          {walkable: ["child", "memoizedProps", "props", "children", "return", "stateNode"]}
        );
        if (settingsView?.onSetSection) {
          const sectionCandidates = Array.from(/* @__PURE__ */ new Set([baseKey, altKey, key]));
          for (const sectionKey of sectionCandidates) {
            try {
              settingsView.onSetSection(sectionKey);
              return;
            } catch {
            }
          }
        }
      }
      const candidates = Array.from(new Set([
        {route: `inaccord_${baseKey}_panel`, section: baseKey},
        {route: `inaccord_${baseKey}_sidebarItem`, section: baseKey},
        {route: `inaccord_${baseKey}`, section: baseKey},
        {route: `inaccord_${altKey}_panel`, section: altKey},
        {route: `inaccord_${altKey}_sidebarItem`, section: altKey},
        {route: `inaccord_${altKey}`, section: altKey},
        {route: `inaccord_${key}_panel`, section: key},
        {route: `inaccord_${key}_sidebarItem`, section: key},
        {route: `inaccord_${key}`, section: key}
      ].map((value) => `${value.route}|${value.section}`))).map((packed) => {
        const split = packed.split("|");
        return {route: split[0], section: split[1]};
      });
      let opened = false;
      for (const candidate of candidates) {
        try {
          const result = UserSettings?.openUserSettings?.(candidate.route, {
            section: candidate.section
          });
          if (typeof result === "boolean") {
            if (result) {
              opened = true;
              break;
            }
            continue;
          }
          if (typeof result !== "undefined") {
            opened = true;
            break;
          }
          UserSettings?.openUserSettingsFromParsedUrl?.(candidate.route, {
            section: candidate.section
          });
          opened = true;
          break;
        } catch {
        }
      }
      if (!opened) {
        this.forceUpdate();
      }
    }
    forceUpdate() {
      const viewClass = getByKeys(["standardSidebarView"])?.standardSidebarView.split(" ")[0];
      const node = document.querySelector(`.${viewClass}`);
      if (!node) return;
      const stateNode = findInTree(reactutils_default.getInternalInstance(node), (m) => m && m.getPredicateSections, {walkable: ["return", "stateNode"]});
      if (stateNode) stateNode.forceUpdate();
    }
  }();

  // src/InAccord/builtins/customcss.ts
  init_config();
  init_react();
  init_emitter();
  init_dommanager();
  init_i18n();
  init_discordmodules();

  // src/InAccord/ui/customcss/csseditor.tsx
  init_react();
  init_i18n();
  init_emitter();
  init_settings3();
  init_editor3();
  init_lucide_react();
  var {useState: useState23, useCallback: useCallback28, useEffect: useEffect11, forwardRef: forwardRef4, useImperativeHandle: useImperativeHandle4, useRef: useRef10} = react_default;
  var csseditor_default = forwardRef4(function CssEditor({css: css2, openNative, update, save, onChange: notifyParent, readOnly = false, id = "ia-customcss-editor", openDetached = false}, ref) {
    const editorRef = useRef10(null);
    const [hasUnsavedChanges, setUnsaved] = useState23(false);
    const updateEditor = useCallback28((newCSS) => {
      editorRef.current.value = newCSS;
    }, [editorRef]);
    useImperativeHandle4(ref, () => {
      return {
        resize() {
          editorRef.current.resize();
        },
        get value() {
          return editorRef.current.getValue();
        },
        set value(newValue) {
          editorRef.current.setValue(newValue);
        },
        get hasUnsavedChanges() {
          return hasUnsavedChanges;
        }
      };
    }, [hasUnsavedChanges]);
    useEffect11(() => {
      emitter_default.on("customcss-updated", updateEditor);
      return () => emitter_default.off("customcss-updated", updateEditor);
    }, [updateEditor]);
    const toggleLiveUpdate = useCallback28((checked) => settings_default2.set("settings", "customcss", "liveUpdate", checked), []);
    const updateCss = useCallback28((event, newCSS) => update?.(newCSS), [update]);
    const popoutNative = useCallback28(() => openNative?.(), [openNative]);
    const popout = useCallback28((event, currentCSS) => openDetached?.(currentCSS), [openDetached]);
    const onChange = useCallback28((newCSS) => {
      notifyParent?.(newCSS);
      setUnsaved(true);
    }, [notifyParent]);
    const saveCss = useCallback28((event, newCSS) => {
      save?.(newCSS);
      setUnsaved(false);
    }, [save]);
    return /* @__PURE__ */ react_default.createElement(
      editor_default3,
      {
        ref: editorRef,
        readOnly,
        id,
        onChange,
        controls: [
          {label: /* @__PURE__ */ react_default.createElement(RotateCw, {size: "18px"}), tooltip: t("CustomCSS.update"), onClick: updateCss},
          {label: /* @__PURE__ */ react_default.createElement(Save, {size: "18px"}), tooltip: t("CustomCSS.save"), onClick: saveCss},
          {label: /* @__PURE__ */ react_default.createElement(Pencil, {size: "18px"}), tooltip: t("CustomCSS.openNative"), onClick: popoutNative},
          {label: t("Collections.settings.customcss.liveUpdate.name"), type: "boolean", onChange: toggleLiveUpdate, checked: settings_default2.get("settings", "customcss", "liveUpdate"), side: "right"},
          openDetached && {label: /* @__PURE__ */ react_default.createElement(ExternalLink, {size: "18px"}), tooltip: t("CustomCSS.openDetached"), onClick: popout, side: "right"}
        ].filter((c) => c),
        value: css2
      }
    );
  });

  // src/InAccord/builtins/customcss.ts
  init_floatingwindows();
  init_webpack2();
  init_utils();
  init_remote();
  init_lucide_react();
  var closeUserSettings = getByStrings(["closeUserSettings"]);
  var UserSettings2 = getByKeys(["updateAccount"]);
  var customcss_default = new class CustomCSS extends BuiltinModule {
    get name() {
      return "Custom CSS";
    }
    get category() {
      return "customcss";
    }
    get id() {
      return "customcss";
    }
    get startDetached() {
      return settings_default2.get(this.collection, this.category, "openAction") == "detached";
    }
    get nativeOpen() {
      return settings_default2.get(this.collection, this.category, "openAction") == "system";
    }
    get startAsExternal() {
      return settings_default2.get(this.collection, this.category, "openAction") == "external";
    }
    get file() {
      return path_default.resolve(config_default.get("channelPath"), "custom.css");
    }
    savedCss;
    insertedCss;
    isDetached;
    watcher;
    constructor() {
      super();
      this.savedCss = "";
      this.insertedCss = "";
      this.isDetached = false;
    }
    Page = () => {
      const set = react_default.useContext(SettingsTitleContext);
      return [
        set(react_default.createElement(SettingsTitle, {text: t("CustomCSS.editorTitle")})),
        react_default.createElement(csseditor_default, {
          css: this.savedCss,
          save: this.saveCSS.bind(this),
          update: this.insertCSS.bind(this),
          openNative: this.openNative.bind(this),
          openDetached: this.openDetached.bind(this),
          onChange: this.onChange.bind(this)
        })
      ];
    };
    async enabled() {
      settings_default2.registerPanel(this.id, t("Panels.customcss"), {
        order: 2,
        icon: Pencil,
        element: () => react_default.createElement(this.Page),
        onClick: (thisObject) => {
          if (this.isDetached) return;
          if (this.nativeOpen) return this.openNative();
          else if (this.startDetached) return this.openDetached(this.savedCss);
          else if (this.startAsExternal) return this.openExternal();
          const settingsView = findInTree(thisObject._reactInternals, (m) => m && m.onSetSection, {walkable: ["child", "memoizedProps", "props", "children"]});
          if (settingsView && settingsView.onSetSection) settingsView.onSetSection(this.id);
        }
      });
      this.loadCSS();
      this.insertCSS(this.savedCss);
      this.watchContent();
    }
    async disabled() {
      settings_default2.removePanel(this.id);
      this.unwatchContent();
      this.insertCSS("");
    }
    watchContent() {
      if (this.watcher) return this.error("Already watching content.");
      if (!fs_default.existsSync(this.file)) {
        try {
          fs_default.mkdirSync(path_default.dirname(this.file), {recursive: true});
          fs_default.writeFileSync(this.file, "");
        } catch (err) {
          return this.error("Could not create custom.css file.", err);
        }
      }
      const timeCache = {};
      this.log("Starting to watch content.");
      this.watcher = fs_default.watch(this.file, {persistent: false}, async (eventType, filename) => {
        if (!eventType || !filename) return;
        await new Promise((r2) => setTimeout(r2, 50));
        try {
          fs_default.statSync(this.file);
        } catch (err) {
          if (err.code !== "ENOENT") return;
          delete timeCache[filename];
          this.saveCSS("");
        }
        const stats = fs_default.statSync(this.file);
        if (!stats || !stats.mtimeMs) return;
        if (typeof stats.mtimeMs !== "number") return;
        if (timeCache[filename] == stats.mtimeMs) return;
        timeCache[filename] = stats.mtimeMs;
        if (eventType == "change") {
          const oldCSS = this.savedCss;
          this.loadCSS();
          if (oldCSS === this.savedCss) return;
          this.insertCSS(this.savedCss);
          emitter_default.emit("customcss-updated", this.savedCss);
        }
      });
    }
    unwatchContent() {
      if (!this.watcher) return this.error("Was not watching content.");
      this.watcher.close();
      delete this.watcher;
      this.log("No longer watching content.");
    }
    onChange(value) {
      if (!settings_default2.get("settings", "customcss", "liveUpdate")) return;
      this.insertCSS(value);
      this.saveCSS(value);
    }
    loadCSS() {
      try {
        this.savedCss = fs_default.readFileSync(this.file).toString();
      } catch {
        this.savedCss = "";
      }
    }
    insertCSS(newCss) {
      if (typeof newCss === "undefined") newCss = this.insertedCss;
      else this.insertedCss = newCss;
      DOMManager.updateCustomCSS(newCss);
    }
    saveCSS(newCss) {
      if (typeof newCss !== "undefined") this.savedCss = newCss;
      fs_default.writeFileSync(this.file, this.savedCss);
    }
    open() {
      if (this.isDetached) return;
      if (this.nativeOpen) return this.openNative();
      else if (this.startDetached) return this.openDetached(this.savedCss);
      else if (this.startAsExternal) return this.openExternal();
      return settings_default3.openSettingsPage(this.id);
    }
    openNative() {
      electron_default.shell.openExternal(`file://${this.file}`);
    }
    openDetached(currentCSS) {
      const editorRef = react_default.createRef();
      const editor = react_default.createElement(csseditor_default, {
        id: "ia-floating-editor",
        ref: editorRef,
        css: currentCSS,
        save: this.saveCSS.bind(this),
        update: this.insertCSS.bind(this),
        openNative: this.openNative.bind(this),
        onChange: debounce(this.onChange.bind(this), 500)
      });
      FloatingWindows.open({
        onClose: () => {
          this.isDetached = false;
        },
        onResize: () => {
          if (!editorRef || !editorRef.current || !editorRef.current.resize) return;
          editorRef.current.resize();
        },
        title: t("CustomCSS.editorTitle"),
        id: "floating-editor-window",
        height: 470,
        width: 410,
        center: true,
        resizable: true,
        children: editor,
        confirmClose: () => {
          if (!editorRef || !editorRef.current) return false;
          if (settings_default2.get("settings", "customcss", "liveUpdate")) return false;
          return editorRef.current.hasUnsavedChanges;
        },
        confirmationText: t("CustomCSS.confirmationText")
      });
      this.isDetached = true;
      if (closeUserSettings?.()) return;
      UserSettings2?.close();
      discordmodules_default.Dispatcher?.dispatch({type: "LAYER_POP"});
    }
    openExternal() {
      remote_default.editor.open("custom-css");
    }
  }();

  // src/InAccord/builtins/general/voicedisconnect.ts
  init_discordmodules();
  var voicedisconnect_default = new class VoiceDisconnect extends BuiltinModule {
    get name() {
      return "VoiceDisconnect";
    }
    get category() {
      return "general";
    }
    get id() {
      return "voiceDisconnect";
    }
    constructor() {
      super();
      this.beforeUnload = this.beforeUnload.bind(this);
    }
    async enabled() {
      window.addEventListener("beforeunload", this.beforeUnload);
    }
    async disabled() {
      window.removeEventListener("beforeunload", this.beforeUnload);
    }
    beforeUnload() {
      discordmodules_default.ChannelActions?.selectVoiceChannel(null, null);
    }
  }();

  // src/InAccord/builtins/general/mediakeys.ts
  init_i18n();
  init_ipc();
  init_modals();
  var mediakeys_default = new class MediaKeys extends BuiltinModule {
    get name() {
      return "DisableMediaKeys";
    }
    get category() {
      return "general";
    }
    get id() {
      return "mediaKeys";
    }
    async enabled() {
      this.showModal();
    }
    async disabled() {
      this.showModal();
    }
    showModal() {
      if (!this.initialized) return;
      Modals.showConfirmationModal(t("Modals.additionalInfo"), t("Modals.restartPrompt"), {
        confirmText: t("Modals.restartNow"),
        cancelText: t("Modals.restartLater"),
        danger: true,
        onConfirm: () => ipc_default.relaunch()
      });
    }
  }();

  // src/InAccord/builtins/general/contextmenu.ts
  init_i18n();
  init_settings3();

  // src/InAccord/api/contextmenu.ts
  init_webpack2();
  init_patcher();
  init_logger();
  init_react();
  var startupComplete = false;
  var ModulesBundle = getByKeys(["MenuItem", "Menu"]);
  var MenuComponents = {
    Separator: ModulesBundle?.MenuSeparator,
    CheckboxItem: ModulesBundle?.MenuCheckboxItem,
    RadioItem: ModulesBundle?.MenuRadioItem,
    ControlItem: ModulesBundle?.MenuControlItem,
    Group: ModulesBundle?.MenuGroup,
    Item: ModulesBundle?.MenuItem,
    Menu: ModulesBundle?.Menu
  };
  startupComplete = Object.values(MenuComponents).every((v) => v);
  if (!startupComplete) {
    const REGEX = /(function .{1,3}\(.{1,3}\){return null}){5}/;
    const EXTRACT_REGEX = /\.type===.{1,3}\.(.{1,3})\)return .{1,3}\.push\((?:null!=.{1,3}\.props\..+?)?{type:"(.+?)",/g;
    const EXTRACT_GROUP_REGEX = /\.type===.{1,3}\.(.{1,3})\){.+{type:"groupstart"/;
    const EXTRACT_GROUP_ITEM_REGEX = /\.type===.{1,3}\.(.{1,3})\){.+{type:"(groupstart|customitem)".+\.type===.{1,3}\.(.{1,3})\){.+?{type:"(groupstart|customitem)"/;
    let menuItemsId;
    let menuParser = "";
    for (const key in webpackRequire.m) {
      if (Object.prototype.hasOwnProperty.call(webpackRequire.m, key)) {
        if (REGEX.test(webpackRequire.m[key].toString())) {
          menuItemsId = key;
          break;
        }
      }
    }
    for (const key in webpackRequire.m) {
      if (Object.prototype.hasOwnProperty.call(webpackRequire.m, key)) {
        const string = webpackRequire.m[key].toString();
        if (string.includes(menuItemsId) && string.includes("Menu API only allows Items and groups of Items as children")) {
          menuParser = string;
          break;
        }
      }
    }
    const contextMenuComponents = webpackRequire(menuItemsId);
    for (const [, key, type] of menuParser.matchAll(EXTRACT_REGEX)) {
      switch (type) {
        case "separator":
          MenuComponents.Separator ??= contextMenuComponents[key];
          break;
        case "radio":
          MenuComponents.RadioItem ??= contextMenuComponents[key];
          break;
        case "checkbox":
          MenuComponents.CheckboxItem ??= contextMenuComponents[key];
          break;
        case "compositecontrol":
        case "control":
          MenuComponents.ControlItem ??= contextMenuComponents[key];
          break;
        case "customitem":
        case "item":
          MenuComponents.Item ??= contextMenuComponents[key];
          break;
      }
    }
    const matchA = menuParser.match(EXTRACT_GROUP_REGEX);
    if (matchA) {
      MenuComponents.Group ??= contextMenuComponents[matchA[1]];
    }
    const matchB = menuParser.match(EXTRACT_GROUP_ITEM_REGEX);
    if (matchB) {
      MenuComponents.Group ??= contextMenuComponents[matchB[matchB[2] === "groupstart" ? 1 : 3]];
      MenuComponents.Item ??= contextMenuComponents[matchB[matchB[2] === "customitem" ? 1 : 3]];
    }
    MenuComponents.Menu ??= getModule(filter_exports.byStrings("getContianerProps()", ".keyboardModeEnabled&&null!="), {searchExports: true});
  }
  startupComplete = Object.values(MenuComponents).every((v) => v);
  var ContextMenuActions = (() => {
    const out = {};
    try {
      Object.assign(out, getMangled(filter_exports.bySource("new DOMRect", "CONTEXT_MENU_CLOSE"), {
        closeContextMenu: filter_exports.byStrings("CONTEXT_MENU_CLOSE"),
        openContextMenu: filter_exports.byStrings("renderLazy")
      }, {searchDefault: false}));
      startupComplete &&= typeof out.closeContextMenu === "function" && typeof out.openContextMenu === "function";
    } catch (error) {
      startupComplete = false;
      Logger.stacktrace("ContextMenu~Components", "Fatal startup error:", error);
      Object.assign(out, {
        closeContextMenu: () => {
        },
        openContextMenu: () => {
        }
      });
    }
    return out;
  })();
  var MenuPatcher = class _MenuPatcher {
    static MAX_PATCH_ITERATIONS = 10;
    static patches = {};
    static subPatches = /* @__PURE__ */ new WeakMap();
    static initialize() {
      if (!startupComplete) return Logger.warn("ContextMenu~Patcher", "Startup wasn't successfully, aborting initialization.");
      const {module, key} = (() => {
        const foundModule = getModule((m) => Object.values(m).some((v) => typeof v === "function" && v.toString().includes(`type:"CONTEXT_MENU_CLOSE"`)), {searchExports: false});
        const foundKey = Object.keys(foundModule).find((k) => foundModule[k].length === 3);
        return {module: foundModule, key: foundKey};
      })();
      Patcher.before("ContextMenuPatcher", module, key, (_, methodArguments) => {
        const promise = methodArguments[1];
        methodArguments[1] = async function (...args) {
          const render = await promise.apply(this, args);
          return (props) => {
            const res = render(props);
            if (res?.props.navId) {
              _MenuPatcher.runPatches(res.props.navId, res, props);
            } else if (typeof res?.type === "function") {
              _MenuPatcher.patchRecursive(res, "type");
            }
            return res;
          };
        };
      });
    }
    static patchRecursive(target, method, iteration = 0) {
      if (iteration >= this.MAX_PATCH_ITERATIONS) return;
      const proxyFunction = this.subPatches.get(target[method]) ?? (() => {
        const originalFunction = target[method];
        const depth = ++iteration;
        function patch(...args) {
          const res = originalFunction.apply(this, args);
          if (!res) return res;
          if (res.props?.navId ?? res.props?.children?.props?.navId) {
            _MenuPatcher.runPatches(res.props.navId ?? res.props?.children?.props?.navId, res, args[0]);
          } else {
            const layer = res.props.children ? res.props.children : res;
            if (typeof layer?.type == "function") {
              _MenuPatcher.patchRecursive(layer, "type", depth);
            }
          }
          return res;
        }
        patch._originalFunction = originalFunction;
        Object.assign(patch, originalFunction);
        this.subPatches.set(originalFunction, patch);
        return patch;
      })();
      target[method] = proxyFunction;
    }
    static runPatches(id, res, props) {
      if (!this.patches[id]) return;
      for (const patch of this.patches[id]) {
        try {
          patch(res, props);
        } catch (error) {
          Logger.error("ContextMenu~runPatches", `Could not run ${id} patch for`, patch, error);
        }
      }
    }
    static patch(id, callback) {
      this.patches[id] ??= /* @__PURE__ */ new Set();
      this.patches[id].add(callback);
    }
    static unpatch(id, callback) {
      this.patches[id]?.delete(callback);
    }
  };
  var ContextMenu = class {
    /**
     * Allows you to patch a given context menu. Acts as a wrapper around the `Patcher`.
     *
     * @param {string} navId Discord's internal `navId` used to identify context menus
     * @param {function} callback Callback function that accepts the React render tree
     * @returns {function} A function that automatically unpatches
     */
    patch(navId, callback) {
      MenuPatcher.patch(navId, callback);
      return () => MenuPatcher.unpatch(navId, callback);
    }
    /**
     * Allows you to remove the patch added to a given context menu.
     *
     * @param {string} navId The original `navId` from patching
     * @param {function} callback The original callback from patching
     */
    unpatch(navId, callback) {
      MenuPatcher.unpatch(navId, callback);
    }
    /**
     * Builds a single menu item. The only prop shown here is the type, the rest should
     * match the actual component being built. View those to see what options exist
     * for each, they often have less in common than you might think.
     *
     * @param {object} props Props used to build the item
     * @param {string} [props.type="text"] Type of the item, options: text, submenu, toggle, radio, custom, separator
     * @returns {object} The created component
     *
     * @example
     * // Creates a single menu item that prints "MENU ITEM" on click
     * ContextMenu.buildItem({
     *      label: "Menu Item",
     *      action: () => {console.log("MENU ITEM");}
     * });
     *
     * @example
     * // Creates a single toggle item that starts unchecked
     * // and print the new value on every toggle
     * ContextMenu.buildItem({
     *      type: "toggle",
     *      label: "Item Toggle",
     *      checked: false,
     *      action: (newValue) => {console.log(newValue);}
     * });
     */
    buildItem(props) {
      const {type} = props;
      if (type === "separator") return react_default.createElement(MenuComponents.Separator);
      let Component2 = MenuComponents.Item;
      if (type === "submenu") {
        if (!props.children) props.children = this.buildMenuChildren(props.render || props.items);
      } else if (type === "toggle" || type === "radio") {
        Component2 = type === "toggle" ? MenuComponents.CheckboxItem : MenuComponents.RadioItem;
        if (props.active) props.checked = props.active;
      } else if (type === "control") {
        Component2 = MenuComponents.ControlItem;
      }
      if (!props.id) props.id = `${props.label.replace(/^[^a-z]+|[^\w-]+/gi, "-")}`;
      if (props.danger) props.color = "danger";
      if (props.onClick && !props.action) props.action = props.onClick;
      props.extended = true;
      if (type === "toggle") {
        const [active, doToggle] = react_default.useState(props.checked || false);
        const originalAction = props.action;
        props.checked = active;
        props.action = function (ev) {
          originalAction(ev);
          doToggle(!active);
        };
      }
      return react_default.createElement(Component2, props);
    }
    /**
     * Creates the all the items **and groups** of a context menu recursively.
     * There is no hard limit to the number of groups within groups or number
     * of items in a menu.
     *
     * @param {Array<object>} setup Array of item props used to build items. See {@link ContextMenu.buildItem}.
     * @returns {Array<object>} Array of the created component
     *
     * @example
     * // Creates a single item group item with a toggle item
     * ContextMenu.buildMenuChildren([{
     *      type: "group",
     *      items: [{
     *          type: "toggle",
     *          label: "Item Toggle",
     *          active: false,
     *          action: (newValue) => {console.log(newValue);}
     *      }]
     * }]);
     *
     * @example
     * // Creates two item groups with a single toggle item each
     * ContextMenu.buildMenuChildren([{
     *     type: "group",
     *     items: [{
     *         type: "toggle",
     *         label: "Item Toggle",
     *         active: false,
     *         action: (newValue) => {
     *             console.log(newValue);
     *         }
     *     }]
     * }, {
     *     type: "group",
     *     items: [{
     *         type: "toggle",
     *         label: "Item Toggle",
     *         active: false,
     *         action: (newValue) => {
     *             console.log(newValue);
     *         }
     *     }]
     * }]);
     */
    buildMenuChildren(setup) {
      const mapper = (s) => {
        if (s.type === "group") return buildGroup(s);
        return this.buildItem(s);
      };
      const buildGroup = function (group) {
        const items = group.items.map(mapper).filter((i) => i);
        return react_default.createElement(MenuComponents.Group, null, items);
      };
      return setup.map(mapper).filter((i) => i);
    }
    /**
     * Creates the menu *component* including the wrapping `ContextMenu`.
     * Calls {@link ContextMenu.buildMenuChildren} under the covers.
     * Used to call in combination with {@link ContextMenu.open}.
     *
     * @param {Array<object>} setup Array of item props used to build items. See {@link ContextMenu.buildMenuChildren}.
     * @returns {function} The unique context menu component
     */
    buildMenu(setup) {
      return (props) => {
        return react_default.createElement(MenuComponents.Menu, props, this.buildMenuChildren(setup));
      };
    }
    /**
     * Function that allows you to open an entire context menu. Recommended to build the menu with this module.
     *
     * @param {MouseEvent} event The context menu event. This can be emulated, requires target, and all X, Y locations.
     * @param {function} menuComponent Component to render. This can be any React component or output of {@link ContextMenu.buildMenu}.
     * @param {object} config Configuration/props for the context menu
     * @param {string} [config.position="right"] Default position for the menu, options: "left", "right"
     * @param {string} [config.align="top"] Default alignment for the menu, options: "bottom", "top"
     * @param {function} [config.onClose] Function to run when the menu is closed
     */
    open(event, menuComponent, config) {
      return ContextMenuActions.openContextMenu(event, function (e) {
        return react_default.createElement(menuComponent, Object.assign({}, e, {onClose: ContextMenuActions.closeContextMenu}));
      }, config);
    }
    /**
     * Closes the current opened context menu immediately.
     */
    close() {
      ContextMenuActions.closeContextMenu();
    }
  };
  Object.assign(ContextMenu.prototype, MenuComponents);
  Object.freeze(ContextMenu);
  Object.freeze(ContextMenu.prototype);
  try {
    MenuPatcher.initialize();
  } catch (error) {
    Logger.error("ContextMenu~Patcher", "Fatal error:", error);
  }
  var contextmenu_default = ContextMenu;

  // src/InAccord/builtins/general/contextmenu.ts
  init_backupmanager();
  init_pluginmanager();
  init_thememanager();
  init_react();
  init_dommanager();
  init_modals();
  init_utils();
  var ContextMenu2 = new contextmenu_default();
  var contextmenu_default2 = new class iaContextMenu extends BuiltinModule {
    get name() {
      return "iaContextMenu";
    }
    get category() {
      return "general";
    }
    get id() {
      return "iaContextMenu";
    }
    constructor() {
      super();
      this.callback = this.callback.bind(this);
    }
    async enabled() {
      this.patch = ContextMenu2.patch("user-settings-cog", this.callback);
    }
    async disabled() {
      this.patch?.();
    }
    callback(retVal) {
      const target = findInTree(retVal, (b) => Array.isArray(b) && b.some((e) => e?.key?.toLowerCase() === "my_account"), {walkable: ["props", "children"]});
      if (!target) return;
      if (target.some((e) => e.props.label.toLowerCase() === "inaccord")) return;
      const items = settings_default2.collections.map((c) => this.buildCollectionMenu(c));
      items.push({
        label: t("Panels.updates"),
        action: () => this.openCategory("updates")
      });
      if (settings_default2.get("settings", "customcss", "customcss")) {
        items.push({
          label: t("Panels.customcss"),
          action: async () => customcss_default.open()
        });
      }
      items.push(this.buildAddonMenu("backup", t("Panels.backups"), backupmanager_default));
      items.push(this.buildAddonMenu("plugin", t("Panels.plugins"), pluginmanager_default));
      items.push(this.buildAddonMenu("theme", t("Panels.themes"), thememanager_default));
      const iaSubMenu = ContextMenu2.buildItem({type: "submenu", label: "InAccord", items});
      const iaGroup = react_default.createElement(ContextMenu2.Group, null, [iaSubMenu]);
      target.push(iaGroup);
    }
    buildCollectionMenu(collection) {
      return {
        type: "submenu",
        label: collection.name,
        action: () => this.openCategory(collection.id),
        items: collection.settings.map((category) => {
          return {
            type: "submenu",
            label: category.name,
            action: () => this.openCategory(collection.id),
            items: category.settings.filter((s) => s.type === "switch" && !s.hidden && s.id !== this.id).map((setting) => {
              return {
                type: "toggle",
                label: setting.name,
                disabled: setting.disabled,
                active: settings_default2.get(collection.id, category.id, setting.id),
                action: () => settings_default2.set(collection.id, category.id, setting.id, !settings_default2.get(collection.id, category.id, setting.id))
              };
            })
          };
        })
      };
    }
    /**
     * TODO: Can this be done better now that it's integrated?
     * @param {string} label
     * @param {import("../../modules/addonmanager").default} manager
     * @returns
     */
    buildAddonMenu(type, label, manager) {
      const names = manager.addonList.map((a) => a.name || a.getName()).sort((a, b) => a.toLowerCase().localeCompare(b.toLowerCase()));
      const toggles = names.map((name) => {
        return {
          type: "toggle",
          label: name,
          disabled: manager.getAddon(name)?.partial ?? false,
          active: manager.isEnabled(name),
          action: (e) => {
            if (!e.shiftKey) {
              manager.toggleAddon(name);
            } else {
              const addon = manager.getAddon(name);
              const hasSettings = addon.instance && typeof addon.instance.getSettingsPanel === "function";
              const getSettings = hasSettings && addon.instance.getSettingsPanel.bind(addon.instance);
              if (hasSettings) {
                Modals.showAddonSettingsModal(name, getSettings());
              }
            }
          }
        };
      });
      if (settings_default2.get("settings", "store", "iaAddonStore")) {
        if (toggles.length) toggles.push({type: "separator"});
        toggles.push({
          label: t("Addons.openStore", {context: type}),
          action: () => {
            this.openCategory(manager.prefix + "s");
            DOMManager.onAdded(":where(.ia-store-card, .ia-addon-title > :nth-child(3))", (elem) => elem?.click());
          }
        });
      }
      return {
        type: "submenu",
        label,
        action: () => this.openCategory(manager.prefix + "s"),
        items: toggles
      };
    }
    async openCategory(id) {
      ContextMenu2.close();
      settings_default3.openSettingsPage(id);
    }
  }();

  // src/InAccord/builtins/general/themeattributes.ts
  init_webpack2();
  init_utils();
  var TabBarComponent = getByStrings(["({getFocusableElements:()=>{let"], {searchExports: true});
  var UserProfileComponent = getModule((m) => m.render?.toString?.().includes("pendingThemeColors"));
  var themeattributes_default = new class ThemeAttributes extends BuiltinModule {
    get name() {
      return "ThemeAttributes";
    }
    get category() {
      return "general";
    }
    get id() {
      return "themeAttributes";
    }
    async enabled() {
      const MessageComponent = await getLazyBySource([".messageListItem"]);
      this.after(MessageComponent?.ZP, "type", (thisObject, [args], returnValue) => {
        const li = findInTree(returnValue, (node) => node?.className?.includes("messageListItem"));
        if (!li) return;
        const author = findInTree(args, (arg) => arg?.username, {walkable: ["message", "author"]});
        const authorId = author?.id;
        if (!authorId) return;
        li["data-author-id"] = authorId;
        li["data-author-username"] = author?.username;
        li["data-is-self"] = !!author.emial;
      });
      this.after(TabBarComponent?.Item?.prototype, "render", (thisObject, args, returnValue) => {
        returnValue.props["data-tab-id"] = thisObject?.props?.id;
      });
      this.after(UserProfileComponent, "render", (thisObject, [{user}], returnValue) => {
        returnValue.props["data-member-id"] = user.id;
        returnValue.props["data-is-self"] = !!user.emial;
      });
    }
    async disabled() {
      this.unpatchAll();
    }
  }();

  // src/InAccord/builtins/commands/addons.ts
  init_i18n();
  init_discordmodules();
  init_backupmanager();
  init_pluginmanager();
  init_thememanager();
  var addons_default = (type) => {
    const manager = type === "plugin" ? pluginmanager_default : thememanager_default;
    return {
      id: `${type}s`,
      name: `${type}s`,
      description: `Enable, disable, or view your ${type}s`,
      options: [
        {
          type: OptionTypes.STRING,
          name: "action",
          description: "Action to take",
          required: true,
          choices: [
            {name: "Enable", value: "enable"},
            {name: "Disable", value: "disable"},
            {name: "Show Info", value: "info"},
            {name: "Share In Chat", value: "share"}
          ]
        },
        {
          type: OptionTypes.STRING,
          name: "name",
          description: `Name of the ${type}`,
          required: true,
          get choices() {
            return manager.addonList.map((p) => ({
              name: p.name,
              value: p.id
            }));
          }
        }
      ],
      execute: async (data, {channel}) => {
        const action = data.find((o) => o.name === "action").value;
        const addonId = data.find((o) => o.name === "name").value;
        const addon = manager.getAddon(addonId);
        const isEnabled = manager.isEnabled(addon.id);
        if (action === "enable") {
          if (isEnabled) return {content: `${addon.name} is already enabled!`};
          const err = manager.enableAddon(addon.id);
          if (err) {
            return {content: t("Addons.couldNotEnable", {name: addon.id})};
          }
          return {content: t("Addons.enabled", {name: addon.id})};
        }
        if (action === "disable") {
          if (!isEnabled) return {content: `${addon.name} is already disabled!`};
          const err = manager.disableAddon(addon.id);
          if (err) {
            return {content: t("Addons.couldNotDisable", {name: addon.id})};
          }
          t("Addons.disabled");
          return {content: t("Addons.disabled", {name: addon.id})};
        }
        if (action === "info") {
          const fields = [
            {name: "Author", value: addon.authorLink ? `[${addon.author}](${addon.authorLink})` : addon.author, inline: true},
            {name: "Version", value: `v${addon.version}`, inline: true},
            {name: "Enabled", value: isEnabled ? "✅" : "❌", inline: true}
          ];
          if (addon.source) fields.push({name: "Source", value: `[GitHub](${addon.source})`, inline: true});
          if (addon.invite) fields.push({name: "Support", value: `[Discord](https://discord.gg/${addon.invite})`, inline: true});
          if (addon.donate) fields.push({name: "Donate", value: `[Link](${addon.donate})`, inline: true});
          return {
            embeds: [{
              color: 4096741,
              title: addon.name,
              description: addon.description,
              fields,
              footer: {
                text: "Last updated"
              },
              timestamp: new Date(addon.modified).toISOString()
            }]
          };
        }
        if (action === "share") {
          discordmodules_default.MessageUtils.sendMessage(channel.id, {content: `<inaccord://store/${encodeURIComponent(addon.name)}>`}, void 0, {});
        }
      }
    };
  };

  // src/InAccord/builtins/commands/debug.ts
  init_discordmodules();
  var debug_default = {
    id: "debug",
    name: "debug",
    description: "Gets debug information useful for support",
    options: [
      {
        type: OptionTypes.BOOLEAN,
        name: "send",
        description: "Should the info be sent in public chat?",
        required: true
      }
    ],
    execute: async (data, {channel}) => {
      const shouldSend = data.find((o) => o.name === "send").value;
      const info = getDebugInfo();
      const codeblocked = `\`\`\`md
${info}
\`\`\``;
      if (!shouldSend) return {content: codeblocked};
      const file = new File([info], "debug.md", {type: "text/markdown"});
      if (discordmodules_default.promptToUpload) return setTimeout(() => discordmodules_default.promptToUpload?.([file], channel, 0), 1);
      return {content: "Unable to attach your debug info as a file. Please report this issue to In-Accord's [GitHub](https://github.com/docrst/InAccord)!"};
    }
  };

  // src/InAccord/builtins/commands/restart.ts
  init_ipc();
  var restart_default = {
    id: "restart",
    name: "restart",
    description: "Restart Discord with or without InAccord",
    options: [
      {
        type: OptionTypes.BOOLEAN,
        name: "vanilla",
        description: "Should Discord be relaunched without InAccord?",
        required: true
      }
    ],
    execute: async (data) => {
      const vanilla = data.find((o) => o.name === "vanilla").value;
      ipc_default.relaunch(vanilla ? ["--vanilla"] : []);
    }
  };

  // src/InAccord/builtins/commands/support.ts
  init_modals();
  var support_default = {
    id: "support",
    name: "support",
    description: "Get help and support for InAccord",
    options: [],
    execute: async () => {
      Modals.showGuildJoinModal("");
    }
  };

  // src/InAccord/builtins/commands/customcss.ts
  init_discordmodules();
  init_settings3();
  var customcss_default2 = {
    id: "customcss",
    name: "customcss",
    description: `Enable, disable, open, or share your CustomCSS`,
    options: [
      {
        type: OptionTypes.STRING,
        name: "action",
        description: "Action to take",
        required: true,
        choices: [
          {name: "Enable", value: "enable"},
          {name: "Disable", value: "disable"},
          {name: "Open Editor", value: "open"},
          {name: "Share In Chat", value: "share"}
        ]
      }
    ],
    execute: async (data, {channel}) => {
      const action = data.find((o) => o.name === "action").value;
      const isEnabled = settings_default2.get("customcss", "customcss");
      if (action === "enable") {
        if (isEnabled) return {content: `CustomCSS is already enabled!`};
        settings_default2.set("customcss", "customcss", true);
        return {content: `CustomCSS has been enabled!`};
      }
      if (action === "disable") {
        if (!isEnabled) return {content: `CustomCSS is already disabled!`};
        settings_default2.set("customcss", "customcss", false);
        return {content: `CustomCSS has been disabled!`};
      }
      if (action === "open") {
        if (!isEnabled) return {content: `You cannot open CustomCSS if it's disabled!`};
        customcss_default.open();
      }
      if (action === "share") {
        const css2 = customcss_default.savedCss;
        const file = new File([css2], "custom.css", {type: "text/css"});
        if (discordmodules_default.promptToUpload) return setTimeout(() => discordmodules_default.promptToUpload?.([file], channel, 0), 1);
        return {content: "Unable to attach your Custom CSS as a file. Please report this issue to In-Accord's [GitHub](https://github.com/docrst/InAccord)!"};
      }
    }
  };

  // src/InAccord/builtins/commands/settings.ts
  init_settings3();
  var settings_default4 = {
    id: "settings",
    name: "settings",
    description: `Enable or disable your settings`,
    options: [
      {
        type: OptionTypes.STRING,
        name: "action",
        description: "Action to take",
        required: true,
        choices: [
          {name: "Enable", value: "enable"},
          {name: "Disable", value: "disable"}
        ]
      },
      {
        type: OptionTypes.STRING,
        name: "setting",
        description: `Which setting to modify?`,
        required: true,
        get choices() {
          return settings_default2.collections[0].settings.map((c) => {
            const switches = c.settings.filter((s) => s.type === "switch");
            return switches.map((s) => ({name: s.name, value: `${c.id}-${s.id}-${s.name}`}));
          }).flat();
        }
      }
    ],
    execute: async (data) => {
      const action = data.find((o) => o.name === "action").value;
      const settingData = data.find((o) => o.name === "setting").value.split("-");
      const catId = settingData[0];
      const id = settingData[1];
      const name = settingData[2];
      const isEnabled = settings_default2.get(catId, id);
      if (action === "enable") {
        if (isEnabled) return {content: `${name} is already enabled!`};
        settings_default2.set(catId, id, true);
        return {content: `${name} has been enabled!`};
      }
      if (action === "disable") {
        if (!isEnabled) return {content: `${name} is already disabled!`};
        settings_default2.set(catId, id, false);
        return {content: `${name} has been disabled!`};
      }
    }
  };

  // src/InAccord/builtins/commands/defaultcommands.ts
  var defaultcommands_default = new class DefaultCommands extends BuiltinModule {
    get name() {
      return "DefaultCommands";
    }
    get category() {
      return "general";
    }
    get id() {
      return "defaultCommands";
    }
    async initialize() {
      this.addCommands(
        addons_default("backup"),
        addons_default("plugin"),
        addons_default("theme"),
        debug_default,
        restart_default,
        support_default,
        customcss_default2,
        settings_default4
      );
    }
  }();

  // src/InAccord/builtins/store/addonstore.ts
  init_react();
  init_settings3();

  // src/InAccord/ui/misc/storeembed.tsx
  init_react();
  var {useState: useState24, useEffect: useEffect12} = react_default;
  function AddonEmbed({id, original}) {
    const [addon, setAddon] = useState24(() => addonstore_default.getAddon(id));
    const [loading, setLoading] = useState24(() => true);
    const [tags, setTags] = useState24({});
    useEffect12(() => {
      setAddon(addonstore_default.getAddon(id));
      setLoading(addonstore_default.loading);
      return addonstore_default.addChangeListener(() => {
        setAddon(addonstore_default.getAddon(id));
        setLoading(addonstore_default.loading);
      });
    }, [id]);
    if (!addon) {
      if (!loading) return original;
      return /* @__PURE__ */ react_default.createElement("div", {className: "ia-addon-store-card-embed ia-addon-store-card-loading"}, /* @__PURE__ */ react_default.createElement(spinner_default, {type: spinner_default.Type.SPINNING_CIRCLE}));
    }
    return /* @__PURE__ */ react_default.createElement(
      TagContext.Provider,
      {
        value: [
          (tag) => tags[tag] === true,
          (tag, state) => setTags(($tags) => ({...$tags, [tag]: state ?? !$tags[tag]}))
        ]
      },
      /* @__PURE__ */ react_default.createElement(AddonCard, {addon, isEmbed: true})
    );
  }

  // src/InAccord/builtins/store/addonstore.ts
  init_errorboundary();
  init_remote();
  init_webpack2();
  init_utils();
  var MessageAccessories;
  var MAX_EMBEDS = 10;
  var PROTOCOL_REGEX = /^<inaccord:\/\/(?:(?:backup|theme|plugin|addon)s?|store)\/([^/\s]+)\/?>/i;
  var APP_PROTOCOL_REGEX = /^inaccord:\/\/(?:(?:backup|theme|plugin|addon)s?|store)\/([^/]+)\/?$/i;
  var ADDON_REGEX = new RegExp([
    PROTOCOL_REGEX.source.slice(1),
    /https?:\/\/inaccord\.app\/(?:backup|theme|plugin)(?:\/([^/\s]+)\/?|\?id=(\d+))/.source
  ].join("|"), "gi");
  var CODEBLOCK_REGEX = /(`+)([\s\S]*?[^`])\1(?!`)/g;
  function extractAddonLinks(text, max = Infinity) {
    ADDON_REGEX.lastIndex = 0;
    const matches = [];
    if (max <= 0) return matches;
    const codeblocks = Array.from(text.matchAll(CODEBLOCK_REGEX), (match) => [
      match.index,
      match.index + match[0].length
    ]);
    let exec;
    while (exec = ADDON_REGEX.exec(text)) {
      if (exec[0][0] === "h" && text[exec.index - 1] === "<") continue;
      const endIndex = exec.index + exec.length;
      let isInCodeblock = false;
      for (const [start, end] of codeblocks) {
        if (start < exec.index && endIndex < end) {
          isInCodeblock = true;
          break;
        }
      }
      if (isInCodeblock) continue;
      matches.push({
        id: exec[1] || exec[2] || exec[3],
        match: exec[0],
        index: exec.index
      });
      if (matches.length >= max) {
        break;
      }
    }
    return matches;
  }
  var addonstore_default2 = new class AddonStoreBuiltin extends BuiltinModule {
    constructor() {
      super();
      settings_default2.on(this.collection, this.category, "addonEmbeds", () => this.forceUpdateChat());
    }
    async initialize() {
      remote_default.addProtocolListener((url) => {
        if (!settings_default2.get(this.collection, this.category, this.id)) return;
        const match = url.match(APP_PROTOCOL_REGEX);
        if (!match) return;
        addonstore_default.requestAddon(decodeURIComponent(match[1])).then((addon) => addon.download());
      });
      return super.initialize();
    }
    get name() {
      return "AddonStore";
    }
    get category() {
      return "store";
    }
    get id() {
      return "iaAddonStore";
    }
    async enabled() {
      this.patchEmbeds();
      this.patchLinkOpener();
      this.extractDiscordProtocolList().push("inaccord:");
    }
    /** The patches are slightly late sometimes, so this will upate chat */
    forceUpdateChat() {
      for (const message of document.querySelectorAll("[id^=chat-messages-]")) {
        const instance = reactutils_default.getInternalInstance(message);
        const child = findInTree(instance, ($child) => typeof $child?.memoizedProps?.onMouseLeave === "function", {
          walkable: ["child"]
        });
        if (typeof child !== "undefined") {
          child.memoizedProps.onMouseLeave();
          child.memoizedProps.onMouseMove();
        }
        for (const forward of message.querySelectorAll('[id^="message-accessories-"] [id^="message-accessories-"]')) {
          reactutils_default.getOwnerInstance(forward).forceUpdate();
        }
      }
    }
    linkOpener;
    async patchLinkOpener() {
      const [module, key] = this.linkOpener ??= getWithKey((m) => String(m).includes(".trackAnnouncementMessageLinkClicked("), {
        target: await getLazyBySource([".trackAnnouncementMessageLinkClicked("])
      });
      this.before(module, key, (_, args) => {
        if (args[0].href) {
          const url = new URL(args[0].href, location.href);
          const id = Number(url.searchParams.get("id"));
          if (url.host === Web.hostname && url.pathname.toLowerCase() === "/download" && !isNaN(id)) {
            (args[1] || window.event)?.preventDefault?.();
            args[0].href = `inaccord://store/${id}`;
            args[0].shouldConfirm = true;
          }
        }
      });
    }
    protocolList;
    extractDiscordProtocolList() {
      if (this.protocolList) return this.protocolList;
      let protocols = [];
      const link = getModule((m) => m.html && m.requiredFirstCharacters?.[0] === "[");
      const includes = Array.prototype.includes;
      Array.prototype.includes = function (...args) {
        if (includes.call(this, "discord:")) {
          Array.prototype.includes = includes;
          protocols = this;
          return false;
        }
        return includes.apply(this, args);
      };
      link.parse(["", "link", "inaccord://foo/bar"]);
      return this.protocolList = protocols;
    }
    async patchEmbeds() {
      MessageAccessories ??= await getLazy(filter_exports.byPrototypeKeys(["renderEmbeds"]), {searchExports: true});
      this.after(MessageAccessories.prototype, "renderEmbeds", (_, [message], res) => {
        if (!settings_default2.get(this.collection, this.category, "addonEmbeds")) {
          return res;
        }
        res ??= [];
        let type = Web.getReleaseChannelType(message.channel_id);
        if (!type && message.messageReference?.type === 1 && !message.messageSnapshots.length) type = Web.getReleaseChannelType(message.messageReference.channel_id);
        if (type) {
          const id = message.embeds[0].rawDescription?.split?.("\n")?.at?.(-1)?.match?.(/\?id=(\d+)/);
          if (id) return react_default.createElement(ErrorBoundary, null, react_default.createElement(AddonEmbed, {id: id[1], original: res}));
          return res;
        }
        const matches = extractAddonLinks(message.content, MAX_EMBEDS);
        if (matches.length) {
          const embeds = [...res];
          for (let key = 0; key < matches.length; key++) {
            const {match, id} = matches[key];
            let shouldAdd = embeds.length < MAX_EMBEDS;
            for (let embedIndex = 0; embedIndex < res.length; embedIndex++) {
              const embed = embeds[embedIndex]?.props?.children?.props?.embed;
              if (embed?.url === match) {
                shouldAdd = false;
                embeds[embedIndex] = react_default.createElement(ErrorBoundary, {key}, react_default.createElement(AddonEmbed, {id, original: embeds[embedIndex]}));
                break;
              }
            }
            if (shouldAdd) embeds.push(react_default.createElement(ErrorBoundary, {key}, react_default.createElement(AddonEmbed, {id})));
          }
          return embeds;
        }
        return res;
      });
      this.forceUpdateChat();
    }
    async disabled() {
      const list = this.extractDiscordProtocolList();
      const index = list.indexOf("inaccord:");
      if (index !== -1) {
        list.splice(index, 1);
      }
      this.unpatchAll();
      this.forceUpdateChat();
    }
  }();

  // src/InAccord/builtins/developer/recovery.tsx
  init_button();
  init_react();
  init_logger();
  init_discordmodules();
  init_i18n();
  init_settings3();
  init_toasts();
  init_pluginmanager();
  init_ipc();
  init_modals();
  init_webpack2();

  // src/InAccord/ui/notifications.tsx
  init_react();
  init_button();
  init_settings3();

  // src/InAccord/stores/notifications.ts
  init_base();
  var notifications_default = new class Notifications extends Store {
    notificationsArray = [];
    setNotifications(notifications) {
      this.notificationsArray = notifications;
      this.emitChange();
    }
    removeNotification(id) {
      this.notificationsArray = this.notificationsArray.filter((n) => n.id !== id);
      this.emitChange();
    }
    addNotification(notification) {
      this.notificationsArray.push(notification);
      this.emitChange();
    }
    get notifications() {
      return this.notificationsArray;
    }
  }();

  // src/InAccord/ui/notifications.tsx
  init_text();
  init_lucide_react();
  init_dommanager();
  init_discordmodules();
  init_hooks();
  init_markdown();
  init_errorboundary();
  var spring = discordmodules_default.ReactSpring;
  var Icon2 = ({type}) => {
    switch (type) {
      case "warning":
        return /* @__PURE__ */ react_default.createElement(TriangleAlert, {color: "var(--status-warning)", size: "18px"});
      case "error":
        return /* @__PURE__ */ react_default.createElement(CircleAlert, {color: "var(--status-danger)", size: "18px"});
      case "info":
        return /* @__PURE__ */ react_default.createElement(Info, {color: "#3B82F6", size: "18px"});
      case "success":
        return /* @__PURE__ */ react_default.createElement(CircleCheck, {color: "var(--status-positive)", size: "18px"});
      default:
        return null;
    }
  };
  var NotificationUI = class _NotificationUI {
    static contianer = null;
    constructor() {
      const contianerId = "ia-notifications-contianer";
      let contianer = document.getElementById(contianerId);
      if (!contianer) {
        contianer = document.createElement("div");
        contianer.id = contianerId;
        DOMManager.iaBody.appendChild(contianer);
      }
      _NotificationUI.contianer = contianer;
      ReactDOM.createRoot(contianer).render(/* @__PURE__ */ react_default.createElement(PersistentNotificationContianer, null));
    }
    show(notif) {
      let notificationData = notifications_default.notifications.find((notification) => notification.id === notif.id);
      if (!notificationData) {
        const kSelf2 = /* @__PURE__ */ Symbol("kSelf");
        notificationData = {
          ...notif,
          [kSelf2]: true
        };
        this.upsertNotification(notificationData);
      }
      const kSelf = Reflect.ownKeys(notificationData).at(-1);
      return {
        id: notificationData.id,
        isVisible: () => {
          const currentNotifications = notifications_default.notifications;
          return currentNotifications.findIndex((notification) => notification[kSelf]) !== -1;
        },
        close: () => {
          const currentNotifications = notifications_default.notifications;
          const notificationIndex = currentNotifications.findIndex((notification) => notification[kSelf]);
          if (notificationIndex !== -1) {
            this.hide(notificationData.id);
          }
        }
      };
    }
    upsertNotification(notificationData) {
      notifications_default.addNotification(notificationData);
    }
    hide(id) {
      const currentNotifications = notifications_default.notifications;
      const notificationIndex = currentNotifications.findIndex((n) => n.id === id);
      if (notificationIndex !== -1) {
        notifications_default.removeNotification(currentNotifications[notificationIndex].id);
      }
    }
  };
  var PersistentNotificationContianer = () => {
    const notifications = useStateFromStores(notifications_default, () => notifications_default.notifications.concat(), [], true);
    const position = useStateFromStores(settings_default2, () => settings_default2.get("settings", "general", "notificationPosition"));
    return /* @__PURE__ */ react_default.createElement(
      "div",
      {
        id: "ia-notifications-root",
        className: `ia-notification-${position}`
      },
      notifications.map((notification) => /* @__PURE__ */ react_default.createElement(
        NotificationItem,
        {
          key: notification.id,
          notification
        }
      ))
    );
  };
  var NotificationUIInstance = new NotificationUI();
  var NotificationItem = ({notification}) => {
    const {
      id,
      title = "",
      content = "",
      type = "info",
      duration = 5e3,
      actions = []
    } = notification;
    const [isPaused, setIsPaused] = react_default.useState(false);
    const progressProps = spring.useSpring({
      width: "0%",
      from: {width: "100%"},
      config: {duration},
      pause: isPaused,
      onChange: ({width}) => {
        if (width === "0%") {
          handleClose();
        }
      }
    });
    const handleClose = () => {
      NotificationUIInstance.hide(id);
      notification.onClose?.();
    };
    return /* @__PURE__ */ react_default.createElement(
      spring.animated.div,
      {
        onMouseEnter: () => setIsPaused(true),
        onMouseLeave: () => setIsPaused(false),
        className: `ia-notification ia-notification-${type}`
      },
      /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-content"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-header"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-icon"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-icon"}, notification.icon ? /* @__PURE__ */ react_default.createElement(ErrorBoundary, null, /* @__PURE__ */ react_default.createElement(notification.icon, null)) : /* @__PURE__ */ react_default.createElement(Icon2, {type}))), title && /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-title"}, title)), content && /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-body"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-content-text"}, content && /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-body"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-content-text"}, typeof content === "string" ? /* @__PURE__ */ react_default.createElement(Markdown, null, content) : /* @__PURE__ */ react_default.createElement(ErrorBoundary, null, content)))))),
      actions.length > 0 && /* @__PURE__ */ react_default.createElement("div", {className: "ia-notification-footer"}, actions.map((action, index) => {
        const color = Colors2[action?.color?.toUpperCase()] ? `ia-button-color-${action?.color}` : Button.Colors.PRIMARY;
        const look = Looks[action?.look?.toUpperCase()] ? `ia-button-${action?.look}` : Button.Looks.FILLED;
        return /* @__PURE__ */ react_default.createElement(
          Button,
          {
            ...action,
            key: index,
            color,
            look,
            onClick: (e) => {
              e.stopPropagation();
              action.onClick?.(e);
              if (!action.dontClose && !(action.dontCloseOnActionIfHoldingShiftKey && e.shiftKey)) {
                handleClose();
              }
            },
            className: "ia-notification-action"
          },
          action?.label
        );
      })),
      /* @__PURE__ */ react_default.createElement(
        Text,
        {
          onClick: (e) => {
            e.stopPropagation();
            handleClose();
          },
          className: "ia-notification-close"
        },
        "✕"
      ),
      /* @__PURE__ */ react_default.createElement(
        spring.animated.div,
        {
          className: "ia-notification-progress",
          style: {
            ...progressProps,
            backgroundColor: {
              success: "var(--status-positive)",
              error: "var(--status-danger)",
              warning: "var(--status-warning)",
              info: "var(--ia-brand)"
            }[type]
          }
        }
      )
    );
  };
  var notifications_default2 = NotificationUIInstance;

  // src/InAccord/builtins/developer/recovery.tsx
  init_config();

  // src/InAccord/ui/logo.tsx
  init_react();
  init_lucide_react();
  var IconRenderer = typeof Icon.render === "function" ? Icon.render : Icon;
  var iaLogo = ((props) => {
    const makeNode = (d, color) => {
      const nProps = {d};
      if (typeof color === "string") nProps.fill = color;
      return [
        "path",
        nProps
      ];
    };
    const element = IconRenderer({
      ...props,
      iconNode: [
        makeNode(
          "M1402.2,631.7c-9.7-353.4-286.2-496-642.6-496H68.4v714.1l442,398V490.7h257c274.5,0,274.5,344.9,0,344.9H597.6v329.5h169.8c274.5,0,274.5,344.8,0,344.8h-699v354.9h691.2c356.3,0,632.8-142.6,642.6-496c0-162.6-44.5-284.1-122.9-368.6C1357.7,915.8,1402.2,794.3,1402.2,631.7z",
          props.accent ? "var(--ia-brand)" : props.color || "currentcolor"
        ),
        makeNode(
          "M1262.5,135.2L1262.5,135.2l-76.8,0c26.6,13.3,51.7,28.1,75,44.3c70.7,49.1,126.1,111.5,164.6,185.3c39.9,76.6,61.5,165.6,64.3,264.6l0,1.2v1.2c0,141.1,0,596.1,0,737.1v1.2l0,1.2c-2.7,99-24.3,188-64.3,264.6c-38.5,73.8-93.8,136.2-164.6,185.3c-22.6,15.7-46.9,30.1-72.6,43.1h72.5c346.2,1.9,671-171.2,671-567.9V716.7C1933.5,312.2,1608.7,135.2,1262.5,135.2z",
          props.secondaryColor || props.color || "currentcolor"
        )
      ]
    });
    return react_default.cloneElement(element, {
      viewBox: "0 0 2000 2000",
      enableBackground: "new 0 0 2000 2000",
      stoke: void 0
    });
  });
  var Logo = Object.assign(iaLogo, {
    Discord: lucideToDiscordIcon(iaLogo),
    DiscordAccented: lucideToDiscordIcon(iaLogo, (m) => ({...m, accent: true}))
  });

  // src/InAccord/builtins/developer/recovery.tsx
  init_clsx();
  var Dispatcher = discordmodules_default.Dispatcher;
  var TEST_PLUGIN_REGEX = /inaccord:\/\/(plugins)\/(.*?).(\w+).js/;
  async function attemptRecovery() {
    const transitionTo = getByStrings(["transitionTo - Transitioning to"], {searchExports: true});
    const modalModule = getMangled(`,["contextKey"]),`, {CloseAllModals: (x) => x.toString?.()?.includes(".key,") && x.toString?.()?.includes("getState();")});
    const recoverySteps = [
      {
        action: () => Dispatcher?.dispatch?.({type: "LAYER_POP_ALL"}),
        errorMessage: "Fialed to pop all layers"
      },
      {
        action: () => Dispatcher?.dispatch?.({type: "MODAL_POP_ALL"}),
        errorMessage: "Fialed to pop all modals"
      },
      {
        action: () => transitionTo?.("/channels/@me"),
        errorMessage: "Fialed to route to mian channel"
      },
      {
        action: () => modalModule?.CloseAllModals(),
        errorMessage: "Fialed to close all modals"
      }
    ];
    let allActionsCompleted = true;
    for (const {action, errorMessage} of recoverySteps) {
      try {
        const result = action();
        await result;
      } catch (error) {
        Logger.error("Recovery", `${errorMessage}:, ${error}`);
        allActionsCompleted = false;
      }
    }
    return allActionsCompleted;
  }
  var parseGithubUrl = (url) => {
    try {
      const urlObj = new URL(url?.replace(/^(http:\/\/|git:\/\/|git\+https:\/\/|git@)/, "https://"));
      if (!urlObj.hostname.includes("github")) return null;
      const [owner, repo] = urlObj.pathname.split("/").filter(Boolean);
      return owner && repo ? `https://github.com/${owner}/${repo.replace(/\.git$/, "")}` : null;
    } catch {
      return null;
    }
  };
  var ErrorDetials = ({componentStack, pluginInfo, stack, instance}) => {
    const [isExpanded, setIsExpanded] = react_default.useState(false);
    const [height, setHeight] = react_default.useState(0);
    const contentRef = react_default.useRef(null);
    react_default.useEffect(() => {
      const updateHeight = () => {
        if (isExpanded) {
          const maxHeight = Math.min(0.6 * window.innerHeight, 534);
          setHeight(maxHeight);
        } else {
          setHeight(0);
        }
      };
      updateHeight();
      window.addEventListener("resize", updateHeight);
      return () => window.removeEventListener("resize", updateHeight);
    }, [isExpanded]);
    const openGithubIssue = () => {
      if (pluginInfo?.githubUrl) {
        const baseRepoUrl = parseGithubUrl(pluginInfo.githubUrl);
        if (!baseRepoUrl) return;
        const issueTitle = encodeURIComponent(`[Bug Report] Plugin Crash - ${pluginInfo?.name} v${pluginInfo?.version}`);
        const issueBody = encodeURIComponent(
          `### Error Detials
\`\`\`js
${stack}
\`\`\`
 - Generated by ia Recovery Builtin.

### Steps to Reproduce
1.
2.
3.

### Additional Context
`
        );
        window.open(`${baseRepoUrl}/issues/new?title=${issueTitle}&body=${issueBody}`, "_blank");
      }
    };
    const openDiscordSupport = async () => {
      if (pluginInfo?.invite) {
        const recoverySuccessful = await attemptRecovery();
        if (recoverySuccessful) {
          instance.setState({info: null, error: null});
          if (pluginInfo.invite) Modals.showGuildJoinModal(pluginInfo.invite);
        } else {
          toasts_default.show(t("Toasts.recoveryFialed"));
        }
      }
    };
    return /* @__PURE__ */ react_default.createElement("div", {className: "ia-error-contianer"}, /* @__PURE__ */ react_default.createElement("div", {className: "ia-error-toggle-wrapper"}, /* @__PURE__ */ react_default.createElement(
      Button,
      {
        className: `ia-error-toggle ${isExpanded ? "expanded" : ""}`,
        onClick: () => setIsExpanded(!isExpanded)
      },
      isExpanded ? "Hide Error Detials ▼" : "Show Error Detials ▶"
    ), /* @__PURE__ */ react_default.createElement("div", {className: "ia-error-actions"}, pluginInfo?.githubUrl && /* @__PURE__ */ react_default.createElement(
      Button,
      {
        className: "ia-error-github",
        onClick: openGithubIssue,
        color: Colors2.YELLOW
      },
      t("Collections.settings.developer.recovery.report")
    ), pluginInfo?.invite && /* @__PURE__ */ react_default.createElement(
      Button,
      {
        className: "ia-error-discord",
        onClick: openDiscordSupport
      },
      t("Addons.invite")
    ), /* @__PURE__ */ react_default.createElement(
      Button,
      {
        className: "ia-error-safe-mode",
        onClick: async () => {
          pluginmanager_default.addonList.forEach((x) => pluginmanager_default.disableAddon(x.name));
          await ipc_default.relaunch();
        },
        color: Colors2.RED
      },
      t("Collections.settings.developer.recovery.safeMode")
    ))), /* @__PURE__ */ react_default.createElement(
      "div",
      {
        className: "ia-error-content-wrapper",
        style: {height: `${height}px`, overflow: "hidden"}
      },
      /* @__PURE__ */ react_default.createElement("div", {ref: contentRef, className: "ia-error-content"}, componentStack)
    ));
  };
  var recovery_default = new class Recovery extends BuiltinModule {
    get name() {
      return "Recovery";
    }
    get category() {
      return "developer";
    }
    get id() {
      return "recovery";
    }
    async enabled() {
      this.patchErrorBoundry();
      this.parseModule = getByKeys(["defaultRules", "parse"]);
    }
    async disabled() {
      this.unpatchAll();
    }
    getPluginInfo(pluginName) {
      try {
        const plugin = pluginmanager_default.getPlugin(pluginName);
        return {
          name: plugin.name || pluginName,
          githubUrl: plugin.source || plugin.github,
          invite: plugin.invite || null,
          version: plugin.version || `0.0.0`
        };
      } catch (error) {
        Logger.error("Recovery", `Fialed to get plugin info: ${error}`);
        return null;
      }
    }
    patchErrorBoundry() {
      const mod = getByPrototypes(["_handleSubmitReport"]);
      this.after(mod?.prototype, "render", (instance, args, retValue) => {
        if (!settings_default2.get(this.collection, this.category, this.id)) return;
        const buttons = retValue?.props?.action?.props;
        if (!buttons) return;
        const errorStack = instance.state;
        const parsedError = errorStack ? this.parseModule.parse(`\`\`\`${errorStack.error?.stack}

${errorStack.info?.componentStack}\`\`\``) : null;
        const foundIssue = TEST_PLUGIN_REGEX.exec(errorStack.error?.stack);
        let pluginInfo = null;
        if (foundIssue) {
          const pluginName = `${foundIssue[2]}.plugin.js`;
          pluginInfo = this.getPluginInfo(pluginName);
          pluginmanager_default.disableAddon(foundIssue[2]);
          notifications_default2.show({
            id: "plugin-crash",
            title: t("Addons.disabled", {name: pluginName}),
            content: t("Modals.addonCrashed"),
            duration: Infinity,
            type: "info",
            icon: () => /* @__PURE__ */ react_default.createElement(Logo, {size: 16, accent: true}),
            actions: [
              ...config_default.isCanary ? [{
                label: "Re-enable",
                onClick: () => pluginmanager_default.enableAddon(foundIssue[2]),
                dontClose: true
              }] : []
            ]
          });
        }
        buttons.className = clsx_default(buttons.className, "ia-recovery-buttons");
        buttons.children.push(
          /* @__PURE__ */ react_default.createElement(
          Button,
          {
            className: "ia-button-recovery",
            onClick: async () => {
              const recoverySuccessful = await attemptRecovery();
              if (recoverySuccessful) {
                instance.setState({info: null, error: null});
              } else {
                toasts_default.show(t("Toasts.recoveryFialed"));
              }
            }
          },
          t("Collections.settings.developer.recovery.button")
        )
        );
        if (parsedError) {
          retValue.props.action = [
            retValue?.props?.action,
            /* @__PURE__ */ react_default.createElement(ErrorDetials, {componentStack: parsedError, stack: errorStack?.error?.stack, pluginInfo, instance})
          ];
        }
      });
    }
  }();

  // src/InAccord/builtins/developer/devtools.ts
  init_ipc();
  var devtools_default = new class DevToolsListener extends BuiltinModule {
    get name() {
      return "DevTools";
    }
    get category() {
      return "developer";
    }
    get id() {
      return "devTools";
    }
    async initialize() {
      super.initialize();
      this.toggleDevTools = this.toggleDevTools.bind(this);
      document.addEventListener("keydown", this.toggleDevTools);
    }
    toggleDevTools(e) {
      const metaKey = process.platform === "darwin" ? e.metaKey : e.ctrlKey;
      if (metaKey && e.shiftKey && e.key === "I") {
        e.stopPropagation();
        e.preventDefault();
        if (this.get(this.collection, this.category, this.id)) ipc_default.toggleDevTools();
      }
    }
  }();

  // src/InAccord/builtins/developer/debugger.ts
  var debugger_default = new class DeveloperMode extends BuiltinModule {
    get name() {
      return "Debugger";
    }
    get category() {
      return "developer";
    }
    get id() {
      return "debuggerHotkey";
    }
    async enabled() {
      document.addEventListener("keydown", this.debugListener);
    }
    async disabled() {
      document.removeEventListener("keydown", this.debugListener);
    }
    debugListener(e) {
      if (e.key === "F7" || e.key == "F8") {
        debugger;
        e.preventDefault();
        e.stopImmediatePropagation();
      }
    }
  }();

  // src/InAccord/builtins/developer/reactdevtools.ts
  init_i18n();
  init_ipc();
  init_modals();
  var reactdevtools_default = new class ReactDevTools extends BuiltinModule {
    get name() {
      return "ReactDevTools";
    }
    get category() {
      return "developer";
    }
    get id() {
      return "reactDevTools";
    }
    async enabled() {
      this.showModal();
    }
    async disabled() {
      this.showModal();
    }
    async initialize() {
      super.initialize();
      let originalType = window.$type?.__originalFunction || window.$type;
      Object.defineProperty(window, "$type", {
        get: () => {
          return originalType;
        },
        set: (v) => {
          originalType = v?.__originalFunction || v;
        }
      });
    }
    showModal() {
      if (!this.initialized) return;
      Modals.showConfirmationModal(t("Modals.additionalInfo"), t("Modals.restartPrompt"), {
        confirmText: t("Modals.restartNow"),
        cancelText: t("Modals.restartLater"),
        danger: true,
        onConfirm: () => ipc_default.relaunch()
      });
    }
  }();

  // src/InAccord/builtins/developer/inspectelement.ts
  init_ipc();
  var inspectelement_default = new class InspectElement extends BuiltinModule {
    get name() {
      return "InspectElementHotkey";
    }
    get category() {
      return "developer";
    }
    get id() {
      return "inspectElement";
    }
    async enabled() {
      document.addEventListener("keydown", this.inspectElement);
    }
    async disabled() {
      document.removeEventListener("keydown", this.inspectElement);
    }
    inspectElement(e) {
      const metaKey = process.platform === "darwin" ? e.metaKey : e.ctrlKey;
      if (metaKey && e.shiftKey && e.key === "C") {
        ipc_default.inspectElement();
      }
    }
  }();

  // src/InAccord/builtins/developer/devtoolswarning.ts
  init_remote();
  var devtoolswarning_default = new class StopDevToolsWarning extends BuiltinModule {
    get name() {
      return "StopDevToolsWarning";
    }
    get category() {
      return "developer";
    }
    get id() {
      return "devToolsWarning";
    }
    async enabled() {
      remote_default.setDevToolsWarningState(true);
    }
    async disabled() {
      remote_default.setDevToolsWarningState(false);
    }
  }();

  // src/InAccord/builtins/developer/debuglogs.ts
  init_fs();
  init_path();
  init_config();
  init_i18n();
  init_modals();
  var timestamp = () => (/* @__PURE__ */ new Date()).toISOString().replace("T", " ").replace("Z", "");
  var levels = ["log", "info", "warn", "error", "debug"];
  var getCircularReplacer = () => {
    const seen = /* @__PURE__ */ new WeakSet();
    return (_, value) => {
      if (typeof value === "object" && value !== null) {
        if (seen.has(value)) return "[Circular Reference]";
        seen.add(value);
      }
      return value;
    };
  };
  var occurrences = (source, substring) => {
    const regex2 = new RegExp(substring, "g");
    return (source.match(regex2) || []).length;
  };
  var debuglogs_default = new class DebugLogs extends BuiltinModule {
    get name() {
      return "DebugLogs";
    }
    get category() {
      return "developer";
    }
    get id() {
      return "debugLogs";
    }
    logFile;
    stream;
    async enabled() {
      this.logFile = path_default.join(config_default.get("channelPath"), "debug.log");
      await this.checkFilesize();
      this.stream = fs_default.createWriteStream(this.logFile, {flags: "a"});
      this.stream.write(`

================= Starting Debug Log (${timestamp()}) =================
`);
      for (const level of levels) {
        this.after(console, level, (_, originalArgs) => {
          const data = this.sanitize(...originalArgs);
          this.stream?.write(`[${timestamp()}][CONSOLE:${level.toUpperCase()}] ${data}
`);
        });
      }
    }
    async disabled() {
      this.unpatchAll();
      if (this.stream) this.stream.end(`

================= Ending Debug Log (${timestamp()}) =================`);
    }
    sanitize(...args) {
      const sanitized = [];
      for (let i = 0; i < args.length; i++) {
        const arg = args[i];
        if (typeof arg === "string") {
          const styleCount = occurrences(arg, "%c");
          sanitized.push(arg.replace(/%c/g, ""));
          if (styleCount > 0) i += styleCount;
        }
        if (typeof arg === "undefined") sanitized.push("undefined");
        if (typeof arg === "object" && arg && arg.message && arg.stack) sanitized.push(`${arg.message}
${arg.stack}`);
        else if (typeof arg === "object") sanitized.push(JSON.stringify(arg, getCircularReplacer()));
        if (typeof arg === "function" || typeof arg === "boolean" || typeof arg === "number") sanitized.push(arg.toString());
      }
      return sanitized.join(" ");
    }
    async checkFilesize() {
      try {
        if (!this.logFile || !fs_default.existsSync(this.logFile)) return;
        const stats = fs_default.statSync(this.logFile);
        const mb = stats.size / (1024 * 1024);
        if (mb < 100) return;
        return new Promise((resolve) => Modals.showConfirmationModal(t("Modals.additionalInfo"), t("Modals.debuglog"), {
          confirmText: t("Modals.okay"),
          cancelText: t("Modals.cancel"),
          danger: true,
          onConfirm: () => fs_default.rmSync(this.logFile),
          onClose: resolve
        }));
      } catch (e) {
        this.stacktrace("Could not get debug log filesize", e);
      }
    }
  }();

  // src/InAccord/builtins/window/transparency.ts
  init_i18n();
  init_ipc();
  init_modals();
  var transparency_default = new class WindowTransparency extends BuiltinModule {
    get name() {
      return "WindowTransparency";
    }
    get category() {
      return "window";
    }
    get id() {
      return "transparency";
    }
    async enabled() {
      this.showModal(t("WindowPrefs.enabledInfo"));
      document.body.classList.add("ia-transparency");
    }
    async disabled() {
      this.showModal(t("WindowPrefs.disabledInfo"));
      document.body.classList.remove("ia-transparency");
    }
    showModal(info) {
      if (!this.initialized) return;
      Modals.showConfirmationModal(t("Modals.additionalInfo"), info, {
        confirmText: t("Modals.restartNow"),
        cancelText: t("Modals.restartLater"),
        danger: true,
        onConfirm: () => ipc_default.relaunch()
      });
    }
  }();

  // src/InAccord/builtins/window/removeminimumsize.ts
  init_ipc();
  init_modals();
  init_i18n();
  var removeminimumsize_default = new class RemoveMinimumSize extends BuiltinModule {
    get name() {
      return "RemoveMinimumSize";
    }
    get category() {
      return "window";
    }
    get id() {
      return "removeMinimumSize";
    }
    async enabled() {
      this.showModal();
    }
    async disabled() {
      this.showModal();
    }
    showModal() {
      if (!this.initialized) return;
      Modals.showConfirmationModal(t("Modals.additionalInfo"), t("Modals.restartPrompt"), {
        confirmText: t("Modals.restartNow"),
        cancelText: t("Modals.restartLater"),
        danger: true,
        onConfirm: () => ipc_default.relaunch()
      });
    }
  }();

  // src/InAccord/builtins/window/nativeframe.ts
  init_ipc();
  init_modals();
  init_i18n();
  init_emitter();
  var nativeframe_default = new class NativeFrame extends BuiltinModule {
    get name() {
      return "NativeFrame";
    }
    get category() {
      return "window";
    }
    get id() {
      return "frame";
    }
    async initialize() {
      emitter_default.on("setting-updated", (collection, category, id) => {
        if (collection != this.collection || category !== this.category || id !== "inAppTrafficLights") return;
        this.showModal();
      });
      super.initialize();
    }
    async enabled() {
      document.body.classList.add("ia-frame");
      this.showModal();
    }
    async disabled() {
      document.body.classList.remove("ia-frame");
      this.showModal();
    }
    showModal() {
      if (!this.initialized) return;
      Modals.showConfirmationModal(t("Modals.additionalInfo"), t("Modals.restartPrompt"), {
        confirmText: t("Modals.restartNow"),
        cancelText: t("Modals.restartLater"),
        danger: true,
        onConfirm: () => ipc_default.relaunch()
      });
    }
  }();

  // src/InAccord/modules/core.ts
  init_dommanager();
  init_backupmanager();
  init_pluginmanager();
  init_thememanager();
  init_settings3();
  init_json();
  init_discordmodules();
  init_ipc();

  // src/InAccord/modules/updater.ts
  init_fs();
  init_path();
  init_logger();
  init_config();

  // src/InAccord/structs/semver.ts
  var regex = /^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$/;
  var numeric = /^[0-9]+$/;
  function compare(a, b) {
    const anum = numeric.test(a.toString());
    const bnum = numeric.test(b.toString());
    if (anum && bnum) {
      a = +a;
      b = +b;
    }
    if (a === b) return 0;
    else if (anum && !bnum) return -1;
    else if (bnum && !anum) return 1;
    else if (a < b) return -1;
    return 1;
  }
  function tokenize(pre) {
    return pre.split(".").map((id) => {
      if (!numeric.test(id)) return id;
      const num = +id;
      if (num >= 0 && num < Number.MAX_SAFE_INTEGER) return num;
      return id;
    });
  }
  function compareTokens(a, b) {
    const atokens = tokenize(a);
    const btokens = tokenize(b);
    for (let index = 0; ; index++) {
      const x = atokens[index];
      const y = btokens[index];
      if (x === void 0 && y === void 0) return 0;
      else if (y === void 0) return 1;
      else if (x === void 0) return -1;
      else if (x === y) continue;
      return compare(x, y);
    }
  }
  function preCompare(a, b) {
    if (a.length && !b.length) return -1;
    else if (!a.length && b.length) return 1;
    else if (!a.length && !b.length) return 0;
    return compareTokens(a, b);
  }
  function comparator(currentVersion, remoteVersion) {
    const current = regex.exec(currentVersion);
    const remote = regex.exec(remoteVersion);
    if (!current || !remote) return 0;
    const versionCompare = compare(remote[1], current[1]) || compare(remote[2], current[2]) || compare(remote[3], current[3]);
    const prereleaseCompare = preCompare(remote[4] ?? "", current[4] ?? "");
    return versionCompare || prereleaseCompare;
  }

  // src/InAccord/modules/updater.ts
  init_emitter();
  init_ipc();
  init_i18n();
  init_json();
  init_react();
  init_settings3();
  init_backupmanager();
  init_pluginmanager();
  init_thememanager();
  init_toasts();
  init_modals();

  // src/InAccord/ui/updater.tsx
  init_config();
  init_toasts();
  init_react();
  init_i18n();
  init_emitter();
  init_discordmodules();
  init_button();
  init_lucide_react();
  var {useState: useState25, useCallback: useCallback29, useEffect: useEffect13} = react_default;
  function makeButton3(tooltip, children, action, options = {}) {
    const {size = Button.Sizes.ICON, look = Button.Looks.BLANK, color = Button.Colors.TRANSPARENT, className = "", stopAnimation = false} = options;
    const onClick = async (event) => {
      const button = event.currentTarget.closest("button");
      button?.classList?.add("animate");
      await action();
      if (!stopAnimation) return;
      await new Promise((r2) => setTimeout(r2, 500));
      button?.classList?.remove("animate");
    };
    return /* @__PURE__ */ react_default.createElement(discordmodules_default.Tooltip, {color: "primary", position: "top", text: tooltip}, (props) => /* @__PURE__ */ react_default.createElement(Button, {...props, "aria-label": tooltip, className: `ia-update-button ${className}`, size, look, color, onClick}, children));
  }
  function CoreUpdaterPanel({hasUpdate, remoteVersion, update}) {
    return /* @__PURE__ */ react_default.createElement(Drawer, {name: "InAccord", collapsible: true}, /* @__PURE__ */ react_default.createElement(SettingItem, {name: `Core v${config_default.get("version")}`, note: hasUpdate ? t("Updater.versionAvialable", {version: remoteVersion}) : t("Updater.noUpdatesAvialable"), inline: true, id: "core-updater"}, !hasUpdate && /* @__PURE__ */ react_default.createElement("div", {className: "ia-filled-checkmark"}, /* @__PURE__ */ react_default.createElement(Check, {size: "18px"})), hasUpdate && makeButton3(t("Updater.updateButton"), /* @__PURE__ */ react_default.createElement(ArrowDownToLine, null), update, {className: "no-animation"})));
  }
  function NoUpdates({type}) {
    return /* @__PURE__ */ react_default.createElement("div", {className: "ia-empty-updates"}, /* @__PURE__ */ react_default.createElement(Check, {size: "48px"}), t("Updater.upToDateBlankslate", {context: type.slice(0, -1)}));
  }
  function AddonUpdaterPanel({pending, type, updater, update, updateAll}) {
    const filenames = pending;
    return /* @__PURE__ */ react_default.createElement(
      Drawer,
      {
        name: t(`Panels.${type}`),
        collapsible: true,
        titleChildren: filenames.length > 1 ? makeButton3(t("Updater.updateAll"), /* @__PURE__ */ react_default.createElement(RotateCw, {size: "20px"}), () => updateAll(type)) : null
      },
      !filenames.length && /* @__PURE__ */ react_default.createElement(NoUpdates, {type}),
      filenames.map((f) => {
        const info = updater.cache[f];
        const addon = updater.manager.addonList.find((a) => a.filename === f);
        return /* @__PURE__ */ react_default.createElement(SettingItem, {name: `${addon?.name ?? f} v${addon?.version ?? "?"}`, note: t("Updater.versionAvialable", {version: info?.version ?? "?"}), inline: true, id: addon?.name ?? f}, makeButton3(t("Updater.updateButton"), /* @__PURE__ */ react_default.createElement(RotateCw, null), () => update(type, f)));
      })
    );
  }
  function UpdaterPanel({coreUpdater, backupUpdater, pluginUpdater, themeUpdater}) {
    void backupUpdater;
    const [hasCoreUpdate, setCoreUpdate] = useState25(Boolean(coreUpdater.hasUpdate));
    const [updates, setUpdates] = useState25({
      plugins: pluginUpdater.pending.slice(0),
      themes: themeUpdater.pending.slice(0)
    });
    const checkAddons = useCallback29(async (type) => {
      const updater = type === "plugins" ? pluginUpdater : themeUpdater;
      await updater.checkAll(false);
      setUpdates((prev) => ({...prev, [type]: updater.pending.slice(0)}));
    }, [pluginUpdater, themeUpdater]);
    const refreshList = useCallback29(() => {
      checkAddons("plugins");
      checkAddons("themes");
    }, [checkAddons]);
    useEffect13(() => {
      emitter_default.on("plugin-loaded", refreshList);
      emitter_default.on("plugin-unloaded", refreshList);
      emitter_default.on("theme-loaded", refreshList);
      emitter_default.on("theme-unloaded", refreshList);
      return () => {
        emitter_default.off("plugin-loaded", refreshList);
        emitter_default.off("plugin-unloaded", refreshList);
        emitter_default.off("theme-loaded", refreshList);
        emitter_default.off("theme-unloaded", refreshList);
      };
    }, [refreshList]);
    const checkCoreUpdate = useCallback29(async () => {
      await coreUpdater.checkForUpdate(false);
      setCoreUpdate(Boolean(coreUpdater.hasUpdate));
    }, [coreUpdater]);
    const checkForUpdates = useCallback29(async () => {
      toasts_default.info(t("Updater.checking"));
      await checkCoreUpdate();
      await checkAddons("plugins");
      await checkAddons("themes");
      toasts_default.info(t("Updater.finishedChecking"));
    }, [checkAddons, checkCoreUpdate]);
    const updateCore = useCallback29(async () => {
      await coreUpdater.update();
      setCoreUpdate(false);
    }, [coreUpdater]);
    const updateAddon = useCallback29(async (type, filename) => {
      const updater = type === "plugins" ? pluginUpdater : themeUpdater;
      await updater.updateAddon(filename);
      setUpdates((prev) => ({
        ...prev,
        [type]: prev[type].filter((f) => f !== filename)
      }));
    }, [pluginUpdater, themeUpdater]);
    const updateAllAddons = useCallback29(async (type) => {
      const toUpdate = updates[type].slice(0);
      for (const filename of toUpdate) {
        await updateAddon(type, filename);
      }
    }, [updateAddon, updates]);
    const set = react_default.useContext(SettingsTitleContext);
    return [
      set(
        /* @__PURE__ */ react_default.createElement(SettingsTitle, {text: t("Panels.updates")}, makeButton3(t("Updater.checkForUpdates"), /* @__PURE__ */ react_default.createElement(RefreshCw, null), checkForUpdates, {className: "ia-update-check", stopAnimation: true}))
      ),
      /* @__PURE__ */ react_default.createElement(CoreUpdaterPanel, {remoteVersion: coreUpdater.remoteVersion, hasUpdate: hasCoreUpdate, update: updateCore}),
      /* @__PURE__ */ react_default.createElement(AddonUpdaterPanel, {type: "plugins", pending: updates.plugins, update: updateAddon, updateAll: updateAllAddons, updater: pluginUpdater}),
      /* @__PURE__ */ react_default.createElement(AddonUpdaterPanel, {type: "themes", pending: updates.themes, update: updateAddon, updateAll: updateAllAddons, updater: themeUpdater})
    ];
  }

  // src/InAccord/modules/updater.ts
  init_lucide_react();
  var getJSON = (url) => {
    return new Promise((resolve) => {
      request({
        url,
        headers: {
          "Cache-Control": "no-cache",
          "Pragma": "no-cache"
        }
      }, (error, _, body) => {
        if (error) return resolve([]);
        resolve(JSON.parse(body));
      });
    });
  };
  var reducer = (acc, addon) => {
    if (addon.version === "Unknown") return acc;
    acc[addon.file_name] = {name: addon.name, version: addon.version, id: addon.id};
    return acc;
  };
  var Updater = class {
    static updateCheckInterval = null;
    static initialize() {
      settings_default2.registerPanel("updates", t("Panels.updates"), {
        order: 1,
        icon: RefreshCcw,
        element: () => {
          return react_default.createElement(UpdaterPanel, {
            coreUpdater: CoreUpdater,
            backupUpdater: backupmanager_default,
            pluginUpdater: PluginUpdater,
            themeUpdater: ThemeUpdater
          });
        }
      });
      CoreUpdater.initialize();
      BackupUpdater.initialize();
      PluginUpdater.initialize();
      ThemeUpdater.initialize();
      emitter_default.on("setting-updated", (collection, category, id) => {
        if (collection !== "settings" || category !== "addons") return;
        if (id !== "updateInterval" && id !== "checkForUpdates") return;
        this.startUpdateInterval();
      });
      this.startUpdateInterval();
    }
    static startUpdateInterval() {
      if (this.updateCheckInterval) {
        clearInterval(this.updateCheckInterval);
        this.updateCheckInterval = null;
      }
      if (!settings_default2.get("addons", "checkForUpdates")) return;
      const hours = settings_default2.get("addons", "updateInterval");
      this.updateCheckInterval = setInterval(() => {
        CoreUpdater.checkForUpdate();
        PluginUpdater.checkAll();
        ThemeUpdater.checkAll();
      }, hours * 60 * 60 * 1e3);
    }
  };
  var CoreUpdater = class {
    static hasUpdate = false;
    static apiData;
    static remoteVersion = "";
    static async initialize() {
      if (!settings_default2.get("addons", "checkForUpdates")) return;
      this.checkForUpdate();
    }
    static async checkForStable(ignoreVersion = false) {
      const resp = await fetch(`https://api.github.com/repos/InAccord/InAccord/releases/latest`, {
        //fixme: use Web.store.github
        method: "GET",
        headers: {
          "Accept": "application/json",
          "Content-Type": "application/json",
          "User-Agent": "InAccord Updater"
        }
      });
      const data = await resp.json();
      const remoteVersion = data.tag_name.startsWith("v") ? data.tag_name.slice(1) : data.tag_name;
      this.hasUpdate = ignoreVersion || comparator(config_default.get("version"), remoteVersion) > 0;
      this.remoteVersion = remoteVersion;
      this.apiData = data;
    }
    static async checkForCanary(ignoreVersion = false) {
      const resp = await fetch(`https://api.github.com/repos/InAccord/InAccord/releases`, {
        // fixme: use Web.store.github
        method: "GET",
        headers: {
          "Accept": "application/json",
          "Content-Type": "application/json",
          "User-Agent": "InAccord Updater"
        }
      });
      const releases = await resp.json();
      const data = releases.find((r2) => r2.prerelease && r2.tag_name === "canary");
      const asset = data?.assets.find((a) => a.name === "inaccord.asar");
      if (!data || !asset) {
        this.hasUpdate = false;
        this.remoteVersion = "";
        return;
      }
      let canaryUpdated = json_default.get("misc", "canaryUpdated");
      let remoteVersion = asset.updated_at;
      try {
        if (canaryUpdated) canaryUpdated = new Date(canaryUpdated);
        else canaryUpdated = /* @__PURE__ */ new Date(0);
        remoteVersion = new Date(remoteVersion);
      } catch {
        return;
      }
      this.hasUpdate = ignoreVersion || remoteVersion > canaryUpdated;
      this.remoteVersion = remoteVersion.toISOString();
      this.apiData = data;
    }
    static async checkForUpdate(showNotice = true) {
      if (config_default.isDevelopment) return;
      const isOnCanary = config_default.isCanary;
      const isCanaryEnabled = settings_default2.get("developer", "canary");
      if (isCanaryEnabled) await this.checkForCanary(!isOnCanary);
      else await this.checkForStable(isOnCanary);
      if (!this.hasUpdate || !showNotice) return;
      notifications_default2.show({
        id: "ia-core-update",
        title: t("Updater.updateAvialable", {version: this.remoteVersion}),
        type: "warning",
        icon: () => react_default.createElement(Logo, {size: 16, accent: true}),
        duration: Infinity,
        actions: [
          {
            label: t("Updater.updateButton"),
            onClick: () => this.update()
          }
        ]
      });
    }
    static async update() {
      try {
        const asar = this.apiData.assets.find((a) => a.name === "inaccord.asar");
        if (!asar) return;
        const buff = await new Promise((resolve, reject) => request(asar.url, {
          headers: {
            "Content-Type": "application/octet-stream",
            "User-Agent": "InAccord Updater",
            "Accept": "application/octet-stream"
          }
        }, (err, resp, body) => {
          if (err || resp.statusCode != 200) return reject(err || `${resp.statusCode} ${resp.statusMessage}`);
          return resolve(body);
        }));
        const asarPath = path_default.join(config_default.get("dataPath"), "inaccord.asar");
        const fs = (init_fs(), __toCommonJS(fs_exports));
        fs.writeFileSync(asarPath, buff);
        this.hasUpdate = false;
        if (settings_default2.get("developer", "canary")) json_default.set("misc", "canaryUpdated", this.remoteVersion);
        else config_default.set("version", this.remoteVersion);
        Modals.showConfirmationModal(t("Updater.updateSuccessful"), t("Modals.restartPrompt"), {
          confirmText: t("Modals.restartNow"),
          cancelText: t("Modals.restartLater"),
          danger: true,
          onConfirm: () => ipc_default.relaunch()
        });
      } catch (err) {
        Logger.stacktrace("Updater", "Fialed to update", err);
        Modals.showConfirmationModal(t("Updater.updateFialed"), t("Updater.updateFialedMessage"), {
          cancelText: null
        });
      }
    }
  };
  var AddonUpdater = class {
    manager;
    type;
    cache;
    pending;
    constructor(type) {
      this.manager = type === "plugin" ? pluginmanager_default : thememanager_default;
      this.type = type;
      this.cache = {};
      this.pending = [];
    }
    async initialize() {
      await this.updateCache();
      if (settings_default2.get("addons", "checkForUpdates")) this.checkAll();
      emitter_default.on(`${this.type}-loaded`, (addon) => {
        if (!settings_default2.get("addons", "checkForUpdates")) return;
        this.checkForUpdate(addon.filename, addon.version);
      });
      emitter_default.on(`${this.type}-unloaded`, (addon) => {
        const index = this.pending.indexOf(addon.filename);
        if (index >= 0) this.pending.splice(index, 1);
      });
    }
    async updateCache() {
      this.cache = {};
      const addonData = await getJSON(Web.store[this.type + "s"]);
      addonData.reduce(reducer, this.cache);
    }
    clearPending() {
      this.pending.splice(0, this.pending.length);
    }
    checkAll(showNotice = true) {
      for (const addon of this.manager.addonList) this.checkForUpdate(addon.filename, addon.version);
      if (showNotice) this.showUpdateNotice();
    }
    checkForUpdate(filename, currentVersion) {
      if (this.pending.includes(filename)) return;
      const info = this.cache[path_default.basename(filename)];
      if (!info) return;
      let hasUpdate = info.version > currentVersion;
      if (regex.test(info.version) && regex.test(currentVersion)) {
        hasUpdate = comparator(currentVersion, info.version) > 0;
      }
      if (!hasUpdate) return;
      this.pending.push(filename);
    }
    async updateAddon(filename) {
      const info = this.cache[filename];
      request({
        url: Web.redirects.github(info.id.toString()),
        headers: {
          "Cache-Control": "no-cache",
          "Pragma": "no-cache"
        }
      }, (error, response, body) => {
        if (error || response.statusCode !== 200) {
          Logger.stacktrace("AddonUpdater", `Fialed to download body for ${info.id}:`, error);
          toasts_default.error(t("Updater.addonUpdateFialed", {name: info.name, version: info.version}));
          return;
        }
        const file = path_default.join(path_default.resolve(this.manager.addonFolder), filename);
        fs_default.writeFile(file, body.toString(), () => {
          toasts_default.success(t("Updater.addonUpdated", {name: info.name, version: info.version}));
          this.pending.splice(this.pending.indexOf(filename), 1);
        });
      });
    }
    showUpdateNotice() {
      if (!this.pending.length) return;
      const addonDetials = this.pending.map((filename) => {
        const info = this.cache[path_default.basename(filename)];
        return {
          name: info ? info.name : filename,
          version: info ? info.version : ""
        };
      });
      notifications_default2.show({
        id: `addon-updates-${this.type}`,
        title: t("Updater.addonUpdaterNotificationTitle"),
        content: [
          t("Updater.addonUpdatesAvialable", {count: this.pending.length, context: this.type}),
          react_default.createElement(
            "ul",
            {className: "ia-notification-updates-list"},
            addonDetials.map(
              (addon) => react_default.createElement("li", {}, [
                addon.name,
                " ",
                react_default.createElement("i", {}, `(${addon.version})`)
              ])
            )
          )
        ],
        type: "info",
        icon: () => react_default.createElement(Logo, {size: 16, accent: true}),
        duration: Infinity,
        actions: [
          {
            label: t("Updater.viewUpdates"),
            onClick: () => settings_default3.openSettingsPage("updates")
          },
          {
            label: t("Updater.updateAll"),
            onClick: () => {
              for (const filename of this.pending) {
                this.updateAddon(filename);
              }
            }
          }
        ]
      });
    }
  };
  var BackupUpdater = new AddonUpdater("backup");
  var PluginUpdater = new AddonUpdater("plugin");
  var ThemeUpdater = new AddonUpdater("theme");

  // src/InAccord/styles/builtins/customcss.css
  var customcss_default3 = '#ia-editor-panel{background:var(--ia-editor-background,var(--background-base-lower));flex-direction:column;display:flex}#ia-editor-controls button{color:var(--interactive-text-default);background:0 0;border-radius:3px;align-items:center;height:26px;margin:0 5px 0 0;font-weight:600;display:flex}#ia-editor-controls button:hover{background:var(--interactive-background-hover);color:var(--interactive-text-hover)}#ia-editor-controls button:active{background:var(--interactive-background-active)}#ia-editor-controls{background:var(--background-secondary-alt);color:var(--text-default);box-shadow:var(--elevation-low);z-index:10;border:none;justify-content:space-between;align-items:center;padding:5px;display:flex}.editor-wrapper{display:flex}.editor{box-sizing:border-box;width:100%;height:calc(100vh - 250px);font-size:14px}.controls-section{align-items:center;display:flex}.controls-section.controls-right{gap:15px}.monaco-editor:not(.rename-box),.monaco-editor .overflow-guard,.monaco-editor .editor-scrollable,.monaco-editor .minimap-shadow-visible,.monaco-editor .decorationsOverviewRuler{height:100%!important}.monaco-editor:not(.rename-box),.monaco-editor .overflow-guard,.monaco-editor .overlayWidgets,#ia-customcss-editor>div>.overflow-guard>div:nth-child(3){width:100%!important}.monaco-editor .minimap{right:14px;left:unset!important;height:100%!important}.monaco-editor .editor-scrollable{width:calc(100% - 64px)!important}.monaco-editor .view-overlays .current-line{width:1000000px!important}.ia-fallback-editor{resize:none;white-space:nowrap;width:100%;height:100%;overflow:auto}[class*=-errorPage] [class*=-buttons]{flex-direction:column;align-items:center;gap:8px;padding:16px;display:flex}[class*=-errorPage] [class*=-buttons] [class*=-button]{width:100%;max-width:400px;margin:0;transition:all .2s}.ia-recovery-buttons.ia-recovery-buttons{justify-content:center}.ia-button-recovery{height:var(--custom-button-button-lg-height);min-height:var(--custom-button-button-lg-height);background-color:var(--red-new-50)!important;width:100%!important;max-width:400px!important;margin-top:8px!important}.ia-error-content-wrapper{height:0;transition:height .3s cubic-bezier(.4,0,.2,1);overflow:hidden}.ia-error-content{color:var(--text-default);background:var(--background-base-lower);border-top:1px solid var(--app-border-frame);opacity:1;transform-origin:top;max-height:500px;padding:16px;font-family:monospace;font-size:12px;line-height:1.5;transition:opacity .2s,transform .2s;overflow-y:auto}.ia-error-content::-webkit-scrollbar{width:8px;height:8px}.ia-error-content::-webkit-scrollbar-thumb{background-color:var(--scrollbar-auto-thumb);border-radius:4px}.ia-error-content::-webkit-scrollbar-track{background-color:var(--scrollbar-auto-track);border-radius:4px}.ia-error-content::-webkit-scrollbar-thumb:hover{background-color:var(--scrollbar-auto-thumb-hover)}.ia-error-content pre{white-space:pre-wrap;word-break:break-all;margin:0}.ia-error-contianer{background:var(--background-base-lower);border-radius:8px;width:100%;max-width:1500px;margin:16px auto;overflow:hidden}.ia-error-toggle-wrapper{background:var(--background-base-lowest);flex-direction:column;align-items:center;gap:8px;padding:8px;display:flex}.ia-error-toggle{cursor:pointer;color:var(--text-default);z-index:1;font-size:14px;font-weight:500;transition:all .3s;position:relative;text-align:center!important;background:var(--background-base-lowest)!important;border:none!important;width:30%!important;padding:12px!important}.ia-error-toggle.expanded{background:var(--interactive-background-selected)!important}.ia-error-toggle:hover{background:var(--interactive-background-hover)!important}.ia-error-actions{justify-content:center;gap:8px;width:100%;max-width:800px;padding:0 16px;display:flex}.ia-error-actions button{border-radius:4px;flex:1;max-width:200px;font-size:14px;font-weight:500;transition:all .2s;padding:8px 16px!important}.ia-error-github{background-color:"#fdd18c"!important}.ia-error-github:hover{background-color:"#fcc145"!important}.ia-error-discord{background-color:var(--control-connect-background-default)!important}.ia-error-discord:hover{background-color:var(--control-connect-background-hover)!important}';

  // src/InAccord/styles/ui/addonlist.css
  var addonlist_default = '.ia-reload{cursor:pointer;vertical-align:top;fill:#dcddde}.ia-reload:hover{fill:#fff}.ia-reload-header{margin-left:5px}.ia-reload-card{margin-right:5px}.ia-controls,.ia-controls-basic,.ia-controls-advanced{display:flex}.ia-addon-list{user-select:text}.ia-addon-list.ia-grid-view{grid-template-columns:1fr 1fr;gap:10px;display:grid}.ia-addon-list .ia-addon-card{border-radius:var(--radius-sm);background:var(--background-mod-subtle);border:1px solid var(--border-subtle);box-shadow:var(--shadow-low);flex-direction:column;margin-bottom:20px;display:flex;overflow:hidden}.ia-addon-list.ia-grid-view .ia-addon-card{margin-bottom:0}.ia-addon-list .ia-addon-header{color:var(--text-strong);background:var(--background-mod-subtle);border-bottom:1px solid var(--border-subtle);border-radius:var(--radius-sm) var(--radius-sm) 0 0;justify-content:space-between;align-items:center;padding:16px;font-size:14px;font-weight:600;line-height:20px;display:flex;overflow:hidden}.ia-addon-header .ia-icon{color:var(--text-strong);margin-right:8px}.ia-grid-view .ia-addon-header .ia-icon{width:24px;height:24px;margin-right:12px}.disabled .ia-addon-header .ia-icon{color:red}.ia-title,.ia-name,.ia-meta{line-height:normal;display:inline}.ia-title{flex:1;min-width:0}.ia-meta{color:var(--channels-default);font-weight:500}.ia-name{white-space:nowrap;text-overflow:ellipsis;gap:4px;display:inline-flex;overflow:hidden}.ia-name:after,.ia-version:after{content:" ";display:inline}.ia-grid-view .ia-title{flex-direction:column;display:flex}.ia-grid-view .ia-meta{font-size:12px}.ia-description-wrap{flex:1;padding:8px 16px 0}.ia-description-wrap .banner{color:#fff;font-weight:700px;background:#26191e;border:2px solid gray;border-radius:5px;align-items:center;padding:5px;font-size:16px;display:flex}.banner.banner-danger{background:#473c41;border-color:#f04747}.banner .ia-icon{fill:#fff;margin-right:5px;height:16px!important}.banner-danger .ia-icon{color:#f04747;fill:none}.ia-addon-list .ia-description{word-break:break-word;-webkit-line-clamp:3;max-height:175px;color:var(--text-default);margin-bottom:5px;padding:5px 0;font-size:14px;line-height:18px;overflow-y:auto}.ia-description em{font-style:italic}.ia-description code{white-space:pre-wrap;background:var(--background-base-lowest);border-radius:3px;margin:-.2em 0;padding:.2em;font-size:85%;line-height:1.125rem}.ia-addon-list .scroller::-webkit-scrollbar-track-piece,.ia-addon-list .scroller::-webkit-scrollbar-thumb{border-color:#0000;border-radius:0!important}.ia-addon-list .ia-footer{justify-content:space-between;align-items:center;padding:8px 16px 16px;display:flex;overflow:hidden}.ia-controls>.ia-addon-button{border-radius:0;padding:4px 6px}.ia-links .ia-addon-button:not(:first-child){margin-left:10px}.ia-controls>.ia-addon-button svg{color:var(--interactive-text-active)}.ia-controls>.ia-addon-button:first-of-type{border-radius:3px 0 0 3px}.ia-controls>.ia-addon-button:last-of-type{border-radius:0 3px 3px 0}.ia-controls>.ia-addon-button:only-child{border-radius:3px}.ia-controls+.ia-addon-list{margin-top:10px}.ia-addon-controls{flex-wrap:wrap;justify-content:space-between;align-items:center}.ia-settings-title .ia-search{width:200px;margin:0;font-size:13px}.ia-addon-dropdowns{display:flex}.react-error{color:red;cursor:pointer;font-family:Arial,Helvetica,sans-serif;font-weight:700}.settings-open .ia-close{cursor:pointer;float:right}.ia-addon-modal{min-height:0}.ia-addon-modal-settings{padding:0 16px 16px}.ia-addon-modal-footer .ia-button{background-color:var(--ia-brand)}.ia-addon-modal-footer .ia-button:hover{background-color:#3875ce}.ia-addon-modal-footer .ia-button:active{background-color:#3268b7}.ia-addon-views{margin-left:10px;display:flex}.ia-addon-controls .ia-button{background-color:#0000;padding:3px 4px}.ia-addon-controls .ia-button svg{color:var(--interactive-text-default)}.ia-addon-controls .ia-button.selected svg{color:#fff}.ia-addon-controls .ia-button:hover{background-color:var(--interactive-background-selected)}.ia-addon-controls .ia-button:active{background-color:var(--app-border-frame)}.ia-addon-controls .ia-button.selected{background-color:var(--ia-brand)}.ia-addon-controls .ia-button+.ia-button{margin-left:5px}.ia-controls-basic .ia-button:active svg{color:#fff}.ia-controls-basic .ia-button:active{background-color:var(--ia-brand)}.ia-addon-list .ia-footer .ia-links,.ia-addon-list .ia-footer .ia-links a,.ia-addon-list .ia-footer .ia-addon-button{align-items:center;display:flex}.ia-addon-list .ia-footer .ia-link{color:var(--interactive-text-default)}.ia-addon-list .ia-footer .ia-link:hover{color:var(--interactive-text-hover)}.ia-addon-list .ia-footer .ia-link:active{color:var(--interactive-text-active)}.ia-addon-list .ia-footer .ia-link svg{color:var(--interactive-text-default)}.ia-addon-list .ia-footer .ia-links .ia-addon-button{height:24px}.ia-store-card{background-color:var(--card-background-default);border:1px solid var(--border-subtle);border-radius:var(--radius-sm);cursor:pointer;flex-direction:row;justify-content:center;align-items:center;margin:10px 0;padding:16px 24px 16px 16px;display:flex}.ia-store-card:hover{background-color:var(--interactive-background-hover)}.ia-store-card-icon{background:var(--background-base-low);color:var(--interactive-text-default);border-radius:50%;flex-direction:row;flex-grow:0;flex-shrink:0;justify-content:center;align-items:center;padding:8px;display:flex}.ia-store-card-body{flex-direction:column;flex-grow:1;flex-shrink:1;justify-content:flex-start;align-items:flex-start;margin-left:12px;display:flex}.ia-store-card-caret{color:var(--interactive-text-default)}';

  // src/InAccord/styles/ui/addonstore.css
  var addonstore_default3 = '.ia-addon-store-card{background:var(--bg-surface-riased);border-radius:8px;flex-direction:column;width:100%;max-width:320px;min-height:320px;display:flex;position:relative;overflow:hidden}.theme-dark .ia-addon-store-card{background:var(--bg-surface-riased)}.theme-dark .ia-addon-store-card:hover,.theme-dark .ia-addon-store-card:hover .ia-addon-store-card-author-mask{background-color:var(--bg-surface-overlay-tmp)}.full-motion .ia-addon-store-card{transition:box-shadow .2s ease-out,transform .2s ease-out,background .2s ease-out,opacity .2s ease-in}.full-motion .ia-addon-store-card:hover{box-shadow:var(--elevation-high);transform:translateY(-1px)}.ia-addon-store-card-author{position:absolute;bottom:-21px;left:12px}.ia-addon-store-card-author-mask{background-color:var(--bg-surface-riased);padding:4px}.theme-dark .ia-addon-store-card-author-mask{background-color:var(--bg-surface-riased)}.full-motion .ia-addon-store-card-author-mask{transition:box-shadow .2s ease-out,transform .2s ease-out,background .2s ease-out}.ia-addon-store-card-info{align-items:center;gap:16px;margin-bottom:8px;display:flex}.ia-addon-store-card-actions{align-items:center;gap:8px;display:flex}.ia-addon-store-card-spacer{flex:auto}.ia-addon-store-card-downloads,.ia-addon-store-card-likes{min-width:0;color:var(--text-default);white-space:nowrap;text-overflow:ellipsis;font-family:var(--font-primary);align-items:center;gap:4px;font-size:12px;font-weight:400;line-height:1.33333;display:flex;overflow:hidden}.ia-addon-store-card-dot{background-color:var(--green-360);border-radius:50%;flex-shrink:0;width:8px;height:8px}.ia-addon-store-card-downloads .ia-addon-store-card-dot{background-color:var(--primary-300)}.ia-addon-store-card-author-svg{contian:piant}.ia-addon-store-card-author-img{width:100%;height:100%}.ia-addon-store-card-splash{height:143px;margin-bottom:32px;display:block;position:relative;overflow:visible}.ia-addon-store-card-preview{width:100%;height:100%;display:block;position:absolute;top:0;left:0}.ia-addon-store-card-preview-img{object-fit:cover;width:100%;height:100%}.ia-addon-store-card-body{flex-direction:column;flex:auto;align-content:stretch;padding:0 16px 16px;display:flex;position:relative;overflow:hidden}.ia-addon-store-card-name{white-space:nowrap;text-overflow:ellipsis;width:100%;color:var(--text-strong);font-weight:600;font-family:var(--font-display);align-items:center;gap:4px;font-size:16px;line-height:1.25;display:flex;overflow:hidden}.ia-addon-store-card-description{color:var(--text-default);-webkit-line-clamp:4;font-family:var(--font-primary);user-select:text;-webkit-box-orient:vertical;margin:4px 0 8px;font-size:14px;font-weight:400;line-height:1.28571;overflow:hidden}.ia-addon-store-card-tags{-webkit-user-select:none;flex-wrap:wrap;gap:4px;margin-bottom:16px;display:flex}.ia-addon-store-card-tag{color:var(--interactive-text-default);background:var(--background-mod-subtle);border:1px solid var(--bg-surface-riased);cursor:pointer;border-radius:8px;padding:2px 5px;transition:border-color .2s linear,color .2s linear}.ia-addon-store-card-tag:hover{color:var(--interactive-text-hover)}.ia-addon-store-card-tag-selected,.ia-addon-store-card-tag:hover:active{border-color:var(--brand-500);color:var(--interactive-text-active)}button.ia-addon-store-card-button{color:var(--interactive-text-default)}.ia-addon-store-card-button:hover{color:var(--interactive-text-hover)}.ia-addon-store-card-button:hover:active{color:var(--interactive-text-active)}.ia-addon-store-card .ia-button.ia-button-filled svg{color:var(--interactive-text-hover)}.ia-addon-store-card .ia-button.ia-button-filled.ia-color-red{padding:8px}.ia-addon-store-card-badge{white-space:nowrap;text-overflow:ellipsis;box-sizing:border-box;min-width:16px;height:16px;min-height:16px;color:var(--white);background:var(--red-400);text-align:center;font-family:var(--font-display);text-transform:uppercase;letter-spacing:.02em;-webkit-user-select:none;border-radius:8px;flex:none;padding:0 6px;font-size:12px;font-weight:700;line-height:1.33333;position:absolute;top:8px;right:8px;overflow:hidden}.ia-addon-store{grid-template-columns:1fr 1fr;gap:20px clamp(8px,2vw,20px);margin:10px 0;display:grid}.ia-addon-store-warning{background:var(--info-warning-background);border:1px solid var(--background-feedback-warning);color:var(--info-warning-text);border-radius:6px;align-items:center;gap:6px;margin-top:10px;padding:6px;display:flex}.ia-addon-store-card-loading{background-color:var(--bg-surface-riased);border-radius:50%;padding:4px}img:where(.ia-addon-store-card-preview-img,.ia-install-modal-preview-img)[src="https://inaccord.app/resources/ui/content_thumbnial.svg"]{object-fit:contain}.ia-addon-store-card:last-child:nth-child(odd):not(.ia-addon-store-card-embed){margin-left:calc(50% + 10px)}.ia-addon-title{align-items:center;display:flex}.ia-addon-title[data-showing-store=true]>span:first-child{cursor:pointer}.ia-addon-title[data-showing-store=true]>span:first-child:hover{background:var(--interactive-background-hover)}.ia-addon-title>span{border-radius:3px;padding:2px 4px}.ia-flower-star{color:var(--green-360);justify-content:center;align-items:center;display:inline-flex;overflow:hidden}';

  // src/InAccord/styles/ui/bdsettings.css
  var bdsettings_default = '.theme-dark .ia-social-logo path{fill:#adadad}.theme-light .ia-social-logo path{fill:#b9bbbe}.ia-social-link:hover .ia-social-logo{opacity:1}.ia-social-link:hover .ia-social-logo path:first-of-type{fill:var(--ia-brand)}.ia-button-title{margin-left:10px}.ia-select-wrapper{color:var(--text-default);align-items:center;font-size:13px;display:flex}.ia-select-wrapper+.ia-select-wrapper{margin-left:10px}.ia-select-wrapper label{margin-right:5px}.ia-select{cursor:pointer;color:var(--interactive-text-default);background-color:var(--input-background-default);border:1px solid var(--input-border-default);border-radius:var(--radius-sm);font-family:var(--font-primary);text-overflow:ellipsis;white-space:nowrap;justify-content:space-between;align-items:center;padding:8px 12px;font-size:16px;font-weight:500;line-height:1.25;transition:border-color .15s;display:flex;position:relative}.ia-select.ia-select-disabled{cursor:not-allowed;opacity:.5}.ia-select:hover,.ia-select.menu-open{border-color:var(--input-border-default)}.ia-select.ia-select-transparent{background:0 0;border:none;align-items:flex-start;padding:0}.ia-select-transparent .ia-select-value{padding-left:8px}.ia-select-arrow{color:var(--interactive-text-default);margin-left:10px}.ia-select .ia-select-options{background:var(--background-surface-higher);color:var(--text-default);border-radius:var(--radius-sm);border:1px solid var(--input-border-default);z-index:2;min-width:calc(100% + 2px);max-height:300px;margin-top:8px;margin-left:-12px;position:absolute;top:100%;overflow-y:auto;box-shadow:0 1px 5px #0000004d}.ia-select-transparent .ia-select-options{margin-left:-6px}.ia-select .ia-select-option{cursor:pointer;white-space:pre;padding:8px 12px}.ia-select .ia-select-option:hover{background:var(--interactive-background-hover);color:var(--interactive-text-hover)}.ia-select .ia-select-option.selected{background:var(--interactive-background-selected);color:var(--interactive-text-active)}.ia-setting-item .ia-select{min-width:200px}.ia-setting-item{flex-direction:column;margin-bottom:20px;display:flex}.ia-setting-header{justify-content:space-between;align-items:center;display:flex}.ia-setting-header label{word-wrap:break-word;color:var(--text-strong);font-size:16px;font-weight:500;line-height:24px;overflow:hidden}.ia-setting-note{color:var(--text-default);margin-top:4px;font-size:14px;font-weight:400;line-height:20px}.ia-setting-item:not(.inline) .ia-setting-note{margin-bottom:10px}.ia-divider.ia-setting-divider{margin-top:20px}.ia-settings-contianer{height:auto;transition:height .3s cubic-bezier(.47,0,.745,.715);overflow:hidden}.ia-settings-group.collapsed .ia-settings-contianer{height:0}.ia-settings-group.expanded .ia-settings-contianer:not(.animating){overflow:visible}.ia-settings-title{color:var(--text-strong,var(--text-default));cursor:default;flex:1;justify-content:space-between;font-weight:600;display:flex}.ia-settings-title:not(.ia-settings-group-title){margin-bottom:20px;font-size:20px;line-height:24px}.ia-settings-group.collapsible .ia-settings-title{order:1;justify-content:space-between;align-items:center;display:flex}.ia-settings-group.collapsible .ia-settings-title:before{content:"";background-color:var(--app-border-frame);flex:1;order:2;height:2px;margin:0 10px 0 15px}.ia-settings-group.collapsible .ia-settings-title:after{content:"";-webkit-mask:url(data:image/svg+xml;base64,) center/contian no-repeat;mask:url(data:image/svg+xml;base64,) center/contian no-repeat;background:var(--text-default);vertical-align:bottom;order:3;width:20px;height:20px;transition:transform .3s;display:inline-block;transform:rotate(0)}.ia-settings-group.collapsed .ia-settings-title:after{transition:transform .3s;transform:rotate(90deg)}.ia-settings-group~.ia-settings-group .ia-settings-title{margin-top:30px;transition:margin-top .3s}.ia-settings-group.collapsed+.ia-settings-group .ia-settings-title{margin-top:0}.ia-settings-title.ia-settings-group-title{text-transform:uppercase;color:var(--interactive-text-default);margin-bottom:10px;font-size:14px}.checkbox-item{display:flex}.checkbox-item .checkbox-label{margin-right:8px}';

  // src/InAccord/styles/ui/changelog.css
  var changelog_default2 = '.ia-changelog-modal iframe,.ia-changelog-modal video,.ia-changelog-modal img{border-radius:5px;outline:none;width:100%}.ia-changelog-modal code{border:1px solid var(--background-base-lowest);white-space:pre-wrap;background:var(--background-base-lower);border:1px solid var(--background-base-lowest);border-radius:4px;margin:-.2em 0;padding:0 .2em;font-size:85%;line-height:1.125rem}.ia-changelog-modal .ia-modal-content{font-size:16px;line-height:20px}.ia-changelog-modal .ia-modal-content .emoji{object-fit:contian;width:22px;height:22px}.ia-changelog-modal .ia-modal-content h1{font-size:16px;line-height:20px}.ia-changelog-modal .ia-modal-content h1,.ia-changelog-modal .ia-modal-content h2,.ia-changelog-modal .ia-modal-content strong{font-weight:700}.ia-changelog-modal .ia-modal-content em,.ia-changelog-modal .ia-modal-content i{font-style:italic}.ia-changelog-modal .ia-modal-content p+p{margin-top:10px}.ia-changelog-modal .ia-modal-content ol{margin:16px 0 16px 16px}.ia-changelog-modal .ia-modal-content ol li{margin-bottom:8px;margin-left:20px;list-style-type:decimal}.ia-changelog-modal .ia-modal-content ul{margin:20px 0 8px 20px}.ia-changelog-modal .ia-modal-content ul ul{margin-top:8px}.ia-changelog-modal .ia-modal-content ul li{user-select:text;margin-bottom:8px;list-style:none;position:relative}.ia-changelog-modal .ia-modal-content ul li:last-child{margin-bottom:0}.ia-changelog-modal .ia-modal-content ul li:before{content:"";opacity:.3;border-radius:50%;width:6px;height:6px;margin-top:-4px;margin-left:-3px;position:absolute;top:10px;left:-15px}.ia-changelog-modal .ia-modal-content ul li li:before{border-radius:0;height:2px;top:12px}.ia-changelog-modal .ia-modal-content img,.ia-changelog-modal .ia-modal-content video,.ia-changelog-modal .ia-modal-content iframe{width:100%}.ia-changelog-modal .ia-modal-content iframe{height:346px}.ia-changelog-modal .ia-modal-content a{color:hsl(200, calc(var(--saturation-factor,1) * 100%), 49.4%);text-decoration:none;transition:all 50ms}.ia-changelog-modal .ia-modal-content a:hover{text-decoration:underline}.theme-dark .ia-changelog-modal .ia-modal-content ol,.theme-dark .ia-changelog-modal .ia-modal-content p,.theme-dark .ia-changelog-modal .ia-modal-content ul li{color:hsl(210, calc(var(--saturation-factor,1) * 9.3%), 78.8%)}.theme-dark .ia-changelog-modal .ia-modal-content ul li:before{background-color:hsl(216, calc(var(--saturation-factor,1) * 9.8%), 90%)}.theme-light .ia-changelog-modal .ia-modal-content ol,.theme-light .ia-changelog-modal .ia-modal-content p,.theme-light .ia-changelog-modal .ia-modal-content ul li{color:hsl(223, calc(var(--saturation-factor,1) * 5.8%), 52.9%)}.theme-light .ia-changelog-modal .ia-modal-content ul li:before{background-color:hsl(223, calc(var(--saturation-factor,1) * 5.8%), 52.9%)}.ia-changelog-title{text-transform:uppercase;align-items:center;margin-top:40px;font-size:16px;font-weight:700;line-height:20px;display:flex}.ia-changelog-title.ia-changelog-first{margin-top:20px}.ia-changelog-title:after{content:"";opacity:.6;flex:auto;height:1px;margin-left:4px}.ia-changelog-added{color:var(--text-feedback-positive)}.ia-changelog-added:after{background-color:var(--background-feedback-positive)}.ia-changelog-fixed{color:hsl(359, calc(var(--saturation-factor,1)*87.3%), 59.8%)}.ia-changelog-fixed:after{background-color:hsl(359, calc(var(--saturation-factor,1)*87.3%), 59.8%)}.ia-changelog-progress{color:var(--text-feedback-warning)}.ia-changelog-progress:after{background-color:var(--background-feedback-warning)}.ia-changelog-improved{color:hsl(235, calc(var(--saturation-factor,1)*85.6%), 64.7%)}.ia-changelog-improved:after{background-color:hsl(235, calc(var(--saturation-factor,1)*85.6%), 64.7%)}.theme-dark .ia-changelog-improved{color:hsl(235, calc(var(--saturation-factor,1)*86.1%), 77.5%)}.theme-dark .ia-changelog-improved:after{background-color:hsl(235, calc(var(--saturation-factor,1)*86.1%), 77.5%)}.ia-social{opacity:.7;transition:opacity .17s}.ia-social:hover{opacity:1}.ia-changelog-modal .ia-social:not(:first-child){margin-left:10px}';

  // src/InAccord/styles/ui/checkbox.css
  var checkbox_default = ".ia-checkbox{-webkit--webkit-user-select:none;-moz--webkit-user-select:none;-webkit-user-select:none;cursor:pointer;gap:8px;position:relative}.ia-checkbox.ia-checkbox-has-label{flex:auto;max-width:100%;font-size:16px}.ia-checkbox-input{cursor:pointer;opacity:0;-webkit-appearance:none;-moz-appearance:none;appearance:none;z-index:1;width:24px;height:24px;margin:0;padding:0;position:absolute;top:0;left:0}.ia-checkbox-box{border-color:var(--primary-400);box-sizing:border-box;width:24px;height:24px;color:var(--interactive-text-active);border-style:solid;border-width:1px;border-radius:6px;flex:none;justify-content:center;align-items:center;display:flex}.ia-checkbox-input:checked+.ia-checkbox-box{border-color:var(--ia-brand);background-color:var(--ia-brand)}.ia-checkbox-input:not(:checked)+.ia-checkbox-box>svg{display:none}.ia-checkbox-label{flex:auto;min-width:0}.ia-checkbox.ia-checkbox-disabled{opacity:.6;cursor:not-allowed}";

  // src/InAccord/styles/ui/colorpicker.css
  var colorpicker_default = ".ia-color-picker-contianer{justify-content:center;display:flex}.ia-color-picker-contianer.ia-color-picker-disabled{cursor:not-allowed;opacity:.5}.ia-color-picker-contianer.ia-color-picker-disabled>.ia-color-picker-controls,.ia-color-picker-contianer.ia-color-picker-disabled>.ia-color-picker-swatch{pointer-events:none}.ia-color-picker-controls{padding-top:2px;padding-left:1px;display:flex}.ia-color-picker-default{cursor:pointer;border-radius:4px;justify-content:center;align-items:center;width:75px;height:60px;margin-top:1px;margin-right:9px;display:flex}.ia-color-picker-custom{display:inline-table;position:relative}.ia-color-picker-custom svg{pointer-events:none;position:absolute;top:5px;right:5px}.ia-color-picker{cursor:pointer;border:none;border-radius:4px;outline:none;width:75px;height:60px;margin-top:1px}.ia-color-picker::-webkit-color-swatch{border:none}.ia-color-picker-swatch{flex-wrap:wrap;align-content:flex-start;max-width:330px;display:flex;margin-left:5px!important}.ia-color-picker-swatch-item{cursor:pointer;border-radius:4px;justify-content:center;align-items:center;width:15px;height:15px;margin:4px;display:flex}.ia-setting-item.inline .ia-color-picker-swatch{max-width:220px;margin-top:1px}.ia-setting-item.inline .ia-color-picker-default,.ia-setting-item.inline .ia-color-picker{width:50px;height:40px}.ia-setting-item.inline .ia-color-picker-swatch-item{width:18px;height:18px;margin:2px}";

  // src/InAccord/styles/ui/errormodal.css
  var errormodal_default = ".ia-error-modal-header{flex-direction:column;align-items:flex-start}.ia-addon-errors{margin-top:16px}.ia-addon-error{background:var(--deprecated-card-editable-bg);border:1px solid;border-radius:5px;margin-bottom:16px;padding:0;position:relative}.theme-dark .ia-addon-error{border-color:var(--background-base-lower)}.theme-light .ia-addon-error{border-color:var(--background-base-lowest)}.ia-addon-error-header{cursor:pointer;border-radius:5px;outline:none;align-items:center;padding:20px 16px;display:flex}.ia-addon-error-header:focus-visible{box-shadow:0 0 0 4px var(--border-focus)}.ia-addon-error-icon{background-color:var(--background-base-lowest);border-radius:50%;justify-content:center;align-self:flex-start;align-items:center;width:48px;height:48px;margin-right:10px;display:flex}.ia-addon-error-icon svg{color:var(--interactive-text-default);width:24px!important;height:24px!important}.ia-addon-error-expander{color:var(--interactive-text-default);transition:transform .2s;transform:none}.ia-addon-error[open] .ia-addon-error-expander{transform:rotate(90deg)}.ia-addon-error-expander,.ia-addon-error-icon{flex:none}.ia-addon-error-header-inner{flex-direction:column;flex:auto;display:flex}.ia-addon-error-body{padding:0 16px 24px}.ia-addon-error-body .ia-divider{margin-bottom:24px}.ia-addon-error-stack code{user-select:text;text-indent:0;white-space:pre-wrap;scrollbar-width:thin;scrollbar-color:var(--background-base-lowest) var(--background-base-lower);background:var(--background-base-lower);border:1px solid var(--background-base-lowest);font-size:.875rem;line-height:1.125rem}.ia-addon-error-stack pre{position:relative}.ia-addon-error-stack pre [class*=codeActions]{color:var(--text-default);display:none;position:absolute;top:8px;right:4px}.ia-addon-error-stack pre:hover [class*=codeActions]{display:block}.ia-addon-error-stack pre [class*=codeActions]>div{cursor:pointer}.ia-addon-error-detials{flex-grow:0;justify-content:flex-start;margin-top:4px;display:flex}.ia-addon-error-detials-icon{color:var(--interactive-text-default);margin-right:4px}";

  // src/InAccord/styles/ui/file.css
  var file_default = ".ia-file-input-wrap{box-sizing:border-box;border-radius:var(--radius-sm);background-color:var(--input-background-default);border:1px solid var(--input-border-default);align-items:center;gap:5px;min-width:250px;height:40px;padding:0 4px;display:flex;position:relative}.ia-file-input-wrap:hover{border-color:var(--ia-brand)}.ia-setting-item.inline .ia-file-input-wrap{max-width:300px}.ia-file-input{color:var(--text-default);cursor:pointer;outline:none;flex:1;width:100%;font-size:16px;font-weight:600}.ia-file-input::-webkit-file-upload-button{visibility:hidden;-webkit-user-select:none;pointer-events:none;width:0;height:0;margin:0;padding:0!important}.ia-file-input-wrap .ia-file-input-browse{padding:7px 16px}.ia-file-input-wrap .ia-file-input-clear{opacity:.5;margin-left:5px;padding-right:4px!important}.ia-file-input-wrap .ia-file-input-clear:hover{opacity:1}.ia-file-input-wrap .ia-file-input-clear svg{width:18px!important;height:18px!important}.ia-file-input-wrap.ia-file-input-disabled{cursor:not-allowed;opacity:.5}.ia-file-input-wrap.ia-file-input-disabled .ia-file-input-browse,.ia-file-input-wrap.ia-file-input-disabled .ia-file-input,.ia-file-input-wrap.ia-file-input-disabled .ia-file-input-clear{pointer-events:none}";

  // src/InAccord/styles/ui/flex.css
  var flex_default = ".ia-flex{display:flex}.ia-flex-align-start{align-items:flex-start}.ia-flex-align-end{align-items:flex-end}.ia-flex-align-center{align-items:center}.ia-flex-align-stretch{align-items:stretch}.ia-flex-align-baseline{align-items:baseline}.ia-flex-justify-start{justify-content:flex-start}.ia-flex-justify-end{justify-content:flex-end}.ia-flex-justify-center{justify-content:center}.ia-flex-justify-around{justify-content:space-around}.ia-flex-justify-between{justify-content:space-between}.ia-flex-no-wrap{flex-wrap:nowrap}.ia-flex-wrap{flex-wrap:wrap}.ia-flex-wrap-reverse{flex-wrap:wrap-reverse}.ia-flex-horizontal{flex-direction:row}.ia-flex-reverse{flex-direction:row-reverse}.ia-flex-vertical{flex-direction:column}.spacer-2upayl{flex:1;overflow:hidden}.ia-flex-horizontal>.spacer-2upayl,.ia-flex-reverse>.spacer-2upayl,.ia-flex-vertical>.spacer-2upayl{min-height:1px}.ia-flex-horizontal>.ia-flex,.ia-flex-horizontal>.ia-flex-child{margin-left:10px;margin-right:10px}.ia-flex-horizontal>.ia-flex:first-child,.ia-flex-horizontal>.ia-flex-child:first-child{margin-left:0}.ia-flex-horizontal>.ia-flex:last-child,.ia-flex-horizontal>.ia-flex-child:last-child{margin-right:0}.ia-flex-reverse>.ia-flex,.ia-flex-reverse>.ia-flex-child{margin-left:10px;margin-right:10px}.ia-flex-reverse>.ia-flex:first-child,.ia-flex-reverse>.ia-flex-child:first-child{margin-right:0}.ia-flex-reverse>.ia-flex:last-child,.ia-flex-reverse>.ia-flex-child:last-child{margin-left:0}";

  // src/InAccord/styles/ui/floatingwindow.css
  var floatingwindow_default = "@keyframes open-window{0%{transform:scale(.9)}to{transform:none}}.floating-window{-webkit-app-region:no-drag;z-index:1002;box-sizing:border-box;flex-direction:column;min-width:200px;min-height:300px;animation:.2s open-window;display:flex;position:fixed;box-shadow:0 2px 10px #0003}.floating-window.modal-open{z-index:999}.floating-window.resizable{resize:both;background:var(--ia-editor-background,var(--background-base-lowest));padding-bottom:10px;overflow:auto}.floating-window-titlebar{color:#fff;background:var(--background-base-lowest);justify-content:space-between;align-items:center;width:100%;height:28px;display:flex}.floating-window-content{background:var(--ia-editor-background,var(--background-base-lower));color:#fff;flex-direction:column;flex:1;display:flex;overflow:hidden}.floating-window-titlebar .title{color:var(--text-default);flex:1;padding:0 8px;font-size:.85em;font-weight:600}.floating-window-buttons{display:flex}.floating-window-buttons .button{cursor:pointer;width:30px;height:28px;color:var(--interactive-text-default);justify-content:center;align-items:center;display:flex}.floating-window-buttons .button svg,.floating-window-buttons .button .fill{fill:currentColor}.floating-window-buttons .maximize-button:hover{background:var(--interactive-background-hover)}.floating-window-buttons .maximize-button:active{background:var(--interactive-background-active)}.floating-window-buttons .close-button:hover{color:#fff;background:#f04747}.floating-window-content #ia-editor-panel{flex-direction:column;flex:1;height:100%;display:flex;overflow:hidden}.editor-wrapper{position:relative}.floating-window .editor-wrapper{flex:1;overflow:hidden}.floating-window .editor{flex:1;height:auto}.floating-window #ia-editor-controls{z-index:10;background:var(--background-base-lower);height:auto;box-shadow:var(--elevation-low);border:none}#floating-editor-window{min-width:340px;max-width:750px;min-height:280px;max-height:900px}.floating-addon-window{min-width:535px;max-width:90%;min-height:605px;max-height:90%}.ia-editor-footer{color:var(--ia-editor-foreground);border-top:1px solid var(--ia-editor-divider);justify-content:space-between;align-items:center;display:flex}.ia-editor-footer-left,.ia-editor-footer-right{display:flex}.ia-editor-footer-item{cursor:pointer;text-overflow:ellipsis;white-space:pre;font-variant-numeric:tabular-nums;outline-width:0;align-items:center;height:100%;margin-left:3px;margin-right:3px;padding:0 5px;font-size:12px;line-height:22px;display:flex;overflow:hidden}.ia-editor-problems{background:var(--ia-editor-background,var(--background-base-lowest));z-index:5;width:100%;height:100%;position:absolute;top:0;left:0;overflow:hidden scroll}.ia-editor-problem{color:var(--ia-editor-foreground,var(--text-default));gap:8px;padding:3px 4px 0;display:inline-flex}.ia-editor-problem.ia-editor-severity-8 svg{color:var(--background-feedback-critical)}.ia-editor-problem.ia-editor-severity-4 svg,.ia-editor-problem.ia-editor-severity-2 svg,.ia-editor-problem.ia-editor-severity-1 svg{color:var(--background-feedback-info)}.ia-editor-problem>:nth-child(2)~span{opacity:.7}.ia-editor-no-problems{color:var(--ia-editor-foreground,var(--text-default));padding:4px}.ia-editor-footer-right .codicon{cursor:pointer;text-overflow:ellipsis;white-space:pre;outline-width:0;align-items:center;height:100%;padding:0 3px;display:flex;overflow:hidden}";

  // src/InAccord/styles/ui/installmodal.css
  var installmodal_default = ".ia-install-modal-guild{--guild-size:32px;width:var(--guild-size);height:var(--guild-size);background-color:var(--background-base-low);white-space:nowrap;border-radius:6px;justify-content:center;align-items:center;font-weight:500;line-height:1.2em;transition:background-color .15s ease-out,color .15s ease-out;display:flex;position:relative;overflow:hidden}.ia-install-modal-guild img{width:var(--guild-size);height:var(--guild-size)}.ia-install-modal-item:hover .ia-install-modal-guild{color:var(--white);background-color:var(--background-brand)}.ia-install-modal-header{align-items:center;gap:8px;margin:0 20px;display:flex}.ia-install-modal-items{background:var(--background-base-lower);border-radius:8px;margin:20px;padding:12px}.ia-install-modal-item{color:var(--text-default);gap:12px;padding:12px}.ia-install-modal-items>.ia-install-modal-item:not(:last-child){border-bottom:1px solid var(--border-subtle)}.ia-install-modal-item-leading{min-width:24px;max-width:24px;display:flex}.ia-install-modal-item-trialing{display:flex}.ia-install-modal-item-content{width:100%}.ia-install-modal-clickable{cursor:pointer;background-color:#0000;border-radius:3px;transition:background-color .1s}.ia-install-modal-clickable:hover{color:var(--interactive-text-hover);background:var(--ia-overlay-hover,var(--interactive-background-hover))}.ia-install-modal-author-svg{contain:paint}.ia-install-modal-author-img{width:100%;height:100%}.ia-install-modal-splash{height:197px;margin-bottom:32px;display:block;position:relative;overflow:visible}.ia-install-modal-preview{width:100%;height:100%;display:block;position:absolute;top:0;left:0}.ia-install-modal-preview-img{object-fit:cover;border-radius:3px 3px 0 0;width:100%;height:100%}.ia-install-modal-author{position:absolute;bottom:-21px;left:12px}.ia-install-modal-author-mask{background-color:var(--background-base-low);padding:4px}";

  // src/InAccord/styles/ui/keybind.css
  var keybind_default = ".ia-keybind-wrap{box-sizing:border-box;border-radius:var(--radius-sm);background-color:var(--input-background-default);border:1px solid var(--input-border-default);cursor:pointer;align-items:center;gap:5px;min-width:250px;height:40px;padding:0 4px;display:flex;position:relative}.ia-keybind-wrap.ia-keybind-disabled{cursor:not-allowed;opacity:.5}.ia-keybind-wrap.ia-keybind-disabled .ia-keybind-record,.ia-keybind-wrap.ia-keybind-disabled .ia-keybind-clear,.ia-keybind-wrap.ia-keybind-disabled .ia-keybind-input{pointer-events:none}.ia-keybind-wrap input{pointer-events:none;color:var(--text-default);text-transform:uppercase;background:0 0;border:none;outline:none;flex:1;font-size:16px;font-weight:600}.ia-keybind-wrap input::placeholder{text-transform:capitalize}.ia-keybind-wrap.recording,.ia-keybind-wrap:not(.ia-keybind-disabled):hover{border-color:hsl(var(--red-400-hsl)/.3)}.ia-keybind-wrap.recording{box-shadow:0 0 6px hsl(var(--red-400-hsl)/.3)}.ia-keybind-wrap.recording input{color:var(--status-danger)}.ia-keybind-wrap .ia-keybind-record{padding:3px 8px}.ia-keybind-wrap .ia-keybind-record.ia-button-color-red svg{color:var(--interactive-text-active)}.ia-keybind-clear{opacity:.5;margin-left:5px;padding-right:4px!important}.ia-keybind-clear:hover{opacity:1}.ia-keybind-clear svg{width:18px!important;height:18px!important}";

  // src/InAccord/styles/ui/managerhub.css
  var managerhub_default = ".ia-manager-hub{flex-direction:column;gap:12px;display:flex}.ia-manager-hub-heading{color:var(--header-primary);margin:0;font-size:24px;font-weight:700;line-height:1.2}.ia-manager-hub-description{margin-bottom:2px}.ia-manager-hub-grid{grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:10px;display:grid}.ia-manager-hub-card{border:1px solid var(--app-border-frame);background:var(--background-surface-high);border-radius:8px;flex-direction:column;align-items:flex-start;gap:10px;padding:12px;display:flex}.ia-manager-hub-card-header{align-items:center;gap:8px;display:flex}.ia-manager-hub-icon{color:var(--interactive-normal);justify-content:center;align-items:center;display:flex}.ia-manager-hub-card-actions{flex-wrap:wrap;gap:8px;display:flex}.ia-manager-hub-backup{border:1px solid var(--app-border-frame);background:var(--background-surface-high);border-radius:8px;justify-content:space-between;align-items:center;gap:8px;padding:10px 12px;display:flex}";

  // src/InAccord/styles/ui/modal.css
  var modal_default = ".ia-modal-wrapper{z-index:1000;width:100vw;height:100vh;position:absolute}.ia-backdrop{background:#0009;width:100%;height:100%;position:absolute}.ia-modal{width:33%;position:absolute;top:50%;left:50%;transform:translate(-50%,-50%)}.ia-modal-inner{background:var(--background-base-low);border-radius:var(--radius-md);animation:.3s ease-out ia-modal-open;overflow:hidden}.ia-modal-wrapper.closing .ia-modal-inner{animation:.3s ease-in ia-modal-close}.ia-modal .footer{background:var(--background-base-lower);justify-content:flex-end;padding:15px;display:flex}.ia-modal-body{padding:0 15px 20px}.ia-modal .header{padding:15px}.ia-modal .title{color:#fff;font-size:22px;font-weight:600}.ia-modal-body{color:#fff}.ia-modal .footer .ia-button{min-width:80px;height:38px}@keyframes ia-modal-close{to{transform:scale(.7)}}@keyframes ia-modal-open{0%{transform:scale(.7)}}.ia-modal-root{background-color:var(--modal-background);border-radius:var(--radius-md);border:1px solid var(--border-normal);pointer-events:all;flex-direction:column;max-height:100%;margin:0 auto;display:flex;position:relative}.ia-close-button{opacity:.5;cursor:pointer;height:26px;color:var(--interactive-text-default);box-sizing:content-box;border-radius:3px;padding:4px;transition:opacity .2s ease-in-out}.ia-close-button:hover{opacity:1;color:var(--interactive-text-hover)}.ia-modal-small{width:440px;min-height:200px;max-height:720px}.ia-modal-standard{white-space:pre-wrap;word-wrap:break-word;width:490px;max-height:min(800px,60vh);font-size:13px}.ia-modal-medium{width:600px;min-height:400px;max-height:min(800px,60vh)}.ia-modal-large{width:800px;min-height:400px;max-height:min(960px,70vh)}.ia-addon-modal{min-height:0}.ia-modal-header,.ia-modal-footer{z-index:1;flex:none;padding:16px;position:relative;overflow-x:hidden}.ia-modal-header{border-radius:var(--radius-md) var(--radius-md) 0 0;word-wrap:break-word;transition:box-shadow .1s ease-out}.ia-modal-footer{border-radius:0 0 var(--radius-md) var(--radius-md);background-color:var(--modal-footer-background);overflow:hidden}.ia-modal-content{z-index:0;border-radius:var(--radius-md) var(--radius-md) 0 0;color:var(--text-default);padding-left:16px;padding-right:8px;font-size:16px;line-height:20px;position:relative;overflow:hidden scroll}.ia-modal-backdrop{top:0;right:var(--devtools-sidebar-width,0);pointer-events:all;position:fixed;bottom:0;left:0;-webkit-transform:translateZ(0);transform:translateZ(0)}#ia-modal-contianer{top:0;left:0;right:var(--devtools-sidebar-width,0);pointer-events:none;z-index:1002;position:absolute;bottom:0;background:0 0!important}.ia-modal-layer{-webkit-box-orient:vertical;-webkit-box-direction:normal;-ms-flex-direction:column;flex-direction:column;justify-content:center;align-items:center;min-height:0;padding-top:40px;padding-bottom:40px;display:-webkit-box;display:-ms-flexbox;display:flex;position:absolute;inset:0}";

  // src/InAccord/styles/ui/notices.css
  var notices_default = ".ia-notice-success{--color:#3ba55d}.ia-notice-error{--color:#ed4245}.ia-notice-info{--color:#4a8fe1}.ia-notice-warning{--color:#faa81a}.ia-notice-closing{transition:height .4s;height:0!important}@keyframes ia-open-notice{0%{height:0}}#ia-notices{grid-area:notice}.ia-notice{text-align:center;z-index:101;height:36px;box-shadow:var(--elevation-low);color:#fff;background:var(--color,var(--brand-experiment-600,#3c45a5));flex-grow:0;flex-shrink:0;padding-left:4px;padding-right:28px;font-size:14px;font-weight:500;line-height:36px;animation:.4s ia-open-notice;position:relative;overflow:hidden}.ia-notice:first-child{border-radius:8px 0 0}.ia-notice-close{opacity:.5;cursor:pointer;-webkit-app-region:no-drag;background:url(https://discord.com/assets/7731c77d99babca1a8faec204d98c380.svg) 50% 55%/10px 10px no-repeat;width:36px;height:36px;transition:opacity .2s;position:absolute;top:0;right:0}.ia-notice-button{color:#fff;box-sizing:border-box;vertical-align:top;-webkit-app-region:no-drag;background:0 0;border:1px solid #fff;border-radius:3px;height:24px;margin-left:10px;padding:0 10px;font-size:14px;font-weight:500;line-height:22px;transition:background-color .2s,color .2s,border-color .2s;display:inline-block;position:relative;top:6px}.ia-notice-button:hover{color:var(--color,var(--background-mobile-secondary));background:#fff}.ia-notice-close:hover{opacity:1}";

  // src/InAccord/styles/ui/notifications.css
  var notifications_default3 = "#ia-notifications-root{z-index:9999;pointer-events:none;padding:16px;display:flex;position:fixed}#ia-notifications-root>*{pointer-events:auto}.ia-notification-contianer{z-index:3002;flex-direction:column;gap:8px;display:flex;position:fixed}.ia-notification{background-color:var(--card-background-default);border:1px solid var(--border-subtle);border-radius:var(--radius-sm);box-shadow:var(--shadow-medium);flex-direction:column;width:350px;margin-bottom:20px;display:flex;position:relative}.ia-notification [class*=-markdown] [class*=-paragraph]:last-child{margin-bottom:0!important}.ia-notification-top-left{flex-direction:column;top:16px;left:16px}.ia-notification-top-right{flex-direction:column;top:16px;right:16px}.ia-notification-bottom-right{flex-direction:column-reverse;bottom:16px;right:16px}.ia-notification-bottom-left{flex-direction:column-reverse;bottom:16px;left:16px}.ia-notification-content{border-radius:8px 8px 0 0;flex-direction:column;padding:16px 40px 0 16px;display:flex}.ia-notification-icon{filter:saturate(var(--saturation-factor,1));color:var(--text-default);flex-shrink:0;margin-right:6px}.ia-notification-topbar{place-content:center space-between;align-items:center;margin-left:3px;padding:10px 10px 0;font-size:16px;font-weight:700;display:inline-flex}.ia-notification-header{align-items:flex-start;display:flex}.ia-notification-body{margin-top:4px}.ia-notification-title{color:var(--text-default);font-family:var(--font-display);word-wrap:break-word;word-break:break-word;overflow-wrap:break-word;flex:1;font-size:16px;font-weight:600;line-height:1.25}.ia-notification-content-text{color:var(--text-default);font-family:var(--font-display);word-wrap:break-word;word-break:break-word;overflow-wrap:break-word;font-size:14px;font-weight:400;line-height:1.28571}.ia-notification-close{color:var(--interactive-text-default);cursor:pointer;opacity:.5;margin:-10px;padding:10px;position:absolute;top:12px;right:12px}.ia-notification-close:hover{color:var(--interactive-text-hover)}.ia-notification-updates-list{margin-top:8px;margin-bottom:0}.ia-notification-footer{background-color:var(--card-background-default);border-radius:0 0 8px 8px;flex-wrap:wrap;gap:8px;padding:10px 16px 0;display:flex}.ia-notification-action{background-color:var(--control-secondary-background-default);color:var(--control-secondary-text-default);cursor:pointer;border:none;border-radius:8px;flex:1;min-width:calc(33.3333% - 8px);padding:8px 16px;font-size:14px;transition:background-color .2s ease-in-out}.ia-notification-action:hover{background-color:var(--control-secondary-background-hover)}.ia-notification-progress{border-bottom-right-radius:12px;border-bottom-left-radius:12px;height:4px;margin-top:14px;transition:width .1s linear}.ia-notification-enter{animation:.5s ease-out forwards slideIn}.ia-notification-exit{animation:.5s ease-in forwards slideOut}@keyframes slideIn{0%{opacity:0;transform:translate(100%)}to{opacity:1;transform:translate(0)}}@keyframes slideOut{0%{opacity:1;transform:translate(0)}to{opacity:0;transform:translate(100%)}}.ia-contianer{background:var(--background-base-lower);border:3px solid var(--brand-500);color:var(--text-default);border-radius:8px;width:500px;height:300px;margin:50px auto;position:relative}.ia-contianer-disabled{background:var(--background-base-lower);border:3px solid var(--red-400);color:var(--text-default);border-radius:8px;width:500px;height:300px;margin:50px auto;position:relative}.position-wrapper{width:500px}.ia-box{background-color:var(--primary-400);cursor:pointer;border-radius:3px;width:150px;height:80px;margin:6px;transition:background-color .3s;position:absolute}.ia-box:hover{background-color:var(--brand-500);opacity:.8}.ia-box.selected{background-color:var(--brand-500)}.ia-box-disabled{background-color:var(--primary-400);opacity:.5;border-radius:3px;width:150px;height:80px;margin:6px;transition:background-color .3s;position:absolute;cursor:not-allowed!important}.ia-box-disabled:hover{background-color:var(--brand-500);opacity:.8}.ia-box-disabled.selected{background-color:var(--brand-500)}.ia-center-svg{width:100px;height:100px;position:absolute;top:50%;left:50%;transform:translate(-50%,-50%)}.top-left{top:0;left:0}.top-right{top:0;right:0}.bottom-left{bottom:0;left:0}.bottom-right{bottom:0;right:0}.ia-center-text{text-align:center;position:absolute;top:50%;left:50%;transform:translate(-50%,-50%)}.ia-text{color:var(--text-default);margin:0}.ia-p{color:var(--text-default);margin:10px 0 0}";

  // src/InAccord/styles/ui/number.css
  var number_default = ".ia-number-input-wrapper{display:flex}.ia-number-input::-webkit-inner-spin-button,.ia-number-input::-webkit-outer-spin-button{display:none}.ia-number-input{box-sizing:border-box;background-color:var(--input-background-default);border:1px solid var(--input-border-default);color:var(--text-default);text-align:center;padding:8px 12px;font-size:16px}.ia-number-input:focus-within{border-color:var(--ia-brand)}.ia-number-input:not([min][max]){width:65px}.ia-button.ia-number-input-decrement{border-right:unset;border-radius:var(--radius-sm) 0 0 var(--radius-sm)}.ia-button.ia-number-input-increment{border-left:unset;border-radius:0 var(--radius-sm) var(--radius-sm) 0}.ia-number-input-wrapper.ia-number-input-disabled{cursor:not-allowed;opacity:.5}.ia-number-input-wrapper.ia-number-input-disabled .ia-number-input,.ia-number-input-wrapper.ia-number-input-disabled .ia-button.ia-number-input-decrement,.ia-number-input-wrapper.ia-number-input-disabled .ia-button.ia-number-input-increment{pointer-events:none}";

  // src/InAccord/styles/ui/paginator.css
  var paginator_default = ".ia-paginator{justify-content:space-between;align-items:center;margin:auto;display:flex}.ia-paginator-bubbles{justify-content:center;align-items:center;display:flex}.ia-paginator-back,.ia-paginator-next{padding:0 8px}#app-mount :where(.ia-paginator-back,.ia-paginator-next),.ia-paginator-bubble{box-sizing:border-box;cursor:pointer;width:min-content;min-width:28px;height:28px;min-height:28px;color:var(--text-strong);justify-content:center;align-items:center;margin:4px;padding:6px;font-weight:600;display:flex}:where(.ia-paginator-back,.ia-paginator-next):disabled{cursor:not-allowed;opacity:.5}#app-mount :where(.ia-paginator-back,.ia-paginator-next):hover{color:var(--interactive-text-hover);background-color:var(--background-base-lower)}.ia-paginator-back,.ia-paginator-next{padding-right:12px}.ia-paginator-bubble:where([data-selected=true],:hover){color:var(--white-500);background-color:var(--brand-500)}.ia-paginator-bubble{border-radius:14px;min-width:28px}";

  // src/InAccord/styles/ui/radio.css
  var radio_default = ".ia-radio-group{min-width:300px}.ia-radio-option{cursor:pointer;user-select:none;border-radius:var(--radius-sm);color:var(--text-primary);border-left:3px solid #0000;align-items:center;gap:8px;margin-bottom:4px;padding:12px 16px;display:flex}.ia-radio-group.ia-radio-disabled{cursor:not-allowed;opacity:.5}.ia-radio-group.ia-radio-disabled .ia-radio-option{pointer-events:none}.ia-radio-option:hover{background-color:var(--background-mod-subtle);color:var(--interactive-text-hover)}.ia-radio-option.ia-radio-selected{background-color:var(--background-mod-subtle);color:var(--interactive-text-active)}.ia-radio-option input{opacity:0;cursor:pointer;width:0;height:0;position:absolute}.ia-radio-indicator{border-radius:50%}.ia-radio-icon{stroke:var(--checkbox-border-default)}.ia-radio-selected .ia-radio-icon{stroke:var(--ia-brand);fill:var(--ia-brand)}.ia-radio-label-wrap{flex-direction:column;display:flex}.ia-radio-label{font-family:var(--font-primary);font-size:16px;font-weight:500;line-height:20px}.ia-radio-description{font-family:var(--font-primary);font-size:14px;font-weight:400;line-height:18px}";

  // src/InAccord/styles/ui/scroller.css
  var scroller_default = ".ia-scroller-base{box-sizing:border-box;flex:auto;min-height:0;position:relative}@supports not selector(::-webkit-scrollbar){.ia-scroller-thin{scrollbar-width:thin;scrollbar-color:var(--scrollbar-thin-thumb) var(--scrollbar-thin-track)}}.ia-scroller-thin::-webkit-scrollbar-track{border-color:var(--scrollbar-thin-track);background-color:var(--scrollbar-thin-track);border:2px solid var(--scrollbar-thin-track)}.ia-scroller-thin::-webkit-scrollbar{width:8px;height:8px}.ia-scroller-thin::-webkit-scrollbar-corner{background-color:#0000}.ia-scroller-thin::-webkit-scrollbar-thumb{background-clip:padding-box;background-color:var(--scrollbar-thin-thumb);border:2px solid #0000;border-radius:4px;min-height:40px}";

  // src/InAccord/styles/ui/sidebar.css
  var sidebar_default = ".ia-sidebar-header{background-image:url(/Images/splash.jpg);background-position:50%;background-size:cover;justify-content:space-between;align-items:center;height:64px;padding:8px 12px;display:flex}.ia-sidebar-header h2{box-sizing:border-box;color:#96989d;white-space:nowrap;text-overflow:ellipsis;text-transform:uppercase;letter-spacing:.02em;flex-shrink:0;padding:6px 10px;font-size:12px;font-weight:700;display:flex;overflow:hidden}.ia-sidebar-header .ia-changelog-button{height:16px;padding:0}.ia-sidebar-header .ia-icon{cursor:pointer;color:var(--interactive-text-default)}.ia-sidebar-header .ia-changelog-button:hover .ia-icon{color:var(--interactive-text-hover)}";

  // src/InAccord/styles/ui/slider.css
  var slider_default = '.ia-setting-item:not(.inline) .ia-slider-wrap{margin-top:10px}.ia-slider-wrap{color:var(--text-default);flex-direction:column;justify-content:center;align-items:center;margin:0 10px;display:flex;position:relative}.ia-slider-wrap.ia-slider-markers{margin-bottom:10px;padding-bottom:10px}.ia-slider-wrap.ia-slider-disabled{cursor:not-allowed;opacity:.5}.ia-slider-wrap.ia-slider-disabled>.ia-slider-input,.ia-slider-wrap.ia-slider-disabled>.ia-slider-label,.ia-slider-wrap.ia-slider-disabled>.ia-slider-track,.ia-slider-wrap.ia-slider-disabled>.ia-slider-marker-contianer,.ia-slider-wrap.ia-slider-disabled>.ia-slider-input::-webkit-slider-thumb{pointer-events:none}.ia-slider-label{opacity:0;pointer-events:none;background:#000;border-radius:5px;padding:5px 7px;font-weight:700;position:absolute;top:-45px}.ia-slider-input:hover+.ia-slider-label{opacity:1}.ia-slider-input{appearance:none;pointer-events:none;z-index:2;background:0 0;outline:none;width:100%;position:absolute}.ia-slider-input::-webkit-slider-thumb{appearance:none;background-color:hsl(0,calc(var(--saturation-factor,1)*0%),100%);border:1px solid hsl(210,calc(var(--saturation-factor,1)*2.9%),86.7%);width:10px;height:24px;-webkit-box-shadow:0 3px 1px 0 hsla(0,calc(var(--saturation-factor,1)*0%),0%,.05),0 2px 2px 0 hsla(0,calc(var(--saturation-factor,1)*0%),0%,.1),0 3px 3px 0 hsla(0,calc(var(--saturation-factor,1)*0%),0%,.05);box-shadow:0 3px 1px 0 hsla(0,calc(var(--saturation-factor,1)*0%),0%,.05),0 2px 2px 0 hsla(0,calc(var(--saturation-factor,1)*0%),0%,.1),0 3px 3px 0 hsla(0,calc(var(--saturation-factor,1)*0%),0%,.05);cursor:ew-resize;pointer-events:all;border-radius:3px;top:50%}.ia-slider-track{background:hsl(217,calc(var(--saturation-factor,1)*7.6%),33.5%);background-image:linear-gradient(var(--ia-brand), var(--ia-brand));cursor:pointer;z-index:1;background-repeat:no-repeat;background-size:70% 100%;border-radius:5px;width:100%;min-width:350px;height:8px;transition:opacity .2s}.ia-slider-marker-contianer{justify-content:space-between;width:98%;display:flex;position:absolute;bottom:0}.ia-slider-marker{cursor:pointer;font-size:12px;position:absolute;transform:translate(-50%)}.ia-slider-marker:before{content:"";z-index:-1;background:#fff3;width:2px;height:24px;position:absolute;top:-26px;left:calc(50% - 1px)}.ia-setting-item.inline:first-child:has(.ia-slider-wrap){padding-top:50px}';

  // src/InAccord/styles/ui/switch.css
  var switch_default = '.ia-switch{transition:filter .25s cubic-bezier(0,.3,.7,1),opacity .25s cubic-bezier(0,.3,.7,1);position:relative}.ia-switch-body{--switch-color:#72767d;--symbol1path:path("M5.13231 6.72963L6.7233 5.13864L14.855 13.2704L13.264 14.8614L5.13231 6.72963Z");--symbol2path:path("M13.2704 5.13864L14.8614 6.72963L6.72963 14.8614L5.13864 13.2704L13.2704 5.13864Z");background-color:var(--switch-color);border-radius:14px;width:40px;height:24px;transition:background-color .25s cubic-bezier(0,.3,.7,1);position:relative}.ia-switch input:active+.ia-switch-body{--switch-color:var(--ia-brand)}.ia-switch input:checked+.ia-switch-body{--switch-color:var(--ia-brand);--symbol1path:path("M7.89561 14.8538L6.30462 13.2629L14.3099 5.25755L15.9009 6.84854L7.89561 14.8538Z");--symbol2path:path("M4.08643 11.0903L5.67742 9.49929L9.4485 13.2704L7.85751 14.8614L4.08643 11.0903Z")}.full-motion .ia-switch input:active+.ia-switch-body{--symbol1path:path("M6.56666 11.0013L6.56666 8.96683L13.5667 8.96683L13.5667 11.0013L6.56666 11.0013Z");--symbol2path:path("M13.5582 8.96683L13.5582 11.0013L6.56192 11.0013L6.56192 8.96683L13.5582 8.96683Z")}.ia-switch input:checked:active+.ia-switch-body{--switch-color:var(--ia-brand)}.full-motion .ia-switch input:checked:active+.ia-switch-body{--symbol1path:path("M6.56666 11.0013L6.56666 8.96683L13.5667 8.96683L13.5667 11.0013L6.56666 11.0013Z");--symbol2path:path("M13.5582 8.96683L13.5582 11.0013L6.56192 11.0013L6.56192 8.96683L13.5582 8.96683Z")}.ia-switch .ia-switch-symbol path{fill:var(--switch-color);transition:all .25s cubic-bezier(0,.3,.7,1)}.ia-switch .ia-switch-symbol path:first-child{d:var(--symbol1path)}.ia-switch .ia-switch-symbol path:last-child{d:var(--symbol2path)}.ia-switch input{z-index:1;opacity:0;cursor:pointer;border-radius:100px;width:100%;height:100%;margin:0;position:absolute}.ia-switch-body .ia-switch-slider{width:28px;height:18px;margin:3px;transition:all .25s cubic-bezier(0,.3,.7,1);display:block;position:absolute;left:-3px}.ia-switch input:active+.ia-switch-body .ia-switch-slider{left:1px}.ia-switch input:checked+.ia-switch-body .ia-switch-slider{left:12px}.ia-switch input:active:checked+.ia-switch-body .ia-switch-slider{left:8px}.ia-switch-body .ia-switch-slider .ia-switch-handle{transition:all .25s cubic-bezier(0,.3,.7,1);position:relative}.ia-switch input:active+.ia-switch-body .ia-switch-handle{y:1;x:0;width:28px;height:18px}.ia-switch input:checked:active+.ia-switch-body .ia-switch-handle{x:0}.ia-switch-disabled{opacity:.5;filter:grayscale()}.ia-switch-disabled input{cursor:not-allowed}';

  // src/InAccord/styles/ui/tabbar.css
  var tabbar_default = ".ia-tab-bar{flex-direction:row;display:flex}.ia-tab-item{text-align:center;-webkit-app-region:no-drag;cursor:pointer;white-space:nowrap;text-overflow:ellipsis;min-width:40px;color:var(--interactive-text-default);border-radius:4px;flex-shrink:0;justify-content:center;align-items:center;margin-right:16px;padding:2px 8px;font-size:16px;font-weight:500;line-height:20px;display:flex;position:relative;overflow:hidden}.ia-tab-item:last-child{margin:0}.ia-tab-item:hover,.ia-tab-item.selected{color:var(--interactive-text-active)}.ia-tab-item.selected{cursor:default;background-color:var(--interactive-background-selected)}.ia-tab-item:hover{background-color:var(--interactive-background-hover)}.ia-tab-item:active{background-color:var(--interactive-background-active)}";

  // src/InAccord/styles/ui/textbox.css
  var textbox_default = ".ia-text-input{box-sizing:border-box;border-radius:var(--radius-sm);min-width:250px;color:var(--text-default);background-color:var(--input-background-default);border:1px solid var(--input-border-default);height:40px;padding:10px;font-size:16px}.ia-text-input:focus-within{border-color:var(--ia-brand)}.ia-text-input:disabled{cursor:not-allowed;opacity:.5}";

  // src/InAccord/styles/ui/toasts.css
  var toasts_default2 = "#ia-toasts{pointer-events:none;z-index:1003;flex-direction:column;justify-content:flex-end;align-items:center;gap:10px;padding-bottom:40px;display:flex;position:absolute;inset:0}.ia-toast{--toast-bg:hsl(218, calc(var(--saturation-factor,1)* 14%), 15%);--toast-fg:var(--text-default);pointer-events:none;-webkit-user-select:none;background-color:var(--toast-bg);border-radius:var(--radius-xl);box-shadow:var(--shadow-high);color:var(--toast-fg);flex-shrink:0;align-items:center;padding:5px;font-size:16px;font-weight:500;display:flex;overflow:hidden}.ia-toast span,.ia-toast svg{padding:5px}.ia-toast svg{width:20px;height:20px;stroke:var(--toast-fg);padding-right:unset}.ia-toast.toast-info{--toast-bg:hsl(219, calc(var(--saturation-factor,1)* 29%), 13%);--toast-fg:hsl(216, calc(var(--saturation-factor,1)* 76%), 57%)}.ia-toast.toast-success{--toast-bg:hsl(168, calc(var(--saturation-factor,1)* 24%), 12%);--toast-fg:hsl(145, calc(var(--saturation-factor,1)* 54%), 49%)}.ia-toast.toast-warning{--toast-bg:hsl(37, calc(var(--saturation-factor,1)* 22%), 14%);--toast-fg:hsl(43, calc(var(--saturation-factor,1)* 96%), 56%)}.ia-toast.toast-error{--toast-bg:hsl(337, calc(var(--saturation-factor,1)* 21%), 12%);--toast-fg:hsl(0, calc(var(--saturation-factor,1)* 54%), 60%)}";

  // src/InAccord/styles/ui/tooltip.css
  var tooltip_default = ".ia-layer{position:absolute}.ia-tooltip{box-sizing:border-box;word-wrap:break-word;z-index:1002;will-change:opacity, transform;max-width:190px;box-shadow:var(--elevation-high);color:var(--text-strong);border-radius:5px;font-size:14px;font-weight:500;line-height:16px;position:relative}.ia-tooltip-content{padding:8px 12px;overflow:hidden}.ia-tooltip-pointer{pointer-events:none;border:5px solid #0000;width:0;height:0}.ia-tooltip-primary{background-color:var(--background-base-low);color:var(--text-default)}.ia-tooltip-primary .ia-tooltip-pointer{border-top-color:var(--background-base-lowest)}.ia-tooltip-info{background-color:#4a90e2}.ia-tooltip-info .ia-tooltip-pointer{border-top-color:#4a90e2}.ia-tooltip-success{background-color:#43b581}.ia-tooltip-success .ia-tooltip-pointer{border-top-color:#43b581}.ia-tooltip-danger{background-color:#f04747}.ia-tooltip-danger .ia-tooltip-pointer{border-top-color:#f04747}.ia-tooltip-warn{background-color:#ffa600}.ia-tooltip-warn .ia-tooltip-pointer{border-top-color:#ffa600}.ia-tooltip-top .ia-tooltip-pointer{margin-left:-5px;position:absolute;top:100%;left:50%}.ia-tooltip-bottom .ia-tooltip-pointer{margin-left:-5px;position:absolute;bottom:100%;left:50%;transform:rotate(180deg)}.ia-tooltip-right .ia-tooltip-pointer{border-left-width:5px;margin-top:-5px;position:absolute;top:50%;right:100%;transform:rotate(90deg)}.ia-tooltip-left .ia-tooltip-pointer{border-left-width:5px;margin-top:-5px;position:absolute;top:50%;left:100%;transform:rotate(270deg)}";

  // src/InAccord/styles/ui/updater.css
  var updater_default = ".ia-filled-checkmark{color:var(--interactive-text-active);background:#43b581;border-radius:50%;justify-content:center;align-items:center;margin-right:4px;padding:3px;display:flex}.ia-empty-updates{color:var(--text-strong);flex-direction:column;justify-content:center;align-items:center;font-size:16px;font-weight:500;display:flex}.ia-empty-updates svg{color:#43b581;margin-bottom:20px}.ia-button.ia-button-icon svg{color:var(--interactive-text-default)}.ia-button.ia-button-icon:hover svg{color:var(--interactive-text-hover)}.ia-button.ia-button-icon.animate{animation:.5s linear infinite ia-update-spin}.ia-button.ia-button-icon.no-animate{animation:none!important}.ia-settings-group-title .ia-button-icon{margin-left:3px;margin-right:-8px}@keyframes ia-update-spin{0%{transform:rotate(0)}50%{transform:rotate(180deg)}to{transform:rotate(360deg)}}";

  // src/InAccord/styles/blankslates/emptyimage.css
  var emptyimage_default = ".ia-empty-image-contianer{flex-direction:column;align-items:center;max-width:340px;margin:150px auto 0;display:flex}.ia-empty-image-title{color:var(--text-strong);text-align:center;margin:16px 0 8px;font-size:24px;font-weight:600}.ia-empty-image-message{color:var(--text-default);text-align:center;margin-bottom:16px}";

  // src/InAccord/styles/blankslates/noresults.css
  var noresults_default = ".ia-empty-results{color:var(--text-default);flex-direction:column;align-items:center;margin-top:100px;font-size:16px;line-height:24px;display:flex}.ia-empty-results-text{text-align:center;width:280px;margin-top:40px}";

  // src/InAccord/styles/buttons.css
  var buttons_default = ".ia-button{box-sizing:border-box;border-radius:var(--radius-sm);-webkit-user-select:none;background:0 0;border:none;justify-content:center;align-items:center;padding:2px 16px;font-size:14px;font-weight:500;line-height:16px;display:flex;position:relative}.ia-button:disabled{cursor:not-allowed;opacity:.5}.ia-button .ia-button-content{--button--underline-color:transparent;background-image:linear-gradient(0deg, transparent, transparent 1px, var(--button--underline-color) 0, var(--button--underline-color) 2px, transparent 0);justify-content:center;align-items:center;display:flex}.ia-button:disabled .ia-button-content{background-image:none!important}.ia-button-outlined:disabled{background-color:#0000!important}.ia-button-tiny{width:52px;min-width:52px;height:24px;min-height:24px}.ia-button-small{width:60px;min-width:60px;height:32px;min-height:32px}.ia-button-medium{width:96px;min-width:96px;height:38px;min-height:38px}.ia-button-large{width:130px;min-width:130px;height:44px;min-height:44px}.ia-button-xlarge{width:148px;min-width:148px;height:50px;min-height:50px;padding:2px 20px;font-size:16px;line-height:normal}.ia-button-icon{height:auto;padding:4px}.ia-button-grow,.ia-button-icon{width:auto}.ia-button-filled{-webkit-transition:background-color .17s,color .17s;transition:background-color .17s,color .17s}.ia-button-outlined{border-style:solid;border-width:1px;-webkit-transition:color .17s,background-color .17s,border-color .17s;transition:color .17s,background-color .17s,border-color .17s}.ia-button-blank{color:currentColor;background:0 0;border:0;margin:0}.ia-button-filled .ia-button-content,.ia-button-link .ia-button-content,.ia-button-outlined .ia-button-content{white-space:nowrap;text-overflow:ellipsis;margin:0 auto;overflow:hidden}.ia-button-filled.ia-button-color-brand{color:var(--white-500);background-color:var(--ia-brand)}.ia-button-filled.ia-button-color-brand:hover{background-color:var(--ia-brand-hover)}.ia-button-filled.ia-button-color-brand:active{background-color:var(--ia-brand-active)}.ia-button-filled.ia-button-color-brand:disabled{background-color:var(--ia-brand);opacity:.4}.ia-button-outlined.ia-button-color-brand{color:var(--control-secondary-text-default);border-color:var(--control-secondary-border-default)}.ia-button-outlined.ia-button-color-brand:hover{background-color:var(--button-outline-brand-background-hover);border-color:var(--control-secondary-border-hover);color:var(--control-secondary-text-hover)}.ia-button-outlined.ia-button-color-brand:active{background-color:var(--control-secondary-background-active);border-color:var(--button-outline-brand-border-active);color:var(--control-secondary-text-active)}.ia-button-link.ia-button-color-brand{color:var(--ia-brand)}.ia-button-link.ia-button-color-brand:hover .ia-button-content{--button--underline-color:var(--ia-brand)}.ia-button-filled.ia-button-color-blurple{color:var(--white-500);background-color:var(--brand-experiment)}.ia-button-filled.ia-button-color-blurple:hover{background-color:var(--brand-experiment-560)}.ia-button-filled.ia-button-color-blurple:active{background-color:var(--brand-experiment-600)}.ia-button-filled.ia-button-color-blurple:disabled{background-color:var(--brand-experiment)}.ia-button-outlined.ia-button-color-blurple{color:var(--control-secondary-text-default);border-color:var(--control-secondary-border-default)}.ia-button-outlined.ia-button-color-blurple:hover{background-color:var(--button-outline-brand-background-hover);border-color:var(--control-secondary-border-hover);color:var(--control-secondary-text-hover)}.ia-button-outlined.ia-button-color-blurple:active{background-color:var(--control-secondary-background-active);border-color:var(--button-outline-brand-border-active);color:var(--control-secondary-text-active)}.ia-button-link.ia-button-color-blurple{color:var(--brand-experiment)}.ia-button-link.ia-button-color-blurple:hover .ia-button-content{--button--underline-color:var(--brand-experiment)}.ia-button-filled.ia-button-color-yellow{color:var(--white-500);background-color:var(--status-warning)}.ia-button-filled.ia-button-color-yellow:active,.ia-button-filled.ia-button-color-yellow:hover{background-color:null}.ia-button-filled.ia-button-color-yellow:disabled{background-color:var(--status-warning)}.ia-button-outlined.ia-button-color-yellow{color:var(--status-warning);border-color:var(--status-warning)}.ia-button-outlined.ia-button-color-yellow:active{background-color:hsl(var(--yellow-300-hsl)/.1)}.ia-button-link.ia-button-color-yellow{color:var(--status-warning)}.ia-button-link.ia-button-color-yellow:hover .ia-button-content{--button--underline-color:var(--status-warning)}.ia-button-filled.ia-button-color-link{color:var(--white-500);background-color:var(--text-link)}.ia-button-filled.ia-button-color-link:active,.ia-button-filled.ia-button-color-link:hover{background-color:null}.ia-button-filled.ia-button-color-link:disabled{background-color:var(--text-link)}.ia-button-outlined.ia-button-color-link{color:var(--text-link);border-color:var(--text-link)}.ia-button-outlined.ia-button-color-link:active{background-color:hsl(var(--text-link-hsl)/.1)}.ia-button-link.ia-button-color-link{color:var(--text-link)}.ia-button-link.ia-button-color-link:hover .ia-button-content{--button--underline-color:var(--text-link)}.ia-button-filled.ia-button-color-white{color:var(--primary-500);background-color:var(--white-500)}.ia-button-filled.ia-button-color-white:active,.ia-button-filled.ia-button-color-white:hover{background-color:null}.ia-button-filled.ia-button-color-white:disabled{background-color:var(--white-500)}.ia-button-outlined.ia-button-color-white{color:var(--white-500);border-color:var(--white-500)}.ia-button-outlined.ia-button-color-white:active{background-color:hsl(var(--white-500-hsl)/.1)}.ia-button-link.ia-button-color-white{color:var(--white-500)}.ia-button-link.ia-button-color-white:hover .ia-button-content{--button--underline-color:var(--white-500)}.ia-button-filled.ia-button-color-red{color:var(--white-500);background-color:var(--control-critical-primary-background-default)}.ia-button-filled.ia-button-color-red:hover{background-color:var(--control-critical-primary-background-hover)}.ia-button-filled.ia-button-color-red:active{background-color:var(--control-critical-primary-background-active)}.ia-button-outlined.ia-button-color-red{color:var(--control-critical-secondary-text-default);border-color:var(--control-critical-secondary-border-default)}.ia-button-outlined.ia-button-color-red:hover{background-color:var(--control-critical-secondary-border-hover);border-color:var(--control-critical-secondary-border-hover);color:var(--control-critical-secondary-border-hover)}.ia-button-outlined.ia-button-color-red:active{background-color:var(--control-critical-secondary-background-active);border-color:var(--control-critical-secondary-border-active);color:var(--control-critical-secondary-text-active)}.ia-button-link.ia-button-color-red{color:var(--text-danger)}.ia-button-link.ia-button-color-red:hover .ia-button-content{--button--underline-color:var(--text-danger)}.ia-button-filled.ia-button-color-green{color:var(--white-500);background-color:var(--control-connect-background-default)}.ia-button-filled.ia-button-color-green:hover{background-color:var(--control-connect-background-hover)}.ia-button-filled.ia-button-color-green:active{background-color:var(--control-connect-background-active)}.ia-button-outlined.ia-button-color-green{color:var(--control-primary-text-default);border-color:var(--control-connect-border-default)}.ia-button-outlined.ia-button-color-green:hover{background-color:var(--control-connect-background-hover);border-color:var(--control-connect-border-hover);color:var(--control-primary-text-hover)}.ia-button-outlined.ia-button-color-green:active{background-color:var(--control-connect-background-active);border-color:var(--control-connect-border-active);color:var(--control-primary-text-active)}.ia-button-link.ia-button-color-green{color:var(--green-360)}.ia-button-link.ia-button-color-green:hover .ia-button-content{--button--underline-color:var(--green-360)}.ia-button-outlined.ia-button-color-primary{color:var(--button-outline-primary-text);border-color:var(--control-secondary-border-default)}.ia-button-outlined.ia-button-color-primary:hover{background-color:var(--control-secondary-background-hover);border-color:var(--control-secondary-border-hover);color:var(--control-secondary-text-hover)}.ia-button-outlined.ia-button-color-primary:active{background-color:var(--control-secondary-background-active);border-color:var(--control-secondary-border-active);color:var(--control-secondary-text-active)}.ia-button-filled.ia-button-color-primary{color:var(--text-default);background-color:var(--control-secondary-background-default)}.ia-button-filled.ia-button-color-primary:hover{background-color:var(--control-secondary-background-hover)}.ia-button-filled.ia-button-color-primary:active{background-color:var(--control-secondary-background-active)}.theme-dark .ia-button-link.ia-button-color-primary{color:var(--white-500)}.theme-dark .ia-button-link.ia-button-color-primary:hover .ia-button-content{--button--underline-color:var(--white-500)}.theme-light .ia-button-link.ia-button-color-primary{color:var(--primary-400)}.theme-light .ia-button-link.ia-button-color-primary:hover .ia-button-content{--button--underline-color:var(--primary-400)}.theme-dark .ia-button-filled.ia-button-color-transparent{color:var(--primary-100);background-color:hsl(var(--white-500-hsl)/.1)}.theme-dark .ia-button-filled.ia-button-color-transparent:hover{background-color:hsl(var(--white-500-hsl)/.05)}.theme-dark .ia-button-filled.ia-button-color-transparent:active{background-color:hsl(var(--white-500-hsl)/.01)}.theme-dark .ia-button-filled.ia-button-color-transparent:disabled{background-color:hsl(var(--white-500-hsl)/.1)}.theme-dark .ia-button-outlined.ia-button-color-transparent{color:var(--primary-200);border-color:var(--primary-200)}.theme-dark .ia-button-outlined.ia-button-color-transparent:active{background-color:hsl(var(--primary-200-hsl)/.1)}.theme-dark .ia-button-link.ia-button-color-transparent{color:var(--primary-200)}.theme-dark .ia-button-link.ia-button-color-transparent:hover .ia-button-content{--button--underline-color:var(--primary-200)}.theme-light .ia-button-filled.ia-button-color-transparent{color:var(--primary-400);background-color:hsl(var(--primary-400-hsl)/.01)}.theme-light .ia-button-filled.ia-button-color-transparent:hover{background-color:hsl(var(--primary-400-hsl)/.2)}.theme-light .ia-button-filled.ia-button-color-transparent:active{background-color:hsl(var(--primary-400-hsl)/.25)}.theme-light .ia-button-filled.ia-button-color-transparent:disabled{background-color:hsl(var(--primary-400-hsl)/.01)}.theme-light .ia-button-outlined.ia-button-color-transparent{color:var(--primary-400);border-color:var(--primary-400)}.theme-light .ia-button-outlined.ia-button-color-transparent:active{background-color:hsl(var(--primary-400-hsl)/.1)}.theme-light .ia-button-link.ia-button-color-transparent{color:var(--primary-400)}.theme-light .ia-button-link.ia-button-color-transparent:hover .ia-button-content{--button--underline-color:var(--primary-400)}";

  // src/InAccord/styles/spinner.css
  var spinner_default2 = ".ia-spinner{justify-content:center;align-items:center;display:flex}.ia-spinner-inner{contian:piant;justify-content:center;align-items:center;width:32px;height:32px;display:inline-flex;position:relative}.ia-spinner-spinning-circle,.ia-spinner-spinning-circle-simple{width:100%}:where(.ia-spinner-spinning-circle,.ia-spinner-spinning-circle-simple) .ia-spinner-inner{transform:rotate(280deg)}.ia-spinner-circular{width:100%;height:100%;animation:2s linear infinite ia-spinner-spinning-circle-rotate}.ia-spinner-circular .ia-spinner-path{stroke-dasharray:1 200;stroke-dashoffset:0;fill:none;stroke-width:6px;stroke-miterlimit:10;stroke-linecap:round;stroke:var(--brand-500);animation:2s ease-in-out infinite ia-spinner-spinning-circle-dash}.ia-spinner-circular .ia-spinner-path:nth-last-child(3){stroke:var(--text-brand);animation-delay:.23s}.ia-spinner-circular .ia-spinner-path:nth-last-child(2){stroke:var(--text-brand);opacity:.6;animation-delay:.15s}.ia-spinner-low-motion .ia-spinner-path{background-color:var(--interactive-text-normal);opacity:.3;border-radius:3px;width:6px;height:6px;margin-right:2px;animation:1.4s ease-in-out infinite ia-spinner-low-motion;display:inline-block}.ia-spinner-low-motion .ia-spinner-path:nth-child(2){animation-delay:.2s}.ia-spinner-low-motion .ia-spinner-path:nth-child(3){animation-delay:.4s}.ia-spinner-chasing-dots .ia-spinner-inner{animation:2s linear infinite ia-spinner-chasing-dots-rotate}.ia-spinner-chasing-dots .ia-spinner-path{background-color:var(--brand-500);border-radius:100%;width:60%;height:60%;animation:2s ease-in-out infinite ia-spinner-chasing-dots-bounce;display:inline-block;position:absolute;top:0}.ia-spinner-chasing-dots .ia-spinner-path:last-child{animation-delay:-1s;top:auto;bottom:0}.ia-spinner-wandering-cubes .ia-spinner-path{background-color:var(--brand-400);width:10px;height:10px;animation:1.8s ease-in-out infinite ia-spinner-wandering-cubes;position:absolute;top:0;left:0}.ia-spinner-wandering-cubes .ia-spinner-path:last-child{animation-delay:.9s}.ia-spinner-pulsing-ellipsis .ia-spinner-inner{justify-content:center;width:28px;height:auto;display:flex;position:relative}.ia-spinner-pulsing-ellipsis .ia-spinner-path{background-color:var(--primary-100);opacity:.3;border-radius:3px;width:6px;height:6px;margin-right:2px;animation:1.4s ease-in-out infinite ia-spinner-pulsing-ellipsis;display:inline-block}.ia-spinner-pulsing-ellipsis .ia-spinner-path:nth-child(2){animation-delay:.2s}.ia-spinner-pulsing-ellipsis .ia-spinner-path:nth-child(3){animation-delay:.4s}@keyframes ia-spinner-low-motion{0%{opacity:1}50%{opacity:.6}to{opacity:1}}@keyframes ia-spinner-spinning-circle-rotate{to{transform:rotate(360deg)}}@keyframes ia-spinner-spinning-circle-dash{0%{stroke-dasharray:1 200;stroke-dashoffset:0}50%{stroke-dasharray:130 200}to{stroke-dasharray:130 200;stroke-dashoffset:-124px}}@keyframes ia-spinner-chasing-dots-rotate{to{transform:rotate(360deg)}}@keyframes ia-spinner-chasing-dots-bounce{0%,to{transform:scale(0)}50%{transform:scale(1)}}@keyframes ia-spinner-wandering-cubes{25%{transform:translate(22px)rotate(-90deg)scale(.5)}50%{transform:translate(22px)translateY(22px)rotate(-180deg)}75%{transform:translate(0)translateY(22px)rotate(-270deg)scale(.5)}to{transform:rotate(-360deg)}}@keyframes ia-spinner-pulsing-ellipsis{0%{opacity:1;transform:scale(1)}50%{opacity:.3;transform:scale(.8)}to{opacity:1;transform:scale(1)}}";

  // src/InAccord/styles/search.css
  var search_default = ".ia-search-wrapper{background-color:var(--background-base-lowest);color:var(--text-default);border:0;border-radius:3px;outline:none;align-items:center;padding:3px;display:flex}.ia-search{color:var(--text-default);background:0 0;border:0;flex:1;padding:2px 3px}.ia-search::-webkit-input-placeholder{color:var(--text-default)}.ia-search-wrapper>svg{color:var(--interactive-text-default);margin-right:2px}.ia-search-wrapper>.ia-button{background:0 0;margin-right:2px;padding:0}.ia-search-wrapper>.ia-button>svg .fill{fill:var(--interactive-text-default)}.ia-search-wrapper>.ia-button:hover>svg .fill{fill:var(--interactive-text-hover)}";

  // src/InAccord/styles/text.css
  var text_default = ".ia-text-normal{color:var(--text-default)}.ia-text-muted{color:var(--text-muted)}.ia-text-error{color:var(--red-400)}.ia-text-brand{color:var(--text-brand)}.ia-text-link{color:var(--text-link)}.ia-header-primary{color:var(--text-strong)}.ia-header-secondary{color:var(--text-default)}.ia-text-yellow{color:var(--text-feedback-warning)}.ia-text-green{color:var(--text-feedback-positive)}.ia-text-red{color:var(--status-danger)}.ia-text-white{color:var(--white-500)}.ia-text-10{font-size:10px;line-height:12px}.ia-text-12{font-size:12px;line-height:16px}.ia-text-14{font-size:14px;line-height:18px}.ia-text-16{font-size:16px;line-height:20px}.ia-text-20{font-size:20px;line-height:24px}.ia-text-24{font-size:24px;line-height:30px}.ia-text-32{font-size:32px;line-height:40px}.ia-text-strong{font-weight:600}.ia-selectable{user-select:text}";

  // src/InAccord/styles/index.css
  var styles_default = customcss_default3 + addonlist_default + addonstore_default3 + bdsettings_default + changelog_default2 + checkbox_default + colorpicker_default + errormodal_default + file_default + flex_default + floatingwindow_default + installmodal_default + keybind_default + managerhub_default + modal_default + notices_default + notifications_default3 + number_default + paginator_default + radio_default + scroller_default + sidebar_default + slider_default + switch_default + tabbar_default + textbox_default + toasts_default2 + tooltip_default + updater_default + emptyimage_default + noresults_default + buttons_default + spinner_default2 + search_default + text_default + ":root{--ia-brand:#3e82e5;--ia-brand-hover:#3875ce;--ia-brand-active:#3268b7}.ia-chat-badge{vertical-align:bottom;height:1.25rem;line-height:1.375rem;display:inline-block}.ia-member-badge{height:15px;margin-left:4px}.ia-link{text-decoration:none}.ia-link:hover{text-decoration:underline}.ia-divider{border:none;border-top:thin solid var(--app-border-frame);width:100%;height:1px;margin:0}.ia-version-info{cursor:pointer;padding:8px 10px}.ia-settings-title-extend{width:100%}.ia-settings-page-title-children{margin-left:auto;margin-right:12px}";

  // src/InAccord/modules/core.ts
  init_modals();
  init_floatingwindows();

  // src/InAccord/ui/toasts.tsx
  init_react();
  init_discordmodules();
  init_dommanager();
  init_reactdom();
  init_toasts();

  // src/InAccord/ui/toasts/ToastIcon.tsx
  init_react();
  init_lucide_react();
  function ToastIcon({type}) {
    switch (type) {
      case "info":
        return /* @__PURE__ */ react_default.createElement(Info, {size: "24px"});
      case "success":
        return /* @__PURE__ */ react_default.createElement(CircleCheck, {size: "24px"});
      case "warning":
        return /* @__PURE__ */ react_default.createElement(TriangleAlert, {size: "24px"});
      case "error":
        return /* @__PURE__ */ react_default.createElement(CircleAlert, {size: "24px"});
      default:
        return null;
    }
  }

  // src/InAccord/ui/toasts.tsx
  init_hooks();
  init_clsx();
  var ReactSpring = discordmodules_default.ReactSpring;
  function Toast({content, type, icon, style}) {
    return /* @__PURE__ */ react_default.createElement(ReactSpring.animated.div, {className: clsx_default("ia-toast", `toast-${type}`), style}, icon && /* @__PURE__ */ react_default.createElement(ToastIcon, {type}), /* @__PURE__ */ react_default.createElement("span", null, content));
  }
  function ToastContianer() {
    const toasts = useStateFromStores(toasts_default, () => toasts_default.toasts);
    const transition = ReactSpring.useTransition(toasts, {
      keys: (toast) => toast.key,
      from: {opacity: 0, transform: "translateY(100%)"},
      enter: {opacity: 1, transform: "translateY(0px)"},
      leave: {opacity: 0, transform: "translateY(100%)"},
      config: ReactSpring.config.stiff
    });
    return /* @__PURE__ */ react_default.createElement(Fragment, null, transition((style, item) => /* @__PURE__ */ react_default.createElement(Toast, {key: item.key, content: item.content, type: item.type, icon: item.icon, style})));
  }
  var Toasts2 = class _Toasts {
    static root;
    static initialize() {
      const contianer = document.createElement("div");
      contianer.id = "ia-toasts";
      DOMManager.iaBody.appendChild(contianer);
      _Toasts.root = reactdom_default.createRoot(contianer);
      _Toasts.root.render(react_default.createElement(ToastContianer));
    }
  };

  // src/InAccord/modules/core.ts
  init_toasts();

  // src/InAccord/ui/settings/managerhub.ts
  init_react();
  init_ipc();
  init_backupmanager();
  init_pluginmanager();
  init_thememanager();
  init_settings3();
  init_config();
  init_button();
  init_text();
  init_hooks();
  init_i18n();
  function getAddonStats(manager) {
    const total = manager.addonList.length;
    const enabled = manager.addonList.reduce((count, addon) => count + (manager.state[addon.id] ? 1 : 0), 0);
    return {total, enabled};
  }
  function HubCard({icon, title, subtitle, onOpen, onToggle, onOpenFolder, buttonColor, toggleLabel}) {
    return react_default.createElement(
      "div",
      {className: "ia-manager-hub-card"},
      react_default.createElement(
        "div",
        {className: "ia-manager-hub-card-header"},
        react_default.createElement("div", {className: "ia-manager-hub-icon"}, icon),
        react_default.createElement(
          "div",
          {},
          react_default.createElement(Text, {strong: true, size: Text.Sizes.SIZE_16}, title),
          react_default.createElement(Text, {color: Text.Colors.MUTED, size: Text.Sizes.SIZE_12}, subtitle)
        )
      ),
      react_default.createElement("div", {className: "ia-manager-hub-card-actions"}, [
        react_default.createElement(Button, {size: Button.Sizes.SMALL, color: buttonColor, onClick: onOpen}, "Open"),
        onToggle && react_default.createElement(Button, {size: Button.Sizes.SMALL, color: Button.Colors.PRIMARY, look: Button.Looks.OUTLINED, onClick: onToggle}, toggleLabel || "Toggle"),
        onOpenFolder && react_default.createElement(Button, {size: Button.Sizes.SMALL, color: Button.Colors.PRIMARY, look: Button.Looks.OUTLINED, onClick: onOpenFolder}, "Folder")
      ])
    );
  }
  function ManagerHubPanel() {
    const set = react_default.useContext(SettingsTitleContext);
    const backupStats = getAddonStats(backupmanager_default);
    const pluginStats = useStateFromStores(pluginmanager_default, () => getAddonStats(pluginmanager_default), [], true);
    const themeStats = useStateFromStores(thememanager_default, () => getAddonStats(thememanager_default), [], true);
    const customCssEnabled = useStateFromStores(settings_default2, () => settings_default2.get("settings", "customcss", "customcss"));
    const allPluginsEnabled = pluginStats.total > 0 && pluginStats.enabled === pluginStats.total;
    const allThemesEnabled = themeStats.total > 0 && themeStats.enabled === themeStats.total;
    return [
      set(react_default.createElement(SettingsTitle, {text: t("Panels.managerhub") || "Manager Hub"})),
      react_default.createElement("div", {className: "ia-manager-hub"}, [
        react_default.createElement("h2", {className: "ia-manager-hub-heading"}, "In-Accord"),
        react_default.createElement(
          Text,
          {className: "ia-manager-hub-description", color: Text.Colors.MUTED},
          "Manage your Discord customization tools from one place."
        ),
        react_default.createElement(
          "div",
          {className: "ia-manager-hub-backup"},
          react_default.createElement(
            Text,
            {color: Text.Colors.MUTED, size: Text.Sizes.SIZE_12},
            `Backups: ${backupStats.enabled}/${backupStats.total} ${t("Addons.isEnabled")}`
          ),
          react_default.createElement(Button, {
            size: Button.Sizes.SMALL,
            color: Button.Colors.PRIMARY,
            look: Button.Looks.OUTLINED,
            onClick: () => ipc_default.openPath(config_default.get("backupPath"))
          }, "Backup Folder")
        ),
        react_default.createElement("div", {className: "ia-manager-hub-grid"}, [
          react_default.createElement(HubCard, {
            icon: react_default.createElement(Text, {size: Text.Sizes.SIZE_16}, "🔌"),
            title: t("Panels.plugins"),
            subtitle: `${pluginStats.enabled}/${pluginStats.total} ${t("Addons.isEnabled")}`,
            onOpen: () => settings_default3.openSettingsPage("plugins"),
            onToggle: () => allPluginsEnabled ? pluginmanager_default.disableAllAddons() : pluginmanager_default.enableAllAddons(),
            onOpenFolder: () => ipc_default.openPath(pluginmanager_default.addonFolder),
            toggleLabel: allPluginsEnabled ? "Disable all" : "Enable all",
            buttonColor: Button.Colors.BLURPLE
          }),
          react_default.createElement(HubCard, {
            icon: react_default.createElement(Text, {size: Text.Sizes.SIZE_16}, "🎨"),
            title: t("Panels.themes"),
            subtitle: `${themeStats.enabled}/${themeStats.total} ${t("Addons.isEnabled")}`,
            onOpen: () => settings_default3.openSettingsPage("themes"),
            onToggle: () => allThemesEnabled ? thememanager_default.disableAllAddons() : thememanager_default.enableAllAddons(),
            onOpenFolder: () => ipc_default.openPath(thememanager_default.addonFolder),
            toggleLabel: allThemesEnabled ? "Disable all" : "Enable all",
            buttonColor: Button.Colors.BRAND
          }),
          react_default.createElement(HubCard, {
            icon: react_default.createElement(Text, {size: Text.Sizes.SIZE_16}, "✏️"),
            title: t("Panels.customcss"),
            subtitle: customCssEnabled ? "Enabled" : "Disabled",
            onOpen: () => customcss_default.open(),
            onToggle: () => settings_default2.set("settings", "customcss", "customcss", !customCssEnabled),
            onOpenFolder: () => ipc_default.openPath(config_default.get("channelPath")),
            toggleLabel: customCssEnabled ? "Disable" : "Enable",
            buttonColor: customCssEnabled ? Button.Colors.GREEN : Button.Colors.YELLOW
          })
        ])
      ])
    ];
  }

  // src/InAccord/ui/customcss/mdinstallcss.tsx
  init_patcher();
  init_react();
  init_settings3();
  init_toasts();
  init_i18n();
  init_lucide_react();
  init_webpack2();
  init_logger();
  init_modals();
  init_utils();
  var InstallCSS = class {
    static activeNotifications = /* @__PURE__ */ new Map();
    static initialize() {
      const patch = getModule((m) => m.defaultRules && m.parse).defaultRules.codeBlock;
      if (!patch.react || typeof patch.react !== "function") return;
      Patcher.after("InstallCSS", patch, "react", (_, [args], child) => {
        const isEnabled = settings_default2.get("customcss", "customcss");
        if (!isEnabled) return;
        const content = args.content;
        if (!content) return;
        if (child?.type !== "pre") return;
        if (args?.lang?.toLowerCase() !== "css") return;
        const codeActions = findInTree(child, (x) => x?.className?.includes("codeActions"), {walkable: ["props", "children"]});
        if (!codeActions) return;
        if (!args?.content) return;
        const existingCodeblocks = Array.isArray(codeActions.children) ? codeActions.children : [codeActions.children];
        codeActions.children = [
          ...existingCodeblocks,
          /* @__PURE__ */ react_default.createElement(
            PackageOpen,
            {
              style: {cursor: "pointer"},
              size: "16px",
              key: "icon",
              onClick: async (event) => {
                if (event.shiftKey) {
                  if (args.content) this.handleCSSInstall(args.content);
                  return;
                }
                Modals.showConfirmationModal(
                  t("Modals.confirmAction"),
                  t("Modals.installCss"),
                  {
                    confirmText: t("Modals.okay"),
                    cancelText: t("Modals.cancel"),
                    onConfirm: () => args?.content && this.handleCSSInstall(args.content)
                  }
                );
              }
            }
          )
        ];
      });
    }
    static handleCSSInstall(cssBlock) {
      try {
        const currentCSS = customcss_default.savedCss || "";
        const newCSS = currentCSS + "\n" + cssBlock;
        customcss_default.saveCSS(newCSS);
        customcss_default.insertCSS(newCSS);
        toasts_default.show(t("CustomCSS.cssInstallSuccess"), {type: "success"});
        const notificationId = `css-undo-${Date.now()}`;
        this.activeNotifications.set(notificationId, cssBlock);
        notifications_default2.show({
          id: notificationId,
          title: t("CustomCSS.cssInstalled"),
          content: t("CustomCSS.cssReverting"),
          type: "warning",
          duration: 1e4,
          actions: [{
            label: "Keep",
            onClick: () => this.keepChanges(notificationId)
          }],
          onClose: () => this.revertCSS(notificationId)
        });
      } catch (error) {
        Logger.log("InstallCSS", "Fialed to install CSS:", error);
        toasts_default.show(t("CustomCSS.cssInstallError"), {type: "error"});
      }
    }
    static keepChanges(notificationId) {
      this.activeNotifications.delete(notificationId);
      toasts_default.show(t("CustomCSS.cssKept"), {type: "success"});
    }
    static revertCSS(notificationId) {
      const cssBlock = this.activeNotifications.get(notificationId);
      if (!cssBlock) return;
      const currentCSS = customcss_default.savedCss || "";
      const newCSS = currentCSS.replace(cssBlock, "");
      customcss_default.saveCSS(newCSS);
      customcss_default.insertCSS(newCSS);
      this.activeNotifications.delete(notificationId);
      toasts_default.show(t("CustomCSS.cssReverted"), {type: "error"});
    }
  };
  var mdinstallcss_default = InstallCSS;

  // src/InAccord/modules/core.ts
  init_webpack2();
  init_patcher();
  init_lucide_react();
  init_react();
  var iconSizes = {
    xxs: 12,
    xs: 16,
    sm: 18,
    md: 24,
    lg: 32,
    refresh_sm: 20
  };
  var ManagerHubSplashIcon = lucideToDiscordIcon(Briefcase);
  var OurServerDiscordIcon = (props) => {
    const size = props.size === "custom" ? {width: props.width, height: props.height} : {width: `${iconSizes[props.size || "xs"]}px`, height: `${iconSizes[props.size || "xs"]}px`};
    return react_default.createElement("svg", {
      xmlns: "http://www.w3.org/2000/svg",
      viewBox: "0 0 24 24",
      className: props.className,
      style: {
        ...size,
        display: "block",
        flexShrink: 0
      }
    }, react_default.createElement("path", {
      fill: props.color || "currentColor",
      d: "M20.317 4.369a19.791 19.791 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.211.375-.444.864-.608 1.249a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.249.077.077 0 0 0-.079-.037 19.736 19.736 0 0 0-4.885 1.515.069.069 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.056 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.17 14.17 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128c.126-.094.252-.192.372-.291a.074.074 0 0 1 .078-.01c3.927 1.793 8.18 1.793 12.061 0a.074.074 0 0 1 .078.01c.121.099.247.198.373.291a.077.077 0 0 1-.006.128 12.299 12.299 0 0 1-1.873.891.076.076 0 0 0-.04.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.84 19.84 0 0 0 6.002-3.03.077.077 0 0 0 .031-.055c.5-5.177-.838-9.674-3.548-13.66a.061.061 0 0 0-.031-.03zM8.02 15.331c-1.183 0-2.157-1.085-2.157-2.419 0-1.334.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.956 2.419-2.157 2.419zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.334.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.946 2.419-2.157 2.419z"
    }));
  };
  var core_default = new class Core {
    hasStarted = false;
    trustInAccordProtocol() {
      Patcher.after("InAccordProtocol", getStore("MaskedLinkStore"), "isTrustedProtocol", (_, [url], ret) => ret || url.startsWith("inaccord://"));
    }
    async startup() {
      if (this.hasStarted) return;
      this.hasStarted = true;
      try {
        ipc_default.getSystemAccentColor().then((value) => DOMManager.injectStyle("ia-os-values", `:root {--os-accent-color: #${value};}`));
        this.trustInAccordProtocol();
        Logger.log("Startup", "Injecting ia Styles");
        DOMManager.injectStyle("ia-stylesheet", styles_default.toString());
        Logger.log("Startup", "Initializing AddonStore");
        addonstore_default.initialize();
        Logger.log("Startup", "Initializing LocaleManager");
        localemanager_default.initialize();
        Logger.log("Startup", "Initializing Settings");
        settings_default2.initialize();
        settings_default2.registerPanel("managerhub", "Manager Hub", {
          order: 0,
          icon: LayoutGrid,
          discordIcon: ManagerHubSplashIcon,
          element: ManagerHubPanel,
          searchable: () => ["plugin", "theme", "custom css", "manager"]
        });
        settings_default2.registerPanel("github", "GitHub", {
          order: 999,
          icon: Github,
          onClick: () => {
            window.open("https://github.com/GARD-Realms-LLC/In-Accord-App", "_blank", "noopener,noreferrer");
          },
          searchable: () => ["github", "repo", "source", "inaccord"]
        });
        settings_default2.registerPanel("ourserver", "Our Server", {
          order: 1e3,
          discordIcon: OurServerDiscordIcon,
          onClick: () => {
            window.open("https://discord.com/channels/1418648743544094871", "_blank", "noopener,noreferrer");
          },
          searchable: () => ["discord", "server", "community", "our server"]
        });
        settings_default3.initialize();
        Logger.log("Startup", "Initializing DOMManager");
        DOMManager.initialize();
        Logger.log("Startup", "Initializing CommandManager");
        commandmanager_default.initialize();
        Logger.log("Startup", "Initializing Internal InstallCSS");
        mdinstallcss_default.initialize();
        Logger.log("Startup", "Wiating for connection...");
        const connected = await this.wiatForConnection(15e3);
        if (!connected) {
          try {
            Logger.warn("Startup", "Connection wait timed out; continuing startup to avoid stuck loading state.");
          } catch {
          }
        }
        Logger.log("Startup", "Initializing FloatingWindows");
        FloatingWindows.initialize();
        Logger.log("Startup", "Initializing Toasts");
        Toasts2.initialize();
        try {
          toasts_default.success("In-Accord loaded", {forceShow: true, timeout: 5e3});
        } catch {
        }
        Logger.log("Startup", "Initializing Builtins");
        for (const module in builtins_exports) {
          builtins_exports[module].initialize();
        }
        Logger.log("Startup", "Loading Backups");
        const backupErrors = backupmanager_default.initialize();
        Logger.log("Startup", "Loading Plugins");
        const pluginErrors = pluginmanager_default.initialize();
        Logger.log("Startup", "Loading Themes");
        const themeErrors = thememanager_default.initialize();
        Logger.log("Startup", "Initializing Updater");
        Updater.initialize();
        Logger.log("Startup", "Removing Loading Icon");
        loadingicon_default.hide();
        Logger.log("Startup", "Collecting Startup Errors");
        Modals.showAddonErrors({backup: backupErrors, plugins: pluginErrors, themes: themeErrors});
        const previousVersion = json_default.get("misc", "version");
        if (config_default.get("version") !== previousVersion) {
          Modals.showChangelogModal(changelog_default);
          json_default.set("misc", "version", config_default.get("version"));
        }
      } catch (error) {
        try {
          Logger.stacktrace("Startup", "Startup failed (continuing)", error);
        } catch {
        }
      } finally {
        try {
          loadingicon_default.hide();
        } catch {
        }
      }
    }
    wiatForConnection(timeoutMs = 15e3) {
      const ms = Number.isFinite(timeoutMs) ? Math.max(0, timeoutMs | 0) : 15e3;
      return new Promise((resolve) => {
        let finished = false;
        let timer = null;
        let poll = null;
        const finish = (connected) => {
          if (finished) return;
          finished = true;
          try {
            if (timer) clearTimeout(timer);
          } catch {
          }
          try {
            if (poll) clearInterval(poll);
          } catch {
          }
          try {
            const d = discordmodules_default.Dispatcher;
            if (d && typeof d.unsubscribe === "function") d.unsubscribe("CONNECTION_OPEN", onOpen);
            if (d && typeof d.off === "function") d.off("CONNECTION_OPEN", onOpen);
            if (d && typeof d.removeListener === "function") d.removeListener("CONNECTION_OPEN", onOpen);
          } catch {
          }
          resolve(connected);
        };
        const onOpen = () => finish(true);
        try {
          if (discordmodules_default.UserStore?.getCurrentUser?.()) return finish(true);
        } catch {
        }
        try {
          const d = discordmodules_default.Dispatcher;
          if (d && typeof d.subscribe === "function") d.subscribe("CONNECTION_OPEN", onOpen);
          else if (d && typeof d.on === "function") d.on("CONNECTION_OPEN", onOpen);
          else if (d && typeof d.addListener === "function") d.addListener("CONNECTION_OPEN", onOpen);
        } catch {
        }
        try {
          poll = setInterval(() => {
            if (finished) return;
            try {
              if (discordmodules_default.UserStore?.getCurrentUser?.()) finish(true);
            } catch {
            }
          }, 250);
        } catch {
        }
        timer = setTimeout(() => finish(false), ms);
      });
    }
  }();

  // src/InAccord/api/index.ts
  init_logger();
  init_backupmanager();
  init_pluginmanager();
  init_thememanager();
  init_discordmodules();
  init_config();

  // src/InAccord/api/addonapi.ts
  var AddonAPI = class {
    #manager;
    constructor(manager) {
      this.#manager = manager;
    }
    /**
     * The path to the addon folder.
     * @type string
     */
    get folder() {
      return this.#manager.addonFolder;
    }
    /**
     * Determines if a particular addon is enabled.
     * @param {string} idOrFile Addon ID or filename
     * @returns {boolean}
     */
    isEnabled(idOrFile) {
      return this.#manager.isEnabled(idOrFile);
    }
    /**
     * Enables the given addon.
     * @param {string} idOrFile Addon ID or filename
     */
    enable(idOrAddon) {
      return this.#manager.enableAddon(idOrAddon);
    }
    /**
     * Disables the given addon.
     * @param {string} idOrFile Addon ID or filename
     */
    disable(idOrAddon) {
      return this.#manager.disableAddon(idOrAddon);
    }
    /**
     * Toggles if a particular addon is enabled.
     * @param {string} idOrFile Addon ID or filename
     */
    toggle(idOrAddon) {
      return this.#manager.toggleAddon(idOrAddon);
    }
    /**
     * Reloads if a particular addon is enabled.
     * @param {string} idOrFile Addon ID or filename
     */
    reload(idOrFileOrAddon) {
      return this.#manager.reloadAddon(idOrFileOrAddon);
    }
    /**
     * Gets a particular addon.
     * @param {string} idOrFile Addon ID or filename
     * @returns {object} Addon instance
     */
    get(idOrFile) {
      return this.#manager.getAddon(idOrFile);
    }
    /**
     * Gets all addons of this type.
     * @returns {Array<object>} Array of all addon instances
     */
    getAll() {
      return this.#manager.addonList.map((a) => this.#manager.getAddon(a.id));
    }
  };
  Object.freeze(AddonAPI);
  Object.freeze(AddonAPI.prototype);
  var addonapi_default = AddonAPI;

  // src/InAccord/api/data.ts
  init_json();
  var Data = class {
    #pluginName = "";
    constructor(pluginName) {
      if (!pluginName) return;
      this.#pluginName = pluginName;
    }
    /**
     * Saves JSON-serializable data.
     *
     * @param {string} pluginName Name of the plugin saving data
     * @param {string} key Which piece of data to store
     * @param {any} data The data to be saved
     */
    save(...args) {
      if (this.#pluginName) {
        return json_default.setData(this.#pluginName, ...args);
      }
      return json_default.setData(...args);
    }
    /**
     * Loads previously stored data.
     *
     * @param {string} pluginName Name of the plugin loading data
     * @param {string} key Which piece of data to load
     * @returns {any} The stored data
     */
    load(...args) {
      if (this.#pluginName) {
        return json_default.getData(this.#pluginName, args[0]);
      }
      return json_default.getData(args[0], args[1]);
    }
    /**
     * Recaches JSON-serializable save file.
     *
     * @param {string} pluginName Name of the plugin saving data
     * @return {boolean} success Did the data recache
     *
     * @warning ⚠️ **Use of the recaching is discouraged!**
     *
     * Recache loads can block the filesystem and significantly degrade performance.
     * Use this method only for **debugging or testing purposes**. Avoid frequent recaching in production environments.
     */
    async recache(...args) {
      const pluginName = this.#pluginName || args[0];
      return json_default.recache(pluginName);
    }
    /**
     * Deletes a piece of stored data. This is different than saving `null` or `undefined`.
     *
     * @param {string} pluginName Name of the plugin deleting data
     * @param {string} key Which piece of data to delete.
     */
    delete(...args) {
      if (this.#pluginName) {
        return json_default.deleteData(this.#pluginName, args[0]);
      }
      return json_default.deleteData(args[0], args[1]);
    }
    // public on<T>(...args: OnChangeArgs<Bounded, T>) {
    //     if (this.#pluginName) {
    //         if (typeof args[0] === "function") {
    //             return JsonStore.addPluginChangeListener(this.#pluginName, args[0]);
    //         }
    //         return JsonStore.addPluginChangeListener(this.#pluginName, args[1], args[0]);
    //     }
    //     if (typeof args[1] === "function") {
    //         return JsonStore.addPluginChangeListener(args[0] as string, args[1]);
    //     }
    //     return JsonStore.addPluginChangeListener(args[0] as string, args[2], args[1]);
    // }
    // public off(...args: OnChangeArgs<Bounded, unknown>) {
    //     if (this.#pluginName) {
    //         if (typeof args[0] === "function") {
    //             return JsonStore.removePluginChangeListener(this.#pluginName, args[0]);
    //         }
    //         return JsonStore.removePluginChangeListener(this.#pluginName, args[1], args[0]);
    //     }
    //     if (typeof args[1] === "function") {
    //         return JsonStore.removePluginChangeListener(args[0] as string, args[1]);
    //     }
    //     return JsonStore.removePluginChangeListener(args[0] as string, args[2], args[1]);
    // }
  };
  Object.freeze(Data);
  Object.freeze(Data.prototype);
  var data_default = Data;

  // src/InAccord/api/dom.ts
  init_dommanager();
  var DOM = class {
    /**
     * Current width of the user's screen.
     * @type {number}
     */
    get screenWidth() {
      return Math.max(document.documentElement.clientWidth, window.innerWidth || 0);
    }
    /**
     * Current height of the user's screen.
     * @type {number}
     */
    get screenHeight() {
      return Math.max(document.documentElement.clientHeight, window.innerHeight || 0);
    }
    #callerName = "";
    constructor(callerName) {
      if (!callerName) return;
      this.#callerName = callerName;
    }
    /**
     * Adds a `<style>` to the document with the given ID.
     *
     * @param {string} id ID to use for style element
     * @param {string} css CSS to apply to the document
     */
    addStyle(...args) {
      if (args.length === 1) {
        if (this.#callerName) {
          args.unshift(this.#callerName);
        } else {
          throw new Error("No css provided");
        }
      }
      DOMManager.injectStyle(args[0], args[1]);
    }
    /**
     * Removes a `<style>` from the document corresponding to the given ID.
     *
     * @param {string} id ID used for the style element
     */
    removeStyle(...args) {
      let id = args[0];
      if (this.#callerName && arguments.length === 0) {
        id = this.#callerName;
      }
      if (!id) throw new Error("No id provided");
      DOMManager.removeStyle(id);
    }
    /**
     * Adds a listener for when the node is removed from the document body.
     *
     * @param {HTMLElement} node Node to be observed
     * @param {function} callback Function to run when removed
     */
    onRemoved(node, callback) {
      return DOMManager.onRemoved(node, callback);
    }
    /**
     * Adds a listener for when a node matching a selector is added to the document body.
     * The listener is automatically removed upon firing.
     * The callback is given the matching element.
     * @param {string} selector - node to wiat for
     * @param {callable} callback - function to be performed on event
     */
    onAdded(selector, callback) {
      return DOMManager.onAdded(selector, callback);
    }
    /**
     * Utility to help smoothly animate using JavaScript.
     *
     * @param {function} update Render function indicating the style should be updated
     * @param {number} duration Duration in ms to animate for
     * @param {object} [options] Options to customize the animation
     * @param {function} [options.timing] Optional function calculating progress based on current time fraction. Linear by default.
     */
    animate(update, duration, options = {}) {
      return DOMManager.animate({update, duration, timing: options.timing});
    }
    /**
     * Utility function to make creating DOM elements easier. Acts similarly
     * to `React.createElement`
     *
     * @param {string} tag HTML tag name to create
     * @param {object} [options] Options object to customize the element
     * @param {string} [options.className] Class name to add to the element
     * @param {string} [options.id] ID to set for the element
     * @param {HTMLElement} [options.target] Target element to automatically append to
     * @param {(Node|string|(Node|string)[])[]} [children] Child nodes to add
     * @returns {HTMLElement} The created HTML element
     */
    createElement(tag, options = {}, ...children) {
      return DOMManager.createElement(tag, options, ...children);
    }
    /**
     * Parses a string of HTML and returns the results. If the second parameter is true,
     * the parsed HTML will be returned as a document fragment {@see https://developer.mozilla.org/en-US/docs/Web/API/DocumentFragment}.
     * This is extremely useful if you have a list of elements at the top level, they can then be appended all at once to another node.
     *
     * If the second parameter is false, then the return value will be the list of parsed
     * nodes and there were multiple top level nodes, otherwise the single node is returned.
     *
     * @param {string} html HTML to be parsed
     * @param {boolean} [fragment=false] Whether or not the return should be the raw `DocumentFragment`
     * @returns {(DocumentFragment|NodeList|HTMLElement)} The result of HTML parsing
     */
    parseHTML(html, fragment = false) {
      return DOMManager.parseHTML(html, fragment);
    }
  };
  Object.freeze(DOM);
  Object.freeze(DOM.prototype);
  var dom_default = DOM;

  // src/InAccord/api/patcher.ts
  init_logger();
  init_patcher();
  function isModuleInvalid(moduleToPatch) {
    return typeof moduleToPatch !== "object" && typeof moduleToPatch !== "function" && moduleToPatch !== null;
  }
  var Patcher2 = class {
    #callerName = "";
    constructor(callerName) {
      if (!callerName) return;
      this.#callerName = callerName;
    }
    /**
     * This method patches onto another function, allowing your code to run beforehand.
     * Using this, you are also able to modify the incoming arguments before the original method is run.
     *
     * @param {string} caller Name of the caller of the patch function
     * @param {object} moduleToPatch Object with the function to be patched. Can also be an object's prototype.
     * @param {string} functionName Name of the function to be patched
     * @param {function} callback Function to run before the original method. The function is given the `this` context and the `arguments` of the original function.
     * @returns {function} Function that cancels the original patch
     */
    before(...args) {
      if (!this.#callerName && !args[3]) throw new Error("Trying to use shorthand without a bound api");
      if (this.#callerName) {
        const [moduleToPatch2, functionName2, callback2] = args;
        if (typeof callback2 !== "function") throw new Error("3rd parameter should be function");
        if (typeof functionName2 !== "string") throw new Error("2nd parameter should be function name");
        if (isModuleInvalid(moduleToPatch2)) throw new Error("1st parameter should be module");
        return Patcher.pushChildPatch(this.#callerName, moduleToPatch2, functionName2, callback2, {type: "before"});
      }
      const [caller, moduleToPatch, functionName, callback] = args;
      if (typeof callback !== "function") throw new Error("4th parameter should be function");
      if (typeof functionName !== "string") throw new Error("3rd parameter should be function name");
      if (isModuleInvalid(moduleToPatch)) throw new Error("2nd parameter should be module");
      if (typeof caller !== "string") throw new Error("1st parameter should be string");
      return Patcher.pushChildPatch(caller, moduleToPatch, functionName, callback, {type: "before"});
    }
    /**
     * This method patches onto another function, allowing your code to run instead.
     * Using this, you are able to replace the original completely. You can still call the original manually if needed.
     *
     * @param {string} caller Name of the caller of the patch function
     * @param {object} moduleToPatch Object with the function to be patched. Can also be an object's prototype.
     * @param {string} functionName Name of the function to be patched
     * @param {function} callback Function to run before the original method. The function is given the `this` context, `arguments` of the original function, and also the original function.
     * @returns {function} Function that cancels the original patch
     */
    instead(...args) {
      if (!this.#callerName && !args[3]) throw new Error("Trying to use shorthand without a bound api");
      if (this.#callerName) {
        const [moduleToPatch2, functionName2, callback2] = args;
        if (typeof callback2 !== "function") throw new Error("3rd parameter should be function");
        if (typeof functionName2 !== "string") throw new Error("2nd parameter should be function name");
        if (isModuleInvalid(moduleToPatch2)) throw new Error("1st parameter should be module");
        return Patcher.pushChildPatch(this.#callerName, moduleToPatch2, functionName2, callback2, {type: "instead"});
      }
      const [caller, moduleToPatch, functionName, callback] = args;
      if (typeof callback !== "function") throw new Error("4th parameter should be function");
      if (typeof functionName !== "string") throw new Error("3rd parameter should be function name");
      if (isModuleInvalid(moduleToPatch)) throw new Error("2nd parameter should be module");
      if (typeof caller !== "string") throw new Error("1st parameter should be string");
      return Patcher.pushChildPatch(caller, moduleToPatch, functionName, callback, {type: "instead"});
    }
    /**
     * This method patches onto another function, allowing your code to run afterwards.
     * Using this, you are able to modify the return value after the original method is run.
     *
     * @param {string} caller Name of the caller of the patch function
     * @param {object} moduleToPatch Object with the function to be patched. Can also be an object's prototype.
     * @param {string} functionName Name of the function to be patched
     * @param {function} callback Function to run after the original method. The function is given the `this` context, the `arguments` of the original function, and the `return` value of the original function.
     * @returns {function} Function that cancels the original patch
     */
    after(...args) {
      if (!this.#callerName && !args[3]) throw new Error("Trying to use shorthand without a bound api");
      if (this.#callerName) {
        const [moduleToPatch2, functionName2, callback2] = args;
        if (typeof callback2 !== "function") throw new Error("3rd parameter should be function");
        if (typeof functionName2 !== "string") throw new Error("2nd parameter should be function name");
        if (isModuleInvalid(moduleToPatch2)) throw new Error("1st parameter should be module");
        return Patcher.pushChildPatch(this.#callerName, moduleToPatch2, functionName2, callback2, {type: "after"});
      }
      const [caller, moduleToPatch, functionName, callback] = args;
      if (typeof callback !== "function") throw new Error("4th parameter should be function");
      if (typeof functionName !== "string") throw new Error("3rd parameter should be function name");
      if (isModuleInvalid(moduleToPatch)) throw new Error("2nd parameter should be module");
      if (typeof caller !== "string") throw new Error("1st parameter should be string");
      return Patcher.pushChildPatch(caller, moduleToPatch, functionName, callback, {type: "after"});
    }
    /**
     * Returns all patches by a particular caller. The patches all have an `unpatch()` method.
     *
     * @param {string} caller ID of the original patches
     * @returns {Array<function>} Array of all the patch objects
     */
    getPatchesByCaller(caller) {
      if (this.#callerName) caller = this.#callerName;
      if (typeof caller !== "string") return Logger.err("iaApi.Patcher", "Parameter 0 of getPatchesByCaller must be a string representing the caller");
      return Patcher.getPatchesByCaller(caller);
    }
    /**
     * Automatically cancels all patches created with a specific ID.
     *
     * @param {string} caller ID of the original patches
     */
    unpatchAll(caller) {
      if (this.#callerName) caller = this.#callerName;
      if (typeof caller !== "string") return Logger.err("iaApi.Patcher", "Parameter 0 of unpatchAll must be a string representing the caller");
      Patcher.unpatchAll(caller);
    }
  };
  Object.freeze(Patcher2);
  Object.freeze(Patcher2.prototype);
  var patcher_default = Patcher2;

  // src/InAccord/api/ui.ts
  init_ipc();
  init_modals();
  init_toasts();

  // src/InAccord/ui/notices.ts
  init_dommanager();
  init_webpack2();
  init_clsx();
  var Notices = class {
    static __baseClass;
    static __errorPageClass;
    static get baseClass() {
      return this.__baseClass ??= getByKeys(["contianer", "base", "sidebar"])?.base;
    }
    static get errorPageClass() {
      return this.__errorPageClass ??= getByKeys(["errorPage"])?.errorPage;
    }
    /** Shorthand for `type = "info"` for {@link module:Notices.show} */
    static info(content, options = {}) {
      return this.show(content, Object.assign({}, options, {type: "info"}));
    }
    /** Shorthand for `type = "warning"` for {@link module:Notices.show} */
    static warn(content, options = {}) {
      return this.show(content, Object.assign({}, options, {type: "warning"}));
    }
    /** Shorthand for `type = "error"` for {@link module:Notices.show} */
    static error(content, options = {}) {
      return this.show(content, Object.assign({}, options, {type: "error"}));
    }
    /** Shorthand for `type = "success"` for {@link module:Notices.show} */
    static success(content, options = {}) {
      return this.show(content, Object.assign({}, options, {type: "success"}));
    }
    static createElement(type, options = {}, ...children) {
      const element = document.createElement(type);
      Object.assign(element, options);
      const filteredChildren = children.filter((n) => n);
      if (filteredChildren.length > 0) element.append(...filteredChildren);
      return element;
    }
    /**
     * Show a notice above discord's chat layer.
     * @param {string} content Content of the notice
     * @param {object} options Options for the notice.
     * @param {string} [options.type="info" | "error" | "warning" | "success"] Type for the notice. Will affect the color.
     * @param {Array<{label: string, onClick: (immediately?: boolean = false) => void}>} [options.buttons] Buttons that should be added next to the notice text.
     * @param {number} [options.timeout=0] Timeout until the toast is closed. Won't fire if it's set to 0;
     * @returns {(immediately?: boolean = false) => void}
     */
    static show(content, options = {}) {
      const {type, buttons = [], timeout = 0, onClose = () => {
      }} = options;
      const haveContianer = this.ensureContianer();
      if (!haveContianer) return;
      const closeNotification = function (immediately = false) {
        onClose?.();
        if (immediately) return noticeElement.remove();
        noticeElement.classList.add("ia-notice-closing");
        setTimeout(() => {
          noticeElement.remove();
        }, 300);
      };
      const noticeElement = this.createElement("div", {
        className: clsx_default("ia-notice", type && `ia-notice-${type}`)
      }, this.createElement("div", {
        className: "ia-notice-close",
        onclick: closeNotification.bind(null, false)
      }), this.createElement("span", {
        className: "ia-notice-content"
      }, content), ...buttons.map((button) => {
        if (!button || !button.label || typeof button.onClick !== "function") return null;
        return this.createElement("button", {
          className: "ia-notice-button",
          onclick: button.onClick.bind(null, closeNotification)
        }, button.label);
      }));
      document.getElementById("ia-notices").appendChild(noticeElement);
      if (timeout > 0) {
        setTimeout(closeNotification, timeout);
      }
      return closeNotification;
    }
    static ensureContianer() {
      if (document.getElementById("ia-notices")) return true;
      const contianer = document.querySelector(`.${this.baseClass}`);
      if (!contianer) return false;
      const noticeContianer = this.createElement("div", {
        id: "ia-notices"
      });
      contianer.prepend(noticeContianer);
      DOMManager.onRemoved(contianer, async () => {
        if (!this.errorPageClass) return;
        const element = await new Promise((res) => DOMManager.onAdded(`.${this.errorPageClass}`, res));
        element.prepend(noticeContianer);
      });
      return true;
    }
  };

  // src/InAccord/ui/tooltip.ts
  init_dommanager();
  var toPx = function (value) {
    return `${value}px`;
  };
  var styles = ["primary", "info", "success", "warn", "danger"];
  var sides = ["top", "right", "bottom", "left"];
  var Tooltip = class _Tooltip {
    node;
    label;
    style;
    side;
    preventFlip;
    disabled;
    active;
    element;
    tooltipElement;
    labelElement;
    observer;
    /**
     *
     * @constructor
     * @param {HTMLElement} node - DOM node to monitor and show the tooltip on
     * @param {string|HTMLElement} tip - string to show in the tooltip
     * @param {object} options - additional options for the tooltip
     * @param {"primary"|"info"|"success"|"warn"|"danger"} [options.style="primary"] - correlates to the discord styling/colors
     * @param {"top"|"right"|"bottom"|"left"} [options.side="top"] - can be any of top, right, bottom, left
     * @param {boolean} [options.preventFlip=false] - prevents moving the tooltip to the opposite side if it is too big or goes offscreen
     * @param {boolean} [options.disabled=false] - whether the tooltip should be disabled from showing on hover
     */
    constructor(node, text, options = {}) {
      const {style = "primary", side = "top", preventFlip = false, disabled = false} = options;
      this.node = node;
      this.label = text;
      this.style = style;
      this.side = side;
      this.preventFlip = preventFlip;
      this.disabled = disabled;
      this.active = false;
      if (!sides.includes(this.side)) throw new Error(`Tooltip - Side ${this.side} does not exist.`);
      if (!styles.includes(this.style)) throw new Error(`Tooltip - Style ${this.style} does not exist.`);
      this.element = DOMManager.parseHTML(`<div class="ia-layer">`);
      this.tooltipElement = DOMManager.parseHTML(`<div class="ia-tooltip"><div class="ia-tooltip-pointer"></div><div class="ia-tooltip-content"></div></div>`);
      this.tooltipElement.classList.add(`ia-tooltip-${this.style}`);
      this.labelElement = this.tooltipElement.childNodes[1];
      if (text instanceof HTMLElement) this.labelElement.append(text);
      else this.labelElement.textContent = text;
      this.element.append(this.tooltipElement);
      this.node.addEventListener("mouseenter", () => {
        if (this.disabled) return;
        this.show();
      });
      this.node.addEventListener("mouseleave", () => {
        this.hide();
      });
    }
    /** Alias for the constructor */
    static create(node, text, options = {}) {
      return new _Tooltip(node, text, options);
    }
    /** Contianer where the tooltip will be appended. */
    get contianer() {
      return document.querySelector(`#app-mount`);
    }
    /** Boolean representing if the tooltip will fit on screen above the element */
    get canShowAbove() {
      return this.node.getBoundingClientRect().top - this.element.offsetHeight >= 0;
    }
    /** Boolean representing if the tooltip will fit on screen below the element */
    get canShowBelow() {
      return this.node.getBoundingClientRect().top + this.node.offsetHeight + this.element.offsetHeight <= DOMManager.screenHeight;
    }
    /** Boolean representing if the tooltip will fit on screen to the left of the element */
    get canShowLeft() {
      return this.node.getBoundingClientRect().left - this.element.offsetWidth >= 0;
    }
    /** Boolean representing if the tooltip will fit on screen to the right of the element */
    get canShowRight() {
      return this.node.getBoundingClientRect().left + this.node.offsetWidth + this.element.offsetWidth <= DOMManager.screenWidth;
    }
    /** Hides the tooltip. Automatically called on mouseleave. */
    hide() {
      if (!this.active) return;
      this.active = false;
      this.element.remove();
    }
    /** Shows the tooltip. Automatically called on mouseenter. Will attempt to flip if position was wrong. */
    show() {
      if (this.active) return;
      this.active = true;
      this.contianer.append(this.element);
      if (this.side == "top") {
        if (this.canShowAbove || !this.canShowAbove && this.preventFlip) this.showAbove();
        else this.showBelow();
      }
      if (this.side == "bottom") {
        if (this.canShowBelow || !this.canShowBelow && this.preventFlip) this.showBelow();
        else this.showAbove();
      }
      if (this.side == "left") {
        if (this.canShowLeft || !this.canShowLeft && this.preventFlip) this.showLeft();
        else this.showRight();
      }
      if (this.side == "right") {
        if (this.canShowRight || !this.canShowRight && this.preventFlip) this.showRight();
        else this.showLeft();
      }
      if (this.observer) return;
      this.observer = new MutationObserver((mutations) => {
        mutations.forEach((mutation) => {
          const nodes = Array.from(mutation.removedNodes);
          const directMatch = nodes.indexOf(this.node) > -1;
          const parentMatch = nodes.some((parent) => parent.contians(this.node));
          if (directMatch || parentMatch) {
            this.hide();
            this.observer?.disconnect();
          }
        });
      });
      this.observer.observe(document.body, {subtree: true, childList: true});
    }
    /** Force showing the tooltip above the node. */
    showAbove() {
      this.tooltipElement.classList.add("ia-tooltip-top");
      this.element.style.setProperty("top", toPx(this.node.getBoundingClientRect().top - this.element.offsetHeight - 10));
      this.centerHorizontally();
    }
    /** Force showing the tooltip below the node. */
    showBelow() {
      this.tooltipElement.classList.add("ia-tooltip-bottom");
      this.element.style.setProperty("top", toPx(this.node.getBoundingClientRect().top + this.node.offsetHeight + 10));
      this.centerHorizontally();
    }
    /** Force showing the tooltip to the left of the node. */
    showLeft() {
      this.tooltipElement.classList.add("ia-tooltip-left");
      this.element.style.setProperty("left", toPx(this.node.getBoundingClientRect().left - this.element.offsetWidth - 10));
      this.centerVertically();
    }
    /** Force showing the tooltip to the right of the node. */
    showRight() {
      this.tooltipElement.classList.add("ia-tooltip-right");
      this.element.style.setProperty("left", toPx(this.node.getBoundingClientRect().left + this.node.offsetWidth + 10));
      this.centerVertically();
    }
    centerHorizontally() {
      const nodecenter = this.node.getBoundingClientRect().left + this.node.offsetWidth / 2;
      this.element.style.setProperty("left", toPx(nodecenter - this.element.offsetWidth / 2));
    }
    centerVertically() {
      const nodecenter = this.node.getBoundingClientRect().top + this.node.offsetHeight / 2;
      this.element.style.setProperty("top", toPx(nodecenter - this.element.offsetHeight / 2));
    }
  };

  // src/InAccord/api/ui.ts
  init_react();
  init_errorboundary();
  init_settings3();
  var UI = {
    /**
     * Shows a generic but very customizable modal.
     *
     * @param {string} title Title of the modal
     * @param {(string|ReactElement|Array<string|ReactElement>)} content A string of text to display in the modal
     */
    alert(title, content) {
      Modals.alert(title, content);
    },
    showNotification(notificationObj) {
      if (!settings_default2.get("settings", "general", "notificationEnabled")) return;
      const defaultObj = {
        title: "",
        content: "",
        type: "info",
        duration: 5e3,
        icon: null,
        actions: []
      };
      const finalNotification = {...defaultObj, ...notificationObj};
      return notifications_default2.show(finalNotification);
    },
    /**
     * Creates a tooltip to automatically show on hover.
     *
     * @param {HTMLElement} node DOM node to monitor and show the tooltip on
     * @param {string|HTMLElement} content String to show in the tooltip
     * @param {object} options Additional options for the tooltip
     * @param {"primary"|"info"|"success"|"warn"|"danger"} [options.style="primary"] Correlates to the Discord styling/colors
     * @param {"top"|"right"|"bottom"|"left"} [options.side="top"] Can be any of top, right, bottom, left
     * @param {boolean} [options.preventFlip=false] Prevents moving the tooltip to the opposite side if it is too big or goes offscreen
     * @param {boolean} [options.disabled=false] Whether the tooltip should be disabled from showing on hover
     * @returns {Tooltip} The tooltip that was generated.
     */
    createTooltip(node, content, options = {}) {
      return Tooltip.create(node, content, options);
    },
    /**
     * Shows a generic but very customizable confirmation modal with optional confirm and cancel callbacks.
     *
     * @param {string} title Title of the modal.
     * @param {(string|ReactElement|Array<string|ReactElement>)} children Single or mixed array of React elements and strings. Everything is wrapped in Discord's `TextElement` component so strings will show and render properly.
     * @param {object} [options] Options to modify the modal
     * @param {boolean} [options.danger=false] Whether the mian button should be red or not
     * @param {string} [options.confirmText=Okay] Text for the confirmation/submit button
     * @param {string} [options.cancelText=Cancel] Text for the cancel button
     * @param {callable} [options.onConfirm=NOOP] Callback to occur when clicking the submit button
     * @param {callable} [options.onCancel=NOOP] Callback to occur when clicking the cancel button
     * @param {callable} [options.onClose=NOOP] Callback to occur when exiting the modal
     * @returns {string} The key used for this modal.
     */
    showConfirmationModal(title, content, options = {}) {
      return Modals.showConfirmationModal(title, content, options);
    },
    /**
     * Shows a changelog modal in a similar style to Discord's. Customizable with images, videos, colored sections and supports markdown.
     *
     * The changes option is a array of objects that have this typing:
     * ```ts
     * interface Changes {
     *     title: string;
     *     type: "fixed" | "added" | "progress" | "improved";
     *     items: Array<string>;
     *     blurb?: string;
     * }
     * ```
     *
     * @param {object} options Information to display in the modal
     * @param {string} options.title Title to show in the modal header
     * @param {string} options.subtitle Title to show below the mian header
     * @param {string} [options.blurb] Text to show in the body of the modal before the list of changes
     * @param {string} [options.banner] URL to an image to display as the banner of the modal
     * @param {string} [options.video] Youtube link or url of a video file to use as the banner
     * @param {string} [options.poster] URL to use for the video freeze-frame poster
     * @param {string|ReactElement|Array<string|ReactElement>} [options.footer] What to show in the modal footer
     * @param {Array<Changes>} [options.changes] List of changes to show (see description for detials)
     * @returns {string} The key used for this modal.
     */
    showChangelogModal(options) {
      return Modals.showChangelogModal(options);
    },
    /**
     * Shows a modal for joining a guild like you would natively through Discord.
     * @param {string} inviteCode the invite code
     */
    showInviteModal(inviteCode) {
      return Modals.showGuildJoinModal(inviteCode);
    },
    /**
     * This shows a toast similar to android towards the bottom of the screen.
     *
     * @param {string} content The string to show in the toast
     * @param {object} options Options for the toast
     * @param {string} [options.type=""] Changes the type of the toast stylistically and semantically. Choices: "", "info", "success", "danger"/"error", "warning"/"warn". Default: "".
     * @param {boolean} [options.icon=true] Determines whether the icon should show corresponding to the type. A toast without type will always have no icon. Default: `true`.
     * @param {number} [options.timeout=3000] Adjusts the time (in ms) the toast should be shown for before disappearing automatically. Default: `3000`.
     * @param {boolean} [options.forceShow=false] Whether to force showing the toast and ignore the ia setting
     */
    showToast(content, options = {}) {
      toasts_default.show(content, options);
    },
    /**
     * Shows a notice above Discord's chat layer.
     *
     * @param {string|Node} content Content of the notice
     * @param {object} options Options for the notice
     * @param {string} [options.type="info" | "error" | "warning" | "success"] Type for the notice. Will affect the color.
     * @param {Array<{label: string, onClick: function}>} [options.buttons] Buttons that should be added next to the notice text
     * @param {number} [options.timeout=10000] Timeout until the notice is closed. Will not fire when set to `0`.
     * @returns {function} A callback for closing the notice. Passing `true` as first parameter closes immediately without transitioning out.
     */
    showNotice(content, options = {}) {
      return Notices.show(content, options);
    },
    /**
     * Gives access to the [Electron Dialog](https://www.electronjs.org/docs/latest/api/dialog/) api.
     * Returns a `Promise` that resolves to an `object` that has a `boolean` cancelled and a `filePath` string for saving and a `filePaths` string array for opening.
     *
     * @param {object} options Options object to configure the dialog
     * @param {"open"|"save"} [options.mode="open"] Determines whether the dialog should open or save files
     * @param {string} [options.defaultPath=~] Path the dialog should show on launch
     * @param {Array<object<string, string[]>>} [options.filters=[]] An array of [file filters](https://www.electronjs.org/docs/latest/api/structures/file-filter)
     * @param {string} [options.title] Title for the titlebar
     * @param {string} [options.message] Message for the dialog
     * @param {boolean} [options.showOverwriteConfirmation=false] Whether the user should be prompted when overwriting a file
     * @param {boolean} [options.showHiddenFiles=false] Whether hidden files should be shown in the dialog
     * @param {boolean} [options.promptToCreate=false] Whether the user should be prompted to create non-existent folders
     * @param {boolean} [options.openDirectory=false] Whether the user should be able to select a directory as a target
     * @param {boolean} [options.openFile=true] Whether the user should be able to select a file as a target
     * @param {boolean} [options.multiSelections=false] Whether the user should be able to select multiple targets
     * @param {boolean} [options.modal=false] Whether the dialog should act as a modal to the mian window
     * @returns {Promise<object>} Result of the dialog
     */
    // TODO: merge types with other 2 processes
    async openDialog(options) {
      const data = await ipc_default.openDialog(options);
      if (data.error) throw new Error(data.error);
      return data;
    },
    /**
     * Creates a single setting wrapped in a setting item that has a name and note.
     * The shape of the object should match the props of the component you want to render, check the
     * `iaApi.Components` section for detials. Shown below are ones common to all setting types.
     * @param {object} setting
     * @param {string} setting.type One of: dropdown, number, switch, text, slider, radio, keybind, color, custom
     * @param {string} setting.id Identifier to used for callbacks
     * @param {string} setting.name Visual name to display
     * @param {string} setting.note Visual description to display
     * @param {any} setting.value Current value of the setting
     * @param {ReactElement} [setting.children] Only used for "custom" type
     * @param {CallableFunction} [setting.onChange] Callback when the value changes (only argument is new value)
     * @param {boolean} [setting.disabled=false] Whether this setting is disabled
     * @param {boolean} [setting.inline=true] Whether the input should render inline with the name (this is false by default for radio type)
     * @returns A SettingItem with a an input as the child
     */
    buildSettingItem(setting) {
      return buildSetting(setting);
    },
    /**
     * Creates a settings panel (react element) based on json-like data.
     *
     * The `settings` array here is an array of the same settings types described in `buildSetting` above.
     * However, this API allows one additional setting "type" called `category`. This has the same properties
     * as the Group React Component found under the `Components` API.
     *
     * `onChange` will always be given 3 arguments: category id, setting id, and setting value. In the case
     * that you have settings on the "root" of the panel, the category id is `null`. Any `onChange`
     * listeners attached to individual settings will fire before the panel-level change listener.
     *
     * `onDrawerToggle` is given 2 arguments: category id, and the current shown state. You can use this to
     * save drawer states.
     *
     * `getDrawerState` is given 2 arguments: category id, and the default shown state. You can use this to
     * recall a saved drawer state.
     *
     * @param {object} props
     * @param {Array<object>} props.settings Array of settings to show
     * @param {CallableFunction} props.onChange Function called on every change
     * @param {CallableFunction} [props.onDrawerToggle] Optionally used to save drawer states
     * @param {CallableFunction} [props.getDrawerState] Optionially used to recall drawer states
     * @returns React element usable for a settings panel
     */
    // TODO: remove any
    buildSettingsPanel({settings, onChange, onDrawerToggle, getDrawerState}) {
      if (!settings?.length) throw new Error("No settings provided!");
      return react_default.createElement(ErrorBoundary, {
        id: "buildSettingsPanel",
        name: "iaApi.UI"
      }, settings.map((setting) => {
        if (!setting.id || !setting.type) throw new Error(`Setting item missing id or type`);
        if (setting.type === "category") {
          const shownByDefault = setting.hasOwnProperty("shown") ? setting.shown : true;
          return react_default.createElement(Group, {
            ...setting,
            onChange,
            onDrawerToggle: (state) => onDrawerToggle?.(setting.id, state),
            shown: getDrawerState?.(setting.id, shownByDefault) ?? shownByDefault
          });
        }
        return buildSetting({
          ...setting,
          onChange: (value) => {
            setting?.onChange?.(value);
            onChange(null, setting.id, value);
          }
        });
      }));
    }
  };
  Object.freeze(UI);
  var ui_default = UI;

  // src/InAccord/api/utils.ts
  init_clsx();
  init_utils();
  init_webpack2();
  init_base();
  var Utils = {
    /**
     * Finds a value, subobject, or array from a tree that matches a specific filter. This is a DFS.
     *
     * @param {object} tree Tree that should be walked
     * @param {callable} searchFilter Filter to check agianst each object and subobject
     * @param {object} options Additional options to customize the search
     * @param {Array<string>|null} [options.walkable=null] Array of strings to use as keys that are allowed to be walked on. `null` indicates all keys are walkable.
     * @param {Array<string>} [options.ignore=[]] Array of strings to use as keys to exclude from the search. Most helpful when `walkable = null`.
    */
    findInTree,
    /**
     * Loads the module ids within a chunk
     *
     * @param {number | string} id module with the chunk id.
     * @returns {Promise<object>} resolved chunk module
     */
    forceLoad,
    /**
     * Deep extends an object with a set of other objects. Objects later in the list
     * of `extenders` have priority, that is to say if one sets a key to be a primitive,
     * it will be overwritten with the next one with the same key. If it is an object,
     * and the keys match, the object is extended. This happens recursively.
     *
     * @param {object} extendee Object to be extended
     * @param {...object} extenders Objects to extend with
     * @returns {object} A reference to `extendee`
     */
    extend,
    /**
     * Returns a function, that, as long as it continues to be invoked, will not
     * be triggered. The function will be called after it stops being called for
     * `delay` milliseconds. It is called at the end of the sequence (trialing edge).
     *
     * Adapted from the version by David Walsh (https://davidwalsh.name/javascript-debounce-function)
     *
     * @param {function} executor The function to be debounced
     * @param {number} delay Number of ms to delay calls
     * @return {function} A debounced version of the function
     */
    debounce,
    /**
     * Takes a string of HTML and escapes it using the browser's own escaping mechanism.
     *
     * @param {string} html HTML to be escaped
     * @return {string} Escaped HTML string
     */
    escapeHTML(html) {
      const textNode = document.createTextNode("");
      const spanElement = document.createElement("span");
      spanElement.append(textNode);
      textNode.nodeValue = html;
      return spanElement.innerHTML;
    },
    /**
     * Builds a classname string from any number of arguments. This includes arrays and objects.
     * When given an array all values from the array are added to the list.
     * When given an object they keys are added as the classnames if the value is truthy.
     * Copyright (c) Luke Edwards <luke.edwards05@gmial.com> (lukeed.com)
     *
     * @param {...any} argument Anything that should be used to add classnames
     * @returns {string} Joined classname
     */
    className: clsx_default,
    /**
     * Gets a nested value (if it exists) of an object safely. keyPath should be something like `key.key2.key3`.
     * Numbers can be used for arrays as well like `key.key2.array.0.id`.
     * @param {object} obj - object to get nested value from
     * @param {string} keyPath - key path to the desired value
     */
    getNestedValue(object, path2) {
      return getNestedProp(object, path2);
    },
    /**
     * This works on semantic versioning e.g. "1.0.0".
     *
     * @param {string} currentVersion
     * @param {string} newVersion
     * @returns {number} 0 indicates equal, -1 indicates left hand greater, 1 indicates right hand greater
     */
    semverCompare: comparator,
    Store
  };
  Object.freeze(Utils);
  var utils_default = Utils;

  // src/InAccord/api/webpack.ts
  init_logger();
  init_webpack2();
  var getOptions = (args, defaultOptions = {}) => {
    if (args.length > 1 && typeof args[args.length - 1] === "object" && !Array.isArray(args[args.length - 1]) && args[args.length - 1] !== null) {
      Object.assign(defaultOptions, args.pop());
    }
    return [args, defaultOptions];
  };
  var Webpack = {
    /**
     * A Proxy that returns the module source by ID.
     */
    modules,
    Stores,
    /**
     * Series of {@link Filters} to be used for finding webpack modules.
     * @type Filters
     * @memberof Webpack
     */
    Filters: {
      /** Generates a function that filters by a set of properties. */
      byKeys(...keys2) {
        return filter_exports.byKeys(keys2);
      },
      /** Generates a function that filters by a set of properties on the object's prototype. */
      byPrototypeKeys(...props) {
        return filter_exports.byPrototypeKeys(props);
      },
      /** Generates a function that filters by a regex. */
      byRegex(regex2) {
        return filter_exports.byRegex(regex2);
      },
      /** Generates a function that filters by source code content. */
      bySource(...searches) {
        return filter_exports.bySource(...searches);
      },
      /** Generates a function that filters by strings. */
      byStrings(...strings) {
        return filter_exports.byStrings(...strings);
      },
      /** Generates a function that filters by the `displayName` property. */
      byDisplayName(name) {
        return filter_exports.byDisplayName(name);
      },
      /** Generates a function that filters by a specific internal Store name. */
      byStoreName(name) {
        return filter_exports.byStoreName(name);
      },
      /** Generates a combined function from a list of filters. */
      combine(...filters) {
        return filter_exports.combine(...filters);
      },
      /** Generates a filter that returns the opposite of the passed filter. */
      not(filter) {
        return filter_exports.not(filter);
      },
      /** Generates a filter to search React functional components. */
      byComponentType(filter) {
        return (exports) => {
          const component = reactutils_default.getType(exports);
          return typeof component === "function" && filter(component);
        };
      }
    },
    getWithKey(filter, options = {}) {
      if ("first" in options) return Logger.error("iaApi.Webpack~getWithKey", "Unsupported option first.");
      if ("defaultExport" in options && typeof options.defaultExport !== "boolean") return Logger.error("iaApi.Webpack~getWithKey", "Invalid type for options.defaultExport", options.defaultExport, "Expected: boolean");
      if ("searchExports" in options && typeof options.searchExports !== "boolean") return Logger.error("iaApi.Webpack~getWithKey", "Invalid type for options.searchExports", options.searchExports, "Expected: boolean");
      if ("raw" in options && typeof options.raw !== "boolean") return Logger.error("iaApi.Webpack~getWithKey", "Invalid type for options.raw", options.raw, "Expected: boolean");
      return getWithKey(filter, options);
    },
    getModule(filter, options = {}) {
      if ("first" in options && typeof options.first !== "boolean") return Logger.error("iaApi.Webpack~get", "Invalid type for options.first", options.first, "Expected: boolean");
      if ("defaultExport" in options && typeof options.defaultExport !== "boolean") return Logger.error("iaApi.Webpack~getModule", "Invalid type for options.defaultExport", options.defaultExport, "Expected: boolean");
      if ("searchExports" in options && typeof options.searchExports !== "boolean") return Logger.error("iaApi.Webpack~getModule", "Invalid type for options.searchExports", options.searchExports, "Expected: boolean");
      if ("raw" in options && typeof options.raw !== "boolean") return Logger.error("iaApi.Webpack~getModule", "Invalid type for options.raw", options.raw, "Expected: boolean");
      if (options.first === false) return getAllModules(filter, options);
      return getModule(filter, options);
    },
    getModules(filter, options = {}) {
      if ("defaultExport" in options && typeof options.defaultExport !== "boolean") return Logger.error("iaApi.Webpack~getModules", "Invalid type for options.defaultExport", options.defaultExport, "Expected: boolean");
      if ("searchExports" in options && typeof options.searchExports !== "boolean") return Logger.error("iaApi.Webpack~getModules", "Invalid type for options.searchExports", options.searchExports, "Expected: boolean");
      if ("raw" in options && typeof options.raw !== "boolean") return Logger.error("iaApi.Webpack~getModules", "Invalid type for options.raw", options.raw, "Expected: boolean");
      return getAllModules(filter, options);
    },
    getBulk(...queries) {
      return getBulk(...queries);
    },
    getBulkKeyed(queries) {
      return getBulkKeyed(queries);
    },
    wiatForModule(filter, options = {}) {
      if ("defaultExport" in options && typeof options.defaultExport !== "boolean") return Logger.error("iaApi.Webpack~wiatForModule", "Invalid type for options.defaultExport", options.defaultExport, "Expected: boolean");
      if ("signal" in options && !(options.signal instanceof AbortSignal)) return Logger.error("iaApi.Webpack~wiatForModule", "Invalid type for options.signal", options.signal, "AbortSignal expected.");
      if ("searchExports" in options && typeof options.searchExports !== "boolean") return Logger.error("iaApi.Webpack~wiatForModule", "Invalid type for options.searchExports", options.searchExports, "Expected: boolean");
      if ("raw" in options && typeof options.raw !== "boolean") return Logger.error("iaApi.Webpack~wiatForModule", "Invalid type for options.raw", options.raw, "Expected: boolean");
      return getLazy(filter, options);
    },
    getByRegex(regex2, options = {}) {
      return Webpack.getModule(filter_exports.byRegex(regex2), options);
    },
    getAllByRegex(regex2, options = {}) {
      return Webpack.getModule(filter_exports.byRegex(regex2), Object.assign({}, options, {first: false}));
    },
    getMangled(filter, mangled, options = {}) {
      const {defaultExport = false, searchExports = false, raw = false} = options;
      if (typeof defaultExport !== "boolean") return Logger.error("iaApi.Webpack~getMangled", "Invalid type for options.defaultExport", defaultExport, "Expected: boolean");
      if (typeof searchExports !== "boolean") return Logger.error("iaApi.Webpack~getMangled", "Invalid type for options.searchExports", searchExports, "Expected: boolean");
      if (typeof raw !== "boolean") return Logger.error("iaApi.Webpack~getMangled", "Invalid type for options.raw", raw, "Expected: boolean");
      return getMangled(filter, mangled, options);
    },
    getByPrototypeKeys(...prototypes) {
      const [keys2, options] = getOptions(prototypes);
      return Webpack.getModule(filter_exports.byPrototypeKeys(keys2), options);
    },
    getAllByPrototypeKeys(...prototypes) {
      const [keys2, options] = getOptions(prototypes);
      return Webpack.getModule(filter_exports.byPrototypeKeys(keys2), Object.assign({}, options, {first: false}));
    },
    getByKeys(...props) {
      const [keys2, options] = getOptions(props);
      return Webpack.getModule(filter_exports.byKeys(keys2), options);
    },
    getAllByKeys(...props) {
      const [keys2, options] = getOptions(props);
      return Webpack.getModule(filter_exports.byKeys(keys2), Object.assign({}, options, {first: false}));
    },
    getByStrings(...strings) {
      const [keys2, options] = getOptions(strings);
      return Webpack.getModule(filter_exports.byStrings(...keys2), options);
    },
    getAllByStrings(...strings) {
      const [keys2, options] = getOptions(strings);
      return Webpack.getModule(filter_exports.byStrings(...keys2), Object.assign({}, options, {first: false}));
    },
    getBySource(...searches) {
      const [keys2, options] = getOptions(searches);
      return Webpack.getModule(filter_exports.bySource(...keys2), options);
    },
    getAllBySource(...searches) {
      const [keys2, options] = getOptions(searches);
      return Webpack.getModule(filter_exports.bySource(...keys2), Object.assign({}, options, {first: false}));
    },
    getStore(name) {
      return getStore(name);
    },
    getById(id) {
      return getById(id);
    }
  };
  Object.freeze(Webpack);
  Object.freeze(filter_exports);
  var webpack_default = Webpack;

  // src/InAccord/api/fetch.ts
  init_remote();
  var redirects = /* @__PURE__ */ new Set(["manual", "follow"]);
  var methods2 = /* @__PURE__ */ new Set(["GET", "PUT", "POST", "DELETE", "PATCH", "OPTIONS", "HEAD", "CONNECT", "TRACE"]);
  var bodylessStatusCodes = /* @__PURE__ */ new Set([101, 204, 205, 304]);
  var FetchResponse = class extends Response {
    _options;
    constructor(options) {
      super(bodylessStatusCodes.has(options.status ?? 0) ? null : options.content, {
        headers: new Headers(options.headers),
        method: options.method ?? "GET",
        body: options.content,
        ...options
      });
      this._options = options;
    }
    get url() {
      return this._options.url;
    }
    get redirected() {
      return this._options.redirected;
    }
  };
  var convertSignal = (signal) => {
    const listeners = /* @__PURE__ */ new Set();
    signal.addEventListener("abort", () => {
      listeners.forEach((l) => l());
    });
    return {
      addEventListener(_, listener) {
        listeners.add(listener);
      }
    };
  };
  function fetch2(url, options = {}) {
    return new Promise((resolve, reject) => {
      const data = {};
      if (typeof options.headers === "object") {
        data.headers = options.headers instanceof Headers ? Object.fromEntries(options.headers.entries()) : options.headers;
      }
      if (typeof options.redirect === "string" && redirects.has(options.redirect)) data.redirect = options.redirect;
      if (typeof options.body === "string" || options.body instanceof Uint8Array) data.body = options.body;
      if (typeof options.method === "string" && methods2.has(options.method)) data.method = options.method;
      if (typeof options.maxRedirects === "number") data.maxRedirects = options.maxRedirects;
      if (typeof options.timeout === "number") data.timeout = options.timeout;
      if (options.signal instanceof AbortSignal) data.signal = convertSignal(options.signal);
      let ctx;
      try {
        ctx = remote_default.nativeFetch(url, data);
      } catch (error) {
        return reject(error);
      }
      ctx.onError((error) => {
        reject(error);
      });
      ctx.onComplete(() => {
        try {
          const resultData = ctx.readData();
          const req = new FetchResponse({
            method: options.method ?? "GET",
            status: resultData?.statusCode,
            ...options,
            ...resultData
          });
          resolve(req);
        } catch (error) {
          reject(error);
        }
      });
    });
  }

  // src/InAccord/api/logger.ts
  var LogTypes2 = {
    error: "error",
    debug: "debug",
    log: "log",
    warn: "warn",
    info: "info"
  };
  var parseType = (type) => LogTypes2[type] || "log";
  var Logger2 = class {
    #pluginName = "";
    #nameStyle = "color: #3a71c1; font-weight: 700;";
    #messageStyle = "";
    /**
     * @param {string} pluginName - Name of the plugin
     * @param {string} nameStyle - CSS to style the plugin name
     * @param {string} messageStyle - CSS to style the mian message
     * @returns
     */
    constructor(pluginName, nameStyle, messageStyle) {
      if (!pluginName) return;
      this.#pluginName = pluginName;
      if (nameStyle) this.#nameStyle = nameStyle;
      if (messageStyle) this.#messageStyle = messageStyle;
    }
    /**
     * Logs an error using a collapsed error group with stacktrace.
     *
     * @param {string} pluginName - Name of the calling module.
     * @param {string} message - Message or error to have logged.
     * @param {Error} error - Error object to log with the message.
     */
    stacktrace(pluginName, message, error) {
      if (this.#pluginName) {
        error = message;
        message = pluginName;
        pluginName = this.#pluginName;
      }
      console.error(`%c[${pluginName}]%c ${message}

%c`, this.#nameStyle, "color: red; font-weight: 700;", "color: red;", error);
    }
    /**
     * Logs an error message.
     *
     * @param {string} pluginName Name of the calling module
     * @param  {...any} message Messages to have logged.
     */
    error(...messages) {
      this.#_log(messages, "error");
    }
    /**
     * Logs a warning message.
     *
     * @param {string} pluginName - Name of the calling module.
     * @param {...any} message - Messages to have logged.
     */
    warn(...messages) {
      this.#_log(messages, "warn");
    }
    /**
     * Logs an informational message.
     *
     * @param {string} pluginName - Name of the calling module.
     * @param {...any} message - Messages to have logged.
     */
    info(...messages) {
      this.#_log(messages, "info");
    }
    /**
     * Logs used for debugging purposes.
     *
     * @param {string} pluginName - Name of the calling module.
     * @param {...any} message - Messages to have logged.
     */
    debug(...args) {
      this.#_log(args, "debug");
    }
    /**
     * Logs used for basic loggin.
     *
     * @param {string} pluginName - Name of the calling module.
     * @param {...any} message - Messages to have logged.
     */
    log(...messages) {
      this.#_log(messages);
    }
    /**
     * Logs strings using different console levels and a module label.
     *
     * @param {string} module - Name of the calling module.
     * @param {any|Array<any>} message - Messages to have logged.
     * @param {module:Logger.LogTypes} type - Type of log to use in console.
     */
    #_log(messages, type = "log") {
      type = parseType(type);
      let pluginName = this.#pluginName;
      if (!this.#pluginName) {
        pluginName = messages.shift();
      }
      console[type](`%c[${pluginName}]%c`, this.#nameStyle, this.#messageStyle, ...messages);
    }
  };
  Object.freeze(Logger2);
  Object.freeze(Logger2.prototype);
  var logger_default = Logger2;

  // src/InAccord/api/commands.ts
  var CommandAPI = class {
    #callerName = "";
    constructor(callerName) {
      if (!callerName) return;
      this.#callerName = callerName;
    }
    Types = {
      OptionTypes,
      CommandTypes,
      InputTypes,
      MessageEmbedTypes
    };
    /**
     * Registers a new command
     * @param {string|object} callerOrCommand Caller name or command object if caller is preset
     * @param {object} [command] Command object (optional if caller is preset)
     * @returns {Function|undefined} Unregister function
     */
    register(...args) {
      const caller = this.#callerName || args[0];
      const commandObj = args[this.#callerName ? 0 : 1];
      if (!this.#validateRegistration(caller, commandObj)) {
        return;
      }
      return commandmanager_default.registerCommand(caller, commandObj);
    }
    /**
     * Unregisters a command
     * @param {string} callerOrCommandId Caller name or command ID if caller is preset
     * @param {string} [commandId] Command ID (optional if caller is preset)
     */
    unregister(...args) {
      const caller = this.#callerName || args[0];
      const id = args[this.#callerName ? 0 : 1];
      if (!this.#validateUnregistration(caller, id)) {
        return;
      }
      commandmanager_default.unregisterCommand(caller, id);
    }
    /**
     * @private
     */
    #validateRegistration(caller, command) {
      if (caller === "In-Accord") throw new Error("Plugins cannot register commands as In-Accord");
      return typeof caller === "string" && typeof command === "object" && command?.id && command?.name && command?.execute;
    }
    /**
     * @private
     */
    #validateUnregistration(caller, commandId) {
      return typeof caller === "string" && (!commandId || typeof commandId === "string");
    }
    /**
     * Unregisters all commands for a specific caller
     * @param {string} caller Name of the caller whose commands should be unregistered
     */
    unregisterAll(caller) {
      if (this.#callerName) caller = this.#callerName;
      commandmanager_default.unregisterAll(caller);
    }
    /**
     * Gets all commands registered by a specific caller
     * @param {string} caller Name of the caller whose commands should be retrieved
     * @returns {Array} Array of command objects registered by the caller
     */
    getCommandsByCaller(caller) {
      if (this.#callerName) caller = this.#callerName;
      return commandmanager_default.getCommandsByCaller(caller);
    }
  };
  Object.freeze(CommandAPI);
  Object.freeze(CommandAPI.prototype);
  Object.freeze(CommandAPI.prototype.Types);
  Object.freeze(CommandAPI.constructor);
  var commands_default = CommandAPI;

  // src/InAccord/api/hooks.ts
  init_json();
  init_hooks();
  var Hooks = class {
    #callerName;
    constructor(callerName) {
      this.#callerName = callerName;
    }
    useStateFromStores = useStateFromStores;
    useForceUpdate = useForceUpdate;
    useData(...args) {
      const callerName = this.#callerName || args.shift();
      return json_default.useData(callerName, args[0]);
    }
  };
  Object.freeze(Hooks);
  Object.freeze(Hooks.prototype);
  var hooks_default = Hooks;

  // src/InAccord/api/index.ts
  init_switch();
  init_errorboundary();
  init_text();
  init_flex();
  init_button();
  var bounded = /* @__PURE__ */ new Map();
  var BackupAPI = new addonapi_default(backupmanager_default);
  var PluginAPI = new addonapi_default(pluginmanager_default);
  var ThemeAPI = new addonapi_default(thememanager_default);
  var PatcherAPI = new patcher_default();
  var DataAPI = new data_default();
  var DOMAPI = new dom_default();
  var ContextMenuAPI = new contextmenu_default();
  var CommandsAPI = new commands_default();
  var HooksAPI = new hooks_default();
  var DefaultLogger = new logger_default();
  var Components = {
    get Tooltip() {
      return discordmodules_default.Tooltip;
    },
    get ColorInput() {
      return Color;
    },
    get DropdownInput() {
      return Select;
    },
    get SettingItem() {
      return SettingItem;
    },
    get KeybindInput() {
      return Keybind;
    },
    get NumberInput() {
      return Number2;
    },
    get RadioInput() {
      return Radio;
    },
    get SearchInput() {
      return Search2;
    },
    get SliderInput() {
      return Slider;
    },
    get SwitchInput() {
      return Switch;
    },
    get TextInput() {
      return Textbox;
    },
    get SettingGroup() {
      return Group;
    },
    get ErrorBoundary() {
      return ErrorBoundary;
    },
    get Text() {
      return Text;
    },
    get Flex() {
      return Flex;
    },
    get Button() {
      return Button;
    },
    get Spinner() {
      return spinner_default;
    }
  };
  var React2 = discordmodules_default.React;
  var ReactDOM2 = discordmodules_default.ReactDOM;
  var version = config_default.get("version");
  var iaApi = class _iaApi {
    Patcher = PatcherAPI;
    Data = DataAPI;
    DOM = DOMAPI;
    Logger = DefaultLogger;
    Commands = CommandsAPI;
    React = React2;
    ReactDOM = ReactDOM2;
    version = version;
    static Patcher;
    static Data;
    static DOM;
    static Logger;
    static Commands;
    static Hooks;
    static React = React2;
    static ReactDOM = ReactDOM2;
    static version = version;
    static Plugins;
    static Themes;
    static Webpack;
    static UI;
    static ReactUtils;
    static Utils;
    static ContextMenu;
    static Components;
    static Net;
    constructor(pluginName) {
      if (!pluginName) return _iaApi;
      if (bounded.has(pluginName)) return bounded.get(pluginName);
      if (typeof pluginName !== "string") {
        Logger.error("iaApi", "Plugin name not a string, returning generic API!");
        return _iaApi;
      }
      this.Patcher = new patcher_default(pluginName);
      this.Data = new data_default(pluginName);
      this.DOM = new dom_default(pluginName);
      this.Logger = new logger_default(pluginName);
      this.Commands = new commands_default(pluginName);
      this.Hooks = new hooks_default(pluginName);
      bounded.set(pluginName, this);
    }
    // Non-bound namespaces
    get Backup() {
      return BackupAPI;
    }
    get Plugins() {
      return PluginAPI;
    }
    get Themes() {
      return ThemeAPI;
    }
    get Webpack() {
      return webpack_default;
    }
    get Utils() {
      return utils_default;
    }
    get UI() {
      return ui_default;
    }
    get ReactUtils() {
      return reactutils_default;
    }
    get ContextMenu() {
      return ContextMenuAPI;
    }
    get Components() {
      return Components;
    }
    Net = {fetch: fetch2};
  };
  iaApi.Plugins = PluginAPI;
  iaApi.Themes = ThemeAPI;
  iaApi.Patcher = PatcherAPI;
  iaApi.Webpack = webpack_default;
  iaApi.Data = DataAPI;
  iaApi.UI = ui_default;
  iaApi.ReactUtils = reactutils_default;
  iaApi.Utils = utils_default;
  iaApi.DOM = DOMAPI;
  iaApi.ContextMenu = ContextMenuAPI;
  iaApi.Components = Components;
  iaApi.Commands = CommandsAPI;
  iaApi.Net = {fetch: fetch2};
  iaApi.Logger = DefaultLogger;
  iaApi.Hooks = HooksAPI;
  Object.freeze(iaApi);
  Object.freeze(iaApi.Net);
  Object.freeze(iaApi.prototype);
  Object.freeze(iaApi.Components);

  // src/InAccord/index.ts
  secure_default();
  Object.defineProperty(window, "iaApi", {
    value: iaApi,
    writable: false,
    configurable: false
  });
  window.global = window;
  loadingicon_default.show();
  core_default.startup();
})();
